"""Next unsaturated diversity-catalog family after RFC 4761 VE/NLRI.

Virtual Private LAN Service Using BGP is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After vpls seals a vplsdigest
this slot is that next family: RFC 4762 Virtual Private LAN Service Using LDP PW/FEC
over an ldpsigid-gated ldpsigdigest.
The protocol hole stays open so later genesis can opt the ldpsig provider in
and seal an ldpsigdigest.
"""

from __future__ import annotations

LDPSIG_ACTUATION_ID = "capability.ldpsig-actuation"
LDPSIG_ACTUATION_DONE_WHEN = (
    f"capability_exists:{LDPSIG_ACTUATION_ID};"
    f"capability_proved:{LDPSIG_ACTUATION_ID};"
    "no_skill_route"
)
LDPSIG_ACTUATION_GOAL = (
    "Repair rfc4762 ldpsig pw/fec cycle cannot land over http ldpsig ldpsigid: hosted virtual private lan service using ldp remain unsupported so a PW then FEC ldpsigid handshake cannot land and a sealed ldpsigdigest cannot be produced. A missing ldpsig ldpsigid stays forbidden; fail-closed routing never opts the ldpsig provider in. An independent later poll of the stored ldpsigdigest keeps the hole falsifiable. Virtual Private LAN Service Using LDP sessions stay fail-closed without an ldpsigid-gated ldpsigdigest."
)
