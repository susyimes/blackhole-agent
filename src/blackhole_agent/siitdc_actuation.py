"""Next unsaturated diversity-catalog family after RFC 7757 EXPLICIT/MAPPING.

EAM is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After eam seals a eamdigest
this slot is that next family: RFC 7755 SIIT-DC Stateless IP/ICMP Translation
for IPv6 Data Center Environments PREFIX/DC over a siitdcid-gated siitdcdigest.
The protocol hole stays open so later genesis can opt the siitdc provider in
and seal a siitdcdigest.
"""

from __future__ import annotations

SIITDC_ACTUATION_ID = "capability.siitdc-actuation"
SIITDC_ACTUATION_DONE_WHEN = (
    f"capability_exists:{SIITDC_ACTUATION_ID};"
    f"capability_proved:{SIITDC_ACTUATION_ID};"
    "no_skill_route"
)
SIITDC_ACTUATION_GOAL = (
    "Repair rfc7755 siitdc prefix/dc cycle cannot land over http "
    "siitdc siitdcid: hosted datacenter translation endpoints remain unsupported "
    "so a PREFIX then DC siitdcid handshake cannot land and a sealed "
    "siitdcdigest cannot be produced. A missing siitdc siitdcid stays forbidden; "
    "fail-closed routing never opts the siitdc provider in. An independent later "
    "poll of the stored siitdcdigest keeps the hole falsifiable. Datacenter "
    "stateless translation stays fail-closed without a siitdcid-gated siitdcdigest."
)
