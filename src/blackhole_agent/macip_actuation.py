"""Next unsaturated diversity-catalog family after RFC 7623 PBB/BMAC.

Provider Backbone Bridging Combined with Ethernet VPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After pbb seals a pbbdigest
this slot is that next family: RFC 7432 BGP MPLS-Based Ethernet VPN MAC/IP
over a macipid-gated macipdigest.
The protocol hole stays open so later genesis can opt the macip provider in
and seal a macipdigest.
"""

from __future__ import annotations

MACIP_ACTUATION_ID = "capability.macip-actuation"
MACIP_ACTUATION_DONE_WHEN = (
    f"capability_exists:{MACIP_ACTUATION_ID};"
    f"capability_proved:{MACIP_ACTUATION_ID};"
    "no_skill_route"
)
MACIP_ACTUATION_GOAL = (
    "Repair rfc7432 macip mac/ip cycle cannot land over http macip macipid: hosted bgp mpls-based ethernet vpn remain unsupported so a MAC then IP macipid handshake cannot land and a sealed macipdigest cannot be produced. A missing macip macipid stays forbidden; fail-closed routing never opts the macip provider in. An independent later poll of the stored macipdigest keeps the hole falsifiable. BGP MPLS-Based Ethernet VPN sessions stay fail-closed without a macipid-gated macipdigest."
)
