"""Next unsaturated diversity-catalog family after RFC 8317 ROOT/LEAF.

Ethernet-Tree (E-Tree) Support in Ethernet VPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After etree seals a etreedigest
this slot is that next family: RFC 8365 Network Virtualization Overlay NVE/VNI
over an nvoid-gated nvodigest.
The protocol hole stays open so later genesis can opt the nvo provider in
and seal a nvodigest.
"""

from __future__ import annotations

NVO_ACTUATION_ID = "capability.nvo-actuation"
NVO_ACTUATION_DONE_WHEN = (
    f"capability_exists:{NVO_ACTUATION_ID};"
    f"capability_proved:{NVO_ACTUATION_ID};"
    "no_skill_route"
)
NVO_ACTUATION_GOAL = (
    "Repair rfc8365 nvo nve/vni cycle cannot land over http nvo nvoid: hosted network virtualization overlay endpoints remain unsupported so a NVE then VNI nvoid handshake cannot land and a sealed nvodigest cannot be produced. A missing nvo nvoid stays forbidden; fail-closed routing never opts the nvo provider in. An independent later poll of the stored nvodigest keeps the hole falsifiable. Network Virtualization Overlay sessions stay fail-closed without a nvoid-gated nvodigest."
)
