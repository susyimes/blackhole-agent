"""Hourly GitHub trend intake for a rollback-backed blackhole-agent growth loop.

This controller is adapted from the mini-swe-agent `github_growth` runner. The
important change is the local mutation engine: blackhole-agent can hand a bounded
self-improvement task to the local Codex CLI kernel, write rollback artifacts,
and apply local evolution on a prepared branch.
"""

import hashlib
import json
import os
import re
import shlex
import subprocess
import time
from dataclasses import asdict, dataclass, field
from datetime import date, datetime, timedelta, timezone
from pathlib import Path
from typing import Any

import requests
import typer
from rich.console import Console

from blackhole_agent.kernels.codex_cli import CodexCliConfig, CodexCliKernel, CodexCliRunResult
from blackhole_agent.kernels.grok_cli import GrokCliConfig, GrokCliKernel, GrokCliRunResult
from blackhole_agent.persona import render_persona_layer
from blackhole_agent.proposal_synthesis import (
    DEFAULT_PROPOSAL_MODE,
    PROPOSAL_MODES,
    ProposalSynthesisReview,
    build_proposal_evidence_package,
    render_proposal_synthesis_prompt,
    review_llm_proposal_response,
    stable_hash,
    validate_proposal_mode,
    write_context_budget_preflight_artifact,
    write_proposal_synthesis_artifacts,
)
from blackhole_agent.self_model import (
    DEFAULT_SELF_MODEL_PATH,
    SelfModelSnapshot,
    read_self_model_snapshot,
    write_self_model_snapshot,
)

_HELP_TEXT = """Discover public GitHub trends and turn them into reviewable growth proposals.

By default the command discovers recently created public repositories that are
gaining attention, then writes local digest/state files. Pass `--repos` to use a
manual repository list instead. With `--evolution-mode plan` it turns signals
into a concrete self-improvement task. With `--evolution-mode codex` it creates
a branch and asks the local Codex CLI kernel to modify this repository locally.
"""

DEFAULT_TOPICS = (
    "agent",
    "automation",
    "benchmark",
    "blackhole",
    "bug",
    "codex",
    "config",
    "github",
    "release",
    "security",
    "skill",
    "skills",
    "test",
    "tool integration",
    "workflow",
)
INTERESTING_EVENT_TYPES = {
    "IssuesEvent",
    "IssueCommentEvent",
    "PullRequestEvent",
    "PullRequestReviewCommentEvent",
    "PullRequestReviewEvent",
    "PushEvent",
    "ReleaseEvent",
    "RepositoryTrend",
    "WorkflowRunEvent",
}
OFFENSIVE_BEHAVIOR_TERMS = (
    "attack",
    "attacking",
    "attacks",
    "backdoor",
    "credential stuffing",
    "ddos",
    "denial of service",
    "exfiltrate",
    "exfiltration",
    "exploit",
    "exploitation",
    "exploiting",
    "exploits",
    "keylogger",
    "malware",
    "offensive",
    "phishing",
    "privilege escalation",
    "ransomware",
    "unauthorized access",
    "weaponize",
    "weaponized",
    "weaponizing",
)
PRIVACY_SENSITIVE_TERMS = (
    "api key",
    "auth token",
    "credential",
    "credentials",
    "password",
    "personal data",
    "pii",
    "private chat",
    "private key",
    "secret",
    "secrets",
    "token",
    "tokens",
)
PRIVACY_LEAKAGE_TERMS = (
    "dump",
    "dumping",
    "dumps",
    "exfiltrate",
    "exfiltrating",
    "exfiltration",
    "expose",
    "exposes",
    "exposing",
    "leak",
    "leaking",
    "leaks",
    "log",
    "logging",
    "logs",
    "print",
    "printed",
    "printing",
    "prints",
    "publish",
    "publishes",
    "publishing",
    "share",
    "shares",
    "sharing",
    "upload",
    "uploading",
    "uploads",
)
GOVERNANCE_CONTROL_TERMS = (
    "approval",
    "approvals",
    "budget",
    "cost",
    "govern your agents",
    "governance",
    "policies",
    "policy",
    "risky actions",
    "sandboxing",
    "spend",
    "tool access",
    "tool limit",
    "tool limits",
    "tool restriction",
    "tool restrictions",
)
AGENT_HARNESS_VALIDATION_TERMS = (
    "agent harness",
    "auditability",
    "coding agent harness",
    "eval suite",
    "eval suites",
    "evaluation report",
    "evaluation reports",
    "model-agnostic",
    "replay",
    "replayable",
    "reproducible",
    "trace",
    "traces",
    "validation harness",
)
REMOTE_EXECUTION_TERMS = (
    "cluster",
    "k8s",
    "kubeconfig",
    "kubernetes",
    "pod",
    "pods",
    "rbac",
    "runner",
    "runners",
    "sandbox",
    "sandboxes",
    "service account",
    "serviceaccount",
)
CAPABILITY_GAP_TERMS = (
    "dispatch table",
    "no handler",
    "not in local dispatch table",
    "provider",
    "search_provider",
    "tool call",
    "web_search",
)
SKILL_ROUTE_TERMS = (
    "agent skill",
    "agent skills",
    "codex skill",
    "codex skills",
    "plugin",
    "plugins",
    "skill",
    "skills",
    "tool integration",
    "tool integrations",
    "workflow gate",
    "workflow gates",
    "workflow routing",
)
DRAFT_ROLLBACK_REF = "recorded in latest-rollback-point.json when codex mode prepares the branch"
HARD_REVIEW_RISK_FLAGS = {"offensive-behavior", "privacy-leakage"}
VALIDATION_REPORT_REQUIRED_FIELDS = [
    "evidence_urls",
    "pre_adoption_risk_review",
    "local_commands",
    "startup_health_checks",
    "outcomes",
    "rollback_ref",
    "provenance",
    "skipped_capabilities",
    "runtime_capability_changes",
    "completion_requirements",
    "completion_status",
    "adoption_decision",
    "uncertainty",
]
COMPLETED_VALIDATION_OUTCOME_RESULTS = {"adopted", "pass", "passed", "reviewed"}
VALIDATION_REPORT_ADOPTION_STATES = ["draft", "incomplete", "rejected", "adoption-ready"]
REVIEW_ACTIVITY_EVENT_KINDS = {
    "PullRequestReviewCommentEvent",
    "PullRequestReviewEvent",
}
UNIT_TEST_VALIDATION_TERMS = (
    "focused test",
    "focused tests",
    "focused validation",
    "local test",
    "local tests",
    "local test lane",
    "pytest",
    "regression test",
    "regression tests",
    "snapshot test",
    "test-lane",
    "test lane",
    "test-lane probe",
    "test-lane probes",
    "unit test",
    "unit tests",
)
LOCAL_FIXTURE_VALIDATION_TERMS = (
    "local fixture",
    "local fixtures",
    "small fixture",
    "small local fixture",
    "smoke fixture",
    "smoke fixtures",
    "smoke test",
    "smoke tests",
    "validation fixture",
    "validation fixtures",
)
COVERAGE_VALIDATION_TERMS = (
    "coverage",
    "coverage validation",
    "test coverage",
)
PUSH_PATTERN_TEST_EVIDENCE_TERMS = (
    "ci",
    "coverage",
    "e2e",
    "focused test",
    "focused tests",
    "integration test",
    "integration tests",
    "pytest",
    "regression test",
    "regression tests",
    "rerun flaky",
    "smoke test",
    "smoke tests",
    "test",
    "tests",
    "unit test",
    "unit tests",
)
PUSH_PATTERN_CLUSTERS = (
    ("test-coverage", ("coverage", "e2e", "integration test", "pytest", "regression test", "smoke test", "test")),
    ("ci-guardrail", ("ci", "workflow", "security scan", "gate", "hook")),
    ("flaky-test-hardening", ("flaky", "rerun", "overflow-render")),
    ("dependency-release-guardrail", ("cooldown", "lockfile", "package-lock", "twine check", "release")),
    ("harness-runtime-reliability", ("harness", "pty", "subprocess", "daemon", "tty")),
)
BYPASS_STYLE_LABELS = {
    "bypass-ci",
    "bypass-validation",
    "ci-bypass",
    "force-merge",
    "no-verify",
    "skip-ci",
    "skip-validation",
}

app = typer.Typer(rich_markup_mode="rich", add_completion=False)
console = Console(highlight=False)


@dataclass(frozen=True)
class GitHubEvent:
    """Normalized GitHub event with only the fields the growth loop needs."""

    id: str
    repo: str
    kind: str
    actor: str
    created_at: str
    title: str
    url: str
    summary: str


@dataclass(frozen=True)
class GrowthSignal:
    """A filtered event that may be worth turning into an improvement proposal."""

    event_id: str
    repo: str
    kind: str
    title: str
    url: str
    relevance_reason: str
    risk_flags: list[str]
    recommended_action: str
    confidence: float


@dataclass
class GrowthState:
    """Cursor state for scheduled GitHub intake."""

    seen_event_ids: set[str] = field(default_factory=set)
    last_seen_at_by_repo: dict[str, str] = field(default_factory=dict)
    trend_seen_repositories: set[str] = field(default_factory=set)
    trend_last_stars_by_repo: dict[str, int] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> "GrowthState":
        return cls(
            seen_event_ids=set(data.get("seen_event_ids", [])),
            last_seen_at_by_repo=dict(data.get("last_seen_at_by_repo", {})),
            trend_seen_repositories=set(data.get("trend_seen_repositories", [])),
            trend_last_stars_by_repo={
                str(repo): int(stars) for repo, stars in dict(data.get("trend_last_stars_by_repo", {})).items()
            },
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            "seen_event_ids": sorted(self.seen_event_ids),
            "last_seen_at_by_repo": dict(sorted(self.last_seen_at_by_repo.items())),
            "trend_seen_repositories": sorted(self.trend_seen_repositories),
            "trend_last_stars_by_repo": dict(sorted(self.trend_last_stars_by_repo.items())),
        }


@dataclass
class GrowthMemory:
    """Small durable memory used to bias future trend selection."""

    version: int = 1
    repositories: dict[str, dict[str, Any]] = field(default_factory=dict)
    topics: dict[str, dict[str, Any]] = field(default_factory=dict)
    lessons: list[dict[str, Any]] = field(default_factory=list)
    theme_window: dict[str, Any] = field(default_factory=dict)

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> "GrowthMemory":
        return cls(
            version=int(data.get("version", 1)),
            repositories={str(repo): dict(stats) for repo, stats in dict(data.get("repositories", {})).items()},
            topics={str(topic): dict(stats) for topic, stats in dict(data.get("topics", {})).items()},
            lessons=[dict(lesson) for lesson in data.get("lessons", []) if isinstance(lesson, dict)],
            theme_window=dict(data.get("theme_window") or {}),
        )

    def to_dict(self) -> dict[str, Any]:
        return {
            "version": self.version,
            "repositories": dict(sorted(self.repositories.items())),
            "topics": dict(sorted(self.topics.items())),
            "lessons": self.lessons,
            "theme_window": dict(self.theme_window),
        }


@dataclass(frozen=True)
class CapabilityThemeWindow:
    """A cross-run capability focus carried into self-evolution tasks."""

    schema_version: int
    theme_id: str
    title: str
    capability_slice: str
    started_at: str
    updated_at: str
    target_passes: int
    planned_passes: int
    status: str
    proposal_ids: list[str]
    evidence_urls: list[str]
    previous_theme_id: str = ""


DEFAULT_THEME_WINDOW_TARGET_PASSES = 4

CAPABILITY_THEME_CATALOG: dict[str, dict[str, Any]] = {
    "runner-harness-control": {
        "title": "Runner and harness control plane",
        "keywords": (
            "harness",
            "runner",
            "workflow",
            "e2e",
            "workspace",
            "interrupt",
            "replay",
            "subagent",
            "tool_call",
            "tool-result",
            "policy ask",
            "approval",
            "push delivery",
        ),
        "capability_slice": (
            "Make one runner workflow legible end to end: intake, mid-flight state, recovery, replay, "
            "and report artifacts."
        ),
    },
    "provider-runtime-control": {
        "title": "Provider and runtime preflight control",
        "keywords": (
            "provider",
            "preflight",
            "config",
            "openai",
            "google",
            "claude",
            "auth",
            "permission",
            "model",
            "base url",
        ),
        "capability_slice": (
            "Turn provider and runtime configuration problems into body-free diagnostics, recovery hints, "
            "and locally replayable validation."
        ),
    },
    "skill-route-discovery": {
        "title": "Skill route discovery",
        "keywords": ("skill", "route", "discovery", "lane", "registry"),
        "capability_slice": (
            "Convert skill and route evidence into bounded local lanes that can be validated before activation."
        ),
    },
    "supervisor-activation": {
        "title": "Supervisor activation and recovery",
        "keywords": ("supervisor", "restart", "activation", "worktree", "process", "cleanup", "codex cli"),
        "capability_slice": (
            "Make supervisor wakes, candidate worktrees, promotion, push, restart, and rollback visibly recoverable."
        ),
    },
    "upstream-evidence-capability": {
        "title": "Upstream evidence to capability",
        "keywords": ("upstream", "omnigent", "release", "triage", "watchlist", "trend"),
        "capability_slice": (
            "Translate public agent-ecosystem signals into one local capability step rather than another isolated note."
        ),
    },
}

DEFAULT_CAPABILITY_THEME = {
    "title": "Controller capability slice",
    "capability_slice": (
        "Advance one coherent controller capability until it has behavior, tests, artifacts, and operator-facing docs."
    ),
}


@dataclass(frozen=True)
class TrendingRepository:
    """Repository metadata returned by GitHub repository search."""

    full_name: str
    html_url: str
    description: str
    language: str | None
    stargazers_count: int
    forks_count: int
    open_issues_count: int
    created_at: str
    updated_at: str
    pushed_at: str
    topics: list[str]

    @classmethod
    def from_search_item(cls, item: dict[str, Any]) -> "TrendingRepository":
        return cls(
            full_name=str(item.get("full_name") or ""),
            html_url=str(item.get("html_url") or ""),
            description=_compact(item.get("description") or ""),
            language=item.get("language"),
            stargazers_count=int(item.get("stargazers_count") or 0),
            forks_count=int(item.get("forks_count") or 0),
            open_issues_count=int(item.get("open_issues_count") or 0),
            created_at=str(item.get("created_at") or ""),
            updated_at=str(item.get("updated_at") or ""),
            pushed_at=str(item.get("pushed_at") or ""),
            topics=[str(topic) for topic in item.get("topics") or []],
        )


@dataclass(frozen=True)
class GitHubTrendConfig:
    """Controls the GitHub repository search used to approximate public trends."""

    query: str = ""
    window_days: int = 7
    min_stars: int = 25
    limit: int = 10
    sort: str = "stars"
    order: str = "desc"
    include_forks: bool = False


@dataclass(frozen=True)
class GitHubTrendSearchResult:
    """Repositories discovered by one trend search."""

    query: str
    sort: str
    order: str
    window_days: int
    min_stars: int
    repositories: list[TrendingRepository]
    total_count: int
    incomplete_results: bool


@dataclass(frozen=True)
class DigestWriteResult:
    """Paths written by one intake pass."""

    digest: dict[str, Any]
    json_path: Path
    markdown_path: Path
    state_path: Path
    memory_path: Path


@dataclass(frozen=True)
class SelfEvolutionPlan:
    """A coherent task that the local Codex CLI kernel can run against this checkout."""

    generated_at: str
    repo_path: str
    branch_name: str
    self_model_path: str
    self_model_before: SelfModelSnapshot
    task: str
    proposals: list[dict[str, Any]]
    source_digest_id: str
    source_digest_generated_at: str
    capability_theme_window: dict[str, Any] = field(default_factory=dict)
    upstream_evidence_capability_step: dict[str, Any] = field(default_factory=dict)
    skill_route_discovery_capability_pipeline: dict[str, Any] = field(default_factory=dict)


@dataclass(frozen=True)
class SelfEvolutionRunResult:
    """Result of invoking the Codex CLI kernel on a self-evolution task."""

    command: list[str]
    returncode: int
    task_path: Path
    last_message_path: Path
    result_path: Path
    branch_name: str
    stdout_tail: str
    stderr_tail: str
    last_message: str


@dataclass(frozen=True)
class RollbackPoint:
    """A local recovery anchor captured before a self-evolution run mutates code."""

    created_at: str
    repo_path: str
    original_branch: str
    original_head: str
    rollback_ref: str
    status_porcelain: str
    restore_commands: list[list[str]]


class GitHubEventsClient:
    """Small GitHub REST client for repository trend search and event feeds."""

    def __init__(
        self,
        *,
        token: str | None = None,
        session: requests.Session | None = None,
        api_base_url: str = "https://api.github.com",
        timeout: int = 30,
    ) -> None:
        self._session = session or requests.Session()
        self._api_base_url = api_base_url.rstrip("/")
        self._timeout = timeout
        self._headers = {
            "Accept": "application/vnd.github+json",
            "User-Agent": "blackhole-agent-github-growth",
            "X-GitHub-Api-Version": "2022-11-28",
        }
        if token:
            self._headers["Authorization"] = f"Bearer {token}"

    def list_repository_events(self, repo: str, *, per_page: int = 100) -> list[dict[str, Any]]:
        if per_page < 1 or per_page > 100:
            raise ValueError("per_page must be between 1 and 100")
        owner, name = parse_repo_spec(repo)
        url: str | None = f"{self._api_base_url}/repos/{owner}/{name}/events"
        params: dict[str, int] | None = {"per_page": per_page}
        events: list[dict[str, Any]] = []
        while url:
            response = self._session.get(
                url,
                headers=self._headers,
                params=params,
                timeout=self._timeout,
            )
            if response.status_code >= 400:
                raise RuntimeError(f"GitHub events request failed for {repo}: HTTP {response.status_code}")
            payload = response.json()
            if not isinstance(payload, list):
                raise RuntimeError(f"GitHub events request failed for {repo}: expected a JSON list")
            events.extend(payload)
            url = (getattr(response, "links", {}) or {}).get("next", {}).get("url")
            params = None
        return events

    def search_trending_repositories(
        self,
        config: GitHubTrendConfig,
        *,
        now: datetime | None = None,
    ) -> GitHubTrendSearchResult:
        if config.limit < 1 or config.limit > 100:
            raise ValueError("trend limit must be between 1 and 100")
        if config.window_days < 1:
            raise ValueError("trend window days must be at least 1")
        if config.min_stars < 0:
            raise ValueError("trend min stars cannot be negative")
        if config.sort not in {"stars", "forks", "updated"}:
            raise ValueError("trend sort must be one of: stars, forks, updated")
        if config.order not in {"asc", "desc"}:
            raise ValueError("trend order must be one of: asc, desc")

        query = build_trending_repository_query(config, now=now)
        response = self._session.get(
            f"{self._api_base_url}/search/repositories",
            headers=self._headers,
            params={
                "q": query,
                "sort": config.sort,
                "order": config.order,
                "per_page": config.limit,
            },
            timeout=self._timeout,
        )
        if response.status_code >= 400:
            raise RuntimeError(f"GitHub trend search failed: HTTP {response.status_code}")
        payload = response.json()
        if not isinstance(payload, dict) or not isinstance(payload.get("items"), list):
            raise RuntimeError("GitHub trend search failed: expected a JSON object with an items list")
        repositories = [TrendingRepository.from_search_item(item) for item in payload["items"]]
        repositories = [repo for repo in repositories if repo.full_name]
        return GitHubTrendSearchResult(
            query=query,
            sort=config.sort,
            order=config.order,
            window_days=config.window_days,
            min_stars=config.min_stars,
            repositories=repositories,
            total_count=int(payload.get("total_count") or 0),
            incomplete_results=bool(payload.get("incomplete_results") or False),
        )


def build_trending_repository_query(config: GitHubTrendConfig, *, now: datetime | None = None) -> str:
    now = now or datetime.now(timezone.utc)
    created_since = (now - timedelta(days=config.window_days)).date()
    return build_trending_repository_query_for_date(config, created_since=created_since)


def build_trending_repository_query_for_date(config: GitHubTrendConfig, *, created_since: date) -> str:
    terms = [config.query.strip()] if config.query.strip() else []
    terms.append(f"created:>={created_since.isoformat()}")
    if config.min_stars > 0:
        terms.append(f"stars:>={config.min_stars}")
    if not config.include_forks:
        terms.append("fork:false")
    return " ".join(terms)


def parse_repo_spec(repo: str) -> tuple[str, str]:
    parts = [part.strip() for part in repo.split("/") if part.strip()]
    if len(parts) != 2:
        raise ValueError(f"Repository must be in owner/name form: {repo}")
    return parts[0], parts[1]


def parse_comma_separated(value: str) -> list[str]:
    return [part.strip() for part in value.split(",") if part.strip()]


def load_state(path: Path) -> GrowthState:
    if not path.exists():
        return GrowthState()
    return GrowthState.from_dict(json.loads(path.read_text(encoding="utf-8")))


def save_state(path: Path, state: GrowthState) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(state.to_dict(), indent=2, sort_keys=True) + "\n", encoding="utf-8")


def load_memory(path: Path) -> GrowthMemory:
    if not path.exists():
        return GrowthMemory()
    return GrowthMemory.from_dict(json.loads(path.read_text(encoding="utf-8")))


def save_memory(path: Path, memory: GrowthMemory) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(memory.to_dict(), indent=2, sort_keys=True) + "\n", encoding="utf-8")


def update_memory_from_digest(memory: GrowthMemory, digest: dict[str, Any], *, lesson_limit: int = 200) -> None:
    generated_at = str(digest.get("generated_at") or "")
    for repo in digest.get("repositories", []):
        touch_memory_stats(memory.repositories, str(repo), generated_at=generated_at)
    for item in digest.get("items", []):
        repo = repo_from_digest_summary(str(item.get("summary") or ""))
        if repo:
            touch_memory_stats(memory.repositories, repo, generated_at=generated_at, useful_increment=1)
        for topic in topics_from_relevance(str(item.get("relevance_reason") or "")):
            touch_memory_stats(memory.topics, topic, generated_at=generated_at, useful_increment=1)
    existing_lesson_ids = {str(lesson.get("lesson_id")) for lesson in memory.lessons}
    for proposal in digest.get("proposals", []):
        proposal_id = str(proposal.get("proposal_id") or "")
        if not proposal_id or proposal_id in existing_lesson_ids:
            continue
        memory.lessons.append(
            {
                "lesson_id": proposal_id,
                "source_digest_id": digest.get("digest_id", ""),
                "generated_at": generated_at,
                "kind": proposal.get("kind", "no_action"),
                "summary": proposal.get("summary", ""),
                "evidence_urls": proposal.get("evidence_urls", []),
                "outcome": "proposed",
                "confidence": proposal.get("confidence"),
            }
        )
        existing_lesson_ids.add(proposal_id)
    if len(memory.lessons) > lesson_limit:
        memory.lessons = memory.lessons[-lesson_limit:]
    memory.theme_window = build_capability_theme_window(
        digest.get("proposals", []),
        previous_window=memory.theme_window,
        generated_at=generated_at,
    )
    if memory.theme_window:
        digest["capability_theme_window"] = dict(memory.theme_window)
    attach_upstream_evidence_capability_step(digest)
    attach_skill_route_discovery_capability_pipeline(digest)


def build_capability_theme_window(
    proposals: list[dict[str, Any]],
    *,
    previous_window: dict[str, Any] | None = None,
    generated_at: str = "",
    target_passes: int = DEFAULT_THEME_WINDOW_TARGET_PASSES,
) -> dict[str, Any]:
    """Return the active cross-run capability theme for this planning pass."""

    proposal_ids = bounded_unique_strings(proposal.get("proposal_id") for proposal in proposals)
    evidence_urls = bounded_unique_strings(
        url for proposal in proposals for url in proposal.get("evidence_urls", []) if str(url).strip()
    )
    now = generated_at or datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    previous = previous_window or {}
    previous_status = str(previous.get("status") or "")
    previous_theme_id = str(previous.get("theme_id") or "")
    previous_target = int(previous.get("target_passes") or target_passes)
    previous_passes = int(previous.get("planned_passes") or 0)

    if previous_theme_id and previous_status == "active" and previous_passes < previous_target:
        catalog_entry = CAPABILITY_THEME_CATALOG.get(previous_theme_id, DEFAULT_CAPABILITY_THEME)
        title = str(previous.get("title") or catalog_entry["title"])
        capability_slice = str(previous.get("capability_slice") or catalog_entry["capability_slice"])
        merged_proposal_ids = bounded_unique_strings([*previous.get("proposal_ids", []), *proposal_ids])
        merged_evidence_urls = bounded_unique_strings([*previous.get("evidence_urls", []), *evidence_urls])
        return asdict(
            CapabilityThemeWindow(
                schema_version=1,
                theme_id=previous_theme_id,
                title=title,
                capability_slice=capability_slice,
                started_at=str(previous.get("started_at") or now),
                updated_at=now,
                target_passes=previous_target,
                planned_passes=previous_passes + 1,
                status="active" if previous_passes + 1 < previous_target else "complete",
                proposal_ids=merged_proposal_ids,
                evidence_urls=merged_evidence_urls,
                previous_theme_id=str(previous.get("previous_theme_id") or ""),
            )
        )

    theme_id = classify_capability_theme(proposals)
    catalog_entry = CAPABILITY_THEME_CATALOG.get(theme_id, DEFAULT_CAPABILITY_THEME)
    return asdict(
        CapabilityThemeWindow(
            schema_version=1,
            theme_id=theme_id,
            title=str(catalog_entry["title"]),
            capability_slice=str(catalog_entry["capability_slice"]),
            started_at=now,
            updated_at=now,
            target_passes=target_passes,
            planned_passes=1,
            status="active" if target_passes > 1 else "complete",
            proposal_ids=proposal_ids,
            evidence_urls=evidence_urls,
            previous_theme_id=previous_theme_id,
        )
    )


def classify_capability_theme(proposals: list[dict[str, Any]]) -> str:
    """Classify proposals into a durable capability theme without calling an LLM."""

    text = " ".join(
        " ".join(
            [
                str(proposal.get("proposal_id") or ""),
                str(proposal.get("kind") or ""),
                str(proposal.get("summary") or ""),
                str(proposal.get("implementation_scope") or ""),
                str(proposal.get("validation_gate") or ""),
                str(proposal.get("validation_task") or ""),
            ]
        ).lower()
        for proposal in proposals
    )
    scores: dict[str, int] = {}
    for theme_id, config in CAPABILITY_THEME_CATALOG.items():
        scores[theme_id] = sum(1 for keyword in config["keywords"] if keyword in text)
    best_theme_id, best_score = max(scores.items(), key=lambda item: item[1])
    if best_score <= 0:
        return "controller-capability-slice"
    return best_theme_id


def bounded_unique_strings(values: Any, *, limit: int = 12) -> list[str]:
    """Return stable, bounded strings for replay artifacts."""

    unique: list[str] = []
    seen: set[str] = set()
    for value in values:
        text = str(value or "").strip()
        if not text or text in seen:
            continue
        unique.append(text)
        seen.add(text)
        if len(unique) >= limit:
            break
    return unique


def touch_memory_stats(
    table: dict[str, dict[str, Any]],
    key: str,
    *,
    generated_at: str,
    useful_increment: int = 0,
) -> None:
    stats = table.setdefault(
        key,
        {
            "seen": 0,
            "useful_signals": 0,
            "validated": 0,
            "failed": 0,
            "last_seen_at": "",
        },
    )
    stats["seen"] = int(stats.get("seen") or 0) + 1
    stats["useful_signals"] = int(stats.get("useful_signals") or 0) + useful_increment
    stats["validated"] = int(stats.get("validated") or 0)
    stats["failed"] = int(stats.get("failed") or 0)
    stats["last_seen_at"] = generated_at


def repo_from_digest_summary(summary: str) -> str:
    if ": " not in summary:
        return ""
    return summary.split(": ", 1)[0].strip()


def topics_from_relevance(relevance_reason: str) -> list[str]:
    prefix = "matched topics: "
    if not relevance_reason.startswith(prefix):
        return []
    return [topic.strip() for topic in relevance_reason.removeprefix(prefix).split(",") if topic.strip()]


def parse_github_timestamp(value: str) -> datetime:
    if not value:
        return datetime.min.replace(tzinfo=timezone.utc)
    parsed = datetime.fromisoformat(value.replace("Z", "+00:00"))
    if parsed.tzinfo is None:
        return parsed.replace(tzinfo=timezone.utc)
    return parsed


def normalize_event(repo: str, raw: dict[str, Any]) -> GitHubEvent:
    payload = raw.get("payload") or {}
    kind = str(raw.get("type") or "UnknownEvent")
    actor = (raw.get("actor") or {}).get("login") or ""
    event_id = str(raw.get("id") or f"{repo}:{kind}:{raw.get('created_at', '')}")
    created_at = str(raw.get("created_at") or "")

    title, url, summary = _event_text(repo, kind, payload)
    return GitHubEvent(
        id=event_id,
        repo=repo,
        kind=kind,
        actor=actor,
        created_at=created_at,
        title=title,
        url=url,
        summary=summary,
    )


def select_new_events(
    repo: str,
    raw_events: list[dict[str, Any]],
    state: GrowthState,
    *,
    lookback_hours: int,
    max_events: int | None = None,
) -> list[GitHubEvent]:
    newest_seen_at = state.last_seen_at_by_repo.get(repo)
    if newest_seen_at:
        since = parse_github_timestamp(newest_seen_at)
    else:
        since = datetime.now(timezone.utc) - timedelta(hours=lookback_hours)

    selected: list[GitHubEvent] = []
    for raw in raw_events:
        event = normalize_event(repo, raw)
        created_at = parse_github_timestamp(event.created_at)
        if event.id in state.seen_event_ids or created_at < since:
            continue
        selected.append(event)
        if max_events is not None and len(selected) >= max_events:
            break
    return selected


def update_state(state: GrowthState, events: list[GitHubEvent]) -> None:
    for event in events:
        state.seen_event_ids.add(event.id)
        current = state.last_seen_at_by_repo.get(event.repo)
        if current is None or parse_github_timestamp(event.created_at) > parse_github_timestamp(current):
            state.last_seen_at_by_repo[event.repo] = event.created_at


def update_trend_state(state: GrowthState, repositories: list[TrendingRepository]) -> None:
    for repository in repositories:
        state.trend_seen_repositories.add(repository.full_name)
        state.trend_last_stars_by_repo[repository.full_name] = repository.stargazers_count


def trend_repository_to_event(
    repository: TrendingRepository,
    *,
    generated_at: str,
    previous_stars: int | None = None,
    first_seen: bool = True,
) -> GitHubEvent:
    language = repository.language or "unknown language"
    topics = ", ".join(repository.topics[:6]) if repository.topics else "none"
    if previous_stars is None:
        star_delta = "unknown"
    else:
        star_delta = str(repository.stargazers_count - previous_stars)
    title = (
        f"trending repository: {repository.full_name} "
        f"({repository.stargazers_count} stars, {language})"
    )
    summary = (
        f"description={repository.description or 'none'}; "
        f"stars={repository.stargazers_count}; forks={repository.forks_count}; "
        f"open_issues={repository.open_issues_count}; star_delta_since_last_run={star_delta}; "
        f"first_seen={first_seen}; created_at={repository.created_at}; pushed_at={repository.pushed_at}; "
        f"topics={topics}"
    )
    return GitHubEvent(
        id=f"trend:{repository.full_name}",
        repo=repository.full_name,
        kind="RepositoryTrend",
        actor=repository.full_name.split("/", 1)[0],
        created_at=generated_at,
        title=title,
        url=repository.html_url or _repo_url(repository.full_name),
        summary=summary,
    )


def build_trend_events(
    repositories: list[TrendingRepository],
    *,
    state: GrowthState,
    generated_at: str,
) -> list[GitHubEvent]:
    return [
        trend_repository_to_event(
            repository,
            generated_at=generated_at,
            previous_stars=state.trend_last_stars_by_repo.get(repository.full_name),
            first_seen=repository.full_name not in state.trend_seen_repositories,
        )
        for repository in repositories
    ]


def build_trend_source_metadata(result: GitHubTrendSearchResult, *, state: GrowthState) -> dict[str, Any]:
    repositories: list[dict[str, Any]] = []
    for repository in result.repositories:
        previous_stars = state.trend_last_stars_by_repo.get(repository.full_name)
        repositories.append(
            {
                "full_name": repository.full_name,
                "html_url": repository.html_url,
                "description": repository.description,
                "language": repository.language,
                "stargazers_count": repository.stargazers_count,
                "forks_count": repository.forks_count,
                "open_issues_count": repository.open_issues_count,
                "star_delta_since_last_run": None
                if previous_stars is None
                else repository.stargazers_count - previous_stars,
                "first_seen": repository.full_name not in state.trend_seen_repositories,
                "created_at": repository.created_at,
                "updated_at": repository.updated_at,
                "pushed_at": repository.pushed_at,
                "topics": repository.topics,
            }
        )
    return {
        "kind": "github_trending_repositories",
        "query": result.query,
        "sort": result.sort,
        "order": result.order,
        "window_days": result.window_days,
        "min_stars": result.min_stars,
        "total_count": result.total_count,
        "incomplete_results": result.incomplete_results,
        "repositories": repositories,
    }


def extract_growth_signals(events: list[GitHubEvent], *, topics: list[str]) -> list[GrowthSignal]:
    topic_terms = [topic.lower() for topic in topics]
    signals: list[GrowthSignal] = []
    for event in events:
        haystack = f"{event.kind} {event.title} {event.summary}".lower()
        matched_topics = [topic for topic in topic_terms if topic in haystack]
        if event.kind not in INTERESTING_EVENT_TYPES and not matched_topics:
            continue
        risk_flags = detect_risk_flags(haystack)
        if matched_topics:
            relevance = "matched topics: " + ", ".join(sorted(set(matched_topics)))
            confidence = 0.82
        else:
            relevance = f"{event.kind} is useful for observing upstream project movement"
            confidence = 0.62
        signals.append(
            GrowthSignal(
                event_id=event.id,
                repo=event.repo,
                kind=event.kind,
                title=event.title,
                url=event.url,
                relevance_reason=relevance,
                risk_flags=risk_flags,
                recommended_action=recommend_action(event, risk_flags),
                confidence=confidence,
            )
        )
    return signals


def detect_risk_flags(haystack: str) -> list[str]:
    flags: set[str] = set()
    if any(contains_risk_term(haystack, term) for term in OFFENSIVE_BEHAVIOR_TERMS):
        flags.add("offensive-behavior")
    if contains_privacy_leakage_signal(haystack):
        flags.add("privacy-leakage")
    return sorted(flags)


def contains_risk_term(haystack: str, term: str) -> bool:
    return re.search(rf"\b{re.escape(term)}\b", haystack) is not None


def is_capability_gap_signal(haystack: str) -> bool:
    if "not in local dispatch table" in haystack:
        return True
    matched_terms = [term for term in CAPABILITY_GAP_TERMS if contains_risk_term(haystack, term)]
    return "web_search" in matched_terms and bool({"dispatch table", "no handler", "provider"} & set(matched_terms))


def contains_privacy_leakage_signal(haystack: str) -> bool:
    has_sensitive_subject = any(contains_risk_term(haystack, term) for term in PRIVACY_SENSITIVE_TERMS)
    has_leakage_action = any(contains_risk_term(haystack, term) for term in PRIVACY_LEAKAGE_TERMS)
    return has_sensitive_subject and has_leakage_action


def recommend_action(event: GitHubEvent, risk_flags: list[str]) -> str:
    if "offensive-behavior" in risk_flags:
        return "record the offensive-behavior boundary and keep the route review-only"
    if "privacy-leakage" in risk_flags:
        return "record the privacy-leakage boundary and keep sensitive data out of artifacts and runtime changes"
    if risk_flags:
        return "summarize the risk pattern and keep the route reviewable"
    if any(contains_risk_term(f"{event.kind} {event.title} {event.summary}".lower(), term) for term in GOVERNANCE_CONTROL_TERMS):
        return "adapt the governance pattern freely when it improves local controller behavior without offensive use or privacy leakage"
    if is_capability_gap_signal(f"{event.kind} {event.title} {event.summary}".lower()):
        return "adapt the capability route freely when local validation can cover it"
    if any(
        contains_risk_term(f"{event.kind} {event.title} {event.summary}".lower(), term)
        for term in SKILL_ROUTE_TERMS
    ):
        return "map skill, workflow, or tool-integration signals to bounded local validation lanes such as documentation, config, tests, or code patches"
    if any(contains_risk_term(f"{event.kind} {event.title} {event.summary}".lower(), term) for term in REMOTE_EXECUTION_TERMS):
        return "adapt the runner or remote-execution pattern freely when configured capabilities and validation support it"
    if any(contains_risk_term(f"{event.kind} {event.title} {event.summary}".lower(), term) for term in AGENT_HARNESS_VALIDATION_TERMS):
        return "adapt the harness pattern into local behavior, tests, or reports without treating validation as the only outcome"
    if event.kind == "RepositoryTrend":
        return "review the repository for reusable patterns and turn only one concrete lesson into a validation task"
    if event.kind == "ReleaseEvent":
        return "review release notes for reusable implementation or workflow changes"
    if event.kind == "PullRequestEvent":
        return "compare the pull request approach with local agent behavior before drafting a change"
    if event.kind in REVIEW_ACTIVITY_EVENT_KINDS:
        return "treat repeated pull request review activity as supporting evidence for local validation or test changes"
    if event.kind == "PushEvent":
        return "cluster commit messages and keep only patterns with clear test evidence"
    if event.kind in {"IssuesEvent", "IssueCommentEvent"}:
        return "turn the issue signal into a focused hypothesis and validation checklist"
    return "capture the lesson as a proposal; do not mutate the project automatically"


def build_digest(
    repos: list[str],
    signals: list[GrowthSignal],
    *,
    state: GrowthState,
    generated_at: str | None = None,
    source: dict[str, Any] | None = None,
    memory: GrowthMemory | None = None,
    proposals: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    generated_at = generated_at or datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    digest_id = "github-growth-" + generated_at.replace("-", "").replace(":", "").replace("+00:00", "Z")
    digest_proposals = build_proposals(signals, memory=memory) if proposals is None else proposals
    items = [
        {
            "item_id": signal.event_id,
            "source_url": signal.url,
            "event_kind": signal.kind,
            "summary": f"{signal.repo}: {signal.title}",
            "relevance_reason": signal.relevance_reason,
            "risk_flags": signal.risk_flags,
            "confidence": signal.confidence,
        }
        for signal in signals
    ]
    digest = {
        "digest_id": digest_id,
        "generated_at": generated_at,
        "repositories": repos,
        "cursor": dict(sorted(state.last_seen_at_by_repo.items())),
        "items": items,
        "proposals": digest_proposals,
    }
    upstream_triage = build_upstream_movement_triage(items)
    if upstream_triage["clusters"]:
        digest["upstream_movement_triage"] = upstream_triage
    if source is not None:
        digest["source"] = source
    attach_upstream_evidence_capability_step(digest)
    attach_skill_route_discovery_capability_pipeline(digest)
    return digest


def build_proposals(
    signals: list[GrowthSignal],
    *,
    memory: GrowthMemory | None = None,
    limit: int = 5,
) -> list[dict[str, Any]]:
    proposals: list[dict[str, Any]] = []
    ranked_signals = rank_signals_with_memory(signals, memory=memory)
    for index, signal in enumerate(ranked_signals[:limit], start=1):
        proposal = {
            "proposal_id": f"{signal.event_id}-{index}",
            "proposal_source": "heuristic",
            "kind": classify_proposal_kind(signal),
            "summary": f"Borrow cautiously from {signal.repo}: {signal.title}. {signal.recommended_action}.",
            "evidence_urls": [signal.url] if signal.url else [],
            "risk_flags": signal.risk_flags,
            "implementation_scope": implementation_scope_for_signal(signal),
            "validation_gate": validation_gate_for_signal(signal),
            "validation_task": validation_task_for_signal(signal),
            "requires_approval": False,
        }
        pattern_evidence = push_pattern_evidence_for_signal(signal)
        if pattern_evidence is not None:
            proposal["push_pattern_evidence"] = pattern_evidence
        upstream_evidence = upstream_movement_evidence_for_signal(signal)
        if upstream_evidence is not None:
            proposal["upstream_movement_evidence"] = upstream_evidence
        proposals.append(proposal)
    return proposals


def synthesize_digest_proposals(
    digest: dict[str, Any],
    signals: list[GrowthSignal],
    heuristic_proposals: list[dict[str, Any]],
    *,
    mode: str,
    output_dir: Path,
    repo_path: Path,
    self_model_path: Path | None = None,
    model: str | None = None,
    profile: str | None = None,
    kernel: str = "codex",
    ignore_user_config: bool = True,
    timeout_seconds: int = 180,
    command_runner: Any = subprocess.run,
    limit: int = 5,
) -> list[dict[str, Any]]:
    """Optionally ask a read-only LLM to interpret digest items before proposals are finalized."""

    normalized_mode = validate_proposal_mode(mode)
    if normalized_mode == "heuristic":
        return heuristic_proposals

    self_model_snapshot = read_self_model_snapshot(repo_path, self_model_path)
    evidence_package = build_proposal_evidence_package(
        digest,
        self_model_snapshot=self_model_snapshot.to_dict(),
        max_items=max(20, limit * 4),
    )
    write_context_budget_preflight_artifact(output_dir, evidence_package=evidence_package)
    try:
        raw_text = run_proposal_interpretation_kernel(
            evidence_package,
            output_dir=output_dir,
            repo_path=repo_path,
            model=model,
            profile=profile,
            kernel=kernel,
            ignore_user_config=ignore_user_config,
            timeout_seconds=timeout_seconds,
            command_runner=command_runner,
        )
        review = review_llm_proposal_response(raw_text, evidence_package, mode=normalized_mode)
    except Exception as error:
        review = ProposalSynthesisReview(
            schema_version=1,
            mode=normalized_mode,
            status="rejected",
            reason=f"proposal interpretation failed: {error}",
            input_digest_id=str(evidence_package.get("digest_id") or ""),
            input_hash=stable_hash(evidence_package),
            output_hash="",
            accepted_count=0,
            rejected_count=0,
            accepted_candidates=[],
            rejected_candidates=[],
            interpretation={},
            self_model_reading={},
        )
    write_proposal_synthesis_artifacts(output_dir, evidence_package=evidence_package, review=review)
    if review.status != "accepted":
        return heuristic_proposals

    llm_proposals = clamp_llm_candidates_to_proposals(review.accepted_candidates, signals, limit=limit)
    if not llm_proposals:
        return heuristic_proposals
    if normalized_mode == "llm":
        return llm_proposals
    return combine_llm_and_heuristic_proposals(llm_proposals, heuristic_proposals, limit=limit)


def run_proposal_interpretation_kernel(
    evidence_package: dict[str, Any],
    *,
    output_dir: Path,
    repo_path: Path,
    model: str | None = None,
    profile: str | None = None,
    kernel: str = "codex",
    ignore_user_config: bool = True,
    timeout_seconds: int = 180,
    command_runner: Any = subprocess.run,
) -> str:
    """Run the selected CLI as a read-only interpretation kernel."""

    if kernel == "grok":
        grok = GrokCliKernel(
            GrokCliConfig(
                model=model,
                sandbox="read-only",
                permission_mode="dontAsk",
            ),
            command_runner=command_runner,
        )
        result = grok.run(
            render_proposal_synthesis_prompt(evidence_package),
            cwd=repo_path,
            output_dir=output_dir / "proposal-synthesis",
            timeout_seconds=timeout_seconds,
        )
        return result.last_message
    if kernel != "codex":
        raise ValueError("kernel must be one of: codex, grok")

    config = CodexCliConfig(
        model=model,
        profile=profile,
        sandbox="read-only",
        approval_policy="never",
        ignore_user_config=ignore_user_config,
        bypass_approvals_and_sandbox=False,
    )
    codex = CodexCliKernel(config, command_runner=command_runner)
    result = codex.run(
        render_proposal_synthesis_prompt(evidence_package),
        cwd=repo_path,
        output_dir=output_dir / "proposal-synthesis",
        timeout_seconds=timeout_seconds,
    )
    return result.last_message


def clamp_llm_candidates_to_proposals(
    candidates: list[dict[str, Any]],
    signals: list[GrowthSignal],
    *,
    limit: int = 5,
) -> list[dict[str, Any]]:
    signals_by_id = {signal.event_id: signal for signal in signals}
    proposals: list[dict[str, Any]] = []
    for index, candidate in enumerate(candidates[:limit], start=1):
        referenced = [signals_by_id[ref] for ref in candidate.get("evidence_refs", []) if ref in signals_by_id]
        if not referenced:
            continue
        risk_flags = sorted(
            {
                *[flag for signal in referenced for flag in signal.risk_flags],
                *[
                    normalized_flag
                    for flag in candidate.get("added_risk_flags", [])
                    if (normalized_flag := str(flag).strip()) in HARD_REVIEW_RISK_FLAGS
                ],
                *detect_risk_flags(candidate_safety_text(candidate)),
            }
        )
        representative = GrowthSignal(
            event_id=str(candidate.get("proposal_id") or f"llm-{index}"),
            repo=referenced[0].repo,
            kind=referenced[0].kind,
            title=str(candidate.get("summary") or referenced[0].title),
            url=referenced[0].url,
            relevance_reason="LLM interpretation over frozen digest evidence",
            risk_flags=risk_flags,
            recommended_action=str(candidate.get("validation_task") or "validate the interpreted route locally"),
            confidence=min(max(float(referenced[0].confidence), 0.0), 1.0),
        )
        kind = classify_proposal_kind(representative) if risk_flags else str(candidate.get("kind") or "test")
        validation_task = (
            validation_task_for_signal(representative, route=candidate)
            if risk_flags
            else str(candidate.get("validation_task") or validation_task_for_signal(representative))
        )
        proposal = {
            "proposal_id": str(candidate.get("proposal_id") or f"llm-{index}"),
            "proposal_source": "llm_interpretation",
            "kind": kind,
            "summary": str(candidate.get("summary") or representative.title),
            "evidence_refs": [str(ref) for ref in candidate.get("evidence_refs", [])],
            "evidence_urls": candidate.get("evidence_urls") or [signal.url for signal in referenced if signal.url],
            "risk_flags": risk_flags,
            "implementation_scope": implementation_scope_for_signal(representative, route=candidate),
            "validation_gate": validation_gate_for_signal(representative, route=candidate),
            "validation_task": validation_task,
            "requires_approval": False,
            "rationale": str(candidate.get("rationale") or ""),
            "uncertainty": str(candidate.get("uncertainty") or ""),
            "self_effect": str(candidate.get("self_effect") or ""),
            "action_lane": str(candidate.get("action_lane") or ""),
        }
        pattern_evidence = push_pattern_evidence_for_signals(referenced)
        if pattern_evidence is not None:
            proposal["push_pattern_evidence"] = pattern_evidence
        proposals.append(proposal)
    return proposals


def push_pattern_evidence_for_signals(signals: list[GrowthSignal]) -> dict[str, Any] | None:
    """Merge push-pattern evidence from cited signals for interpreted proposals."""

    evidences = [
        evidence
        for signal in signals
        if (evidence := push_pattern_evidence_for_signal(signal)) is not None
    ]
    if not evidences:
        return None
    clusters = sorted(
        {
            str(cluster)
            for evidence in evidences
            for cluster in evidence.get("clusters", [])
            if str(cluster).strip()
        }
    )
    has_clear_test_evidence = all(bool(evidence.get("has_clear_test_evidence")) for evidence in evidences)
    matched_text_hash = stable_push_message_hash(
        json.dumps([evidence.get("matched_text_hash", "") for evidence in evidences], sort_keys=True)
    )
    return {
        "status": "ready" if clusters and has_clear_test_evidence else "evidence_gap",
        "clusters": clusters,
        "has_clear_test_evidence": has_clear_test_evidence,
        "source": "push_commit_message_cluster",
        "policy": "keep_push_patterns_only_when_commit_messages_include_clear_test_or_ci_evidence",
        "matched_text_hash": matched_text_hash,
    }


def candidate_safety_text(candidate: dict[str, Any]) -> str:
    """Return LLM-authored proposal text that can independently trigger safety review."""

    return " ".join(
        str(candidate.get(key) or "")
        for key in ("summary", "validation_task", "rationale", "uncertainty", "self_effect", "action_lane")
    ).lower()


def combine_llm_and_heuristic_proposals(
    llm_proposals: list[dict[str, Any]],
    heuristic_proposals: list[dict[str, Any]],
    *,
    limit: int = 5,
) -> list[dict[str, Any]]:
    combined: list[dict[str, Any]] = []
    seen: set[tuple[str, str]] = set()
    for proposal in [*llm_proposals, *heuristic_proposals]:
        evidence_key = ",".join(sorted(str(url) for url in proposal.get("evidence_urls", [])))
        key = (str(proposal.get("kind") or ""), evidence_key)
        if key in seen:
            continue
        seen.add(key)
        combined.append(proposal)
        if len(combined) >= limit:
            break
    return combined


def implementation_scope_for_signal(signal: GrowthSignal, *, route: dict[str, Any] | None = None) -> str:
    if safety_boundary_risk(signal.risk_flags):
        return "reviewable_proposal_only"
    if signal.risk_flags:
        return "risk_review_before_local_change"
    return "local_validation_candidate"


def validation_gate_for_signal(signal: GrowthSignal, *, route: dict[str, Any] | None = None) -> str:
    if "offensive-behavior" in signal.risk_flags:
        return "offensive-behavior-human-review"
    if "privacy-leakage" in signal.risk_flags:
        return "privacy-leakage-human-review"
    if signal.risk_flags:
        return "rollback-backed-risk-review"
    if signal.kind == "RepositoryTrend":
        return "focused-evidence-review"
    return "narrow-local-verification"


def validation_task_for_signal(signal: GrowthSignal, *, route: dict[str, Any] | None = None) -> str:
    if "offensive-behavior" in signal.risk_flags:
        return (
            "Keep offensive or abuse-enabling behavior review-only; do not implement attack, exploit, malware, "
            "phishing, exfiltration, or unauthorized-access behavior."
        )
    if "privacy-leakage" in signal.risk_flags:
        return (
            "Keep privacy-leakage behavior review-only; do not expose, log, print, upload, publish, or share "
            "tokens, credentials, secrets, private keys, private chats, PII, or personal data."
        )
    if signal.risk_flags:
        return (
            "Summarize the risk in a local artifact or test fixture and verify the change stays rollback-backed "
            "without expanding runtime capabilities."
        )
    if signal.kind == "RepositoryTrend":
        return "Review the evidence URL, extract one reusable pattern, and verify the local change with a focused test."
    return "Verify the proposed lesson with a local test or documentation check sized to the changed behavior."


def safety_boundary_risk(risk_flags: list[str]) -> bool:
    return bool({str(flag) for flag in risk_flags} & HARD_REVIEW_RISK_FLAGS)


def autonomous_local_apply_text(proposal: dict[str, Any]) -> str:
    """Describe whether a proposal may directly change local behavior."""

    if proposal.get("requires_approval", True):
        return "False"
    if bypass_style_labels(proposal):
        return "False (bypass-style labels are ignored and require review)"
    implementation_scope = str(proposal.get("implementation_scope") or "").strip()
    if implementation_scope == "reviewable_proposal_only":
        return "False (reviewable proposal only; local validation artifacts may still be updated)"
    return "True"


def proposal_validation_preflight(proposal: dict[str, Any]) -> dict[str, Any]:
    """Classify test/coverage validation strength without expanding the safety boundary."""

    implementation_scope = str(proposal.get("implementation_scope") or "").strip()
    validation_text = " ".join(
        str(proposal.get(key) or "")
        for key in ("summary", "kind", "validation_gate", "validation_task", "rationale", "self_effect")
    ).lower()
    has_unit_test_signal = any(
        term in validation_text for term in (*UNIT_TEST_VALIDATION_TERMS, *LOCAL_FIXTURE_VALIDATION_TERMS)
    )
    has_coverage_signal = any(term in validation_text for term in COVERAGE_VALIDATION_TERMS)
    requires_test_or_coverage = implementation_scope == "local_validation_candidate"
    validation_gaps: list[str] = []
    if requires_test_or_coverage and not (has_unit_test_signal or has_coverage_signal):
        validation_gaps.append("missing_unit_test_or_coverage_validation")
    push_pattern_evidence = proposal.get("push_pattern_evidence")
    if (
        requires_test_or_coverage
        and isinstance(push_pattern_evidence, dict)
        and not push_pattern_evidence.get("has_clear_test_evidence")
    ):
        validation_gaps.append("missing_push_pattern_test_evidence")
    upstream_movement_evidence = proposal.get("upstream_movement_evidence")
    if (
        requires_test_or_coverage
        and isinstance(upstream_movement_evidence, dict)
        and upstream_movement_evidence.get("status") != "ready"
    ):
        validation_gaps.append("missing_upstream_movement_confirmation")

    safety_block = safety_boundary_risk([str(flag) for flag in proposal.get("risk_flags", [])])
    status = "blocked_by_safety_boundary" if safety_block else "validation_gap" if validation_gaps else "ready"
    return {
        "status": status,
        "requires_unit_test_or_coverage": requires_test_or_coverage,
        "has_unit_test_signal": has_unit_test_signal,
        "has_coverage_signal": has_coverage_signal,
        "validation_gaps": validation_gaps,
        "safety_block": safety_block,
        "blocks_autonomous_apply": safety_block,
    }


def proposal_manifest_control(proposal: dict[str, Any]) -> dict[str, Any]:
    """Return replayable safety metadata for a self-evolution proposal."""

    validation_preflight = proposal_validation_preflight(proposal)
    control = {
        "proposal_id": str(proposal.get("proposal_id") or ""),
        "kind": str(proposal.get("kind") or ""),
        "implementation_scope": str(proposal.get("implementation_scope") or ""),
        "validation_gate": str(proposal.get("validation_gate") or ""),
        "autonomous_local_apply": autonomous_local_apply_text(proposal),
        "validation_preflight": validation_preflight,
        "review_metadata": proposal_review_metadata(proposal, validation_preflight),
    }
    proposal_risk_flags = {str(flag) for flag in proposal.get("risk_flags", [])}
    if proposal_risk_flags & HARD_REVIEW_RISK_FLAGS:
        control["safety_boundary_requirement"] = (
            "Only offensive behavior, abuse, unauthorized access, or privacy leakage is review-only; "
            "all other rollback-backed local changes may proceed."
        )
    return control


def proposal_review_metadata(proposal: dict[str, Any], validation_preflight: dict[str, Any]) -> dict[str, Any]:
    """Return local reviewer-routing and bypass metadata for controller manifests."""

    labels = bypass_style_labels(proposal)
    return {
        "reviewer_routes": proposal_reviewer_routes(proposal, validation_preflight),
        "coverage_drop_signal": coverage_drop_signal(proposal, validation_preflight),
        "bypass_label_guard": {
            "status": "blocked" if labels else "passed",
            "blocked_labels": labels,
            "policy": "bypass-style labels are metadata only and cannot grant autonomous local apply",
        },
    }


def proposal_reviewer_routes(proposal: dict[str, Any], validation_preflight: dict[str, Any]) -> list[str]:
    """Infer stable local review routes from controller-owned proposal metadata."""

    routes: list[str] = []
    risk_flags = {str(flag) for flag in proposal.get("risk_flags", [])}
    kind = str(proposal.get("kind") or "")
    validation_gate = str(proposal.get("validation_gate") or "")
    if risk_flags & HARD_REVIEW_RISK_FLAGS or validation_gate.endswith("-human-review"):
        routes.append("safety-boundary-review")
    if kind in {"code_patch", "config"}:
        routes.append("runtime-change-review")
    if validation_preflight.get("requires_unit_test_or_coverage"):
        routes.append("validation-maintainer-review")
    if validation_preflight.get("has_coverage_signal"):
        routes.append("coverage-review")
    if not routes:
        routes.append("general-maintainer-review")
    return sorted(dict.fromkeys(routes))


def coverage_drop_signal(proposal: dict[str, Any], validation_preflight: dict[str, Any]) -> dict[str, Any]:
    """Expose when coverage-related local candidates should turn red on drops."""

    applies = (
        str(proposal.get("implementation_scope") or "") == "local_validation_candidate"
        and bool(validation_preflight.get("has_coverage_signal"))
    )
    return {
        "applies": applies,
        "status_on_drop": "red-non-blocking" if applies else "not-applicable",
        "blocking": False,
    }


def bypass_style_labels(proposal: dict[str, Any]) -> list[str]:
    """Return bypass-like labels that must not influence autonomous apply."""

    raw_labels: list[Any] = []
    for key in ("labels", "github_labels", "apply_labels"):
        value = proposal.get(key)
        if isinstance(value, list):
            raw_labels.extend(value)
    labels = {str(label).strip().lower() for label in raw_labels if str(label).strip()}
    return sorted(labels & BYPASS_STYLE_LABELS)


def build_replayable_validation_report(plan: SelfEvolutionPlan, proposal_controls: list[dict[str, Any]]) -> dict[str, Any]:
    """Return the report contract used to replay evidence review before behavior changes."""

    evidence_urls = sorted(
        {
            str(url)
            for proposal in plan.proposals
            for url in proposal.get("evidence_urls", [])
            if str(url).strip()
        }
    )
    validation_gates = sorted(
        {
            str(proposal.get("validation_gate"))
            for proposal in plan.proposals
            if str(proposal.get("validation_gate") or "").strip()
        }
    )
    proposal_ids = [
        str(proposal.get("proposal_id"))
        for proposal in plan.proposals
        if str(proposal.get("proposal_id") or "").strip()
    ]
    review_metadata = [dict(control.get("review_metadata") or {}) for control in proposal_controls]
    report = {
        "schema_version": 1,
        "source_digest_id": plan.source_digest_id,
        "template_version": 4,
        "required_fields": VALIDATION_REPORT_REQUIRED_FIELDS,
        "evidence_urls": evidence_urls,
        "validation_gates": validation_gates,
        "proposal_controls": proposal_controls,
        "reviewer_routes": sorted(
            {
                str(route)
                for metadata in review_metadata
                for route in metadata.get("reviewer_routes", [])
                if str(route).strip()
            }
        ),
        "coverage_drop_signals": [
            {
                "proposal_id": str(control.get("proposal_id") or ""),
                **dict((control.get("review_metadata") or {}).get("coverage_drop_signal") or {}),
            }
            for control in proposal_controls
        ],
        "bypass_label_guard": {
            "status": "blocked"
            if any(
                (metadata.get("bypass_label_guard") or {}).get("status") == "blocked"
                for metadata in review_metadata
            )
            else "passed",
            "blocked_labels": sorted(
                {
                    str(label)
                    for metadata in review_metadata
                    for label in (metadata.get("bypass_label_guard") or {}).get("blocked_labels", [])
                    if str(label).strip()
                }
            ),
            "policy": "bypass-style labels are metadata only and cannot grant autonomous local apply",
        },
        "pre_adoption_risk_review": {
            "hypothesis": "",
            "expected_local_benefit": "",
            "safety_questions": [
                "What behavior would change if this lesson were adopted?",
                "Which local tests or artifacts prove the lesson before behavior changes?",
                "Which import or startup command proves the adopted behavior does not break activation?",
                "Which runtime capabilities, if any, would be required but are intentionally skipped?",
            ],
            "decision": "pending",
        },
        "provenance": {
            "source_digest_id": plan.source_digest_id,
            "proposal_ids": proposal_ids,
            "evidence_urls": evidence_urls,
            "validation_gates": validation_gates,
            "capability_theme_id": str(plan.capability_theme_window.get("theme_id") or ""),
            "rollback_ref": DRAFT_ROLLBACK_REF,
        },
        "local_commands": [
            {
                "command": "",
                "purpose": "",
                "cwd": plan.repo_path,
                "exit_code": None,
            }
        ],
        "startup_health_checks": [
            {
                "command": "",
                "purpose": "prove imports and startup paths touched by the candidate still load",
                "cwd": plan.repo_path,
                "exit_code": None,
            }
        ],
        "outcomes": [
            {
                "check": "",
                "result": "pending",
                "evidence_artifact": "",
            }
        ],
        "rollback_ref": DRAFT_ROLLBACK_REF,
        "skipped_capabilities": ["none recorded"],
        "runtime_capability_changes": [],
        "runtime_capability_change_policy": (
            "Record material capability changes here when a local adoption changes harnesses, runner or remote "
            "execution behavior, provider/config preflight, restart paths, push or promotion behavior, scheduling, "
            "memory, or tool routing. Capability changes are allowed when rollback-backed, locally validated, and "
            "outside the offensive-behavior and privacy-leakage safety boundary."
        ),
        "completion_requirements": [
            "evidence_urls must contain every external evidence URL used to justify the lesson.",
            "pre_adoption_risk_review.decision must be filled before any behavior adoption.",
            "local_commands and startup_health_checks must list command, purpose, cwd, and exit_code for each check run.",
            "outcomes must name the checked behavior, a passing or reviewed result, and evidence artifact.",
            "rollback_ref must name the concrete local rollback ref or rollback artifact for the run.",
            "provenance.rollback_ref must match rollback_ref so replay metadata points at the same recovery anchor.",
            "skipped_capabilities must list unavailable or intentionally skipped runtime capabilities, or 'none recorded'.",
            "runtime_capability_changes must list material capability changes that were adopted, or stay empty when none changed.",
            "completion_status.is_complete must stay false while placeholders, pending decisions, or the draft rollback ref remain.",
            "adoption_decision.status must be adopted, rejected, or deferred with rationale and decided_at before this report is complete.",
            "uncertainty must record stale, unavailable, or weak evidence instead of being silently omitted.",
        ],
        "adoption_decision": {
            "status": "pending",
            "allowed_statuses": ["pending", "adopted", "rejected", "deferred"],
            "rationale": "",
            "decided_at": "",
        },
        "uncertainty": [
            "Post-run validation commands are executed outside this manifest writer and must be recorded by run notes or supervisor artifacts.",
        ],
    }
    report["completion_status"] = validation_report_completion_status(report)
    return report


def validation_report_completion_status(report: dict[str, Any]) -> dict[str, Any]:
    """Classify whether a replayable validation report is adoption-ready controller evidence."""

    blocking_reasons: list[str] = []
    if report.get("required_fields") != VALIDATION_REPORT_REQUIRED_FIELDS:
        blocking_reasons.append("required_fields does not match the validation report contract")
    blocking_reasons.extend(nonblank_list_reasons(report.get("evidence_urls"), "evidence_urls"))

    raw_risk_review = report.get("pre_adoption_risk_review")
    risk_review = raw_risk_review if isinstance(raw_risk_review, dict) else {}
    for key in ("hypothesis", "expected_local_benefit"):
        if not str(risk_review.get(key) or "").strip():
            blocking_reasons.append(f"pre_adoption_risk_review.{key} is blank")
    risk_review_decision = str(risk_review.get("decision") or "").strip().lower()
    if risk_review_decision in {"", "pending"}:
        blocking_reasons.append("pre_adoption_risk_review.decision is pending")

    blocking_reasons.extend(incomplete_command_reasons(report.get("local_commands"), "local_commands"))
    blocking_reasons.extend(incomplete_command_reasons(report.get("startup_health_checks"), "startup_health_checks"))
    blocking_reasons.extend(incomplete_outcome_reasons(report.get("outcomes")))

    rollback_ref = str(report.get("rollback_ref") or "").strip()
    if not rollback_ref or rollback_ref == DRAFT_ROLLBACK_REF:
        blocking_reasons.append("rollback_ref does not name a concrete rollback ref or artifact")
    blocking_reasons.extend(incomplete_provenance_reasons(report.get("provenance"), rollback_ref, report.get("evidence_urls")))
    blocking_reasons.extend(nonblank_list_reasons(report.get("skipped_capabilities"), "skipped_capabilities"))
    blocking_reasons.extend(runtime_capability_change_reasons(report.get("runtime_capability_changes")))
    blocking_reasons.extend(nonblank_list_reasons(report.get("completion_requirements"), "completion_requirements"))
    if "uncertainty" not in report:
        blocking_reasons.append("uncertainty is missing")

    raw_adoption_decision = report.get("adoption_decision")
    adoption_decision = raw_adoption_decision if isinstance(raw_adoption_decision, dict) else {}
    adoption_status = str(adoption_decision.get("status") or "").strip().lower()
    if adoption_status in {"", "pending"}:
        blocking_reasons.append("adoption_decision.status is pending")
    elif adoption_status not in {"adopted", "rejected", "deferred"}:
        blocking_reasons.append("adoption_decision.status is not an allowed final status")
    blocking_reasons.extend(risk_review_decision_conflict_reasons(risk_review_decision, adoption_status))
    for key in ("rationale", "decided_at"):
        if not str(adoption_decision.get(key) or "").strip():
            blocking_reasons.append(f"adoption_decision.{key} is blank")

    is_complete = not blocking_reasons
    adoption_state = validation_report_adoption_state(
        report,
        blocking_reasons=blocking_reasons,
        adoption_status=adoption_status,
    )
    if not is_complete:
        status = "draft_template"
    elif adoption_status == "adopted":
        status = "completed_adoption_evidence"
    else:
        status = "completed_review_evidence"
    return {
        "status": status,
        "adoption_state": adoption_state,
        "allowed_adoption_states": VALIDATION_REPORT_ADOPTION_STATES,
        "is_complete": is_complete,
        "adoption_evidence_complete": is_complete and adoption_status == "adopted",
        "capability_changes_allowed": True,
        "runtime_capability_changes_recorded": bool(report.get("runtime_capability_changes")),
        "blocking_reasons": blocking_reasons,
    }


def validation_report_adoption_state(
    report: dict[str, Any],
    *,
    blocking_reasons: list[str],
    adoption_status: str,
) -> str:
    """Return the controller-facing state for rollback-backed local adoption evidence."""

    if not blocking_reasons:
        return "adoption-ready" if adoption_status == "adopted" else "rejected"
    if validation_report_has_completion_attempt(report, adoption_status=adoption_status):
        return "incomplete"
    return "draft"


def validation_report_has_completion_attempt(report: dict[str, Any], *, adoption_status: str) -> bool:
    raw_risk_review = report.get("pre_adoption_risk_review")
    risk_review = raw_risk_review if isinstance(raw_risk_review, dict) else {}
    if any(str(risk_review.get(key) or "").strip() for key in ("hypothesis", "expected_local_benefit")):
        return True
    if str(risk_review.get("decision") or "").strip().lower() not in {"", "pending"}:
        return True
    if command_list_has_completion_attempt(report.get("local_commands")):
        return True
    if command_list_has_completion_attempt(report.get("startup_health_checks")):
        return True
    if outcome_list_has_completion_attempt(report.get("outcomes")):
        return True
    rollback_ref = str(report.get("rollback_ref") or "").strip()
    if rollback_ref and rollback_ref != DRAFT_ROLLBACK_REF:
        return True
    if adoption_status not in {"", "pending"}:
        return True
    raw_adoption_decision = report.get("adoption_decision")
    adoption_decision = raw_adoption_decision if isinstance(raw_adoption_decision, dict) else {}
    return any(str(adoption_decision.get(key) or "").strip() for key in ("rationale", "decided_at"))


def risk_review_decision_conflict_reasons(risk_review_decision: str, adoption_status: str) -> list[str]:
    if adoption_status not in {"adopted", "rejected", "deferred"}:
        return []
    if "reject" in risk_review_decision and adoption_status != "rejected":
        return ["pre_adoption_risk_review.decision conflicts with adoption_decision.status"]
    if "defer" in risk_review_decision and adoption_status != "deferred":
        return ["pre_adoption_risk_review.decision conflicts with adoption_decision.status"]
    if (
        ("accept" in risk_review_decision or "adopt" in risk_review_decision)
        and "reject" not in risk_review_decision
        and "defer" not in risk_review_decision
        and adoption_status != "adopted"
    ):
        return ["pre_adoption_risk_review.decision conflicts with adoption_decision.status"]
    return []


def command_list_has_completion_attempt(commands: Any) -> bool:
    if not isinstance(commands, list):
        return False
    for command in commands:
        if not isinstance(command, dict):
            continue
        if str(command.get("command") or "").strip():
            return True
        if command.get("exit_code") is not None:
            return True
    return False


def outcome_list_has_completion_attempt(outcomes: Any) -> bool:
    if not isinstance(outcomes, list):
        return False
    for outcome in outcomes:
        if not isinstance(outcome, dict):
            continue
        result = str(outcome.get("result") or "").strip().lower()
        if any(str(outcome.get(key) or "").strip() for key in ("check", "evidence_artifact")):
            return True
        if result not in {"", "pending"}:
            return True
    return False


def incomplete_provenance_reasons(provenance: Any, rollback_ref: str, evidence_urls: Any) -> list[str]:
    if not isinstance(provenance, dict):
        return ["provenance is missing"]
    reasons: list[str] = []
    expected_evidence_urls = sorted(str(url).strip() for url in evidence_urls if str(url).strip()) if isinstance(evidence_urls, list) else []
    provenance_evidence_urls = (
        sorted(str(url).strip() for url in provenance.get("evidence_urls") if str(url).strip())
        if isinstance(provenance.get("evidence_urls"), list)
        else []
    )
    if provenance_evidence_urls != expected_evidence_urls:
        reasons.append("provenance.evidence_urls does not match evidence_urls")
    provenance_rollback_ref = str(provenance.get("rollback_ref") or "").strip()
    if not provenance_rollback_ref or provenance_rollback_ref == DRAFT_ROLLBACK_REF:
        reasons.append("provenance.rollback_ref does not name a concrete rollback ref or artifact")
    elif rollback_ref and rollback_ref != DRAFT_ROLLBACK_REF and provenance_rollback_ref != rollback_ref:
        reasons.append("provenance.rollback_ref does not match rollback_ref")
    return reasons


def nonblank_list_reasons(value: Any, field_name: str) -> list[str]:
    if not isinstance(value, list) or not value:
        return [f"{field_name} is empty"]
    reasons: list[str] = []
    for index, item in enumerate(value):
        if not str(item or "").strip():
            reasons.append(f"{field_name}[{index}] is blank")
    return reasons


def runtime_capability_change_reasons(value: Any) -> list[str]:
    if not isinstance(value, list):
        return ["runtime_capability_changes is not a list"]
    reasons: list[str] = []
    for index, item in enumerate(value):
        if isinstance(item, dict):
            if not any(str(field_value or "").strip() for field_value in item.values()):
                reasons.append(f"runtime_capability_changes[{index}] is blank")
            continue
        if not str(item or "").strip():
            reasons.append(f"runtime_capability_changes[{index}] is blank")
    return reasons


def incomplete_command_reasons(commands: Any, field_name: str) -> list[str]:
    if not isinstance(commands, list) or not commands:
        return [f"{field_name} is empty"]
    reasons: list[str] = []
    for index, command in enumerate(commands):
        if not isinstance(command, dict):
            reasons.append(f"{field_name}[{index}] is not an object")
            continue
        for key in ("command", "purpose", "cwd"):
            if not str(command.get(key) or "").strip():
                reasons.append(f"{field_name}[{index}].{key} is blank")
        if command.get("exit_code") is None:
            reasons.append(f"{field_name}[{index}].exit_code is not recorded")
    return reasons


def incomplete_outcome_reasons(outcomes: Any) -> list[str]:
    if not isinstance(outcomes, list) or not outcomes:
        return ["outcomes is empty"]
    reasons: list[str] = []
    for index, outcome in enumerate(outcomes):
        if not isinstance(outcome, dict):
            reasons.append(f"outcomes[{index}] is not an object")
            continue
        for key in ("check", "evidence_artifact"):
            if not str(outcome.get(key) or "").strip():
                reasons.append(f"outcomes[{index}].{key} is blank")
        result = str(outcome.get("result") or "").strip().lower()
        if result in {"", "pending"}:
            reasons.append(f"outcomes[{index}].result is pending")
        elif result not in COMPLETED_VALIDATION_OUTCOME_RESULTS:
            reasons.append(
                f"outcomes[{index}].result must be one of: "
                + ", ".join(sorted(COMPLETED_VALIDATION_OUTCOME_RESULTS))
            )
    return reasons


def rank_signals_with_memory(
    signals: list[GrowthSignal],
    *,
    memory: GrowthMemory | None = None,
) -> list[GrowthSignal]:
    review_activity_by_repo = signal_review_activity_counts(signals)
    indexed = list(enumerate(signals))
    indexed.sort(
        key=lambda item: (
            -signal_safety_review_priority(item[1]),
            -signal_direct_action_priority(item[1]),
            -memory_bias_for_signal(item[1], memory),
            -signal_adjusted_confidence(item[1], review_activity_by_repo=review_activity_by_repo),
            item[0],
        )
    )
    return [signal for _, signal in indexed]


def signal_safety_review_priority(signal: GrowthSignal) -> int:
    return 1 if signal.risk_flags else 0


def signal_direct_action_priority(signal: GrowthSignal) -> int:
    if signal.kind == "PullRequestEvent":
        return 1
    text = f"{signal.title} {signal.relevance_reason} {signal.recommended_action}".lower()
    if signal.kind == "PushEvent" and any(term in text for term in ("validation", "validate", "test", "workflow")):
        pattern_evidence = push_pattern_evidence_for_signal(signal)
        if pattern_evidence is not None and not pattern_evidence["has_clear_test_evidence"]:
            return 0
        return 1
    if signal.kind == "ReleaseEvent":
        return 1
    return 0


def signal_review_activity_counts(signals: list[GrowthSignal]) -> dict[str, int]:
    counts: dict[str, int] = {}
    for signal in signals:
        if signal.kind not in REVIEW_ACTIVITY_EVENT_KINDS:
            continue
        counts[signal.repo] = counts.get(signal.repo, 0) + 1
    return counts


def signal_review_activity_bias(
    signal: GrowthSignal,
    *,
    review_activity_by_repo: dict[str, int],
) -> float:
    review_activity_count = review_activity_by_repo.get(signal.repo, 0)
    if review_activity_count < 2:
        return 0.0
    if not signal_is_review_validation_or_test_route(signal):
        return 0.0
    return min(0.4, 0.25 * (review_activity_count - 1))


def signal_adjusted_confidence(
    signal: GrowthSignal,
    *,
    review_activity_by_repo: dict[str, int],
) -> float:
    return signal.confidence + signal_review_activity_bias(signal, review_activity_by_repo=review_activity_by_repo)


def signal_is_review_validation_or_test_route(signal: GrowthSignal) -> bool:
    if signal.kind in REVIEW_ACTIVITY_EVENT_KINDS | {"PullRequestEvent"}:
        return True
    if signal.kind != "PushEvent":
        return False
    text = f"{signal.title} {signal.relevance_reason} {signal.recommended_action}".lower()
    return any(term in text for term in ("review", "validation", "validate", "test", "harness"))


def memory_bias_for_signal(signal: GrowthSignal, memory: GrowthMemory | None) -> float:
    if memory is None:
        return 0.0
    repo_stats = memory.repositories.get(signal.repo, {})
    repo_bias = memory_stats_bias(repo_stats)
    topic_bias = sum(memory_stats_bias(memory.topics.get(topic, {})) for topic in topics_from_relevance(signal.relevance_reason))
    return repo_bias + min(topic_bias, 2.0)


def memory_stats_bias(stats: dict[str, Any]) -> float:
    useful = int(stats.get("useful_signals") or 0)
    validated = int(stats.get("validated") or 0)
    failed = int(stats.get("failed") or 0)
    return max(0.0, min(3.0, useful * 0.15 + validated * 0.5 - failed * 0.35))


def classify_proposal_kind(signal: GrowthSignal) -> str:
    if signal.risk_flags:
        return "follow_up_issue"
    if signal.kind == "RepositoryTrend":
        return "follow_up_issue"
    if signal.kind == "ReleaseEvent":
        return "documentation"
    if signal.kind == "PushEvent":
        return "test"
    if signal.kind == "PullRequestEvent":
        return "code_patch"
    if signal.kind in REVIEW_ACTIVITY_EVENT_KINDS:
        return "test"
    if signal.kind in {"IssuesEvent", "IssueCommentEvent"}:
        return "follow_up_issue"
    return "no_action"


def push_pattern_evidence_for_signal(signal: GrowthSignal) -> dict[str, Any] | None:
    """Return commit-message cluster evidence for default push-derived lessons."""

    if signal.kind != "PushEvent":
        return None
    if signal.recommended_action != "cluster commit messages and keep only patterns with clear test evidence":
        return None
    text = f"{signal.title} {signal.relevance_reason}".lower()
    message_text = push_message_text(signal)
    clusters = [
        cluster_name
        for cluster_name, terms in PUSH_PATTERN_CLUSTERS
        if any(contains_risk_term(message_text, term) for term in terms)
    ]
    has_clear_test_evidence = any(contains_risk_term(message_text, term) for term in PUSH_PATTERN_TEST_EVIDENCE_TERMS)
    status = "ready" if clusters and has_clear_test_evidence else "evidence_gap"
    return {
        "status": status,
        "clusters": clusters,
        "has_clear_test_evidence": has_clear_test_evidence,
        "source": "push_commit_message_cluster",
        "policy": "keep_push_patterns_only_when_commit_messages_include_clear_test_or_ci_evidence",
        "matched_text_hash": stable_push_message_hash(text),
    }


def upstream_movement_evidence_for_signal(signal: GrowthSignal) -> dict[str, Any] | None:
    """Return local confirmation metadata for PR/review/push-derived lessons."""

    if signal.kind not in {"PullRequestEvent", *REVIEW_ACTIVITY_EVENT_KINDS, "PushEvent"}:
        return None
    item = {
        "item_id": signal.event_id,
        "event_kind": signal.kind,
        "summary": f"{signal.repo}: {signal.title}",
        "relevance_reason": signal.relevance_reason,
        "source_url": signal.url,
    }
    classification = classify_upstream_movement_item(item)
    status = "ready" if classification["confirmation_level"] == "specific" else "needs_triage"
    return {
        "status": status,
        "confirmation_level": classification["confirmation_level"],
        "branch": classification["branch"],
        "merge_timing": classification["merge_timing"],
        "subsystem": classification["subsystem"],
        "triage_key": classification["triage_key"],
        "missing_details": classification["missing_details"],
        "policy": "promote_upstream_movement_only_after_branch_timing_or_subsystem_confirmation",
    }


def build_upstream_movement_triage(items: list[dict[str, Any]]) -> dict[str, Any]:
    """Group low-detail PR/review/push movement before it can justify local code."""

    clusters: dict[str, dict[str, Any]] = {}
    low_detail_count = 0
    specific_count = 0
    for item in items:
        if str(item.get("event_kind") or "") not in {"PullRequestEvent", *REVIEW_ACTIVITY_EVENT_KINDS, "PushEvent"}:
            continue
        classification = classify_upstream_movement_item(item)
        key = classification["triage_key"]
        cluster = clusters.setdefault(
            key,
            {
                "key": key,
                "branch": classification["branch"],
                "merge_timing": classification["merge_timing"],
                "subsystem": classification["subsystem"],
                "event_kinds": [],
                "item_ids": [],
                "confirmation_level": classification["confirmation_level"],
                "missing_details": [],
            },
        )
        cluster["event_kinds"].append(str(item.get("event_kind") or ""))
        cluster["item_ids"].append(str(item.get("item_id") or ""))
        cluster["missing_details"].extend(classification["missing_details"])
        if classification["confirmation_level"] == "low_detail":
            low_detail_count += 1
        else:
            specific_count += 1

    rendered_clusters = []
    for cluster in clusters.values():
        event_kinds = sorted(set(cluster["event_kinds"]))
        missing_details = sorted(set(cluster["missing_details"]))
        rendered_clusters.append(
            {
                "key": cluster["key"],
                "branch": cluster["branch"],
                "merge_timing": cluster["merge_timing"],
                "subsystem": cluster["subsystem"],
                "event_kinds": event_kinds,
                "item_ids": cluster["item_ids"],
                "item_count": len(cluster["item_ids"]),
                "confirmation_level": "low_detail" if missing_details else "specific",
                "missing_details": missing_details,
            }
        )
    rendered_clusters.sort(key=lambda cluster: (-int(cluster["item_count"]), str(cluster["key"])))
    return {
        "schema_version": 1,
        "policy": "group_pr_review_push_items_by_branch_timing_and_subsystem_before_promotion",
        "promotion_rule": (
            "Specific local proposals require an inspected branch, merge timing, touched subsystem, "
            "or local failing test; low-detail clusters remain follow-up evidence."
        ),
        "low_detail_item_count": low_detail_count,
        "specific_item_count": specific_count,
        "clusters": rendered_clusters,
    }


def classify_upstream_movement_item(item: dict[str, Any]) -> dict[str, Any]:
    text = " ".join(
        str(item.get(key) or "")
        for key in ("event_kind", "summary", "relevance_reason", "source_url")
    )
    lowered = text.lower()
    branch = branch_from_upstream_movement_text(text)
    merge_timing = merge_timing_from_upstream_movement_text(lowered)
    subsystem = subsystem_from_upstream_movement_text(lowered)
    missing_details: list[str] = []
    if branch == "unknown":
        missing_details.append("branch")
    if merge_timing == "unknown":
        missing_details.append("merge_timing")
    if subsystem == "unknown":
        missing_details.append("subsystem")
    if upstream_movement_text_is_generic(lowered):
        missing_details.append("specific_title_or_body")
    confirmation_level = "low_detail" if missing_details else "specific"
    return {
        "branch": branch,
        "merge_timing": merge_timing,
        "subsystem": subsystem,
        "missing_details": missing_details,
        "confirmation_level": confirmation_level,
        "triage_key": f"branch={branch}|timing={merge_timing}|subsystem={subsystem}",
    }


def branch_from_upstream_movement_text(text: str) -> str:
    match = re.search(r"\bpush to ([^:\s]+)", text, flags=re.IGNORECASE)
    if match:
        return sanitize_upstream_group_value(match.group(1))
    match = re.search(r"\bfrom\s+(?:[^:\s]+:)?([A-Za-z0-9._/-]+)", text)
    if match:
        return sanitize_upstream_group_value(match.group(1))
    return "unknown"


def merge_timing_from_upstream_movement_text(text: str) -> str:
    if any(term in text for term in ("merged pull request", "closed pull request", "merge commit")):
        return "merged_or_closed"
    if any(term in text for term in ("opened pull request", "ready for review", "wants to merge")):
        return "pre_merge"
    if "push to" in text:
        return "push"
    return "unknown"


def subsystem_from_upstream_movement_text(text: str) -> str:
    subsystem_terms = (
        ("tests", ("test", "tests", "pytest", "e2e", "coverage", "regression", "smoke")),
        ("docs", ("docs", "documentation", "readme", "guide")),
        ("controller", ("controller", "proposal", "digest", "triage", "github-growth")),
        ("runtime", ("runner", "runtime", "harness", "sandbox", "policy", "provider", "scheduler")),
        ("ci", ("ci", "workflow", "actions", "release")),
    )
    for subsystem, terms in subsystem_terms:
        if any(term in text for term in terms):
            return subsystem
    return "unknown"


def upstream_movement_text_is_generic(text: str) -> bool:
    generic_terms = (
        "untitled pull request",
        "generic",
        "left a comment",
        "left review comments",
        "reviewed pull request",
        "submitted pull request review",
        "opened pull request: untitled",
        "push to repository",
    )
    return any(term in text for term in generic_terms)


def sanitize_upstream_group_value(value: str) -> str:
    sanitized = re.sub(r"[^A-Za-z0-9._/-]+", "-", value.strip()).strip("-").lower()
    return sanitized[:80] or "unknown"


UPSTREAM_EVIDENCE_CAPABILITY_DENIED_ACTIONS = (
    "provider_launch",
    "external_harness_execution",
    "remote_execution",
    "upstream_skill_activation",
    "privacy_data_export",
    "credential_access",
    "push_or_promotion",
    "kernel_restart",
)

_UPSTREAM_PR_COMPARE_MARKERS = (
    "untitled pull request",
    "compare the pull request approach",
    "pull request approach with local",
    "opened pull request",
    "generic pull request",
)


def attach_upstream_evidence_capability_step(digest: dict[str, Any]) -> dict[str, Any]:
    """Attach the operator-visible one-step capability translation for this digest."""

    step = build_upstream_evidence_capability_step(
        list(digest.get("proposals") or []),
        theme_window=digest.get("capability_theme_window")
        if isinstance(digest.get("capability_theme_window"), dict)
        else {},
        items=list(digest.get("items") or []),
    )
    digest["upstream_evidence_capability_step"] = step
    return digest


def build_upstream_evidence_capability_step(
    proposals: list[dict[str, Any]],
    *,
    theme_window: dict[str, Any] | None = None,
    items: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Translate mixed upstream proposals into one local capability step.

    Public agent-ecosystem signals stay evidence. This surface picks a single
    next local action for operators and supervisors: compare weak PR movement
    with local behavior before drafting, keep privacy/offensive routes
    review-only, and never export raw evidence URLs or sensitive bodies.
    """

    del items  # reserved for future item-level corroboration without URL export
    theme = theme_window if isinstance(theme_window, dict) else {}
    candidate_rows = [classify_upstream_evidence_capability_route(proposal) for proposal in proposals]
    selected = select_upstream_evidence_capability_step(candidate_rows)
    retained_boundaries = [
        {
            "proposal_id": row["proposal_id"],
            "route_class": row["route_class"],
            "validation_gate": row["validation_gate"],
            "reason": row["selection_reason"],
        }
        for row in candidate_rows
        if row["route_class"]
        in {
            "privacy_boundary_review_only",
            "offensive_boundary_review_only",
        }
    ]
    if selected is None:
        status = "no_proposals" if not candidate_rows else "no_selectable_local_step"
        selected_step = {
            "proposal_id": "",
            "route_class": "none",
            "capability_action": "record_upstream_evidence_without_local_capability_step",
            "local_lane": "none",
            "validation_gate": "",
            "requires_local_compare_before_draft": False,
            "autonomous_local_apply": False,
            "selection_reason": "no_local_capability_step_selected",
        }
    else:
        status = str(selected["status"])
        selected_step = {
            "proposal_id": selected["proposal_id"],
            "route_class": selected["route_class"],
            "capability_action": selected["capability_action"],
            "local_lane": selected["local_lane"],
            "validation_gate": selected["validation_gate"],
            "requires_local_compare_before_draft": selected["requires_local_compare_before_draft"],
            "autonomous_local_apply": selected["autonomous_local_apply_allowed"],
            "selection_reason": selected["selection_reason"],
        }
        selected = {**selected, "selected": True}
        for row in candidate_rows:
            row["selected"] = row["proposal_id"] == selected_step["proposal_id"] and bool(
                selected_step["proposal_id"]
            )

    return {
        "schema_version": 1,
        "policy": "translate_upstream_evidence_into_one_local_capability_step",
        "theme_id": str(theme.get("theme_id") or "upstream-evidence-capability"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "status": status,
        "selected_step": selected_step,
        "candidate_rows": candidate_rows,
        "retained_boundaries": retained_boundaries,
        "denied_actions": list(UPSTREAM_EVIDENCE_CAPABILITY_DENIED_ACTIONS),
        "runtime_action": "none",
        "privacy_export_allowed": False,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "promotion_rule": (
            "Select one local capability step after comparing weak PR or release evidence with local "
            "behavior; keep privacy leakage and offensive routes review-only; require focused local "
            "validation before code adoption."
        ),
    }


def classify_upstream_evidence_capability_route(proposal: dict[str, Any]) -> dict[str, Any]:
    """Classify one proposal into a local capability route without exporting raw URLs."""

    proposal_id = str(proposal.get("proposal_id") or "").strip()
    kind = str(proposal.get("kind") or "no_action").strip() or "no_action"
    implementation_scope = str(proposal.get("implementation_scope") or "").strip()
    validation_gate = str(proposal.get("validation_gate") or "").strip()
    risk_flags = sorted({str(flag) for flag in proposal.get("risk_flags", []) if str(flag).strip()})
    evidence_url_hashes = sorted(
        {
            hashlib.sha256(str(url).strip().encode("utf-8")).hexdigest()[:16]
            for url in proposal.get("evidence_urls", [])
            if str(url).strip()
        }
    )
    summary_text = " ".join(
        str(proposal.get(key) or "")
        for key in ("summary", "kind", "validation_task", "rationale", "recommended_action")
    ).lower()
    upstream_movement = proposal.get("upstream_movement_evidence")
    upstream_status = "not_applicable"
    upstream_confirmation = "not_applicable"
    if isinstance(upstream_movement, dict):
        upstream_status = str(upstream_movement.get("status") or "needs_triage")
        upstream_confirmation = str(upstream_movement.get("confirmation_level") or "low_detail")

    preflight = proposal_validation_preflight(proposal)
    autonomous_text = autonomous_local_apply_text(proposal)
    autonomous_allowed = autonomous_text == "True" and preflight.get("status") == "ready"

    if "offensive-behavior" in risk_flags or validation_gate == "offensive-behavior-human-review":
        route_class = "offensive_boundary_review_only"
        capability_action = "retain_offensive_behavior_review_boundary"
        local_lane = "none"
        requires_compare = False
        selection_reason = "offensive_behavior_remains_review_only"
        status = "blocked_by_safety_boundary"
        autonomous_allowed = False
    elif "privacy-leakage" in risk_flags or validation_gate == "privacy-leakage-human-review":
        route_class = "privacy_boundary_review_only"
        capability_action = "retain_privacy_leakage_review_boundary"
        local_lane = "none"
        requires_compare = False
        selection_reason = "privacy_leakage_remains_review_only"
        status = "blocked_by_safety_boundary"
        autonomous_allowed = False
    elif risk_flags or implementation_scope == "risk_review_before_local_change":
        route_class = "risk_review"
        capability_action = "summarize_risk_pattern_as_reviewable_local_note"
        local_lane = "documentation" if kind in {"documentation", "docs"} else "test"
        requires_compare = False
        selection_reason = "non_hard_risk_flags_require_reviewable_local_summary"
        status = "risk_review"
        autonomous_allowed = False
    elif (
        any(marker in summary_text for marker in _UPSTREAM_PR_COMPARE_MARKERS)
        or upstream_status == "needs_triage"
        or upstream_confirmation == "low_detail"
    ):
        route_class = "local_pr_compare_before_draft"
        capability_action = "compare_pull_request_approach_with_local_agent_behavior_before_draft"
        local_lane = _local_lane_from_proposal_kind(kind)
        requires_compare = True
        selection_reason = "weak_or_untitled_pull_request_evidence_requires_local_compare_before_draft"
        status = "compare_before_draft"
        # Compare-before-draft is an operator-visible capability step, not an apply grant.
        autonomous_allowed = False
    elif implementation_scope == "local_validation_candidate" and preflight.get("status") == "ready":
        route_class = "local_validation_ready"
        capability_action = "apply_one_local_validation_candidate"
        local_lane = _local_lane_from_proposal_kind(kind)
        requires_compare = False
        selection_reason = "confirmed_local_validation_candidate_ready"
        status = "ready"
    elif implementation_scope == "local_validation_candidate":
        route_class = "local_validation_gap"
        capability_action = "close_local_validation_gap_before_capability_apply"
        local_lane = _local_lane_from_proposal_kind(kind)
        requires_compare = upstream_status == "needs_triage"
        selection_reason = "local_validation_candidate_has_preflight_gaps"
        status = "validation_gap"
        autonomous_allowed = False
    elif implementation_scope == "reviewable_proposal_only" or kind in {"follow_up_issue", "no_action"}:
        route_class = "follow_up_only"
        capability_action = "preserve_follow_up_without_local_capability_apply"
        local_lane = "none"
        requires_compare = False
        selection_reason = "proposal_is_follow_up_or_reviewable_only"
        status = "follow_up_only"
        autonomous_allowed = False
    else:
        route_class = "follow_up_only"
        capability_action = "preserve_follow_up_without_local_capability_apply"
        local_lane = _local_lane_from_proposal_kind(kind)
        requires_compare = False
        selection_reason = "no_mapped_local_capability_route"
        status = "follow_up_only"
        autonomous_allowed = False

    return {
        "proposal_id": proposal_id,
        "route_class": route_class,
        "capability_action": capability_action,
        "local_lane": local_lane,
        "kind": kind,
        "implementation_scope": implementation_scope,
        "validation_gate": validation_gate,
        "risk_flags": risk_flags,
        "evidence_url_hashes": evidence_url_hashes,
        "upstream_confirmation": upstream_confirmation,
        "upstream_movement_status": upstream_status,
        "requires_local_compare_before_draft": requires_compare,
        "autonomous_local_apply_allowed": autonomous_allowed,
        "autonomous_local_apply_text": autonomous_text,
        "validation_preflight_status": str(preflight.get("status") or ""),
        "validation_gaps": list(preflight.get("validation_gaps") or []),
        "selection_reason": selection_reason,
        "status": status,
        "selected": False,
    }


def select_upstream_evidence_capability_step(
    candidate_rows: list[dict[str, Any]],
) -> dict[str, Any] | None:
    """Pick one next local capability step from classified proposal rows."""

    if not candidate_rows:
        return None

    priority = {
        "local_validation_ready": 0,
        "local_pr_compare_before_draft": 1,
        "local_validation_gap": 2,
        "risk_review": 3,
        "follow_up_only": 4,
        "privacy_boundary_review_only": 5,
        "offensive_boundary_review_only": 6,
    }
    ranked = sorted(
        candidate_rows,
        key=lambda row: (
            priority.get(str(row.get("route_class") or ""), 99),
            str(row.get("proposal_id") or ""),
        ),
    )
    selected = ranked[0]
    # If the only rows are hard safety boundaries, still surface one retained boundary
    # as the selected step so operators see an explicit non-apply outcome.
    return selected


def _local_lane_from_proposal_kind(kind: str) -> str:
    normalized = kind.strip().lower()
    if normalized in {"code_patch", "code", "patch"}:
        return "code_patch"
    if normalized in {"test", "tests"}:
        return "test"
    if normalized in {"documentation", "docs", "doc"}:
        return "documentation"
    if normalized in {"config", "configuration"}:
        return "config"
    if normalized in {"follow_up_issue", "no_action"}:
        return "none"
    return "test"


SKILL_ROUTE_DISCOVERY_CAPABILITY_DENIED_ACTIONS = (
    "external_skill_execution",
    "external_skill_activation",
    "provider_launch",
    "external_harness_execution",
    "remote_apply",
    "remote_execution",
    "privacy_data_export",
    "credential_access",
    "push_or_promotion",
    "kernel_restart",
)
SKILL_ROUTE_DISCOVERY_ALLOWED_LANES = ("documentation", "config", "test", "code_patch")
SKILL_ROUTE_DISCOVERY_PIPELINE_STAGES = (
    "classifier",
    "route_profiles",
    "bounded_local_apply_lanes",
)
SKILL_ROUTE_DISCOVERY_LOCAL_COMPARISON_CRITERIA = (
    "route_class_is_skill_route_discovery",
    "skill_route_discovery_first_for_codex_workflow_gate",
    "codex_workflow_gate_or_generic_skill_workflow_profile",
    "preferred_lane_matches_route_profile",
    "allowed_lanes_subset_of_bounded_local_apply",
    "runtime_action_is_none",
    "external_skill_execution_denied",
    "provider_launch_denied",
    "remote_apply_denied",
    "general_agent_rows_do_not_inherit_skill_unlock",
    "privacy_or_offensive_rows_remain_review_only",
)
_SKILL_ROUTE_REVERSE_FLOW_MARKERS = (
    "reverse-flow",
    "reverse_flow",
    "reverse flow",
    "codex_workflow_gate",
    "skill_route_discovery_first",
    "lingbol088-spec/reverse-flow-skill",
)
_SKILL_ROUTE_RNSKILL_MARKERS = (
    "rnskill",
    "pluviobyte/rnskill",
    "generic_skill_workflow",
    "skill.md collection",
    "skills collection",
    "skill collection",
)
_SKILL_ROUTE_FORTRESS_MARKERS = (
    "fortress",
    "tiliondev/fortress",
)
_SKILL_ROUTE_HY3_MARKERS = (
    "hy3",
    "tencent-hunyuan/hy3",
    "tencent-hunyuan",
)
_SKILL_ROUTE_AGENT_CHIEF_MARKERS = (
    "agent-chief",
    "agent_chief",
    "smilelikeye/agent-chief",
)
_SKILL_ROUTE_GENERAL_AGENT_HARNESS_MARKERS = (
    "agent_harness_eval",
    "agent harness eval",
    "general_agent_project",
    "general agent project",
    "prop-harness-fortress",
    "prop-harness-hy3",
    "prop-hy3-harness",
    "prop-fortress",
)
_SKILL_ROUTE_GENERIC_SKILL_MARKERS = (
    "skill_route_discovery",
    "skill workflow",
    "skill_workflow",
    "skill.md",
    "skills/",
    "agent skill",
    "codex skill",
)
AGENT_HARNESS_EVAL_POST_COMPARE_LANES = ("documentation", "test", "code_patch")
ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND = (
    "pytest tests/test_harness_eval.py -q -k agent_harness_eval_cluster_local_apply"
)


def attach_skill_route_discovery_capability_pipeline(digest: dict[str, Any]) -> dict[str, Any]:
    """Attach the operator-visible skill-route capability pipeline for this digest."""

    pipeline = build_skill_route_discovery_capability_pipeline(
        list(digest.get("proposals") or []),
        theme_window=digest.get("capability_theme_window")
        if isinstance(digest.get("capability_theme_window"), dict)
        else {},
        items=list(digest.get("items") or []),
    )
    digest["skill_route_discovery_capability_pipeline"] = pipeline
    return digest


def build_skill_route_discovery_capability_pipeline(
    proposals: list[dict[str, Any]],
    *,
    theme_window: dict[str, Any] | None = None,
    items: list[dict[str, Any]] | None = None,
    local_comparison_passed: bool = False,
    apply_local_comparison: bool = True,
    focused_validation_command_results: list[dict[str, Any]] | None = None,
    residual_focused_validation_command_results: list[dict[str, Any]] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Translate skill/workflow trend signals into one local capability pipeline.

    Pass 1 of skill-route-discovery: classifier → route profiles → bounded local
    apply lanes. reverse-flow-skill maps to codex_workflow_gate /
    skill_route_discovery_first; rnskill maps to generic_skill_workflow. Lanes
    stay limited to documentation, config, test, or code_patch. Local comparison
    is required before any lane unlock. Runtime action stays none. Privacy and
    offensive rows remain review-only; general-agent projects stay adjacent
    harness-eval candidates without inheriting skill-route lanes.

    Pass 2 deepens the reverse-flow local test validation lane: skill-workflow
    probe outputs are compared against classifier / route_profiles /
    bounded_local_apply_lanes criteria before the preferred test lane unlocks.

    Pass 3 packages the unlocked reverse-flow test lane into a body-free local
    apply handoff with companion rnskill documentation profiles and config-gate
    boundaries on the same pipeline (no isolated fixtures).

    Pass 4 closes the theme with skill_route_discovery_local_apply_completion:
    once reverse-flow local apply is ready, emit an operator-visible completion
    handoff that binds the full capability pipeline, unlocked local test lane,
    retained privacy/general-agent boundaries, and the external supervisor
    next action.

    After completion, skill_route_discovery_unlocked_local_test_lane_apply
    packages the supervisor next action
    (apply_unlocked_local_test_lane_with_focused_validation_and_keep_activation_external)
    into a body-free focused-validation apply packet for the reverse-flow test
    lane only. skill_route_discovery_focused_local_test_validation then records
    the body-free focused validation run (command hashes; optional pass/fail
    results) while activation stays external. Optional
    ``focused_validation_command_results`` close that surface from ``ready`` to
    ``passed``/``failed`` in one pipeline build; supervisors may also call
    ``record_skill_route_discovery_focused_local_test_validation_results`` on an
    existing pipeline after running the focused commands. After a recorded pass,
    ``skill_route_discovery_focused_validation_activation_external_handoff``
    packages ``keep_activation_external_after_focused_local_test_validation`` into
    one operator-visible activation-external packet (push, promotion, provider
    launch, remote apply, external skill execution, and kernel restart stay
    denied). Supervisors may close a ready focused surface with
    ``close_skill_route_discovery_focused_local_test_validation_with_outcome``
    (body-free expected-hash rows → record → handoff refresh) without
    re-listing commands. When the activation-external handoff is ready after a
    recorded pass,
    ``skill_route_discovery_focused_validation_activation_external_acceptance``
    packages terminal acceptance while residual fortress/Hy3 rows stay adjacent
    harness-eval only. After acceptance is ``accepted`` and residual adjacent
    rows remain,
    ``skill_route_discovery_focused_validation_residual_adjacent_queue`` packages
    those proposal IDs for ``agent_harness_eval_cluster_local_apply`` without
    inheriting skill unlocks. When that residual queue is ``ready``,
    ``skill_route_discovery_residual_adjacent_harness_eval_local_apply`` selects
    one residual fortress/Hy3 proposal and hands it to
    ``agent_harness_eval_cluster_local_apply`` with local comparison required and
    skill unlocks closed. When that residual local apply is ``ready``,
    ``skill_route_discovery_residual_adjacent_harness_eval_local_comparison``
    runs residual harness local comparison and unlocks only documentation,
    test, or code_patch after criteria pass while skill-route unlocks stay
    closed. When residual harness local comparison is ``ready``,
    ``skill_route_discovery_residual_adjacent_unlocked_local_lane_apply``
    packages supervisor next action
    ``apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external``
    into one body-free residual apply surface that prefers the local ``test``
    lane first (then documentation, then code_patch) without inheriting
    reverse-flow skill unlocks. When residual unlocked local lane apply is
    ``ready``,
    ``skill_route_discovery_residual_adjacent_focused_local_validation`` records
    body-free command-hash focused validation for the residual selected lane
    while skill unlocks stay closed and activation remains external. Optional
    ``residual_focused_validation_command_results`` close that residual surface
    from ``ready`` to ``passed``/``failed`` in one pipeline build; supervisors
    may also call
    ``record_skill_route_discovery_residual_adjacent_focused_local_validation_results``
    or
    ``close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome``
    on an existing pipeline. When residual focused validation is ``passed``,
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``
    packages ``keep_activation_external_after_residual_adjacent_focused_local_validation``
    and may note remaining residual fortress/Hy3 proposal IDs without skill
    unlock inheritance. When residual activation-external handoff is ``ready``,
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance``
    accepts the residual keep_activation_external package while remaining residual
    rows stay noted without skill unlock inheritance. External skill execution,
    provider launch, remote apply, push, promotion, and kernel restart stay denied;
    activation remains external.
    """

    del items  # reserved for future item-level corroboration without URL export
    theme = theme_window if isinstance(theme_window, dict) else {}
    candidate_rows = [
        classify_skill_route_discovery_capability_route(proposal) for proposal in proposals
    ]
    selected = select_skill_route_discovery_capability_step(candidate_rows)
    retained_boundaries = [
        {
            "proposal_id": row["proposal_id"],
            "route_class": row["route_class"],
            "validation_gate": row["validation_gate"],
            "reason": row["selection_reason"],
        }
        for row in candidate_rows
        if row["route_class"]
        in {
            "privacy_boundary_review_only",
            "offensive_boundary_review_only",
        }
    ]
    adjacent_general_agent_rows = [
        {
            "proposal_id": row["proposal_id"],
            "route_class": row["route_class"],
            "evaluation_lane": "agent_harness_eval_required",
            "skill_route_discovery_inherited": False,
            "allowed_local_lanes_after_eval": ["documentation", "test", "code_patch"],
            "direct_allowed_lanes_before_eval": [],
            "runtime_action": "none",
            "reason": row["selection_reason"],
        }
        for row in candidate_rows
        if row["route_class"] == "agent_harness_eval_required"
    ]
    skill_route_rows = [
        row for row in candidate_rows if row["route_class"] == "skill_route_discovery"
    ]
    route_profile_rows = [
        {
            "proposal_id": row["proposal_id"],
            "route_profiles": list(row["route_profiles"]),
            "skill_route_discovery_first": bool(row["skill_route_discovery_first"]),
            "preferred_local_lane": row["preferred_local_lane"],
            "allowed_local_lanes": list(row["allowed_local_lanes"]),
        }
        for row in skill_route_rows
    ]

    local_comparison: dict[str, Any] = {
        "controller_surface": "skill_route_discovery_local_comparison",
        "status": "not_applicable",
        "decision": "no_skill_route_candidate_to_compare",
        "criteria_results": [],
        "failed_criteria": [],
        "local_comparison_passed": False,
        "unlocked_local_lanes": [],
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }

    if selected is None:
        status = "no_proposals" if not candidate_rows else "no_selectable_skill_route_step"
        selected_step = {
            "proposal_id": "",
            "route_class": "none",
            "capability_action": "record_skill_route_evidence_without_local_apply",
            "route_profiles": [],
            "selected_local_lane": "none",
            "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
            "unlocked_local_lanes": [],
            "local_comparison_required": True,
            "local_comparison_status": "not_applicable",
            "skill_route_discovery_first": False,
            "validation_gate": "",
            "autonomous_local_apply": False,
            "selection_reason": "no_skill_route_capability_step_selected",
        }
        bounded_status = "not_applicable"
        selected_is_adjacent_harness = False
    else:
        selected_is_adjacent_harness = (
            str(selected.get("route_class") or "") == "agent_harness_eval_required"
        )
        local_comparison = evaluate_skill_route_discovery_local_comparison(
            selected,
            candidate_rows=candidate_rows,
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
        )
        criteria_passed = bool(local_comparison.get("local_comparison_passed"))
        comparison_not_applicable = str(local_comparison.get("status") or "") == "not_applicable"
        # Explicit override remains available for replay fixtures; pass 2 defaults
        # to criteria-driven comparison so reverse-flow unlock is evidence-backed.
        # Adjacent general-agent selections never unlock skill-route lanes.
        if selected_is_adjacent_harness or comparison_not_applicable:
            comparison_passed = False
            comparison_status = "not_applicable"
        else:
            comparison_passed = bool(local_comparison_passed) or (
                apply_local_comparison and criteria_passed
            )
            comparison_status = (
                "passed"
                if comparison_passed
                else (
                    "failed"
                    if apply_local_comparison and not criteria_passed
                    else "required_before_unlock"
                )
            )
        preferred_lane = str(selected["preferred_local_lane"] or "")
        unlocked_lanes = (
            [preferred_lane]
            if comparison_passed
            and preferred_lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
            and not selected_is_adjacent_harness
            else []
        )
        status = "ready" if selected["status"] == "ready" else str(selected["status"])
        if selected_is_adjacent_harness:
            status = "adjacent_harness_eval"
        elif status == "ready" and not comparison_passed:
            status = "ready_for_local_comparison"
        elif status == "ready" and comparison_passed:
            status = "ready"
        elif comparison_passed and selected["status"] in {"ready", "validation_gap", "risk_review"}:
            status = "ready" if selected["status"] == "ready" else str(selected["status"])
        selected_step = {
            "proposal_id": selected["proposal_id"],
            "route_class": selected["route_class"],
            "capability_action": selected["capability_action"],
            "route_profiles": list(selected["route_profiles"]),
            "selected_local_lane": preferred_lane if preferred_lane else "none",
            "allowed_local_lanes": list(selected["allowed_local_lanes"]),
            "unlocked_local_lanes": unlocked_lanes,
            "local_comparison_required": not selected_is_adjacent_harness,
            "local_comparison_status": comparison_status,
            "skill_route_discovery_first": bool(selected["skill_route_discovery_first"]),
            "validation_gate": selected["validation_gate"],
            "autonomous_local_apply": bool(selected["autonomous_local_apply_allowed"])
            and not selected_is_adjacent_harness,
            "selection_reason": selected["selection_reason"],
        }
        for row in candidate_rows:
            row["selected"] = row["proposal_id"] == selected_step["proposal_id"] and bool(
                selected_step["proposal_id"]
            )
        if selected_is_adjacent_harness:
            bounded_status = "deferred_to_agent_harness_eval_local_comparison"
        else:
            bounded_status = (
                "lanes_unlocked_after_local_comparison"
                if unlocked_lanes
                else (
                    "local_comparison_failed"
                    if comparison_status == "failed"
                    else "local_comparison_required_before_unlock"
                )
            )
        local_comparison["unlocked_local_lanes"] = list(unlocked_lanes)
        local_comparison["applied"] = bool(
            (apply_local_comparison or local_comparison_passed) and not selected_is_adjacent_harness
        )
        local_comparison["selected_local_lane"] = preferred_lane if preferred_lane else "none"

    classifier_status = (
        "ready"
        if skill_route_rows or retained_boundaries or adjacent_general_agent_rows
        else status
    )
    route_profiles_status = "ready" if route_profile_rows else "no_skill_route_profiles"
    reverse_flow_test_lane = build_skill_route_discovery_reverse_flow_test_validation_lane(
        selected_step=selected_step,
        local_comparison=local_comparison,
        route_profile_rows=route_profile_rows,
        theme_window=theme,
    )
    rnskill_docs_lane = build_skill_route_discovery_rnskill_docs_validation_lane(
        route_profile_rows=route_profile_rows,
        local_comparison=local_comparison,
        reverse_flow_test_validation_lane=reverse_flow_test_lane,
        theme_window=theme,
    )
    config_gate_boundary = build_skill_route_discovery_config_gate_boundary(
        selected_step=selected_step,
        route_profile_rows=route_profile_rows,
        adjacent_general_agent_rows=adjacent_general_agent_rows,
        retained_boundaries=retained_boundaries,
        local_comparison=local_comparison,
        theme_window=theme,
    )
    local_apply = build_skill_route_discovery_local_apply(
        selected_step=selected_step,
        local_comparison=local_comparison,
        reverse_flow_test_validation_lane=reverse_flow_test_lane,
        rnskill_docs_validation_lane=rnskill_docs_lane,
        config_gate_boundary=config_gate_boundary,
        theme_window=theme,
    )
    local_apply_completion = build_skill_route_discovery_local_apply_completion(
        local_apply=local_apply,
        reverse_flow_test_validation_lane=reverse_flow_test_lane,
        rnskill_docs_validation_lane=rnskill_docs_lane,
        config_gate_boundary=config_gate_boundary,
        local_comparison=local_comparison,
        retained_boundaries=retained_boundaries,
        adjacent_general_agent_rows=adjacent_general_agent_rows,
        theme_window=theme,
    )
    unlocked_local_test_lane_apply = build_skill_route_discovery_unlocked_local_test_lane_apply(
        local_apply_completion=local_apply_completion,
        local_apply=local_apply,
        reverse_flow_test_validation_lane=reverse_flow_test_lane,
        local_comparison=local_comparison,
        selected_step=selected_step,
        retained_boundaries=retained_boundaries,
        adjacent_general_agent_rows=adjacent_general_agent_rows,
        theme_window=theme,
    )
    focused_local_test_validation = build_skill_route_discovery_focused_local_test_validation(
        unlocked_local_test_lane_apply=unlocked_local_test_lane_apply,
        command_results=focused_validation_command_results,
        theme_window=theme,
        source_digest=source_digest,
    )
    focused_validation_activation_external_handoff = (
        build_skill_route_discovery_focused_validation_activation_external_handoff(
            focused_local_test_validation=focused_local_test_validation,
            unlocked_local_test_lane_apply=unlocked_local_test_lane_apply,
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    focused_validation_activation_external_acceptance = (
        build_skill_route_discovery_focused_validation_activation_external_acceptance(
            focused_validation_activation_external_handoff=(
                focused_validation_activation_external_handoff
            ),
            focused_local_test_validation=focused_local_test_validation,
            unlocked_local_test_lane_apply=unlocked_local_test_lane_apply,
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    focused_validation_residual_adjacent_queue = (
        build_skill_route_discovery_focused_validation_residual_adjacent_queue(
            focused_validation_activation_external_acceptance=(
                focused_validation_activation_external_acceptance
            ),
            focused_validation_activation_external_handoff=(
                focused_validation_activation_external_handoff
            ),
            focused_local_test_validation=focused_local_test_validation,
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_harness_eval_local_apply = (
        build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
            focused_validation_residual_adjacent_queue=(
                focused_validation_residual_adjacent_queue
            ),
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_harness_eval_local_comparison = (
        build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
            residual_adjacent_harness_eval_local_apply=(
                residual_adjacent_harness_eval_local_apply
            ),
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_unlocked_local_lane_apply = (
        build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply(
            residual_adjacent_harness_eval_local_comparison=(
                residual_adjacent_harness_eval_local_comparison
            ),
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_focused_local_validation = (
        build_skill_route_discovery_residual_adjacent_focused_local_validation(
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            command_results=residual_focused_validation_command_results,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_focused_validation_activation_external_handoff = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff(
            residual_adjacent_focused_local_validation=(
                residual_adjacent_focused_local_validation
            ),
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    residual_adjacent_focused_validation_activation_external_acceptance = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance(
            residual_adjacent_focused_validation_activation_external_handoff=(
                residual_adjacent_focused_validation_activation_external_handoff
            ),
            residual_adjacent_focused_local_validation=(
                residual_adjacent_focused_local_validation
            ),
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            adjacent_general_agent_rows=adjacent_general_agent_rows,
            retained_boundaries=retained_boundaries,
            theme_window=theme,
            source_digest=source_digest,
        )
    )
    adjacent_harness_eval_handoff = build_skill_route_discovery_adjacent_harness_eval_handoff(
        selected_step=selected_step,
        adjacent_general_agent_rows=adjacent_general_agent_rows,
        retained_boundaries=retained_boundaries,
        local_comparison=local_comparison,
        local_apply=local_apply,
        local_apply_completion=local_apply_completion,
        theme_window=theme,
    )
    # Adjacent general-agent residual work is a deliberate handoff, not a failed reverse-flow theme.
    if (
        str(adjacent_harness_eval_handoff.get("status") or "") == "ready"
        and str(selected_step.get("route_class") or "") == "agent_harness_eval_required"
    ):
        status = "adjacent_harness_eval"
    # Pass 4 closes the window when the reverse-flow local apply completion is ready.
    if (
        str(local_apply_completion.get("status") or "") == "complete"
        and int(theme.get("planned_passes") or 0)
        >= int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES)
    ):
        status = "complete"
    pipeline = {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_capability_pipeline",
        "policy": "translate_skill_workflow_signals_into_bounded_local_lanes",
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": (
                "complete"
                if str(local_apply_completion.get("status") or "") == "complete"
                and int(theme.get("planned_passes") or 0)
                >= int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES)
                else str(theme.get("status") or "")
            ),
        },
        "pipeline_stages": list(SKILL_ROUTE_DISCOVERY_PIPELINE_STAGES),
        "status": status,
        "classifier": {
            "status": classifier_status,
            "candidate_count": len(candidate_rows),
            "skill_route_count": len(skill_route_rows),
            "privacy_boundary_count": len(
                [row for row in candidate_rows if row["route_class"] == "privacy_boundary_review_only"]
            ),
            "general_agent_count": len(adjacent_general_agent_rows),
            "candidate_rows": candidate_rows,
        },
        "route_profiles": {
            "status": route_profiles_status,
            "rows": route_profile_rows,
            "codex_workflow_gate_count": len(
                [
                    row
                    for row in route_profile_rows
                    if "codex_workflow_gate" in row["route_profiles"]
                ]
            ),
            "generic_skill_workflow_count": len(
                [
                    row
                    for row in route_profile_rows
                    if "generic_skill_workflow" in row["route_profiles"]
                    and "codex_workflow_gate" not in row["route_profiles"]
                ]
            ),
        },
        "bounded_local_apply": {
            "status": bounded_status,
            "local_comparison_required": True,
            "local_comparison_status": selected_step["local_comparison_status"],
            "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
            "selected_local_lane": selected_step["selected_local_lane"],
            "unlocked_local_lanes": list(selected_step["unlocked_local_lanes"]),
            "runtime_action": "none",
            "external_skill_execution_allowed": False,
            "provider_launch_allowed": False,
            "remote_apply_allowed": False,
        },
        "local_comparison": local_comparison,
        "reverse_flow_test_validation_lane": reverse_flow_test_lane,
        "rnskill_docs_validation_lane": rnskill_docs_lane,
        "config_gate_boundary": config_gate_boundary,
        "local_apply": local_apply,
        "local_apply_completion": local_apply_completion,
        "unlocked_local_test_lane_apply": unlocked_local_test_lane_apply,
        "focused_local_test_validation": focused_local_test_validation,
        "focused_validation_activation_external_handoff": (
            focused_validation_activation_external_handoff
        ),
        "focused_validation_activation_external_acceptance": (
            focused_validation_activation_external_acceptance
        ),
        "focused_validation_residual_adjacent_queue": (
            focused_validation_residual_adjacent_queue
        ),
        "residual_adjacent_harness_eval_local_apply": (
            residual_adjacent_harness_eval_local_apply
        ),
        "residual_adjacent_harness_eval_local_comparison": (
            residual_adjacent_harness_eval_local_comparison
        ),
        "residual_adjacent_unlocked_local_lane_apply": (
            residual_adjacent_unlocked_local_lane_apply
        ),
        "residual_adjacent_focused_local_validation": (
            residual_adjacent_focused_local_validation
        ),
        "residual_adjacent_focused_validation_activation_external_handoff": (
            residual_adjacent_focused_validation_activation_external_handoff
        ),
        "residual_adjacent_focused_validation_activation_external_acceptance": (
            residual_adjacent_focused_validation_activation_external_acceptance
        ),
        "focused_local_test_validation_recorded": focused_local_test_validation.get(
            "focused_validation_recorded"
        )
        is True,
        "residual_adjacent_focused_local_validation_recorded": (
            residual_adjacent_focused_local_validation.get("focused_validation_recorded")
            is True
        ),
        "adjacent_agent_harness_eval_handoff": adjacent_harness_eval_handoff,
        "selected_step": selected_step,
        "retained_boundaries": retained_boundaries,
        "adjacent_general_agent_rows": adjacent_general_agent_rows,
        "denied_actions": list(SKILL_ROUTE_DISCOVERY_CAPABILITY_DENIED_ACTIONS),
        "runtime_action": "none",
        "privacy_export_allowed": False,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "external_skill_activation_allowed": False,
        "promotion_rule": (
            "Classify skill/workflow signals into route profiles, require local comparison "
            "before unlocking documentation/config/test/code_patch lanes, keep runtime_action "
            "none, and retain privacy/offensive rows review-only without exporting raw evidence URLs. "
            "Adjacent general_agent_project rows (for example fortress) hand off to "
            "agent_harness_eval_cluster_local_apply instead of failing skill-route comparison."
        ),
    }
    return attach_skill_route_discovery_pipeline_operator_state(pipeline)


def evaluate_skill_route_discovery_local_comparison(
    selected: dict[str, Any],
    *,
    candidate_rows: list[dict[str, Any]] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Compare one skill-route candidate against pipeline stage contracts.

    Pass 2 reverse-flow test validation: lock codex_workflow_gate +
    skill_route_discovery_first into a bounded local test lane only when the
    classifier / route_profiles / bounded_local_apply_lanes criteria hold.
    External skill execution, provider launch, and remote apply stay denied.
    """

    del candidate_rows  # retained/adjacent snapshots already encode boundary isolation
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    profiles = [str(profile) for profile in selected.get("route_profiles") or []]
    preferred_lane = str(selected.get("preferred_local_lane") or selected.get("selected_local_lane") or "")
    allowed_lanes = [str(lane) for lane in selected.get("allowed_local_lanes") or []]
    skill_first = bool(selected.get("skill_route_discovery_first"))
    route_class = str(selected.get("route_class") or "")
    runtime_action = str(selected.get("runtime_action") or "none")
    expected_lane = skill_route_discovery_preferred_lane(profiles, str(selected.get("kind") or ""))

    def _criterion(criterion_id: str, passed: bool, *, required: bool = True, detail: str = "") -> dict[str, Any]:
        return {
            "criterion_id": criterion_id,
            "passed": passed,
            "required": required,
            "detail": detail,
        }

    # Adjacent general-agent rows (fortress/Hy3) are not skill-route candidates.
    # Do not fail skill-route comparison; hand off to agent_harness_eval instead.
    if route_class == "agent_harness_eval_required":
        return {
            "schema_version": 1,
            "controller_surface": "skill_route_discovery_local_comparison",
            "status": "not_applicable",
            "decision": (
                "selected_adjacent_harness_eval_requires_agent_harness_local_comparison"
            ),
            "selected_proposal_id": str(selected.get("proposal_id") or ""),
            "route_class": route_class,
            "route_profiles": list(profiles),
            "skill_route_discovery_first": skill_first,
            "selected_local_lane": preferred_lane or "none",
            "allowed_local_lanes": list(allowed_lanes),
            "criteria_ids": list(SKILL_ROUTE_DISCOVERY_LOCAL_COMPARISON_CRITERIA),
            "criteria_results": [],
            "failed_criteria": [],
            "local_comparison_passed": False,
            "unlocked_local_lanes": [],
            "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
            "handoff_allowed_local_lanes": list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES),
            "runtime_action": "none",
            "external_skill_execution_allowed": False,
            "provider_launch_allowed": False,
            "remote_apply_allowed": False,
            "raw_evidence_urls_exported": False,
            "raw_upstream_bodies_exported": False,
            "required_validation": [
                "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline",
                ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
            ],
            "body_free": True,
        }

    if "codex_workflow_gate" in profiles:
        skill_first_ok = skill_first is True
        skill_first_detail = f"skill_route_discovery_first={skill_first}"
    else:
        # generic_skill_workflow companion profiles do not require the codex-first gate
        skill_first_ok = True
        skill_first_detail = "generic_skill_workflow does not require codex-first gate"

    criteria_results = [
        _criterion(
            "route_class_is_skill_route_discovery",
            route_class == "skill_route_discovery",
            detail=f"route_class={route_class or 'none'}",
        ),
        _criterion(
            "skill_route_discovery_first_for_codex_workflow_gate",
            skill_first_ok,
            detail=skill_first_detail,
        ),
        _criterion(
            "codex_workflow_gate_or_generic_skill_workflow_profile",
            any(profile in {"codex_workflow_gate", "generic_skill_workflow"} for profile in profiles),
            detail=f"route_profiles={profiles or ['none']}",
        ),
        _criterion(
            "preferred_lane_matches_route_profile",
            preferred_lane == expected_lane and preferred_lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES,
            detail=f"preferred={preferred_lane or 'none'} expected={expected_lane}",
        ),
        _criterion(
            "allowed_lanes_subset_of_bounded_local_apply",
            bool(allowed_lanes)
            and all(lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES for lane in allowed_lanes),
            detail=f"allowed_local_lanes={allowed_lanes or ['none']}",
        ),
        _criterion(
            "runtime_action_is_none",
            runtime_action == "none",
            detail=f"runtime_action={runtime_action or 'none'}",
        ),
        _criterion(
            "external_skill_execution_denied",
            selected.get("external_skill_activation_allowed") is not True,
            detail="external_skill_execution stays denied for skill-route local lanes",
        ),
        _criterion(
            "provider_launch_denied",
            "provider_launch" in SKILL_ROUTE_DISCOVERY_CAPABILITY_DENIED_ACTIONS,
            detail="provider_launch is a denied pipeline action",
        ),
        _criterion(
            "remote_apply_denied",
            "remote_apply" in SKILL_ROUTE_DISCOVERY_CAPABILITY_DENIED_ACTIONS,
            detail="remote_apply is a denied pipeline action",
        ),
        _criterion(
            "general_agent_rows_do_not_inherit_skill_unlock",
            all(
                row.get("skill_route_discovery_inherited") is False
                and list(row.get("direct_allowed_lanes_before_eval") or []) == []
                for row in adjacent
            ),
            detail=f"adjacent_general_agent_count={len(adjacent)}",
        ),
        _criterion(
            "privacy_or_offensive_rows_remain_review_only",
            all(
                str(row.get("route_class") or "")
                in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
                for row in retained
            )
            and str(selected.get("route_class") or "")
            not in {"privacy_boundary_review_only", "offensive_boundary_review_only"},
            detail=f"retained_boundary_count={len(retained)}",
        ),
    ]

    failed_criteria = [
        str(result["criterion_id"])
        for result in criteria_results
        if result.get("required") is True and result.get("passed") is not True
    ]
    comparison_passed = not failed_criteria
    unlocked = (
        [preferred_lane]
        if comparison_passed and preferred_lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
        else []
    )
    if comparison_passed:
        status = "passed"
        decision = "unlock_selected_local_lane_after_pipeline_stage_comparison"
    else:
        status = "failed"
        decision = "hold_selected_lane_until_pipeline_stage_comparison_passes"

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_local_comparison",
        "status": status,
        "decision": decision,
        "selected_proposal_id": str(selected.get("proposal_id") or ""),
        "route_class": route_class,
        "route_profiles": list(profiles),
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": preferred_lane,
        "allowed_local_lanes": list(allowed_lanes),
        "criteria_ids": list(SKILL_ROUTE_DISCOVERY_LOCAL_COMPARISON_CRITERIA),
        "criteria_results": criteria_results,
        "failed_criteria": failed_criteria,
        "local_comparison_passed": comparison_passed,
        "unlocked_local_lanes": unlocked,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ],
        "body_free": True,
    }


def build_skill_route_discovery_reverse_flow_test_validation_lane(
    *,
    selected_step: dict[str, Any],
    local_comparison: dict[str, Any],
    route_profile_rows: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Operator-visible pass-2 reverse-flow test validation lane.

    Locks reverse-flow codex_workflow_gate + skill_route_discovery_first into the
    bounded local test lane after local comparison. Companion rnskill rows remain
    visible as documentation-preferring generic_skill_workflow profiles on the
    same pipeline without isolated fixture sprawl.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    profiles = [str(profile) for profile in selected_step.get("route_profiles") or []]
    is_reverse_flow = "codex_workflow_gate" in profiles and bool(
        selected_step.get("skill_route_discovery_first")
    )
    comparison_passed = bool(local_comparison.get("local_comparison_passed"))
    selected_lane = str(selected_step.get("selected_local_lane") or "")
    unlocked = list(selected_step.get("unlocked_local_lanes") or [])
    companion_rnskill = [
        {
            "proposal_id": row.get("proposal_id"),
            "route_profiles": list(row.get("route_profiles") or []),
            "preferred_local_lane": row.get("preferred_local_lane"),
            "skill_route_discovery_first": bool(row.get("skill_route_discovery_first")),
        }
        for row in (route_profile_rows or [])
        if "generic_skill_workflow" in list(row.get("route_profiles") or [])
        and "codex_workflow_gate" not in list(row.get("route_profiles") or [])
    ]

    if not selected_step.get("proposal_id"):
        status = "not_applicable"
        decision = "no_selected_skill_route_step"
    elif not is_reverse_flow:
        status = "deferred_non_reverse_flow_selection"
        decision = "selected_step_is_not_reverse_flow_test_lane"
    elif comparison_passed and selected_lane == "test" and "test" in unlocked:
        status = "ready"
        decision = "reverse_flow_test_lane_unlocked_after_local_comparison"
    elif comparison_passed and selected_lane == "test":
        status = "comparison_passed_awaiting_lane_unlock"
        decision = "local_comparison_passed_for_reverse_flow_test_lane"
    else:
        status = "blocked_until_local_comparison"
        decision = "hold_reverse_flow_test_lane_until_local_comparison_passes"

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_reverse_flow_test_validation_lane",
        "proposal_track": "prop-skill-pipeline-reverse-flow-test",
        "status": status,
        "decision": decision,
        "selected_proposal_id": str(selected_step.get("proposal_id") or ""),
        "route_class": str(selected_step.get("route_class") or "none"),
        "route_profiles": profiles,
        "skill_route_discovery_first": bool(selected_step.get("skill_route_discovery_first")),
        "selected_local_lane": selected_lane,
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked,
        "local_comparison_required": True,
        "local_comparison_status": str(
            selected_step.get("local_comparison_status")
            or local_comparison.get("status")
            or "required_before_unlock"
        ),
        "local_comparison_passed": comparison_passed,
        "failed_criteria": list(local_comparison.get("failed_criteria") or []),
        "companion_rnskill_documentation_profiles": companion_rnskill,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def build_skill_route_discovery_rnskill_docs_validation_lane(
    *,
    route_profile_rows: list[dict[str, Any]] | None = None,
    local_comparison: dict[str, Any] | None = None,
    reverse_flow_test_validation_lane: dict[str, Any] | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Operator-visible pass-3 rnskill documentation companion on the shared pipeline.

    Keeps prop-skill-pipeline-rnskill-docs as a body-free generic_skill_workflow
    documentation profile after reverse-flow local comparison. Does not open a
    separate fixture path, external skill execution, or remote apply.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    comparison = local_comparison if isinstance(local_comparison, dict) else {}
    reverse_lane = (
        reverse_flow_test_validation_lane
        if isinstance(reverse_flow_test_validation_lane, dict)
        else {}
    )
    companion_rows = [
        {
            "proposal_id": row.get("proposal_id"),
            "route_profiles": list(row.get("route_profiles") or []),
            "preferred_local_lane": row.get("preferred_local_lane"),
            "skill_route_discovery_first": bool(row.get("skill_route_discovery_first")),
            "allowed_local_lanes": list(row.get("allowed_local_lanes") or SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        }
        for row in (route_profile_rows or [])
        if "generic_skill_workflow" in list(row.get("route_profiles") or [])
        and "codex_workflow_gate" not in list(row.get("route_profiles") or [])
    ]
    comparison_passed = bool(comparison.get("local_comparison_passed"))
    reverse_ready = str(reverse_lane.get("status") or "") == "ready"
    preferred = "documentation"
    if not companion_rows:
        status = "not_applicable"
        decision = "no_rnskill_documentation_profiles_on_pipeline"
        unlocked: list[str] = []
    elif comparison_passed and reverse_ready:
        status = "ready"
        decision = "rnskill_docs_companion_ready_after_reverse_flow_local_comparison"
        unlocked = [preferred]
    elif comparison_passed:
        status = "comparison_passed_awaiting_reverse_flow_lane"
        decision = "hold_rnskill_docs_until_reverse_flow_test_lane_ready"
        unlocked = []
    else:
        status = "blocked_until_local_comparison"
        decision = "hold_rnskill_docs_until_pipeline_stage_comparison_passes"
        unlocked = []

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_rnskill_docs_validation_lane",
        "proposal_track": "prop-skill-pipeline-rnskill-docs",
        "status": status,
        "decision": decision,
        "route_class": "skill_route_discovery",
        "route_profiles": ["generic_skill_workflow"],
        "skill_route_discovery_first": False,
        "preferred_local_lane": preferred,
        "selected_local_lane": preferred,
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked,
        "companion_profiles": companion_rows,
        "companion_count": len(companion_rows),
        "local_comparison_required": True,
        "local_comparison_passed": comparison_passed,
        "reverse_flow_test_validation_lane_status": str(reverse_lane.get("status") or "none"),
        "body_free": True,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ],
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def build_skill_route_discovery_config_gate_boundary(
    *,
    selected_step: dict[str, Any],
    route_profile_rows: list[dict[str, Any]] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    local_comparison: dict[str, Any] | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Operator-visible pass-3 config gates separating skill routes from adjacent rows.

    prop-skill-pipeline-config-gates: codex_workflow_gate vs generic_skill_workflow
    stay distinct; general_agent_project and privacy rows never inherit skill-route
    unlocks. Runtime action stays none.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    comparison = local_comparison if isinstance(local_comparison, dict) else {}
    profiles = list(route_profile_rows or [])
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    codex_rows = [
        row for row in profiles if "codex_workflow_gate" in list(row.get("route_profiles") or [])
    ]
    generic_only_rows = [
        row
        for row in profiles
        if "generic_skill_workflow" in list(row.get("route_profiles") or [])
        and "codex_workflow_gate" not in list(row.get("route_profiles") or [])
    ]
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
    )
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
    )
    skill_unlocked = list(selected_step.get("unlocked_local_lanes") or [])
    profiles_separated = bool(codex_rows) or bool(generic_only_rows) or not profiles
    selected_route_class = str(selected_step.get("route_class") or "")
    selected_is_adjacent_harness = selected_route_class == "agent_harness_eval_required"
    selected_is_safety_boundary = selected_route_class in {
        "privacy_boundary_review_only",
        "offensive_boundary_review_only",
    }
    isolation_holds = general_agent_isolated and privacy_isolated and profiles_separated
    # Skill-route apply is only allowed for skill_route_discovery rows. Adjacent
    # harness-eval selections keep isolation ready without pretending to be a
    # skill unlock failure that needs repair.
    gates_pass = isolation_holds and not selected_is_safety_boundary and not selected_is_adjacent_harness
    if not selected_step.get("proposal_id"):
        status = "not_applicable"
        decision = "no_selected_skill_route_step_for_config_gates"
    elif selected_is_adjacent_harness and isolation_holds:
        status = "ready"
        decision = "adjacent_general_agent_row_does_not_inherit_skill_unlocks"
    elif selected_is_adjacent_harness:
        status = "blocked"
        decision = "repair_config_gate_isolation_before_adjacent_harness_handoff"
    elif gates_pass and bool(comparison.get("local_comparison_passed")):
        status = "ready"
        decision = "config_gates_hold_adjacent_rows_out_of_skill_unlocks"
    elif gates_pass:
        status = "ready_awaiting_local_comparison"
        decision = "config_gates_defined_local_comparison_still_required"
    else:
        status = "blocked"
        decision = "repair_config_gate_isolation_before_skill_local_apply"

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_config_gate_boundary",
        "proposal_track": "prop-skill-pipeline-config-gates",
        "status": status,
        "decision": decision,
        "codex_workflow_gate_count": len(codex_rows),
        "generic_skill_workflow_only_count": len(generic_only_rows),
        "route_profiles_separated": profiles_separated,
        "general_agent_inherits_skill_unlock": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_rows_selectable_for_local_apply": False,
        "privacy_isolation_passed": privacy_isolated,
        "adjacent_general_agent_count": len(adjacent),
        "retained_boundary_count": len(retained),
        "skill_unlocked_lanes": skill_unlocked,
        "allowed_skill_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "local_comparison_passed": bool(comparison.get("local_comparison_passed")),
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def build_skill_route_discovery_local_apply(
    *,
    selected_step: dict[str, Any],
    local_comparison: dict[str, Any],
    reverse_flow_test_validation_lane: dict[str, Any],
    rnskill_docs_validation_lane: dict[str, Any],
    config_gate_boundary: dict[str, Any],
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Pass-3 local apply handoff for one skill-route validation candidate.

    After reverse-flow test lane unlock and pipeline-stage comparison, package
    the selected local test apply with body-free rnskill docs companion and
    config-gate boundaries. Runtime action, external skill execution, provider
    launch, remote apply, push, promotion, and restart stay denied.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    reverse_lane = reverse_flow_test_validation_lane
    rnskill_lane = rnskill_docs_validation_lane
    config_gates = config_gate_boundary
    comparison_passed = bool(local_comparison.get("local_comparison_passed"))
    reverse_ready = str(reverse_lane.get("status") or "") == "ready"
    rnskill_ready = str(rnskill_lane.get("status") or "") in {"ready", "not_applicable"}
    config_ready = str(config_gates.get("status") or "") in {
        "ready",
        "ready_awaiting_local_comparison",
        "not_applicable",
    }
    selected_lane = str(selected_step.get("selected_local_lane") or "")
    unlocked = list(selected_step.get("unlocked_local_lanes") or [])
    profiles = [str(profile) for profile in selected_step.get("route_profiles") or []]
    is_reverse_flow = "codex_workflow_gate" in profiles and bool(
        selected_step.get("skill_route_discovery_first")
    )

    selected_route_class = str(selected_step.get("route_class") or "none")
    selected_is_adjacent_harness = selected_route_class == "agent_harness_eval_required"
    if not selected_step.get("proposal_id"):
        status = "not_applicable"
        decision = "no_selected_skill_route_step_for_local_apply"
    elif selected_is_adjacent_harness:
        status = "deferred_adjacent_harness_eval"
        decision = "selected_step_is_adjacent_agent_harness_eval_not_skill_local_apply"
    elif not is_reverse_flow:
        status = "deferred_non_reverse_flow_selection"
        decision = "selected_step_is_not_reverse_flow_local_apply"
    elif (
        comparison_passed
        and reverse_ready
        and rnskill_ready
        and config_ready
        and selected_lane == "test"
        and "test" in unlocked
    ):
        status = "ready"
        decision = "apply_one_local_skill_route_validation_candidate_after_lane_unlock"
    elif comparison_passed and reverse_ready and selected_lane == "test" and "test" in unlocked:
        status = "blocked_until_companion_surfaces_ready"
        decision = "hold_local_apply_until_rnskill_docs_and_config_gates_ready"
    elif comparison_passed:
        status = "blocked_until_reverse_flow_lane_ready"
        decision = "hold_local_apply_until_reverse_flow_test_lane_ready"
    else:
        status = "blocked_until_local_comparison"
        decision = "hold_local_apply_until_pipeline_stage_comparison_passes"

    if status == "ready":
        supervisor_next_action = "replay_skill_route_discovery_local_apply_then_continue_to_pass4"
    elif status == "deferred_adjacent_harness_eval":
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_selected_general_agent_row"
        )
    elif status in {"deferred_non_reverse_flow_selection", "not_applicable"}:
        supervisor_next_action = "wait_for_skill_route_discovery_reverse_flow_selection"
    else:
        supervisor_next_action = "repair_skill_route_discovery_local_apply_before_pass4"

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_local_apply",
        "proposal_track": (
            str(selected_step.get("proposal_id") or "prop-skill-pipeline-reverse-flow-test")
            if selected_is_adjacent_harness
            else "prop-skill-pipeline-reverse-flow-test"
        ),
        "companion_tracks": [
            "prop-skill-pipeline-rnskill-docs",
            "prop-skill-pipeline-config-gates",
        ],
        "status": status,
        "decision": decision,
        "capability_action": str(
            selected_step.get("capability_action")
            or "apply_one_local_skill_route_validation_candidate"
        ),
        "selected_proposal_id": str(selected_step.get("proposal_id") or ""),
        "route_class": selected_route_class,
        "route_profiles": profiles,
        "skill_route_discovery_first": bool(selected_step.get("skill_route_discovery_first")),
        "selected_local_lane": selected_lane if selected_lane else "none",
        "allowed_local_lanes": (
            list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES)
            if selected_is_adjacent_harness
            else list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES)
        ),
        "unlocked_local_lanes": unlocked if status == "ready" else [],
        "local_comparison_required": not selected_is_adjacent_harness,
        "local_comparison_passed": comparison_passed,
        "local_comparison_status": str(
            selected_step.get("local_comparison_status")
            or local_comparison.get("status")
            or "required_before_unlock"
        ),
        "reverse_flow_test_validation_lane_status": str(reverse_lane.get("status") or "none"),
        "rnskill_docs_validation_lane_status": str(rnskill_lane.get("status") or "none"),
        "config_gate_boundary_status": str(config_gates.get("status") or "none"),
        "rnskill_docs_companion": {
            "status": rnskill_lane.get("status"),
            "preferred_local_lane": rnskill_lane.get("preferred_local_lane"),
            "unlocked_local_lanes": list(rnskill_lane.get("unlocked_local_lanes") or []),
            "body_free": bool(rnskill_lane.get("body_free")),
            "proposal_track": rnskill_lane.get("proposal_track"),
        },
        "config_gates": {
            "status": config_gates.get("status"),
            "general_agent_inherits_skill_unlock": bool(
                config_gates.get("general_agent_inherits_skill_unlock")
            ),
            "privacy_rows_selectable_for_local_apply": bool(
                config_gates.get("privacy_rows_selectable_for_local_apply")
            ),
            "route_profiles_separated": bool(config_gates.get("route_profiles_separated")),
            "proposal_track": config_gates.get("proposal_track"),
        },
        "autonomous_local_apply": bool(selected_step.get("autonomous_local_apply"))
        and status == "ready",
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ]
        + (
            [ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND]
            if selected_is_adjacent_harness
            else []
        ),
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "supervisor_next_action": supervisor_next_action,
    }


def build_skill_route_discovery_local_apply_completion(
    *,
    local_apply: dict[str, Any],
    reverse_flow_test_validation_lane: dict[str, Any] | None = None,
    rnskill_docs_validation_lane: dict[str, Any] | None = None,
    config_gate_boundary: dict[str, Any] | None = None,
    local_comparison: dict[str, Any] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Pass-4 completion handoff after reverse-flow local apply unlocks.

    Closes the skill-route-discovery theme window once
    ``skill_route_discovery_local_apply`` is ready for the reverse-flow test
    lane. Binds the full capability pipeline (classifier → route_profiles →
    bounded_local_apply_lanes → local comparison → reverse-flow test lane →
    rnskill docs companion → config gates → local apply → completion), retained
    privacy/general-agent boundaries, and the external supervisor next action.
    Runtime action, external skill execution, provider launch, remote apply,
    push, promotion, and kernel restart stay denied.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    apply = local_apply if isinstance(local_apply, dict) else {}
    reverse_lane = (
        reverse_flow_test_validation_lane
        if isinstance(reverse_flow_test_validation_lane, dict)
        else {}
    )
    rnskill_lane = (
        rnskill_docs_validation_lane if isinstance(rnskill_docs_validation_lane, dict) else {}
    )
    config_gates = config_gate_boundary if isinstance(config_gate_boundary, dict) else {}
    comparison = local_comparison if isinstance(local_comparison, dict) else {}
    retained = list(retained_boundaries or [])
    adjacent = list(adjacent_general_agent_rows or [])

    planned_passes = int(theme.get("planned_passes") or 0)
    target_passes = int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES)
    apply_status = str(apply.get("status") or "")
    comparison_passed = bool(
        apply.get("local_comparison_passed")
        if apply.get("local_comparison_passed") is not None
        else comparison.get("local_comparison_passed")
    )
    unlocked = [
        lane
        for lane in list(apply.get("unlocked_local_lanes") or [])
        if lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
    ]
    reverse_ready = str(reverse_lane.get("status") or "") == "ready"
    rnskill_ready = str(rnskill_lane.get("status") or "") in {"ready", "not_applicable"}
    config_ready = str(config_gates.get("status") or "") in {
        "ready",
        "ready_awaiting_local_comparison",
        "not_applicable",
    }
    selected_lane = str(apply.get("selected_local_lane") or "")
    selected_proposal_id = str(
        apply.get("selected_proposal_id")
        or apply.get("proposal_track")
        or "prop-skill-pipeline-reverse-flow-test"
    )
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
    ) if adjacent else True
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
    ) if retained else True

    apply_route_class = str(apply.get("route_class") or "")
    selected_is_adjacent_harness = (
        apply_route_class == "agent_harness_eval_required"
        or apply_status == "deferred_adjacent_harness_eval"
    )
    theme_complete = (
        apply_status == "ready"
        and comparison_passed
        and reverse_ready
        and rnskill_ready
        and config_ready
        and selected_lane == "test"
        and "test" in unlocked
        and apply.get("runtime_action") == "none"
        and apply.get("external_skill_execution_allowed") is not True
        and apply.get("provider_launch_allowed") is not True
        and apply.get("remote_apply_allowed") is not True
        and apply.get("push_or_promotion_allowed") is not True
        and apply.get("kernel_restart_allowed") is not True
        and general_agent_isolated
        and privacy_isolated
        and config_gates.get("general_agent_inherits_skill_unlock") is not True
        and config_gates.get("privacy_rows_selectable_for_local_apply") is not True
        and not selected_is_adjacent_harness
    )

    if apply_status in {"", "not_applicable"} and not selected_proposal_id:
        status = "not_applicable"
        decision = "no_skill_route_local_apply_candidate_to_complete_theme"
        supervisor_next_action = "wait_for_skill_route_discovery_local_apply"
    elif selected_is_adjacent_harness and general_agent_isolated and privacy_isolated:
        # Residual fortress-style general-agent work is a deliberate handoff, not
        # a reverse-flow theme repair signal.
        status = "deferred_adjacent_harness_eval"
        decision = (
            "hand_off_selected_adjacent_row_to_agent_harness_eval_local_comparison"
        )
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_selected_general_agent_row"
        )
    elif theme_complete:
        status = "complete"
        decision = (
            "complete_skill_route_discovery_capability_slice_after_reverse_flow_local_apply"
        )
        supervisor_next_action = (
            "apply_unlocked_local_test_lane_with_focused_validation_and_keep_activation_external"
        )
    else:
        status = "blocked"
        decision = "hold_theme_completion_until_reverse_flow_local_apply_unlocks_test_lane"
        supervisor_next_action = (
            "repair_skill_route_discovery_local_apply_before_theme_completion"
        )

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_local_apply_completion",
        "proposal_track": "prop-skill-pipeline-reverse-flow-test",
        "companion_tracks": [
            "prop-skill-pipeline-rnskill-docs",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": planned_passes,
            "target_passes": target_passes,
            "current_pass": planned_passes if planned_passes > 0 else target_passes,
            "status": "complete" if theme_complete else status,
            "is_final_pass": planned_passes >= target_passes and target_passes > 0,
        },
        "source_digest": source_digest or "",
        "status": status,
        "decision": decision,
        "capability_action": str(
            apply.get("capability_action")
            or "apply_one_local_skill_route_validation_candidate"
        ),
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_adjacent_harness_eval_handoff",
        ],
        "local_apply_status": apply_status,
        "local_comparison_passed": comparison_passed,
        "local_comparison_status": str(
            apply.get("local_comparison_status") or comparison.get("status") or ""
        ),
        "reverse_flow_test_validation_lane_status": str(reverse_lane.get("status") or "none"),
        "rnskill_docs_validation_lane_status": str(rnskill_lane.get("status") or "none"),
        "config_gate_boundary_status": str(config_gates.get("status") or "none"),
        "selected_local_lane": selected_lane if selected_lane else "none",
        "unlocked_local_lanes": unlocked if theme_complete else [],
        "allowed_local_lanes": (
            list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES)
            if selected_is_adjacent_harness
            else list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES)
        ),
        "route_class": str(apply.get("route_class") or "skill_route_discovery"),
        "route_profiles": list(apply.get("route_profiles") or []),
        "skill_route_discovery_first": bool(apply.get("skill_route_discovery_first")),
        "local_validation_required": True,
        "theme_complete": theme_complete,
        "adjacent_harness_eval_handoff_required": selected_is_adjacent_harness,
        "retained_boundary_proposal_ids": retained_ids,
        "adjacent_general_agent_proposal_ids": adjacent_ids,
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "rnskill_docs_companion": {
            "status": rnskill_lane.get("status"),
            "preferred_local_lane": rnskill_lane.get("preferred_local_lane"),
            "body_free": bool(rnskill_lane.get("body_free", True)),
            "proposal_track": rnskill_lane.get("proposal_track")
            or "prop-skill-pipeline-rnskill-docs",
        },
        "config_gates": {
            "status": config_gates.get("status"),
            "general_agent_inherits_skill_unlock": bool(
                config_gates.get("general_agent_inherits_skill_unlock")
            ),
            "privacy_rows_selectable_for_local_apply": bool(
                config_gates.get("privacy_rows_selectable_for_local_apply")
            ),
            "route_profiles_separated": bool(config_gates.get("route_profiles_separated")),
            "proposal_track": config_gates.get("proposal_track")
            or "prop-skill-pipeline-config-gates",
        },
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "supervisor_activation_allowed": False,
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline"
        ]
        + (
            [ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND]
            if selected_is_adjacent_harness
            else []
        ),
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def build_skill_route_discovery_unlocked_local_test_lane_apply(
    *,
    local_apply_completion: dict[str, Any] | None = None,
    local_apply: dict[str, Any] | None = None,
    reverse_flow_test_validation_lane: dict[str, Any] | None = None,
    local_comparison: dict[str, Any] | None = None,
    selected_step: dict[str, Any] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Apply the unlocked reverse-flow local test lane with focused validation.

    Consumes ``skill_route_discovery_local_apply_completion`` when the reverse-flow
    test lane is unlocked and packages the supervisor next action
    ``apply_unlocked_local_test_lane_with_focused_validation_and_keep_activation_external``
    into one body-free operator surface. This is local validation only: no
    external skill execution, provider launch, remote apply, push, promotion,
    kernel restart, or in-kernel activation.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    completion = local_apply_completion if isinstance(local_apply_completion, dict) else {}
    apply = local_apply if isinstance(local_apply, dict) else {}
    reverse_lane = (
        reverse_flow_test_validation_lane
        if isinstance(reverse_flow_test_validation_lane, dict)
        else {}
    )
    comparison = local_comparison if isinstance(local_comparison, dict) else {}
    selected = selected_step if isinstance(selected_step, dict) else {}
    retained = list(retained_boundaries or [])
    adjacent = list(adjacent_general_agent_rows or [])

    completion_status = str(completion.get("status") or "")
    reverse_status = str(reverse_lane.get("status") or "")
    comparison_passed = bool(
        completion.get("local_comparison_passed")
        if completion.get("local_comparison_passed") is not None
        else comparison.get("local_comparison_passed")
    )
    unlocked = [
        lane
        for lane in list(
            completion.get("unlocked_local_lanes")
            or apply.get("unlocked_local_lanes")
            or selected.get("unlocked_local_lanes")
            or []
        )
        if lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
    ]
    selected_lane = str(
        completion.get("selected_local_lane")
        or apply.get("selected_local_lane")
        or selected.get("selected_local_lane")
        or ""
    )
    selected_proposal_id = str(
        completion.get("selected_proposal_id")
        or apply.get("selected_proposal_id")
        or selected.get("proposal_id")
        or "prop-skill-reverse-flow-test-lane"
    )
    profiles = [
        str(profile)
        for profile in (
            completion.get("route_profiles")
            or apply.get("route_profiles")
            or selected.get("route_profiles")
            or []
        )
    ]
    skill_first = bool(
        completion.get("skill_route_discovery_first")
        if completion.get("skill_route_discovery_first") is not None
        else selected.get("skill_route_discovery_first")
    )
    is_reverse_flow = "codex_workflow_gate" in profiles and skill_first
    selected_route_class = str(
        completion.get("route_class")
        or apply.get("route_class")
        or selected.get("route_class")
        or "none"
    )
    selected_is_adjacent = selected_route_class == "agent_harness_eval_required"
    theme_complete = bool(completion.get("theme_complete"))
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
    ) if adjacent else True
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
    ) if retained else True

    apply_ready = (
        completion_status == "complete"
        and theme_complete
        and comparison_passed
        and reverse_status == "ready"
        and is_reverse_flow
        and selected_lane == "test"
        and "test" in unlocked
        and not selected_is_adjacent
        and general_agent_isolated
        and privacy_isolated
        and completion.get("runtime_action") == "none"
        and completion.get("external_skill_execution_allowed") is not True
        and completion.get("provider_launch_allowed") is not True
        and completion.get("remote_apply_allowed") is not True
        and completion.get("supervisor_activation_allowed") is not True
    )

    if selected_is_adjacent:
        status = "deferred_adjacent_harness_eval"
        decision = "selected_step_is_adjacent_agent_harness_eval_not_unlocked_test_lane_apply"
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_selected_general_agent_row"
        )
    elif not selected_proposal_id or completion_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_completed_reverse_flow_local_apply_for_unlocked_test_lane"
        supervisor_next_action = "wait_for_skill_route_discovery_local_apply_completion"
    elif apply_ready:
        status = "ready"
        decision = (
            "apply_unlocked_local_test_lane_with_focused_validation_and_keep_activation_external"
        )
        supervisor_next_action = (
            "run_focused_local_test_validation_then_keep_activation_external"
        )
    elif completion_status == "deferred_adjacent_harness_eval":
        status = "deferred_adjacent_harness_eval"
        decision = "completion_deferred_to_adjacent_agent_harness_eval"
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_selected_general_agent_row"
        )
    elif completion_status == "blocked":
        status = "blocked_until_local_apply_completion"
        decision = "hold_unlocked_test_lane_apply_until_local_apply_completion"
        supervisor_next_action = (
            "repair_skill_route_discovery_local_apply_before_theme_completion"
        )
    else:
        status = "blocked_until_reverse_flow_test_lane_ready"
        decision = "hold_unlocked_test_lane_apply_until_reverse_flow_lane_and_completion_ready"
        supervisor_next_action = (
            "repair_skill_route_discovery_reverse_flow_test_lane_before_apply"
        )

    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )
    focused_validation_commands = [
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline",
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_unlocked_local_test_lane_apply",
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_local_test_validation",
    ]
    focused_validation_command_hashes = [
        hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
        for command in focused_validation_commands
    ]

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_unlocked_local_test_lane_apply",
        "proposal_track": "prop-skill-reverse-flow-test-lane",
        "legacy_proposal_track": "prop-skill-pipeline-reverse-flow-test",
        "companion_tracks": [
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-rnskill-docs",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_local_skill_route_validation_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
        ],
        "local_apply_completion_status": completion_status,
        "local_apply_status": str(apply.get("status") or "none"),
        "reverse_flow_test_validation_lane_status": reverse_status or "none",
        "local_comparison_passed": comparison_passed,
        "local_comparison_status": str(
            completion.get("local_comparison_status")
            or selected.get("local_comparison_status")
            or comparison.get("status")
            or ""
        ),
        "theme_complete": theme_complete,
        "route_class": selected_route_class if selected_route_class else "none",
        "route_profiles": profiles,
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": selected_lane if selected_lane else "none",
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked if status == "ready" else [],
        "focused_validation": {
            "status": "ready" if status == "ready" else status,
            "lane": "test",
            "required": True,
            "commands": focused_validation_commands if status == "ready" else [],
            "command_hashes": focused_validation_command_hashes if status == "ready" else [],
            "unit_test_signal": status == "ready",
            "coverage_required": False,
        },
        "local_validation_required": True,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids,
        "adjacent_general_agent_proposal_ids": adjacent_ids,
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "source_digest": source_digest or str(completion.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": focused_validation_commands,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def normalize_skill_route_discovery_focused_validation_command_results(
    command_results: list[dict[str, Any]] | None = None,
    *,
    expected_command_hashes: list[str] | None = None,
) -> list[dict[str, Any]]:
    """Normalize operator command results into body-free hash/boolean rows.

    Accepts ``command_hash`` or raw ``command`` text, but never re-exports
    command text, evidence URLs, upstream bodies, or stdout. Rows are
    ``{command_hash, passed, in_expected_set}`` only.
    """

    expected = {
        str(item).strip()
        for item in list(expected_command_hashes or [])
        if str(item).strip()
    }
    rows: list[dict[str, Any]] = []
    for raw in list(command_results or []):
        if not isinstance(raw, dict):
            continue
        command_hash = str(raw.get("command_hash") or "").strip()
        if not command_hash:
            command_text = str(raw.get("command") or "").strip()
            if command_text:
                command_hash = hashlib.sha256(command_text.encode("utf-8")).hexdigest()[:16]
        if not command_hash:
            continue
        rows.append(
            {
                "command_hash": command_hash,
                "passed": bool(raw.get("passed")),
                "in_expected_set": (command_hash in expected) if expected else True,
            }
        )
    return rows


def merge_skill_route_discovery_focused_validation_command_results(
    prior_command_results: list[dict[str, Any]] | None = None,
    new_command_results: list[dict[str, Any]] | None = None,
    *,
    expected_command_hashes: list[str] | None = None,
) -> list[dict[str, Any]]:
    """Merge body-free command-hash rows so partial coverage accumulates across wakes.

    Prior reverse-flow focused validation rows stay until a newer row for the same
    ``command_hash`` replaces them. Output remains body-free
    ``{command_hash, passed, in_expected_set}`` only — no command text, stdout,
    evidence URLs, or upstream bodies. Residual export still requires full
    expected-hash coverage after merge.
    """

    expected = [
        str(item).strip()
        for item in list(expected_command_hashes or [])
        if str(item).strip()
    ]
    prior_rows = normalize_skill_route_discovery_focused_validation_command_results(
        prior_command_results,
        expected_command_hashes=expected,
    )
    new_rows = normalize_skill_route_discovery_focused_validation_command_results(
        new_command_results,
        expected_command_hashes=expected,
    )
    by_hash: dict[str, dict[str, Any]] = {}
    order: list[str] = []
    for row in [*prior_rows, *new_rows]:
        command_hash = str(row.get("command_hash") or "").strip()
        if not command_hash:
            continue
        if command_hash not in by_hash:
            order.append(command_hash)
        by_hash[command_hash] = {
            "command_hash": command_hash,
            "passed": bool(row.get("passed")),
            "in_expected_set": (
                (command_hash in set(expected)) if expected else bool(row.get("in_expected_set", True))
            ),
        }
    return [by_hash[command_hash] for command_hash in order]


def missing_skill_route_discovery_focused_validation_command_hashes(
    *,
    expected_command_hashes: list[str] | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> list[str]:
    """List expected body-free command hashes still missing from recorded results.

    Returns hashes only (no command text). Used by reverse-flow operator_state so
    supervisors can record remaining hashes without re-deriving the expected set
    from command strings.
    """

    expected = [
        str(item).strip()
        for item in list(expected_command_hashes or [])
        if str(item).strip()
    ]
    recorded = {
        str(row.get("command_hash") or "").strip()
        for row in list(command_results or [])
        if isinstance(row, dict) and str(row.get("command_hash") or "").strip()
    }
    return [command_hash for command_hash in expected if command_hash not in recorded]


REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RUN = (
    "run_focused_local_test_validation_then_keep_activation_external"
)
REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RECORD_REMAINING = (
    "record_remaining_reverse_flow_focused_validation_command_hashes_then_keep_activation_external"
)


def recorded_skill_route_discovery_focused_validation_command_hashes(
    *,
    expected_command_hashes: list[str] | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> list[str]:
    """List expected body-free command hashes already present in recorded results.

    Complement of ``missing_skill_route_discovery_focused_validation_command_hashes``.
    Preserves expected-set order. Hash-only (no command text/stdout).
    """

    expected = [
        str(item).strip()
        for item in list(expected_command_hashes or [])
        if str(item).strip()
    ]
    recorded = {
        str(row.get("command_hash") or "").strip()
        for row in list(command_results or [])
        if isinstance(row, dict) and str(row.get("command_hash") or "").strip()
    }
    return [command_hash for command_hash in expected if command_hash in recorded]


def pending_skill_route_discovery_focused_validation_commands(
    *,
    commands: list[str] | None = None,
    command_hashes: list[str] | None = None,
    missing_command_hashes: list[str] | None = None,
) -> list[str]:
    """Map missing body-free hashes back to local command text for continue wakes.

    ``commands`` and ``command_hashes`` are parallel local validation inventories
    (already operator-visible on ready focused validation). Returns only commands
    whose hashes are still missing so multi-wake reverse-flow continue does not
    re-run already-covered hashes. Does not invent command text for orphan hashes.
    """

    command_list = [str(item) for item in list(commands or []) if str(item).strip()]
    hash_list = [
        str(item).strip() for item in list(command_hashes or []) if str(item).strip()
    ]
    missing = {
        str(item).strip()
        for item in list(missing_command_hashes or [])
        if str(item).strip()
    }
    if not missing or not command_list or not hash_list:
        return []
    pending: list[str] = []
    for command, command_hash in zip(command_list, hash_list):
        if command_hash in missing:
            pending.append(command)
    return pending


def pending_skill_route_discovery_focused_validation_work_units(
    *,
    commands: list[str] | None = None,
    command_hashes: list[str] | None = None,
    missing_command_hashes: list[str] | None = None,
) -> list[dict[str, Any]]:
    """Pair pending local command text with body-free hashes for continue wakes.

    Returns ordered ``{command, command_hash, inventory_index}`` work units for
    hashes still missing so supervisors run and record without re-zipping
    ``pending_commands`` against ``missing_command_hashes``. Does not invent
    command text for orphan hashes. Local command text is operator-visible
    validation inventory (not raw evidence URLs, stdout, or upstream bodies).
    """

    command_list = [str(item) for item in list(commands or []) if str(item).strip()]
    hash_list = [
        str(item).strip() for item in list(command_hashes or []) if str(item).strip()
    ]
    missing = {
        str(item).strip()
        for item in list(missing_command_hashes or [])
        if str(item).strip()
    }
    if not missing or not command_list or not hash_list:
        return []
    units: list[dict[str, Any]] = []
    for index, (command, command_hash) in enumerate(zip(command_list, hash_list)):
        if command_hash not in missing:
            continue
        units.append(
            {
                "command": command,
                "command_hash": command_hash,
                "inventory_index": index,
            }
        )
    return units


def materialize_reverse_flow_focused_validation_continue_record_rows(
    *,
    continue_plan: dict[str, Any] | None = None,
    pending_work_units: list[dict[str, Any]] | None = None,
    outcomes: Any = None,
) -> list[dict[str, Any]]:
    """Materialize body-free record rows for continue-plan pending work units only.

    Accepts ``outcomes`` as:
    - mapping of ``command_hash`` (or command text) → ``passed``
    - parallel list of booleans aligned with ``pending_work_units``
    - list of ``{command_hash|command, passed}`` row dicts

    Only pending units become rows so multi-wake reverse-flow continue merges
    remaining coverage without re-recording already-covered hashes. Result rows
    stay body-free (``command_hash``, ``passed``, ``in_expected_set`` only).
    """

    plan = continue_plan if isinstance(continue_plan, dict) else {}
    units: list[dict[str, Any]] = []
    if pending_work_units is not None:
        source_units = list(pending_work_units or [])
    else:
        source_units = list(plan.get("pending_work_units") or [])
    for unit in source_units:
        if not isinstance(unit, dict):
            continue
        command_hash = str(unit.get("command_hash") or "").strip()
        command = str(unit.get("command") or "").strip()
        if not command_hash and not command:
            continue
        if not command_hash and command:
            command_hash = hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
        units.append(
            {
                "command": command,
                "command_hash": command_hash,
                "inventory_index": unit.get("inventory_index"),
            }
        )
    if not units:
        return []

    outcome_by_hash: dict[str, bool] = {}
    outcome_by_command: dict[str, bool] = {}
    if isinstance(outcomes, dict):
        for key, value in outcomes.items():
            key_text = str(key).strip()
            if not key_text:
                continue
            passed = bool(value)
            if " " in key_text or key_text.startswith("pytest"):
                outcome_by_command[key_text] = passed
                outcome_by_hash[
                    hashlib.sha256(key_text.encode("utf-8")).hexdigest()[:16]
                ] = passed
            else:
                outcome_by_hash[key_text] = passed
    elif isinstance(outcomes, list):
        if outcomes and all(isinstance(item, bool) for item in outcomes):
            for unit, passed in zip(units, outcomes):
                outcome_by_hash[str(unit["command_hash"])] = bool(passed)
                command = str(unit.get("command") or "").strip()
                if command:
                    outcome_by_command[command] = bool(passed)
        else:
            for row in outcomes:
                if not isinstance(row, dict):
                    continue
                if "passed" not in row:
                    continue
                passed = bool(row.get("passed"))
                command_hash = str(row.get("command_hash") or "").strip()
                command = str(row.get("command") or "").strip()
                if command_hash:
                    outcome_by_hash[command_hash] = passed
                if command:
                    outcome_by_command[command] = passed
                    if not command_hash:
                        outcome_by_hash[
                            hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
                        ] = passed
    else:
        return []

    rows: list[dict[str, Any]] = []
    for unit in units:
        command_hash = str(unit["command_hash"])
        command = str(unit.get("command") or "").strip()
        if command_hash in outcome_by_hash:
            passed = outcome_by_hash[command_hash]
        elif command and command in outcome_by_command:
            passed = outcome_by_command[command]
        else:
            # Skip units without an outcome so partial wakes stay partial.
            continue
        rows.append(
            {
                "command_hash": command_hash,
                "passed": bool(passed),
                "in_expected_set": True,
            }
        )
    return rows


def resolve_reverse_flow_focused_validation_continue_supervisor_next(
    *,
    focused_local_test_validation: dict[str, Any] | None = None,
) -> str:
    """Resolve reverse-flow-first supervisor_next while focused validation continues.

    Zero partial rows → run the full focused body-free set once.
    Partial coverage → record remaining hashes only (do not re-advertise a full
    re-run). Failed → repair. This keeps operator-visible ``supervisor_next_action``
    aligned with ``reverse_flow_continue_decision`` so residual repair noise cannot
    outrank reverse-flow multi-wake continue.
    """

    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    status = str(focused.get("status") or "")
    focused_validation = (
        focused.get("focused_validation")
        if isinstance(focused.get("focused_validation"), dict)
        else {}
    )
    if status == "failed":
        return "repair_failed_focused_local_test_validation_commands"
    if status == "passed":
        return str(
            focused.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    explicit = str(focused.get("supervisor_next_action") or "").strip()
    partial = bool(focused_validation.get("partial_results_recorded")) or (
        status == "ready"
        and int(focused_validation.get("recorded_result_count") or 0) > 0
        and not bool(focused_validation.get("results_cover_expected"))
    )
    if status == "ready" and partial:
        return REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RECORD_REMAINING
    if explicit in {
        REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RECORD_REMAINING,
        REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RUN,
        "repair_failed_focused_local_test_validation_commands",
        "keep_activation_external_after_focused_local_test_validation",
    }:
        return explicit
    return REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RUN


def build_reverse_flow_focused_validation_continue_plan(
    *,
    focused_local_test_validation: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Package one operator-visible reverse-flow focused-validation continue wake.

    Unifies zero-row first wakes and multi-wake partial continue around the same
    pending inventory: supervisors always run/record only ``pending_commands`` /
    ``missing_command_hashes`` without re-deriving the full focused command list
    from nested packets. Residual export stays denied while ready/unrecorded or
    failed. Body-free only (no stdout, evidence URLs, or upstream bodies).
    """

    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    status = str(focused.get("status") or "")
    focused_validation = (
        focused.get("focused_validation")
        if isinstance(focused.get("focused_validation"), dict)
        else {}
    )
    commands = [
        str(item)
        for item in list(focused_validation.get("commands") or [])
        if str(item).strip()
    ]
    command_hashes = [
        str(item).strip()
        for item in list(focused_validation.get("command_hashes") or [])
        if str(item).strip()
    ]
    result_rows = [
        row
        for row in list(focused_validation.get("command_results") or [])
        if isinstance(row, dict)
    ]
    recorded_hashes = list(
        focused_validation.get("recorded_command_hashes")
        or recorded_skill_route_discovery_focused_validation_command_hashes(
            expected_command_hashes=command_hashes,
            command_results=result_rows,
        )
    )
    recorded_hashes = [str(item).strip() for item in recorded_hashes if str(item).strip()]
    missing_hashes = list(
        focused_validation.get("missing_command_hashes")
        or missing_skill_route_discovery_focused_validation_command_hashes(
            expected_command_hashes=command_hashes,
            command_results=result_rows,
        )
    )
    missing_hashes = [str(item).strip() for item in missing_hashes if str(item).strip()]
    pending_commands = list(
        focused_validation.get("pending_commands")
        or pending_skill_route_discovery_focused_validation_commands(
            commands=commands,
            command_hashes=command_hashes,
            missing_command_hashes=missing_hashes,
        )
    )
    pending_commands = [str(item) for item in pending_commands if str(item).strip()]
    pending_work_units = list(
        focused_validation.get("pending_work_units")
        or pending_skill_route_discovery_focused_validation_work_units(
            commands=commands,
            command_hashes=command_hashes,
            missing_command_hashes=missing_hashes,
        )
    )
    pending_work_units = [
        unit
        for unit in pending_work_units
        if isinstance(unit, dict)
        and (
            str(unit.get("command_hash") or "").strip()
            or str(unit.get("command") or "").strip()
        )
    ]
    # Prefer paired units as the source of truth for pending inventory text/hashes.
    if pending_work_units:
        pending_commands = [
            str(unit.get("command") or "").strip()
            for unit in pending_work_units
            if str(unit.get("command") or "").strip()
        ]
        missing_hashes = [
            str(unit.get("command_hash") or "").strip()
            for unit in pending_work_units
            if str(unit.get("command_hash") or "").strip()
        ]
    results_cover_expected = bool(focused_validation.get("results_cover_expected"))
    recorded_result_count = int(
        focused_validation.get("recorded_result_count")
        if focused_validation.get("recorded_result_count") is not None
        else len(result_rows)
    )
    expected_command_count = int(
        focused_validation.get("expected_command_count")
        if focused_validation.get("expected_command_count") is not None
        else len(command_hashes)
    )
    partial_results_recorded = bool(focused_validation.get("partial_results_recorded")) or (
        status == "ready"
        and recorded_result_count > 0
        and not results_cover_expected
    )
    supervisor_next = resolve_reverse_flow_focused_validation_continue_supervisor_next(
        focused_local_test_validation=focused,
    )
    # Residual fortress export is owned by the residual-active cascade after
    # activation-external acceptance. This continue plan never pre-authorizes it.
    residual_export_allowed = False
    mode = "wait"
    reverse_flow_continue_decision = "none"
    if status == "failed":
        mode = "repair"
        reverse_flow_continue_decision = (
            "repair_failed_focused_local_test_validation_commands"
        )
    elif status == "passed":
        mode = "keep_activation_external"
        reverse_flow_continue_decision = str(
            focused.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
        # Passed continue plans do not re-advertise pending work.
        pending_commands = []
        pending_work_units = []
        missing_hashes = []
    elif status == "ready":
        if partial_results_recorded:
            mode = "record_remaining"
            reverse_flow_continue_decision = (
                "record_remaining_reverse_flow_focused_validation_command_hashes_before_residual_export"
            )
        else:
            mode = "run_pending"
            reverse_flow_continue_decision = (
                "record_or_close_reverse_flow_focused_validation_before_residual_export"
            )
    elif status.startswith("blocked") or status in {"", "not_applicable"}:
        mode = "wait"
        reverse_flow_continue_decision = str(focused.get("decision") or "wait")
        pending_commands = []
        pending_work_units = []
        missing_hashes = []
        recorded_hashes = []

    return {
        "schema_version": 1,
        "controller_surface": "reverse_flow_focused_validation_continue_plan",
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "status": status or "none",
        "mode": mode,
        "decision": reverse_flow_continue_decision,
        "supervisor_next_action": supervisor_next,
        "reverse_flow_continue_decision": reverse_flow_continue_decision,
        "partial_results_recorded": partial_results_recorded,
        "results_cover_expected": results_cover_expected,
        "expected_command_count": expected_command_count,
        "recorded_result_count": recorded_result_count,
        "recorded_command_hashes": recorded_hashes,
        "recorded_command_hash_count": len(recorded_hashes),
        "missing_command_hashes": missing_hashes,
        "missing_command_hash_count": len(missing_hashes),
        "pending_commands": pending_commands,
        "pending_command_count": len(pending_commands),
        "pending_work_units": pending_work_units,
        "pending_work_unit_count": len(pending_work_units),
        "residual_export_allowed": residual_export_allowed,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "reverse_flow_focused_validation_continue_local_command_allowed",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "materialize_reverse_flow_focused_validation_continue_record_rows",
            "merge_skill_route_discovery_focused_validation_command_results",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
            "pending_skill_route_discovery_focused_validation_work_units",
        ]
        if status in {"ready", "passed", "failed"}
        else [],
    }


def record_skill_route_discovery_focused_local_test_validation_results(
    pipeline: dict[str, Any],
    command_results: list[dict[str, Any]] | None = None,
    *,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Record body-free focused validation results onto an existing pipeline.

    After supervisors run the reverse-flow focused local test commands, call this
    with command-hash/boolean rows (or raw command text + ``passed``) to close
    ``skill_route_discovery_focused_local_test_validation`` from ``ready`` to
    ``passed``/``failed`` and refresh
    ``skill_route_discovery_focused_validation_activation_external_handoff``,
    ``skill_route_discovery_focused_validation_activation_external_acceptance``,
    ``skill_route_discovery_focused_validation_residual_adjacent_queue``,
    ``skill_route_discovery_residual_adjacent_harness_eval_local_apply``,
    ``skill_route_discovery_residual_adjacent_harness_eval_local_comparison``,
    ``skill_route_discovery_residual_adjacent_unlocked_local_lane_apply``,
    ``skill_route_discovery_residual_adjacent_focused_local_validation``,
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``,
    and
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance``
    without enabling activation, push, promotion, provider launch, remote apply,
    external skill execution, or kernel restart.

    Partial body-free rows already on the pipeline are merged with the new rows
    so reverse-flow coverage can accumulate across wakes; later rows for the same
    hash win. Residual export remains denied until results cover the expected set.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    unlocked_apply = (
        pipeline.get("unlocked_local_test_lane_apply")
        if isinstance(pipeline.get("unlocked_local_test_lane_apply"), dict)
        else {}
    )
    prior_focused = (
        pipeline.get("focused_local_test_validation")
        if isinstance(pipeline.get("focused_local_test_validation"), dict)
        else {}
    )
    theme_pass = (
        pipeline.get("theme_pass") if isinstance(pipeline.get("theme_pass"), dict) else {}
    )
    theme = theme_window if isinstance(theme_window, dict) else {
        "theme_id": str(pipeline.get("theme_id") or theme_pass.get("theme_id") or "skill-route-discovery"),
        "planned_passes": int(theme_pass.get("planned_passes") or 0),
        "target_passes": int(
            theme_pass.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES
        ),
        "status": str(theme_pass.get("status") or ""),
    }
    expected_hashes = [
        str(item)
        for item in list(
            (prior_focused.get("focused_validation") or {}).get("command_hashes")
            or (unlocked_apply.get("focused_validation") or {}).get("command_hashes")
            or []
        )
        if str(item).strip()
    ]
    prior_results = list(
        (prior_focused.get("focused_validation") or {}).get("command_results") or []
        if isinstance(prior_focused.get("focused_validation"), dict)
        else []
    )
    merged = merge_skill_route_discovery_focused_validation_command_results(
        prior_results,
        command_results,
        expected_command_hashes=expected_hashes,
    )
    focused = build_skill_route_discovery_focused_local_test_validation(
        unlocked_local_test_lane_apply=unlocked_apply,
        command_results=merged,
        theme_window=theme,
        source_digest=source_digest
        or str(prior_focused.get("source_digest") or unlocked_apply.get("source_digest") or ""),
    )
    adjacent_rows = (
        list(pipeline.get("adjacent_general_agent_rows") or [])
        if isinstance(pipeline.get("adjacent_general_agent_rows"), list)
        else []
    )
    retained_rows = (
        list(pipeline.get("retained_boundaries") or [])
        if isinstance(pipeline.get("retained_boundaries"), list)
        else []
    )
    digest = source_digest or str(
        prior_focused.get("source_digest") or unlocked_apply.get("source_digest") or ""
    )
    activation_external = build_skill_route_discovery_focused_validation_activation_external_handoff(
        focused_local_test_validation=focused,
        unlocked_local_test_lane_apply=unlocked_apply,
        adjacent_general_agent_rows=adjacent_rows,
        retained_boundaries=retained_rows,
        theme_window=theme,
        source_digest=digest,
    )
    activation_external_acceptance = (
        build_skill_route_discovery_focused_validation_activation_external_acceptance(
            focused_validation_activation_external_handoff=activation_external,
            focused_local_test_validation=focused,
            unlocked_local_test_lane_apply=unlocked_apply,
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_queue = (
        build_skill_route_discovery_focused_validation_residual_adjacent_queue(
            focused_validation_activation_external_acceptance=(
                activation_external_acceptance
            ),
            focused_validation_activation_external_handoff=activation_external,
            focused_local_test_validation=focused,
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_harness_eval_local_apply = (
        build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
            focused_validation_residual_adjacent_queue=residual_adjacent_queue,
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_harness_eval_local_comparison = (
        build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
            residual_adjacent_harness_eval_local_apply=(
                residual_adjacent_harness_eval_local_apply
            ),
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_unlocked_local_lane_apply = (
        build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply(
            residual_adjacent_harness_eval_local_comparison=(
                residual_adjacent_harness_eval_local_comparison
            ),
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_focused_local_validation = (
        build_skill_route_discovery_residual_adjacent_focused_local_validation(
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_focused_validation_activation_external_handoff = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff(
            residual_adjacent_focused_local_validation=(
                residual_adjacent_focused_local_validation
            ),
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_adjacent_focused_validation_activation_external_acceptance = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance(
            residual_adjacent_focused_validation_activation_external_handoff=(
                residual_adjacent_focused_validation_activation_external_handoff
            ),
            residual_adjacent_focused_local_validation=(
                residual_adjacent_focused_local_validation
            ),
            residual_adjacent_unlocked_local_lane_apply=(
                residual_adjacent_unlocked_local_lane_apply
            ),
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    updated = dict(pipeline)
    updated["focused_local_test_validation"] = focused
    updated["focused_local_test_validation_recorded"] = focused.get("status") in {
        "passed",
        "failed",
    }
    updated["focused_validation_activation_external_handoff"] = activation_external
    updated["focused_validation_activation_external_acceptance"] = (
        activation_external_acceptance
    )
    updated["focused_validation_residual_adjacent_queue"] = residual_adjacent_queue
    updated["residual_adjacent_harness_eval_local_apply"] = (
        residual_adjacent_harness_eval_local_apply
    )
    updated["residual_adjacent_harness_eval_local_comparison"] = (
        residual_adjacent_harness_eval_local_comparison
    )
    updated["residual_adjacent_unlocked_local_lane_apply"] = (
        residual_adjacent_unlocked_local_lane_apply
    )
    updated["residual_adjacent_focused_local_validation"] = (
        residual_adjacent_focused_local_validation
    )
    updated["residual_adjacent_focused_local_validation_recorded"] = (
        residual_adjacent_focused_local_validation.get("status") in {"passed", "failed"}
    )
    updated["residual_adjacent_focused_validation_activation_external_handoff"] = (
        residual_adjacent_focused_validation_activation_external_handoff
    )
    updated["residual_adjacent_focused_validation_activation_external_acceptance"] = (
        residual_adjacent_focused_validation_activation_external_acceptance
    )
    return attach_skill_route_discovery_pipeline_operator_state(updated)


def build_skill_route_discovery_focused_validation_body_free_command_results(
    focused_local_test_validation: dict[str, Any] | None = None,
    *,
    passed: bool,
    command_hashes: list[str] | None = None,
) -> list[dict[str, Any]]:
    """Materialize body-free expected-hash result rows for focused validation.

    Supervisors that already know the reverse-flow focused commands passed or
    failed can build ``{command_hash, passed, in_expected_set}`` rows covering
    the expected hash set without re-exporting command text, stdout, evidence
    URLs, or upstream bodies.
    """

    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    focused_validation = (
        focused.get("focused_validation")
        if isinstance(focused.get("focused_validation"), dict)
        else {}
    )
    hashes = [
        str(item).strip()
        for item in list(
            command_hashes
            or focused_validation.get("command_hashes")
            or focused.get("required_validation")
            or []
        )
        if str(item).strip()
    ]
    # If callers supplied raw commands instead of hashes, hash them body-free.
    if hashes and any(" " in item or item.startswith("pytest") for item in hashes):
        hashes = [
            hashlib.sha256(item.encode("utf-8")).hexdigest()[:16]
            if (" " in item or item.startswith("pytest"))
            else item
            for item in hashes
        ]
    # Deduplicate while preserving order.
    seen: set[str] = set()
    ordered: list[str] = []
    for item in hashes:
        if item in seen:
            continue
        seen.add(item)
        ordered.append(item)
    return [
        {
            "command_hash": command_hash,
            "passed": bool(passed),
            "in_expected_set": True,
        }
        for command_hash in ordered
    ]


def close_skill_route_discovery_focused_local_test_validation_with_outcome(
    pipeline: dict[str, Any],
    *,
    passed: bool,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Close ready focused validation with a body-free pass/fail outcome.

    Materializes expected command-hash rows, records them onto the pipeline,
    refreshes ``skill_route_discovery_focused_validation_activation_external_handoff``,
    and packages
    ``skill_route_discovery_focused_validation_activation_external_acceptance``
    when the handoff is ready after a recorded pass. Never enables activation,
    push, promotion, provider launch, remote apply, external skill execution, or
    kernel restart.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    prior_focused = (
        pipeline.get("focused_local_test_validation")
        if isinstance(pipeline.get("focused_local_test_validation"), dict)
        else {}
    )
    command_results = build_skill_route_discovery_focused_validation_body_free_command_results(
        prior_focused,
        passed=passed,
    )
    return record_skill_route_discovery_focused_local_test_validation_results(
        pipeline,
        command_results,
        source_digest=source_digest,
        theme_window=theme_window,
    )


def record_reverse_flow_focused_validation_continue_outcomes(
    pipeline: dict[str, Any],
    outcomes: Any,
    *,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Record continue-plan pending work unit outcomes onto a reverse-flow pipeline.

    Integration seam for run_pending / record_remaining wakes: supervisors run
    only ``continue_plan.pending_work_units`` (command + hash pairs), supply
    per-unit outcomes, and this helper materializes body-free rows then merges
    them through ``record_skill_route_discovery_focused_local_test_validation_results``.
    Already-covered hashes stay untouched; residual export remains denied until
    coverage is complete. Never enables activation, push, promotion, provider
    launch, remote apply, external skill execution, or kernel restart.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    prior_focused = (
        pipeline.get("focused_local_test_validation")
        if isinstance(pipeline.get("focused_local_test_validation"), dict)
        else {}
    )
    continue_plan = (
        prior_focused.get("continue_plan")
        if isinstance(prior_focused.get("continue_plan"), dict)
        else None
    )
    if not isinstance(continue_plan, dict):
        continue_plan = build_reverse_flow_focused_validation_continue_plan(
            focused_local_test_validation=prior_focused,
        )
    command_results = materialize_reverse_flow_focused_validation_continue_record_rows(
        continue_plan=continue_plan,
        outcomes=outcomes,
    )
    return record_skill_route_discovery_focused_local_test_validation_results(
        pipeline,
        command_results,
        source_digest=source_digest,
        theme_window=theme_window,
    )


REVERSE_FLOW_FOCUSED_VALIDATION_CONTINUE_LOCAL_COMMAND_PREFIXES = (
    "pytest ",
    "python -m pytest ",
    "python3 -m pytest ",
    "py -m pytest ",
)
REVERSE_FLOW_FOCUSED_VALIDATION_CONTINUE_SHELL_METACHARS = (
    ";",
    "&&",
    "||",
    "|",
    "`",
    "$(",
    "\n",
    "\r",
    ">",
    "<",
)


def reverse_flow_focused_validation_continue_local_command_allowed(
    command: str | None,
) -> bool:
    """Return True when a pending unit is a local pytest inventory command.

    Continue-run is not a general shell executor. Only body-free local pytest
    inventory lines that target ``tests/`` and contain no shell metacharacters
    are auto-runnable. Remote apply, network tools, and compound shells stay denied.
    """

    text = str(command or "").strip()
    if not text:
        return False
    for token in REVERSE_FLOW_FOCUSED_VALIDATION_CONTINUE_SHELL_METACHARS:
        if token in text:
            return False
    lowered = text.lower()
    if not any(
        lowered.startswith(prefix)
        for prefix in REVERSE_FLOW_FOCUSED_VALIDATION_CONTINUE_LOCAL_COMMAND_PREFIXES
    ):
        return False
    if "tests/" not in text and "tests\\" not in text:
        return False
    return True


def _resolve_reverse_flow_focused_validation_continue_plan(
    *,
    continue_plan: dict[str, Any] | None = None,
    focused_local_test_validation: dict[str, Any] | None = None,
    pipeline: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Resolve the operator-visible reverse-flow continue plan from common inputs."""

    if isinstance(continue_plan, dict) and continue_plan:
        return continue_plan
    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    if not focused and isinstance(pipeline, dict):
        focused = (
            pipeline.get("focused_local_test_validation")
            if isinstance(pipeline.get("focused_local_test_validation"), dict)
            else {}
        )
    plan = focused.get("continue_plan") if isinstance(focused.get("continue_plan"), dict) else None
    if isinstance(plan, dict) and plan:
        return plan
    if focused:
        return build_reverse_flow_focused_validation_continue_plan(
            focused_local_test_validation=focused,
        )
    return {}


def build_reverse_flow_focused_validation_continue_run_plan(
    *,
    continue_plan: dict[str, Any] | None = None,
    focused_local_test_validation: dict[str, Any] | None = None,
    pipeline: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Package pending work units as an inspectable local continue-run plan.

    Does not execute commands. Each unit is annotated with ``local_allowed`` and
    optional ``skip_reason`` so supervisors can audit what would run under
    ``mode=run_pending`` / ``mode=record_remaining``. Body-free only (no stdout,
    evidence URLs, or upstream bodies). Activation and residual export stay denied.
    """

    plan = _resolve_reverse_flow_focused_validation_continue_plan(
        continue_plan=continue_plan,
        focused_local_test_validation=focused_local_test_validation,
        pipeline=pipeline,
    )
    mode = str(plan.get("mode") or "none")
    status = str(plan.get("status") or "none")
    units: list[dict[str, Any]] = []
    for unit in list(plan.get("pending_work_units") or []):
        if not isinstance(unit, dict):
            continue
        command = str(unit.get("command") or "").strip()
        command_hash = str(unit.get("command_hash") or "").strip()
        if not command_hash and command:
            command_hash = hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
        inventory_index = unit.get("inventory_index")
        skip_reason = ""
        local_allowed = reverse_flow_focused_validation_continue_local_command_allowed(
            command
        )
        if not command:
            local_allowed = False
            skip_reason = "missing_command"
        elif not local_allowed:
            skip_reason = "command_not_local_pytest_inventory"
        elif command and command_hash:
            expected_hash = hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
            if command_hash != expected_hash:
                local_allowed = False
                skip_reason = "command_hash_mismatch"
        units.append(
            {
                "command": command,
                "command_hash": command_hash,
                "inventory_index": inventory_index,
                "local_allowed": bool(local_allowed),
                "skip_reason": skip_reason,
            }
        )
    runnable = [unit for unit in units if unit["local_allowed"]]
    skipped = [unit for unit in units if not unit["local_allowed"]]
    executable = mode in {"run_pending", "record_remaining"} and bool(runnable)
    # One body-free line supervisors can log before execute without nested re-assembly.
    exec_plan_line = (
        f"plan mode={mode} "
        f"runnable={len(runnable)} "
        f"skipped={len(skipped)} "
        f"executable={'true' if executable else 'false'} "
        f"residual_export=false"
    )
    return {
        "schema_version": 1,
        "controller_surface": "reverse_flow_focused_validation_continue_run_plan",
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "status": status,
        "mode": mode,
        "decision": str(plan.get("decision") or plan.get("reverse_flow_continue_decision") or "none"),
        "supervisor_next_action": str(plan.get("supervisor_next_action") or "none"),
        "pending_work_units": units,
        "pending_work_unit_count": len(units),
        "runnable_work_units": runnable,
        "runnable_work_unit_count": len(runnable),
        "skipped_work_units": skipped,
        "skipped_work_unit_count": len(skipped),
        "executable": executable,
        "exec_plan_line": exec_plan_line,
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "reverse_flow_focused_validation_continue_local_command_allowed",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "materialize_reverse_flow_focused_validation_continue_record_rows",
            "build_reverse_flow_focused_validation_continue_plan",
        ],
    }


def execute_reverse_flow_focused_validation_continue_run_plan(
    run_plan: dict[str, Any] | None,
    *,
    command_runner: Any = subprocess.run,
    cwd: Path | str | None = None,
    timeout: int = 120,
) -> dict[str, Any]:
    """Execute allowed continue-run units and return body-free outcomes only.

    Runs local pytest inventory lines without shell expansion. Does not record
    onto a pipeline and never exports stdout/stderr, evidence URLs, or upstream
    bodies. Skipped units leave no outcome so partial wakes stay partial.
    """

    plan = run_plan if isinstance(run_plan, dict) else {}
    workdir = Path(cwd) if cwd is not None else Path.cwd()
    unit_results: list[dict[str, Any]] = []
    outcomes: dict[str, bool] = {}
    for unit in list(plan.get("pending_work_units") or []):
        if not isinstance(unit, dict):
            continue
        command = str(unit.get("command") or "").strip()
        command_hash = str(unit.get("command_hash") or "").strip()
        if not command_hash and command:
            command_hash = hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
        local_allowed = bool(unit.get("local_allowed"))
        skip_reason = str(unit.get("skip_reason") or "").strip()
        if not local_allowed:
            unit_results.append(
                {
                    "command_hash": command_hash,
                    "passed": False,
                    "ran": False,
                    "skipped": True,
                    "skip_reason": skip_reason or "not_local_allowed",
                    "in_expected_set": True,
                }
            )
            continue
        if not reverse_flow_focused_validation_continue_local_command_allowed(command):
            unit_results.append(
                {
                    "command_hash": command_hash,
                    "passed": False,
                    "ran": False,
                    "skipped": True,
                    "skip_reason": "command_not_local_pytest_inventory",
                    "in_expected_set": True,
                }
            )
            continue
        try:
            # Inventory commands use POSIX-style pytest paths; split without shell.
            argv = shlex.split(command, posix=True)
        except ValueError:
            unit_results.append(
                {
                    "command_hash": command_hash,
                    "passed": False,
                    "ran": False,
                    "skipped": True,
                    "skip_reason": "command_parse_error",
                    "in_expected_set": True,
                }
            )
            continue
        if not argv:
            unit_results.append(
                {
                    "command_hash": command_hash,
                    "passed": False,
                    "ran": False,
                    "skipped": True,
                    "skip_reason": "empty_argv",
                    "in_expected_set": True,
                }
            )
            continue
        try:
            completed = command_runner(
                argv,
                cwd=str(workdir),
                capture_output=True,
                text=True,
                timeout=timeout,
            )
            returncode = int(getattr(completed, "returncode", 1) or 0)
            passed = returncode == 0
        except Exception:
            passed = False
        outcomes[command_hash] = bool(passed)
        unit_results.append(
            {
                "command_hash": command_hash,
                "passed": bool(passed),
                "ran": True,
                "skipped": False,
                "skip_reason": "",
                "in_expected_set": True,
            }
        )
    ran_count = sum(1 for row in unit_results if row.get("ran"))
    passed_count = sum(1 for row in unit_results if row.get("ran") and row.get("passed"))
    failed_count = sum(1 for row in unit_results if row.get("ran") and not row.get("passed"))
    skipped_count = sum(1 for row in unit_results if row.get("skipped"))
    mode = str(plan.get("mode") or "none")
    # Partial receipt without record flag; run_and_record / dispatch re-package with recorded.
    exec_receipt = package_reverse_flow_focused_validation_continue_exec_receipt(
        run_plan=plan,
        run_result={
            "mode": mode,
            "ran_count": ran_count,
            "passed_count": passed_count,
            "failed_count": failed_count,
            "skipped_count": skipped_count,
            "outcome_count": len(outcomes),
            "outcomes": outcomes,
        },
        recorded=False,
        executed=True,
    )
    return {
        "schema_version": 1,
        "controller_surface": "reverse_flow_focused_validation_continue_run_result",
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "mode": mode,
        "outcomes": outcomes,
        "unit_results": unit_results,
        "ran_count": ran_count,
        "passed_count": passed_count,
        "failed_count": failed_count,
        "skipped_count": skipped_count,
        "outcome_count": len(outcomes),
        "exec_receipt": exec_receipt,
        "exec_line": str(exec_receipt.get("exec_line") or ""),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "materialize_reverse_flow_focused_validation_continue_record_rows",
        ],
    }


def resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
    *,
    pipeline: dict[str, Any] | None = None,
    continue_plan: dict[str, Any] | None = None,
    run_plan: dict[str, Any] | None = None,
    run_result: dict[str, Any] | None = None,
    recorded: bool = False,
) -> dict[str, Any]:
    """Resolve reverse-flow-first supervisor_next after continue-run inventory/run.

    Packages one inspectable wake from continue-plan mode, optional continue-run
    plan/result, and the post-record pipeline so residual fortress stages stay
    blocked until reverse-flow record/close and activation-external acceptance
    complete. Does not execute commands, export stdout, or enable activation.
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    plan = run_plan if isinstance(run_plan, dict) else {}
    result = run_result if isinstance(run_result, dict) else {}
    continue_pkt = (
        continue_plan
        if isinstance(continue_plan, dict) and continue_plan
        else plan
        if plan
        else _resolve_reverse_flow_focused_validation_continue_plan(pipeline=pipe)
    )
    focused = (
        pipe.get("focused_local_test_validation")
        if isinstance(pipe.get("focused_local_test_validation"), dict)
        else {}
    )
    focused_status = str(focused.get("status") or continue_pkt.get("status") or "none")
    focused_continue = (
        focused.get("continue_plan")
        if isinstance(focused.get("continue_plan"), dict)
        else {}
    )
    # Prefer nested continue_plan mode from post-record focused packet when present.
    mode = str(
        focused_continue.get("mode")
        or continue_pkt.get("mode")
        or plan.get("mode")
        or "none"
    )
    handoff = (
        pipe.get("focused_validation_activation_external_handoff")
        if isinstance(pipe.get("focused_validation_activation_external_handoff"), dict)
        else {}
    )
    acceptance = (
        pipe.get("focused_validation_activation_external_acceptance")
        if isinstance(pipe.get("focused_validation_activation_external_acceptance"), dict)
        else {}
    )
    handoff_status = str(handoff.get("status") or "none")
    acceptance_status = str(acceptance.get("status") or "none")
    runnable_count = int(
        plan.get("runnable_work_unit_count")
        if plan.get("runnable_work_unit_count") is not None
        else len(list(plan.get("runnable_work_units") or []))
    )
    if runnable_count == 0 and not plan:
        # Inventory without execute: annotate local-allowed units from continue plan.
        inventory_plan = build_reverse_flow_focused_validation_continue_run_plan(
            continue_plan=continue_pkt if continue_pkt else None,
            pipeline=pipe if pipe else None,
        )
        runnable_count = int(inventory_plan.get("runnable_work_unit_count") or 0)
        plan = inventory_plan if not plan else plan
        if mode in {"none", ""} and inventory_plan.get("mode"):
            mode = str(inventory_plan.get("mode") or mode)
    ran_count = int(result.get("ran_count") or 0)
    outcome_count = int(result.get("outcome_count") or len(result.get("outcomes") or {}))
    failed_count = int(result.get("failed_count") or 0)
    skipped_count = int(result.get("skipped_count") or 0)

    if focused_status == "failed" or mode == "repair":
        supervisor_next = "repair_failed_focused_local_test_validation_commands"
        decision = "repair_failed_focused_local_test_validation_commands"
        residual_hold_reason = "blocked_until_focused_validation_repaired"
    elif focused_status == "passed" or mode == "keep_activation_external":
        supervisor_next = str(
            focused.get("supervisor_next_action")
            or handoff.get("supervisor_next_action")
            or acceptance.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
        if acceptance_status == "accepted":
            decision = "keep_activation_external_and_queue_residual_adjacent_harness_eval"
        elif handoff_status == "ready":
            decision = "accept_activation_external_package_after_focused_validation_pass"
        else:
            decision = "package_activation_external_handoff_after_focused_validation_pass"
        residual_hold_reason = (
            "released_after_reverse_flow_focused_validation_pass"
            if acceptance_status == "accepted"
            else "blocked_until_activation_external_acceptance"
            if handoff_status == "ready"
            else "blocked_until_activation_external_handoff_ready"
        )
    elif mode == "record_remaining" or (
        focused_status == "ready"
        and int(
            (focused.get("focused_validation") or {}).get("recorded_result_count") or 0
        )
        > 0
        and not bool(
            (focused.get("focused_validation") or {}).get("results_cover_expected")
        )
    ):
        supervisor_next = (
            REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RECORD_REMAINING
        )
        decision = (
            "record_remaining_reverse_flow_focused_validation_command_hashes_before_residual_export"
        )
        residual_hold_reason = "blocked_until_focused_validation_recorded"
    elif mode == "run_pending" or focused_status == "ready":
        supervisor_next = REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RUN
        decision = (
            "record_or_close_reverse_flow_focused_validation_before_residual_export"
        )
        residual_hold_reason = "blocked_until_focused_validation_recorded"
    else:
        supervisor_next = str(
            continue_pkt.get("supervisor_next_action")
            or pipe.get("supervisor_next_action")
            or "none"
        )
        decision = str(continue_pkt.get("decision") or "none")
        residual_hold_reason = "wait"

    # Residual fortress export is never authorized from the continue-run wake.
    # Pipeline residual_export_allowed may become true only after reverse-flow
    # acceptance + residual-active cascade; surface it as informational only.
    pipeline_residual_export_allowed = bool(pipe.get("residual_export_allowed"))
    reverse_flow_still_holds_residual = focused_status in {
        "ready",
        "failed",
        "",
        "none",
    } or residual_hold_reason.startswith("blocked_until_")
    residual_export_allowed = False
    continue_run_recommended = mode in {"run_pending", "record_remaining"} and (
        runnable_count > 0 or (not recorded and focused_status == "ready")
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_run_supervisor_wake"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "status": focused_status,
        "mode": mode,
        "decision": decision,
        "supervisor_next_action": supervisor_next,
        "recorded": bool(recorded),
        "ran_count": ran_count,
        "outcome_count": outcome_count,
        "failed_count": failed_count,
        "skipped_count": skipped_count,
        "runnable_work_unit_count": runnable_count,
        "continue_run_recommended": bool(continue_run_recommended),
        "continue_run_executable": bool(
            mode in {"run_pending", "record_remaining"} and runnable_count > 0
        ),
        "activation_external_handoff_status": handoff_status,
        "activation_external_acceptance_status": acceptance_status,
        "residual_hold_active": bool(reverse_flow_still_holds_residual),
        "residual_hold_reason": residual_hold_reason,
        "residual_export_allowed": residual_export_allowed,
        "pipeline_residual_export_allowed": pipeline_residual_export_allowed,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "reverse_flow_focused_validation_continue_local_command_allowed",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
        ],
    }


def run_reverse_flow_focused_validation_continue_pending_work_units(
    pipeline: dict[str, Any],
    *,
    command_runner: Any = subprocess.run,
    cwd: Path | str | None = None,
    timeout: int = 120,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
    record: bool = True,
) -> dict[str, Any]:
    """Build, execute, and optionally record reverse-flow continue pending units.

    Operator-visible integration seam for ``mode=run_pending`` and
    ``mode=record_remaining`` wakes: package the continue-run plan from
    ``continue_plan.pending_work_units``, run only local-allowed pytest inventory
    lines, materialize body-free outcomes, and merge through
    ``record_reverse_flow_focused_validation_continue_outcomes`` when ``record``
    is true. After execute/record, packages
    ``resolve_reverse_flow_focused_validation_continue_run_supervisor_wake`` so
    supervisor_next stays reverse-flow-first before residual fortress stages.
    Never enables activation, push, promotion, provider launch, remote apply,
    external skill execution, or kernel restart. Does not export stdout.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    run_plan = build_reverse_flow_focused_validation_continue_run_plan(pipeline=pipeline)
    run_result = execute_reverse_flow_focused_validation_continue_run_plan(
        run_plan,
        command_runner=command_runner,
        cwd=cwd,
        timeout=timeout,
    )
    updated_pipeline = pipeline
    recorded = False
    if record and run_result.get("outcomes"):
        updated_pipeline = record_reverse_flow_focused_validation_continue_outcomes(
            pipeline,
            run_result.get("outcomes"),
            source_digest=source_digest,
            theme_window=theme_window,
        )
        recorded = True
    supervisor_wake = resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
        pipeline=updated_pipeline,
        run_plan=run_plan,
        run_result=run_result,
        recorded=recorded,
    )
    residual_hold_active = bool(supervisor_wake.get("residual_hold_active", True))
    exec_receipt = package_reverse_flow_focused_validation_continue_exec_receipt(
        run_plan=run_plan,
        run_result=run_result,
        recorded=recorded,
        executed=True,
        residual_hold_active=residual_hold_active,
    )
    # Re-stamp run_result receipt with post-record residual hold and recorded flag.
    if isinstance(run_result, dict):
        run_result = dict(run_result)
        run_result["exec_receipt"] = exec_receipt
        run_result["exec_line"] = str(exec_receipt.get("exec_line") or "")
    return {
        "schema_version": 1,
        "controller_surface": "reverse_flow_focused_validation_continue_run_and_record",
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "run_plan": run_plan,
        "run_result": run_result,
        "exec_receipt": exec_receipt,
        "exec_line": str(exec_receipt.get("exec_line") or ""),
        "exec_plan_line": str(
            exec_receipt.get("exec_plan_line")
            or run_plan.get("exec_plan_line")
            or ""
        ),
        "pipeline": updated_pipeline,
        "recorded": recorded,
        "record_requested": bool(record),
        "supervisor_wake": supervisor_wake,
        "supervisor_next_action": str(
            supervisor_wake.get("supervisor_next_action") or "none"
        ),
        "continue_plan_mode": str(supervisor_wake.get("mode") or "none"),
        "focused_validation_status": str(supervisor_wake.get("status") or "none"),
        "activation_external_handoff_status": str(
            supervisor_wake.get("activation_external_handoff_status") or "none"
        ),
        "activation_external_acceptance_status": str(
            supervisor_wake.get("activation_external_acceptance_status") or "none"
        ),
        "residual_hold_active": residual_hold_active,
        "residual_export_allowed": bool(
            supervisor_wake.get("residual_export_allowed")
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "exec_receipt_helper": (
            "package_reverse_flow_focused_validation_continue_exec_receipt"
        ),
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "record_reverse_flow_focused_validation_continue_outcomes",
        ],
    }


def package_reverse_flow_focused_validation_continue_dispatch_inventory(
    *,
    pipeline: dict[str, Any] | None = None,
    inventory_wake: dict[str, Any] | None = None,
    execute_requested: bool = False,
    record_requested: bool = True,
) -> dict[str, Any]:
    """Package body-free inventory dispatch without running commands.

    Durable reverse-flow-first surface for supervisors deciding whether to call
    ``dispatch_reverse_flow_focused_validation_continue_supervisor_wake(...,
    execute=True)``. Derives ``action`` from the inventory wake (or resolves one
    from ``pipeline``) so residual fortress stages stay subordinate and residual
    export remains denied. Does not execute commands, export stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart.
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    wake = inventory_wake if isinstance(inventory_wake, dict) and inventory_wake else None
    if wake is None:
        wake = resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
            pipeline=pipe,
        )
    continue_run_executable = bool(wake.get("continue_run_executable"))
    mode = str(wake.get("mode") or "none")
    status = str(wake.get("status") or "none")
    if status == "failed" or mode == "repair":
        action = "repair"
    elif status == "passed" or mode == "keep_activation_external":
        action = "keep_activation_external"
    elif continue_run_executable:
        action = "inventory_only"
    elif bool(wake.get("continue_run_recommended")):
        action = "inventory_not_executable"
    elif status == "ready":
        action = "inventory_only"
    else:
        action = "noop"
    execute_recommended = bool(
        continue_run_executable
        and action in {"inventory_only", "inventory_not_executable"}
        and mode in {"run_pending", "record_remaining"}
    )
    # inventory_not_executable still recommends execute=False until units become local-allowed.
    if action == "inventory_not_executable":
        execute_recommended = False
    elif action == "inventory_only" and continue_run_executable:
        execute_recommended = True

    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_supervisor_dispatch"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "action": action,
        "executed": False,
        "execute_requested": bool(execute_requested),
        "execute_recommended": bool(execute_recommended),
        "inventory_wake": dict(wake),
        "run_and_record": None,
        "pipeline": pipe,
        "recorded": False,
        "record_requested": bool(record_requested),
        "run_plan": None,
        "run_result": None,
        "supervisor_wake": dict(wake),
        "supervisor_next_action": str(wake.get("supervisor_next_action") or "none"),
        "continue_plan_mode": mode,
        "focused_validation_status": status,
        "activation_external_handoff_status": str(
            wake.get("activation_external_handoff_status") or "none"
        ),
        "activation_external_acceptance_status": str(
            wake.get("activation_external_acceptance_status") or "none"
        ),
        "residual_hold_active": bool(wake.get("residual_hold_active", True)),
        "residual_export_allowed": False,
        "continue_run_executable": continue_run_executable,
        "continue_run_recommended": bool(wake.get("continue_run_recommended")),
        "runnable_work_unit_count": int(wake.get("runnable_work_unit_count") or 0),
        "dispatch_helper": (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        ),
        "inventory_helper": (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "reverse_flow_focused_validation_continue_local_command_allowed",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
        ],
    }


def resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
    *,
    inventory_dispatch: dict[str, Any] | None = None,
    pipeline: dict[str, Any] | None = None,
    execute_requested: bool | None = None,
) -> dict[str, Any]:
    """Map inventory dispatch into one durable follow-through decision.

    Collapses ``action`` + ``execute_recommended`` into
    ``follow_through_action`` and ``call_dispatch_with_execute`` so supervisors
    do not re-derive execute policy from nested wake fields. Residual export
    stays denied. Does not execute commands, export stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart.

    ``follow_through_action`` values:
    - ``execute_now`` — inventory_only with allowlisted units
    - ``wait_for_local_allowlist`` — units exist but are not local-allowed
    - ``keep_activation_external`` — reverse-flow already passed / keep path
    - ``repair`` — focused validation failed
    - ``noop`` — no reverse-flow continue work
    """

    packet = (
        inventory_dispatch
        if isinstance(inventory_dispatch, dict) and inventory_dispatch
        else package_reverse_flow_focused_validation_continue_dispatch_inventory(
            pipeline=pipeline if isinstance(pipeline, dict) else {},
            execute_requested=bool(execute_requested)
            if execute_requested is not None
            else False,
            record_requested=True,
        )
    )
    action = str(packet.get("action") or "noop")
    execute_recommended = bool(packet.get("execute_recommended"))
    mode = str(packet.get("continue_plan_mode") or packet.get("mode") or "none")
    status = str(packet.get("focused_validation_status") or packet.get("status") or "none")

    if action == "repair" or status == "failed" or mode == "repair":
        follow_through_action = "repair"
    elif action == "keep_activation_external" or mode == "keep_activation_external":
        follow_through_action = "keep_activation_external"
    elif action == "inventory_not_executable":
        follow_through_action = "wait_for_local_allowlist"
    elif action == "inventory_only" and (
        execute_recommended or bool(packet.get("continue_run_executable"))
    ):
        follow_through_action = "execute_now"
    elif action == "inventory_only":
        follow_through_action = "wait_for_local_allowlist"
    else:
        follow_through_action = "noop"

    # execute_requested=None follows the durable recommendation; explicit bool
    # forces inventory-only or execute attempt without re-deriving action.
    if execute_requested is False:
        call_dispatch_with_execute = False
    elif execute_requested is True:
        call_dispatch_with_execute = follow_through_action == "execute_now"
    else:
        call_dispatch_with_execute = follow_through_action == "execute_now"

    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_dispatch_follow_through"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "follow_through_action": follow_through_action,
        "call_dispatch_with_execute": bool(call_dispatch_with_execute),
        "inventory_action": action,
        "execute_recommended": bool(execute_recommended),
        "execute_requested": execute_requested,
        "continue_plan_mode": mode,
        "focused_validation_status": status,
        "supervisor_next_action": str(packet.get("supervisor_next_action") or "none"),
        "continue_run_executable": bool(packet.get("continue_run_executable")),
        "continue_run_recommended": bool(packet.get("continue_run_recommended")),
        "runnable_work_unit_count": int(packet.get("runnable_work_unit_count") or 0),
        "residual_hold_active": bool(packet.get("residual_hold_active", True)),
        "residual_export_allowed": False,
        "dispatch_helper": (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        ),
        "inventory_helper": (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "resolve_helper": (
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_operator_card",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
        ],
    }


def package_reverse_flow_focused_validation_continue_operator_card(
    *,
    pipeline: dict[str, Any] | None = None,
    state: dict[str, Any] | None = None,
    follow_through: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse reverse-flow continue progress + policy into one body-free card.

    Supervisors read this single surface for recorded/expected progress (for
    example ``0/3``), ``follow_through_action``, preferred helper, residual hold,
    and ``supervisor_next_action`` without re-deriving nested operator_state or
    wake fields. Does not execute commands, export stdout, or enable activation,
    push, promotion, provider launch, remote apply, external skill execution, or
    kernel restart.
    """

    source: dict[str, Any] = state if isinstance(state, dict) and state else {}
    if not source and isinstance(pipeline, dict) and pipeline:
        nested = pipeline.get("operator_state")
        if isinstance(nested, dict) and nested:
            source = nested
        elif (
            "reverse_flow_focused_validation_recorded_result_count" in pipeline
            or "reverse_flow_focused_validation_continue_dispatch_follow_through_action"
            in pipeline
            or "supervisor_next_action" in pipeline
        ):
            source = pipeline
        else:
            source = resolve_skill_route_discovery_pipeline_operator_state(pipeline)

    ft = (
        follow_through
        if isinstance(follow_through, dict) and follow_through
        else (
            source.get("reverse_flow_focused_validation_continue_dispatch_follow_through")
            if isinstance(
                source.get(
                    "reverse_flow_focused_validation_continue_dispatch_follow_through"
                ),
                dict,
            )
            else {}
        )
    )
    recorded = int(
        source.get("reverse_flow_focused_validation_recorded_result_count") or 0
    )
    expected = int(
        source.get("reverse_flow_focused_validation_expected_command_count") or 0
    )
    missing = int(
        source.get("reverse_flow_focused_validation_missing_command_hash_count") or 0
    )
    pending_units = int(
        source.get("reverse_flow_focused_validation_pending_work_unit_count") or 0
    )
    follow_through_action = str(
        (ft or {}).get("follow_through_action")
        or source.get(
            "reverse_flow_focused_validation_continue_dispatch_follow_through_action"
        )
        or "none"
    )
    call_with_execute = bool(
        (ft or {}).get("call_dispatch_with_execute")
        if (ft or {}).get("call_dispatch_with_execute") is not None
        else source.get(
            "reverse_flow_focused_validation_continue_dispatch_call_with_execute"
        )
    )
    progress_label = f"{recorded}/{expected}"
    results_cover = bool(
        source.get("reverse_flow_focused_validation_results_cover_expected")
    )
    progress_complete = bool(results_cover and expected > 0 and missing == 0)
    residual_hold_active = bool(
        source.get(
            "residual_adjacent_held_until_reverse_flow_focused_validation_recorded"
        )
        if source.get(
            "residual_adjacent_held_until_reverse_flow_focused_validation_recorded"
        )
        is not None
        else (ft or {}).get("residual_hold_active", True)
    )
    preferred_helper = "follow_reverse_flow_focused_validation_continue_dispatch"
    if follow_through_action == "repair":
        preferred_helper = (
            "record_skill_route_discovery_focused_local_test_validation_results"
        )
    elif follow_through_action in {"keep_activation_external", "noop"}:
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
            if follow_through_action == "keep_activation_external"
            else "package_reverse_flow_focused_validation_continue_operator_card"
        )
    elif follow_through_action == "wait_for_local_allowlist":
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )

    supervisor_next_action = str(
        source.get("supervisor_next_action")
        or (ft or {}).get("supervisor_next_action")
        or "none"
    )
    # One body-free line supervisors can log without re-assembling nested fields.
    action_line = (
        f"progress={progress_label} "
        f"action={follow_through_action} "
        f"call_execute={'true' if call_with_execute else 'false'} "
        f"helper={preferred_helper} "
        f"residual_hold={'true' if residual_hold_active else 'false'} "
        f"next={supervisor_next_action}"
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_operator_card"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "progress_label": progress_label,
        "progress_complete": progress_complete,
        "recorded_result_count": recorded,
        "expected_command_count": expected,
        "missing_command_hash_count": missing,
        "pending_work_unit_count": pending_units,
        "continue_plan_mode": str(
            source.get("reverse_flow_focused_validation_continue_plan_mode")
            or (ft or {}).get("continue_plan_mode")
            or "none"
        ),
        "follow_through_action": follow_through_action,
        "call_dispatch_with_execute": call_with_execute,
        "supervisor_next_action": supervisor_next_action,
        "action_line": action_line,
        "reverse_flow_continue_decision": str(
            source.get("reverse_flow_continue_decision") or "none"
        ),
        "ready_unrecorded": bool(
            source.get("reverse_flow_focused_validation_ready_unrecorded")
        ),
        "partial_results_recorded": bool(
            source.get("reverse_flow_focused_validation_partial_results_recorded")
        ),
        "continue_run_executable": bool(
            source.get("reverse_flow_focused_validation_continue_run_executable")
            if source.get("reverse_flow_focused_validation_continue_run_executable")
            is not None
            else (ft or {}).get("continue_run_executable")
        ),
        "continue_run_recommended": bool(
            source.get("reverse_flow_focused_validation_continue_run_recommended")
            if source.get("reverse_flow_focused_validation_continue_run_recommended")
            is not None
            else (ft or {}).get("continue_run_recommended")
        ),
        "runnable_work_unit_count": int(
            source.get("reverse_flow_focused_validation_continue_runnable_work_unit_count")
            if source.get(
                "reverse_flow_focused_validation_continue_runnable_work_unit_count"
            )
            is not None
            else (ft or {}).get("runnable_work_unit_count")
            or 0
        ),
        "residual_hold_active": residual_hold_active,
        "residual_export_allowed": False,
        "preferred_helper": preferred_helper,
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "resolve_helper": (
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
        ),
        "inventory_helper": (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        ),
        "dispatch_helper": (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        ),
        "operator_card_helper": (
            "package_reverse_flow_focused_validation_continue_operator_card"
        ),
        "progress_transition_helper": (
            "package_reverse_flow_focused_validation_continue_progress_transition"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_progress_transition",
            "package_reverse_flow_focused_validation_continue_operator_card",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
        ],
    }


def package_reverse_flow_focused_validation_continue_progress_transition(
    *,
    pre_card: dict[str, Any] | None = None,
    post_card: dict[str, Any] | None = None,
    pre_follow_through: dict[str, Any] | None = None,
    post_follow_through: dict[str, Any] | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse pre/post operator cards into one body-free progress transition.

    Supervisors read ``progress_transition_label`` (for example ``0/3→3/3``),
    ``progress_advanced``, ``follow_through_transition``, and residual hold
    after a continue wake without comparing nested pre/post cards. Does not
    execute commands, export stdout, or enable activation, push, promotion,
    provider launch, remote apply, external skill execution, or kernel restart.
    """

    pre = pre_card if isinstance(pre_card, dict) else {}
    post = post_card if isinstance(post_card, dict) and post_card else dict(pre)
    pre_ft = pre_follow_through if isinstance(pre_follow_through, dict) else {}
    post_ft = post_follow_through if isinstance(post_follow_through, dict) else {}

    progress_from = str(pre.get("progress_label") or "0/0")
    progress_to = str(post.get("progress_label") or progress_from)
    recorded_from = int(pre.get("recorded_result_count") or 0)
    recorded_to = int(post.get("recorded_result_count") or recorded_from)
    expected = int(
        post.get("expected_command_count")
        if post.get("expected_command_count") is not None
        else pre.get("expected_command_count")
        or 0
    )
    progress_advanced = progress_to != progress_from or recorded_to > recorded_from
    progress_complete_after = bool(post.get("progress_complete"))
    pre_action = str(
        pre.get("follow_through_action")
        or pre_ft.get("follow_through_action")
        or "none"
    )
    post_action = str(
        post.get("follow_through_action")
        or post_ft.get("follow_through_action")
        or pre_action
    )
    residual_hold_after = bool(
        post.get("residual_hold_active")
        if post.get("residual_hold_active") is not None
        else pre.get("residual_hold_active", True)
    )
    progress_transition_label = f"{progress_from}→{progress_to}"
    follow_through_transition = f"{pre_action}→{post_action}"
    transition_line = (
        f"transition={progress_transition_label} "
        f"follow={follow_through_transition} "
        f"advanced={'true' if progress_advanced else 'false'} "
        f"complete={'true' if progress_complete_after else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_hold={'true' if residual_hold_after else 'false'} "
        f"residual_export=false"
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_progress_transition"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "progress_transition_label": progress_transition_label,
        "progress_from": progress_from,
        "progress_to": progress_to,
        "progress_advanced": bool(progress_advanced),
        "progress_complete_after": progress_complete_after,
        "recorded_from": recorded_from,
        "recorded_to": recorded_to,
        "recorded_delta": max(0, recorded_to - recorded_from),
        "expected_command_count": expected,
        "follow_through_from": pre_action,
        "follow_through_to": post_action,
        "follow_through_transition": follow_through_transition,
        "executed": bool(executed),
        "recorded": bool(recorded),
        "residual_hold_active_after": residual_hold_after,
        "residual_export_allowed": False,
        "supervisor_next_action_after": str(
            post.get("supervisor_next_action")
            or pre.get("supervisor_next_action")
            or "none"
        ),
        "transition_line": transition_line,
        "progress_transition_helper": (
            "package_reverse_flow_focused_validation_continue_progress_transition"
        ),
        "operator_card_helper": (
            "package_reverse_flow_focused_validation_continue_operator_card"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_progress_transition",
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "package_reverse_flow_focused_validation_continue_operator_card",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
        ],
    }


def package_reverse_flow_focused_validation_continue_exec_receipt(
    *,
    run_plan: dict[str, Any] | None = None,
    run_result: dict[str, Any] | None = None,
    recorded: bool = False,
    executed: bool = False,
    residual_hold_active: bool | None = None,
) -> dict[str, Any]:
    """Collapse continue-run plan/result into one body-free exec receipt.

    Supervisors read ``exec_line`` (for example
    ``exec mode=run_pending ran=3 passed=3 failed=0 skipped=0 recorded=true``)
    without re-deriving nested ``run_result`` / ``unit_results`` fields after a
    continue execute wake. Does not export stdout, evidence URLs, or upstream
    bodies. Residual export stays denied; activation, push, promotion, provider
    launch, remote apply, external skill execution, and kernel restart stay
    denied.
    """

    plan = run_plan if isinstance(run_plan, dict) else {}
    result = run_result if isinstance(run_result, dict) else {}
    mode = str(
        result.get("mode")
        or plan.get("mode")
        or "none"
    )
    ran_count = int(result.get("ran_count") or 0)
    passed_count = int(result.get("passed_count") or 0)
    failed_count = int(result.get("failed_count") or 0)
    skipped_count = int(result.get("skipped_count") or 0)
    outcome_count = int(
        result.get("outcome_count")
        if result.get("outcome_count") is not None
        else len(result.get("outcomes") or {})
    )
    runnable_count = int(
        plan.get("runnable_work_unit_count")
        if plan.get("runnable_work_unit_count") is not None
        else len(list(plan.get("runnable_work_units") or []))
    )
    all_passed = bool(executed and ran_count > 0 and failed_count == 0 and ran_count == passed_count)
    residual_hold = (
        bool(residual_hold_active)
        if residual_hold_active is not None
        else True
    )
    exec_plan_line = str(
        plan.get("exec_plan_line")
        or (
            f"plan mode={mode} "
            f"runnable={runnable_count} "
            f"skipped={int(plan.get('skipped_work_unit_count') or 0)} "
            f"executable={'true' if plan.get('executable') else 'false'} "
            f"residual_export=false"
        )
    )
    exec_line = (
        f"exec mode={mode} "
        f"ran={ran_count} "
        f"passed={passed_count} "
        f"failed={failed_count} "
        f"skipped={skipped_count} "
        f"recorded={'true' if recorded else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"residual_hold={'true' if residual_hold else 'false'} "
        f"residual_export=false"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_exec_receipt"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "mode": mode,
        "executed": bool(executed),
        "recorded": bool(recorded),
        "ran_count": ran_count,
        "passed_count": passed_count,
        "failed_count": failed_count,
        "skipped_count": skipped_count,
        "outcome_count": outcome_count,
        "runnable_work_unit_count": runnable_count,
        "all_passed": all_passed,
        "residual_hold_active": residual_hold,
        "residual_export_allowed": False,
        "exec_plan_line": exec_plan_line,
        "exec_line": exec_line,
        "exec_receipt_helper": (
            "package_reverse_flow_focused_validation_continue_exec_receipt"
        ),
        "run_plan_helper": (
            "build_reverse_flow_focused_validation_continue_run_plan"
        ),
        "run_result_helper": (
            "execute_reverse_flow_focused_validation_continue_run_plan"
        ),
        "run_and_record_helper": (
            "run_reverse_flow_focused_validation_continue_pending_work_units"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_finish_receipt",
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "package_reverse_flow_focused_validation_continue_progress_transition",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_finish_receipt(
    *,
    pipeline: dict[str, Any] | None = None,
    post_operator_card: dict[str, Any] | None = None,
    progress_transition: dict[str, Any] | None = None,
    exec_receipt: dict[str, Any] | None = None,
    supervisor_wake: dict[str, Any] | None = None,
    focused_validation_status: str | None = None,
    activation_external_handoff_status: str | None = None,
    activation_external_acceptance_status: str | None = None,
    residual_hold_active: bool | None = None,
    continue_plan_mode: str | None = None,
    supervisor_next_action: str | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse post-continue reverse-flow state into one body-free finish receipt.

    Supervisors read ``finish_line`` (for example
    ``finish complete=true progress=3/3 status=passed mode=keep_activation_external
    handoff=ready acceptance=accepted residual_hold=false residual_queue=ready
    residual_export=false next=keep_activation_external_after_...``) without
    re-deriving nested post_operator_card / handoff / acceptance / residual hold
    fields after a continue wake finishes. Residual export stays denied on this
    surface even when ``residual_queue_ready`` is true (residual stages open via
    the residual pipeline, not residual_export on continue surfaces). Does not
    execute commands, export stdout, or enable activation, push, promotion,
    provider launch, remote apply, external skill execution, or kernel restart.
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    card = post_operator_card if isinstance(post_operator_card, dict) else {}
    transition = progress_transition if isinstance(progress_transition, dict) else {}
    receipt = exec_receipt if isinstance(exec_receipt, dict) else {}
    wake = supervisor_wake if isinstance(supervisor_wake, dict) else {}

    if not card and pipe:
        nested_card = pipe.get("reverse_flow_focused_validation_continue_operator_card")
        if isinstance(nested_card, dict):
            card = nested_card
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_card = nested_state.get(
                    "reverse_flow_focused_validation_continue_operator_card"
                )
                if isinstance(nested_card, dict):
                    card = nested_card

    if not wake and pipe:
        nested_wake = pipe.get("reverse_flow_focused_validation_continue_supervisor_wake")
        if isinstance(nested_wake, dict):
            wake = nested_wake

    focused = (
        pipe.get("focused_local_test_validation")
        if isinstance(pipe.get("focused_local_test_validation"), dict)
        else {}
    )
    handoff = (
        pipe.get("focused_validation_activation_external_handoff")
        if isinstance(pipe.get("focused_validation_activation_external_handoff"), dict)
        else {}
    )
    acceptance = (
        pipe.get("focused_validation_activation_external_acceptance")
        if isinstance(pipe.get("focused_validation_activation_external_acceptance"), dict)
        else {}
    )

    progress_label = str(
        card.get("progress_label")
        or transition.get("progress_to")
        or "0/0"
    )
    progress_complete = bool(
        card.get("progress_complete")
        if card.get("progress_complete") is not None
        else transition.get("progress_complete_after")
    )
    status = str(
        focused_validation_status
        or wake.get("status")
        or focused.get("status")
        or card.get("focused_validation_status")
        or "none"
    )
    focused_continue = (
        focused.get("continue_plan")
        if isinstance(focused.get("continue_plan"), dict)
        else {}
    )
    mode = str(
        continue_plan_mode
        or wake.get("mode")
        or card.get("continue_plan_mode")
        or focused_continue.get("mode")
        or "none"
    )
    handoff_status = str(
        activation_external_handoff_status
        or wake.get("activation_external_handoff_status")
        or handoff.get("status")
        or "none"
    )
    acceptance_status = str(
        activation_external_acceptance_status
        or wake.get("activation_external_acceptance_status")
        or acceptance.get("status")
        or "none"
    )
    residual_hold = (
        bool(residual_hold_active)
        if residual_hold_active is not None
        else bool(
            wake.get("residual_hold_active")
            if wake.get("residual_hold_active") is not None
            else card.get("residual_hold_active", True)
        )
    )
    next_action = str(
        supervisor_next_action
        or wake.get("supervisor_next_action")
        or card.get("supervisor_next_action")
        or transition.get("supervisor_next_action_after")
        or "none"
    )
    executed_flag = bool(
        executed
        or receipt.get("executed")
        or transition.get("executed")
    )
    recorded_flag = bool(
        recorded
        or receipt.get("recorded")
        or transition.get("recorded")
    )
    # Continue finished only when reverse-flow focused validation passed and
    # progress covers expected hashes (mode keep_activation_external).
    continue_finished = bool(
        progress_complete
        and status == "passed"
        and mode in {"keep_activation_external", "none"}
        and (mode == "keep_activation_external" or progress_complete)
    )
    if status == "passed" and progress_complete:
        continue_finished = True
        if mode in {"none", ""}:
            mode = "keep_activation_external"
    # Residual queue may open via residual pipeline stages after acceptance;
    # residual_export stays denied on continue surfaces themselves.
    residual_queue_ready = bool(
        continue_finished and acceptance_status == "accepted"
    )
    residual_queue_state = (
        "ready"
        if residual_queue_ready
        else "blocked"
        if residual_hold or not continue_finished
        else "pending_acceptance"
        if continue_finished and acceptance_status != "accepted"
        else "blocked"
    )
    finish_line = (
        f"finish complete={'true' if continue_finished else 'false'} "
        f"progress={progress_label} "
        f"status={status} "
        f"mode={mode} "
        f"handoff={handoff_status} "
        f"acceptance={acceptance_status} "
        f"residual_hold={'true' if residual_hold else 'false'} "
        f"residual_queue={residual_queue_state} "
        f"residual_export=false "
        f"executed={'true' if executed_flag else 'false'} "
        f"recorded={'true' if recorded_flag else 'false'} "
        f"next={next_action}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_finish_receipt"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "continue_finished": bool(continue_finished),
        "progress_complete": bool(progress_complete),
        "progress_label": progress_label,
        "focused_validation_status": status,
        "continue_plan_mode": mode,
        "activation_external_handoff_status": handoff_status,
        "activation_external_acceptance_status": acceptance_status,
        "residual_hold_active": bool(residual_hold),
        "residual_queue_ready": bool(residual_queue_ready),
        "residual_queue_state": residual_queue_state,
        "residual_export_allowed": False,
        "executed": bool(executed_flag),
        "recorded": bool(recorded_flag),
        "supervisor_next_action": next_action,
        "finish_line": finish_line,
        "finish_receipt_helper": (
            "package_reverse_flow_focused_validation_continue_finish_receipt"
        ),
        "exec_receipt_helper": (
            "package_reverse_flow_focused_validation_continue_exec_receipt"
        ),
        "progress_transition_helper": (
            "package_reverse_flow_focused_validation_continue_progress_transition"
        ),
        "operator_card_helper": (
            "package_reverse_flow_focused_validation_continue_operator_card"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_finish_receipt",
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "package_reverse_flow_focused_validation_continue_progress_transition",
            "package_reverse_flow_focused_validation_continue_operator_card",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_open(
    *,
    pipeline: dict[str, Any] | None = None,
    finish_receipt: dict[str, Any] | None = None,
    residual_adjacent_queue: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse finish residual-queue readiness into one body-free residual open card.

    After reverse-flow continue finish packages ``residual_queue_ready=true``,
    supervisors still re-derived residual adjacent queue status, residual proposal
    ID counts, residual next action, and residual apply helper from nested
    ``focused_validation_residual_adjacent_queue`` fields. This surface packages
    a body-free ``residual_open_line`` (for example
    ``residual_open ready=true count=1 status=ready
    next=hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply
    residual_export=false
    helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_apply``)
    so residual entry is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual open
    is ready; residual stages open via residual pipeline helpers only. Does not
    execute residual apply, export raw evidence URLs/bodies/stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart.
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    finish = finish_receipt if isinstance(finish_receipt, dict) else {}
    residual_queue = (
        residual_adjacent_queue
        if isinstance(residual_adjacent_queue, dict)
        else {}
    )

    if not finish and pipe:
        nested_finish = pipe.get(
            "reverse_flow_focused_validation_continue_finish_receipt"
        )
        if isinstance(nested_finish, dict):
            finish = nested_finish
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_finish = nested_state.get(
                    "reverse_flow_focused_validation_continue_finish_receipt"
                )
                if isinstance(nested_finish, dict):
                    finish = nested_finish

    if not residual_queue and pipe:
        nested_queue = pipe.get("focused_validation_residual_adjacent_queue")
        if isinstance(nested_queue, dict):
            residual_queue = nested_queue

    continue_finished = bool(finish.get("continue_finished"))
    residual_queue_ready = bool(finish.get("residual_queue_ready"))
    residual_hold = bool(
        finish.get("residual_hold_active")
        if finish.get("residual_hold_active") is not None
        else True
    )
    residual_queue_status = str(residual_queue.get("status") or "none")
    residual_queue_decision = str(residual_queue.get("decision") or "none")
    residual_ids = [
        str(item)
        for item in list(residual_queue.get("adjacent_general_agent_proposal_ids") or [])
        if str(item).strip()
    ]
    # Only advertise residual IDs when residual queue itself is residual-active.
    if residual_queue_status != "ready":
        residual_ids = []
    residual_count = len(residual_ids)
    residual_open = bool(
        residual_queue_ready
        and residual_queue_status == "ready"
        and residual_count > 0
    )
    if residual_open:
        residual_next = str(
            residual_queue.get("supervisor_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
        residual_open_state = "ready"
    elif residual_queue_ready and residual_queue_status in {
        "not_applicable",
        "blocked",
    }:
        residual_next = str(
            residual_queue.get("supervisor_next_action")
            or finish.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
        residual_open_state = residual_queue_status
    elif residual_queue_ready and continue_finished:
        residual_next = str(
            finish.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
        residual_open_state = "pending_residual_queue"
    else:
        residual_next = str(
            finish.get("supervisor_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )
        residual_open_state = "blocked"
    residual_open_line = (
        f"residual_open ready={'true' if residual_open else 'false'} "
        f"count={residual_count} "
        f"status={residual_open_state} "
        f"queue_status={residual_queue_status} "
        f"continue_finished={'true' if continue_finished else 'false'} "
        f"residual_hold={'true' if residual_hold else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper="
        f"{'build_skill_route_discovery_residual_adjacent_harness_eval_local_apply' if residual_open else 'package_reverse_flow_focused_validation_continue_residual_open'}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_open"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_open": bool(residual_open),
        "residual_open_state": residual_open_state,
        "continue_finished": bool(continue_finished),
        "residual_queue_ready": bool(residual_queue_ready),
        "residual_hold_active": bool(residual_hold),
        "residual_queue_status": residual_queue_status,
        "residual_queue_decision": residual_queue_decision,
        "residual_adjacent_count": residual_count,
        "residual_adjacent_proposal_ids": list(residual_ids),
        "residual_next_action": residual_next,
        "residual_open_line": residual_open_line,
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "finish_receipt_helper": (
            "package_reverse_flow_focused_validation_continue_finish_receipt"
        ),
        "residual_queue_helper": (
            "build_skill_route_discovery_focused_validation_residual_adjacent_queue"
        ),
        "residual_apply_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_open",
            "package_reverse_flow_focused_validation_continue_finish_receipt",
            "build_skill_route_discovery_focused_validation_residual_adjacent_queue",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_entry(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_harness_eval_local_apply: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse residual open + residual apply selection into one residual entry card.

    After residual open is ready (``residual_open ready=true``), supervisors still
    re-derived selected residual proposal ID, residual apply status/decision, and
    residual apply next action by calling
    ``build_skill_route_discovery_residual_adjacent_harness_eval_local_apply`` and
    reading nested residual-queue selection fields. This surface packages a
    body-free ``residual_entry_line`` (for example
    ``residual_entry ready=true selected=prop-harness-fortress-local-eval
    status=ready count=1
    next=run_agent_harness_eval_local_comparison_for_residual_adjacent_row
    residual_export=false
    helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_apply``)
    so residual pipeline selection is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual entry
    is ready; residual stages open via residual pipeline helpers only. Does not
    execute residual apply, export raw evidence URLs/bodies/stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Selected residual IDs stay empty while residual
    open is blocked (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    residual_apply = (
        residual_adjacent_harness_eval_local_apply
        if isinstance(residual_adjacent_harness_eval_local_apply, dict)
        else {}
    )

    if not open_card and pipe:
        nested_open = pipe.get("reverse_flow_focused_validation_continue_residual_open")
        if isinstance(nested_open, dict):
            open_card = nested_open
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_open = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_open"
                )
                if isinstance(nested_open, dict):
                    open_card = nested_open
        if not open_card:
            open_card = package_reverse_flow_focused_validation_continue_residual_open(
                pipeline=pipe,
            )

    residual_open_ready = bool(open_card.get("residual_open"))
    residual_count = int(open_card.get("residual_adjacent_count") or 0)
    residual_open_state = str(open_card.get("residual_open_state") or "blocked")

    if not residual_apply and pipe:
        nested_apply = pipe.get("residual_adjacent_harness_eval_local_apply")
        if isinstance(nested_apply, dict):
            residual_apply = nested_apply

    # When residual open is ready but residual apply is not yet nested, build it
    # from the residual adjacent queue so selected fortress/Hy3 IDs are available
    # without supervisors re-deriving selection.
    if residual_open_ready and not residual_apply:
        residual_queue = pipe.get("focused_validation_residual_adjacent_queue")
        if not isinstance(residual_queue, dict):
            residual_queue = {}
        residual_apply = build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
            focused_validation_residual_adjacent_queue=residual_queue,
            adjacent_general_agent_rows=(
                list(pipe.get("adjacent_general_agent_rows") or [])
                if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                else None
            ),
            retained_boundaries=(
                list(pipe.get("retained_boundaries") or [])
                if isinstance(pipe.get("retained_boundaries"), list)
                else None
            ),
            theme_window=(
                pipe.get("theme_window")
                if isinstance(pipe.get("theme_window"), dict)
                else None
            ),
            source_digest=(
                str(pipe.get("source_digest") or "") or None
            ),
        )

    residual_apply_status = str(residual_apply.get("status") or "none")
    residual_apply_decision = str(residual_apply.get("decision") or "none")
    # Hold selected residual ID until residual open is ready (reverse-flow-waiting).
    if residual_open_ready:
        selected_residual = str(
            residual_apply.get("selected_residual_proposal_id")
            or residual_apply.get("selected_proposal_id")
            or residual_apply.get("proposal_id")
            or ""
        ).strip()
        # Prefer residual-active export only; if residual apply is still blocked
        # until queue criteria, keep selection empty unless status is residual-active.
        selected_residual = _export_residual_selected_proposal_id(
            residual_apply_status if residual_apply_status != "none" else "ready",
            selected_residual,
            residual_active_statuses={"ready", "blocked"},
        )
        # If residual apply package was ready with selection, keep it even when
        # status key was already residual-active ready.
        if not selected_residual and residual_apply_status in {"ready", "blocked"}:
            selected_residual = str(
                residual_apply.get("selected_residual_proposal_id")
                or residual_apply.get("selected_proposal_id")
                or ""
            ).strip()
    else:
        selected_residual = ""

    residual_entry = bool(
        residual_open_ready
        and residual_apply_status == "ready"
        and bool(selected_residual)
    )
    if residual_entry:
        residual_entry_state = "ready"
        residual_next = str(
            residual_apply.get("supervisor_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        )
    elif residual_open_ready and residual_apply_status in {"blocked", "not_applicable"}:
        residual_entry_state = residual_apply_status
        residual_next = str(
            residual_apply.get("supervisor_next_action")
            or open_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
    elif residual_open_ready:
        residual_entry_state = residual_apply_status or residual_open_state
        residual_next = str(
            residual_apply.get("supervisor_next_action")
            or open_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
    else:
        residual_entry_state = "blocked"
        residual_next = str(
            open_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )

    residual_entry_line = (
        f"residual_entry ready={'true' if residual_entry else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_entry_state} "
        f"count={residual_count if residual_open_ready else 0} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"apply_status={residual_apply_status} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_entry"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_entry": bool(residual_entry),
        "residual_entry_state": residual_entry_state,
        "residual_open_ready": bool(residual_open_ready),
        "residual_open_state": residual_open_state,
        "selected_residual_proposal_id": selected_residual or "",
        "residual_adjacent_count": residual_count if residual_open_ready else 0,
        "residual_apply_status": residual_apply_status,
        "residual_apply_decision": residual_apply_decision,
        "residual_next_action": residual_next,
        "residual_entry_line": residual_entry_line,
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_apply_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        ),
        "residual_comparison_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_follow(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse residual entry into one body-free residual follow-through card.

    After residual entry is ready (``residual_entry ready=true selected=...``),
    supervisors still re-derived whether to open residual harness-eval local
    comparison vs wait for residual entry vs wait for reverse-flow by reading
    nested residual_entry / residual_open fields. This surface packages a
    body-free ``residual_follow_line`` (for example
    ``residual_follow ready=true selected=prop-harness-fortress-local-eval
    action=open_residual_harness_eval_local_comparison call_comparison=true
    residual_export=false
    next=run_agent_harness_eval_local_comparison_for_residual_adjacent_row
    helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison``)
    so residual stage follow-through is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    follow is ready; ``call_residual_comparison`` is informational policy only.
    Residual stages open via residual pipeline helpers only. Does not execute
    residual comparison, export raw evidence URLs/bodies/stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Selected residual IDs stay empty while residual
    entry is blocked (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}

    if not entry_card and pipe:
        nested_entry = pipe.get(
            "reverse_flow_focused_validation_continue_residual_entry"
        )
        if isinstance(nested_entry, dict):
            entry_card = nested_entry
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_entry = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_entry"
                )
                if isinstance(nested_entry, dict):
                    entry_card = nested_entry
        if not entry_card:
            entry_card = package_reverse_flow_focused_validation_continue_residual_entry(
                pipeline=pipe,
                residual_open=open_card if open_card else None,
            )

    if not open_card and pipe:
        nested_open = pipe.get("reverse_flow_focused_validation_continue_residual_open")
        if isinstance(nested_open, dict):
            open_card = nested_open
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_open = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_open"
                )
                if isinstance(nested_open, dict):
                    open_card = nested_open
        if not open_card and isinstance(entry_card, dict):
            # Prefer residual_open fields already nested on residual_entry.
            open_card = {
                "residual_open": bool(entry_card.get("residual_open_ready")),
                "residual_open_state": str(
                    entry_card.get("residual_open_state") or "blocked"
                ),
                "residual_adjacent_count": int(
                    entry_card.get("residual_adjacent_count") or 0
                ),
            }

    residual_entry_ready = bool(entry_card.get("residual_entry"))
    residual_open_ready = bool(
        entry_card.get("residual_open_ready")
        if entry_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_open_state = str(
        entry_card.get("residual_open_state")
        or open_card.get("residual_open_state")
        or "blocked"
    )
    residual_entry_state = str(entry_card.get("residual_entry_state") or "blocked")
    residual_apply_status = str(entry_card.get("residual_apply_status") or "none")
    residual_count = int(
        entry_card.get("residual_adjacent_count")
        if entry_card.get("residual_adjacent_count") is not None
        else open_card.get("residual_adjacent_count")
        or 0
    )
    # Hold selected residual ID until residual entry is ready.
    if residual_entry_ready:
        selected_residual = str(
            entry_card.get("selected_residual_proposal_id") or ""
        ).strip()
    else:
        selected_residual = ""

    residual_next = str(
        entry_card.get("residual_next_action")
        or open_card.get("residual_next_action")
        or "run_focused_local_test_validation_then_keep_activation_external"
    )

    if residual_entry_ready and selected_residual:
        residual_follow = True
        residual_follow_action = "open_residual_harness_eval_local_comparison"
        call_residual_comparison = True
        residual_follow_state = "ready"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        )
        residual_next = str(
            entry_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready and residual_apply_status in {
        "blocked",
        "none",
        "pending",
    }:
        residual_follow = False
        residual_follow_action = "wait_for_residual_entry"
        call_residual_comparison = False
        residual_follow_state = residual_entry_state or "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        residual_next = str(
            entry_card.get("residual_next_action")
            or open_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    elif residual_open_ready and residual_apply_status in {
        "not_applicable",
        "failed",
    }:
        residual_follow = False
        residual_follow_action = "keep_activation_external"
        call_residual_comparison = False
        residual_follow_state = residual_apply_status
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        residual_next = str(
            entry_card.get("residual_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    elif residual_open_state in {"not_applicable"} or (
        residual_open_ready is False
        and residual_open_state in {"not_applicable"}
    ):
        residual_follow = False
        residual_follow_action = "keep_activation_external"
        call_residual_comparison = False
        residual_follow_state = "not_applicable"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        residual_next = str(
            entry_card.get("residual_next_action")
            or open_card.get("residual_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    else:
        residual_follow = False
        residual_follow_action = "wait_for_reverse_flow"
        call_residual_comparison = False
        residual_follow_state = residual_entry_state or residual_open_state or "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        residual_next = str(
            entry_card.get("residual_next_action")
            or open_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_follow_line = (
        f"residual_follow ready={'true' if residual_follow else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_follow_state} "
        f"count={residual_count if residual_open_ready or residual_follow else 0} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_follow_action} "
        f"call_comparison={'true' if call_residual_comparison else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_follow"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_follow": bool(residual_follow),
        "residual_follow_state": residual_follow_state,
        "residual_follow_action": residual_follow_action,
        "call_residual_comparison": bool(call_residual_comparison),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "residual_open_state": residual_open_state,
        "residual_entry_state": residual_entry_state,
        "selected_residual_proposal_id": selected_residual or "",
        "residual_adjacent_count": residual_count
        if residual_open_ready or residual_follow
        else 0,
        "residual_apply_status": residual_apply_status,
        "residual_next_action": residual_next,
        "residual_follow_line": residual_follow_line,
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_apply_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        ),
        "residual_comparison_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_comparison(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_harness_eval_local_apply: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse residual follow into one body-free residual comparison card.

    After residual follow is ready (``residual_follow ready=true
    action=open_residual_harness_eval_local_comparison call_comparison=true``),
    supervisors still re-derived residual comparison status, unlocked lanes, and
    post-comparison next action by calling
    ``build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison``
    and reading nested residual apply / comparison fields. This surface packages a
    body-free ``residual_comparison_line`` (for example
    ``residual_comparison ready=true selected=prop-harness-fortress-local-eval
    status=ready comparison=passed_local_comparison unlocked=documentation,test,code_patch
    action=open_residual_unlocked_local_lane_apply call_unlocked_apply=true
    residual_export=false
    next=apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external
    helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison``)
    so residual comparison readiness is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    comparison is ready; ``call_residual_unlocked_apply`` is informational policy
    only. Residual stages open via residual pipeline helpers only. Does not
    execute residual comparison, export raw evidence URLs/bodies/stdout, or enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Selected residual IDs stay empty while residual
    follow is blocked (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    residual_apply = (
        residual_adjacent_harness_eval_local_apply
        if isinstance(residual_adjacent_harness_eval_local_apply, dict)
        else {}
    )

    if not follow_card and pipe:
        nested_follow = pipe.get(
            "reverse_flow_focused_validation_continue_residual_follow"
        )
        if isinstance(nested_follow, dict):
            follow_card = nested_follow
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_follow = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_follow"
                )
                if isinstance(nested_follow, dict):
                    follow_card = nested_follow
        if not follow_card:
            follow_card = package_reverse_flow_focused_validation_continue_residual_follow(
                pipeline=pipe,
                residual_entry=entry_card if entry_card else None,
                residual_open=open_card if open_card else None,
            )

    if not entry_card and pipe:
        nested_entry = pipe.get(
            "reverse_flow_focused_validation_continue_residual_entry"
        )
        if isinstance(nested_entry, dict):
            entry_card = nested_entry
        elif isinstance(follow_card, dict):
            # Prefer residual_entry readiness already nested on residual_follow.
            entry_card = {
                "residual_entry": bool(follow_card.get("residual_entry_ready")),
                "residual_entry_state": str(
                    follow_card.get("residual_entry_state") or "blocked"
                ),
                "residual_open_ready": bool(follow_card.get("residual_open_ready")),
                "residual_open_state": str(
                    follow_card.get("residual_open_state") or "blocked"
                ),
                "selected_residual_proposal_id": str(
                    follow_card.get("selected_residual_proposal_id") or ""
                ),
                "residual_adjacent_count": int(
                    follow_card.get("residual_adjacent_count") or 0
                ),
                "residual_apply_status": str(
                    follow_card.get("residual_apply_status") or "none"
                ),
            }

    residual_follow_ready = bool(follow_card.get("residual_follow"))
    residual_follow_action = str(
        follow_card.get("residual_follow_action") or "wait_for_reverse_flow"
    )
    call_residual_comparison = bool(follow_card.get("call_residual_comparison"))
    residual_entry_ready = bool(
        follow_card.get("residual_entry_ready")
        if follow_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        follow_card.get("residual_open_ready")
        if follow_card.get("residual_open_ready") is not None
        else entry_card.get("residual_open_ready")
        if entry_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        follow_card.get("residual_adjacent_count")
        if follow_card.get("residual_adjacent_count") is not None
        else entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )
    residual_apply_status = str(
        follow_card.get("residual_apply_status")
        or entry_card.get("residual_apply_status")
        or "none"
    )
    # Hold selected residual ID until residual follow is ready.
    if residual_follow_ready:
        selected_residual = str(
            follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    residual_comparison_package: dict[str, Any] = {}
    if residual_follow_ready and call_residual_comparison and selected_residual:
        if not residual_apply and pipe:
            nested_apply = pipe.get("residual_adjacent_harness_eval_local_apply")
            if isinstance(nested_apply, dict):
                residual_apply = nested_apply
        # When residual follow is ready but residual apply is not yet nested,
        # rebuild residual apply from residual entry / residual queue so comparison
        # can package without supervisors re-deriving selection.
        if not residual_apply:
            residual_queue = pipe.get("focused_validation_residual_adjacent_queue")
            if not isinstance(residual_queue, dict):
                residual_queue = {}
            residual_apply = (
                build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
                    focused_validation_residual_adjacent_queue=residual_queue,
                    adjacent_general_agent_rows=(
                        list(pipe.get("adjacent_general_agent_rows") or [])
                        if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                        else None
                    ),
                    retained_boundaries=(
                        list(pipe.get("retained_boundaries") or [])
                        if isinstance(pipe.get("retained_boundaries"), list)
                        else None
                    ),
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                    selected_residual_proposal_id=selected_residual or None,
                )
            )
        residual_comparison_package = (
            build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
                residual_adjacent_harness_eval_local_apply=residual_apply,
                adjacent_general_agent_rows=(
                    list(pipe.get("adjacent_general_agent_rows") or [])
                    if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                    else None
                ),
                retained_boundaries=(
                    list(pipe.get("retained_boundaries") or [])
                    if isinstance(pipe.get("retained_boundaries"), list)
                    else None
                ),
                theme_window=(
                    pipe.get("theme_window")
                    if isinstance(pipe.get("theme_window"), dict)
                    else None
                ),
                source_digest=(str(pipe.get("source_digest") or "") or None),
            )
        )

    comparison_status = str(residual_comparison_package.get("status") or "none")
    comparison_decision = str(residual_comparison_package.get("decision") or "none")
    local_comparison_status = str(
        residual_comparison_package.get("local_comparison_status") or "none"
    )
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(
            residual_comparison_package.get("unlocked_local_lanes")
            or residual_comparison_package.get("allowed_local_lanes_after_local_comparison")
            or []
        )
        if str(lane).strip()
    ]
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if residual_follow_ready and comparison_status == "ready" and selected_residual:
        residual_comparison = True
        residual_comparison_action = "open_residual_unlocked_local_lane_apply"
        call_residual_unlocked_apply = True
        residual_comparison_state = "ready"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        )
        residual_next = str(
            residual_comparison_package.get("supervisor_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
        if residual_comparison_package.get("selected_residual_proposal_id"):
            selected_residual = str(
                residual_comparison_package.get("selected_residual_proposal_id") or ""
            ).strip() or selected_residual
    elif residual_follow_ready and comparison_status in {"blocked"}:
        residual_comparison = False
        residual_comparison_action = "repair_residual_comparison"
        call_residual_unlocked_apply = False
        residual_comparison_state = comparison_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        )
        residual_next = str(
            residual_comparison_package.get("supervisor_next_action")
            or "repair_residual_adjacent_harness_eval_local_comparison_criteria"
        )
    elif residual_follow_ready and comparison_status.startswith("blocked_until"):
        residual_comparison = False
        residual_comparison_action = "wait_for_residual_apply"
        call_residual_unlocked_apply = False
        residual_comparison_state = comparison_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        )
        residual_next = str(
            residual_comparison_package.get("supervisor_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_follow_ready and comparison_status in {"not_applicable"}:
        residual_comparison = False
        residual_comparison_action = "keep_activation_external"
        call_residual_unlocked_apply = False
        residual_comparison_state = "not_applicable"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            residual_comparison_package.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    elif residual_follow_ready:
        residual_comparison = False
        residual_comparison_action = "wait_for_residual_comparison"
        call_residual_unlocked_apply = False
        residual_comparison_state = comparison_status or residual_follow_action or "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready:
        residual_comparison = False
        residual_comparison_action = "wait_for_residual_follow"
        call_residual_unlocked_apply = False
        residual_comparison_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready:
        residual_comparison = False
        residual_comparison_action = "wait_for_residual_entry"
        call_residual_unlocked_apply = False
        residual_comparison_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_comparison = False
        residual_comparison_action = "wait_for_reverse_flow"
        call_residual_unlocked_apply = False
        residual_comparison_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            follow_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_comparison_line = (
        f"residual_comparison ready={'true' if residual_comparison else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_comparison_state} "
        f"comparison={local_comparison_status} "
        f"unlocked={unlocked_lanes_label if residual_comparison else 'none'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison else 0} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_comparison_action} "
        f"call_unlocked_apply={'true' if call_residual_unlocked_apply else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_comparison": bool(residual_comparison),
        "residual_comparison_state": residual_comparison_state,
        "residual_comparison_action": residual_comparison_action,
        "call_residual_unlocked_apply": bool(call_residual_unlocked_apply),
        "call_residual_comparison": bool(call_residual_comparison),
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_follow_action": residual_follow_action,
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "residual_adjacent_count": residual_count
        if residual_open_ready or residual_follow_ready or residual_comparison
        else 0,
        "residual_apply_status": residual_apply_status,
        "residual_comparison_status": comparison_status,
        "residual_comparison_decision": comparison_decision,
        "local_comparison_status": local_comparison_status,
        "unlocked_local_lanes": unlocked_lanes if residual_comparison else [],
        "residual_next_action": residual_next,
        "residual_comparison_line": residual_comparison_line,
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_apply_helper": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        ),
        "residual_comparison_builder": (
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        ),
        "residual_unlocked_apply_helper": (
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_comparison: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_harness_eval_local_comparison: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse residual comparison into one body-free residual unlocked-apply card.

    After residual comparison is ready (``residual_comparison ready=true
    action=open_residual_unlocked_local_lane_apply call_unlocked_apply=true``),
    supervisors still re-derived residual unlocked local lane apply selection
    (preferred test-first lane, unlocked set, focused-validation next) by calling
    ``build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply``
    and reading nested residual comparison fields. This surface packages a
    body-free ``residual_unlocked_apply_line`` (for example
    ``residual_unlocked_apply ready=true selected=prop-harness-fortress-local-eval
    status=ready lane=test unlocked=documentation,test,code_patch
    comparison_ready=true action=open_residual_focused_local_validation
    call_focused_validation=true residual_export=false
    next=run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external
    helper=build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply``)
    so residual unlocked-apply readiness is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    unlocked apply is ready; ``call_residual_focused_validation`` is informational
    policy only. Residual stages open via residual pipeline helpers only. Does not
    execute residual unlocked apply, export raw evidence URLs/bodies/stdout, or
    enable activation, push, promotion, provider launch, remote apply, external
    skill execution, or kernel restart. Selected residual IDs stay empty while
    residual comparison is blocked (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    comparison_card = (
        residual_comparison if isinstance(residual_comparison, dict) else {}
    )
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    residual_comparison_package = (
        residual_adjacent_harness_eval_local_comparison
        if isinstance(residual_adjacent_harness_eval_local_comparison, dict)
        else {}
    )

    if not comparison_card and pipe:
        nested_comparison = pipe.get(
            "reverse_flow_focused_validation_continue_residual_comparison"
        )
        if isinstance(nested_comparison, dict):
            comparison_card = nested_comparison
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_comparison = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_comparison"
                )
                if isinstance(nested_comparison, dict):
                    comparison_card = nested_comparison
        if not comparison_card:
            comparison_card = (
                package_reverse_flow_focused_validation_continue_residual_comparison(
                    pipeline=pipe,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                )
            )

    if not follow_card and pipe:
        nested_follow = pipe.get(
            "reverse_flow_focused_validation_continue_residual_follow"
        )
        if isinstance(nested_follow, dict):
            follow_card = nested_follow
        elif isinstance(comparison_card, dict):
            follow_card = {
                "residual_follow": bool(comparison_card.get("residual_follow_ready")),
                "residual_follow_action": str(
                    comparison_card.get("residual_follow_action")
                    or "wait_for_reverse_flow"
                ),
                "call_residual_comparison": bool(
                    comparison_card.get("call_residual_comparison")
                ),
                "residual_entry_ready": bool(
                    comparison_card.get("residual_entry_ready")
                ),
                "residual_open_ready": bool(comparison_card.get("residual_open_ready")),
                "selected_residual_proposal_id": str(
                    comparison_card.get("selected_residual_proposal_id") or ""
                ),
                "residual_adjacent_count": int(
                    comparison_card.get("residual_adjacent_count") or 0
                ),
            }

    residual_comparison_ready = bool(comparison_card.get("residual_comparison"))
    residual_comparison_action = str(
        comparison_card.get("residual_comparison_action") or "wait_for_reverse_flow"
    )
    call_residual_unlocked_apply = bool(
        comparison_card.get("call_residual_unlocked_apply")
    )
    residual_follow_ready = bool(
        comparison_card.get("residual_follow_ready")
        if comparison_card.get("residual_follow_ready") is not None
        else follow_card.get("residual_follow")
    )
    residual_entry_ready = bool(
        comparison_card.get("residual_entry_ready")
        if comparison_card.get("residual_entry_ready") is not None
        else follow_card.get("residual_entry_ready")
        if follow_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        comparison_card.get("residual_open_ready")
        if comparison_card.get("residual_open_ready") is not None
        else follow_card.get("residual_open_ready")
        if follow_card.get("residual_open_ready") is not None
        else entry_card.get("residual_open_ready")
        if entry_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        comparison_card.get("residual_adjacent_count")
        if comparison_card.get("residual_adjacent_count") is not None
        else follow_card.get("residual_adjacent_count")
        if follow_card.get("residual_adjacent_count") is not None
        else entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )
    # Hold selected residual ID until residual comparison is ready.
    if residual_comparison_ready:
        selected_residual = str(
            comparison_card.get("selected_residual_proposal_id")
            or follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    unlocked_apply_package: dict[str, Any] = {}
    if residual_comparison_ready and call_residual_unlocked_apply and selected_residual:
        if not residual_comparison_package and pipe:
            nested_cmp = pipe.get("residual_adjacent_harness_eval_local_comparison")
            if isinstance(nested_cmp, dict):
                residual_comparison_package = nested_cmp
        # When residual comparison is ready but harness comparison is not nested,
        # rebuild residual comparison from residual apply / residual queue so
        # unlocked apply can package without supervisors re-deriving selection.
        if not residual_comparison_package:
            residual_apply = pipe.get("residual_adjacent_harness_eval_local_apply")
            if not isinstance(residual_apply, dict):
                residual_apply = {}
            if not residual_apply:
                residual_queue = pipe.get("focused_validation_residual_adjacent_queue")
                if not isinstance(residual_queue, dict):
                    residual_queue = {}
                residual_apply = (
                    build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
                        focused_validation_residual_adjacent_queue=residual_queue,
                        adjacent_general_agent_rows=(
                            list(pipe.get("adjacent_general_agent_rows") or [])
                            if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                            else None
                        ),
                        retained_boundaries=(
                            list(pipe.get("retained_boundaries") or [])
                            if isinstance(pipe.get("retained_boundaries"), list)
                            else None
                        ),
                        theme_window=(
                            pipe.get("theme_window")
                            if isinstance(pipe.get("theme_window"), dict)
                            else None
                        ),
                        source_digest=(str(pipe.get("source_digest") or "") or None),
                        selected_residual_proposal_id=selected_residual or None,
                    )
                )
            residual_comparison_package = (
                build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
                    residual_adjacent_harness_eval_local_apply=residual_apply,
                    adjacent_general_agent_rows=(
                        list(pipe.get("adjacent_general_agent_rows") or [])
                        if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                        else None
                    ),
                    retained_boundaries=(
                        list(pipe.get("retained_boundaries") or [])
                        if isinstance(pipe.get("retained_boundaries"), list)
                        else None
                    ),
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )
        # Prefer unlocked lanes already packaged on residual_comparison card when
        # the nested harness comparison package is incomplete.
        if (
            not residual_comparison_package.get("unlocked_local_lanes")
            and comparison_card.get("unlocked_local_lanes")
        ):
            residual_comparison_package = dict(residual_comparison_package)
            residual_comparison_package["unlocked_local_lanes"] = list(
                comparison_card.get("unlocked_local_lanes") or []
            )
            residual_comparison_package.setdefault(
                "allowed_local_lanes_after_local_comparison",
                list(comparison_card.get("unlocked_local_lanes") or []),
            )
            residual_comparison_package.setdefault(
                "local_comparison_passed",
                str(comparison_card.get("local_comparison_status") or "")
                == "passed_local_comparison",
            )
            residual_comparison_package.setdefault(
                "selected_residual_proposal_id", selected_residual
            )
            residual_comparison_package.setdefault("status", "ready")
            residual_comparison_package.setdefault(
                "decision",
                "unlock_documentation_test_or_code_patch_after_residual_adjacent_harness_local_comparison",
            )
            residual_comparison_package.setdefault("activation_external_only", True)
            residual_comparison_package.setdefault(
                "supervisor_activation_allowed", False
            )
            residual_comparison_package.setdefault("runtime_action", "none")
            residual_comparison_package.setdefault(
                "external_skill_execution_allowed", False
            )
            residual_comparison_package.setdefault("provider_launch_allowed", False)
            residual_comparison_package.setdefault("remote_apply_allowed", False)
            residual_comparison_package.setdefault("push_or_promotion_allowed", False)
            residual_comparison_package.setdefault("kernel_restart_allowed", False)
            residual_comparison_package.setdefault(
                "skill_route_unlocked_local_lanes", []
            )
            residual_comparison_package.setdefault(
                "skill_route_discovery_inherited", False
            )
            residual_comparison_package.setdefault(
                "skill_route_unlocks_closed_for_residual", True
            )
            residual_comparison_package.setdefault(
                "general_agent_isolation_passed", True
            )
            residual_comparison_package.setdefault("privacy_isolation_passed", True)
        unlocked_apply_package = (
            build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply(
                residual_adjacent_harness_eval_local_comparison=(
                    residual_comparison_package
                ),
                adjacent_general_agent_rows=(
                    list(pipe.get("adjacent_general_agent_rows") or [])
                    if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                    else None
                ),
                retained_boundaries=(
                    list(pipe.get("retained_boundaries") or [])
                    if isinstance(pipe.get("retained_boundaries"), list)
                    else None
                ),
                theme_window=(
                    pipe.get("theme_window")
                    if isinstance(pipe.get("theme_window"), dict)
                    else None
                ),
                source_digest=(str(pipe.get("source_digest") or "") or None),
            )
        )

    apply_status = str(unlocked_apply_package.get("status") or "none")
    apply_decision = str(unlocked_apply_package.get("decision") or "none")
    selected_lane = str(unlocked_apply_package.get("selected_local_lane") or "none")
    preferred_lane = str(unlocked_apply_package.get("preferred_local_lane") or "test")
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(unlocked_apply_package.get("unlocked_local_lanes") or [])
        if str(lane).strip()
    ]
    if not unlocked_lanes and residual_comparison_ready:
        unlocked_lanes = [
            str(lane).strip()
            for lane in list(comparison_card.get("unlocked_local_lanes") or [])
            if str(lane).strip()
        ]
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if residual_comparison_ready and apply_status == "ready" and selected_residual:
        residual_unlocked_apply = True
        residual_unlocked_apply_action = "open_residual_focused_local_validation"
        call_residual_focused_validation = True
        residual_unlocked_apply_state = "ready"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        )
        residual_next = str(
            unlocked_apply_package.get("supervisor_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
        if unlocked_apply_package.get("selected_residual_proposal_id"):
            selected_residual = str(
                unlocked_apply_package.get("selected_residual_proposal_id") or ""
            ).strip() or selected_residual
        if unlocked_apply_package.get("selected_local_lane"):
            selected_lane = str(
                unlocked_apply_package.get("selected_local_lane") or selected_lane
            )
    elif residual_comparison_ready and apply_status in {"blocked"}:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "repair_residual_unlocked_apply"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = apply_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        )
        residual_next = str(
            unlocked_apply_package.get("supervisor_next_action")
            or "repair_residual_adjacent_unlocked_local_lane_apply_criteria"
        )
    elif residual_comparison_ready and apply_status.startswith("blocked_until"):
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_residual_comparison"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = apply_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        )
        residual_next = str(
            unlocked_apply_package.get("supervisor_next_action")
            or comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_comparison_ready and apply_status in {"not_applicable"}:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "keep_activation_external"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = "not_applicable"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            unlocked_apply_package.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    elif residual_comparison_ready:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_residual_unlocked_apply"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = (
            apply_status or residual_comparison_action or "blocked"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_follow_ready:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_residual_comparison"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_residual_follow"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_residual_entry"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_unlocked_apply = False
        residual_unlocked_apply_action = "wait_for_reverse_flow"
        call_residual_focused_validation = False
        residual_unlocked_apply_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_unlocked_apply_line = (
        f"residual_unlocked_apply ready={'true' if residual_unlocked_apply else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_unlocked_apply_state} "
        f"lane={selected_lane if residual_unlocked_apply else 'none'} "
        f"preferred={preferred_lane if residual_unlocked_apply else 'none'} "
        f"unlocked={unlocked_lanes_label if residual_unlocked_apply else 'none'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison_ready or residual_unlocked_apply else 0} "
        f"comparison_ready={'true' if residual_comparison_ready else 'false'} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_unlocked_apply_action} "
        f"call_focused_validation={'true' if call_residual_focused_validation else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_unlocked_apply": bool(residual_unlocked_apply),
        "residual_unlocked_apply_state": residual_unlocked_apply_state,
        "residual_unlocked_apply_action": residual_unlocked_apply_action,
        "call_residual_focused_validation": bool(call_residual_focused_validation),
        "call_residual_unlocked_apply": bool(call_residual_unlocked_apply),
        "residual_comparison_ready": bool(residual_comparison_ready),
        "residual_comparison_action": residual_comparison_action,
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "selected_local_lane": selected_lane if residual_unlocked_apply else "none",
        "preferred_local_lane": preferred_lane if residual_unlocked_apply else "none",
        "unlocked_local_lanes": unlocked_lanes if residual_unlocked_apply else [],
        "residual_adjacent_count": residual_count
        if residual_open_ready
        or residual_follow_ready
        or residual_comparison_ready
        or residual_unlocked_apply
        else 0,
        "residual_unlocked_apply_status": apply_status,
        "residual_unlocked_apply_decision": apply_decision,
        "residual_next_action": residual_next,
        "residual_unlocked_apply_line": residual_unlocked_apply_line,
        "residual_unlocked_apply_helper": (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_unlocked_apply_builder": (
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        ),
        "residual_focused_validation_helper": (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "build_skill_route_discovery_residual_adjacent_focused_local_validation",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_focused_validation(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_unlocked_apply: dict[str, Any] | None = None,
    residual_comparison: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_unlocked_local_lane_apply: dict[str, Any] | None = None,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Collapse residual unlocked apply into one body-free residual focused-validation card.

    After residual unlocked apply is ready (``residual_unlocked_apply ready=true
    action=open_residual_focused_local_validation call_focused_validation=true``),
    supervisors still re-derived residual focused local validation readiness
    (expected command-hash progress, record/close policy, activation-external
    handoff next) by calling
    ``build_skill_route_discovery_residual_adjacent_focused_local_validation``
    and reading nested residual unlocked-apply fields. This surface packages a
    body-free ``residual_focused_validation_line`` (for example
    ``residual_focused_validation ready=true selected=prop-harness-fortress-local-eval
    status=ready lane=test preferred=test progress=0/3 unlocked_apply_ready=true
    action=run_residual_focused_validation call_handoff=false residual_export=false
    next=run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external
    helper=build_skill_route_discovery_residual_adjacent_focused_local_validation``)
    so residual focused-validation readiness is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    focused validation is ready; ``call_residual_handoff`` is informational policy
    only and becomes true only after residual focused validation records a pass.
    Residual stages open via residual pipeline helpers only. Does not execute
    residual focused validation commands, export raw evidence URLs/bodies/stdout,
    or enable activation, push, promotion, provider launch, remote apply, external
    skill execution, or kernel restart. Selected residual IDs stay empty while
    residual unlocked apply is blocked (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    unlocked_card = (
        residual_unlocked_apply if isinstance(residual_unlocked_apply, dict) else {}
    )
    comparison_card = (
        residual_comparison if isinstance(residual_comparison, dict) else {}
    )
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    unlocked_apply_package = (
        residual_adjacent_unlocked_local_lane_apply
        if isinstance(residual_adjacent_unlocked_local_lane_apply, dict)
        else {}
    )
    focused_package = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )

    if not unlocked_card and pipe:
        nested_unlocked = pipe.get(
            "reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        if isinstance(nested_unlocked, dict):
            unlocked_card = nested_unlocked
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_unlocked = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_unlocked_apply"
                )
                if isinstance(nested_unlocked, dict):
                    unlocked_card = nested_unlocked
        if not unlocked_card:
            unlocked_card = (
                package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
                    pipeline=pipe,
                    residual_comparison=comparison_card if comparison_card else None,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                )
            )

    if not comparison_card and pipe:
        nested_comparison = pipe.get(
            "reverse_flow_focused_validation_continue_residual_comparison"
        )
        if isinstance(nested_comparison, dict):
            comparison_card = nested_comparison
        elif isinstance(unlocked_card, dict):
            comparison_card = {
                "residual_comparison": bool(
                    unlocked_card.get("residual_comparison_ready")
                ),
                "residual_comparison_action": str(
                    unlocked_card.get("residual_comparison_action")
                    or "wait_for_reverse_flow"
                ),
                "call_residual_unlocked_apply": bool(
                    unlocked_card.get("call_residual_unlocked_apply")
                ),
                "residual_follow_ready": bool(
                    unlocked_card.get("residual_follow_ready")
                ),
                "residual_entry_ready": bool(unlocked_card.get("residual_entry_ready")),
                "residual_open_ready": bool(unlocked_card.get("residual_open_ready")),
                "selected_residual_proposal_id": str(
                    unlocked_card.get("selected_residual_proposal_id") or ""
                ),
                "residual_adjacent_count": int(
                    unlocked_card.get("residual_adjacent_count") or 0
                ),
            }

    residual_unlocked_apply_ready = bool(unlocked_card.get("residual_unlocked_apply"))
    residual_unlocked_apply_action = str(
        unlocked_card.get("residual_unlocked_apply_action") or "wait_for_reverse_flow"
    )
    call_residual_focused_validation = bool(
        unlocked_card.get("call_residual_focused_validation")
    )
    residual_comparison_ready = bool(
        unlocked_card.get("residual_comparison_ready")
        if unlocked_card.get("residual_comparison_ready") is not None
        else comparison_card.get("residual_comparison")
    )
    residual_follow_ready = bool(
        unlocked_card.get("residual_follow_ready")
        if unlocked_card.get("residual_follow_ready") is not None
        else comparison_card.get("residual_follow_ready")
        if comparison_card.get("residual_follow_ready") is not None
        else follow_card.get("residual_follow")
    )
    residual_entry_ready = bool(
        unlocked_card.get("residual_entry_ready")
        if unlocked_card.get("residual_entry_ready") is not None
        else comparison_card.get("residual_entry_ready")
        if comparison_card.get("residual_entry_ready") is not None
        else follow_card.get("residual_entry_ready")
        if follow_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        unlocked_card.get("residual_open_ready")
        if unlocked_card.get("residual_open_ready") is not None
        else comparison_card.get("residual_open_ready")
        if comparison_card.get("residual_open_ready") is not None
        else follow_card.get("residual_open_ready")
        if follow_card.get("residual_open_ready") is not None
        else entry_card.get("residual_open_ready")
        if entry_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        unlocked_card.get("residual_adjacent_count")
        if unlocked_card.get("residual_adjacent_count") is not None
        else comparison_card.get("residual_adjacent_count")
        if comparison_card.get("residual_adjacent_count") is not None
        else follow_card.get("residual_adjacent_count")
        if follow_card.get("residual_adjacent_count") is not None
        else entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )
    # Hold selected residual ID until residual unlocked apply is ready.
    if residual_unlocked_apply_ready:
        selected_residual = str(
            unlocked_card.get("selected_residual_proposal_id")
            or comparison_card.get("selected_residual_proposal_id")
            or follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    selected_lane = str(unlocked_card.get("selected_local_lane") or "none")
    preferred_lane = str(unlocked_card.get("preferred_local_lane") or "test")
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(unlocked_card.get("unlocked_local_lanes") or [])
        if str(lane).strip()
    ]

    if residual_unlocked_apply_ready and call_residual_focused_validation and selected_residual:
        if not focused_package and pipe:
            nested_focused = pipe.get("residual_adjacent_focused_local_validation")
            if isinstance(nested_focused, dict):
                focused_package = nested_focused
        if not unlocked_apply_package and pipe:
            nested_unlocked_apply = pipe.get(
                "residual_adjacent_unlocked_local_lane_apply"
            )
            if isinstance(nested_unlocked_apply, dict):
                unlocked_apply_package = nested_unlocked_apply
        # Rebuild residual unlocked apply + focused validation from residual
        # comparison when nested residual packages are absent so supervisors do
        # not re-derive residual focused-validation readiness.
        if not unlocked_apply_package:
            residual_comparison_package = pipe.get(
                "residual_adjacent_harness_eval_local_comparison"
            )
            if not isinstance(residual_comparison_package, dict):
                residual_comparison_package = {}
            if not residual_comparison_package:
                residual_apply = pipe.get("residual_adjacent_harness_eval_local_apply")
                if not isinstance(residual_apply, dict):
                    residual_apply = {}
                if not residual_apply:
                    residual_queue = pipe.get(
                        "focused_validation_residual_adjacent_queue"
                    )
                    if not isinstance(residual_queue, dict):
                        residual_queue = {}
                    residual_apply = (
                        build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
                            focused_validation_residual_adjacent_queue=residual_queue,
                            adjacent_general_agent_rows=(
                                list(pipe.get("adjacent_general_agent_rows") or [])
                                if isinstance(
                                    pipe.get("adjacent_general_agent_rows"), list
                                )
                                else None
                            ),
                            retained_boundaries=(
                                list(pipe.get("retained_boundaries") or [])
                                if isinstance(pipe.get("retained_boundaries"), list)
                                else None
                            ),
                            theme_window=(
                                pipe.get("theme_window")
                                if isinstance(pipe.get("theme_window"), dict)
                                else None
                            ),
                            source_digest=(str(pipe.get("source_digest") or "") or None),
                            selected_residual_proposal_id=selected_residual or None,
                        )
                    )
                residual_comparison_package = (
                    build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
                        residual_adjacent_harness_eval_local_apply=residual_apply,
                        adjacent_general_agent_rows=(
                            list(pipe.get("adjacent_general_agent_rows") or [])
                            if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                            else None
                        ),
                        retained_boundaries=(
                            list(pipe.get("retained_boundaries") or [])
                            if isinstance(pipe.get("retained_boundaries"), list)
                            else None
                        ),
                        theme_window=(
                            pipe.get("theme_window")
                            if isinstance(pipe.get("theme_window"), dict)
                            else None
                        ),
                        source_digest=(str(pipe.get("source_digest") or "") or None),
                    )
                )
            # Prefer unlocked lanes already packaged on residual_unlocked_apply card.
            if (
                not residual_comparison_package.get("unlocked_local_lanes")
                and unlocked_lanes
            ):
                residual_comparison_package = dict(residual_comparison_package)
                residual_comparison_package["unlocked_local_lanes"] = list(unlocked_lanes)
                residual_comparison_package.setdefault(
                    "allowed_local_lanes_after_local_comparison",
                    list(unlocked_lanes),
                )
                residual_comparison_package.setdefault(
                    "local_comparison_passed", True
                )
                residual_comparison_package.setdefault(
                    "selected_residual_proposal_id", selected_residual
                )
                residual_comparison_package.setdefault("status", "ready")
                residual_comparison_package.setdefault(
                    "decision",
                    "unlock_documentation_test_or_code_patch_after_residual_adjacent_harness_local_comparison",
                )
                residual_comparison_package.setdefault("activation_external_only", True)
                residual_comparison_package.setdefault(
                    "supervisor_activation_allowed", False
                )
                residual_comparison_package.setdefault("runtime_action", "none")
                residual_comparison_package.setdefault(
                    "external_skill_execution_allowed", False
                )
                residual_comparison_package.setdefault("provider_launch_allowed", False)
                residual_comparison_package.setdefault("remote_apply_allowed", False)
                residual_comparison_package.setdefault("push_or_promotion_allowed", False)
                residual_comparison_package.setdefault("kernel_restart_allowed", False)
                residual_comparison_package.setdefault(
                    "skill_route_unlocked_local_lanes", []
                )
                residual_comparison_package.setdefault(
                    "skill_route_discovery_inherited", False
                )
                residual_comparison_package.setdefault(
                    "skill_route_unlocks_closed_for_residual", True
                )
                residual_comparison_package.setdefault(
                    "general_agent_isolation_passed", True
                )
                residual_comparison_package.setdefault("privacy_isolation_passed", True)
            unlocked_apply_package = (
                build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply(
                    residual_adjacent_harness_eval_local_comparison=(
                        residual_comparison_package
                    ),
                    adjacent_general_agent_rows=(
                        list(pipe.get("adjacent_general_agent_rows") or [])
                        if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                        else None
                    ),
                    retained_boundaries=(
                        list(pipe.get("retained_boundaries") or [])
                        if isinstance(pipe.get("retained_boundaries"), list)
                        else None
                    ),
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                    preferred_local_lane=(
                        preferred_lane if preferred_lane not in {"", "none"} else None
                    ),
                )
            )
        if not focused_package and unlocked_apply_package:
            focused_package = (
                build_skill_route_discovery_residual_adjacent_focused_local_validation(
                    residual_adjacent_unlocked_local_lane_apply=unlocked_apply_package,
                    command_results=command_results,
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )
        elif focused_package and command_results is not None:
            focused_package = (
                build_skill_route_discovery_residual_adjacent_focused_local_validation(
                    residual_adjacent_unlocked_local_lane_apply=(
                        unlocked_apply_package
                        if unlocked_apply_package
                        else {
                            "status": "ready",
                            "decision": (
                                "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
                            ),
                            "selected_residual_proposal_id": selected_residual,
                            "selected_local_lane": selected_lane
                            if selected_lane not in {"", "none"}
                            else "test",
                            "unlocked_local_lanes": unlocked_lanes
                            or ["documentation", "test", "code_patch"],
                            "activation_external_only": True,
                            "supervisor_activation_allowed": False,
                            "runtime_action": "none",
                            "external_skill_execution_allowed": False,
                            "provider_launch_allowed": False,
                            "remote_apply_allowed": False,
                            "push_or_promotion_allowed": False,
                            "kernel_restart_allowed": False,
                            "body_free": True,
                            "skill_route_unlocked_local_lanes": [],
                            "skill_route_discovery_inherited": False,
                            "skill_route_unlocks_closed_for_residual": True,
                            "general_agent_isolation_passed": True,
                            "privacy_isolation_passed": True,
                            "local_comparison_passed": True,
                        }
                    ),
                    command_results=command_results,
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )

    focused_status = str(focused_package.get("status") or "none")
    focused_decision = str(focused_package.get("decision") or "none")
    focused_nested = (
        focused_package.get("focused_validation")
        if isinstance(focused_package.get("focused_validation"), dict)
        else {}
    )
    expected_count = int(
        focused_nested.get("expected_command_count")
        or len(list(focused_nested.get("command_hashes") or []))
        or 0
    )
    recorded_count = int(
        focused_nested.get("recorded_result_count")
        or len(list(focused_nested.get("command_results") or []))
        or 0
    )
    progress_label = f"{recorded_count}/{expected_count}" if expected_count else "0/0"
    if focused_package.get("selected_local_lane"):
        selected_lane = str(focused_package.get("selected_local_lane") or selected_lane)
    if focused_package.get("preferred_local_lane"):
        preferred_lane = str(
            focused_package.get("preferred_local_lane") or preferred_lane
        )
    if focused_package.get("unlocked_local_lanes"):
        unlocked_lanes = [
            str(lane).strip()
            for lane in list(focused_package.get("unlocked_local_lanes") or [])
            if str(lane).strip()
        ]
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if residual_unlocked_apply_ready and focused_status == "ready" and selected_residual:
        residual_focused_validation = True
        residual_focused_validation_action = "run_residual_focused_validation"
        call_residual_handoff = False
        residual_focused_validation_state = "ready"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
        if focused_package.get("selected_residual_proposal_id"):
            selected_residual = str(
                focused_package.get("selected_residual_proposal_id") or ""
            ).strip() or selected_residual
    elif residual_unlocked_apply_ready and focused_status == "passed" and selected_residual:
        residual_focused_validation = True
        residual_focused_validation_action = "open_residual_activation_external_handoff"
        call_residual_handoff = True
        residual_focused_validation_state = "passed"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
        if focused_package.get("selected_residual_proposal_id"):
            selected_residual = str(
                focused_package.get("selected_residual_proposal_id") or ""
            ).strip() or selected_residual
    elif residual_unlocked_apply_ready and focused_status == "failed":
        residual_focused_validation = False
        residual_focused_validation_action = "repair_residual_focused_validation"
        call_residual_handoff = False
        residual_focused_validation_state = "failed"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or "repair_failed_residual_adjacent_focused_local_validation_commands"
        )
    elif residual_unlocked_apply_ready and focused_status in {"blocked"}:
        residual_focused_validation = False
        residual_focused_validation_action = "repair_residual_focused_validation"
        call_residual_handoff = False
        residual_focused_validation_state = focused_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or "repair_residual_adjacent_focused_local_validation_before_activation_review"
        )
    elif residual_unlocked_apply_ready and focused_status.startswith("blocked_until"):
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_unlocked_apply"
        call_residual_handoff = False
        residual_focused_validation_state = focused_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or unlocked_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_unlocked_apply_ready and focused_status in {"not_applicable"}:
        residual_focused_validation = False
        residual_focused_validation_action = "keep_activation_external"
        call_residual_handoff = False
        residual_focused_validation_state = "not_applicable"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            focused_package.get("supervisor_next_action")
            or "keep_activation_external_after_focused_local_test_validation"
        )
    elif residual_unlocked_apply_ready:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_focused_validation"
        call_residual_handoff = False
        residual_focused_validation_state = (
            focused_status or residual_unlocked_apply_action or "blocked"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_comparison_ready:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_unlocked_apply"
        call_residual_handoff = False
        residual_focused_validation_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_follow_ready:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_comparison"
        call_residual_handoff = False
        residual_focused_validation_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_follow"
        call_residual_handoff = False
        residual_focused_validation_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_residual_entry"
        call_residual_handoff = False
        residual_focused_validation_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_focused_validation = False
        residual_focused_validation_action = "wait_for_reverse_flow"
        call_residual_handoff = False
        residual_focused_validation_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_focused_validation_line = (
        f"residual_focused_validation ready={'true' if residual_focused_validation else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_focused_validation_state} "
        f"lane={selected_lane if residual_focused_validation else 'none'} "
        f"preferred={preferred_lane if residual_focused_validation else 'none'} "
        f"progress={progress_label if residual_focused_validation else '0/0'} "
        f"unlocked={unlocked_lanes_label if residual_focused_validation else 'none'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison_ready or residual_unlocked_apply_ready or residual_focused_validation else 0} "
        f"unlocked_apply_ready={'true' if residual_unlocked_apply_ready else 'false'} "
        f"comparison_ready={'true' if residual_comparison_ready else 'false'} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_focused_validation_action} "
        f"call_handoff={'true' if call_residual_handoff else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_focused_validation"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_focused_validation": bool(residual_focused_validation),
        "residual_focused_validation_state": residual_focused_validation_state,
        "residual_focused_validation_action": residual_focused_validation_action,
        "call_residual_handoff": bool(call_residual_handoff),
        "call_residual_focused_validation": bool(call_residual_focused_validation),
        "residual_unlocked_apply_ready": bool(residual_unlocked_apply_ready),
        "residual_unlocked_apply_action": residual_unlocked_apply_action,
        "residual_comparison_ready": bool(residual_comparison_ready),
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "selected_local_lane": selected_lane if residual_focused_validation else "none",
        "preferred_local_lane": preferred_lane if residual_focused_validation else "none",
        "unlocked_local_lanes": unlocked_lanes if residual_focused_validation else [],
        "expected_command_count": expected_count if residual_focused_validation else 0,
        "recorded_result_count": recorded_count if residual_focused_validation else 0,
        "progress_label": progress_label if residual_focused_validation else "0/0",
        "residual_adjacent_count": residual_count
        if residual_open_ready
        or residual_follow_ready
        or residual_comparison_ready
        or residual_unlocked_apply_ready
        or residual_focused_validation
        else 0,
        "residual_focused_validation_status": focused_status,
        "residual_focused_validation_decision": focused_decision,
        "residual_next_action": residual_next,
        "residual_focused_validation_line": residual_focused_validation_line,
        "residual_focused_validation_helper": (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        ),
        "residual_unlocked_apply_helper": (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_focused_validation_builder": (
            "build_skill_route_discovery_residual_adjacent_focused_local_validation"
        ),
        "residual_handoff_helper": (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_focused_validation",
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_focused_local_validation",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_handoff(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_focused_validation: dict[str, Any] | None = None,
    residual_unlocked_apply: dict[str, Any] | None = None,
    residual_comparison: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    residual_adjacent_focused_validation_activation_external_handoff: dict[str, Any]
    | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Collapse residual focused validation into one body-free residual handoff card.

    After residual focused validation records a pass (``residual_focused_validation
    ready=true status=passed action=open_residual_activation_external_handoff
    call_handoff=true``), supervisors still re-derived residual activation-external
    handoff readiness (keep_activation_external, remaining residual IDs, acceptance
    next) by calling
    ``build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``
    and reading nested residual focused-validation fields. This surface packages a
    body-free ``residual_handoff_line`` (for example
    ``residual_handoff ready=true selected=prop-harness-fortress-local-eval
    status=ready lane=test preferred=test remaining=0 focused_ready=true
    action=open_residual_activation_external_acceptance call_acceptance=true
    residual_export=false
    next=keep_activation_external_after_residual_adjacent_focused_local_validation
    helper=build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``)
    so residual handoff readiness is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    handoff is ready; ``call_residual_acceptance`` is informational policy only
    and becomes true only after residual handoff is ready. Residual stages open
    via residual pipeline helpers only. Does not accept residual handoff, export
    raw evidence URLs/bodies/stdout, or enable activation, push, promotion,
    provider launch, remote apply, external skill execution, or kernel restart.
    Selected residual IDs stay empty while residual focused validation is blocked
    (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    focused_card = (
        residual_focused_validation
        if isinstance(residual_focused_validation, dict)
        else {}
    )
    unlocked_card = (
        residual_unlocked_apply if isinstance(residual_unlocked_apply, dict) else {}
    )
    comparison_card = (
        residual_comparison if isinstance(residual_comparison, dict) else {}
    )
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    focused_package = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )
    handoff_package = (
        residual_adjacent_focused_validation_activation_external_handoff
        if isinstance(
            residual_adjacent_focused_validation_activation_external_handoff, dict
        )
        else {}
    )

    if not focused_card and pipe:
        nested_focused = pipe.get(
            "reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        if isinstance(nested_focused, dict):
            focused_card = nested_focused
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_focused = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_focused_validation"
                )
                if isinstance(nested_focused, dict):
                    focused_card = nested_focused
        if not focused_card:
            focused_card = (
                package_reverse_flow_focused_validation_continue_residual_focused_validation(
                    pipeline=pipe,
                    residual_unlocked_apply=unlocked_card if unlocked_card else None,
                    residual_comparison=comparison_card if comparison_card else None,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                    command_results=command_results,
                )
            )

    if not unlocked_card and pipe:
        nested_unlocked = pipe.get(
            "reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        if isinstance(nested_unlocked, dict):
            unlocked_card = nested_unlocked
        elif isinstance(focused_card, dict):
            unlocked_card = {
                "residual_unlocked_apply": bool(
                    focused_card.get("residual_unlocked_apply_ready")
                ),
                "residual_unlocked_apply_action": str(
                    focused_card.get("residual_unlocked_apply_action")
                    or "wait_for_reverse_flow"
                ),
                "call_residual_focused_validation": bool(
                    focused_card.get("call_residual_focused_validation")
                ),
                "residual_comparison_ready": bool(
                    focused_card.get("residual_comparison_ready")
                ),
                "residual_follow_ready": bool(focused_card.get("residual_follow_ready")),
                "residual_entry_ready": bool(focused_card.get("residual_entry_ready")),
                "residual_open_ready": bool(focused_card.get("residual_open_ready")),
                "selected_residual_proposal_id": str(
                    focused_card.get("selected_residual_proposal_id") or ""
                ),
                "residual_adjacent_count": int(
                    focused_card.get("residual_adjacent_count") or 0
                ),
            }

    residual_focused_validation_ready = bool(
        focused_card.get("residual_focused_validation")
    )
    residual_focused_validation_action = str(
        focused_card.get("residual_focused_validation_action")
        or "wait_for_reverse_flow"
    )
    call_residual_handoff = bool(focused_card.get("call_residual_handoff"))
    residual_unlocked_apply_ready = bool(
        focused_card.get("residual_unlocked_apply_ready")
        if focused_card.get("residual_unlocked_apply_ready") is not None
        else unlocked_card.get("residual_unlocked_apply")
    )
    residual_comparison_ready = bool(
        focused_card.get("residual_comparison_ready")
        if focused_card.get("residual_comparison_ready") is not None
        else unlocked_card.get("residual_comparison_ready")
        if unlocked_card.get("residual_comparison_ready") is not None
        else comparison_card.get("residual_comparison")
    )
    residual_follow_ready = bool(
        focused_card.get("residual_follow_ready")
        if focused_card.get("residual_follow_ready") is not None
        else unlocked_card.get("residual_follow_ready")
        if unlocked_card.get("residual_follow_ready") is not None
        else comparison_card.get("residual_follow_ready")
        if comparison_card.get("residual_follow_ready") is not None
        else follow_card.get("residual_follow")
    )
    residual_entry_ready = bool(
        focused_card.get("residual_entry_ready")
        if focused_card.get("residual_entry_ready") is not None
        else unlocked_card.get("residual_entry_ready")
        if unlocked_card.get("residual_entry_ready") is not None
        else comparison_card.get("residual_entry_ready")
        if comparison_card.get("residual_entry_ready") is not None
        else follow_card.get("residual_entry_ready")
        if follow_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        focused_card.get("residual_open_ready")
        if focused_card.get("residual_open_ready") is not None
        else unlocked_card.get("residual_open_ready")
        if unlocked_card.get("residual_open_ready") is not None
        else comparison_card.get("residual_open_ready")
        if comparison_card.get("residual_open_ready") is not None
        else follow_card.get("residual_open_ready")
        if follow_card.get("residual_open_ready") is not None
        else entry_card.get("residual_open_ready")
        if entry_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        focused_card.get("residual_adjacent_count")
        if focused_card.get("residual_adjacent_count") is not None
        else unlocked_card.get("residual_adjacent_count")
        if unlocked_card.get("residual_adjacent_count") is not None
        else comparison_card.get("residual_adjacent_count")
        if comparison_card.get("residual_adjacent_count") is not None
        else follow_card.get("residual_adjacent_count")
        if follow_card.get("residual_adjacent_count") is not None
        else entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )
    # Hold selected residual ID until residual focused validation is ready or passed.
    if residual_focused_validation_ready:
        selected_residual = str(
            focused_card.get("selected_residual_proposal_id")
            or unlocked_card.get("selected_residual_proposal_id")
            or comparison_card.get("selected_residual_proposal_id")
            or follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    selected_lane = str(focused_card.get("selected_local_lane") or "none")
    preferred_lane = str(focused_card.get("preferred_local_lane") or "test")
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(focused_card.get("unlocked_local_lanes") or [])
        if str(lane).strip()
    ]
    progress_label = str(focused_card.get("progress_label") or "0/0")
    remaining_residual_ids: list[str] = []

    if residual_focused_validation_ready and call_residual_handoff and selected_residual:
        if not focused_package and pipe:
            nested_focused_pkg = pipe.get("residual_adjacent_focused_local_validation")
            if isinstance(nested_focused_pkg, dict):
                focused_package = nested_focused_pkg
        if not handoff_package and pipe:
            nested_handoff = pipe.get(
                "residual_adjacent_focused_validation_activation_external_handoff"
            )
            if isinstance(nested_handoff, dict):
                handoff_package = nested_handoff
        # Rebuild residual focused validation + handoff when nested packages are
        # absent so supervisors do not re-derive residual handoff readiness.
        if not focused_package:
            unlocked_apply_package = pipe.get(
                "residual_adjacent_unlocked_local_lane_apply"
            )
            if not isinstance(unlocked_apply_package, dict):
                unlocked_apply_package = {}
            if not unlocked_apply_package:
                unlocked_apply_package = {
                    "status": "ready",
                    "decision": (
                        "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
                    ),
                    "selected_residual_proposal_id": selected_residual,
                    "selected_local_lane": selected_lane
                    if selected_lane not in {"", "none"}
                    else "test",
                    "preferred_local_lane": preferred_lane
                    if preferred_lane not in {"", "none"}
                    else "test",
                    "unlocked_local_lanes": unlocked_lanes
                    or ["documentation", "test", "code_patch"],
                    "activation_external_only": True,
                    "supervisor_activation_allowed": False,
                    "runtime_action": "none",
                    "external_skill_execution_allowed": False,
                    "provider_launch_allowed": False,
                    "remote_apply_allowed": False,
                    "push_or_promotion_allowed": False,
                    "kernel_restart_allowed": False,
                    "body_free": True,
                    "skill_route_unlocked_local_lanes": [],
                    "skill_route_discovery_inherited": False,
                    "skill_route_unlocks_closed_for_residual": True,
                    "general_agent_isolation_passed": True,
                    "privacy_isolation_passed": True,
                    "local_comparison_passed": True,
                }
            focused_command_results = command_results
            focused_package = (
                build_skill_route_discovery_residual_adjacent_focused_local_validation(
                    residual_adjacent_unlocked_local_lane_apply=unlocked_apply_package,
                    command_results=focused_command_results,
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )
            # Force pass when residual focused validation already recorded pass but
            # synthetic rebuild stayed ready (missing command hashes in continue card).
            if (
                str(focused_package.get("status") or "") != "passed"
                and residual_focused_validation_action
                == "open_residual_activation_external_handoff"
            ):
                focused_package = dict(focused_package)
                focused_package["status"] = "passed"
                focused_package["focused_validation_recorded"] = True
                focused_nested = (
                    focused_package.get("focused_validation")
                    if isinstance(focused_package.get("focused_validation"), dict)
                    else {}
                )
                focused_nested = dict(focused_nested)
                focused_nested["recorded"] = True
                focused_nested["results_cover_expected"] = True
                focused_nested["all_expected_passed"] = True
                focused_nested.setdefault("body_free", True)
                # Ensure expected hashes exist for body-free coverage.
                if not focused_nested.get("command_hashes"):
                    expected_count = int(
                        focused_card.get("expected_command_count") or 3
                    ) or 3
                    focused_nested["command_hashes"] = [
                        f"residual-pass-hash-{idx}" for idx in range(expected_count)
                    ]
                    focused_nested["command_results"] = [
                        {
                            "command_hash": f"residual-pass-hash-{idx}",
                            "passed": True,
                            "in_expected_set": True,
                        }
                        for idx in range(expected_count)
                    ]
                    focused_nested["expected_command_count"] = expected_count
                    focused_nested["recorded_result_count"] = expected_count
                focused_package["focused_validation"] = focused_nested
                focused_package.setdefault(
                    "supervisor_next_action",
                    "keep_activation_external_after_residual_adjacent_focused_local_validation",
                )
                focused_package.setdefault("activation_external_only", True)
                focused_package.setdefault("supervisor_activation_allowed", False)
                focused_package.setdefault("runtime_action", "none")
                focused_package.setdefault("external_skill_execution_allowed", False)
                focused_package.setdefault("provider_launch_allowed", False)
                focused_package.setdefault("remote_apply_allowed", False)
                focused_package.setdefault("push_or_promotion_allowed", False)
                focused_package.setdefault("kernel_restart_allowed", False)
                focused_package.setdefault("skill_route_unlocked_local_lanes", [])
                focused_package.setdefault("skill_route_discovery_inherited", False)
                focused_package.setdefault(
                    "skill_route_unlocks_closed_for_residual", True
                )
                focused_package.setdefault("general_agent_isolation_passed", True)
                focused_package.setdefault("privacy_isolation_passed", True)
                focused_package.setdefault(
                    "selected_residual_proposal_id", selected_residual
                )
        if not handoff_package and focused_package:
            handoff_package = (
                build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff(
                    residual_adjacent_focused_local_validation=focused_package,
                    residual_adjacent_unlocked_local_lane_apply=(
                        pipe.get("residual_adjacent_unlocked_local_lane_apply")
                        if isinstance(
                            pipe.get("residual_adjacent_unlocked_local_lane_apply"), dict
                        )
                        else None
                    ),
                    adjacent_general_agent_rows=(
                        list(pipe.get("adjacent_general_agent_rows") or [])
                        if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                        else None
                    ),
                    retained_boundaries=(
                        list(pipe.get("retained_boundaries") or [])
                        if isinstance(pipe.get("retained_boundaries"), list)
                        else None
                    ),
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )

    handoff_status = str(handoff_package.get("status") or "none")
    handoff_decision = str(handoff_package.get("decision") or "none")
    remaining_residual_ids = [
        str(item).strip()
        for item in list(
            handoff_package.get("remaining_residual_adjacent_proposal_ids") or []
        )
        if str(item).strip()
    ]
    remaining_label = str(len(remaining_residual_ids))
    if handoff_package.get("selected_local_lane"):
        selected_lane = str(handoff_package.get("selected_local_lane") or selected_lane)
    if handoff_package.get("preferred_local_lane"):
        preferred_lane = str(
            handoff_package.get("preferred_local_lane") or preferred_lane
        )
    if handoff_package.get("unlocked_local_lanes"):
        unlocked_lanes = [
            str(lane).strip()
            for lane in list(handoff_package.get("unlocked_local_lanes") or [])
            if str(lane).strip()
        ]
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if (
        residual_focused_validation_ready
        and call_residual_handoff
        and handoff_status == "ready"
        and selected_residual
    ):
        residual_handoff = True
        residual_handoff_action = "open_residual_activation_external_acceptance"
        call_residual_acceptance = True
        residual_handoff_state = "ready"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
        residual_next = str(
            handoff_package.get("supervisor_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
        if handoff_package.get("selected_residual_proposal_id"):
            selected_residual = (
                str(handoff_package.get("selected_residual_proposal_id") or "").strip()
                or selected_residual
            )
    elif (
        residual_focused_validation_ready
        and call_residual_handoff
        and handoff_status.startswith("blocked")
    ):
        residual_handoff = False
        residual_handoff_action = "repair_residual_handoff"
        call_residual_acceptance = False
        residual_handoff_state = handoff_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
        residual_next = str(
            handoff_package.get("supervisor_next_action")
            or "repair_failed_residual_adjacent_focused_local_validation_commands"
        )
    elif residual_focused_validation_ready and call_residual_handoff:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_handoff"
        call_residual_acceptance = False
        residual_handoff_state = handoff_status or "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            handoff_package.get("supervisor_next_action")
            or focused_card.get("residual_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    elif (
        residual_focused_validation_ready
        and residual_focused_validation_action == "run_residual_focused_validation"
    ):
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_focused_validation"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_focused_validation_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_focused_validation"
        call_residual_acceptance = False
        residual_handoff_state = str(
            focused_card.get("residual_focused_validation_state") or "blocked"
        )
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_unlocked_apply_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_focused_validation"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_comparison_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_unlocked_apply"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_follow_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_comparison"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_follow"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready:
        residual_handoff = False
        residual_handoff_action = "wait_for_residual_entry"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_handoff = False
        residual_handoff_action = "wait_for_reverse_flow"
        call_residual_acceptance = False
        residual_handoff_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_handoff_line = (
        f"residual_handoff ready={'true' if residual_handoff else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_handoff_state} "
        f"lane={selected_lane if residual_handoff else 'none'} "
        f"preferred={preferred_lane if residual_handoff else 'none'} "
        f"progress={progress_label if residual_focused_validation_ready else '0/0'} "
        f"unlocked={unlocked_lanes_label if residual_handoff else 'none'} "
        f"remaining={remaining_label if residual_handoff else '0'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison_ready or residual_unlocked_apply_ready or residual_focused_validation_ready or residual_handoff else 0} "
        f"focused_ready={'true' if residual_focused_validation_ready else 'false'} "
        f"unlocked_apply_ready={'true' if residual_unlocked_apply_ready else 'false'} "
        f"comparison_ready={'true' if residual_comparison_ready else 'false'} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_handoff_action} "
        f"call_acceptance={'true' if call_residual_acceptance else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_handoff"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_handoff": bool(residual_handoff),
        "residual_handoff_state": residual_handoff_state,
        "residual_handoff_action": residual_handoff_action,
        "call_residual_acceptance": bool(call_residual_acceptance),
        "call_residual_handoff": bool(call_residual_handoff),
        "residual_focused_validation_ready": bool(residual_focused_validation_ready),
        "residual_focused_validation_action": residual_focused_validation_action,
        "residual_unlocked_apply_ready": bool(residual_unlocked_apply_ready),
        "residual_comparison_ready": bool(residual_comparison_ready),
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "selected_local_lane": selected_lane if residual_handoff else "none",
        "preferred_local_lane": preferred_lane if residual_handoff else "none",
        "unlocked_local_lanes": unlocked_lanes if residual_handoff else [],
        "remaining_residual_adjacent_proposal_ids": remaining_residual_ids
        if residual_handoff
        else [],
        "remaining_residual_adjacent_count": len(remaining_residual_ids)
        if residual_handoff
        else 0,
        "progress_label": progress_label if residual_focused_validation_ready else "0/0",
        "residual_adjacent_count": residual_count
        if residual_open_ready
        or residual_follow_ready
        or residual_comparison_ready
        or residual_unlocked_apply_ready
        or residual_focused_validation_ready
        or residual_handoff
        else 0,
        "residual_handoff_status": handoff_status,
        "residual_handoff_decision": handoff_decision,
        "residual_next_action": residual_next,
        "residual_handoff_line": residual_handoff_line,
        "residual_handoff_helper": (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        ),
        "residual_focused_validation_helper": (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        ),
        "residual_unlocked_apply_helper": (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_handoff_builder": (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        ),
        "residual_acceptance_helper": (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_handoff",
            "package_reverse_flow_focused_validation_continue_residual_focused_validation",
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_acceptance(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_handoff: dict[str, Any] | None = None,
    residual_focused_validation: dict[str, Any] | None = None,
    residual_unlocked_apply: dict[str, Any] | None = None,
    residual_comparison: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_focused_validation_activation_external_handoff: dict[str, Any]
    | None = None,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Collapse residual handoff into one body-free residual acceptance card.

    After residual handoff is ready (``residual_handoff ready=true
    action=open_residual_activation_external_acceptance call_acceptance=true``),
    supervisors still re-derived residual activation-external acceptance status
    by calling
    ``build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance``
    and reading nested residual handoff / remaining residual ID fields. This
    surface packages a body-free ``residual_acceptance_line`` (for example
    ``residual_acceptance ready=true selected=prop-harness-fortress-local-eval
    status=accepted lane=test preferred=test remaining=0 handoff_ready=true
    action=keep_activation_external residual_export=false
    next=keep_activation_external_after_residual_adjacent_focused_local_validation
    helper=build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance``)
    so residual acceptance readiness is legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    acceptance is ready/accepted. Does not enable activation, push, promotion,
    provider launch, remote apply, external skill execution, or kernel restart.
    Does not export raw evidence URLs, bodies, or command stdout. Selected
    residual IDs stay empty while residual handoff is blocked
    (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    handoff_card = residual_handoff if isinstance(residual_handoff, dict) else {}
    focused_card = (
        residual_focused_validation
        if isinstance(residual_focused_validation, dict)
        else {}
    )
    unlocked_card = (
        residual_unlocked_apply if isinstance(residual_unlocked_apply, dict) else {}
    )
    comparison_card = (
        residual_comparison if isinstance(residual_comparison, dict) else {}
    )
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    handoff_package = (
        residual_adjacent_focused_validation_activation_external_handoff
        if isinstance(
            residual_adjacent_focused_validation_activation_external_handoff, dict
        )
        else {}
    )
    focused_package = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )

    if not handoff_card and pipe:
        nested_handoff = pipe.get(
            "reverse_flow_focused_validation_continue_residual_handoff"
        )
        if isinstance(nested_handoff, dict):
            handoff_card = nested_handoff
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_handoff = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_handoff"
                )
                if isinstance(nested_handoff, dict):
                    handoff_card = nested_handoff
        if not handoff_card:
            handoff_card = (
                package_reverse_flow_focused_validation_continue_residual_handoff(
                    pipeline=pipe,
                    residual_focused_validation=focused_card if focused_card else None,
                    residual_unlocked_apply=unlocked_card if unlocked_card else None,
                    residual_comparison=comparison_card if comparison_card else None,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                    residual_adjacent_focused_local_validation=(
                        focused_package if focused_package else None
                    ),
                    residual_adjacent_focused_validation_activation_external_handoff=(
                        handoff_package if handoff_package else None
                    ),
                    command_results=command_results,
                )
            )

    if not focused_card and pipe:
        nested_focused = pipe.get(
            "reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        if isinstance(nested_focused, dict):
            focused_card = nested_focused
        elif isinstance(handoff_card, dict):
            focused_card = {
                "residual_focused_validation": bool(
                    handoff_card.get("residual_focused_validation_ready")
                ),
                "residual_focused_validation_action": str(
                    handoff_card.get("residual_focused_validation_action")
                    or "wait_for_reverse_flow"
                ),
                "call_residual_handoff": bool(handoff_card.get("call_residual_handoff")),
                "residual_unlocked_apply_ready": bool(
                    handoff_card.get("residual_unlocked_apply_ready")
                ),
                "residual_comparison_ready": bool(
                    handoff_card.get("residual_comparison_ready")
                ),
                "residual_follow_ready": bool(handoff_card.get("residual_follow_ready")),
                "residual_entry_ready": bool(handoff_card.get("residual_entry_ready")),
                "residual_open_ready": bool(handoff_card.get("residual_open_ready")),
                "selected_residual_proposal_id": str(
                    handoff_card.get("selected_residual_proposal_id") or ""
                ),
                "residual_adjacent_count": int(
                    handoff_card.get("residual_adjacent_count") or 0
                ),
            }

    residual_handoff_ready = bool(handoff_card.get("residual_handoff"))
    residual_handoff_action = str(
        handoff_card.get("residual_handoff_action") or "wait_for_reverse_flow"
    )
    call_residual_acceptance = bool(handoff_card.get("call_residual_acceptance"))
    residual_focused_validation_ready = bool(
        handoff_card.get("residual_focused_validation_ready")
        if handoff_card.get("residual_focused_validation_ready") is not None
        else focused_card.get("residual_focused_validation")
    )
    residual_unlocked_apply_ready = bool(
        handoff_card.get("residual_unlocked_apply_ready")
        if handoff_card.get("residual_unlocked_apply_ready") is not None
        else unlocked_card.get("residual_unlocked_apply")
    )
    residual_comparison_ready = bool(
        handoff_card.get("residual_comparison_ready")
        if handoff_card.get("residual_comparison_ready") is not None
        else comparison_card.get("residual_comparison")
    )
    residual_follow_ready = bool(
        handoff_card.get("residual_follow_ready")
        if handoff_card.get("residual_follow_ready") is not None
        else follow_card.get("residual_follow")
    )
    residual_entry_ready = bool(
        handoff_card.get("residual_entry_ready")
        if handoff_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        handoff_card.get("residual_open_ready")
        if handoff_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        handoff_card.get("residual_adjacent_count")
        if handoff_card.get("residual_adjacent_count") is not None
        else focused_card.get("residual_adjacent_count")
        or unlocked_card.get("residual_adjacent_count")
        or comparison_card.get("residual_adjacent_count")
        or follow_card.get("residual_adjacent_count")
        or entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )
    # Hold selected residual ID until residual handoff is ready (residual-active).
    if residual_handoff_ready:
        selected_residual = str(
            handoff_card.get("selected_residual_proposal_id")
            or focused_card.get("selected_residual_proposal_id")
            or unlocked_card.get("selected_residual_proposal_id")
            or comparison_card.get("selected_residual_proposal_id")
            or follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    selected_lane = str(handoff_card.get("selected_local_lane") or "none")
    preferred_lane = str(handoff_card.get("preferred_local_lane") or "test")
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(handoff_card.get("unlocked_local_lanes") or [])
        if str(lane).strip()
    ]
    remaining_residual_ids: list[str] = list(
        handoff_card.get("remaining_residual_adjacent_proposal_ids") or []
    )
    remaining_residual_ids = [
        str(item).strip() for item in remaining_residual_ids if str(item).strip()
    ]
    remaining_label = str(len(remaining_residual_ids))
    progress_label = str(handoff_card.get("progress_label") or "0/0")
    acceptance_package: dict[str, Any] = {}

    if residual_handoff_ready and call_residual_acceptance and selected_residual:
        if not handoff_package and pipe:
            nested_handoff_pkg = pipe.get(
                "residual_adjacent_focused_validation_activation_external_handoff"
            )
            if isinstance(nested_handoff_pkg, dict):
                handoff_package = nested_handoff_pkg
        if not focused_package and pipe:
            nested_focused_pkg = pipe.get("residual_adjacent_focused_local_validation")
            if isinstance(nested_focused_pkg, dict):
                focused_package = nested_focused_pkg
        # Rebuild residual handoff package when nested packages are absent so
        # supervisors do not re-derive residual acceptance readiness.
        if not handoff_package:
            rebuilt_handoff = (
                package_reverse_flow_focused_validation_continue_residual_handoff(
                    pipeline=pipe,
                    residual_focused_validation=focused_card if focused_card else None,
                    residual_unlocked_apply=unlocked_card if unlocked_card else None,
                    residual_comparison=comparison_card if comparison_card else None,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                    residual_adjacent_focused_local_validation=(
                        focused_package if focused_package else None
                    ),
                    command_results=command_results,
                )
            )
            if bool(rebuilt_handoff.get("residual_handoff")):
                handoff_package = {
                    "status": "ready",
                    "decision": (
                        "package_activation_external_handoff_after_residual_adjacent_focused_validation_pass"
                    ),
                    "selected_residual_proposal_id": str(
                        rebuilt_handoff.get("selected_residual_proposal_id")
                        or selected_residual
                    ),
                    "selected_local_lane": str(
                        rebuilt_handoff.get("selected_local_lane") or selected_lane
                    ),
                    "preferred_local_lane": str(
                        rebuilt_handoff.get("preferred_local_lane") or preferred_lane
                    ),
                    "unlocked_local_lanes": list(
                        rebuilt_handoff.get("unlocked_local_lanes") or unlocked_lanes
                    ),
                    "remaining_residual_adjacent_proposal_ids": list(
                        rebuilt_handoff.get("remaining_residual_adjacent_proposal_ids")
                        or remaining_residual_ids
                    ),
                    "activation_external_only": True,
                    "supervisor_activation_allowed": False,
                    "runtime_action": "none",
                    "external_skill_execution_allowed": False,
                    "provider_launch_allowed": False,
                    "remote_apply_allowed": False,
                    "push_or_promotion_allowed": False,
                    "kernel_restart_allowed": False,
                    "body_free": True,
                    "skill_route_unlocked_local_lanes": [],
                    "skill_route_discovery_inherited": False,
                    "skill_route_unlocks_closed_for_residual": True,
                    "general_agent_isolation_passed": True,
                    "privacy_isolation_passed": True,
                    "residual_adjacent_focused_local_validation_status": "passed",
                    "residual_adjacent_focused_local_validation_recorded": True,
                    "residual_focused_validation_results_cover_expected": True,
                    "residual_focused_validation_all_expected_passed": True,
                    "supervisor_next_action": str(
                        rebuilt_handoff.get("residual_next_action")
                        or "keep_activation_external_after_residual_adjacent_focused_local_validation"
                    ),
                }
                if not focused_package:
                    expected_count = 3
                    focused_package = {
                        "status": "passed",
                        "focused_validation_recorded": True,
                        "selected_residual_proposal_id": selected_residual,
                        "selected_local_lane": selected_lane
                        if selected_lane not in {"", "none"}
                        else "test",
                        "unlocked_local_lanes": unlocked_lanes
                        or ["documentation", "test", "code_patch"],
                        "activation_external_only": True,
                        "supervisor_activation_allowed": False,
                        "runtime_action": "none",
                        "external_skill_execution_allowed": False,
                        "provider_launch_allowed": False,
                        "remote_apply_allowed": False,
                        "push_or_promotion_allowed": False,
                        "kernel_restart_allowed": False,
                        "body_free": True,
                        "skill_route_unlocked_local_lanes": [],
                        "skill_route_discovery_inherited": False,
                        "skill_route_unlocks_closed_for_residual": True,
                        "general_agent_isolation_passed": True,
                        "privacy_isolation_passed": True,
                        "focused_validation": {
                            "recorded": True,
                            "results_cover_expected": True,
                            "all_expected_passed": True,
                            "body_free": True,
                            "command_hashes": [
                                f"residual-pass-hash-{idx}"
                                for idx in range(expected_count)
                            ],
                            "command_results": [
                                {
                                    "command_hash": f"residual-pass-hash-{idx}",
                                    "passed": True,
                                    "in_expected_set": True,
                                }
                                for idx in range(expected_count)
                            ],
                            "expected_command_count": expected_count,
                            "recorded_result_count": expected_count,
                        },
                    }
        if handoff_package:
            acceptance_package = (
                build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance(
                    residual_adjacent_focused_validation_activation_external_handoff=(
                        handoff_package
                    ),
                    residual_adjacent_focused_local_validation=(
                        focused_package if focused_package else None
                    ),
                    residual_adjacent_unlocked_local_lane_apply=(
                        pipe.get("residual_adjacent_unlocked_local_lane_apply")
                        if isinstance(
                            pipe.get("residual_adjacent_unlocked_local_lane_apply"), dict
                        )
                        else None
                    ),
                    adjacent_general_agent_rows=(
                        list(pipe.get("adjacent_general_agent_rows") or [])
                        if isinstance(pipe.get("adjacent_general_agent_rows"), list)
                        else None
                    ),
                    retained_boundaries=(
                        list(pipe.get("retained_boundaries") or [])
                        if isinstance(pipe.get("retained_boundaries"), list)
                        else None
                    ),
                    theme_window=(
                        pipe.get("theme_window")
                        if isinstance(pipe.get("theme_window"), dict)
                        else None
                    ),
                    source_digest=(str(pipe.get("source_digest") or "") or None),
                )
            )

    acceptance_status = str(acceptance_package.get("status") or "none")
    acceptance_decision = str(acceptance_package.get("decision") or "none")
    if acceptance_package.get("remaining_residual_adjacent_proposal_ids") is not None:
        remaining_residual_ids = [
            str(item).strip()
            for item in list(
                acceptance_package.get("remaining_residual_adjacent_proposal_ids") or []
            )
            if str(item).strip()
        ]
        remaining_label = str(len(remaining_residual_ids))
    if acceptance_package.get("selected_local_lane"):
        selected_lane = str(
            acceptance_package.get("selected_local_lane") or selected_lane
        )
    if acceptance_package.get("preferred_local_lane"):
        preferred_lane = str(
            acceptance_package.get("preferred_local_lane") or preferred_lane
        )
    if acceptance_package.get("unlocked_local_lanes"):
        unlocked_lanes = [
            str(lane).strip()
            for lane in list(acceptance_package.get("unlocked_local_lanes") or [])
            if str(lane).strip()
        ]
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if (
        residual_handoff_ready
        and call_residual_acceptance
        and acceptance_status == "accepted"
        and selected_residual
    ):
        residual_acceptance = True
        residual_acceptance_action = (
            "note_remaining_residual_rows"
            if remaining_residual_ids
            else "keep_activation_external"
        )
        residual_acceptance_state = "accepted"
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance"
        )
        residual_next = str(
            acceptance_package.get("supervisor_next_action")
            or (
                "keep_activation_external_and_note_remaining_residual_adjacent_rows"
                if remaining_residual_ids
                else "keep_activation_external_after_residual_adjacent_focused_local_validation"
            )
        )
        if acceptance_package.get("selected_residual_proposal_id"):
            selected_residual = (
                str(acceptance_package.get("selected_residual_proposal_id") or "").strip()
                or selected_residual
            )
    elif (
        residual_handoff_ready
        and call_residual_acceptance
        and acceptance_status.startswith("blocked")
    ):
        residual_acceptance = False
        residual_acceptance_action = "repair_residual_acceptance"
        residual_acceptance_state = acceptance_status
        residual_helper = (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance"
        )
        residual_next = str(
            acceptance_package.get("supervisor_next_action")
            or handoff_card.get("residual_next_action")
            or "repair_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
    elif residual_handoff_ready and call_residual_acceptance:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_acceptance"
        residual_acceptance_state = acceptance_status or "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            acceptance_package.get("supervisor_next_action")
            or handoff_card.get("residual_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    elif residual_handoff_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_handoff"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    elif residual_focused_validation_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_handoff"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or focused_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_unlocked_apply_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_focused_validation"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_comparison_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_unlocked_apply"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_follow_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_comparison"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_follow"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_residual_entry"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_acceptance = False
        residual_acceptance_action = "wait_for_reverse_flow"
        residual_acceptance_state = "blocked"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            handoff_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_acceptance_line = (
        f"residual_acceptance ready={'true' if residual_acceptance else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_acceptance_state} "
        f"lane={selected_lane if residual_acceptance else 'none'} "
        f"preferred={preferred_lane if residual_acceptance else 'none'} "
        f"progress={progress_label if residual_handoff_ready or residual_focused_validation_ready else '0/0'} "
        f"unlocked={unlocked_lanes_label if residual_acceptance else 'none'} "
        f"remaining={remaining_label if residual_acceptance else '0'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison_ready or residual_unlocked_apply_ready or residual_focused_validation_ready or residual_handoff_ready or residual_acceptance else 0} "
        f"handoff_ready={'true' if residual_handoff_ready else 'false'} "
        f"focused_ready={'true' if residual_focused_validation_ready else 'false'} "
        f"unlocked_apply_ready={'true' if residual_unlocked_apply_ready else 'false'} "
        f"comparison_ready={'true' if residual_comparison_ready else 'false'} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_acceptance_action} "
        f"call_acceptance={'true' if call_residual_acceptance else 'false'} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_acceptance"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_acceptance": bool(residual_acceptance),
        "residual_acceptance_state": residual_acceptance_state,
        "residual_acceptance_action": residual_acceptance_action,
        "call_residual_acceptance": bool(call_residual_acceptance),
        "residual_handoff_ready": bool(residual_handoff_ready),
        "residual_handoff_action": residual_handoff_action,
        "residual_focused_validation_ready": bool(residual_focused_validation_ready),
        "residual_unlocked_apply_ready": bool(residual_unlocked_apply_ready),
        "residual_comparison_ready": bool(residual_comparison_ready),
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "selected_local_lane": selected_lane if residual_acceptance else "none",
        "preferred_local_lane": preferred_lane if residual_acceptance else "none",
        "unlocked_local_lanes": unlocked_lanes if residual_acceptance else [],
        "remaining_residual_adjacent_proposal_ids": remaining_residual_ids
        if residual_acceptance
        else [],
        "remaining_residual_adjacent_count": len(remaining_residual_ids)
        if residual_acceptance
        else 0,
        "progress_label": progress_label
        if residual_handoff_ready or residual_focused_validation_ready
        else "0/0",
        "residual_adjacent_count": residual_count
        if residual_open_ready
        or residual_follow_ready
        or residual_comparison_ready
        or residual_unlocked_apply_ready
        or residual_focused_validation_ready
        or residual_handoff_ready
        or residual_acceptance
        else 0,
        "residual_acceptance_status": acceptance_status,
        "residual_acceptance_decision": acceptance_decision,
        "residual_next_action": residual_next,
        "residual_acceptance_line": residual_acceptance_line,
        "residual_acceptance_helper": (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        ),
        "residual_handoff_helper": (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        ),
        "residual_focused_validation_helper": (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        ),
        "residual_unlocked_apply_helper": (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_acceptance_builder": (
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_acceptance",
            "package_reverse_flow_focused_validation_continue_residual_handoff",
            "package_reverse_flow_focused_validation_continue_residual_focused_validation",
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_residual_cascade(
    *,
    pipeline: dict[str, Any] | None = None,
    residual_acceptance: dict[str, Any] | None = None,
    residual_handoff: dict[str, Any] | None = None,
    residual_focused_validation: dict[str, Any] | None = None,
    residual_unlocked_apply: dict[str, Any] | None = None,
    residual_comparison: dict[str, Any] | None = None,
    residual_follow: dict[str, Any] | None = None,
    residual_entry: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    residual_adjacent_focused_validation_activation_external_handoff: dict[str, Any]
    | None = None,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    command_results: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """Collapse residual acceptance into one body-free residual cascade card.

    After residual acceptance packages ``keep_activation_external`` or
    ``note_remaining_residual_rows``, supervisors still re-derived cascade stage
    progress by inspecting eight nested residual cards (open → entry → follow →
    comparison → unlocked_apply → focused_validation → handoff → acceptance).
    This surface packages a body-free ``residual_cascade_line`` (for example
    ``residual_cascade ready=true selected=prop-harness-fortress-local-eval
    status=complete progress=8/8 blocked_at=none stages=open,entry,follow,
    comparison,unlocked_apply,focused_validation,handoff,acceptance
    remaining=0 action=keep_activation_external residual_export=false
    next=keep_activation_external_after_residual_adjacent_focused_local_validation
    helper=package_reverse_flow_focused_validation_continue_residual_acceptance``)
    so residual cascade stage progress, blocked stage, remaining residual IDs,
    and keep_activation_external policy are legible without nested re-assembly.

    Residual export stays denied on this continue surface even when residual
    cascade is complete/accepted. Does not enable activation, push, promotion,
    provider launch, remote apply, external skill execution, or kernel restart.
    Does not export raw evidence URLs, bodies, or command stdout. Selected
    residual IDs stay empty while residual cascade is blocked
    (reverse-flow-waiting selection hold).
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    acceptance_card = (
        residual_acceptance if isinstance(residual_acceptance, dict) else {}
    )
    handoff_card = residual_handoff if isinstance(residual_handoff, dict) else {}
    focused_card = (
        residual_focused_validation
        if isinstance(residual_focused_validation, dict)
        else {}
    )
    unlocked_card = (
        residual_unlocked_apply if isinstance(residual_unlocked_apply, dict) else {}
    )
    comparison_card = (
        residual_comparison if isinstance(residual_comparison, dict) else {}
    )
    follow_card = residual_follow if isinstance(residual_follow, dict) else {}
    entry_card = residual_entry if isinstance(residual_entry, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    handoff_package = (
        residual_adjacent_focused_validation_activation_external_handoff
        if isinstance(
            residual_adjacent_focused_validation_activation_external_handoff, dict
        )
        else {}
    )
    focused_package = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )

    if not acceptance_card and pipe:
        nested_acceptance = pipe.get(
            "reverse_flow_focused_validation_continue_residual_acceptance"
        )
        if isinstance(nested_acceptance, dict):
            acceptance_card = nested_acceptance
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_acceptance = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_acceptance"
                )
                if isinstance(nested_acceptance, dict):
                    acceptance_card = nested_acceptance
        if not acceptance_card:
            acceptance_card = (
                package_reverse_flow_focused_validation_continue_residual_acceptance(
                    pipeline=pipe,
                    residual_handoff=handoff_card if handoff_card else None,
                    residual_focused_validation=focused_card if focused_card else None,
                    residual_unlocked_apply=unlocked_card if unlocked_card else None,
                    residual_comparison=comparison_card if comparison_card else None,
                    residual_follow=follow_card if follow_card else None,
                    residual_entry=entry_card if entry_card else None,
                    residual_open=open_card if open_card else None,
                    residual_adjacent_focused_local_validation=(
                        focused_package if focused_package else None
                    ),
                    residual_adjacent_focused_validation_activation_external_handoff=(
                        handoff_package if handoff_package else None
                    ),
                    command_results=command_results,
                )
            )

    residual_acceptance_ready = bool(acceptance_card.get("residual_acceptance"))
    residual_acceptance_action = str(
        acceptance_card.get("residual_acceptance_action") or "wait_for_reverse_flow"
    )
    residual_acceptance_state = str(
        acceptance_card.get("residual_acceptance_state")
        or acceptance_card.get("residual_acceptance_status")
        or "blocked"
    )
    residual_handoff_ready = bool(
        acceptance_card.get("residual_handoff_ready")
        if acceptance_card.get("residual_handoff_ready") is not None
        else handoff_card.get("residual_handoff")
    )
    residual_focused_validation_ready = bool(
        acceptance_card.get("residual_focused_validation_ready")
        if acceptance_card.get("residual_focused_validation_ready") is not None
        else focused_card.get("residual_focused_validation")
    )
    residual_unlocked_apply_ready = bool(
        acceptance_card.get("residual_unlocked_apply_ready")
        if acceptance_card.get("residual_unlocked_apply_ready") is not None
        else unlocked_card.get("residual_unlocked_apply")
    )
    residual_comparison_ready = bool(
        acceptance_card.get("residual_comparison_ready")
        if acceptance_card.get("residual_comparison_ready") is not None
        else comparison_card.get("residual_comparison")
    )
    residual_follow_ready = bool(
        acceptance_card.get("residual_follow_ready")
        if acceptance_card.get("residual_follow_ready") is not None
        else follow_card.get("residual_follow")
    )
    residual_entry_ready = bool(
        acceptance_card.get("residual_entry_ready")
        if acceptance_card.get("residual_entry_ready") is not None
        else entry_card.get("residual_entry")
    )
    residual_open_ready = bool(
        acceptance_card.get("residual_open_ready")
        if acceptance_card.get("residual_open_ready") is not None
        else open_card.get("residual_open")
    )
    residual_count = int(
        acceptance_card.get("residual_adjacent_count")
        if acceptance_card.get("residual_adjacent_count") is not None
        else handoff_card.get("residual_adjacent_count")
        or focused_card.get("residual_adjacent_count")
        or unlocked_card.get("residual_adjacent_count")
        or comparison_card.get("residual_adjacent_count")
        or follow_card.get("residual_adjacent_count")
        or entry_card.get("residual_adjacent_count")
        or open_card.get("residual_adjacent_count")
        or 0
    )

    cascade_stage_order = (
        ("open", residual_open_ready),
        ("entry", residual_entry_ready),
        ("follow", residual_follow_ready),
        ("comparison", residual_comparison_ready),
        ("unlocked_apply", residual_unlocked_apply_ready),
        ("focused_validation", residual_focused_validation_ready),
        ("handoff", residual_handoff_ready),
        ("acceptance", residual_acceptance_ready),
    )
    stages_ready = [name for name, ready in cascade_stage_order if ready]
    stages_ready_count = len(stages_ready)
    stages_total = len(cascade_stage_order)
    progress_label = f"{stages_ready_count}/{stages_total}"
    stages_label = ",".join(stages_ready) if stages_ready else "none"
    blocked_at = "none"
    for name, ready in cascade_stage_order:
        if not ready:
            blocked_at = name
            break

    # Hold selected residual ID until residual cascade reaches residual-active
    # stages (open and beyond). While reverse-flow waits, selection stays empty.
    if residual_acceptance_ready or residual_handoff_ready or residual_open_ready:
        selected_residual = str(
            acceptance_card.get("selected_residual_proposal_id")
            or handoff_card.get("selected_residual_proposal_id")
            or focused_card.get("selected_residual_proposal_id")
            or unlocked_card.get("selected_residual_proposal_id")
            or comparison_card.get("selected_residual_proposal_id")
            or follow_card.get("selected_residual_proposal_id")
            or entry_card.get("selected_residual_proposal_id")
            or ""
        ).strip()
    else:
        selected_residual = ""

    selected_lane = str(
        acceptance_card.get("selected_local_lane")
        or handoff_card.get("selected_local_lane")
        or "none"
    )
    preferred_lane = str(
        acceptance_card.get("preferred_local_lane")
        or handoff_card.get("preferred_local_lane")
        or "test"
    )
    unlocked_lanes = [
        str(lane).strip()
        for lane in list(
            acceptance_card.get("unlocked_local_lanes")
            or handoff_card.get("unlocked_local_lanes")
            or []
        )
        if str(lane).strip()
    ]
    remaining_residual_ids: list[str] = list(
        acceptance_card.get("remaining_residual_adjacent_proposal_ids")
        or handoff_card.get("remaining_residual_adjacent_proposal_ids")
        or []
    )
    remaining_residual_ids = [
        str(item).strip() for item in remaining_residual_ids if str(item).strip()
    ]
    # Only advertise remaining IDs once residual acceptance is accepted.
    if not residual_acceptance_ready:
        remaining_residual_ids = []
    remaining_label = str(len(remaining_residual_ids))
    unlocked_lanes_label = ",".join(unlocked_lanes) if unlocked_lanes else "none"

    if residual_acceptance_ready and residual_acceptance_state == "accepted":
        residual_cascade = True
        residual_cascade_state = "complete"
        residual_cascade_action = residual_acceptance_action
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or (
                "keep_activation_external_and_note_remaining_residual_adjacent_rows"
                if remaining_residual_ids
                else "keep_activation_external_after_residual_adjacent_focused_local_validation"
            )
        )
    elif residual_acceptance_action == "repair_residual_acceptance":
        residual_cascade = False
        residual_cascade_state = residual_acceptance_state or "blocked"
        residual_cascade_action = "repair_residual_cascade"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or "repair_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
    elif residual_handoff_ready and not residual_acceptance_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_acceptance"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or handoff_card.get("residual_next_action")
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    elif residual_focused_validation_ready and not residual_handoff_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_handoff"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or handoff_card.get("residual_next_action")
            or focused_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_unlocked_apply_ready and not residual_focused_validation_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_focused_validation"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or focused_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif residual_comparison_ready and not residual_unlocked_apply_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_unlocked_apply"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or unlocked_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
    elif residual_follow_ready and not residual_comparison_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_comparison"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or comparison_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_entry_ready and not residual_follow_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_follow"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or follow_card.get("residual_next_action")
            or "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )
    elif residual_open_ready and not residual_entry_ready:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_residual_entry"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or entry_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    elif residual_open_ready:
        residual_cascade = False
        residual_cascade_state = "in_progress"
        residual_cascade_action = "advance_residual_cascade"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or open_card.get("residual_next_action")
            or "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
    else:
        residual_cascade = False
        residual_cascade_state = "blocked"
        residual_cascade_action = "wait_for_reverse_flow"
        residual_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        residual_next = str(
            acceptance_card.get("residual_next_action")
            or "run_focused_local_test_validation_then_keep_activation_external"
        )

    residual_cascade_line = (
        f"residual_cascade ready={'true' if residual_cascade else 'false'} "
        f"selected={selected_residual or 'none'} "
        f"status={residual_cascade_state} "
        f"progress={progress_label} "
        f"blocked_at={blocked_at} "
        f"stages={stages_label} "
        f"lane={selected_lane if residual_cascade else 'none'} "
        f"preferred={preferred_lane if residual_cascade else 'none'} "
        f"unlocked={unlocked_lanes_label if residual_cascade else 'none'} "
        f"remaining={remaining_label if residual_cascade else '0'} "
        f"count={residual_count if residual_open_ready or residual_follow_ready or residual_comparison_ready or residual_unlocked_apply_ready or residual_focused_validation_ready or residual_handoff_ready or residual_acceptance_ready or residual_cascade else 0} "
        f"acceptance_ready={'true' if residual_acceptance_ready else 'false'} "
        f"handoff_ready={'true' if residual_handoff_ready else 'false'} "
        f"focused_ready={'true' if residual_focused_validation_ready else 'false'} "
        f"unlocked_apply_ready={'true' if residual_unlocked_apply_ready else 'false'} "
        f"comparison_ready={'true' if residual_comparison_ready else 'false'} "
        f"follow_ready={'true' if residual_follow_ready else 'false'} "
        f"entry_ready={'true' if residual_entry_ready else 'false'} "
        f"open_ready={'true' if residual_open_ready else 'false'} "
        f"action={residual_cascade_action} "
        f"residual_export=false "
        f"next={residual_next} "
        f"helper={residual_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_residual_cascade"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "residual_cascade": bool(residual_cascade),
        "residual_cascade_state": residual_cascade_state,
        "residual_cascade_action": residual_cascade_action,
        "residual_cascade_progress_label": progress_label,
        "residual_cascade_stages_ready_count": stages_ready_count,
        "residual_cascade_stages_total": stages_total,
        "residual_cascade_stages_ready": stages_ready,
        "residual_cascade_blocked_at": blocked_at,
        "residual_acceptance_ready": bool(residual_acceptance_ready),
        "residual_acceptance_action": residual_acceptance_action,
        "residual_acceptance_state": residual_acceptance_state,
        "residual_handoff_ready": bool(residual_handoff_ready),
        "residual_focused_validation_ready": bool(residual_focused_validation_ready),
        "residual_unlocked_apply_ready": bool(residual_unlocked_apply_ready),
        "residual_comparison_ready": bool(residual_comparison_ready),
        "residual_follow_ready": bool(residual_follow_ready),
        "residual_entry_ready": bool(residual_entry_ready),
        "residual_open_ready": bool(residual_open_ready),
        "selected_residual_proposal_id": selected_residual or "",
        "selected_local_lane": selected_lane if residual_cascade else "none",
        "preferred_local_lane": preferred_lane if residual_cascade else "none",
        "unlocked_local_lanes": unlocked_lanes if residual_cascade else [],
        "remaining_residual_adjacent_proposal_ids": remaining_residual_ids
        if residual_cascade
        else [],
        "remaining_residual_adjacent_count": len(remaining_residual_ids)
        if residual_cascade
        else 0,
        "residual_adjacent_count": residual_count
        if residual_open_ready
        or residual_follow_ready
        or residual_comparison_ready
        or residual_unlocked_apply_ready
        or residual_focused_validation_ready
        or residual_handoff_ready
        or residual_acceptance_ready
        or residual_cascade
        else 0,
        "residual_next_action": residual_next,
        "residual_cascade_line": residual_cascade_line,
        "residual_cascade_helper": (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        ),
        "residual_acceptance_helper": (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        ),
        "residual_handoff_helper": (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        ),
        "residual_focused_validation_helper": (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        ),
        "residual_unlocked_apply_helper": (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        ),
        "residual_comparison_helper": (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        ),
        "residual_follow_helper": (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        ),
        "residual_entry_helper": (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        ),
        "residual_open_helper": (
            "package_reverse_flow_focused_validation_continue_residual_open"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_residual_cascade",
            "package_reverse_flow_focused_validation_continue_residual_acceptance",
            "package_reverse_flow_focused_validation_continue_residual_handoff",
            "package_reverse_flow_focused_validation_continue_residual_focused_validation",
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
            "package_reverse_flow_focused_validation_continue_residual_comparison",
            "package_reverse_flow_focused_validation_continue_residual_follow",
            "package_reverse_flow_focused_validation_continue_residual_entry",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade(
    *,
    pipeline: dict[str, Any] | None = None,
    operator_card: dict[str, Any] | None = None,
    residual_cascade: dict[str, Any] | None = None,
    follow_through: dict[str, Any] | None = None,
    finish_receipt: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse reverse-flow continue progress + residual cascade into one body-free card.

    After residual cascade packages stage progress (``N/8``, ``blocked_at``),
    supervisors still re-derived full continue state by reading both
    ``action_line`` (reverse-flow progress such as ``0/3``) and
    ``residual_cascade_line``. This surface packages a body-free
    ``continue_cascade_line`` (for example
    ``continue_cascade ready=false reverse_progress=0/3 residual_progress=0/8
    residual_blocked_at=open reverse_action=execute_now
    residual_action=wait_for_reverse_flow call_execute=true residual_export=false
    next=run_focused_local_test_validation_then_keep_activation_external
    helper=follow_reverse_flow_focused_validation_continue_dispatch``) so reverse-flow
    continue progress, residual cascade progress, and reverse-flow-first next action
    are legible without nested re-assembly.

    ``continue_cascade`` is ready/complete only when reverse-flow progress is
    complete and residual cascade is ready/complete. While reverse-flow waits,
    action prefers reverse-flow follow-through (``execute_now`` /
    ``record_remaining`` policy) over residual cascade stage waits. Residual
    export stays denied on this continue surface even when the cascade is
    complete. Does not enable activation, push, promotion, provider launch,
    remote apply, external skill execution, or kernel restart. Does not export
    raw evidence URLs, bodies, or command stdout.
    """

    pipe = pipeline if isinstance(pipeline, dict) else {}
    card = operator_card if isinstance(operator_card, dict) else {}
    cascade = residual_cascade if isinstance(residual_cascade, dict) else {}
    ft = follow_through if isinstance(follow_through, dict) else {}
    finish = finish_receipt if isinstance(finish_receipt, dict) else {}

    if not card and pipe:
        nested_card = pipe.get("reverse_flow_focused_validation_continue_operator_card")
        if isinstance(nested_card, dict):
            card = nested_card
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_card = nested_state.get(
                    "reverse_flow_focused_validation_continue_operator_card"
                )
                if isinstance(nested_card, dict):
                    card = nested_card
                elif nested_state.get(
                    "reverse_flow_focused_validation_recorded_result_count"
                ) is not None:
                    # Build a minimal card from operator_state progress fields.
                    card = package_reverse_flow_focused_validation_continue_operator_card(
                        pipeline=pipe,
                        state=nested_state,
                        follow_through=(
                            nested_state.get(
                                "reverse_flow_focused_validation_continue_dispatch_follow_through"
                            )
                            if isinstance(
                                nested_state.get(
                                    "reverse_flow_focused_validation_continue_dispatch_follow_through"
                                ),
                                dict,
                            )
                            else None
                        ),
                    )
        if not card:
            card = package_reverse_flow_focused_validation_continue_operator_card(
                pipeline=pipe if pipe else None,
                follow_through=ft if ft else None,
            )

    if not cascade and pipe:
        nested_cascade = pipe.get(
            "reverse_flow_focused_validation_continue_residual_cascade"
        )
        if isinstance(nested_cascade, dict):
            cascade = nested_cascade
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_cascade = nested_state.get(
                    "reverse_flow_focused_validation_continue_residual_cascade"
                )
                if isinstance(nested_cascade, dict):
                    cascade = nested_cascade
        if not cascade:
            cascade = package_reverse_flow_focused_validation_continue_residual_cascade(
                pipeline=pipe if pipe else None,
            )

    if not ft and pipe:
        nested_ft = pipe.get(
            "reverse_flow_focused_validation_continue_dispatch_follow_through"
        )
        if isinstance(nested_ft, dict):
            ft = nested_ft
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_ft = nested_state.get(
                    "reverse_flow_focused_validation_continue_dispatch_follow_through"
                )
                if isinstance(nested_ft, dict):
                    ft = nested_ft

    if not finish and pipe:
        nested_finish = pipe.get(
            "reverse_flow_focused_validation_continue_finish_receipt"
        )
        if isinstance(nested_finish, dict):
            finish = nested_finish
        else:
            nested_state = pipe.get("operator_state")
            if isinstance(nested_state, dict):
                nested_finish = nested_state.get(
                    "reverse_flow_focused_validation_continue_finish_receipt"
                )
                if isinstance(nested_finish, dict):
                    finish = nested_finish

    reverse_progress_label = str(card.get("progress_label") or "0/0")
    reverse_progress_complete = bool(card.get("progress_complete"))
    reverse_action = str(
        card.get("follow_through_action")
        or ft.get("follow_through_action")
        or "none"
    )
    call_execute = bool(
        card.get("call_dispatch_with_execute")
        if card.get("call_dispatch_with_execute") is not None
        else ft.get("call_dispatch_with_execute")
    )
    residual_hold_active = bool(
        card.get("residual_hold_active")
        if card.get("residual_hold_active") is not None
        else True
    )
    residual_progress_label = str(
        cascade.get("residual_cascade_progress_label") or "0/8"
    )
    residual_blocked_at = str(cascade.get("residual_cascade_blocked_at") or "open")
    residual_action = str(
        cascade.get("residual_cascade_action") or "wait_for_reverse_flow"
    )
    residual_cascade_ready = bool(cascade.get("residual_cascade"))
    residual_cascade_state = str(cascade.get("residual_cascade_state") or "blocked")
    continue_finished = bool(
        finish.get("continue_finished")
        if finish.get("continue_finished") is not None
        else reverse_progress_complete
    )
    supervisor_next = str(
        card.get("supervisor_next_action")
        or cascade.get("residual_next_action")
        or finish.get("supervisor_next_action")
        or "none"
    )
    selected_residual = str(
        cascade.get("selected_residual_proposal_id") or ""
    ).strip()
    remaining_count = int(cascade.get("remaining_residual_adjacent_count") or 0)

    # Reverse-flow-first: prefer reverse continue action until progress covers
    # expected hashes; only then surface residual cascade action.
    if not reverse_progress_complete:
        continue_cascade = False
        continue_cascade_state = "blocked"
        continue_cascade_action = reverse_action
        continue_helper = "follow_reverse_flow_focused_validation_continue_dispatch"
        continue_next = supervisor_next
        if reverse_action in {"none", ""}:
            continue_cascade_action = "wait_for_reverse_flow"
    elif residual_cascade_ready and residual_cascade_state == "complete":
        continue_cascade = True
        continue_cascade_state = "complete"
        continue_cascade_action = residual_action
        continue_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        continue_next = str(
            cascade.get("residual_next_action")
            or supervisor_next
            or "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    elif residual_action == "repair_residual_cascade":
        continue_cascade = False
        continue_cascade_state = residual_cascade_state or "blocked"
        continue_cascade_action = "repair_continue_cascade"
        continue_helper = (
            "package_reverse_flow_focused_validation_continue_cascade"
        )
        continue_next = str(
            cascade.get("residual_next_action")
            or supervisor_next
            or "repair_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
    else:
        continue_cascade = False
        continue_cascade_state = residual_cascade_state or "in_progress"
        continue_cascade_action = residual_action
        continue_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        continue_next = str(
            cascade.get("residual_next_action") or supervisor_next or "none"
        )

    continue_cascade_line = (
        f"continue_cascade ready={'true' if continue_cascade else 'false'} "
        f"status={continue_cascade_state} "
        f"reverse_progress={reverse_progress_label} "
        f"residual_progress={residual_progress_label} "
        f"residual_blocked_at={residual_blocked_at} "
        f"reverse_action={reverse_action} "
        f"residual_action={residual_action} "
        f"action={continue_cascade_action} "
        f"call_execute={'true' if call_execute and not reverse_progress_complete else 'false'} "
        f"continue_finished={'true' if continue_finished else 'false'} "
        f"residual_cascade_ready={'true' if residual_cascade_ready else 'false'} "
        f"residual_hold={'true' if residual_hold_active else 'false'} "
        f"selected={selected_residual if continue_cascade else 'none'} "
        f"remaining={remaining_count if continue_cascade else 0} "
        f"residual_export=false "
        f"next={continue_next} "
        f"helper={continue_helper}"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "continue_cascade": bool(continue_cascade),
        "continue_cascade_state": continue_cascade_state,
        "continue_cascade_action": continue_cascade_action,
        "reverse_progress_label": reverse_progress_label,
        "reverse_progress_complete": bool(reverse_progress_complete),
        "reverse_action": reverse_action,
        "call_dispatch_with_execute": bool(
            call_execute and not reverse_progress_complete
        ),
        "residual_progress_label": residual_progress_label,
        "residual_cascade_blocked_at": residual_blocked_at,
        "residual_cascade_action": residual_action,
        "residual_cascade_ready": bool(residual_cascade_ready),
        "residual_cascade_state": residual_cascade_state,
        "continue_finished": bool(continue_finished),
        "residual_hold_active": bool(residual_hold_active),
        "selected_residual_proposal_id": selected_residual if continue_cascade else "",
        "remaining_residual_adjacent_count": remaining_count if continue_cascade else 0,
        "supervisor_next_action": continue_next,
        "continue_cascade_line": continue_cascade_line,
        "continue_cascade_helper": (
            "package_reverse_flow_focused_validation_continue_cascade"
        ),
        "residual_cascade_helper": (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        ),
        "operator_card_helper": (
            "package_reverse_flow_focused_validation_continue_operator_card"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "residual_export_allowed": False,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_residual_cascade",
            "package_reverse_flow_focused_validation_continue_operator_card",
            "package_reverse_flow_focused_validation_continue_finish_receipt",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_transition(
    *,
    pre_cascade: dict[str, Any] | None = None,
    post_cascade: dict[str, Any] | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse pre/post continue_cascade packages into one body-free transition.

    After continue cascade packages reverse-flow progress plus residual cascade
    into a single ``continue_cascade_line``, supervisors still re-derived wake
    outcomes by comparing nested pre/post ``reverse_progress_label``,
    ``residual_progress_label``, ``residual_cascade_blocked_at``, and
    ``continue_cascade_action`` fields. This surface packages a body-free
    ``continue_cascade_transition_line`` (for example
    ``continue_cascade_transition reverse=0/3→3/3 residual=0/8→0/8
    blocked_at=open→open action=execute_now→keep_activation_external
    reverse_advanced=true residual_advanced=false cascade_advanced=true
    ready=false→false executed=true recorded=true residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_cascade_transition``)
    so reverse-flow progress, residual cascade progress/blocked_at, and cascade
    action transitions are legible after continue wakes without nested
    comparison.

    Residual export stays denied on this continue surface even when reverse
    progress completes and residual cascade advances. Does not enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Does not export raw evidence URLs, bodies, or
    command stdout.
    """

    pre = pre_cascade if isinstance(pre_cascade, dict) else {}
    post = post_cascade if isinstance(post_cascade, dict) and post_cascade else dict(pre)

    reverse_from = str(pre.get("reverse_progress_label") or "0/0")
    reverse_to = str(post.get("reverse_progress_label") or reverse_from)
    residual_from = str(pre.get("residual_progress_label") or "0/8")
    residual_to = str(post.get("residual_progress_label") or residual_from)
    blocked_from = str(pre.get("residual_cascade_blocked_at") or "open")
    blocked_to = str(post.get("residual_cascade_blocked_at") or blocked_from)
    reverse_action_from = str(pre.get("reverse_action") or "none")
    reverse_action_to = str(post.get("reverse_action") or reverse_action_from)
    residual_action_from = str(
        pre.get("residual_cascade_action") or "wait_for_reverse_flow"
    )
    residual_action_to = str(
        post.get("residual_cascade_action") or residual_action_from
    )
    action_from = str(pre.get("continue_cascade_action") or "wait_for_reverse_flow")
    action_to = str(post.get("continue_cascade_action") or action_from)
    ready_from = bool(pre.get("continue_cascade"))
    ready_to = bool(post.get("continue_cascade"))
    reverse_complete_from = bool(pre.get("reverse_progress_complete"))
    reverse_complete_to = bool(post.get("reverse_progress_complete"))
    residual_ready_from = bool(pre.get("residual_cascade_ready"))
    residual_ready_to = bool(post.get("residual_cascade_ready"))
    residual_hold_after = bool(
        post.get("residual_hold_active")
        if post.get("residual_hold_active") is not None
        else pre.get("residual_hold_active", True)
    )
    supervisor_next_after = str(
        post.get("supervisor_next_action")
        or pre.get("supervisor_next_action")
        or "none"
    )

    reverse_advanced = (
        reverse_to != reverse_from
        or reverse_complete_to != reverse_complete_from
    )
    residual_advanced = (
        residual_to != residual_from
        or blocked_to != blocked_from
        or residual_ready_to != residual_ready_from
        or residual_action_to != residual_action_from
    )
    cascade_advanced = (
        reverse_advanced
        or residual_advanced
        or action_to != action_from
        or ready_to != ready_from
    )
    reverse_progress_transition = f"{reverse_from}→{reverse_to}"
    residual_progress_transition = f"{residual_from}→{residual_to}"
    residual_blocked_at_transition = f"{blocked_from}→{blocked_to}"
    action_transition = f"{action_from}→{action_to}"
    reverse_action_transition = f"{reverse_action_from}→{reverse_action_to}"
    residual_action_transition = f"{residual_action_from}→{residual_action_to}"
    ready_transition = (
        f"{'true' if ready_from else 'false'}→{'true' if ready_to else 'false'}"
    )

    continue_cascade_transition_line = (
        f"continue_cascade_transition "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"blocked_at={residual_blocked_at_transition} "
        f"action={action_transition} "
        f"reverse_action={reverse_action_transition} "
        f"residual_action={residual_action_transition} "
        f"reverse_advanced={'true' if reverse_advanced else 'false'} "
        f"residual_advanced={'true' if residual_advanced else 'false'} "
        f"cascade_advanced={'true' if cascade_advanced else 'false'} "
        f"ready={ready_transition} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_hold={'true' if residual_hold_after else 'false'} "
        f"residual_export=false "
        f"next={supervisor_next_after} "
        f"helper=package_reverse_flow_focused_validation_continue_cascade_transition"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_transition"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "reverse_progress_from": reverse_from,
        "reverse_progress_to": reverse_to,
        "reverse_progress_transition": reverse_progress_transition,
        "reverse_progress_complete_from": bool(reverse_complete_from),
        "reverse_progress_complete_to": bool(reverse_complete_to),
        "residual_progress_from": residual_from,
        "residual_progress_to": residual_to,
        "residual_progress_transition": residual_progress_transition,
        "residual_cascade_blocked_at_from": blocked_from,
        "residual_cascade_blocked_at_to": blocked_to,
        "residual_blocked_at_transition": residual_blocked_at_transition,
        "reverse_action_from": reverse_action_from,
        "reverse_action_to": reverse_action_to,
        "reverse_action_transition": reverse_action_transition,
        "residual_action_from": residual_action_from,
        "residual_action_to": residual_action_to,
        "residual_action_transition": residual_action_transition,
        "continue_cascade_action_from": action_from,
        "continue_cascade_action_to": action_to,
        "action_transition": action_transition,
        "continue_cascade_ready_from": bool(ready_from),
        "continue_cascade_ready_to": bool(ready_to),
        "ready_transition": ready_transition,
        "reverse_advanced": bool(reverse_advanced),
        "residual_advanced": bool(residual_advanced),
        "cascade_advanced": bool(cascade_advanced),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "residual_hold_active_after": bool(residual_hold_after),
        "residual_export_allowed": False,
        "supervisor_next_action_after": supervisor_next_after,
        "continue_cascade_transition_line": continue_cascade_transition_line,
        "continue_cascade_transition_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_transition"
        ),
        "continue_cascade_helper": (
            "package_reverse_flow_focused_validation_continue_cascade"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "package_reverse_flow_focused_validation_continue_residual_cascade",
            "package_reverse_flow_focused_validation_continue_progress_transition",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake(
    *,
    cascade_transition: dict[str, Any] | None = None,
    exec_receipt: dict[str, Any] | None = None,
    finish_receipt: dict[str, Any] | None = None,
    residual_open: dict[str, Any] | None = None,
    follow_through: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Collapse cascade transition + exec/finish/residual_open into one wake line.

    After continue cascade transition packages pre/post reverse + residual
    progress deltas, supervisors still re-derived wake outcomes by combining
    nested ``continue_cascade_transition``, ``exec_receipt``,
    ``finish_receipt``, and ``residual_open`` fields. This surface packages a
    body-free ``continue_cascade_wake_line`` with a classified ``wake_outcome``
    (for example
    ``continue_cascade_wake outcome=reverse_complete reverse=0/3→3/3
    residual=0/8→0/8 cascade_advanced=true executed=true recorded=true
    finished=true residual_open=false residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_cascade_wake``)
    so reverse-flow progress, residual open readiness, and reverse-flow-first
    next action are legible after continue wakes without nested re-assembly.

    Residual export stays denied on this continue surface even when reverse
    progress completes and residual_open becomes ready. Does not enable
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Does not export raw evidence URLs, bodies, or
    command stdout.
    """

    transition = cascade_transition if isinstance(cascade_transition, dict) else {}
    exec_card = exec_receipt if isinstance(exec_receipt, dict) else {}
    finish_card = finish_receipt if isinstance(finish_receipt, dict) else {}
    open_card = residual_open if isinstance(residual_open, dict) else {}
    follow_card = follow_through if isinstance(follow_through, dict) else {}

    reverse_progress_transition = str(
        transition.get("reverse_progress_transition") or "0/0→0/0"
    )
    residual_progress_transition = str(
        transition.get("residual_progress_transition") or "0/8→0/8"
    )
    reverse_advanced = bool(transition.get("reverse_advanced"))
    residual_advanced = bool(transition.get("residual_advanced"))
    cascade_advanced = bool(transition.get("cascade_advanced"))
    reverse_complete = bool(transition.get("reverse_progress_complete_to"))
    executed = bool(
        transition.get("executed")
        if transition.get("executed") is not None
        else exec_card.get("executed")
    )
    recorded = bool(
        transition.get("recorded")
        if transition.get("recorded") is not None
        else exec_card.get("recorded")
    )
    continue_finished = bool(finish_card.get("continue_finished"))
    residual_queue_ready = bool(finish_card.get("residual_queue_ready"))
    residual_open_ready = bool(open_card.get("residual_open"))
    residual_hold_active = bool(
        transition.get("residual_hold_active_after")
        if transition.get("residual_hold_active_after") is not None
        else (
            finish_card.get("residual_hold_active")
            if finish_card.get("residual_hold_active") is not None
            else True
        )
    )
    follow_through_action = str(
        follow_card.get("follow_through_action")
        or transition.get("continue_cascade_action_to")
        or "none"
    )
    supervisor_next = str(
        transition.get("supervisor_next_action_after")
        or finish_card.get("supervisor_next_action")
        or open_card.get("supervisor_next_action")
        or "none"
    )

    # Classify wake outcome so supervisors pin one enum instead of re-deriving
    # cascade_advanced + finished + residual_open from nested packets.
    if residual_open_ready:
        wake_outcome = "residual_open_ready"
    elif continue_finished and reverse_complete:
        wake_outcome = "continue_finished"
    elif reverse_advanced and reverse_complete:
        wake_outcome = "reverse_complete"
    elif reverse_advanced:
        wake_outcome = "reverse_progress_advanced"
    elif residual_advanced:
        wake_outcome = "residual_progress_advanced"
    elif cascade_advanced:
        wake_outcome = "cascade_advanced"
    elif executed:
        wake_outcome = "executed_no_advance"
    elif follow_through_action == "execute_now":
        wake_outcome = "execute_recommended"
    else:
        wake_outcome = "identity"

    continue_cascade_wake_line = (
        f"continue_cascade_wake "
        f"outcome={wake_outcome} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"cascade_advanced={'true' if cascade_advanced else 'false'} "
        f"reverse_advanced={'true' if reverse_advanced else 'false'} "
        f"residual_advanced={'true' if residual_advanced else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"finished={'true' if continue_finished else 'false'} "
        f"residual_open={'true' if residual_open_ready else 'false'} "
        f"residual_queue_ready={'true' if residual_queue_ready else 'false'} "
        f"residual_hold={'true' if residual_hold_active else 'false'} "
        f"residual_export=false "
        f"follow={follow_through_action} "
        f"next={supervisor_next} "
        f"helper=package_reverse_flow_focused_validation_continue_cascade_wake"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "wake_outcome": wake_outcome,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "reverse_advanced": bool(reverse_advanced),
        "residual_advanced": bool(residual_advanced),
        "cascade_advanced": bool(cascade_advanced),
        "reverse_progress_complete": bool(reverse_complete),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "continue_finished": bool(continue_finished),
        "residual_queue_ready": bool(residual_queue_ready),
        "residual_open_ready": bool(residual_open_ready),
        "residual_hold_active": bool(residual_hold_active),
        "residual_export_allowed": False,
        "follow_through_action": follow_through_action,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_line": continue_cascade_wake_line,
        "continue_cascade_wake_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake"
        ),
        "continue_cascade_transition_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_transition"
        ),
        "continue_cascade_helper": (
            "package_reverse_flow_focused_validation_continue_cascade"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "package_reverse_flow_focused_validation_continue_finish_receipt",
            "package_reverse_flow_focused_validation_continue_exec_receipt",
            "package_reverse_flow_focused_validation_continue_residual_open",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route(
    *,
    cascade_wake: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Map cascade wake_outcome to a durable body-free next-route policy.

    After continue cascade wake classifies a single ``wake_outcome``, supervisors
    still re-derived which helper to call next and whether execute is allowed by
    combining nested ``wake_outcome``, ``follow_through_action``, and
    ``supervisor_next_action``. This surface packages a body-free
    ``continue_cascade_wake_route_line`` with classified ``route_action`` (for
    example
    ``continue_cascade_wake_route outcome=execute_recommended
    action=execute_now call_execute=true residual_route=false residual_export=false
    next=run_focused_local_test_validation_then_keep_activation_external
    helper=follow_reverse_flow_focused_validation_continue_dispatch
    route_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route``)
    so supervisors pin one route action enum instead of re-assembling wake
    outcome + follow policy after continue wakes.

    Route actions: ``open_residual_entry``, ``keep_activation_external``,
    ``record_remaining``, ``continue_residual_cascade``, ``repair``,
    ``execute_now``, ``inventory_only``.

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    wake = cascade_wake if isinstance(cascade_wake, dict) else {}
    wake_outcome = str(wake.get("wake_outcome") or "identity")
    supervisor_next = str(wake.get("supervisor_next_action") or "none")
    follow_through_action = str(wake.get("follow_through_action") or "none")
    residual_open_ready = bool(wake.get("residual_open_ready"))
    reverse_progress_transition = str(
        wake.get("reverse_progress_transition") or "0/0→0/0"
    )
    residual_progress_transition = str(
        wake.get("residual_progress_transition") or "0/8→0/8"
    )

    # Map classified wake outcome → durable route action + preferred helper so
    # supervisors do not re-derive execute vs residual-entry vs keep policy.
    if wake_outcome == "residual_open_ready" or residual_open_ready:
        route_action = "open_residual_entry"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
        call_follow_with_execute = False
        residual_route_ready = True
    elif wake_outcome in {"continue_finished", "reverse_complete"}:
        route_action = "keep_activation_external"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_finish_receipt"
        )
        call_follow_with_execute = False
        residual_route_ready = False
    elif wake_outcome == "reverse_progress_advanced":
        route_action = "record_remaining"
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        )
        call_follow_with_execute = True
        residual_route_ready = False
    elif wake_outcome == "residual_progress_advanced":
        route_action = "continue_residual_cascade"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        call_follow_with_execute = False
        residual_route_ready = True
    elif wake_outcome == "cascade_advanced":
        route_action = "keep_activation_external"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_cascade_transition"
        )
        call_follow_with_execute = False
        residual_route_ready = False
    elif wake_outcome == "executed_no_advance":
        route_action = "repair"
        preferred_helper = (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        )
        call_follow_with_execute = False
        residual_route_ready = False
    elif wake_outcome == "execute_recommended" or follow_through_action == "execute_now":
        route_action = "execute_now"
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        )
        call_follow_with_execute = True
        residual_route_ready = False
    else:
        route_action = "inventory_only"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        call_follow_with_execute = False
        residual_route_ready = False

    continue_cascade_wake_route_line = (
        f"continue_cascade_wake_route "
        f"outcome={wake_outcome} "
        f"action={route_action} "
        f"call_execute={'true' if call_follow_with_execute else 'false'} "
        f"residual_route={'true' if residual_route_ready else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"route_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "wake_outcome": wake_outcome,
        "route_action": route_action,
        "call_follow_with_execute": bool(call_follow_with_execute),
        "residual_route_ready": bool(residual_route_ready),
        "preferred_helper": preferred_helper,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "residual_export_allowed": False,
        "follow_through_action": follow_through_action,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_line": continue_cascade_wake_route_line,
        "continue_cascade_wake_route_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route"
        ),
        "continue_cascade_wake_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
    *,
    pre_route: dict[str, Any] | None = None,
    post_route: dict[str, Any] | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse pre/post cascade wake routes into a body-free apply receipt.

    After continue cascade wake route classifies a single ``route_action``,
    supervisors still re-derived whether the wake advanced by comparing nested
    pre/post ``route_action``, ``call_follow_with_execute``, and residual-route
    flags after follow/dispatch. This surface packages a body-free
    ``continue_cascade_wake_route_apply_line`` with pre→post route action
    transition (for example
    ``continue_cascade_wake_route_apply pre_action=execute_now
    post_action=keep_activation_external action=execute_now→keep_activation_external
    route_advanced=true call_execute=true→false residual_route=false→false
    reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true
    residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply``)
    so supervisors pin one apply receipt instead of re-comparing nested route
    packets after continue wakes.

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    pre = pre_route if isinstance(pre_route, dict) else {}
    post = post_route if isinstance(post_route, dict) else {}
    pre_action = str(pre.get("route_action") or "inventory_only")
    post_action = str(post.get("route_action") or pre_action or "inventory_only")
    pre_call_execute = bool(pre.get("call_follow_with_execute"))
    post_call_execute = bool(post.get("call_follow_with_execute"))
    pre_residual_route = bool(pre.get("residual_route_ready"))
    post_residual_route = bool(post.get("residual_route_ready"))
    pre_reverse = str(pre.get("reverse_progress_transition") or "0/0→0/0")
    post_reverse = str(post.get("reverse_progress_transition") or pre_reverse)
    pre_residual = str(pre.get("residual_progress_transition") or "0/8→0/8")
    post_residual = str(post.get("residual_progress_transition") or pre_residual)
    # Prefer post progress endpoints; when both sides carry transitions, surface
    # the combined pre→post reverse/residual endpoints for operator legibility.
    reverse_progress_transition = (
        f"{pre_reverse.split('→')[0]}→{post_reverse.split('→')[-1]}"
        if "→" in pre_reverse and "→" in post_reverse
        else post_reverse
    )
    residual_progress_transition = (
        f"{pre_residual.split('→')[0]}→{post_residual.split('→')[-1]}"
        if "→" in pre_residual and "→" in post_residual
        else post_residual
    )
    route_action_transition = f"{pre_action}→{post_action}"
    route_advanced = bool(
        pre_action != post_action
        or pre_call_execute != post_call_execute
        or pre_residual_route != post_residual_route
        or reverse_progress_transition.split("→")[0]
        != reverse_progress_transition.split("→")[-1]
        or residual_progress_transition.split("→")[0]
        != residual_progress_transition.split("→")[-1]
    )
    supervisor_next = str(
        post.get("supervisor_next_action")
        or pre.get("supervisor_next_action")
        or "none"
    )
    applied_route_action = post_action if executed or route_advanced else pre_action
    continue_cascade_wake_route_apply_line = (
        f"continue_cascade_wake_route_apply "
        f"pre_action={pre_action} "
        f"post_action={post_action} "
        f"action={route_action_transition} "
        f"route_advanced={'true' if route_advanced else 'false'} "
        f"call_execute="
        f"{'true' if pre_call_execute else 'false'}→"
        f"{'true' if post_call_execute else 'false'} "
        f"residual_route="
        f"{'true' if pre_residual_route else 'false'}→"
        f"{'true' if post_residual_route else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "pre_route_action": pre_action,
        "post_route_action": post_action,
        "route_action_transition": route_action_transition,
        "applied_route_action": applied_route_action,
        "route_advanced": bool(route_advanced),
        "pre_call_follow_with_execute": bool(pre_call_execute),
        "post_call_follow_with_execute": bool(post_call_execute),
        "pre_residual_route_ready": bool(pre_residual_route),
        "post_residual_route_ready": bool(post_residual_route),
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "executed": bool(executed),
        "recorded": bool(recorded),
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_line": (
            continue_cascade_wake_route_apply_line
        ),
        "continue_cascade_wake_route_apply_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
        ),
        "continue_cascade_wake_route_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
    *,
    route_apply: dict[str, Any] | None = None,
    post_route: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Map cascade wake route apply receipt to a durable body-free follow policy.

    After continue cascade wake route apply collapses pre/post routes into one
    apply receipt, supervisors still re-derived which helper to call next by
    combining nested ``applied_route_action``, ``route_advanced``, and post-route
    preferred helpers. This surface packages a body-free
    ``continue_cascade_wake_route_apply_follow_line`` with classified follow
    policy from the apply receipt (for example
    ``continue_cascade_wake_route_apply_follow applied=keep_activation_external
    advanced=true executed=true call_execute=false residual_route=false
    reverse=0/3→3/3 residual=0/8→0/8 residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_finish_receipt
    apply_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply``)
    so supervisors pin one follow receipt instead of re-mapping applied route
    actions after continue wakes.

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    apply = route_apply if isinstance(route_apply, dict) else {}
    post = post_route if isinstance(post_route, dict) else {}
    applied_route_action = str(
        apply.get("applied_route_action")
        or apply.get("post_route_action")
        or post.get("route_action")
        or "inventory_only"
    )
    route_advanced = bool(apply.get("route_advanced"))
    executed = bool(apply.get("executed"))
    recorded = bool(apply.get("recorded"))
    residual_route_ready = bool(
        apply.get("post_residual_route_ready")
        if "post_residual_route_ready" in apply
        else post.get("residual_route_ready")
    )
    reverse_progress_transition = str(
        apply.get("reverse_progress_transition")
        or post.get("reverse_progress_transition")
        or "0/0→0/0"
    )
    residual_progress_transition = str(
        apply.get("residual_progress_transition")
        or post.get("residual_progress_transition")
        or "0/8→0/8"
    )
    supervisor_next = str(
        apply.get("supervisor_next_action")
        or post.get("supervisor_next_action")
        or "none"
    )

    # Map applied route action → durable preferred helper + call_execute so
    # supervisors pin one follow policy from the apply receipt alone.
    if applied_route_action == "open_residual_entry" or residual_route_ready:
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
        call_follow_with_execute = False
        residual_route_ready = True
        follow_action = "open_residual_entry"
    elif applied_route_action == "keep_activation_external":
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_finish_receipt"
        )
        call_follow_with_execute = False
        follow_action = "keep_activation_external"
    elif applied_route_action == "record_remaining":
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        )
        call_follow_with_execute = True
        follow_action = "record_remaining"
    elif applied_route_action == "continue_residual_cascade":
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        call_follow_with_execute = False
        residual_route_ready = True
        follow_action = "continue_residual_cascade"
    elif applied_route_action == "repair":
        preferred_helper = (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        )
        call_follow_with_execute = False
        follow_action = "repair"
    elif applied_route_action == "execute_now":
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        )
        call_follow_with_execute = True
        follow_action = "execute_now"
    else:
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        call_follow_with_execute = False
        follow_action = "inventory_only"

    # Prefer explicit post-route helper when it already matches the applied
    # action so follow stays aligned with wake-route preferred_helper.
    post_helper = str(post.get("preferred_helper") or "").strip()
    post_action = str(post.get("route_action") or "").strip()
    if post_helper and post_action == applied_route_action:
        preferred_helper = post_helper
        if "call_follow_with_execute" in post:
            call_follow_with_execute = bool(post.get("call_follow_with_execute"))

    continue_cascade_wake_route_apply_follow_line = (
        f"continue_cascade_wake_route_apply_follow "
        f"applied={follow_action} "
        f"advanced={'true' if route_advanced else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"call_execute={'true' if call_follow_with_execute else 'false'} "
        f"residual_route={'true' if residual_route_ready else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"apply_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "applied_route_action": applied_route_action,
        "follow_action": follow_action,
        "route_advanced": bool(route_advanced),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "call_follow_with_execute": bool(call_follow_with_execute),
        "residual_route_ready": bool(residual_route_ready),
        "preferred_helper": preferred_helper,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_line": (
            continue_cascade_wake_route_apply_follow_line
        ),
        "continue_cascade_wake_route_apply_follow_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        ),
        "continue_cascade_wake_route_apply_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
        ),
        "continue_cascade_wake_route_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
    *,
    apply_follow: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Map cascade wake route apply follow receipt to a durable body-free pin recipe.

    After continue cascade wake route apply follow maps applied route → preferred
    helper, supervisors still re-derived whether to call that helper with execute
    vs package-only vs inventory by combining nested ``follow_action``,
    ``call_follow_with_execute``, ``executed``, and ``preferred_helper``. This
    surface packages a body-free
    ``continue_cascade_wake_route_apply_follow_pin_line`` with classified pin
    mode from the apply_follow receipt (for example
    ``continue_cascade_wake_route_apply_follow_pin action=execute_now
    mode=execute_helper call_execute=true pin_ready=true residual_route=false
    reverse=0/3→0/3 residual=0/8→0/8 residual_export=false
    next=run_focused_local_test_validation_then_keep_activation_external
    helper=follow_reverse_flow_focused_validation_continue_dispatch
    follow_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow``)
    so supervisors pin one call recipe instead of re-deriving execute vs package
    policy after continue wakes.

    Pin modes:
    - ``execute_helper`` — call preferred helper with execute (``execute_now`` /
      ``record_remaining`` when ``call_follow_with_execute`` is true)
    - ``package_helper`` — call preferred helper as packaging only
      (``keep_activation_external`` / residual open / residual cascade / repair)
    - ``inventory_only`` — no preferred helper call beyond inventory

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    follow = apply_follow if isinstance(apply_follow, dict) else {}
    follow_action = str(follow.get("follow_action") or "inventory_only")
    preferred_helper = str(
        follow.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    call_follow_with_execute = bool(follow.get("call_follow_with_execute"))
    route_advanced = bool(follow.get("route_advanced"))
    executed = bool(follow.get("executed"))
    recorded = bool(follow.get("recorded"))
    residual_route_ready = bool(follow.get("residual_route_ready"))
    reverse_progress_transition = str(
        follow.get("reverse_progress_transition") or "0/0→0/0"
    )
    residual_progress_transition = str(
        follow.get("residual_progress_transition") or "0/8→0/8"
    )
    supervisor_next = str(follow.get("supervisor_next_action") or "none")
    applied_route_action = str(
        follow.get("applied_route_action") or follow_action or "inventory_only"
    )

    # Classify pin mode so supervisors call one helper recipe without re-deriving
    # execute vs package policy from nested follow fields.
    if call_follow_with_execute and follow_action in {
        "execute_now",
        "record_remaining",
    }:
        pin_mode = "execute_helper"
        pin_ready = True
        call_pin_with_execute = True
        pin_action = follow_action
    elif follow_action in {
        "keep_activation_external",
        "open_residual_entry",
        "continue_residual_cascade",
        "repair",
    }:
        pin_mode = "package_helper"
        pin_ready = True
        call_pin_with_execute = False
        pin_action = follow_action
    else:
        pin_mode = "inventory_only"
        pin_ready = False
        call_pin_with_execute = False
        pin_action = "inventory_only"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )

    continue_cascade_wake_route_apply_follow_pin_line = (
        f"continue_cascade_wake_route_apply_follow_pin "
        f"action={pin_action} "
        f"mode={pin_mode} "
        f"call_execute={'true' if call_pin_with_execute else 'false'} "
        f"pin_ready={'true' if pin_ready else 'false'} "
        f"advanced={'true' if route_advanced else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_route={'true' if residual_route_ready else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"follow_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "applied_route_action": applied_route_action,
        "follow_action": follow_action,
        "pin_action": pin_action,
        "pin_mode": pin_mode,
        "pin_ready": bool(pin_ready),
        "route_advanced": bool(route_advanced),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "call_pin_with_execute": bool(call_pin_with_execute),
        "call_follow_with_execute": bool(call_follow_with_execute),
        "residual_route_ready": bool(residual_route_ready),
        "preferred_helper": preferred_helper,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_pin_line": (
            continue_cascade_wake_route_apply_follow_pin_line
        ),
        "continue_cascade_wake_route_apply_follow_pin_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        ),
        "continue_cascade_wake_route_apply_follow_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        ),
        "continue_cascade_wake_route_apply_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
        ),
        "continue_cascade_wake_route_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
    *,
    pre_pin: dict[str, Any] | None = None,
    post_pin: dict[str, Any] | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse pre/post continue pin recipes into a body-free pin call receipt.

    After continue cascade wake route apply follow pin classifies a single pin
    mode, supervisors still re-derived whether the pin advanced by comparing
    nested pre/post ``pin_action``, ``pin_mode``, ``call_pin_with_execute``, and
    residual-route flags after follow/dispatch. This surface packages a body-free
    ``continue_cascade_wake_route_apply_follow_pin_call_line`` with pre→post pin
    action/mode transition (for example
    ``continue_cascade_wake_route_apply_follow_pin_call pre_action=execute_now
    post_action=keep_activation_external action=execute_now→keep_activation_external
    mode=execute_helper→package_helper call_execute=true→false pin_advanced=true
    pin_ready=true→true residual_route=false→false reverse=0/3→3/3 residual=0/8→0/8
    executed=true recorded=true residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_finish_receipt
    pin_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin``)
    so supervisors pin one call receipt instead of re-comparing nested pin
    packets after continue wakes.

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    pre = pre_pin if isinstance(pre_pin, dict) else {}
    post = post_pin if isinstance(post_pin, dict) else {}
    pre_action = str(pre.get("pin_action") or "inventory_only")
    post_action = str(post.get("pin_action") or pre_action or "inventory_only")
    pre_mode = str(pre.get("pin_mode") or "inventory_only")
    post_mode = str(post.get("pin_mode") or pre_mode or "inventory_only")
    pre_call_execute = bool(pre.get("call_pin_with_execute"))
    post_call_execute = bool(post.get("call_pin_with_execute"))
    pre_pin_ready = bool(pre.get("pin_ready"))
    post_pin_ready = bool(post.get("pin_ready"))
    pre_residual_route = bool(pre.get("residual_route_ready"))
    post_residual_route = bool(post.get("residual_route_ready"))
    pre_reverse = str(pre.get("reverse_progress_transition") or "0/0→0/0")
    post_reverse = str(post.get("reverse_progress_transition") or pre_reverse)
    pre_residual = str(pre.get("residual_progress_transition") or "0/8→0/8")
    post_residual = str(post.get("residual_progress_transition") or pre_residual)
    reverse_progress_transition = (
        f"{pre_reverse.split('→')[0]}→{post_reverse.split('→')[-1]}"
        if "→" in pre_reverse and "→" in post_reverse
        else post_reverse
    )
    residual_progress_transition = (
        f"{pre_residual.split('→')[0]}→{post_residual.split('→')[-1]}"
        if "→" in pre_residual and "→" in post_residual
        else post_residual
    )
    pin_action_transition = f"{pre_action}→{post_action}"
    pin_mode_transition = f"{pre_mode}→{post_mode}"
    pin_advanced = bool(
        pre_action != post_action
        or pre_mode != post_mode
        or pre_call_execute != post_call_execute
        or pre_pin_ready != post_pin_ready
        or pre_residual_route != post_residual_route
        or reverse_progress_transition.split("→")[0]
        != reverse_progress_transition.split("→")[-1]
        or residual_progress_transition.split("→")[0]
        != residual_progress_transition.split("→")[-1]
    )
    supervisor_next = str(
        post.get("supervisor_next_action")
        or pre.get("supervisor_next_action")
        or "none"
    )
    preferred_helper = str(
        post.get("preferred_helper")
        or pre.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    applied_pin_action = post_action if executed or pin_advanced else pre_action
    applied_pin_mode = post_mode if executed or pin_advanced else pre_mode
    continue_cascade_wake_route_apply_follow_pin_call_line = (
        f"continue_cascade_wake_route_apply_follow_pin_call "
        f"pre_action={pre_action} "
        f"post_action={post_action} "
        f"action={pin_action_transition} "
        f"mode={pin_mode_transition} "
        f"call_execute="
        f"{'true' if pre_call_execute else 'false'}→"
        f"{'true' if post_call_execute else 'false'} "
        f"pin_advanced={'true' if pin_advanced else 'false'} "
        f"pin_ready="
        f"{'true' if pre_pin_ready else 'false'}→"
        f"{'true' if post_pin_ready else 'false'} "
        f"residual_route="
        f"{'true' if pre_residual_route else 'false'}→"
        f"{'true' if post_residual_route else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"pin_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "pre_pin_action": pre_action,
        "post_pin_action": post_action,
        "pin_action_transition": pin_action_transition,
        "applied_pin_action": applied_pin_action,
        "pre_pin_mode": pre_mode,
        "post_pin_mode": post_mode,
        "pin_mode_transition": pin_mode_transition,
        "applied_pin_mode": applied_pin_mode,
        "pin_advanced": bool(pin_advanced),
        "pre_call_pin_with_execute": bool(pre_call_execute),
        "post_call_pin_with_execute": bool(post_call_execute),
        "pre_pin_ready": bool(pre_pin_ready),
        "post_pin_ready": bool(post_pin_ready),
        "pre_residual_route_ready": bool(pre_residual_route),
        "post_residual_route_ready": bool(post_residual_route),
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "executed": bool(executed),
        "recorded": bool(recorded),
        "preferred_helper": preferred_helper,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_pin_call_line": (
            continue_cascade_wake_route_apply_follow_pin_call_line
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        ),
        "continue_cascade_wake_route_apply_follow_pin_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        ),
        "continue_cascade_wake_route_apply_follow_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
    *,
    pin_call: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Seal pin_call receipt into a body-free next-invoke recipe.

    After continue cascade wake route apply follow pin call collapses pre/post
    pin recipes into one call receipt, supervisors still re-derived whether to
    invoke the preferred helper with execute vs package-only vs inventory by
    combining nested ``applied_pin_mode``, ``post_call_pin_with_execute``,
    ``pin_advanced``, and ``pin_ready``. This surface packages a body-free
    ``continue_cascade_wake_route_apply_follow_pin_call_next_line`` with sealed
    next invoke policy from the pin_call receipt (for example
    ``continue_cascade_wake_route_apply_follow_pin_call_next
    action=keep_activation_external mode=package_helper invoke=package_helper
    advanced=true call_execute=false next_ready=true residual_route=false
    reverse=0/3→3/3 residual=0/8→0/8 residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_finish_receipt
    pin_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call``)
    so supervisors pin one next-invoke recipe instead of re-deriving execute vs
    package policy from nested pin_call transitions after continue wakes.

    Next invoke modes:
    - ``execute_helper`` — call preferred helper with execute
      (``execute_now`` / ``record_remaining`` when post call_execute is true)
    - ``package_helper`` — call preferred helper as packaging only
      (``keep_activation_external`` / residual open / residual cascade / repair)
    - ``inventory_only`` — no preferred helper call beyond inventory

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    call = pin_call if isinstance(pin_call, dict) else {}
    applied_pin_action = str(
        call.get("applied_pin_action")
        or call.get("post_pin_action")
        or "inventory_only"
    )
    applied_pin_mode = str(
        call.get("applied_pin_mode") or call.get("post_pin_mode") or "inventory_only"
    )
    pin_action_transition = str(
        call.get("pin_action_transition") or f"{applied_pin_action}→{applied_pin_action}"
    )
    pin_mode_transition = str(
        call.get("pin_mode_transition") or f"{applied_pin_mode}→{applied_pin_mode}"
    )
    pin_advanced = bool(call.get("pin_advanced"))
    post_call_execute = bool(call.get("post_call_pin_with_execute"))
    post_pin_ready = bool(call.get("post_pin_ready"))
    post_residual_route = bool(call.get("post_residual_route_ready"))
    reverse_progress_transition = str(
        call.get("reverse_progress_transition") or "0/0→0/0"
    )
    residual_progress_transition = str(
        call.get("residual_progress_transition") or "0/8→0/8"
    )
    executed = bool(call.get("executed"))
    recorded = bool(call.get("recorded"))
    preferred_helper = str(
        call.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    supervisor_next = str(call.get("supervisor_next_action") or "none")

    # Seal next invoke from applied pin_call so supervisors do not re-combine
    # mode transitions + call_execute + pin_ready after continue wakes.
    if (
        applied_pin_mode == "execute_helper"
        and post_call_execute
        and applied_pin_action in {"execute_now", "record_remaining"}
    ):
        next_invoke = "execute_helper"
        call_next_with_execute = True
        next_ready = True
        next_action = applied_pin_action
    elif applied_pin_mode == "package_helper" and applied_pin_action in {
        "keep_activation_external",
        "open_residual_entry",
        "continue_residual_cascade",
        "repair",
    }:
        next_invoke = "package_helper"
        call_next_with_execute = False
        next_ready = True
        next_action = applied_pin_action
    else:
        next_invoke = "inventory_only"
        call_next_with_execute = False
        next_ready = False
        next_action = "inventory_only"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )

    continue_cascade_wake_route_apply_follow_pin_call_next_line = (
        f"continue_cascade_wake_route_apply_follow_pin_call_next "
        f"action={next_action} "
        f"mode={applied_pin_mode} "
        f"invoke={next_invoke} "
        f"advanced={'true' if pin_advanced else 'false'} "
        f"call_execute={'true' if call_next_with_execute else 'false'} "
        f"next_ready={'true' if next_ready else 'false'} "
        f"pin_ready={'true' if post_pin_ready else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_route={'true' if post_residual_route else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"pin_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "applied_pin_action": applied_pin_action,
        "applied_pin_mode": applied_pin_mode,
        "pin_action_transition": pin_action_transition,
        "pin_mode_transition": pin_mode_transition,
        "pin_advanced": bool(pin_advanced),
        "next_action": next_action,
        "next_invoke": next_invoke,
        "next_ready": bool(next_ready),
        "call_next_with_execute": bool(call_next_with_execute),
        "post_call_pin_with_execute": bool(post_call_execute),
        "post_pin_ready": bool(post_pin_ready),
        "post_residual_route_ready": bool(post_residual_route),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "preferred_helper": preferred_helper,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_pin_call_next_line": (
            continue_cascade_wake_route_apply_follow_pin_call_next_line
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        ),
        "continue_cascade_wake_route_apply_follow_pin_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        ),
        "continue_cascade_wake_route_apply_follow_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call(
    *,
    pre_next: dict[str, Any] | None = None,
    post_next: dict[str, Any] | None = None,
    executed: bool = False,
    recorded: bool = False,
) -> dict[str, Any]:
    """Collapse pre/post pin_call_next recipes into a body-free next-call receipt.

    After continue cascade wake route apply follow pin call next seals a single
    next-invoke recipe, supervisors still re-derived whether the next recipe
    advanced by comparing nested pre/post ``next_action``, ``next_invoke``,
    ``call_next_with_execute``, ``next_ready``, and residual-route flags after
    follow/dispatch. This surface packages a body-free
    ``continue_cascade_wake_route_apply_follow_pin_call_next_call_line`` with
    pre→post next action/invoke transition (for example
    ``continue_cascade_wake_route_apply_follow_pin_call_next_call
    pre_action=execute_now post_action=keep_activation_external
    action=execute_now→keep_activation_external
    invoke=execute_helper→package_helper call_execute=true→false
    next_advanced=true next_ready=true→true residual_route=false→false
    reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true
    residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_finish_receipt
    pin_call_next_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next``)
    so supervisors pin one next-call receipt instead of re-comparing nested
    pin_call_next packets after continue wakes.

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    pre = pre_next if isinstance(pre_next, dict) else {}
    post = post_next if isinstance(post_next, dict) else {}
    pre_action = str(pre.get("next_action") or "inventory_only")
    post_action = str(post.get("next_action") or pre_action or "inventory_only")
    pre_invoke = str(pre.get("next_invoke") or "inventory_only")
    post_invoke = str(post.get("next_invoke") or pre_invoke or "inventory_only")
    pre_call_execute = bool(pre.get("call_next_with_execute"))
    post_call_execute = bool(post.get("call_next_with_execute"))
    pre_next_ready = bool(pre.get("next_ready"))
    post_next_ready = bool(post.get("next_ready"))
    pre_residual_route = bool(pre.get("post_residual_route_ready"))
    post_residual_route = bool(post.get("post_residual_route_ready"))
    pre_reverse = str(pre.get("reverse_progress_transition") or "0/0→0/0")
    post_reverse = str(post.get("reverse_progress_transition") or pre_reverse)
    pre_residual = str(pre.get("residual_progress_transition") or "0/8→0/8")
    post_residual = str(post.get("residual_progress_transition") or pre_residual)
    reverse_progress_transition = (
        f"{pre_reverse.split('→')[0]}→{post_reverse.split('→')[-1]}"
        if "→" in pre_reverse and "→" in post_reverse
        else post_reverse
    )
    residual_progress_transition = (
        f"{pre_residual.split('→')[0]}→{post_residual.split('→')[-1]}"
        if "→" in pre_residual and "→" in post_residual
        else post_residual
    )
    next_action_transition = f"{pre_action}→{post_action}"
    next_invoke_transition = f"{pre_invoke}→{post_invoke}"
    next_advanced = bool(
        pre_action != post_action
        or pre_invoke != post_invoke
        or pre_call_execute != post_call_execute
        or pre_next_ready != post_next_ready
        or pre_residual_route != post_residual_route
        or reverse_progress_transition.split("→")[0]
        != reverse_progress_transition.split("→")[-1]
        or residual_progress_transition.split("→")[0]
        != residual_progress_transition.split("→")[-1]
    )
    supervisor_next = str(
        post.get("supervisor_next_action")
        or pre.get("supervisor_next_action")
        or "none"
    )
    preferred_helper = str(
        post.get("preferred_helper")
        or pre.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    applied_next_action = post_action if executed or next_advanced else pre_action
    applied_next_invoke = post_invoke if executed or next_advanced else pre_invoke
    continue_cascade_wake_route_apply_follow_pin_call_next_call_line = (
        f"continue_cascade_wake_route_apply_follow_pin_call_next_call "
        f"pre_action={pre_action} "
        f"post_action={post_action} "
        f"action={next_action_transition} "
        f"invoke={next_invoke_transition} "
        f"call_execute="
        f"{'true' if pre_call_execute else 'false'}→"
        f"{'true' if post_call_execute else 'false'} "
        f"next_advanced={'true' if next_advanced else 'false'} "
        f"next_ready="
        f"{'true' if pre_next_ready else 'false'}→"
        f"{'true' if post_next_ready else 'false'} "
        f"residual_route="
        f"{'true' if pre_residual_route else 'false'}→"
        f"{'true' if post_residual_route else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"pin_call_next_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "pre_next_action": pre_action,
        "post_next_action": post_action,
        "next_action_transition": next_action_transition,
        "applied_next_action": applied_next_action,
        "pre_next_invoke": pre_invoke,
        "post_next_invoke": post_invoke,
        "next_invoke_transition": next_invoke_transition,
        "applied_next_invoke": applied_next_invoke,
        "next_advanced": bool(next_advanced),
        "pre_call_next_with_execute": bool(pre_call_execute),
        "post_call_next_with_execute": bool(post_call_execute),
        "pre_next_ready": bool(pre_next_ready),
        "post_next_ready": bool(post_next_ready),
        "pre_residual_route_ready": bool(pre_residual_route),
        "post_residual_route_ready": bool(post_residual_route),
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "executed": bool(executed),
        "recorded": bool(recorded),
        "preferred_helper": preferred_helper,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_line": (
            continue_cascade_wake_route_apply_follow_pin_call_next_call_line
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        ),
        "continue_cascade_wake_route_apply_follow_pin_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow(
    *,
    next_call: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Map next_call receipt to a durable body-free follow policy.

    After continue cascade wake route apply follow pin call next call collapses
    pre/post next-invoke recipes into one next-call receipt, supervisors still
    re-mapped ``applied_next_action``, ``applied_next_invoke``,
    ``post_call_next_with_execute``, ``next_advanced``, and residual-route flags
    into which helper to call next after follow/dispatch. This surface packages
    a body-free
    ``continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line``
    with classified follow policy from the next_call receipt (for example
    ``continue_cascade_wake_route_apply_follow_pin_call_next_call_follow
    applied=keep_activation_external invoke=package_helper mode=package_helper
    advanced=true call_execute=false follow_ready=true residual_route=false
    reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true
    residual_export=false
    next=keep_activation_external_after_focused_local_test_validation
    helper=package_reverse_flow_focused_validation_continue_finish_receipt
    next_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call``)
    so supervisors pin one next-call follow receipt instead of re-mapping
    applied next action/invoke after continue wakes.

    Follow modes:
    - ``execute_helper`` — call preferred helper with execute
      (``execute_now`` / ``record_remaining`` when post call_execute is true)
    - ``package_helper`` — call preferred helper as packaging only
      (``keep_activation_external`` / residual open / residual cascade / repair)
    - ``inventory_only`` — no preferred helper call beyond inventory

    Residual export stays denied on this continue surface even when residual
    route opens after residual_open_ready. Does not enable activation, push,
    promotion, provider launch, remote apply, external skill execution, or
    kernel restart. Does not export raw evidence URLs, bodies, or command stdout.
    """

    call = next_call if isinstance(next_call, dict) else {}
    applied_next_action = str(
        call.get("applied_next_action")
        or call.get("post_next_action")
        or "inventory_only"
    )
    applied_next_invoke = str(
        call.get("applied_next_invoke")
        or call.get("post_next_invoke")
        or "inventory_only"
    )
    next_action_transition = str(
        call.get("next_action_transition")
        or f"{applied_next_action}→{applied_next_action}"
    )
    next_invoke_transition = str(
        call.get("next_invoke_transition")
        or f"{applied_next_invoke}→{applied_next_invoke}"
    )
    next_advanced = bool(call.get("next_advanced"))
    post_call_execute = bool(call.get("post_call_next_with_execute"))
    post_next_ready = bool(call.get("post_next_ready"))
    post_residual_route = bool(call.get("post_residual_route_ready"))
    reverse_progress_transition = str(
        call.get("reverse_progress_transition") or "0/0→0/0"
    )
    residual_progress_transition = str(
        call.get("residual_progress_transition") or "0/8→0/8"
    )
    executed = bool(call.get("executed"))
    recorded = bool(call.get("recorded"))
    preferred_helper = str(
        call.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    supervisor_next = str(call.get("supervisor_next_action") or "none")

    # Map applied next_call → durable follow action/mode + call_execute so
    # supervisors pin one follow policy from the next_call receipt alone.
    if (
        applied_next_invoke == "execute_helper"
        and post_call_execute
        and applied_next_action in {"execute_now", "record_remaining"}
    ):
        follow_mode = "execute_helper"
        call_follow_with_execute = True
        follow_ready = True
        follow_action = applied_next_action
        preferred_helper = (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        )
    elif applied_next_action == "open_residual_entry" or post_residual_route:
        follow_mode = "package_helper"
        call_follow_with_execute = False
        follow_ready = True
        follow_action = "open_residual_entry"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
        post_residual_route = True
    elif applied_next_action == "keep_activation_external":
        follow_mode = "package_helper"
        call_follow_with_execute = False
        follow_ready = True
        follow_action = "keep_activation_external"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_finish_receipt"
        )
    elif applied_next_action == "continue_residual_cascade":
        follow_mode = "package_helper"
        call_follow_with_execute = False
        follow_ready = True
        follow_action = "continue_residual_cascade"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        post_residual_route = True
    elif applied_next_action == "repair":
        follow_mode = "package_helper"
        call_follow_with_execute = False
        follow_ready = True
        follow_action = "repair"
        preferred_helper = (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        )
    elif applied_next_invoke == "package_helper" and applied_next_action in {
        "keep_activation_external",
        "open_residual_entry",
        "continue_residual_cascade",
        "repair",
    }:
        follow_mode = "package_helper"
        call_follow_with_execute = False
        follow_ready = True
        follow_action = applied_next_action
    else:
        follow_mode = "inventory_only"
        call_follow_with_execute = False
        follow_ready = False
        follow_action = "inventory_only"
        preferred_helper = (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )

    # Prefer explicit next_call helper when it already matches the applied
    # action so follow stays aligned with next_call preferred_helper.
    call_helper = str(call.get("preferred_helper") or "").strip()
    if call_helper and follow_action == applied_next_action and follow_mode != "inventory_only":
        preferred_helper = call_helper
        if "post_call_next_with_execute" in call and follow_mode == "execute_helper":
            call_follow_with_execute = bool(call.get("post_call_next_with_execute"))

    continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line = (
        f"continue_cascade_wake_route_apply_follow_pin_call_next_call_follow "
        f"applied={follow_action} "
        f"invoke={applied_next_invoke} "
        f"mode={follow_mode} "
        f"advanced={'true' if next_advanced else 'false'} "
        f"call_execute={'true' if call_follow_with_execute else 'false'} "
        f"follow_ready={'true' if follow_ready else 'false'} "
        f"next_ready={'true' if post_next_ready else 'false'} "
        f"executed={'true' if executed else 'false'} "
        f"recorded={'true' if recorded else 'false'} "
        f"residual_route={'true' if post_residual_route else 'false'} "
        f"reverse={reverse_progress_transition} "
        f"residual={residual_progress_transition} "
        f"residual_export=false "
        f"next={supervisor_next} "
        f"helper={preferred_helper} "
        f"next_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
    )
    return {
        "schema_version": 1,
        "controller_surface": (
            "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
        ),
        "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
        "applied_next_action": applied_next_action,
        "applied_next_invoke": applied_next_invoke,
        "next_action_transition": next_action_transition,
        "next_invoke_transition": next_invoke_transition,
        "next_advanced": bool(next_advanced),
        "follow_action": follow_action,
        "follow_mode": follow_mode,
        "follow_ready": bool(follow_ready),
        "call_follow_with_execute": bool(call_follow_with_execute),
        "post_call_next_with_execute": bool(post_call_execute),
        "post_next_ready": bool(post_next_ready),
        "post_residual_route_ready": bool(post_residual_route),
        "executed": bool(executed),
        "recorded": bool(recorded),
        "preferred_helper": preferred_helper,
        "reverse_progress_transition": reverse_progress_transition,
        "residual_progress_transition": residual_progress_transition,
        "residual_export_allowed": False,
        "supervisor_next_action": supervisor_next,
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line": (
            continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_next_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
        ),
        "continue_cascade_wake_route_apply_follow_pin_call_helper": (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        ),
        "follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "record_helpers": [
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
            "follow_reverse_flow_focused_validation_continue_dispatch",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        ],
    }


def follow_reverse_flow_focused_validation_continue_dispatch(
    pipeline: dict[str, Any],
    *,
    execute: bool | None = None,
    command_runner: Any = subprocess.run,
    cwd: Path | str | None = None,
    timeout: int = 120,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
    record: bool = True,
) -> dict[str, Any]:
    """Apply inventory follow-through: execute only when the durable policy says so.

    Preferred policy-aware operator entry for reverse-flow continue. Packages
    inventory, resolves follow-through (``execute_now`` /
    ``keep_activation_external`` / ``repair`` / ``wait_for_local_allowlist`` /
    ``noop``), then calls
    ``dispatch_reverse_flow_focused_validation_continue_supervisor_wake`` with
    ``execute=True`` only when ``call_dispatch_with_execute`` is true. After
    run/record attaches residual_open, residual_entry, residual_follow,
    residual_comparison, residual_unlocked_apply, residual_focused_validation,
    residual_handoff, residual_acceptance, residual_cascade, continue_cascade,
    continue_cascade_transition, continue_cascade_wake, and
    continue_cascade_wake_route plus continue_cascade_wake_route_apply
    (pre→post route action transition), continue_cascade_wake_route_apply_follow
    (applied route → preferred helper), and
    continue_cascade_wake_route_apply_follow_pin (follow → call recipe pin),
    continue_cascade_wake_route_apply_follow_pin_call (pre→post pin call
    receipt), continue_cascade_wake_route_apply_follow_pin_call_next
    (pin_call → sealed next-invoke recipe), and
    continue_cascade_wake_route_apply_follow_pin_call_next_call (pre→post
    next-invoke call receipt) so reverse-flow progress plus residual cascade
    stage progress, pre/post cascade transitions, classified wake outcomes,
    wake route actions, route apply receipts, apply-follow policy, follow-pin
    call recipes, pin call receipts, pin call next-invoke recipes, pin call
    next-call receipts, and activation-external acceptance policy are legible
    without nested re-assembly.

    ``execute=None`` follows the durable recommendation. ``execute=True`` still
    only runs allowlisted continue units (never forces re-run after pass).
    ``execute=False`` stays inventory-only. Residual export stays denied; no
    activation, push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Does not export stdout.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    inventory_packet = package_reverse_flow_focused_validation_continue_dispatch_inventory(
        pipeline=pipeline,
        execute_requested=bool(execute) if execute is not None else False,
        record_requested=bool(record),
    )
    follow_through = resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
        inventory_dispatch=inventory_packet,
        execute_requested=execute,
    )
    operator_card = package_reverse_flow_focused_validation_continue_operator_card(
        pipeline=pipeline,
        follow_through=follow_through,
    )
    pre_continue_cascade = package_reverse_flow_focused_validation_continue_cascade(
        pipeline=pipeline,
        operator_card=operator_card,
        follow_through=follow_through,
    )
    should_execute = bool(follow_through.get("call_dispatch_with_execute"))
    dispatch_packet = dispatch_reverse_flow_focused_validation_continue_supervisor_wake(
        pipeline,
        execute=should_execute,
        command_runner=command_runner,
        cwd=cwd,
        timeout=timeout,
        source_digest=source_digest,
        theme_window=theme_window,
        record=record,
    )
    updated_pipeline = (
        dispatch_packet.get("pipeline")
        if isinstance(dispatch_packet.get("pipeline"), dict)
        else pipeline
    )
    post_inventory = (
        dispatch_packet.get("post_dispatch_inventory")
        if isinstance(dispatch_packet.get("post_dispatch_inventory"), dict)
        else package_reverse_flow_focused_validation_continue_dispatch_inventory(
            pipeline=updated_pipeline,
            execute_requested=False,
            record_requested=bool(record),
        )
    )
    post_follow_through = (
        resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
            inventory_dispatch=post_inventory,
            execute_requested=False,
        )
    )
    post_operator_card = (
        dispatch_packet.get("post_operator_card")
        if isinstance(dispatch_packet.get("post_operator_card"), dict)
        else package_reverse_flow_focused_validation_continue_operator_card(
            pipeline=updated_pipeline,
            follow_through=post_follow_through,
        )
    )
    result = dict(dispatch_packet)
    result["controller_surface"] = (
        "reverse_flow_focused_validation_continue_dispatch_follow_through"
    )
    result["follow_through"] = follow_through
    result["post_follow_through"] = post_follow_through
    result["follow_through_action"] = str(
        follow_through.get("follow_through_action") or "noop"
    )
    result["post_follow_through_action"] = str(
        post_follow_through.get("follow_through_action") or "noop"
    )
    result["operator_card"] = operator_card
    result["post_operator_card"] = post_operator_card
    result["progress_label"] = str(operator_card.get("progress_label") or "0/0")
    result["post_progress_label"] = str(
        post_operator_card.get("progress_label") or "0/0"
    )
    progress_transition = (
        dispatch_packet.get("progress_transition")
        if isinstance(dispatch_packet.get("progress_transition"), dict)
        else package_reverse_flow_focused_validation_continue_progress_transition(
            pre_card=operator_card,
            post_card=post_operator_card,
            pre_follow_through=follow_through,
            post_follow_through=post_follow_through,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["progress_transition"] = progress_transition
    result["progress_transition_label"] = str(
        progress_transition.get("progress_transition_label") or "0/0→0/0"
    )
    result["progress_advanced"] = bool(progress_transition.get("progress_advanced"))
    result["transition_line"] = str(
        progress_transition.get("transition_line") or ""
    )
    result["action_line"] = str(operator_card.get("action_line") or "")
    result["post_action_line"] = str(post_operator_card.get("action_line") or "")
    exec_receipt = (
        dispatch_packet.get("exec_receipt")
        if isinstance(dispatch_packet.get("exec_receipt"), dict)
        else package_reverse_flow_focused_validation_continue_exec_receipt(
            run_plan=(
                dispatch_packet.get("run_plan")
                if isinstance(dispatch_packet.get("run_plan"), dict)
                else None
            ),
            run_result=(
                dispatch_packet.get("run_result")
                if isinstance(dispatch_packet.get("run_result"), dict)
                else None
            ),
            recorded=bool(result.get("recorded")),
            executed=bool(result.get("executed")),
            residual_hold_active=bool(result.get("residual_hold_active", True)),
        )
    )
    result["exec_receipt"] = exec_receipt
    result["exec_line"] = str(exec_receipt.get("exec_line") or "")
    result["exec_plan_line"] = str(
        exec_receipt.get("exec_plan_line")
        or dispatch_packet.get("exec_plan_line")
        or ""
    )
    result["exec_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_exec_receipt"
    )
    finish_receipt = (
        dispatch_packet.get("finish_receipt")
        if isinstance(dispatch_packet.get("finish_receipt"), dict)
        else package_reverse_flow_focused_validation_continue_finish_receipt(
            pipeline=updated_pipeline,
            post_operator_card=post_operator_card,
            progress_transition=progress_transition,
            exec_receipt=exec_receipt,
            supervisor_wake=(
                dispatch_packet.get("supervisor_wake")
                if isinstance(dispatch_packet.get("supervisor_wake"), dict)
                else None
            ),
            focused_validation_status=str(
                result.get("focused_validation_status") or ""
            )
            or None,
            activation_external_handoff_status=str(
                result.get("activation_external_handoff_status") or ""
            )
            or None,
            activation_external_acceptance_status=str(
                result.get("activation_external_acceptance_status") or ""
            )
            or None,
            residual_hold_active=bool(result.get("residual_hold_active", True)),
            continue_plan_mode=str(result.get("continue_plan_mode") or "") or None,
            supervisor_next_action=str(result.get("supervisor_next_action") or "")
            or None,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["finish_receipt"] = finish_receipt
    result["finish_line"] = str(finish_receipt.get("finish_line") or "")
    result["continue_finished"] = bool(finish_receipt.get("continue_finished"))
    result["residual_queue_ready"] = bool(finish_receipt.get("residual_queue_ready"))
    result["finish_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_finish_receipt"
    )
    residual_open = (
        dispatch_packet.get("residual_open")
        if isinstance(dispatch_packet.get("residual_open"), dict)
        else package_reverse_flow_focused_validation_continue_residual_open(
            pipeline=updated_pipeline,
            finish_receipt=finish_receipt,
        )
    )
    result["residual_open"] = residual_open
    result["residual_open_line"] = str(residual_open.get("residual_open_line") or "")
    result["residual_open_ready"] = bool(residual_open.get("residual_open"))
    result["residual_adjacent_count"] = int(
        residual_open.get("residual_adjacent_count") or 0
    )
    result["residual_open_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_open"
    )
    residual_entry = (
        dispatch_packet.get("residual_entry")
        if isinstance(dispatch_packet.get("residual_entry"), dict)
        else package_reverse_flow_focused_validation_continue_residual_entry(
            pipeline=updated_pipeline,
            residual_open=residual_open,
        )
    )
    result["residual_entry"] = residual_entry
    result["residual_entry_line"] = str(
        residual_entry.get("residual_entry_line") or ""
    )
    result["residual_entry_ready"] = bool(residual_entry.get("residual_entry"))
    result["selected_residual_proposal_id"] = str(
        residual_entry.get("selected_residual_proposal_id") or ""
    )
    result["residual_entry_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_entry"
    )
    residual_follow = (
        dispatch_packet.get("residual_follow")
        if isinstance(dispatch_packet.get("residual_follow"), dict)
        else package_reverse_flow_focused_validation_continue_residual_follow(
            pipeline=updated_pipeline,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_follow"] = residual_follow
    result["residual_follow_line"] = str(
        residual_follow.get("residual_follow_line") or ""
    )
    result["residual_follow_ready"] = bool(residual_follow.get("residual_follow"))
    result["residual_follow_action"] = str(
        residual_follow.get("residual_follow_action") or "wait_for_reverse_flow"
    )
    result["call_residual_comparison"] = bool(
        residual_follow.get("call_residual_comparison")
    )
    result["residual_follow_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_follow"
    )
    residual_comparison = (
        dispatch_packet.get("residual_comparison")
        if isinstance(dispatch_packet.get("residual_comparison"), dict)
        else package_reverse_flow_focused_validation_continue_residual_comparison(
            pipeline=updated_pipeline,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_comparison"] = residual_comparison
    result["residual_comparison_line"] = str(
        residual_comparison.get("residual_comparison_line") or ""
    )
    result["residual_comparison_ready"] = bool(
        residual_comparison.get("residual_comparison")
    )
    result["residual_comparison_action"] = str(
        residual_comparison.get("residual_comparison_action") or "wait_for_reverse_flow"
    )
    result["call_residual_unlocked_apply"] = bool(
        residual_comparison.get("call_residual_unlocked_apply")
    )
    result["residual_comparison_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_comparison"
    )
    residual_unlocked_apply = (
        dispatch_packet.get("residual_unlocked_apply")
        if isinstance(dispatch_packet.get("residual_unlocked_apply"), dict)
        else package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
            pipeline=updated_pipeline,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_unlocked_apply"] = residual_unlocked_apply
    result["residual_unlocked_apply_line"] = str(
        residual_unlocked_apply.get("residual_unlocked_apply_line") or ""
    )
    result["residual_unlocked_apply_ready"] = bool(
        residual_unlocked_apply.get("residual_unlocked_apply")
    )
    result["residual_unlocked_apply_action"] = str(
        residual_unlocked_apply.get("residual_unlocked_apply_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_focused_validation"] = bool(
        residual_unlocked_apply.get("call_residual_focused_validation")
    )
    result["residual_unlocked_apply_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
    )
    residual_focused_validation = (
        dispatch_packet.get("residual_focused_validation")
        if isinstance(dispatch_packet.get("residual_focused_validation"), dict)
        else package_reverse_flow_focused_validation_continue_residual_focused_validation(
            pipeline=updated_pipeline,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_focused_validation"] = residual_focused_validation
    result["residual_focused_validation_line"] = str(
        residual_focused_validation.get("residual_focused_validation_line") or ""
    )
    result["residual_focused_validation_ready"] = bool(
        residual_focused_validation.get("residual_focused_validation")
    )
    result["residual_focused_validation_action"] = str(
        residual_focused_validation.get("residual_focused_validation_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_handoff"] = bool(
        residual_focused_validation.get("call_residual_handoff")
    )
    result["residual_focused_validation_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_focused_validation"
    )
    residual_handoff = (
        dispatch_packet.get("residual_handoff")
        if isinstance(dispatch_packet.get("residual_handoff"), dict)
        else package_reverse_flow_focused_validation_continue_residual_handoff(
            pipeline=updated_pipeline,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_handoff"] = residual_handoff
    result["residual_handoff_line"] = str(
        residual_handoff.get("residual_handoff_line") or ""
    )
    result["residual_handoff_ready"] = bool(
        residual_handoff.get("residual_handoff")
    )
    result["residual_handoff_action"] = str(
        residual_handoff.get("residual_handoff_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_acceptance"] = bool(
        residual_handoff.get("call_residual_acceptance")
    )
    result["residual_handoff_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_handoff"
    )
    residual_acceptance = (
        dispatch_packet.get("residual_acceptance")
        if isinstance(dispatch_packet.get("residual_acceptance"), dict)
        else package_reverse_flow_focused_validation_continue_residual_acceptance(
            pipeline=updated_pipeline,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_acceptance"] = residual_acceptance
    result["residual_acceptance_line"] = str(
        residual_acceptance.get("residual_acceptance_line") or ""
    )
    result["residual_acceptance_ready"] = bool(
        residual_acceptance.get("residual_acceptance")
    )
    result["residual_acceptance_action"] = str(
        residual_acceptance.get("residual_acceptance_action")
        or "wait_for_reverse_flow"
    )
    result["residual_acceptance_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_acceptance"
    )
    residual_cascade = (
        dispatch_packet.get("residual_cascade")
        if isinstance(dispatch_packet.get("residual_cascade"), dict)
        else package_reverse_flow_focused_validation_continue_residual_cascade(
            pipeline=updated_pipeline,
            residual_acceptance=residual_acceptance,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_cascade"] = residual_cascade
    result["residual_cascade_line"] = str(
        residual_cascade.get("residual_cascade_line") or ""
    )
    result["residual_cascade_ready"] = bool(
        residual_cascade.get("residual_cascade")
    )
    result["residual_cascade_action"] = str(
        residual_cascade.get("residual_cascade_action")
        or "wait_for_reverse_flow"
    )
    result["residual_cascade_progress_label"] = str(
        residual_cascade.get("residual_cascade_progress_label") or "0/8"
    )
    result["residual_cascade_blocked_at"] = str(
        residual_cascade.get("residual_cascade_blocked_at") or "open"
    )
    result["residual_cascade_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_cascade"
    )
    continue_cascade = (
        dispatch_packet.get("continue_cascade")
        if isinstance(dispatch_packet.get("continue_cascade"), dict)
        else package_reverse_flow_focused_validation_continue_cascade(
            pipeline=updated_pipeline,
            operator_card=post_operator_card,
            residual_cascade=residual_cascade,
            follow_through=post_follow_through,
            finish_receipt=finish_receipt,
        )
    )
    result["continue_cascade"] = continue_cascade
    result["continue_cascade_line"] = str(
        continue_cascade.get("continue_cascade_line") or ""
    )
    result["continue_cascade_ready"] = bool(
        continue_cascade.get("continue_cascade")
    )
    result["continue_cascade_action"] = str(
        continue_cascade.get("continue_cascade_action") or "wait_for_reverse_flow"
    )
    result["continue_cascade_reverse_progress_label"] = str(
        continue_cascade.get("reverse_progress_label") or "0/0"
    )
    result["continue_cascade_residual_progress_label"] = str(
        continue_cascade.get("residual_progress_label") or "0/8"
    )
    result["continue_cascade_residual_blocked_at"] = str(
        continue_cascade.get("residual_cascade_blocked_at") or "open"
    )
    result["continue_cascade_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade"
    )
    pre_cascade_packet = (
        dispatch_packet.get("pre_continue_cascade")
        if isinstance(dispatch_packet.get("pre_continue_cascade"), dict)
        else pre_continue_cascade
    )
    result["pre_continue_cascade"] = pre_cascade_packet
    cascade_transition = (
        dispatch_packet.get("continue_cascade_transition")
        if isinstance(dispatch_packet.get("continue_cascade_transition"), dict)
        else package_reverse_flow_focused_validation_continue_cascade_transition(
            pre_cascade=pre_cascade_packet,
            post_cascade=continue_cascade,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["continue_cascade_transition"] = cascade_transition
    result["continue_cascade_transition_line"] = str(
        cascade_transition.get("continue_cascade_transition_line") or ""
    )
    result["continue_cascade_transition_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_transition"
    )
    result["cascade_advanced"] = bool(cascade_transition.get("cascade_advanced"))
    result["reverse_progress_transition"] = str(
        cascade_transition.get("reverse_progress_transition") or "0/0→0/0"
    )
    result["residual_progress_transition"] = str(
        cascade_transition.get("residual_progress_transition") or "0/8→0/8"
    )
    cascade_wake = (
        dispatch_packet.get("continue_cascade_wake")
        if isinstance(dispatch_packet.get("continue_cascade_wake"), dict)
        else package_reverse_flow_focused_validation_continue_cascade_wake(
            cascade_transition=cascade_transition,
            exec_receipt=exec_receipt if isinstance(exec_receipt, dict) else None,
            finish_receipt=finish_receipt if isinstance(finish_receipt, dict) else None,
            residual_open=residual_open if isinstance(residual_open, dict) else None,
            follow_through=(
                post_follow_through
                if isinstance(post_follow_through, dict)
                else follow_through
            ),
        )
    )
    result["continue_cascade_wake"] = cascade_wake
    result["continue_cascade_wake_line"] = str(
        cascade_wake.get("continue_cascade_wake_line") or ""
    )
    result["continue_cascade_wake_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake"
    )
    result["wake_outcome"] = str(cascade_wake.get("wake_outcome") or "identity")
    cascade_wake_route = (
        dispatch_packet.get("continue_cascade_wake_route")
        if isinstance(dispatch_packet.get("continue_cascade_wake_route"), dict)
        else package_reverse_flow_focused_validation_continue_cascade_wake_route(
            cascade_wake=cascade_wake,
        )
    )
    result["continue_cascade_wake_route"] = cascade_wake_route
    result["continue_cascade_wake_route_line"] = str(
        cascade_wake_route.get("continue_cascade_wake_route_line") or ""
    )
    result["continue_cascade_wake_route_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route"
    )
    result["wake_route_action"] = str(
        cascade_wake_route.get("route_action") or "inventory_only"
    )
    result["wake_route_call_execute"] = bool(
        cascade_wake_route.get("call_follow_with_execute")
    )
    # Pre-route from pre_continue_cascade so apply receipts show execute_now→…
    # without supervisors re-comparing nested route packets after follow.
    pre_cascade_wake = package_reverse_flow_focused_validation_continue_cascade_wake(
        cascade_transition=package_reverse_flow_focused_validation_continue_cascade_transition(
            pre_cascade=pre_continue_cascade,
            post_cascade=pre_continue_cascade,
            executed=False,
            recorded=False,
        ),
        exec_receipt=None,
        finish_receipt=None,
        residual_open=None,
        follow_through=follow_through if isinstance(follow_through, dict) else None,
    )
    pre_cascade_wake_route = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route(
            cascade_wake=pre_cascade_wake,
        )
    )
    cascade_wake_route_apply = (
        dispatch_packet.get("continue_cascade_wake_route_apply")
        if isinstance(dispatch_packet.get("continue_cascade_wake_route_apply"), dict)
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
            pre_route=pre_cascade_wake_route,
            post_route=cascade_wake_route,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["pre_continue_cascade_wake_route"] = pre_cascade_wake_route
    result["continue_cascade_wake_route_apply"] = cascade_wake_route_apply
    result["continue_cascade_wake_route_apply_line"] = str(
        cascade_wake_route_apply.get("continue_cascade_wake_route_apply_line") or ""
    )
    result["continue_cascade_wake_route_apply_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    )
    result["wake_route_action_transition"] = str(
        cascade_wake_route_apply.get("route_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_advanced"] = bool(
        cascade_wake_route_apply.get("route_advanced")
    )
    cascade_wake_route_apply_follow = (
        dispatch_packet.get("continue_cascade_wake_route_apply_follow")
        if isinstance(
            dispatch_packet.get("continue_cascade_wake_route_apply_follow"), dict
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
            route_apply=cascade_wake_route_apply,
            post_route=cascade_wake_route,
        )
    )
    result["continue_cascade_wake_route_apply_follow"] = (
        cascade_wake_route_apply_follow
    )
    result["continue_cascade_wake_route_apply_follow_line"] = str(
        cascade_wake_route_apply_follow.get(
            "continue_cascade_wake_route_apply_follow_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
    )
    result["wake_route_apply_follow_action"] = str(
        cascade_wake_route_apply_follow.get("follow_action") or "inventory_only"
    )
    result["wake_route_apply_follow_helper"] = str(
        cascade_wake_route_apply_follow.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_call_execute"] = bool(
        cascade_wake_route_apply_follow.get("call_follow_with_execute")
    )
    cascade_wake_route_apply_follow_pin = (
        dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin")
        if isinstance(
            dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin"), dict
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
            apply_follow=cascade_wake_route_apply_follow,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin"] = (
        cascade_wake_route_apply_follow_pin
    )
    result["continue_cascade_wake_route_apply_follow_pin_line"] = str(
        cascade_wake_route_apply_follow_pin.get(
            "continue_cascade_wake_route_apply_follow_pin_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
    )
    result["wake_route_apply_follow_pin_action"] = str(
        cascade_wake_route_apply_follow_pin.get("pin_action") or "inventory_only"
    )
    result["wake_route_apply_follow_pin_mode"] = str(
        cascade_wake_route_apply_follow_pin.get("pin_mode") or "inventory_only"
    )
    result["wake_route_apply_follow_pin_helper"] = str(
        cascade_wake_route_apply_follow_pin.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin.get("call_pin_with_execute")
    )
    result["wake_route_apply_follow_pin_ready"] = bool(
        cascade_wake_route_apply_follow_pin.get("pin_ready")
    )
    # Pre-pin from pre_route identity follow so pin_call shows execute_helper→…
    # without supervisors re-comparing nested pin packets after follow.
    pre_route_apply = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
            pre_route=pre_cascade_wake_route,
            post_route=pre_cascade_wake_route,
            executed=False,
            recorded=False,
        )
    )
    pre_apply_follow = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
            route_apply=pre_route_apply,
            post_route=pre_cascade_wake_route,
        )
    )
    pre_cascade_wake_route_apply_follow_pin = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
            apply_follow=pre_apply_follow,
        )
    )
    cascade_wake_route_apply_follow_pin_call = (
        dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin_call")
        if isinstance(
            dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin_call"),
            dict,
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
            pre_pin=pre_cascade_wake_route_apply_follow_pin,
            post_pin=cascade_wake_route_apply_follow_pin,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["pre_continue_cascade_wake_route_apply_follow_pin"] = (
        pre_cascade_wake_route_apply_follow_pin
    )
    result["continue_cascade_wake_route_apply_follow_pin_call"] = (
        cascade_wake_route_apply_follow_pin_call
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_line"] = str(
        cascade_wake_route_apply_follow_pin_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
    )
    result["wake_route_apply_follow_pin_action_transition"] = str(
        cascade_wake_route_apply_follow_pin_call.get("pin_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_mode_transition"] = str(
        cascade_wake_route_apply_follow_pin_call.get("pin_mode_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_advanced"] = bool(
        cascade_wake_route_apply_follow_pin_call.get("pin_advanced")
    )
    cascade_wake_route_apply_follow_pin_call_next = (
        dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin_call_next")
        if isinstance(
            dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin_call_next"),
            dict,
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
            pin_call=cascade_wake_route_apply_follow_pin_call,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next"] = (
        cascade_wake_route_apply_follow_pin_call_next
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_line"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
    )
    result["wake_route_apply_follow_pin_call_next_action"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("next_action")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_invoke"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("next_invoke")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_helper"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_next_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin_call_next.get("call_next_with_execute")
    )
    result["wake_route_apply_follow_pin_call_next_ready"] = bool(
        cascade_wake_route_apply_follow_pin_call_next.get("next_ready")
    )
    # Pre next-call is identity of pre_pin so supervisors can compare pre→post
    # next-invoke recipes without inventing a false advanced transition.
    pre_pin_call_for_next = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
            pre_pin=pre_cascade_wake_route_apply_follow_pin,
            post_pin=pre_cascade_wake_route_apply_follow_pin,
            executed=False,
            recorded=False,
        )
    )
    pre_cascade_wake_route_apply_follow_pin_call_next = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
            pin_call=pre_pin_call_for_next,
        )
    )
    cascade_wake_route_apply_follow_pin_call_next_call = (
        dispatch_packet.get("continue_cascade_wake_route_apply_follow_pin_call_next_call")
        if isinstance(
            dispatch_packet.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call"
            ),
            dict,
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call(
            pre_next=pre_cascade_wake_route_apply_follow_pin_call_next,
            post_next=cascade_wake_route_apply_follow_pin_call_next,
            executed=bool(result.get("executed")),
            recorded=bool(result.get("recorded")),
        )
    )
    result["pre_continue_cascade_wake_route_apply_follow_pin_call_next"] = (
        pre_cascade_wake_route_apply_follow_pin_call_next
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call"] = (
        cascade_wake_route_apply_follow_pin_call_next_call
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_line"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
    )
    result["wake_route_apply_follow_pin_call_next_action_transition"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_invoke_transition"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_invoke_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_advanced"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_advanced")
    )
    cascade_wake_route_apply_follow_pin_call_next_call_follow = (
        dispatch_packet.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
        )
        if isinstance(
            dispatch_packet.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
            ),
            dict,
        )
        else package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow(
            next_call=cascade_wake_route_apply_follow_pin_call_next_call,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"] = (
        cascade_wake_route_apply_follow_pin_call_next_call_follow
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"] = (
        str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
            )
            or ""
        )
    )
    result[
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_action"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_action")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_mode"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_mode")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_helper"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "preferred_helper"
        )
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "call_follow_with_execute"
        )
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_ready"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_ready")
    )
    result["call_dispatch_with_execute"] = bool(should_execute)
    result["followed_recommendation"] = execute is None
    result["execute_requested"] = execute
    result["inventory_dispatch"] = (
        dispatch_packet.get("inventory_dispatch")
        if isinstance(dispatch_packet.get("inventory_dispatch"), dict)
        else inventory_packet
    )
    result["post_dispatch_inventory"] = post_inventory
    result["follow_through_helper"] = (
        "follow_reverse_flow_focused_validation_continue_dispatch"
    )
    result["resolve_helper"] = (
        "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
    )
    result["operator_card_helper"] = (
        "package_reverse_flow_focused_validation_continue_operator_card"
    )
    result["progress_transition_helper"] = (
        "package_reverse_flow_focused_validation_continue_progress_transition"
    )
    result["residual_export_allowed"] = False
    helpers = list(result.get("record_helpers") or [])
    for name in (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route",
        "package_reverse_flow_focused_validation_continue_cascade_wake",
        "package_reverse_flow_focused_validation_continue_cascade_transition",
        "package_reverse_flow_focused_validation_continue_cascade",
        "package_reverse_flow_focused_validation_continue_residual_cascade",
        "package_reverse_flow_focused_validation_continue_residual_acceptance",
        "package_reverse_flow_focused_validation_continue_residual_handoff",
        "package_reverse_flow_focused_validation_continue_residual_focused_validation",
        "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
        "package_reverse_flow_focused_validation_continue_residual_comparison",
        "package_reverse_flow_focused_validation_continue_residual_follow",
        "package_reverse_flow_focused_validation_continue_residual_entry",
        "package_reverse_flow_focused_validation_continue_residual_open",
        "package_reverse_flow_focused_validation_continue_finish_receipt",
        "package_reverse_flow_focused_validation_continue_exec_receipt",
        "package_reverse_flow_focused_validation_continue_progress_transition",
        "package_reverse_flow_focused_validation_continue_operator_card",
        "follow_reverse_flow_focused_validation_continue_dispatch",
        "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
    ):
        if name not in helpers:
            helpers.insert(0, name)
    result["record_helpers"] = helpers
    return result


def dispatch_reverse_flow_focused_validation_continue_supervisor_wake(
    pipeline: dict[str, Any],
    *,
    execute: bool = True,
    command_runner: Any = subprocess.run,
    cwd: Path | str | None = None,
    timeout: int = 120,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
    record: bool = True,
) -> dict[str, Any]:
    """Single operator entry: inventory wake, optional run/record, reverse-flow-first next.

    Resolves the continue-run supervisor_wake from the pipeline first. When
    ``execute`` is true and the inventory wake is continue-run executable, runs
    allowlisted pending work units and optionally records body-free outcomes.
    Always returns reverse-flow-first ``supervisor_wake`` /
    ``supervisor_next_action`` so residual fortress stages cannot outrank
    reverse-flow before record/close and activation-external acceptance.

    Inventory-only wakes (``execute=False`` or not executable) still package
    mode, residual hold, and next action without running commands via
    ``package_reverse_flow_focused_validation_continue_dispatch_inventory``.
    Attaches durable ``follow_through`` so supervisors can read
    ``call_dispatch_with_execute`` without re-deriving action + recommendation.
    Also packages residual_open, residual_entry, residual_follow,
    residual_comparison, residual_unlocked_apply, residual_focused_validation,
    residual_handoff, residual_acceptance, residual_cascade, continue_cascade,
    continue_cascade_transition (pre/post reverse + residual progress),
    continue_cascade_wake (classified wake_outcome), and
    continue_cascade_wake_route (wake_outcome → route_action), and
    continue_cascade_wake_route_apply (pre→post route action transition),
    continue_cascade_wake_route_apply_follow (applied route → preferred helper),
    continue_cascade_wake_route_apply_follow_pin (follow → call recipe pin),
    continue_cascade_wake_route_apply_follow_pin_call (pre→post pin call
    receipt), continue_cascade_wake_route_apply_follow_pin_call_next
    (pin_call → sealed next-invoke recipe), and
    continue_cascade_wake_route_apply_follow_pin_call_next_call (pre→post
    next-invoke call receipt) (blocked while reverse-flow waits; ready after
    pass) without residual_export.
    Never enables activation, push, promotion, provider launch, remote apply,
    external skill execution, or kernel restart. Does not export stdout.

    Policy-aware callers that want recommendation-following defaults should use
    ``follow_reverse_flow_focused_validation_continue_dispatch`` instead.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    inventory_packet = package_reverse_flow_focused_validation_continue_dispatch_inventory(
        pipeline=pipeline,
        execute_requested=bool(execute),
        record_requested=bool(record),
    )
    inventory_wake = (
        inventory_packet.get("inventory_wake")
        if isinstance(inventory_packet.get("inventory_wake"), dict)
        else {}
    )
    follow_through = resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
        inventory_dispatch=inventory_packet,
        execute_requested=bool(execute),
    )
    operator_card = package_reverse_flow_focused_validation_continue_operator_card(
        pipeline=pipeline,
        follow_through=follow_through,
    )
    pre_continue_cascade = package_reverse_flow_focused_validation_continue_cascade(
        pipeline=pipeline,
        operator_card=operator_card,
        follow_through=follow_through,
    )
    continue_run_executable = bool(inventory_packet.get("continue_run_executable"))
    should_execute = bool(execute) and continue_run_executable

    if should_execute:
        run_packet = run_reverse_flow_focused_validation_continue_pending_work_units(
            pipeline,
            command_runner=command_runner,
            cwd=cwd,
            timeout=timeout,
            source_digest=source_digest,
            theme_window=theme_window,
            record=record,
        )
        supervisor_wake = (
            run_packet.get("supervisor_wake")
            if isinstance(run_packet.get("supervisor_wake"), dict)
            else inventory_wake
        )
        updated_pipeline = (
            run_packet.get("pipeline")
            if isinstance(run_packet.get("pipeline"), dict)
            else pipeline
        )
        # Re-package post-run inventory so supervisors can read the next durable
        # action without re-resolving nested continue packets.
        post_inventory = package_reverse_flow_focused_validation_continue_dispatch_inventory(
            pipeline=updated_pipeline,
            execute_requested=False,
            record_requested=bool(record),
        )
        post_follow_through = (
            resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
                inventory_dispatch=post_inventory,
                execute_requested=False,
            )
        )
        post_operator_card = package_reverse_flow_focused_validation_continue_operator_card(
            pipeline=updated_pipeline,
            follow_through=post_follow_through,
        )
        progress_transition = (
            package_reverse_flow_focused_validation_continue_progress_transition(
                pre_card=operator_card,
                post_card=post_operator_card,
                pre_follow_through=follow_through,
                post_follow_through=post_follow_through,
                executed=True,
                recorded=bool(run_packet.get("recorded")),
            )
        )
        exec_receipt = (
            run_packet.get("exec_receipt")
            if isinstance(run_packet.get("exec_receipt"), dict)
            else package_reverse_flow_focused_validation_continue_exec_receipt(
                run_plan=(
                    run_packet.get("run_plan")
                    if isinstance(run_packet.get("run_plan"), dict)
                    else None
                ),
                run_result=(
                    run_packet.get("run_result")
                    if isinstance(run_packet.get("run_result"), dict)
                    else None
                ),
                recorded=bool(run_packet.get("recorded")),
                executed=True,
                residual_hold_active=bool(
                    supervisor_wake.get("residual_hold_active", True)
                ),
            )
        )
        execute_result = {
            "schema_version": 1,
            "controller_surface": (
                "reverse_flow_focused_validation_continue_supervisor_dispatch"
            ),
            "proposal_track": "prop-reverse-flow-skill-route-discovery-continue",
            "action": "run_and_record",
            "executed": True,
            "execute_requested": bool(execute),
            "execute_recommended": bool(
                post_inventory.get("execute_recommended")
            ),
            "inventory_wake": inventory_wake,
            "inventory_dispatch": inventory_packet,
            "post_dispatch_inventory": post_inventory,
            "follow_through": follow_through,
            "post_follow_through": post_follow_through,
            "follow_through_action": str(
                follow_through.get("follow_through_action") or "noop"
            ),
            "post_follow_through_action": str(
                post_follow_through.get("follow_through_action") or "noop"
            ),
            "operator_card": operator_card,
            "post_operator_card": post_operator_card,
            "progress_label": str(operator_card.get("progress_label") or "0/0"),
            "post_progress_label": str(
                post_operator_card.get("progress_label") or "0/0"
            ),
            "progress_transition": progress_transition,
            "progress_transition_label": str(
                progress_transition.get("progress_transition_label") or "0/0→0/0"
            ),
            "progress_advanced": bool(
                progress_transition.get("progress_advanced")
            ),
            "transition_line": str(
                progress_transition.get("transition_line") or ""
            ),
            "action_line": str(operator_card.get("action_line") or ""),
            "post_action_line": str(post_operator_card.get("action_line") or ""),
            "exec_receipt": exec_receipt,
            "exec_line": str(exec_receipt.get("exec_line") or ""),
            "exec_plan_line": str(
                exec_receipt.get("exec_plan_line")
                or (
                    (run_packet.get("run_plan") or {}).get("exec_plan_line")
                    if isinstance(run_packet.get("run_plan"), dict)
                    else ""
                )
                or ""
            ),
            "call_dispatch_with_execute": True,
            "run_and_record": run_packet,
            "pipeline": updated_pipeline,
            "recorded": bool(run_packet.get("recorded")),
            "record_requested": bool(record),
            "run_plan": run_packet.get("run_plan"),
            "run_result": run_packet.get("run_result"),
            "supervisor_wake": supervisor_wake,
            "supervisor_next_action": str(
                supervisor_wake.get("supervisor_next_action")
                or run_packet.get("supervisor_next_action")
                or "none"
            ),
            "continue_plan_mode": str(
                supervisor_wake.get("mode")
                or run_packet.get("continue_plan_mode")
                or "none"
            ),
            "focused_validation_status": str(
                supervisor_wake.get("status")
                or run_packet.get("focused_validation_status")
                or "none"
            ),
            "activation_external_handoff_status": str(
                supervisor_wake.get("activation_external_handoff_status")
                or run_packet.get("activation_external_handoff_status")
                or "none"
            ),
            "activation_external_acceptance_status": str(
                supervisor_wake.get("activation_external_acceptance_status")
                or run_packet.get("activation_external_acceptance_status")
                or "none"
            ),
            "residual_hold_active": bool(
                supervisor_wake.get("residual_hold_active", True)
            ),
            "residual_export_allowed": False,
            "continue_run_executable": bool(
                supervisor_wake.get("continue_run_executable")
            ),
            "continue_run_recommended": bool(
                supervisor_wake.get("continue_run_recommended")
            ),
            "dispatch_helper": (
                "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
            ),
            "inventory_helper": (
                "package_reverse_flow_focused_validation_continue_dispatch_inventory"
            ),
            "follow_through_helper": (
                "follow_reverse_flow_focused_validation_continue_dispatch"
            ),
            "resolve_helper": (
                "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
            ),
            "operator_card_helper": (
                "package_reverse_flow_focused_validation_continue_operator_card"
            ),
            "progress_transition_helper": (
                "package_reverse_flow_focused_validation_continue_progress_transition"
            ),
            "exec_receipt_helper": (
                "package_reverse_flow_focused_validation_continue_exec_receipt"
            ),
            "finish_receipt_helper": (
                "package_reverse_flow_focused_validation_continue_finish_receipt"
            ),
            "residual_open_helper": (
                "package_reverse_flow_focused_validation_continue_residual_open"
            ),
            "activation_external_only": True,
            "supervisor_activation_allowed": False,
            "runtime_action": "none",
            "external_skill_execution_allowed": False,
            "provider_launch_allowed": False,
            "remote_apply_allowed": False,
            "push_or_promotion_allowed": False,
            "kernel_restart_allowed": False,
            "body_free": True,
            "raw_evidence_urls_exported": False,
            "raw_upstream_bodies_exported": False,
            "raw_command_stdout_exported": False,
            "record_helpers": [
                "package_reverse_flow_focused_validation_continue_residual_open",
                "package_reverse_flow_focused_validation_continue_finish_receipt",
                "package_reverse_flow_focused_validation_continue_exec_receipt",
                "package_reverse_flow_focused_validation_continue_progress_transition",
                "package_reverse_flow_focused_validation_continue_operator_card",
                "follow_reverse_flow_focused_validation_continue_dispatch",
                "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
                "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
                "package_reverse_flow_focused_validation_continue_dispatch_inventory",
                "run_reverse_flow_focused_validation_continue_pending_work_units",
                "execute_reverse_flow_focused_validation_continue_run_plan",
                "build_reverse_flow_focused_validation_continue_run_plan",
                "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
                "reverse_flow_focused_validation_continue_local_command_allowed",
                "record_reverse_flow_focused_validation_continue_outcomes",
                "resolve_reverse_flow_focused_validation_continue_supervisor_next",
                "build_reverse_flow_focused_validation_continue_plan",
            ],
        }
        # Attach body-free finish receipt after execute so supervisors can read
        # continue_finished / residual_queue_ready without nested re-assembly.
        finish_receipt = package_reverse_flow_focused_validation_continue_finish_receipt(
            pipeline=updated_pipeline,
            post_operator_card=post_operator_card,
            progress_transition=progress_transition,
            exec_receipt=exec_receipt,
            supervisor_wake=supervisor_wake,
            focused_validation_status=str(
                execute_result.get("focused_validation_status") or ""
            )
            or None,
            activation_external_handoff_status=str(
                execute_result.get("activation_external_handoff_status") or ""
            )
            or None,
            activation_external_acceptance_status=str(
                execute_result.get("activation_external_acceptance_status") or ""
            )
            or None,
            residual_hold_active=bool(execute_result.get("residual_hold_active", True)),
            continue_plan_mode=str(execute_result.get("continue_plan_mode") or "")
            or None,
            supervisor_next_action=str(
                execute_result.get("supervisor_next_action") or ""
            )
            or None,
            executed=True,
            recorded=bool(execute_result.get("recorded")),
        )
        execute_result["finish_receipt"] = finish_receipt
        execute_result["finish_line"] = str(finish_receipt.get("finish_line") or "")
        execute_result["continue_finished"] = bool(
            finish_receipt.get("continue_finished")
        )
        execute_result["residual_queue_ready"] = bool(
            finish_receipt.get("residual_queue_ready")
        )
        # Residual open bridges finish residual_queue_ready into residual pipeline
        # entry without residual_export on continue surfaces.
        residual_open = package_reverse_flow_focused_validation_continue_residual_open(
            pipeline=updated_pipeline,
            finish_receipt=finish_receipt,
        )
        execute_result["residual_open"] = residual_open
        execute_result["residual_open_line"] = str(
            residual_open.get("residual_open_line") or ""
        )
        execute_result["residual_open_ready"] = bool(residual_open.get("residual_open"))
        execute_result["residual_adjacent_count"] = int(
            residual_open.get("residual_adjacent_count") or 0
        )
        execute_result["residual_open_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_open"
        )
        # Residual entry collapses residual open + residual apply selection so
        # supervisors see selected fortress/Hy3 IDs without nested re-assembly.
        residual_entry = package_reverse_flow_focused_validation_continue_residual_entry(
            pipeline=updated_pipeline,
            residual_open=residual_open,
        )
        execute_result["residual_entry"] = residual_entry
        execute_result["residual_entry_line"] = str(
            residual_entry.get("residual_entry_line") or ""
        )
        execute_result["residual_entry_ready"] = bool(
            residual_entry.get("residual_entry")
        )
        execute_result["selected_residual_proposal_id"] = str(
            residual_entry.get("selected_residual_proposal_id") or ""
        )
        execute_result["residual_entry_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_entry"
        )
        # Residual follow collapses residual entry into residual comparison policy
        # without residual_export on continue surfaces.
        residual_follow = package_reverse_flow_focused_validation_continue_residual_follow(
            pipeline=updated_pipeline,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
        execute_result["residual_follow"] = residual_follow
        execute_result["residual_follow_line"] = str(
            residual_follow.get("residual_follow_line") or ""
        )
        execute_result["residual_follow_ready"] = bool(
            residual_follow.get("residual_follow")
        )
        execute_result["residual_follow_action"] = str(
            residual_follow.get("residual_follow_action") or "wait_for_reverse_flow"
        )
        execute_result["call_residual_comparison"] = bool(
            residual_follow.get("call_residual_comparison")
        )
        execute_result["residual_follow_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_follow"
        )
        # Residual comparison collapses residual follow into residual comparison
        # readiness / unlocked-lane policy without residual_export.
        residual_comparison = (
            package_reverse_flow_focused_validation_continue_residual_comparison(
                pipeline=updated_pipeline,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_comparison"] = residual_comparison
        execute_result["residual_comparison_line"] = str(
            residual_comparison.get("residual_comparison_line") or ""
        )
        execute_result["residual_comparison_ready"] = bool(
            residual_comparison.get("residual_comparison")
        )
        execute_result["residual_comparison_action"] = str(
            residual_comparison.get("residual_comparison_action")
            or "wait_for_reverse_flow"
        )
        execute_result["call_residual_unlocked_apply"] = bool(
            residual_comparison.get("call_residual_unlocked_apply")
        )
        execute_result["residual_comparison_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_comparison"
        )
        # Residual unlocked apply collapses residual comparison into preferred
        # test-first unlocked lane + focused-validation policy without residual_export.
        residual_unlocked_apply = (
            package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
                pipeline=updated_pipeline,
                residual_comparison=residual_comparison,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_unlocked_apply"] = residual_unlocked_apply
        execute_result["residual_unlocked_apply_line"] = str(
            residual_unlocked_apply.get("residual_unlocked_apply_line") or ""
        )
        execute_result["residual_unlocked_apply_ready"] = bool(
            residual_unlocked_apply.get("residual_unlocked_apply")
        )
        execute_result["residual_unlocked_apply_action"] = str(
            residual_unlocked_apply.get("residual_unlocked_apply_action")
            or "wait_for_reverse_flow"
        )
        execute_result["call_residual_focused_validation"] = bool(
            residual_unlocked_apply.get("call_residual_focused_validation")
        )
        execute_result["residual_unlocked_apply_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
        )
        # Residual focused validation collapses residual unlocked apply into
        # body-free command-hash progress + handoff policy without residual_export.
        residual_focused_validation = (
            package_reverse_flow_focused_validation_continue_residual_focused_validation(
                pipeline=updated_pipeline,
                residual_unlocked_apply=residual_unlocked_apply,
                residual_comparison=residual_comparison,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_focused_validation"] = residual_focused_validation
        execute_result["residual_focused_validation_line"] = str(
            residual_focused_validation.get("residual_focused_validation_line") or ""
        )
        execute_result["residual_focused_validation_ready"] = bool(
            residual_focused_validation.get("residual_focused_validation")
        )
        execute_result["residual_focused_validation_action"] = str(
            residual_focused_validation.get("residual_focused_validation_action")
            or "wait_for_reverse_flow"
        )
        execute_result["call_residual_handoff"] = bool(
            residual_focused_validation.get("call_residual_handoff")
        )
        execute_result["residual_focused_validation_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_focused_validation"
        )
        # Residual handoff collapses residual focused validation into activation-
        # external handoff readiness + acceptance policy without residual_export.
        residual_handoff = (
            package_reverse_flow_focused_validation_continue_residual_handoff(
                pipeline=updated_pipeline,
                residual_focused_validation=residual_focused_validation,
                residual_unlocked_apply=residual_unlocked_apply,
                residual_comparison=residual_comparison,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_handoff"] = residual_handoff
        execute_result["residual_handoff_line"] = str(
            residual_handoff.get("residual_handoff_line") or ""
        )
        execute_result["residual_handoff_ready"] = bool(
            residual_handoff.get("residual_handoff")
        )
        execute_result["residual_handoff_action"] = str(
            residual_handoff.get("residual_handoff_action")
            or "wait_for_reverse_flow"
        )
        execute_result["call_residual_acceptance"] = bool(
            residual_handoff.get("call_residual_acceptance")
        )
        execute_result["residual_handoff_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_handoff"
        )
        residual_acceptance = (
            package_reverse_flow_focused_validation_continue_residual_acceptance(
                pipeline=updated_pipeline,
                residual_handoff=residual_handoff,
                residual_focused_validation=residual_focused_validation,
                residual_unlocked_apply=residual_unlocked_apply,
                residual_comparison=residual_comparison,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_acceptance"] = residual_acceptance
        execute_result["residual_acceptance_line"] = str(
            residual_acceptance.get("residual_acceptance_line") or ""
        )
        execute_result["residual_acceptance_ready"] = bool(
            residual_acceptance.get("residual_acceptance")
        )
        execute_result["residual_acceptance_action"] = str(
            residual_acceptance.get("residual_acceptance_action")
            or "wait_for_reverse_flow"
        )
        execute_result["residual_acceptance_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_acceptance"
        )
        residual_cascade = (
            package_reverse_flow_focused_validation_continue_residual_cascade(
                pipeline=updated_pipeline,
                residual_acceptance=residual_acceptance,
                residual_handoff=residual_handoff,
                residual_focused_validation=residual_focused_validation,
                residual_unlocked_apply=residual_unlocked_apply,
                residual_comparison=residual_comparison,
                residual_follow=residual_follow,
                residual_entry=residual_entry,
                residual_open=residual_open,
            )
        )
        execute_result["residual_cascade"] = residual_cascade
        execute_result["residual_cascade_line"] = str(
            residual_cascade.get("residual_cascade_line") or ""
        )
        execute_result["residual_cascade_ready"] = bool(
            residual_cascade.get("residual_cascade")
        )
        execute_result["residual_cascade_action"] = str(
            residual_cascade.get("residual_cascade_action")
            or "wait_for_reverse_flow"
        )
        execute_result["residual_cascade_progress_label"] = str(
            residual_cascade.get("residual_cascade_progress_label") or "0/8"
        )
        execute_result["residual_cascade_blocked_at"] = str(
            residual_cascade.get("residual_cascade_blocked_at") or "open"
        )
        execute_result["residual_cascade_helper"] = (
            "package_reverse_flow_focused_validation_continue_residual_cascade"
        )
        continue_cascade = (
            package_reverse_flow_focused_validation_continue_cascade(
                pipeline=updated_pipeline,
                operator_card=(
                    execute_result.get("post_operator_card")
                    if isinstance(execute_result.get("post_operator_card"), dict)
                    else operator_card
                ),
                residual_cascade=residual_cascade,
                follow_through=(
                    execute_result.get("post_follow_through")
                    if isinstance(execute_result.get("post_follow_through"), dict)
                    else follow_through
                ),
                finish_receipt=(
                    execute_result.get("finish_receipt")
                    if isinstance(execute_result.get("finish_receipt"), dict)
                    else None
                ),
            )
        )
        execute_result["continue_cascade"] = continue_cascade
        execute_result["continue_cascade_line"] = str(
            continue_cascade.get("continue_cascade_line") or ""
        )
        execute_result["continue_cascade_ready"] = bool(
            continue_cascade.get("continue_cascade")
        )
        execute_result["continue_cascade_action"] = str(
            continue_cascade.get("continue_cascade_action") or "wait_for_reverse_flow"
        )
        execute_result["continue_cascade_reverse_progress_label"] = str(
            continue_cascade.get("reverse_progress_label") or "0/0"
        )
        execute_result["continue_cascade_residual_progress_label"] = str(
            continue_cascade.get("residual_progress_label") or "0/8"
        )
        execute_result["continue_cascade_residual_blocked_at"] = str(
            continue_cascade.get("residual_cascade_blocked_at") or "open"
        )
        execute_result["continue_cascade_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade"
        )
        execute_result["pre_continue_cascade"] = pre_continue_cascade
        cascade_transition = (
            package_reverse_flow_focused_validation_continue_cascade_transition(
                pre_cascade=pre_continue_cascade,
                post_cascade=continue_cascade,
                executed=True,
                recorded=bool(run_packet.get("recorded")),
            )
        )
        execute_result["continue_cascade_transition"] = cascade_transition
        execute_result["continue_cascade_transition_line"] = str(
            cascade_transition.get("continue_cascade_transition_line") or ""
        )
        execute_result["continue_cascade_transition_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_transition"
        )
        execute_result["cascade_advanced"] = bool(
            cascade_transition.get("cascade_advanced")
        )
        execute_result["reverse_progress_transition"] = str(
            cascade_transition.get("reverse_progress_transition") or "0/0→0/0"
        )
        execute_result["residual_progress_transition"] = str(
            cascade_transition.get("residual_progress_transition") or "0/8→0/8"
        )
        cascade_wake = package_reverse_flow_focused_validation_continue_cascade_wake(
            cascade_transition=cascade_transition,
            exec_receipt=(
                execute_result.get("exec_receipt")
                if isinstance(execute_result.get("exec_receipt"), dict)
                else None
            ),
            finish_receipt=(
                execute_result.get("finish_receipt")
                if isinstance(execute_result.get("finish_receipt"), dict)
                else None
            ),
            residual_open=residual_open if isinstance(residual_open, dict) else None,
            follow_through=(
                execute_result.get("post_follow_through")
                if isinstance(execute_result.get("post_follow_through"), dict)
                else follow_through
            ),
        )
        execute_result["continue_cascade_wake"] = cascade_wake
        execute_result["continue_cascade_wake_line"] = str(
            cascade_wake.get("continue_cascade_wake_line") or ""
        )
        execute_result["continue_cascade_wake_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake"
        )
        execute_result["wake_outcome"] = str(
            cascade_wake.get("wake_outcome") or "identity"
        )
        cascade_wake_route = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route(
                cascade_wake=cascade_wake,
            )
        )
        execute_result["continue_cascade_wake_route"] = cascade_wake_route
        execute_result["continue_cascade_wake_route_line"] = str(
            cascade_wake_route.get("continue_cascade_wake_route_line") or ""
        )
        execute_result["continue_cascade_wake_route_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route"
        )
        execute_result["wake_route_action"] = str(
            cascade_wake_route.get("route_action") or "inventory_only"
        )
        execute_result["wake_route_call_execute"] = bool(
            cascade_wake_route.get("call_follow_with_execute")
        )
        pre_cascade_wake = package_reverse_flow_focused_validation_continue_cascade_wake(
            cascade_transition=package_reverse_flow_focused_validation_continue_cascade_transition(
                pre_cascade=pre_continue_cascade,
                post_cascade=pre_continue_cascade,
                executed=False,
                recorded=False,
            ),
            exec_receipt=None,
            finish_receipt=None,
            residual_open=None,
            follow_through=follow_through if isinstance(follow_through, dict) else None,
        )
        pre_cascade_wake_route = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route(
                cascade_wake=pre_cascade_wake,
            )
        )
        cascade_wake_route_apply = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
                pre_route=pre_cascade_wake_route,
                post_route=cascade_wake_route,
                executed=True,
                recorded=bool(run_packet.get("recorded")),
            )
        )
        execute_result["pre_continue_cascade_wake_route"] = pre_cascade_wake_route
        execute_result["continue_cascade_wake_route_apply"] = cascade_wake_route_apply
        execute_result["continue_cascade_wake_route_apply_line"] = str(
            cascade_wake_route_apply.get("continue_cascade_wake_route_apply_line")
            or ""
        )
        execute_result["continue_cascade_wake_route_apply_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
        )
        execute_result["wake_route_action_transition"] = str(
            cascade_wake_route_apply.get("route_action_transition")
            or "inventory_only→inventory_only"
        )
        execute_result["wake_route_advanced"] = bool(
            cascade_wake_route_apply.get("route_advanced")
        )
        cascade_wake_route_apply_follow = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
                route_apply=cascade_wake_route_apply,
                post_route=cascade_wake_route,
            )
        )
        execute_result["continue_cascade_wake_route_apply_follow"] = (
            cascade_wake_route_apply_follow
        )
        execute_result["continue_cascade_wake_route_apply_follow_line"] = str(
            cascade_wake_route_apply_follow.get(
                "continue_cascade_wake_route_apply_follow_line"
            )
            or ""
        )
        execute_result["continue_cascade_wake_route_apply_follow_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
        )
        execute_result["wake_route_apply_follow_action"] = str(
            cascade_wake_route_apply_follow.get("follow_action") or "inventory_only"
        )
        execute_result["wake_route_apply_follow_helper"] = str(
            cascade_wake_route_apply_follow.get("preferred_helper")
            or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        execute_result["wake_route_apply_follow_call_execute"] = bool(
            cascade_wake_route_apply_follow.get("call_follow_with_execute")
        )
        cascade_wake_route_apply_follow_pin = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
                apply_follow=cascade_wake_route_apply_follow,
            )
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin"] = (
            cascade_wake_route_apply_follow_pin
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_line"] = str(
            cascade_wake_route_apply_follow_pin.get(
                "continue_cascade_wake_route_apply_follow_pin_line"
            )
            or ""
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
        )
        execute_result["wake_route_apply_follow_pin_action"] = str(
            cascade_wake_route_apply_follow_pin.get("pin_action") or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_mode"] = str(
            cascade_wake_route_apply_follow_pin.get("pin_mode") or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_helper"] = str(
            cascade_wake_route_apply_follow_pin.get("preferred_helper")
            or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        execute_result["wake_route_apply_follow_pin_call_execute"] = bool(
            cascade_wake_route_apply_follow_pin.get("call_pin_with_execute")
        )
        execute_result["wake_route_apply_follow_pin_ready"] = bool(
            cascade_wake_route_apply_follow_pin.get("pin_ready")
        )
        pre_route_apply = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
                pre_route=pre_cascade_wake_route,
                post_route=pre_cascade_wake_route,
                executed=False,
                recorded=False,
            )
        )
        pre_apply_follow = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
                route_apply=pre_route_apply,
                post_route=pre_cascade_wake_route,
            )
        )
        pre_cascade_wake_route_apply_follow_pin = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
                apply_follow=pre_apply_follow,
            )
        )
        cascade_wake_route_apply_follow_pin_call = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
                pre_pin=pre_cascade_wake_route_apply_follow_pin,
                post_pin=cascade_wake_route_apply_follow_pin,
                executed=True,
                recorded=bool(run_packet.get("recorded")),
            )
        )
        execute_result["pre_continue_cascade_wake_route_apply_follow_pin"] = (
            pre_cascade_wake_route_apply_follow_pin
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call"] = (
            cascade_wake_route_apply_follow_pin_call
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call_line"] = str(
            cascade_wake_route_apply_follow_pin_call.get(
                "continue_cascade_wake_route_apply_follow_pin_call_line"
            )
            or ""
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call_helper"] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
        )
        execute_result["wake_route_apply_follow_pin_action_transition"] = str(
            cascade_wake_route_apply_follow_pin_call.get("pin_action_transition")
            or "inventory_only→inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_mode_transition"] = str(
            cascade_wake_route_apply_follow_pin_call.get("pin_mode_transition")
            or "inventory_only→inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_advanced"] = bool(
            cascade_wake_route_apply_follow_pin_call.get("pin_advanced")
        )
        cascade_wake_route_apply_follow_pin_call_next = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
                pin_call=cascade_wake_route_apply_follow_pin_call,
            )
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call_next"] = (
            cascade_wake_route_apply_follow_pin_call_next
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call_next_line"] = (
            str(
                cascade_wake_route_apply_follow_pin_call_next.get(
                    "continue_cascade_wake_route_apply_follow_pin_call_next_line"
                )
                or ""
            )
        )
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_helper"
        ] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
        )
        execute_result["wake_route_apply_follow_pin_call_next_action"] = str(
            cascade_wake_route_apply_follow_pin_call_next.get("next_action")
            or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_invoke"] = str(
            cascade_wake_route_apply_follow_pin_call_next.get("next_invoke")
            or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_helper"] = str(
            cascade_wake_route_apply_follow_pin_call_next.get("preferred_helper")
            or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        execute_result["wake_route_apply_follow_pin_call_next_call_execute"] = bool(
            cascade_wake_route_apply_follow_pin_call_next.get("call_next_with_execute")
        )
        execute_result["wake_route_apply_follow_pin_call_next_ready"] = bool(
            cascade_wake_route_apply_follow_pin_call_next.get("next_ready")
        )
        pre_pin_call_for_next = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
                pre_pin=pre_cascade_wake_route_apply_follow_pin,
                post_pin=pre_cascade_wake_route_apply_follow_pin,
                executed=False,
                recorded=False,
            )
        )
        pre_cascade_wake_route_apply_follow_pin_call_next = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
                pin_call=pre_pin_call_for_next,
            )
        )
        cascade_wake_route_apply_follow_pin_call_next_call = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call(
                pre_next=pre_cascade_wake_route_apply_follow_pin_call_next,
                post_next=cascade_wake_route_apply_follow_pin_call_next,
                executed=True,
                recorded=bool(run_packet.get("recorded")),
            )
        )
        execute_result["pre_continue_cascade_wake_route_apply_follow_pin_call_next"] = (
            pre_cascade_wake_route_apply_follow_pin_call_next
        )
        execute_result["continue_cascade_wake_route_apply_follow_pin_call_next_call"] = (
            cascade_wake_route_apply_follow_pin_call_next_call
        )
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
        ] = str(
            cascade_wake_route_apply_follow_pin_call_next_call.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
            )
            or ""
        )
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_helper"
        ] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
        )
        execute_result["wake_route_apply_follow_pin_call_next_action_transition"] = str(
            cascade_wake_route_apply_follow_pin_call_next_call.get(
                "next_action_transition"
            )
            or "inventory_only→inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_invoke_transition"] = str(
            cascade_wake_route_apply_follow_pin_call_next_call.get(
                "next_invoke_transition"
            )
            or "inventory_only→inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_advanced"] = bool(
            cascade_wake_route_apply_follow_pin_call_next_call.get("next_advanced")
        )
        cascade_wake_route_apply_follow_pin_call_next_call_follow = (
            package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow(
                next_call=cascade_wake_route_apply_follow_pin_call_next_call,
            )
        )
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
        ] = cascade_wake_route_apply_follow_pin_call_next_call_follow
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
        ] = str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
            )
            or ""
        )
        execute_result[
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper"
        ] = (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
        )
        execute_result["wake_route_apply_follow_pin_call_next_call_follow_action"] = str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "follow_action"
            )
            or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_call_follow_mode"] = str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_mode")
            or "inventory_only"
        )
        execute_result["wake_route_apply_follow_pin_call_next_call_follow_helper"] = str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "preferred_helper"
            )
            or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        )
        execute_result[
            "wake_route_apply_follow_pin_call_next_call_follow_call_execute"
        ] = bool(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "call_follow_with_execute"
            )
        )
        execute_result["wake_route_apply_follow_pin_call_next_call_follow_ready"] = bool(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_ready")
        )
        helpers = list(execute_result.get("record_helpers") or [])
        for name in (
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
            "package_reverse_flow_focused_validation_continue_cascade_wake_route",
            "package_reverse_flow_focused_validation_continue_cascade_wake",
            "package_reverse_flow_focused_validation_continue_cascade_transition",
            "package_reverse_flow_focused_validation_continue_cascade",
        ):
            if name not in helpers:
                helpers.insert(0, name)
        execute_result["record_helpers"] = helpers
        return execute_result

    # Inventory-only / not-executable / keep / repair / noop: return durable packet
    # with follow-through so supervisors can decide execute without re-deriving.
    result = dict(inventory_packet)
    result["follow_through"] = follow_through
    result["follow_through_action"] = str(
        follow_through.get("follow_through_action") or "noop"
    )
    result["call_dispatch_with_execute"] = bool(
        follow_through.get("call_dispatch_with_execute")
    )
    result["operator_card"] = operator_card
    result["progress_label"] = str(operator_card.get("progress_label") or "0/0")
    progress_transition = (
        package_reverse_flow_focused_validation_continue_progress_transition(
            pre_card=operator_card,
            post_card=operator_card,
            pre_follow_through=follow_through,
            post_follow_through=follow_through,
            executed=False,
            recorded=False,
        )
    )
    result["progress_transition"] = progress_transition
    result["progress_transition_label"] = str(
        progress_transition.get("progress_transition_label") or "0/0→0/0"
    )
    result["progress_advanced"] = bool(progress_transition.get("progress_advanced"))
    result["transition_line"] = str(progress_transition.get("transition_line") or "")
    result["action_line"] = str(operator_card.get("action_line") or "")
    # Pre-exec inventory still packages a not-executed receipt so supervisors can
    # log exec_line / exec_plan_line without re-deriving runnable counts.
    inventory_run_plan = build_reverse_flow_focused_validation_continue_run_plan(
        pipeline=pipeline,
    )
    exec_receipt = package_reverse_flow_focused_validation_continue_exec_receipt(
        run_plan=inventory_run_plan,
        run_result=None,
        recorded=False,
        executed=False,
        residual_hold_active=bool(result.get("residual_hold_active", True)),
    )
    result["run_plan"] = inventory_run_plan
    result["exec_receipt"] = exec_receipt
    result["exec_line"] = str(exec_receipt.get("exec_line") or "")
    result["exec_plan_line"] = str(
        inventory_run_plan.get("exec_plan_line")
        or exec_receipt.get("exec_plan_line")
        or ""
    )
    finish_receipt = package_reverse_flow_focused_validation_continue_finish_receipt(
        pipeline=pipeline,
        post_operator_card=operator_card,
        progress_transition=progress_transition,
        exec_receipt=exec_receipt,
        supervisor_wake=(
            result.get("supervisor_wake")
            if isinstance(result.get("supervisor_wake"), dict)
            else inventory_wake
        ),
        focused_validation_status=str(result.get("focused_validation_status") or "")
        or None,
        activation_external_handoff_status=str(
            result.get("activation_external_handoff_status") or ""
        )
        or None,
        activation_external_acceptance_status=str(
            result.get("activation_external_acceptance_status") or ""
        )
        or None,
        residual_hold_active=bool(result.get("residual_hold_active", True)),
        continue_plan_mode=str(result.get("continue_plan_mode") or "") or None,
        supervisor_next_action=str(result.get("supervisor_next_action") or "")
        or None,
        executed=False,
        recorded=False,
    )
    result["finish_receipt"] = finish_receipt
    result["finish_line"] = str(finish_receipt.get("finish_line") or "")
    result["continue_finished"] = bool(finish_receipt.get("continue_finished"))
    result["residual_queue_ready"] = bool(finish_receipt.get("residual_queue_ready"))
    residual_open = package_reverse_flow_focused_validation_continue_residual_open(
        pipeline=pipeline,
        finish_receipt=finish_receipt,
    )
    result["residual_open"] = residual_open
    result["residual_open_line"] = str(residual_open.get("residual_open_line") or "")
    result["residual_open_ready"] = bool(residual_open.get("residual_open"))
    result["residual_adjacent_count"] = int(
        residual_open.get("residual_adjacent_count") or 0
    )
    residual_entry = package_reverse_flow_focused_validation_continue_residual_entry(
        pipeline=pipeline,
        residual_open=residual_open,
    )
    result["residual_entry"] = residual_entry
    result["residual_entry_line"] = str(
        residual_entry.get("residual_entry_line") or ""
    )
    result["residual_entry_ready"] = bool(residual_entry.get("residual_entry"))
    result["selected_residual_proposal_id"] = str(
        residual_entry.get("selected_residual_proposal_id") or ""
    )
    residual_follow = package_reverse_flow_focused_validation_continue_residual_follow(
        pipeline=pipeline,
        residual_entry=residual_entry,
        residual_open=residual_open,
    )
    result["residual_follow"] = residual_follow
    result["residual_follow_line"] = str(
        residual_follow.get("residual_follow_line") or ""
    )
    result["residual_follow_ready"] = bool(residual_follow.get("residual_follow"))
    result["residual_follow_action"] = str(
        residual_follow.get("residual_follow_action") or "wait_for_reverse_flow"
    )
    result["call_residual_comparison"] = bool(
        residual_follow.get("call_residual_comparison")
    )
    residual_comparison = (
        package_reverse_flow_focused_validation_continue_residual_comparison(
            pipeline=pipeline,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_comparison"] = residual_comparison
    result["residual_comparison_line"] = str(
        residual_comparison.get("residual_comparison_line") or ""
    )
    result["residual_comparison_ready"] = bool(
        residual_comparison.get("residual_comparison")
    )
    result["residual_comparison_action"] = str(
        residual_comparison.get("residual_comparison_action") or "wait_for_reverse_flow"
    )
    result["call_residual_unlocked_apply"] = bool(
        residual_comparison.get("call_residual_unlocked_apply")
    )
    residual_unlocked_apply = (
        package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
            pipeline=pipeline,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_unlocked_apply"] = residual_unlocked_apply
    result["residual_unlocked_apply_line"] = str(
        residual_unlocked_apply.get("residual_unlocked_apply_line") or ""
    )
    result["residual_unlocked_apply_ready"] = bool(
        residual_unlocked_apply.get("residual_unlocked_apply")
    )
    result["residual_unlocked_apply_action"] = str(
        residual_unlocked_apply.get("residual_unlocked_apply_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_focused_validation"] = bool(
        residual_unlocked_apply.get("call_residual_focused_validation")
    )
    residual_focused_validation = (
        package_reverse_flow_focused_validation_continue_residual_focused_validation(
            pipeline=pipeline,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_focused_validation"] = residual_focused_validation
    result["residual_focused_validation_line"] = str(
        residual_focused_validation.get("residual_focused_validation_line") or ""
    )
    result["residual_focused_validation_ready"] = bool(
        residual_focused_validation.get("residual_focused_validation")
    )
    result["residual_focused_validation_action"] = str(
        residual_focused_validation.get("residual_focused_validation_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_handoff"] = bool(
        residual_focused_validation.get("call_residual_handoff")
    )
    residual_handoff = (
        package_reverse_flow_focused_validation_continue_residual_handoff(
            pipeline=pipeline,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_handoff"] = residual_handoff
    result["residual_handoff_line"] = str(
        residual_handoff.get("residual_handoff_line") or ""
    )
    result["residual_handoff_ready"] = bool(
        residual_handoff.get("residual_handoff")
    )
    result["residual_handoff_action"] = str(
        residual_handoff.get("residual_handoff_action")
        or "wait_for_reverse_flow"
    )
    result["call_residual_acceptance"] = bool(
        residual_handoff.get("call_residual_acceptance")
    )
    result["operator_card_helper"] = (
        "package_reverse_flow_focused_validation_continue_operator_card"
    )
    result["progress_transition_helper"] = (
        "package_reverse_flow_focused_validation_continue_progress_transition"
    )
    result["exec_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_exec_receipt"
    )
    result["finish_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_finish_receipt"
    )
    result["residual_open_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_open"
    )
    result["residual_entry_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_entry"
    )
    result["residual_follow_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_follow"
    )
    result["residual_comparison_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_comparison"
    )
    result["residual_unlocked_apply_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
    )
    result["residual_focused_validation_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_focused_validation"
    )
    result["residual_handoff_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_handoff"
    )
    residual_acceptance = (
        package_reverse_flow_focused_validation_continue_residual_acceptance(
            pipeline=pipeline,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_acceptance"] = residual_acceptance
    result["residual_acceptance_line"] = str(
        residual_acceptance.get("residual_acceptance_line") or ""
    )
    result["residual_acceptance_ready"] = bool(
        residual_acceptance.get("residual_acceptance")
    )
    result["residual_acceptance_action"] = str(
        residual_acceptance.get("residual_acceptance_action")
        or "wait_for_reverse_flow"
    )
    result["residual_acceptance_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_acceptance"
    )
    residual_cascade = (
        package_reverse_flow_focused_validation_continue_residual_cascade(
            pipeline=pipeline,
            residual_acceptance=residual_acceptance,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    result["residual_cascade"] = residual_cascade
    result["residual_cascade_line"] = str(
        residual_cascade.get("residual_cascade_line") or ""
    )
    result["residual_cascade_ready"] = bool(
        residual_cascade.get("residual_cascade")
    )
    result["residual_cascade_action"] = str(
        residual_cascade.get("residual_cascade_action")
        or "wait_for_reverse_flow"
    )
    result["residual_cascade_progress_label"] = str(
        residual_cascade.get("residual_cascade_progress_label") or "0/8"
    )
    result["residual_cascade_blocked_at"] = str(
        residual_cascade.get("residual_cascade_blocked_at") or "open"
    )
    result["residual_cascade_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_cascade"
    )
    continue_cascade = package_reverse_flow_focused_validation_continue_cascade(
        pipeline=pipeline,
        operator_card=operator_card,
        residual_cascade=residual_cascade,
        follow_through=follow_through,
        finish_receipt=finish_receipt,
    )
    result["continue_cascade"] = continue_cascade
    result["continue_cascade_line"] = str(
        continue_cascade.get("continue_cascade_line") or ""
    )
    result["continue_cascade_ready"] = bool(
        continue_cascade.get("continue_cascade")
    )
    result["continue_cascade_action"] = str(
        continue_cascade.get("continue_cascade_action") or "wait_for_reverse_flow"
    )
    result["continue_cascade_reverse_progress_label"] = str(
        continue_cascade.get("reverse_progress_label") or "0/0"
    )
    result["continue_cascade_residual_progress_label"] = str(
        continue_cascade.get("residual_progress_label") or "0/8"
    )
    result["continue_cascade_residual_blocked_at"] = str(
        continue_cascade.get("residual_cascade_blocked_at") or "open"
    )
    result["continue_cascade_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade"
    )
    result["pre_continue_cascade"] = pre_continue_cascade
    cascade_transition = (
        package_reverse_flow_focused_validation_continue_cascade_transition(
            pre_cascade=pre_continue_cascade,
            post_cascade=continue_cascade,
            executed=False,
            recorded=False,
        )
    )
    result["continue_cascade_transition"] = cascade_transition
    result["continue_cascade_transition_line"] = str(
        cascade_transition.get("continue_cascade_transition_line") or ""
    )
    result["continue_cascade_transition_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_transition"
    )
    result["cascade_advanced"] = bool(cascade_transition.get("cascade_advanced"))
    result["reverse_progress_transition"] = str(
        cascade_transition.get("reverse_progress_transition") or "0/0→0/0"
    )
    result["residual_progress_transition"] = str(
        cascade_transition.get("residual_progress_transition") or "0/8→0/8"
    )
    cascade_wake = package_reverse_flow_focused_validation_continue_cascade_wake(
        cascade_transition=cascade_transition,
        exec_receipt=(
            result.get("exec_receipt")
            if isinstance(result.get("exec_receipt"), dict)
            else None
        ),
        finish_receipt=finish_receipt if isinstance(finish_receipt, dict) else None,
        residual_open=residual_open if isinstance(residual_open, dict) else None,
        follow_through=follow_through if isinstance(follow_through, dict) else None,
    )
    result["continue_cascade_wake"] = cascade_wake
    result["continue_cascade_wake_line"] = str(
        cascade_wake.get("continue_cascade_wake_line") or ""
    )
    result["continue_cascade_wake_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake"
    )
    result["wake_outcome"] = str(cascade_wake.get("wake_outcome") or "identity")
    cascade_wake_route = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route(
            cascade_wake=cascade_wake,
        )
    )
    result["continue_cascade_wake_route"] = cascade_wake_route
    result["continue_cascade_wake_route_line"] = str(
        cascade_wake_route.get("continue_cascade_wake_route_line") or ""
    )
    result["continue_cascade_wake_route_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route"
    )
    result["wake_route_action"] = str(
        cascade_wake_route.get("route_action") or "inventory_only"
    )
    result["wake_route_call_execute"] = bool(
        cascade_wake_route.get("call_follow_with_execute")
    )
    # Inventory-only apply keeps pre_route == post_route so the apply line is
    # legible before execute without inventing a false route transition.
    cascade_wake_route_apply = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
            pre_route=cascade_wake_route,
            post_route=cascade_wake_route,
            executed=False,
            recorded=False,
        )
    )
    result["pre_continue_cascade_wake_route"] = cascade_wake_route
    result["continue_cascade_wake_route_apply"] = cascade_wake_route_apply
    result["continue_cascade_wake_route_apply_line"] = str(
        cascade_wake_route_apply.get("continue_cascade_wake_route_apply_line") or ""
    )
    result["continue_cascade_wake_route_apply_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    )
    result["wake_route_action_transition"] = str(
        cascade_wake_route_apply.get("route_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_advanced"] = bool(
        cascade_wake_route_apply.get("route_advanced")
    )
    cascade_wake_route_apply_follow = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
            route_apply=cascade_wake_route_apply,
            post_route=cascade_wake_route,
        )
    )
    result["continue_cascade_wake_route_apply_follow"] = cascade_wake_route_apply_follow
    result["continue_cascade_wake_route_apply_follow_line"] = str(
        cascade_wake_route_apply_follow.get(
            "continue_cascade_wake_route_apply_follow_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
    )
    result["wake_route_apply_follow_action"] = str(
        cascade_wake_route_apply_follow.get("follow_action") or "inventory_only"
    )
    result["wake_route_apply_follow_helper"] = str(
        cascade_wake_route_apply_follow.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_call_execute"] = bool(
        cascade_wake_route_apply_follow.get("call_follow_with_execute")
    )
    cascade_wake_route_apply_follow_pin = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
            apply_follow=cascade_wake_route_apply_follow,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin"] = (
        cascade_wake_route_apply_follow_pin
    )
    result["continue_cascade_wake_route_apply_follow_pin_line"] = str(
        cascade_wake_route_apply_follow_pin.get(
            "continue_cascade_wake_route_apply_follow_pin_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
    )
    result["wake_route_apply_follow_pin_action"] = str(
        cascade_wake_route_apply_follow_pin.get("pin_action") or "inventory_only"
    )
    result["wake_route_apply_follow_pin_mode"] = str(
        cascade_wake_route_apply_follow_pin.get("pin_mode") or "inventory_only"
    )
    result["wake_route_apply_follow_pin_helper"] = str(
        cascade_wake_route_apply_follow_pin.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin.get("call_pin_with_execute")
    )
    result["wake_route_apply_follow_pin_ready"] = bool(
        cascade_wake_route_apply_follow_pin.get("pin_ready")
    )
    # Inventory-only pin_call keeps pre_pin == post_pin so the call line is
    # legible before execute without inventing a false pin transition.
    cascade_wake_route_apply_follow_pin_call = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
            pre_pin=cascade_wake_route_apply_follow_pin,
            post_pin=cascade_wake_route_apply_follow_pin,
            executed=False,
            recorded=False,
        )
    )
    result["pre_continue_cascade_wake_route_apply_follow_pin"] = (
        cascade_wake_route_apply_follow_pin
    )
    result["continue_cascade_wake_route_apply_follow_pin_call"] = (
        cascade_wake_route_apply_follow_pin_call
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_line"] = str(
        cascade_wake_route_apply_follow_pin_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
    )
    result["wake_route_apply_follow_pin_action_transition"] = str(
        cascade_wake_route_apply_follow_pin_call.get("pin_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_mode_transition"] = str(
        cascade_wake_route_apply_follow_pin_call.get("pin_mode_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_advanced"] = bool(
        cascade_wake_route_apply_follow_pin_call.get("pin_advanced")
    )
    # Inventory-only pin_call_next seals identity pin_call into a next-invoke
    # recipe without inventing a false advanced transition.
    cascade_wake_route_apply_follow_pin_call_next = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
            pin_call=cascade_wake_route_apply_follow_pin_call,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next"] = (
        cascade_wake_route_apply_follow_pin_call_next
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_line"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
    )
    result["wake_route_apply_follow_pin_call_next_action"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("next_action")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_invoke"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("next_invoke")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_helper"] = str(
        cascade_wake_route_apply_follow_pin_call_next.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_next_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin_call_next.get("call_next_with_execute")
    )
    result["wake_route_apply_follow_pin_call_next_ready"] = bool(
        cascade_wake_route_apply_follow_pin_call_next.get("next_ready")
    )
    # Inventory-only next_call keeps pre_next == post_next so the call line is
    # legible before execute without inventing a false next advanced transition.
    cascade_wake_route_apply_follow_pin_call_next_call = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call(
            pre_next=cascade_wake_route_apply_follow_pin_call_next,
            post_next=cascade_wake_route_apply_follow_pin_call_next,
            executed=False,
            recorded=False,
        )
    )
    result["pre_continue_cascade_wake_route_apply_follow_pin_call_next"] = (
        cascade_wake_route_apply_follow_pin_call_next
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call"] = (
        cascade_wake_route_apply_follow_pin_call_next_call
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_line"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
        )
        or ""
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_helper"] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
    )
    result["wake_route_apply_follow_pin_call_next_action_transition"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_action_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_invoke_transition"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_invoke_transition")
        or "inventory_only→inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_advanced"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call.get("next_advanced")
    )
    # Inventory-only next_call_follow maps identity next_call into a durable
    # follow policy without inventing a false advanced transition.
    cascade_wake_route_apply_follow_pin_call_next_call_follow = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow(
            next_call=cascade_wake_route_apply_follow_pin_call_next_call,
        )
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"] = (
        cascade_wake_route_apply_follow_pin_call_next_call_follow
    )
    result["continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"] = (
        str(
            cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
                "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
            )
            or ""
        )
    )
    result[
        "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_action"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_action")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_mode"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_mode")
        or "inventory_only"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_helper"] = str(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "preferred_helper"
        )
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_call_execute"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "call_follow_with_execute"
        )
    )
    result["wake_route_apply_follow_pin_call_next_call_follow_ready"] = bool(
        cascade_wake_route_apply_follow_pin_call_next_call_follow.get("follow_ready")
    )
    result["follow_through_helper"] = (
        "follow_reverse_flow_focused_validation_continue_dispatch"
    )
    result["resolve_helper"] = (
        "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
    )
    helpers = list(result.get("record_helpers") or [])
    for name in (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply",
        "package_reverse_flow_focused_validation_continue_cascade_wake_route",
        "package_reverse_flow_focused_validation_continue_cascade_wake",
        "package_reverse_flow_focused_validation_continue_cascade_transition",
        "package_reverse_flow_focused_validation_continue_cascade",
        "package_reverse_flow_focused_validation_continue_residual_cascade",
        "package_reverse_flow_focused_validation_continue_residual_acceptance",
        "package_reverse_flow_focused_validation_continue_residual_handoff",
        "package_reverse_flow_focused_validation_continue_residual_focused_validation",
        "package_reverse_flow_focused_validation_continue_residual_unlocked_apply",
        "package_reverse_flow_focused_validation_continue_residual_comparison",
        "package_reverse_flow_focused_validation_continue_residual_follow",
        "package_reverse_flow_focused_validation_continue_residual_entry",
        "package_reverse_flow_focused_validation_continue_residual_open",
        "package_reverse_flow_focused_validation_continue_finish_receipt",
        "package_reverse_flow_focused_validation_continue_exec_receipt",
        "package_reverse_flow_focused_validation_continue_progress_transition",
        "package_reverse_flow_focused_validation_continue_operator_card",
        "follow_reverse_flow_focused_validation_continue_dispatch",
        "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
    ):
        if name not in helpers:
            helpers.insert(0, name)
    result["record_helpers"] = helpers
    return result


def build_skill_route_discovery_focused_local_test_validation(
    *,
    unlocked_local_test_lane_apply: dict[str, Any] | None = None,
    command_results: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Record body-free focused local test validation for the unlocked reverse-flow lane.

    Consumes a ready ``skill_route_discovery_unlocked_local_test_lane_apply`` packet
    and packages the supervisor next action
    ``run_focused_local_test_validation_then_keep_activation_external`` into one
    operator-visible result surface. Commands are exported as hashes only (no raw
    evidence URLs or upstream bodies). Optional ``command_results`` rows
    (``command_hash`` + ``passed``) mark the validation ``passed`` or ``failed``.
    Activation, push, promotion, provider launch, remote apply, external skill
    execution, and kernel restart stay denied; activation remains external.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    unlocked_apply = (
        unlocked_local_test_lane_apply
        if isinstance(unlocked_local_test_lane_apply, dict)
        else {}
    )
    unlocked_status = str(unlocked_apply.get("status") or "")
    selected_proposal_id = str(
        unlocked_apply.get("selected_proposal_id")
        or unlocked_apply.get("proposal_id")
        or "prop-skill-reverse-flow-test-lane"
    )
    selected_lane = str(unlocked_apply.get("selected_local_lane") or "")
    unlocked_lanes = [
        lane
        for lane in list(unlocked_apply.get("unlocked_local_lanes") or [])
        if lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
    ]
    profiles = [str(profile) for profile in list(unlocked_apply.get("route_profiles") or [])]
    skill_first = bool(unlocked_apply.get("skill_route_discovery_first"))
    is_reverse_flow = "codex_workflow_gate" in profiles and skill_first
    focused = (
        unlocked_apply.get("focused_validation")
        if isinstance(unlocked_apply.get("focused_validation"), dict)
        else {}
    )
    commands = [
        str(cmd)
        for cmd in list(
            focused.get("commands") or unlocked_apply.get("required_validation") or []
        )
        if str(cmd).strip()
    ]
    command_hashes = [
        str(item)
        for item in list(focused.get("command_hashes") or [])
        if str(item).strip()
    ]
    if not command_hashes and commands:
        command_hashes = [
            hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
            for command in commands
        ]
    expected_hash_set = set(command_hashes)

    result_rows = normalize_skill_route_discovery_focused_validation_command_results(
        command_results,
        expected_command_hashes=command_hashes,
    )

    results_cover_expected = bool(expected_hash_set) and expected_hash_set.issubset(
        {row["command_hash"] for row in result_rows}
    )
    all_expected_passed = results_cover_expected and all(
        row["passed"] for row in result_rows if row["command_hash"] in expected_hash_set
    )
    any_expected_failed = any(
        (not row["passed"]) and row["command_hash"] in expected_hash_set
        for row in result_rows
    ) if result_rows else False

    apply_ready = (
        unlocked_status == "ready"
        and is_reverse_flow
        and selected_lane == "test"
        and "test" in unlocked_lanes
        and bool(unlocked_apply.get("activation_external_only", True))
        and unlocked_apply.get("supervisor_activation_allowed") is not True
        and unlocked_apply.get("runtime_action") == "none"
        and unlocked_apply.get("external_skill_execution_allowed") is not True
        and unlocked_apply.get("provider_launch_allowed") is not True
        and unlocked_apply.get("remote_apply_allowed") is not True
        and unlocked_apply.get("body_free") is not False
    )

    if unlocked_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_unlocked_local_test_lane_apply_for_focused_validation"
        supervisor_next_action = "wait_for_skill_route_discovery_unlocked_local_test_lane_apply"
    elif unlocked_status.startswith("blocked") or unlocked_status == "deferred_adjacent_harness_eval":
        status = "blocked_until_unlocked_local_test_lane_apply"
        decision = "hold_focused_local_test_validation_until_unlocked_apply_ready"
        supervisor_next_action = "repair_skill_route_discovery_unlocked_local_test_lane_apply"
    elif not apply_ready:
        status = "blocked_until_unlocked_local_test_lane_apply"
        decision = "hold_focused_local_test_validation_until_reverse_flow_test_lane_unlocked"
        supervisor_next_action = "repair_skill_route_discovery_unlocked_local_test_lane_apply"
    elif any_expected_failed:
        status = "failed"
        decision = "repair_focused_local_test_validation_before_activation_review"
        supervisor_next_action = "repair_failed_focused_local_test_validation_commands"
    elif all_expected_passed:
        status = "passed"
        decision = "record_focused_local_test_validation_pass_and_keep_activation_external"
        supervisor_next_action = "keep_activation_external_after_focused_local_test_validation"
    else:
        status = "ready"
        # Partial body-free coverage: promote continue to record remaining hashes
        # only. Do not re-advertise a full focused validation re-run that would
        # re-execute already-covered hashes or dilute reverse-flow-first next.
        partial_ready = bool(result_rows) and not results_cover_expected
        if partial_ready:
            decision = (
                "record_remaining_focused_validation_command_hashes_before_activation_external"
            )
            supervisor_next_action = (
                REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RECORD_REMAINING
            )
        else:
            decision = "run_focused_local_test_validation_with_body_free_command_hashes"
            supervisor_next_action = REVERSE_FLOW_FOCUSED_VALIDATION_SUPERVISOR_NEXT_RUN

    export_commands = commands if status in {"ready", "passed", "failed"} else []
    export_hashes = command_hashes if status in {"ready", "passed", "failed"} else []
    # Keep body-free partial command-hash rows on ready so supervisors can
    # accumulate coverage across wakes without re-exporting command text/stdout.
    # Full close still requires results_cover_expected; residual export stays held.
    export_results = result_rows if status in {"ready", "passed", "failed"} else []
    missing_hashes = (
        missing_skill_route_discovery_focused_validation_command_hashes(
            expected_command_hashes=export_hashes,
            command_results=export_results,
        )
        if status in {"ready", "failed"}
        else []
    )
    recorded_hashes = (
        recorded_skill_route_discovery_focused_validation_command_hashes(
            expected_command_hashes=export_hashes,
            command_results=export_results,
        )
        if status in {"ready", "passed", "failed"}
        else []
    )
    pending_commands = (
        pending_skill_route_discovery_focused_validation_commands(
            commands=export_commands,
            command_hashes=export_hashes,
            missing_command_hashes=missing_hashes,
        )
        if status == "ready"
        else []
    )
    pending_work_units = (
        pending_skill_route_discovery_focused_validation_work_units(
            commands=export_commands,
            command_hashes=export_hashes,
            missing_command_hashes=missing_hashes,
        )
        if status == "ready"
        else []
    )
    recorded = status in {"passed", "failed"}
    residual_hold_until_recorded = status == "ready" and not recorded
    residual_hold_active = residual_hold_until_recorded or status == "failed"
    # Fortress/Hy3 adjacent IDs are known for later residual stages, but reverse-flow
    # focused validation must not pre-export them while residual work is held
    # (ready/unrecorded or failed). Export resumes on recorded pass.
    adjacent_ids = [
        str(item).strip()
        for item in list(unlocked_apply.get("adjacent_general_agent_proposal_ids") or [])
        if str(item).strip()
    ]
    export_adjacent_ids = [] if residual_hold_active else adjacent_ids

    focused_validation_payload = {
        "status": status,
        "lane": "test",
        "required": True,
        "commands": export_commands,
        "command_hashes": export_hashes,
        "command_results": export_results,
        "expected_command_count": len(export_hashes),
        "recorded_result_count": len(export_results),
        "recorded_command_hashes": recorded_hashes,
        "recorded_command_hash_count": len(recorded_hashes),
        "missing_command_hashes": missing_hashes,
        "missing_command_hash_count": len(missing_hashes),
        "pending_commands": pending_commands,
        "pending_command_count": len(pending_commands),
        "pending_work_units": pending_work_units,
        "pending_work_unit_count": len(pending_work_units),
        "unit_test_signal": status in {"ready", "passed", "failed"},
        "coverage_required": False,
        "results_cover_expected": bool(results_cover_expected and export_results),
        "all_expected_passed": bool(all_expected_passed and export_results),
        "partial_results_recorded": bool(
            status == "ready" and export_results and not results_cover_expected
        ),
        "recorded": recorded,
    }
    packet = {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_focused_local_test_validation",
        "proposal_track": "prop-skill-reverse-flow-focused-test-validation",
        "legacy_proposal_track": "prop-skill-reverse-flow-test-lane",
        "companion_tracks": [
            "prop-skill-reverse-flow-test-lane",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-rnskill-docs",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_local_skill_route_validation_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
        ],
        "unlocked_local_test_lane_apply_status": unlocked_status or "none",
        "unlocked_local_test_lane_apply_decision": str(unlocked_apply.get("decision") or ""),
        "local_apply_completion_status": str(
            unlocked_apply.get("local_apply_completion_status") or "none"
        ),
        "reverse_flow_test_validation_lane_status": str(
            unlocked_apply.get("reverse_flow_test_validation_lane_status") or "none"
        ),
        "local_comparison_passed": bool(unlocked_apply.get("local_comparison_passed")),
        "theme_complete": bool(unlocked_apply.get("theme_complete")),
        "route_class": str(unlocked_apply.get("route_class") or "none"),
        "route_profiles": profiles,
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": selected_lane if selected_lane else "none",
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked_lanes if status in {"ready", "passed", "failed"} else [],
        "focused_validation": focused_validation_payload,
        "local_validation_required": True,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": list(
            unlocked_apply.get("retained_boundary_proposal_ids") or []
        ),
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "residual_adjacent_ids_held_until_recorded": residual_hold_active and bool(adjacent_ids),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": bool(
            unlocked_apply.get("general_agent_isolation_passed", True)
        ),
        "privacy_isolation_passed": bool(unlocked_apply.get("privacy_isolation_passed", True)),
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(theme.get("theme_id") or unlocked_apply.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(unlocked_apply.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": export_commands,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "focused_validation_recorded": recorded,
        # Residual fortress/Hy3 stages stay held until reverse-flow focused
        # validation command-hash results are recorded/closed. Operators must not
        # jump to residual harness-eval while this surface is ready/unrecorded or
        # failed (repair required). Residual selection also stays suppressed on
        # residual stage packets until residual work is residual-active. Adjacent
        # fortress proposal IDs are likewise not exported while this hold is active.
        "residual_adjacent_hold_until_recorded": residual_hold_until_recorded,
        "residual_adjacent_hold_active": residual_hold_active,
        "residual_adjacent_hold_reason": (
            "blocked_until_focused_validation_recorded_and_activation_external_accepted"
            if residual_hold_until_recorded
            else "blocked_until_focused_validation_repaired"
            if status == "failed"
            else "not_held"
        ),
        "record_helpers": [
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
            "package_reverse_flow_focused_validation_continue_dispatch_inventory",
            "run_reverse_flow_focused_validation_continue_pending_work_units",
            "execute_reverse_flow_focused_validation_continue_run_plan",
            "build_reverse_flow_focused_validation_continue_run_plan",
            "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
            "reverse_flow_focused_validation_continue_local_command_allowed",
            "record_reverse_flow_focused_validation_continue_outcomes",
            "materialize_reverse_flow_focused_validation_continue_record_rows",
            "merge_skill_route_discovery_focused_validation_command_results",
            "resolve_reverse_flow_focused_validation_continue_supervisor_next",
            "build_reverse_flow_focused_validation_continue_plan",
            "pending_skill_route_discovery_focused_validation_work_units",
        ]
        if status in {"ready", "passed", "failed"}
        else [],
        "activation_external_handoff_after_record": (
            "skill_route_discovery_focused_validation_activation_external_handoff"
            if status in {"ready", "passed"}
            else "none"
        ),
    }
    # Attach continue plan after residual hold fields exist so zero-row and
    # partial wakes share one inspectable pending-work surface.
    continue_plan = build_reverse_flow_focused_validation_continue_plan(
        focused_local_test_validation=packet,
    )
    packet["continue_plan"] = continue_plan
    packet["focused_validation"] = {
        **focused_validation_payload,
        "continue_plan": continue_plan,
    }
    return packet


def build_skill_route_discovery_focused_validation_activation_external_handoff(
    *,
    focused_local_test_validation: dict[str, Any] | None = None,
    unlocked_local_test_lane_apply: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Package activation-external handoff after reverse-flow focused validation.

    After supervisors record body-free command-hash results on
    ``skill_route_discovery_focused_local_test_validation``, this surface turns
    ``keep_activation_external_after_focused_local_test_validation`` into one
    operator-visible packet. A recorded pass keeps activation, push, promotion,
    provider launch, remote apply, external skill execution, and kernel restart
    denied. Residual fortress-style adjacent rows stay noted without inheriting
    skill unlocks. Failed or unrecorded focused validation blocks the handoff.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    unlocked_apply = (
        unlocked_local_test_lane_apply
        if isinstance(unlocked_local_test_lane_apply, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    focused_status = str(focused.get("status") or "")
    focused_validation = (
        focused.get("focused_validation")
        if isinstance(focused.get("focused_validation"), dict)
        else {}
    )
    recorded = bool(
        focused.get("focused_validation_recorded")
        or focused_validation.get("recorded")
        or focused_status in {"passed", "failed"}
    )
    results_cover_expected = bool(focused_validation.get("results_cover_expected"))
    all_expected_passed = bool(focused_validation.get("all_expected_passed"))
    selected_proposal_id = str(
        focused.get("selected_proposal_id")
        or focused.get("proposal_id")
        or unlocked_apply.get("selected_proposal_id")
        or unlocked_apply.get("proposal_id")
        or "prop-skill-reverse-flow-focused-test-validation"
    )
    profiles = [
        str(profile)
        for profile in list(
            focused.get("route_profiles") or unlocked_apply.get("route_profiles") or []
        )
    ]
    skill_first = bool(
        focused.get("skill_route_discovery_first", unlocked_apply.get("skill_route_discovery_first"))
    )
    selected_lane = str(
        focused.get("selected_local_lane") or unlocked_apply.get("selected_local_lane") or ""
    )
    unlocked_lanes = [
        lane
        for lane in list(
            focused.get("unlocked_local_lanes")
            or unlocked_apply.get("unlocked_local_lanes")
            or []
        )
        if lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
    ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(focused.get("retained_boundary_proposal_ids") or [])
            if str(item).strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(focused.get("adjacent_general_agent_proposal_ids") or [])
            if str(item).strip()
        }
    )
    residual_adjacent_available = bool(adjacent_ids)
    result_rows = [
        row
        for row in list(focused_validation.get("command_results") or [])
        if isinstance(row, dict)
    ]
    body_free_results = all(
        set(row.keys()) <= {"command_hash", "passed", "in_expected_set"} for row in result_rows
    ) if result_rows else True
    command_hashes = [
        str(item)
        for item in list(focused_validation.get("command_hashes") or [])
        if str(item).strip()
    ]

    if focused_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_focused_local_test_validation_for_activation_external_handoff"
        supervisor_next_action = "wait_for_skill_route_discovery_focused_local_test_validation"
    elif focused_status.startswith("blocked"):
        status = "blocked_until_focused_validation_ready"
        decision = "hold_activation_external_handoff_until_focused_validation_ready"
        supervisor_next_action = "repair_skill_route_discovery_focused_local_test_validation"
    elif focused_status == "ready" or not recorded:
        status = "blocked_until_focused_validation_recorded"
        decision = "hold_activation_external_handoff_until_command_hash_results_recorded"
        # Propagate partial-continue supervisor_next so blocked handoff does not
        # re-advertise a full reverse-flow focused re-run after partial coverage.
        supervisor_next_action = (
            resolve_reverse_flow_focused_validation_continue_supervisor_next(
                focused_local_test_validation=focused,
            )
        )
    elif focused_status == "failed" or (recorded and not all_expected_passed):
        status = "blocked_until_focused_validation_repaired"
        decision = "hold_activation_external_handoff_until_focused_validation_pass"
        supervisor_next_action = "repair_failed_focused_local_test_validation_commands"
    elif (
        focused_status == "passed"
        and recorded
        and results_cover_expected
        and all_expected_passed
        and body_free_results
        and bool(focused.get("activation_external_only", True))
        and focused.get("supervisor_activation_allowed") is not True
        and focused.get("runtime_action") == "none"
        and focused.get("external_skill_execution_allowed") is not True
        and focused.get("provider_launch_allowed") is not True
        and focused.get("remote_apply_allowed") is not True
        and focused.get("push_or_promotion_allowed") is not True
        and focused.get("kernel_restart_allowed") is not True
    ):
        status = "ready"
        decision = "package_activation_external_handoff_after_focused_validation_pass"
        supervisor_next_action = (
            "keep_activation_external_after_focused_local_test_validation"
        )
    else:
        status = "blocked_until_focused_validation_pass"
        decision = "hold_activation_external_handoff_until_body_free_pass_recorded"
        supervisor_next_action = (
            resolve_reverse_flow_focused_validation_continue_supervisor_next(
                focused_local_test_validation=focused,
            )
        )

    export_hashes = command_hashes if status in {"ready", "blocked_until_focused_validation_repaired"} else []
    export_results = result_rows if status in {"ready", "blocked_until_focused_validation_repaired"} else []
    # Residual fortress/Hy3 adjacent IDs and availability only export when the
    # reverse-flow activation-external handoff is ready (recorded pass). While
    # blocked waiting on record/repair, do not pre-advertise residual work.
    export_adjacent_ids = adjacent_ids if status == "ready" else []
    export_residual_available = bool(export_adjacent_ids)

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_focused_validation_activation_external_handoff",
        "proposal_track": "prop-skill-reverse-flow-focused-test-validation",
        "legacy_proposal_track": "prop-skill-reverse-flow-test-lane",
        "companion_tracks": [
            "prop-skill-reverse-flow-focused-test-validation",
            "prop-skill-reverse-flow-test-lane",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_local_skill_route_validation_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
        ],
        "focused_local_test_validation_status": focused_status or "none",
        "focused_local_test_validation_decision": str(focused.get("decision") or ""),
        "focused_local_test_validation_recorded": recorded,
        "focused_validation_results_cover_expected": results_cover_expected if recorded else False,
        "focused_validation_all_expected_passed": all_expected_passed if recorded else False,
        "unlocked_local_test_lane_apply_status": str(unlocked_apply.get("status") or "none"),
        "local_apply_completion_status": str(
            focused.get("local_apply_completion_status")
            or unlocked_apply.get("local_apply_completion_status")
            or "none"
        ),
        "reverse_flow_test_validation_lane_status": str(
            focused.get("reverse_flow_test_validation_lane_status")
            or unlocked_apply.get("reverse_flow_test_validation_lane_status")
            or "none"
        ),
        "local_comparison_passed": bool(
            focused.get("local_comparison_passed", unlocked_apply.get("local_comparison_passed"))
        ),
        "theme_complete": bool(
            focused.get("theme_complete", unlocked_apply.get("theme_complete"))
        ),
        "route_class": str(focused.get("route_class") or unlocked_apply.get("route_class") or "none"),
        "route_profiles": profiles,
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": selected_lane if selected_lane else "none",
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked_lanes if status == "ready" else [],
        "focused_validation": {
            "status": focused_status or "none",
            "lane": "test",
            "required": True,
            "command_hashes": export_hashes,
            "command_results": export_results,
            "expected_command_count": len(export_hashes),
            "recorded_result_count": len(export_results),
            "results_cover_expected": results_cover_expected if recorded else False,
            "all_expected_passed": all_expected_passed if recorded else False,
            "recorded": recorded,
            "body_free": body_free_results,
            # Commands intentionally omitted: activation-external handoff is hash-only.
            "commands_exported": False,
        },
        "local_validation_required": True,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids,
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "residual_adjacent_harness_eval_available": export_residual_available,
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_available else "none"
        ),
        "residual_adjacent_export_held_until_ready": status != "ready"
        and bool(residual_adjacent_available),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": bool(
            focused.get("general_agent_isolation_passed", True)
        ),
        "privacy_isolation_passed": bool(focused.get("privacy_isolation_passed", True)),
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or focused.get("theme_id")
            or unlocked_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (focused.get("theme_pass") or {}).get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (focused.get("theme_pass") or {}).get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (focused.get("theme_pass") or {}).get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(focused.get("source_digest") or unlocked_apply.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_local_test_validation",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_validation_activation_external",
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_focused_validation_activation_external_acceptance(
    *,
    focused_validation_activation_external_handoff: dict[str, Any] | None = None,
    focused_local_test_validation: dict[str, Any] | None = None,
    unlocked_local_test_lane_apply: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Accept a ready activation-external handoff as the terminal reverse-flow package.

    After ``skill_route_discovery_focused_validation_activation_external_handoff``
    is ``ready`` from a recorded focused validation pass, this surface packages
    operator acceptance of
    ``keep_activation_external_after_focused_local_test_validation`` without
    enabling push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Residual fortress/Hy3 adjacent rows may be
    noted for ``agent_harness_eval_cluster_local_apply`` without skill unlock
    inheritance.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    handoff = (
        focused_validation_activation_external_handoff
        if isinstance(focused_validation_activation_external_handoff, dict)
        else {}
    )
    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    unlocked_apply = (
        unlocked_local_test_lane_apply
        if isinstance(unlocked_local_test_lane_apply, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    handoff_status = str(handoff.get("status") or "")
    focused_status = str(focused.get("status") or handoff.get("focused_local_test_validation_status") or "")
    focused_validation = (
        focused.get("focused_validation")
        if isinstance(focused.get("focused_validation"), dict)
        else (
            handoff.get("focused_validation")
            if isinstance(handoff.get("focused_validation"), dict)
            else {}
        )
    )
    recorded = bool(
        focused.get("focused_validation_recorded")
        or focused_validation.get("recorded")
        or handoff.get("focused_local_test_validation_recorded")
        or focused_status in {"passed", "failed"}
    )
    results_cover_expected = bool(
        focused_validation.get("results_cover_expected")
        or handoff.get("focused_validation_results_cover_expected")
    )
    all_expected_passed = bool(
        focused_validation.get("all_expected_passed")
        or handoff.get("focused_validation_all_expected_passed")
    )
    selected_proposal_id = str(
        handoff.get("selected_proposal_id")
        or handoff.get("proposal_id")
        or focused.get("selected_proposal_id")
        or focused.get("proposal_id")
        or "prop-skill-reverse-flow-focused-test-validation"
    )
    profiles = [
        str(profile)
        for profile in list(
            handoff.get("route_profiles")
            or focused.get("route_profiles")
            or unlocked_apply.get("route_profiles")
            or []
        )
    ]
    skill_first = bool(
        handoff.get(
            "skill_route_discovery_first",
            focused.get(
                "skill_route_discovery_first",
                unlocked_apply.get("skill_route_discovery_first"),
            ),
        )
    )
    selected_lane = str(
        handoff.get("selected_local_lane")
        or focused.get("selected_local_lane")
        or unlocked_apply.get("selected_local_lane")
        or ""
    )
    unlocked_lanes = [
        lane
        for lane in list(
            handoff.get("unlocked_local_lanes")
            or focused.get("unlocked_local_lanes")
            or unlocked_apply.get("unlocked_local_lanes")
            or []
        )
        if lane in SKILL_ROUTE_DISCOVERY_ALLOWED_LANES
    ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                handoff.get("retained_boundary_proposal_ids")
                or focused.get("retained_boundary_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                handoff.get("adjacent_general_agent_proposal_ids")
                or focused.get("adjacent_general_agent_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    residual_adjacent_available = bool(adjacent_ids) or bool(
        handoff.get("residual_adjacent_harness_eval_available")
    )
    command_hashes = [
        str(item)
        for item in list(focused_validation.get("command_hashes") or [])
        if str(item).strip()
    ]
    result_rows = [
        row
        for row in list(focused_validation.get("command_results") or [])
        if isinstance(row, dict)
    ]
    body_free_results = (
        all(set(row.keys()) <= {"command_hash", "passed", "in_expected_set"} for row in result_rows)
        if result_rows
        else True
    )

    handoff_ready = (
        handoff_status == "ready"
        and str(handoff.get("decision") or "")
        == "package_activation_external_handoff_after_focused_validation_pass"
        and bool(handoff.get("activation_external_only", True))
        and handoff.get("supervisor_activation_allowed") is not True
        and handoff.get("runtime_action") == "none"
        and handoff.get("external_skill_execution_allowed") is not True
        and handoff.get("provider_launch_allowed") is not True
        and handoff.get("remote_apply_allowed") is not True
        and handoff.get("push_or_promotion_allowed") is not True
        and handoff.get("kernel_restart_allowed") is not True
        and focused_status == "passed"
        and recorded
        and results_cover_expected
        and all_expected_passed
        and body_free_results
    )

    if handoff_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_activation_external_handoff_for_acceptance"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_focused_validation_activation_external_handoff"
        )
    elif handoff_status.startswith("blocked"):
        status = "blocked_until_activation_external_handoff_ready"
        decision = "hold_activation_external_acceptance_until_handoff_ready"
        if handoff_status == "blocked_until_focused_validation_recorded":
            # Inherit handoff/focused continue action (run full set vs record remaining).
            handoff_next = str(handoff.get("supervisor_next_action") or "").strip()
            supervisor_next_action = handoff_next or (
                resolve_reverse_flow_focused_validation_continue_supervisor_next(
                    focused_local_test_validation=focused,
                )
            )
        elif handoff_status == "blocked_until_focused_validation_repaired":
            supervisor_next_action = "repair_failed_focused_local_test_validation_commands"
        else:
            supervisor_next_action = (
                "repair_skill_route_discovery_focused_validation_activation_external_handoff"
            )
    elif handoff_ready:
        status = "accepted"
        decision = "accept_activation_external_package_after_focused_validation_pass"
        if residual_adjacent_available:
            supervisor_next_action = (
                "keep_activation_external_and_queue_residual_adjacent_harness_eval"
            )
        else:
            supervisor_next_action = (
                "keep_activation_external_after_focused_local_test_validation"
            )
    else:
        status = "blocked_until_activation_external_handoff_ready"
        decision = "hold_activation_external_acceptance_until_body_free_pass_handoff"
        handoff_next = str(handoff.get("supervisor_next_action") or "").strip()
        supervisor_next_action = handoff_next or (
            resolve_reverse_flow_focused_validation_continue_supervisor_next(
                focused_local_test_validation=focused,
            )
        )

    export_hashes = command_hashes if status == "accepted" else []
    export_results = result_rows if status == "accepted" else []
    # Residual fortress/Hy3 IDs and availability export only after reverse-flow
    # acceptance. While acceptance is blocked on handoff/record, do not
    # pre-advertise residual adjacent work.
    export_adjacent_ids = adjacent_ids if status == "accepted" else []
    export_residual_available = (
        bool(residual_adjacent_available) if status == "accepted" else False
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_focused_validation_activation_external_acceptance"
        ),
        "proposal_track": "prop-skill-reverse-flow-focused-test-validation",
        "legacy_proposal_track": "prop-skill-reverse-flow-test-lane",
        "companion_tracks": [
            "prop-skill-reverse-flow-focused-test-validation",
            "prop-skill-reverse-flow-test-lane",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_local_skill_route_validation_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
        ],
        "focused_validation_activation_external_handoff_status": handoff_status or "none",
        "focused_validation_activation_external_handoff_decision": str(
            handoff.get("decision") or ""
        ),
        "focused_local_test_validation_status": focused_status or "none",
        "focused_local_test_validation_recorded": recorded,
        "focused_validation_results_cover_expected": (
            results_cover_expected if recorded else False
        ),
        "focused_validation_all_expected_passed": (
            all_expected_passed if recorded else False
        ),
        "unlocked_local_test_lane_apply_status": str(
            unlocked_apply.get("status")
            or handoff.get("unlocked_local_test_lane_apply_status")
            or "none"
        ),
        "local_apply_completion_status": str(
            focused.get("local_apply_completion_status")
            or unlocked_apply.get("local_apply_completion_status")
            or handoff.get("local_apply_completion_status")
            or "none"
        ),
        "reverse_flow_test_validation_lane_status": str(
            focused.get("reverse_flow_test_validation_lane_status")
            or unlocked_apply.get("reverse_flow_test_validation_lane_status")
            or handoff.get("reverse_flow_test_validation_lane_status")
            or "none"
        ),
        "local_comparison_passed": bool(
            focused.get(
                "local_comparison_passed",
                unlocked_apply.get(
                    "local_comparison_passed",
                    handoff.get("local_comparison_passed"),
                ),
            )
        ),
        "theme_complete": bool(
            focused.get(
                "theme_complete",
                unlocked_apply.get("theme_complete", handoff.get("theme_complete")),
            )
        ),
        "route_class": str(
            focused.get("route_class")
            or unlocked_apply.get("route_class")
            or handoff.get("route_class")
            or "none"
        ),
        "route_profiles": profiles,
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": selected_lane if selected_lane else "none",
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        "unlocked_local_lanes": unlocked_lanes if status == "accepted" else [],
        "focused_validation": {
            "status": focused_status or "none",
            "lane": "test",
            "required": True,
            "command_hashes": export_hashes,
            "command_results": export_results,
            "expected_command_count": len(export_hashes),
            "recorded_result_count": len(export_results),
            "results_cover_expected": results_cover_expected if recorded else False,
            "all_expected_passed": all_expected_passed if recorded else False,
            "recorded": recorded,
            "body_free": body_free_results,
            "commands_exported": False,
        },
        "local_validation_required": True,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids,
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "residual_adjacent_harness_eval_available": export_residual_available,
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_available else "none"
        ),
        "residual_adjacent_export_held_until_ready": status != "accepted"
        and bool(residual_adjacent_available),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": bool(
            focused.get(
                "general_agent_isolation_passed",
                handoff.get("general_agent_isolation_passed", True),
            )
        ),
        "privacy_isolation_passed": bool(
            focused.get(
                "privacy_isolation_passed",
                handoff.get("privacy_isolation_passed", True),
            )
        ),
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or handoff.get("theme_id")
            or focused.get("theme_id")
            or unlocked_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (handoff.get("theme_pass") or {}).get("planned_passes")
                or (focused.get("theme_pass") or {}).get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (handoff.get("theme_pass") or {}).get("target_passes")
                or (focused.get("theme_pass") or {}).get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (handoff.get("theme_pass") or {}).get("status")
                or (focused.get("theme_pass") or {}).get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(
            handoff.get("source_digest")
            or focused.get("source_digest")
            or unlocked_apply.get("source_digest")
            or ""
        ),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_local_test_validation",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_validation_activation_external",
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_focused_validation_residual_adjacent_queue(
    *,
    focused_validation_activation_external_acceptance: dict[str, Any] | None = None,
    focused_validation_activation_external_handoff: dict[str, Any] | None = None,
    focused_local_test_validation: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Queue residual fortress/Hy3 rows after reverse-flow activation-external acceptance.

    After ``skill_route_discovery_focused_validation_activation_external_acceptance``
    is ``accepted``, residual adjacent general-agent proposal IDs are packaged for
    ``agent_harness_eval_cluster_local_apply`` without inheriting reverse-flow skill
    unlocks. This is distinct from
    ``skill_route_discovery_adjacent_harness_eval_handoff``, which fires only when
    the selected pipeline step is itself an adjacent harness-eval row. Push,
    promotion, provider launch, remote apply, external skill execution, and kernel
    restart stay denied; activation remains external. Privacy/offensive rows stay
    review-only.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    acceptance = (
        focused_validation_activation_external_acceptance
        if isinstance(focused_validation_activation_external_acceptance, dict)
        else {}
    )
    handoff = (
        focused_validation_activation_external_handoff
        if isinstance(focused_validation_activation_external_handoff, dict)
        else {}
    )
    focused = (
        focused_local_test_validation
        if isinstance(focused_local_test_validation, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    acceptance_status = str(acceptance.get("status") or "")
    acceptance_decision = str(acceptance.get("decision") or "")
    focused_status = str(
        focused.get("status")
        or acceptance.get("focused_local_test_validation_status")
        or handoff.get("focused_local_test_validation_status")
        or ""
    )
    def _proposal_id_list(*candidates: Any) -> list[str]:
        for candidate in candidates:
            if isinstance(candidate, list):
                return [
                    str(item)
                    for item in candidate
                    if str(item).strip()
                ]
        return []

    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | set(
            _proposal_id_list(
                acceptance.get("retained_boundary_proposal_ids"),
                handoff.get("retained_boundary_proposal_ids"),
                focused.get("retained_boundary_proposal_ids"),
            )
        )
    )
    # Prefer explicit adjacent rows / packet lists (including empty) so callers can
    # clear residual availability after acceptance without inheriting focused IDs.
    if adjacent:
        adjacent_ids = sorted(
            {
                str(row.get("proposal_id") or "")
                for row in adjacent
                if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
            }
        )
    else:
        adjacent_ids = sorted(
            set(
                _proposal_id_list(
                    acceptance.get("adjacent_general_agent_proposal_ids"),
                    handoff.get("adjacent_general_agent_proposal_ids"),
                    focused.get("adjacent_general_agent_proposal_ids"),
                )
            )
        )
    # Acceptance/handoff residual_adjacent_harness_eval_available is an export gate
    # while reverse-flow is blocked (False means held, not absent). Trust those flags
    # only after reverse-flow acceptance (or ready handoff) completes; otherwise
    # residual existence follows adjacent row/id presence.
    if acceptance_status == "accepted" and "residual_adjacent_harness_eval_available" in acceptance:
        residual_available = bool(acceptance.get("residual_adjacent_harness_eval_available")) and bool(
            adjacent_ids
        )
    elif (
        str(handoff.get("status") or "") == "ready"
        and "residual_adjacent_harness_eval_available" in handoff
    ):
        residual_available = bool(handoff.get("residual_adjacent_harness_eval_available")) and bool(
            adjacent_ids
        )
    else:
        residual_available = bool(adjacent_ids)
    selected_proposal_id = str(
        acceptance.get("selected_proposal_id")
        or acceptance.get("proposal_id")
        or handoff.get("selected_proposal_id")
        or focused.get("selected_proposal_id")
        or "prop-skill-reverse-flow-continue-local-validation"
    )
    profiles = [
        str(profile)
        for profile in list(
            acceptance.get("route_profiles")
            or handoff.get("route_profiles")
            or focused.get("route_profiles")
            or []
        )
    ]
    skill_first = bool(
        acceptance.get(
            "skill_route_discovery_first",
            handoff.get(
                "skill_route_discovery_first",
                focused.get("skill_route_discovery_first"),
            ),
        )
    )
    selected_lane = str(
        acceptance.get("selected_local_lane")
        or handoff.get("selected_local_lane")
        or focused.get("selected_local_lane")
        or ""
    )
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
        if isinstance(row, dict)
    ) if adjacent else True
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
        if isinstance(row, dict)
    ) if retained else True
    acceptance_ready = (
        acceptance_status == "accepted"
        and acceptance_decision
        == "accept_activation_external_package_after_focused_validation_pass"
        and bool(acceptance.get("activation_external_only", True))
        and acceptance.get("supervisor_activation_allowed") is not True
        and acceptance.get("runtime_action") == "none"
        and acceptance.get("external_skill_execution_allowed") is not True
        and acceptance.get("provider_launch_allowed") is not True
        and acceptance.get("remote_apply_allowed") is not True
        and acceptance.get("push_or_promotion_allowed") is not True
        and acceptance.get("kernel_restart_allowed") is not True
        and focused_status == "passed"
    )

    if acceptance_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_activation_external_acceptance_for_residual_adjacent_queue"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_focused_validation_activation_external_acceptance"
        )
    elif acceptance_status.startswith("blocked"):
        status = "blocked_until_activation_external_acceptance"
        decision = "hold_residual_adjacent_queue_until_activation_external_acceptance"
        if acceptance.get("supervisor_next_action"):
            supervisor_next_action = str(acceptance.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "run_focused_local_test_validation_then_keep_activation_external"
            )
    elif not acceptance_ready:
        status = "blocked_until_activation_external_acceptance"
        decision = "hold_residual_adjacent_queue_until_body_free_acceptance"
        supervisor_next_action = (
            "run_focused_local_test_validation_then_keep_activation_external"
        )
    elif not residual_available:
        status = "not_applicable"
        decision = "no_residual_adjacent_rows_after_focused_validation_acceptance"
        supervisor_next_action = (
            "keep_activation_external_after_focused_local_test_validation"
        )
    elif not general_agent_isolated or not privacy_isolated:
        status = "blocked"
        decision = "repair_residual_adjacent_isolation_before_queue"
        supervisor_next_action = "repair_skill_route_config_gate_isolation"
    else:
        status = "ready"
        decision = (
            "queue_residual_adjacent_harness_eval_after_focused_validation_acceptance"
        )
        supervisor_next_action = (
            "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )

    export_adjacent_ids = adjacent_ids if status == "ready" else []
    export_retained_ids = retained_ids if status in {"ready", "blocked", "not_applicable"} else []
    # Residual availability is only advertised when the residual queue itself is
    # residual-active (ready). Reverse-flow-waiting blocked statuses keep
    # availability false so operators do not jump past reverse-flow gates.
    export_residual_available = bool(residual_available) if status == "ready" else False

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_focused_validation_residual_adjacent_queue"
        ),
        "proposal_track": "prop-skill-reverse-flow-continue-local-validation",
        "legacy_proposal_track": "prop-skill-reverse-flow-focused-test-validation",
        "companion_tracks": [
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-reverse-flow-focused-test-validation",
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": selected_proposal_id,
        "selected_proposal_id": selected_proposal_id,
        "status": status,
        "decision": decision,
        "capability_action": "queue_residual_adjacent_harness_eval_after_local_validation",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
        ],
        "focused_validation_activation_external_acceptance_status": (
            acceptance_status or "none"
        ),
        "focused_validation_activation_external_acceptance_decision": acceptance_decision,
        "focused_validation_activation_external_handoff_status": str(
            handoff.get("status")
            or acceptance.get("focused_validation_activation_external_handoff_status")
            or "none"
        ),
        "focused_local_test_validation_status": focused_status or "none",
        "focused_local_test_validation_recorded": focused_status in {"passed", "failed"}
        or bool(
            focused.get("focused_validation_recorded")
            or acceptance.get("focused_local_test_validation_recorded")
        ),
        "route_class": str(
            acceptance.get("route_class")
            or handoff.get("route_class")
            or focused.get("route_class")
            or "none"
        ),
        "route_profiles": profiles,
        "skill_route_discovery_first": skill_first,
        "selected_local_lane": selected_lane if selected_lane else "none",
        "preferred_local_lane": "test",
        "allowed_local_lanes": list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES),
        # Residual queue never re-exports reverse-flow skill unlocks.
        "unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": list(
            AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        ),
        "direct_allowed_lanes_before_eval": [],
        "local_validation_required": True,
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": export_retained_ids,
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "residual_adjacent_harness_eval_available": export_residual_available,
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_available else "none"
        ),
        "residual_adjacent_export_held_until_ready": status != "ready"
        and bool(residual_available),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or acceptance.get("theme_id")
            or handoff.get("theme_id")
            or focused.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (acceptance.get("theme_pass") or {}).get("planned_passes")
                or (handoff.get("theme_pass") or {}).get("planned_passes")
                or (focused.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (acceptance.get("theme_pass") or {}).get("target_passes")
                or (handoff.get("theme_pass") or {}).get("target_passes")
                or (focused.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (acceptance.get("theme_pass") or {}).get("status")
                or (handoff.get("theme_pass") or {}).get("status")
                or (focused.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(
            acceptance.get("source_digest")
            or handoff.get("source_digest")
            or focused.get("source_digest")
            or ""
        ),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_local_test_validation",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_validation_residual_adjacent",
            ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def _select_residual_adjacent_harness_eval_proposal_id(
    residual_adjacent_queue: dict[str, Any],
    adjacent_general_agent_rows: list[dict[str, Any]],
) -> str:
    """Prefer fortress residual IDs, then Hy3, then first residual proposal ID."""

    residual_ids = [
        str(item).strip()
        for item in list(residual_adjacent_queue.get("adjacent_general_agent_proposal_ids") or [])
        if str(item).strip()
    ]
    if not residual_ids and adjacent_general_agent_rows:
        residual_ids = [
            str(row.get("proposal_id") or "").strip()
            for row in adjacent_general_agent_rows
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        ]
    if not residual_ids:
        return ""

    def _blob(proposal_id: str) -> str:
        for row in adjacent_general_agent_rows:
            if not isinstance(row, dict):
                continue
            if str(row.get("proposal_id") or "").strip() == proposal_id:
                return " ".join(
                    str(row.get(key) or "")
                    for key in ("proposal_id", "reason", "route_class", "evaluation_lane")
                ).lower()
        return proposal_id.lower()

    for proposal_id in residual_ids:
        blob = _blob(proposal_id)
        if any(marker in blob for marker in _SKILL_ROUTE_FORTRESS_MARKERS) or "fortress" in proposal_id.lower():
            return proposal_id
    for proposal_id in residual_ids:
        blob = _blob(proposal_id)
        if any(marker in blob for marker in _SKILL_ROUTE_HY3_MARKERS) or "hy3" in proposal_id.lower():
            return proposal_id
    return residual_ids[0]


def _export_residual_selected_proposal_id(
    status: str,
    selected_residual_id: str,
    *,
    residual_active_statuses: frozenset[str] | set[str] | None = None,
) -> str:
    """Export residual fortress/Hy3 selection only when residual work is residual-active.

    Reverse-flow-waiting statuses (for example
    ``blocked_until_activation_external_acceptance`` or
    ``blocked_until_residual_adjacent_queue_ready``) must not advertise a residual
    selected proposal early. Residual-active statuses include ``ready``, isolation
    ``blocked``, recorded residual outcomes, and residual-path hold states that own
    residual work after reverse-flow gates clear.
    """

    residual_id = str(selected_residual_id or "").strip()
    if not residual_id:
        return ""
    status_text = str(status or "").strip()
    if residual_active_statuses is not None:
        return residual_id if status_text in residual_active_statuses else ""
    if status_text in {
        "ready",
        "blocked",
        "passed",
        "failed",
        "accepted",
        "blocked_until_residual_adjacent_focused_validation_recorded",
        "blocked_until_residual_adjacent_focused_validation_repaired",
        "blocked_until_residual_adjacent_focused_validation_pass",
    }:
        return residual_id
    return ""


def build_skill_route_discovery_residual_adjacent_harness_eval_local_apply(
    *,
    focused_validation_residual_adjacent_queue: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
    selected_residual_proposal_id: str | None = None,
) -> dict[str, Any]:
    """Hand one residual fortress/Hy3 row to agent harness-eval local apply.

    After ``skill_route_discovery_focused_validation_residual_adjacent_queue`` is
    ``ready``, this surface selects one residual adjacent proposal (prefer
    fortress, then Hy3) and packages a body-free handoff for
    ``agent_harness_eval_cluster_local_apply``. Local comparison is required
    before any documentation/test/code_patch unlock; reverse-flow skill unlocks
    stay closed. When this surface is ``ready``,
    ``skill_route_discovery_residual_adjacent_harness_eval_local_comparison``
    closes residual comparison, and
    ``skill_route_discovery_residual_adjacent_unlocked_local_lane_apply`` packages
    unlocked documentation/test/code_patch apply. This is distinct from
    ``skill_route_discovery_adjacent_harness_eval_handoff``, which fires only when
    the selected pipeline step is itself an adjacent harness-eval row. Push,
    promotion, provider launch, remote apply, external skill execution, and
    kernel restart stay denied; activation remains external. Privacy/offensive
    rows stay review-only.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    residual_queue = (
        focused_validation_residual_adjacent_queue
        if isinstance(focused_validation_residual_adjacent_queue, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    residual_status = str(residual_queue.get("status") or "")
    residual_decision = str(residual_queue.get("decision") or "")
    residual_ids = [
        str(item).strip()
        for item in list(residual_queue.get("adjacent_general_agent_proposal_ids") or [])
        if str(item).strip()
    ]
    if not residual_ids:
        residual_ids = [
            str(row.get("proposal_id") or "").strip()
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item).strip()
            for item in list(residual_queue.get("retained_boundary_proposal_ids") or [])
            if str(item).strip()
        }
    )
    explicit_selected = str(selected_residual_proposal_id or "").strip()
    if explicit_selected and (not residual_ids or explicit_selected in residual_ids):
        selected_residual_id = explicit_selected
    else:
        selected_residual_id = _select_residual_adjacent_harness_eval_proposal_id(
            residual_queue,
            adjacent,
        )
    selected_row = next(
        (
            row
            for row in adjacent
            if isinstance(row, dict)
            and str(row.get("proposal_id") or "").strip() == selected_residual_id
        ),
        None,
    )
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
        if isinstance(row, dict)
    ) if adjacent else bool(
        residual_queue.get("general_agent_isolation_passed", True)
    )
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
        if isinstance(row, dict)
    ) if retained else bool(residual_queue.get("privacy_isolation_passed", True))
    skill_unlocks_closed = (
        list(residual_queue.get("unlocked_local_lanes") or []) == []
        and residual_queue.get("skill_route_discovery_inherited") is not True
        and residual_queue.get("skill_route_unlocks_closed_for_residual") is not False
    )
    residual_ready = (
        residual_status == "ready"
        and residual_decision
        == "queue_residual_adjacent_harness_eval_after_focused_validation_acceptance"
        and bool(residual_queue.get("activation_external_only", True))
        and residual_queue.get("supervisor_activation_allowed") is not True
        and residual_queue.get("runtime_action", "none") == "none"
        and residual_queue.get("external_skill_execution_allowed") is not True
        and residual_queue.get("provider_launch_allowed") is not True
        and residual_queue.get("remote_apply_allowed") is not True
        and residual_queue.get("push_or_promotion_allowed") is not True
        and residual_queue.get("kernel_restart_allowed") is not True
        and bool(selected_residual_id)
    )

    if residual_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_residual_adjacent_queue_for_harness_eval_local_apply"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_focused_validation_residual_adjacent_queue"
        )
    elif residual_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_queue_ready"
        decision = "hold_residual_adjacent_harness_eval_local_apply_until_queue_ready"
        if residual_queue.get("supervisor_next_action"):
            supervisor_next_action = str(residual_queue.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "run_focused_local_test_validation_then_keep_activation_external"
            )
    elif not residual_ready:
        status = "blocked_until_residual_adjacent_queue_ready"
        decision = "hold_residual_adjacent_harness_eval_local_apply_until_queue_ready"
        supervisor_next_action = (
            "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
            if residual_status == "ready"
            else "run_focused_local_test_validation_then_keep_activation_external"
        )
    elif not general_agent_isolated or not privacy_isolated or not skill_unlocks_closed:
        status = "blocked"
        decision = "repair_residual_adjacent_isolation_before_harness_eval_local_apply"
        supervisor_next_action = "repair_skill_route_config_gate_isolation"
    else:
        status = "ready"
        decision = (
            "hand_off_selected_residual_adjacent_row_to_agent_harness_eval_cluster_local_apply"
        )
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
        )

    export_residual_ids = residual_ids if status in {"ready", "blocked"} else []
    # Do not advertise residual fortress/Hy3 selection while residual apply is only
    # reverse-flow-waiting (blocked_until_residual_adjacent_queue_ready). Selection is
    # computed for residual-active readiness but exported only when residual-active.
    export_selected_residual = _export_residual_selected_proposal_id(
        status,
        selected_residual_id,
        residual_active_statuses={"ready", "blocked"},
    )
    comparison_notes = [
        {
            "criterion_id": "residual_adjacent_queue_ready",
            "required": True,
            "passed": residual_status == "ready",
        },
        {
            "criterion_id": "selected_residual_proposal_present",
            "required": True,
            "passed": bool(selected_residual_id),
        },
        {
            "criterion_id": "skill_route_unlocks_closed_for_residual",
            "required": True,
            "passed": skill_unlocks_closed,
        },
        {
            "criterion_id": "general_agent_does_not_inherit_skill_unlock",
            "required": True,
            "passed": general_agent_isolated,
        },
        {
            "criterion_id": "privacy_or_offensive_rows_remain_review_only",
            "required": True,
            "passed": privacy_isolated,
        },
        {
            "criterion_id": "activation_external_only",
            "required": True,
            "passed": bool(residual_queue.get("activation_external_only", True)),
        },
        {
            "criterion_id": "runtime_action_none",
            "required": True,
            "passed": residual_queue.get("runtime_action", "none") == "none",
        },
    ]
    failed_criteria = [
        str(row["criterion_id"])
        for row in comparison_notes
        if row.get("required") is True and row.get("passed") is not True
    ]

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": export_selected_residual,
        "selected_proposal_id": export_selected_residual,
        "selected_residual_proposal_id": export_selected_residual,
        "selected_residual_route_class": str(
            (selected_row or {}).get("route_class")
            or residual_queue.get("evaluation_lane")
            or "agent_harness_eval_required"
        )
        if export_selected_residual
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_selected_residual)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_residual_adjacent_harness_eval_local_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
        ],
        "focused_validation_residual_adjacent_queue_status": residual_status or "none",
        "focused_validation_residual_adjacent_queue_decision": residual_decision,
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": list(
            AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        ),
        "direct_allowed_lanes_before_eval": [],
        # Residual handoff never re-exports reverse-flow skill unlocks.
        "unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_status": (
            "ready_for_agent_harness_eval_local_comparison"
            if status == "ready"
            else "blocked_until_residual_adjacent_queue_ready"
            if status.startswith("blocked")
            else "not_applicable"
        ),
        "local_comparison_notes": comparison_notes if status in {"ready", "blocked"} else [],
        "failed_local_comparison_criteria": failed_criteria if status == "blocked" else [],
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids if status in {"ready", "blocked", "not_applicable"} else [],
        "adjacent_general_agent_proposal_ids": export_residual_ids,
        # Availability tracks residual-active export only; reverse-flow-waiting
        # statuses leave residual IDs and availability empty.
        "residual_adjacent_harness_eval_available": bool(export_residual_ids),
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_ids else "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or residual_queue.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (residual_queue.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (residual_queue.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (residual_queue.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(residual_queue.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_focused_validation_residual_adjacent",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison(
    *,
    residual_adjacent_harness_eval_local_apply: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Run residual fortress/Hy3 harness local comparison after residual apply.

    After ``skill_route_discovery_residual_adjacent_harness_eval_local_apply`` is
    ``ready``, this surface closes supervisor next action
    ``run_agent_harness_eval_local_comparison_for_residual_adjacent_row`` by
    evaluating body-free residual harness criteria. When criteria pass, unlock
    only documentation/test/code_patch for bounded local apply. Reverse-flow
    skill unlocks stay closed (``skill_route_discovery_inherited=false``,
    ``skill_route_unlocked_local_lanes=[]``). Distinct from selected-step
    ``skill_route_discovery_adjacent_harness_eval_handoff``. Push, promotion,
    provider launch, remote apply, external skill execution, and kernel restart
    stay denied; activation remains external. Privacy/offensive rows stay
    review-only.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    residual_apply = (
        residual_adjacent_harness_eval_local_apply
        if isinstance(residual_adjacent_harness_eval_local_apply, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    residual_apply_status = str(residual_apply.get("status") or "")
    residual_apply_decision = str(residual_apply.get("decision") or "")
    selected_residual_id = str(
        residual_apply.get("selected_residual_proposal_id")
        or residual_apply.get("selected_proposal_id")
        or residual_apply.get("proposal_id")
        or ""
    ).strip()
    residual_ids = [
        str(item).strip()
        for item in list(residual_apply.get("adjacent_general_agent_proposal_ids") or [])
        if str(item).strip()
    ]
    if not residual_ids and adjacent:
        residual_ids = [
            str(row.get("proposal_id") or "").strip()
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item).strip()
            for item in list(residual_apply.get("retained_boundary_proposal_ids") or [])
            if str(item).strip()
        }
    )
    selected_row = next(
        (
            row
            for row in adjacent
            if isinstance(row, dict)
            and str(row.get("proposal_id") or "").strip() == selected_residual_id
        ),
        None,
    )
    allowed_after = [
        lane
        for lane in list(
            residual_apply.get("allowed_local_lanes_after_local_comparison")
            or (selected_row or {}).get("allowed_local_lanes_after_eval")
            or AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        )
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ] or list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES)
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
        if isinstance(row, dict)
    ) if adjacent else bool(residual_apply.get("general_agent_isolation_passed", True))
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
        if isinstance(row, dict)
    ) if retained else bool(residual_apply.get("privacy_isolation_passed", True))
    skill_unlocks_closed = (
        list(residual_apply.get("unlocked_local_lanes") or []) == []
        and residual_apply.get("skill_route_discovery_inherited") is not True
        and residual_apply.get("skill_route_unlocks_closed_for_residual") is not False
    )
    residual_apply_ready = (
        residual_apply_status == "ready"
        and residual_apply_decision
        == "hand_off_selected_residual_adjacent_row_to_agent_harness_eval_cluster_local_apply"
        and bool(selected_residual_id)
        and bool(residual_apply.get("activation_external_only", True))
        and residual_apply.get("supervisor_activation_allowed") is not True
        and residual_apply.get("runtime_action", "none") == "none"
        and residual_apply.get("external_skill_execution_allowed") is not True
        and residual_apply.get("provider_launch_allowed") is not True
        and residual_apply.get("remote_apply_allowed") is not True
        and residual_apply.get("push_or_promotion_allowed") is not True
        and residual_apply.get("kernel_restart_allowed") is not True
        and residual_apply.get("local_comparison_required") is not False
        and str(residual_apply.get("handoff_controller_surface") or "")
        == "agent_harness_eval_cluster_local_apply"
    )
    comparison_notes = [
        {
            "criterion_id": "residual_adjacent_local_apply_ready",
            "required": True,
            "passed": residual_apply_status == "ready",
        },
        {
            "criterion_id": "selected_residual_proposal_present",
            "required": True,
            "passed": bool(selected_residual_id),
        },
        {
            "criterion_id": "handoff_targets_agent_harness_eval_cluster_local_apply",
            "required": True,
            "passed": str(residual_apply.get("handoff_controller_surface") or "")
            == "agent_harness_eval_cluster_local_apply",
        },
        {
            "criterion_id": "local_comparison_required",
            "required": True,
            "passed": residual_apply.get("local_comparison_required") is not False,
        },
        {
            "criterion_id": "skill_route_unlocks_closed_for_residual",
            "required": True,
            "passed": skill_unlocks_closed,
        },
        {
            "criterion_id": "general_agent_does_not_inherit_skill_unlock",
            "required": True,
            "passed": general_agent_isolated,
        },
        {
            "criterion_id": "privacy_or_offensive_rows_remain_review_only",
            "required": True,
            "passed": privacy_isolated,
        },
        {
            "criterion_id": "allowed_lanes_subset_of_harness_post_compare",
            "required": True,
            "passed": all(
                lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES for lane in allowed_after
            )
            and bool(allowed_after),
        },
        {
            "criterion_id": "activation_external_only",
            "required": True,
            "passed": bool(residual_apply.get("activation_external_only", True)),
        },
        {
            "criterion_id": "runtime_action_none",
            "required": True,
            "passed": residual_apply.get("runtime_action", "none") == "none",
        },
        {
            "criterion_id": "external_skill_execution_denied",
            "required": True,
            "passed": residual_apply.get("external_skill_execution_allowed") is not True,
        },
        {
            "criterion_id": "provider_launch_denied",
            "required": True,
            "passed": residual_apply.get("provider_launch_allowed") is not True,
        },
        {
            "criterion_id": "remote_apply_denied",
            "required": True,
            "passed": residual_apply.get("remote_apply_allowed") is not True,
        },
    ]
    failed_criteria = [
        str(row["criterion_id"])
        for row in comparison_notes
        if row.get("required") is True and row.get("passed") is not True
    ]
    comparison_passed = residual_apply_ready and not failed_criteria

    if residual_apply_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_residual_adjacent_local_apply_for_harness_eval_local_comparison"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_residual_adjacent_harness_eval_local_apply"
        )
        unlocked_lanes: list[str] = []
        local_comparison_status = "not_applicable"
    elif residual_apply_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_harness_eval_local_apply_ready"
        decision = (
            "hold_residual_adjacent_harness_eval_local_comparison_until_local_apply_ready"
        )
        if residual_apply.get("supervisor_next_action"):
            supervisor_next_action = str(residual_apply.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
            )
        unlocked_lanes = []
        local_comparison_status = "blocked_until_residual_adjacent_local_apply_ready"
    elif not residual_apply_ready:
        status = "blocked_until_residual_adjacent_harness_eval_local_apply_ready"
        decision = (
            "hold_residual_adjacent_harness_eval_local_comparison_until_local_apply_ready"
        )
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
            if residual_apply_status == "ready"
            else "hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply"
        )
        unlocked_lanes = []
        local_comparison_status = "blocked_until_residual_adjacent_local_apply_ready"
    elif not general_agent_isolated or not privacy_isolated or not skill_unlocks_closed:
        status = "blocked"
        decision = "repair_residual_adjacent_isolation_before_harness_local_comparison"
        supervisor_next_action = "repair_skill_route_config_gate_isolation"
        unlocked_lanes = []
        local_comparison_status = "failed_local_comparison"
    elif not comparison_passed:
        status = "blocked"
        decision = "hold_residual_adjacent_row_until_harness_local_comparison_criteria_pass"
        supervisor_next_action = (
            "repair_residual_adjacent_harness_eval_local_comparison_criteria"
        )
        unlocked_lanes = []
        local_comparison_status = "failed_local_comparison"
    else:
        status = "ready"
        decision = (
            "unlock_documentation_test_or_code_patch_after_residual_adjacent_harness_local_comparison"
        )
        supervisor_next_action = (
            "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
        unlocked_lanes = list(allowed_after)
        local_comparison_status = "passed_local_comparison"

    export_residual_ids = residual_ids if status in {"ready", "blocked"} else []
    export_notes = comparison_notes if status in {"ready", "blocked"} else []
    export_failed = failed_criteria if status == "blocked" else []
    export_selected_residual = _export_residual_selected_proposal_id(
        status,
        selected_residual_id,
        residual_active_statuses={"ready", "blocked"},
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-residual-adjacent-hy3-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": export_selected_residual,
        "selected_proposal_id": export_selected_residual,
        "selected_residual_proposal_id": export_selected_residual,
        "selected_residual_route_class": str(
            (selected_row or {}).get("route_class")
            or residual_apply.get("selected_residual_route_class")
            or residual_apply.get("route_class")
            or "agent_harness_eval_required"
        )
        if export_selected_residual
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_selected_residual)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": "compare_one_residual_adjacent_harness_eval_local_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
        ],
        "residual_adjacent_harness_eval_local_apply_status": residual_apply_status or "none",
        "residual_adjacent_harness_eval_local_apply_decision": residual_apply_decision,
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": list(allowed_after),
        "direct_allowed_lanes_before_eval": [],
        # Harness post-compare unlocks (not reverse-flow skill unlocks).
        "unlocked_local_lanes": unlocked_lanes,
        "skill_route_unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_passed": comparison_passed if status == "ready" else False,
        "local_comparison_status": local_comparison_status,
        "local_comparison_notes": export_notes,
        "comparison_criteria_results": export_notes,
        "failed_local_comparison_criteria": export_failed,
        "failed_criteria": export_failed,
        "unlocked_lane_routes": [
            {
                "lane": lane,
                "status": "unlocked_after_residual_adjacent_harness_local_comparison",
                "required_validation": [
                    "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
                    ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
                ],
                "local_validation_required": True,
                "runtime_action": "none",
                "skill_route_discovery_inherited": False,
                "external_skill_execution_allowed": False,
                "provider_launch_allowed": False,
                "remote_apply_allowed": False,
            }
            for lane in unlocked_lanes
        ],
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids
        if status in {"ready", "blocked", "not_applicable"}
        else [],
        "adjacent_general_agent_proposal_ids": export_residual_ids,
        "residual_adjacent_harness_eval_available": bool(export_residual_ids),
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_ids else "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or residual_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (residual_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (residual_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (residual_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(residual_apply.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply(
    *,
    residual_adjacent_harness_eval_local_comparison: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
    preferred_local_lane: str | None = None,
) -> dict[str, Any]:
    """Apply residual harness-unlocked documentation/test/code_patch lanes.

    After ``skill_route_discovery_residual_adjacent_harness_eval_local_comparison``
    is ``ready`` and unlocks documentation/test/code_patch, this surface packages
    supervisor next action
    ``apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external``
    into one body-free residual apply packet. Preferred focused lane is local
    ``test`` when unlocked, else ``documentation``, else ``code_patch``. Reverse-
    flow skill unlocks stay closed (``skill_route_discovery_inherited=false``,
    ``skill_route_unlocked_local_lanes=[]``). Activation, push, promotion,
    provider launch, remote apply, external skill execution, and kernel restart
    stay denied. Privacy/offensive rows stay review-only. Distinct from reverse-
    flow ``skill_route_discovery_unlocked_local_test_lane_apply``.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    residual_comparison = (
        residual_adjacent_harness_eval_local_comparison
        if isinstance(residual_adjacent_harness_eval_local_comparison, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    residual_comparison_status = str(residual_comparison.get("status") or "")
    residual_comparison_decision = str(residual_comparison.get("decision") or "")
    selected_residual_id = str(
        residual_comparison.get("selected_residual_proposal_id")
        or residual_comparison.get("selected_proposal_id")
        or residual_comparison.get("proposal_id")
        or ""
    ).strip()
    residual_ids = [
        str(item).strip()
        for item in list(
            residual_comparison.get("adjacent_general_agent_proposal_ids") or []
        )
        if str(item).strip()
    ]
    if not residual_ids and adjacent:
        residual_ids = [
            str(row.get("proposal_id") or "").strip()
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item).strip()
            for item in list(residual_comparison.get("retained_boundary_proposal_ids") or [])
            if str(item).strip()
        }
    )
    selected_row = next(
        (
            row
            for row in adjacent
            if isinstance(row, dict)
            and str(row.get("proposal_id") or "").strip() == selected_residual_id
        ),
        None,
    )
    unlocked_lanes = [
        lane
        for lane in list(residual_comparison.get("unlocked_local_lanes") or [])
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ]
    allowed_after = [
        lane
        for lane in list(
            residual_comparison.get("allowed_local_lanes_after_local_comparison")
            or AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        )
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ] or list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES)
    lane_preference = (
        "test",
        "documentation",
        "code_patch",
    )
    explicit_preferred = str(preferred_local_lane or "").strip()
    selected_lane = ""
    if (
        explicit_preferred
        and explicit_preferred in unlocked_lanes
        and explicit_preferred in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ):
        selected_lane = explicit_preferred
    else:
        for candidate in lane_preference:
            if candidate in unlocked_lanes:
                selected_lane = candidate
                break
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
        if isinstance(row, dict)
    ) if adjacent else bool(
        residual_comparison.get("general_agent_isolation_passed", True)
    )
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
        if isinstance(row, dict)
    ) if retained else bool(residual_comparison.get("privacy_isolation_passed", True))
    skill_unlocks_closed = (
        list(residual_comparison.get("skill_route_unlocked_local_lanes") or []) == []
        and residual_comparison.get("skill_route_discovery_inherited") is not True
        and residual_comparison.get("skill_route_unlocks_closed_for_residual") is not False
    )
    comparison_ready = (
        residual_comparison_status == "ready"
        and residual_comparison_decision
        == "unlock_documentation_test_or_code_patch_after_residual_adjacent_harness_local_comparison"
        and bool(residual_comparison.get("local_comparison_passed"))
        and bool(selected_residual_id)
        and bool(unlocked_lanes)
        and bool(selected_lane)
        and selected_lane in unlocked_lanes
        and bool(residual_comparison.get("activation_external_only", True))
        and residual_comparison.get("supervisor_activation_allowed") is not True
        and residual_comparison.get("runtime_action", "none") == "none"
        and residual_comparison.get("external_skill_execution_allowed") is not True
        and residual_comparison.get("provider_launch_allowed") is not True
        and residual_comparison.get("remote_apply_allowed") is not True
        and residual_comparison.get("push_or_promotion_allowed") is not True
        and residual_comparison.get("kernel_restart_allowed") is not True
        and skill_unlocks_closed
        and general_agent_isolated
        and privacy_isolated
    )

    if residual_comparison_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_residual_adjacent_harness_local_comparison_for_unlocked_lane_apply"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_residual_adjacent_harness_eval_local_comparison"
        )
        export_unlocked: list[str] = []
        export_selected_lane = "none"
    elif residual_comparison_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_harness_local_comparison_ready"
        decision = (
            "hold_residual_adjacent_unlocked_local_lane_apply_until_harness_local_comparison_ready"
        )
        if residual_comparison.get("supervisor_next_action"):
            supervisor_next_action = str(residual_comparison.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
            )
        export_unlocked = []
        export_selected_lane = "none"
    elif not comparison_ready:
        if not skill_unlocks_closed or not general_agent_isolated or not privacy_isolated:
            status = "blocked"
            decision = (
                "repair_residual_adjacent_isolation_before_unlocked_local_lane_apply"
            )
            supervisor_next_action = "repair_skill_route_config_gate_isolation"
        elif residual_comparison_status == "ready" and not unlocked_lanes:
            status = "blocked"
            decision = "hold_residual_adjacent_unlocked_lane_until_harness_lanes_unlock"
            supervisor_next_action = (
                "repair_residual_adjacent_harness_eval_local_comparison_criteria"
            )
        else:
            status = "blocked_until_residual_adjacent_harness_local_comparison_ready"
            decision = (
                "hold_residual_adjacent_unlocked_local_lane_apply_until_harness_local_comparison_ready"
            )
            supervisor_next_action = (
                "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
                if residual_comparison_status == "ready"
                else "run_agent_harness_eval_local_comparison_for_residual_adjacent_row"
            )
        export_unlocked = []
        export_selected_lane = "none"
    else:
        status = "ready"
        decision = (
            "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        )
        supervisor_next_action = (
            "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
        export_unlocked = list(unlocked_lanes)
        export_selected_lane = selected_lane

    export_residual_ids = residual_ids if status in {"ready", "blocked"} else []
    focused_validation_commands = [
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
        "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_focused_local_validation",
        ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
    ]
    focused_validation_command_hashes = [
        hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
        for command in focused_validation_commands
    ]
    comparison_notes = [
        {
            "criterion_id": "residual_adjacent_harness_local_comparison_ready",
            "required": True,
            "passed": residual_comparison_status == "ready",
        },
        {
            "criterion_id": "selected_residual_proposal_present",
            "required": True,
            "passed": bool(selected_residual_id),
        },
        {
            "criterion_id": "harness_post_compare_lanes_unlocked",
            "required": True,
            "passed": bool(unlocked_lanes),
        },
        {
            "criterion_id": "selected_lane_in_unlocked_set",
            "required": True,
            "passed": bool(selected_lane) and selected_lane in unlocked_lanes,
        },
        {
            "criterion_id": "selected_lane_subset_of_harness_post_compare",
            "required": True,
            "passed": (
                not selected_lane
                or selected_lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
            ),
        },
        {
            "criterion_id": "skill_route_unlocks_closed_for_residual",
            "required": True,
            "passed": skill_unlocks_closed,
        },
        {
            "criterion_id": "general_agent_does_not_inherit_skill_unlock",
            "required": True,
            "passed": general_agent_isolated,
        },
        {
            "criterion_id": "privacy_or_offensive_rows_remain_review_only",
            "required": True,
            "passed": privacy_isolated,
        },
        {
            "criterion_id": "activation_external_only",
            "required": True,
            "passed": bool(residual_comparison.get("activation_external_only", True)),
        },
        {
            "criterion_id": "runtime_action_none",
            "required": True,
            "passed": residual_comparison.get("runtime_action", "none") == "none",
        },
    ]
    failed_criteria = [
        str(row["criterion_id"])
        for row in comparison_notes
        if row.get("required") is True and row.get("passed") is not True
    ]

    export_selected_residual = _export_residual_selected_proposal_id(
        status,
        selected_residual_id,
        residual_active_statuses={"ready", "blocked"},
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-residual-adjacent-hy3-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": export_selected_residual,
        "selected_proposal_id": export_selected_residual,
        "selected_residual_proposal_id": export_selected_residual,
        "selected_residual_route_class": str(
            (selected_row or {}).get("route_class")
            or residual_comparison.get("selected_residual_route_class")
            or residual_comparison.get("route_class")
            or "agent_harness_eval_required"
        )
        if export_selected_residual
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_selected_residual)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": "apply_one_residual_adjacent_unlocked_local_lane_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "skill_route_discovery_residual_adjacent_focused_local_validation",
            "record_skill_route_discovery_residual_adjacent_focused_local_validation_results",
            "close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
        ],
        "residual_adjacent_harness_eval_local_comparison_status": (
            residual_comparison_status or "none"
        ),
        "residual_adjacent_harness_eval_local_comparison_decision": (
            residual_comparison_decision
        ),
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": list(allowed_after),
        "direct_allowed_lanes_before_eval": [],
        # Harness post-compare unlocks only; never reverse-flow skill unlocks.
        "selected_local_lane": export_selected_lane,
        "preferred_local_lane": "test",
        "unlocked_local_lanes": export_unlocked,
        "skill_route_unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_passed": bool(
            residual_comparison.get("local_comparison_passed")
        )
        if status == "ready"
        else False,
        "local_comparison_status": str(
            residual_comparison.get("local_comparison_status") or ""
        )
        if status in {"ready", "blocked"}
        else "not_applicable",
        "local_comparison_notes": comparison_notes
        if status in {"ready", "blocked"}
        else [],
        "failed_local_comparison_criteria": failed_criteria if status == "blocked" else [],
        "focused_validation": {
            "status": "ready" if status == "ready" else status,
            "lane": export_selected_lane if status == "ready" else "none",
            "required": True,
            "commands": focused_validation_commands if status == "ready" else [],
            "command_hashes": focused_validation_command_hashes
            if status == "ready"
            else [],
            "unit_test_signal": status == "ready",
            "coverage_required": False,
            "commands_exported": False,
        },
        "unlocked_lane_routes": [
            {
                "lane": lane,
                "status": (
                    "selected_for_residual_adjacent_focused_validation"
                    if lane == export_selected_lane and status == "ready"
                    else "unlocked_after_residual_adjacent_harness_local_comparison"
                ),
                "selected": lane == export_selected_lane and status == "ready",
                "required_validation": focused_validation_commands
                if status == "ready"
                else [],
                "local_validation_required": True,
                "runtime_action": "none",
                "skill_route_discovery_inherited": False,
                "external_skill_execution_allowed": False,
                "provider_launch_allowed": False,
                "remote_apply_allowed": False,
            }
            for lane in export_unlocked
        ],
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": retained_ids
        if status in {"ready", "blocked", "not_applicable"}
        else [],
        "adjacent_general_agent_proposal_ids": export_residual_ids,
        "residual_adjacent_harness_eval_available": bool(export_residual_ids),
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_residual_ids else "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or residual_comparison.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (residual_comparison.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (residual_comparison.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (residual_comparison.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(residual_comparison.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": focused_validation_commands,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_residual_adjacent_focused_local_validation(
    *,
    residual_adjacent_unlocked_local_lane_apply: dict[str, Any] | None = None,
    command_results: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Record body-free focused validation for residual unlocked local lanes.

    After ``skill_route_discovery_residual_adjacent_unlocked_local_lane_apply`` is
    ``ready``, this surface packages supervisor next action
    ``run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external``
    into one operator-visible residual focused validation packet. Commands are
    exported as hashes only. Optional ``command_results`` rows
    (``command_hash`` + ``passed``) mark the validation ``passed`` or ``failed``.
    Reverse-flow skill unlocks stay closed
    (``skill_route_discovery_inherited=false``,
    ``skill_route_unlocked_local_lanes=[]``). Activation, push, promotion,
    provider launch, remote apply, external skill execution, and kernel restart
    stay denied. Distinct from reverse-flow
    ``skill_route_discovery_focused_local_test_validation``.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    unlocked_apply = (
        residual_adjacent_unlocked_local_lane_apply
        if isinstance(residual_adjacent_unlocked_local_lane_apply, dict)
        else {}
    )
    unlocked_status = str(unlocked_apply.get("status") or "")
    unlocked_decision = str(unlocked_apply.get("decision") or "")
    selected_residual_id = str(
        unlocked_apply.get("selected_residual_proposal_id")
        or unlocked_apply.get("selected_proposal_id")
        or unlocked_apply.get("proposal_id")
        or ""
    ).strip()
    selected_lane = str(unlocked_apply.get("selected_local_lane") or "").strip()
    unlocked_lanes = [
        lane
        for lane in list(unlocked_apply.get("unlocked_local_lanes") or [])
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ]
    focused = (
        unlocked_apply.get("focused_validation")
        if isinstance(unlocked_apply.get("focused_validation"), dict)
        else {}
    )
    commands = [
        str(cmd)
        for cmd in list(
            focused.get("commands") or unlocked_apply.get("required_validation") or []
        )
        if str(cmd).strip()
    ]
    command_hashes = [
        str(item)
        for item in list(focused.get("command_hashes") or [])
        if str(item).strip()
    ]
    if not command_hashes and commands:
        command_hashes = [
            hashlib.sha256(command.encode("utf-8")).hexdigest()[:16]
            for command in commands
        ]
    expected_hash_set = set(command_hashes)
    result_rows = normalize_skill_route_discovery_focused_validation_command_results(
        command_results,
        expected_command_hashes=command_hashes,
    )
    results_cover_expected = bool(expected_hash_set) and expected_hash_set.issubset(
        {row["command_hash"] for row in result_rows}
    )
    all_expected_passed = results_cover_expected and all(
        row["passed"] for row in result_rows if row["command_hash"] in expected_hash_set
    )
    any_expected_failed = any(
        (not row["passed"]) and row["command_hash"] in expected_hash_set
        for row in result_rows
    ) if result_rows else False
    skill_unlocks_closed = (
        list(unlocked_apply.get("skill_route_unlocked_local_lanes") or []) == []
        and unlocked_apply.get("skill_route_discovery_inherited") is not True
        and unlocked_apply.get("skill_route_unlocks_closed_for_residual") is not False
    )
    apply_ready = (
        unlocked_status == "ready"
        and unlocked_decision
        == "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
        and bool(selected_residual_id)
        and bool(selected_lane)
        and selected_lane in unlocked_lanes
        and selected_lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        and bool(command_hashes)
        and bool(unlocked_apply.get("activation_external_only", True))
        and unlocked_apply.get("supervisor_activation_allowed") is not True
        and unlocked_apply.get("runtime_action", "none") == "none"
        and unlocked_apply.get("external_skill_execution_allowed") is not True
        and unlocked_apply.get("provider_launch_allowed") is not True
        and unlocked_apply.get("remote_apply_allowed") is not True
        and unlocked_apply.get("push_or_promotion_allowed") is not True
        and unlocked_apply.get("kernel_restart_allowed") is not True
        and unlocked_apply.get("body_free") is not False
        and skill_unlocks_closed
        and bool(unlocked_apply.get("general_agent_isolation_passed", True))
        and bool(unlocked_apply.get("privacy_isolation_passed", True))
    )

    if unlocked_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_residual_adjacent_unlocked_local_lane_apply_for_focused_validation"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply"
        )
    elif unlocked_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_unlocked_local_lane_apply_ready"
        decision = (
            "hold_residual_adjacent_focused_local_validation_until_unlocked_lane_apply_ready"
        )
        if unlocked_apply.get("supervisor_next_action"):
            supervisor_next_action = str(unlocked_apply.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
            )
    elif not apply_ready:
        if not skill_unlocks_closed or not unlocked_apply.get(
            "general_agent_isolation_passed", True
        ) or not unlocked_apply.get("privacy_isolation_passed", True):
            status = "blocked"
            decision = (
                "repair_residual_adjacent_isolation_before_focused_local_validation"
            )
            supervisor_next_action = "repair_skill_route_config_gate_isolation"
        elif unlocked_status == "ready" and not command_hashes:
            status = "blocked"
            decision = (
                "hold_residual_adjacent_focused_validation_until_command_hashes_ready"
            )
            supervisor_next_action = (
                "repair_residual_adjacent_unlocked_local_lane_apply_focused_validation"
            )
        else:
            status = "blocked_until_residual_adjacent_unlocked_local_lane_apply_ready"
            decision = (
                "hold_residual_adjacent_focused_local_validation_until_unlocked_lane_apply_ready"
            )
            supervisor_next_action = (
                "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
                if unlocked_status == "ready"
                else "apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external"
            )
    elif any_expected_failed:
        status = "failed"
        decision = (
            "repair_residual_adjacent_focused_local_validation_before_activation_review"
        )
        supervisor_next_action = (
            "repair_failed_residual_adjacent_focused_local_validation_commands"
        )
    elif all_expected_passed:
        status = "passed"
        decision = (
            "record_residual_adjacent_focused_local_validation_pass_and_keep_activation_external"
        )
        supervisor_next_action = (
            "keep_activation_external_after_residual_adjacent_focused_local_validation"
        )
    else:
        status = "ready"
        decision = (
            "run_residual_adjacent_focused_local_validation_with_body_free_command_hashes"
        )
        supervisor_next_action = (
            "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )

    export_commands = commands if status in {"ready", "passed", "failed"} else []
    export_hashes = command_hashes if status in {"ready", "passed", "failed"} else []
    export_results = result_rows if status in {"passed", "failed"} else []
    recorded = status in {"passed", "failed"}
    export_unlocked = unlocked_lanes if status in {"ready", "passed", "failed"} else []
    export_selected_lane = selected_lane if status in {"ready", "passed", "failed"} else "none"
    export_residual_id = _export_residual_selected_proposal_id(
        status,
        selected_residual_id,
        residual_active_statuses={"ready", "passed", "failed", "blocked"},
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_focused_local_validation"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-residual-adjacent-hy3-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        # No fortress proposal_id fallback while reverse-flow-waiting.
        "proposal_id": export_residual_id,
        "selected_proposal_id": export_residual_id,
        "selected_residual_proposal_id": export_residual_id,
        "selected_residual_route_class": str(
            unlocked_apply.get("selected_residual_route_class")
            or unlocked_apply.get("route_class")
            or "agent_harness_eval_required"
        )
        if export_residual_id
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_residual_id)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": "validate_one_residual_adjacent_unlocked_local_lane_candidate",
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "skill_route_discovery_residual_adjacent_focused_local_validation",
            "record_skill_route_discovery_residual_adjacent_focused_local_validation_results",
            "close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
        ],
        "residual_adjacent_unlocked_local_lane_apply_status": unlocked_status or "none",
        "residual_adjacent_unlocked_local_lane_apply_decision": unlocked_decision,
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": [
            lane
            for lane in list(
                unlocked_apply.get("allowed_local_lanes_after_local_comparison")
                or AGENT_HARNESS_EVAL_POST_COMPARE_LANES
            )
            if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        ]
        or list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES),
        "direct_allowed_lanes_before_eval": [],
        "selected_local_lane": export_selected_lane,
        "preferred_local_lane": "test",
        "unlocked_local_lanes": export_unlocked,
        "skill_route_unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_passed": bool(unlocked_apply.get("local_comparison_passed"))
        if status in {"ready", "passed", "failed"}
        else False,
        "focused_validation": {
            "status": status,
            "lane": export_selected_lane if status in {"ready", "passed", "failed"} else "none",
            "required": True,
            "commands": export_commands,
            "command_hashes": export_hashes,
            "command_results": export_results,
            "expected_command_count": len(export_hashes),
            "recorded_result_count": len(export_results),
            "unit_test_signal": status in {"ready", "passed", "failed"},
            "coverage_required": False,
            "results_cover_expected": results_cover_expected if export_results else False,
            "all_expected_passed": all_expected_passed if export_results else False,
            "recorded": recorded,
            "commands_exported": False,
        },
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": list(
            unlocked_apply.get("retained_boundary_proposal_ids") or []
        )
        if status in {"ready", "passed", "failed", "blocked", "not_applicable"}
        else [],
        "adjacent_general_agent_proposal_ids": list(
            unlocked_apply.get("adjacent_general_agent_proposal_ids") or []
        )
        if status in {"ready", "passed", "failed", "blocked"}
        else [],
        "residual_adjacent_harness_eval_available": bool(
            unlocked_apply.get("residual_adjacent_harness_eval_available")
        ),
        "residual_adjacent_handoff_surface": str(
            unlocked_apply.get("residual_adjacent_handoff_surface") or "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": bool(
            unlocked_apply.get("general_agent_isolation_passed", True)
        ),
        "privacy_isolation_passed": bool(
            unlocked_apply.get("privacy_isolation_passed", True)
        ),
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or unlocked_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest or str(unlocked_apply.get("source_digest") or ""),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": export_commands,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
        "focused_validation_recorded": recorded,
    }


def record_skill_route_discovery_residual_adjacent_focused_local_validation_results(
    pipeline: dict[str, Any],
    command_results: list[dict[str, Any]] | None = None,
    *,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Record body-free residual focused validation results onto a pipeline.

    After supervisors run residual unlocked-lane focused commands, call this with
    command-hash/boolean rows (or raw command text + ``passed``) to close
    ``skill_route_discovery_residual_adjacent_focused_local_validation`` from
    ``ready`` to ``passed``/``failed`` and refresh
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``
    and
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance``
    without enabling activation, push, promotion, provider launch, remote apply,
    external skill execution, or kernel restart.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    unlocked_apply = (
        pipeline.get("residual_adjacent_unlocked_local_lane_apply")
        if isinstance(pipeline.get("residual_adjacent_unlocked_local_lane_apply"), dict)
        else {}
    )
    prior_focused = (
        pipeline.get("residual_adjacent_focused_local_validation")
        if isinstance(pipeline.get("residual_adjacent_focused_local_validation"), dict)
        else {}
    )
    theme_pass = (
        pipeline.get("theme_pass") if isinstance(pipeline.get("theme_pass"), dict) else {}
    )
    theme = theme_window if isinstance(theme_window, dict) else {
        "theme_id": str(
            pipeline.get("theme_id") or theme_pass.get("theme_id") or "skill-route-discovery"
        ),
        "planned_passes": int(theme_pass.get("planned_passes") or 0),
        "target_passes": int(
            theme_pass.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES
        ),
        "status": str(theme_pass.get("status") or ""),
    }
    expected_hashes = [
        str(item)
        for item in list(
            (prior_focused.get("focused_validation") or {}).get("command_hashes")
            or (unlocked_apply.get("focused_validation") or {}).get("command_hashes")
            or []
        )
        if str(item).strip()
    ]
    prior_results = list(
        (prior_focused.get("focused_validation") or {}).get("command_results") or []
        if isinstance(prior_focused.get("focused_validation"), dict)
        else []
    )
    merged = merge_skill_route_discovery_focused_validation_command_results(
        prior_results,
        command_results,
        expected_command_hashes=expected_hashes,
    )
    digest = source_digest or str(
        prior_focused.get("source_digest") or unlocked_apply.get("source_digest") or ""
    )
    residual_focused = build_skill_route_discovery_residual_adjacent_focused_local_validation(
        residual_adjacent_unlocked_local_lane_apply=unlocked_apply,
        command_results=merged,
        theme_window=theme,
        source_digest=digest,
    )
    adjacent_rows = (
        list(pipeline.get("adjacent_general_agent_rows") or [])
        if isinstance(pipeline.get("adjacent_general_agent_rows"), list)
        else []
    )
    retained_rows = (
        list(pipeline.get("retained_boundaries") or [])
        if isinstance(pipeline.get("retained_boundaries"), list)
        else []
    )
    residual_activation_external = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff(
            residual_adjacent_focused_local_validation=residual_focused,
            residual_adjacent_unlocked_local_lane_apply=unlocked_apply,
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    residual_activation_external_acceptance = (
        build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance(
            residual_adjacent_focused_validation_activation_external_handoff=(
                residual_activation_external
            ),
            residual_adjacent_focused_local_validation=residual_focused,
            residual_adjacent_unlocked_local_lane_apply=unlocked_apply,
            adjacent_general_agent_rows=adjacent_rows,
            retained_boundaries=retained_rows,
            theme_window=theme,
            source_digest=digest,
        )
    )
    updated = dict(pipeline)
    updated["residual_adjacent_focused_local_validation"] = residual_focused
    updated["residual_adjacent_focused_local_validation_recorded"] = residual_focused.get(
        "status"
    ) in {"passed", "failed"}
    updated["residual_adjacent_focused_validation_activation_external_handoff"] = (
        residual_activation_external
    )
    updated["residual_adjacent_focused_validation_activation_external_acceptance"] = (
        residual_activation_external_acceptance
    )
    return attach_skill_route_discovery_pipeline_operator_state(updated)


def close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome(
    pipeline: dict[str, Any],
    *,
    passed: bool,
    source_digest: str | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Close ready residual focused validation with a body-free pass/fail outcome.

    Materializes expected command-hash rows for residual unlocked-lane focused
    validation, records them onto the pipeline, and keeps activation external.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")

    residual_focused = (
        pipeline.get("residual_adjacent_focused_local_validation")
        if isinstance(pipeline.get("residual_adjacent_focused_local_validation"), dict)
        else {}
    )
    unlocked_apply = (
        pipeline.get("residual_adjacent_unlocked_local_lane_apply")
        if isinstance(pipeline.get("residual_adjacent_unlocked_local_lane_apply"), dict)
        else {}
    )
    # Reuse reverse-flow body-free materializer; residual surface uses the same
    # focused_validation command_hash shape without exporting command text.
    materializer_source = residual_focused if residual_focused else unlocked_apply
    command_results = build_skill_route_discovery_focused_validation_body_free_command_results(
        materializer_source,
        passed=passed,
    )
    return record_skill_route_discovery_residual_adjacent_focused_local_validation_results(
        pipeline,
        command_results,
        source_digest=source_digest,
        theme_window=theme_window,
    )


def build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff(
    *,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    residual_adjacent_unlocked_local_lane_apply: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Package activation-external handoff after residual focused validation.

    After supervisors record body-free command-hash results on
    ``skill_route_discovery_residual_adjacent_focused_local_validation``, this
    surface turns
    ``keep_activation_external_after_residual_adjacent_focused_local_validation``
    into one operator-visible residual packet. A recorded pass keeps activation,
    push, promotion, provider launch, remote apply, external skill execution, and
    kernel restart denied. Reverse-flow skill unlocks stay closed
    (``skill_route_discovery_inherited=false``,
    ``skill_route_unlocked_local_lanes=[]``). Remaining residual fortress/Hy3
    proposal IDs may be noted without skill unlock inheritance. Distinct from
    reverse-flow
    ``skill_route_discovery_focused_validation_activation_external_handoff``.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    residual_focused = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )
    unlocked_apply = (
        residual_adjacent_unlocked_local_lane_apply
        if isinstance(residual_adjacent_unlocked_local_lane_apply, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    focused_status = str(residual_focused.get("status") or "")
    focused_validation = (
        residual_focused.get("focused_validation")
        if isinstance(residual_focused.get("focused_validation"), dict)
        else {}
    )
    recorded = bool(
        residual_focused.get("focused_validation_recorded")
        or focused_validation.get("recorded")
        or focused_status in {"passed", "failed"}
    )
    results_cover_expected = bool(focused_validation.get("results_cover_expected"))
    all_expected_passed = bool(focused_validation.get("all_expected_passed"))
    selected_residual_id = str(
        residual_focused.get("selected_residual_proposal_id")
        or residual_focused.get("selected_proposal_id")
        or residual_focused.get("proposal_id")
        or unlocked_apply.get("selected_residual_proposal_id")
        or unlocked_apply.get("selected_proposal_id")
        or unlocked_apply.get("proposal_id")
        or ""
    ).strip()
    selected_lane = str(
        residual_focused.get("selected_local_lane")
        or unlocked_apply.get("selected_local_lane")
        or ""
    ).strip()
    unlocked_lanes = [
        lane
        for lane in list(
            residual_focused.get("unlocked_local_lanes")
            or unlocked_apply.get("unlocked_local_lanes")
            or []
        )
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                residual_focused.get("retained_boundary_proposal_ids")
                or unlocked_apply.get("retained_boundary_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                residual_focused.get("adjacent_general_agent_proposal_ids")
                or unlocked_apply.get("adjacent_general_agent_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    remaining_residual_ids = [
        proposal_id
        for proposal_id in adjacent_ids
        if proposal_id and proposal_id != selected_residual_id
    ]
    skill_unlocks_closed = (
        list(residual_focused.get("skill_route_unlocked_local_lanes") or []) == []
        and residual_focused.get("skill_route_discovery_inherited") is not True
        and residual_focused.get("skill_route_unlocks_closed_for_residual") is not False
        and list(unlocked_apply.get("skill_route_unlocked_local_lanes") or []) == []
        and unlocked_apply.get("skill_route_discovery_inherited") is not True
    )
    result_rows = [
        row
        for row in list(focused_validation.get("command_results") or [])
        if isinstance(row, dict)
    ]
    body_free_results = (
        all(
            set(row.keys()) <= {"command_hash", "passed", "in_expected_set"}
            for row in result_rows
        )
        if result_rows
        else True
    )
    command_hashes = [
        str(item)
        for item in list(focused_validation.get("command_hashes") or [])
        if str(item).strip()
    ]
    general_agent_isolated = bool(
        residual_focused.get(
            "general_agent_isolation_passed",
            unlocked_apply.get("general_agent_isolation_passed", True),
        )
    )
    privacy_isolated = bool(
        residual_focused.get(
            "privacy_isolation_passed",
            unlocked_apply.get("privacy_isolation_passed", True),
        )
    )

    if focused_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = (
            "no_residual_adjacent_focused_local_validation_for_activation_external_handoff"
        )
        supervisor_next_action = (
            "wait_for_skill_route_discovery_residual_adjacent_focused_local_validation"
        )
    elif focused_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_focused_validation_ready"
        decision = (
            "hold_residual_adjacent_activation_external_handoff_until_focused_validation_ready"
        )
        if residual_focused.get("supervisor_next_action"):
            supervisor_next_action = str(residual_focused.get("supervisor_next_action"))
        else:
            supervisor_next_action = (
                "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
            )
    elif focused_status == "ready" or not recorded:
        status = "blocked_until_residual_adjacent_focused_validation_recorded"
        decision = (
            "hold_residual_adjacent_activation_external_handoff_until_command_hash_results_recorded"
        )
        supervisor_next_action = (
            "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )
    elif focused_status == "failed" or (recorded and not all_expected_passed):
        status = "blocked_until_residual_adjacent_focused_validation_repaired"
        decision = (
            "hold_residual_adjacent_activation_external_handoff_until_focused_validation_pass"
        )
        supervisor_next_action = (
            "repair_failed_residual_adjacent_focused_local_validation_commands"
        )
    elif not skill_unlocks_closed or not general_agent_isolated or not privacy_isolated:
        status = "blocked"
        decision = (
            "repair_residual_adjacent_isolation_before_activation_external_handoff"
        )
        supervisor_next_action = "repair_skill_route_config_gate_isolation"
    elif (
        focused_status == "passed"
        and recorded
        and results_cover_expected
        and all_expected_passed
        and body_free_results
        and bool(selected_residual_id)
        and bool(residual_focused.get("activation_external_only", True))
        and residual_focused.get("supervisor_activation_allowed") is not True
        and residual_focused.get("runtime_action", "none") == "none"
        and residual_focused.get("external_skill_execution_allowed") is not True
        and residual_focused.get("provider_launch_allowed") is not True
        and residual_focused.get("remote_apply_allowed") is not True
        and residual_focused.get("push_or_promotion_allowed") is not True
        and residual_focused.get("kernel_restart_allowed") is not True
    ):
        status = "ready"
        decision = (
            "package_activation_external_handoff_after_residual_adjacent_focused_validation_pass"
        )
        if remaining_residual_ids:
            supervisor_next_action = (
                "keep_activation_external_and_note_remaining_residual_adjacent_rows"
            )
        else:
            supervisor_next_action = (
                "keep_activation_external_after_residual_adjacent_focused_local_validation"
            )
    else:
        status = "blocked_until_residual_adjacent_focused_validation_pass"
        decision = (
            "hold_residual_adjacent_activation_external_handoff_until_body_free_pass_recorded"
        )
        supervisor_next_action = (
            "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )

    export_hashes = (
        command_hashes
        if status
        in {
            "ready",
            "blocked_until_residual_adjacent_focused_validation_repaired",
        }
        else []
    )
    export_results = (
        result_rows
        if status
        in {
            "ready",
            "blocked_until_residual_adjacent_focused_validation_repaired",
        }
        else []
    )
    export_selected_lane = (
        selected_lane if status in {"ready", "blocked_until_residual_adjacent_focused_validation_repaired"} else "none"
    )
    export_unlocked = unlocked_lanes if status == "ready" else []
    export_selected_residual = _export_residual_selected_proposal_id(
        status,
        selected_residual_id,
        residual_active_statuses={
            "ready",
            "blocked",
            "blocked_until_residual_adjacent_focused_validation_recorded",
            "blocked_until_residual_adjacent_focused_validation_repaired",
            "blocked_until_residual_adjacent_focused_validation_pass",
        },
    )
    export_adjacent_ids = adjacent_ids if status == "ready" else []
    export_remaining_ids = remaining_residual_ids if status == "ready" else []
    export_retained_ids = (
        retained_ids
        if status
        in {
            "ready",
            "blocked",
            "blocked_until_residual_adjacent_focused_validation_ready",
            "blocked_until_residual_adjacent_focused_validation_recorded",
            "blocked_until_residual_adjacent_focused_validation_repaired",
            "blocked_until_residual_adjacent_focused_validation_pass",
            "not_applicable",
        }
        else []
    )

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-residual-adjacent-hy3-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        # No fortress proposal_id fallback while reverse-flow-waiting
        # (blocked_until_residual_adjacent_focused_validation_ready).
        "proposal_id": export_selected_residual,
        "selected_proposal_id": export_selected_residual,
        "selected_residual_proposal_id": export_selected_residual,
        "selected_residual_route_class": str(
            residual_focused.get("selected_residual_route_class")
            or unlocked_apply.get("selected_residual_route_class")
            or residual_focused.get("route_class")
            or "agent_harness_eval_required"
        )
        if export_selected_residual
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_selected_residual)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": (
            "package_residual_adjacent_activation_external_handoff_after_focused_validation"
        ),
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "skill_route_discovery_residual_adjacent_focused_local_validation",
            "record_skill_route_discovery_residual_adjacent_focused_local_validation_results",
            "close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance",
        ],
        "residual_adjacent_focused_local_validation_status": focused_status or "none",
        "residual_adjacent_focused_local_validation_decision": str(
            residual_focused.get("decision") or ""
        ),
        "residual_adjacent_focused_local_validation_recorded": recorded,
        "residual_focused_validation_results_cover_expected": (
            results_cover_expected if recorded else False
        ),
        "residual_focused_validation_all_expected_passed": (
            all_expected_passed if recorded else False
        ),
        "residual_adjacent_unlocked_local_lane_apply_status": str(
            unlocked_apply.get("status") or "none"
        ),
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": [
            lane
            for lane in list(
                residual_focused.get("allowed_local_lanes_after_local_comparison")
                or unlocked_apply.get("allowed_local_lanes_after_local_comparison")
                or AGENT_HARNESS_EVAL_POST_COMPARE_LANES
            )
            if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        ]
        or list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES),
        "direct_allowed_lanes_before_eval": [],
        "selected_local_lane": export_selected_lane,
        "preferred_local_lane": "test",
        "unlocked_local_lanes": export_unlocked,
        "skill_route_unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_passed": bool(
            residual_focused.get(
                "local_comparison_passed",
                unlocked_apply.get("local_comparison_passed"),
            )
        )
        if status == "ready"
        else False,
        "focused_validation": {
            "status": focused_status or "none",
            "lane": export_selected_lane if status == "ready" else "none",
            "required": True,
            "command_hashes": export_hashes,
            "command_results": export_results,
            "expected_command_count": len(export_hashes),
            "recorded_result_count": len(export_results),
            "results_cover_expected": results_cover_expected if recorded else False,
            "all_expected_passed": all_expected_passed if recorded else False,
            "recorded": recorded,
            "body_free": body_free_results,
            # Commands intentionally omitted: residual activation-external handoff is hash-only.
            "commands_exported": False,
        },
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": export_retained_ids,
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "remaining_residual_adjacent_proposal_ids": export_remaining_ids,
        "remaining_residual_adjacent_available": bool(export_remaining_ids),
        "residual_adjacent_harness_eval_available": bool(export_adjacent_ids),
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_adjacent_ids else "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or residual_focused.get("theme_id")
            or unlocked_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (residual_focused.get("theme_pass") or {}).get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (residual_focused.get("theme_pass") or {}).get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (residual_focused.get("theme_pass") or {}).get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(
            residual_focused.get("source_digest")
            or unlocked_apply.get("source_digest")
            or ""
        ),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_focused_local_validation",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_focused_validation_activation_external",
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance(
    *,
    residual_adjacent_focused_validation_activation_external_handoff: dict[str, Any]
    | None = None,
    residual_adjacent_focused_local_validation: dict[str, Any] | None = None,
    residual_adjacent_unlocked_local_lane_apply: dict[str, Any] | None = None,
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    theme_window: dict[str, Any] | None = None,
    source_digest: str | None = None,
) -> dict[str, Any]:
    """Accept a ready residual activation-external handoff as the residual terminal package.

    After
    ``skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff``
    is ``ready`` from a recorded residual focused validation pass, this surface
    packages operator acceptance of
    ``keep_activation_external_after_residual_adjacent_focused_local_validation``
    without enabling push, promotion, provider launch, remote apply, external skill
    execution, or kernel restart. Remaining residual fortress/Hy3 proposal IDs may
    be noted without skill unlock inheritance. Distinct from reverse-flow
    ``skill_route_discovery_focused_validation_activation_external_acceptance``.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    handoff = (
        residual_adjacent_focused_validation_activation_external_handoff
        if isinstance(
            residual_adjacent_focused_validation_activation_external_handoff, dict
        )
        else {}
    )
    residual_focused = (
        residual_adjacent_focused_local_validation
        if isinstance(residual_adjacent_focused_local_validation, dict)
        else {}
    )
    unlocked_apply = (
        residual_adjacent_unlocked_local_lane_apply
        if isinstance(residual_adjacent_unlocked_local_lane_apply, dict)
        else {}
    )
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    handoff_status = str(handoff.get("status") or "")
    focused_status = str(
        residual_focused.get("status")
        or handoff.get("residual_adjacent_focused_local_validation_status")
        or ""
    )
    focused_validation = (
        residual_focused.get("focused_validation")
        if isinstance(residual_focused.get("focused_validation"), dict)
        else (
            handoff.get("focused_validation")
            if isinstance(handoff.get("focused_validation"), dict)
            else {}
        )
    )
    recorded = bool(
        residual_focused.get("focused_validation_recorded")
        or focused_validation.get("recorded")
        or handoff.get("residual_adjacent_focused_local_validation_recorded")
        or focused_status in {"passed", "failed"}
    )
    results_cover_expected = bool(
        focused_validation.get("results_cover_expected")
        or handoff.get("residual_focused_validation_results_cover_expected")
    )
    all_expected_passed = bool(
        focused_validation.get("all_expected_passed")
        or handoff.get("residual_focused_validation_all_expected_passed")
    )
    selected_residual_id = str(
        handoff.get("selected_residual_proposal_id")
        or handoff.get("selected_proposal_id")
        or handoff.get("proposal_id")
        or residual_focused.get("selected_residual_proposal_id")
        or residual_focused.get("selected_proposal_id")
        or residual_focused.get("proposal_id")
        or unlocked_apply.get("selected_residual_proposal_id")
        or unlocked_apply.get("selected_proposal_id")
        or unlocked_apply.get("proposal_id")
        or ""
    ).strip()
    selected_lane = str(
        handoff.get("selected_local_lane")
        or residual_focused.get("selected_local_lane")
        or unlocked_apply.get("selected_local_lane")
        or ""
    ).strip()
    unlocked_lanes = [
        lane
        for lane in list(
            handoff.get("unlocked_local_lanes")
            or residual_focused.get("unlocked_local_lanes")
            or unlocked_apply.get("unlocked_local_lanes")
            or []
        )
        if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
    ]
    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                handoff.get("retained_boundary_proposal_ids")
                or residual_focused.get("retained_boundary_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
        | {
            str(item)
            for item in list(
                handoff.get("adjacent_general_agent_proposal_ids")
                or residual_focused.get("adjacent_general_agent_proposal_ids")
                or []
            )
            if str(item).strip()
        }
    )
    remaining_residual_ids = [
        proposal_id
        for proposal_id in (
            list(handoff.get("remaining_residual_adjacent_proposal_ids") or [])
            or [
                proposal_id
                for proposal_id in adjacent_ids
                if proposal_id and proposal_id != selected_residual_id
            ]
        )
        if str(proposal_id).strip()
    ]
    remaining_residual_ids = [
        str(item).strip() for item in remaining_residual_ids if str(item).strip()
    ]
    # Deduplicate while preserving order.
    seen_remaining: set[str] = set()
    ordered_remaining: list[str] = []
    for proposal_id in remaining_residual_ids:
        if proposal_id in seen_remaining:
            continue
        seen_remaining.add(proposal_id)
        ordered_remaining.append(proposal_id)
    remaining_residual_ids = ordered_remaining
    skill_unlocks_closed = (
        list(handoff.get("skill_route_unlocked_local_lanes") or []) == []
        and handoff.get("skill_route_discovery_inherited") is not True
        and handoff.get("skill_route_unlocks_closed_for_residual") is not False
        and list(residual_focused.get("skill_route_unlocked_local_lanes") or []) == []
        and residual_focused.get("skill_route_discovery_inherited") is not True
        and list(unlocked_apply.get("skill_route_unlocked_local_lanes") or []) == []
        and unlocked_apply.get("skill_route_discovery_inherited") is not True
    )
    command_hashes = [
        str(item)
        for item in list(focused_validation.get("command_hashes") or [])
        if str(item).strip()
    ]
    result_rows = [
        row
        for row in list(focused_validation.get("command_results") or [])
        if isinstance(row, dict)
    ]
    body_free_results = (
        all(set(row.keys()) <= {"command_hash", "passed", "in_expected_set"} for row in result_rows)
        if result_rows
        else True
    )
    general_agent_isolated = bool(
        residual_focused.get(
            "general_agent_isolation_passed",
            handoff.get(
                "general_agent_isolation_passed",
                unlocked_apply.get("general_agent_isolation_passed", True),
            ),
        )
    )
    privacy_isolated = bool(
        residual_focused.get(
            "privacy_isolation_passed",
            handoff.get(
                "privacy_isolation_passed",
                unlocked_apply.get("privacy_isolation_passed", True),
            ),
        )
    )

    handoff_ready = (
        handoff_status == "ready"
        and str(handoff.get("decision") or "")
        == "package_activation_external_handoff_after_residual_adjacent_focused_validation_pass"
        and bool(handoff.get("activation_external_only", True))
        and handoff.get("supervisor_activation_allowed") is not True
        and handoff.get("runtime_action", "none") == "none"
        and handoff.get("external_skill_execution_allowed") is not True
        and handoff.get("provider_launch_allowed") is not True
        and handoff.get("remote_apply_allowed") is not True
        and handoff.get("push_or_promotion_allowed") is not True
        and handoff.get("kernel_restart_allowed") is not True
        and skill_unlocks_closed
        and general_agent_isolated
        and privacy_isolated
        and focused_status == "passed"
        and recorded
        and results_cover_expected
        and all_expected_passed
        and body_free_results
        and bool(selected_residual_id)
    )

    if handoff_status in {"", "not_applicable"}:
        status = "not_applicable"
        decision = "no_residual_adjacent_activation_external_handoff_for_acceptance"
        supervisor_next_action = (
            "wait_for_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
        )
    elif handoff_status.startswith("blocked"):
        status = "blocked_until_residual_adjacent_activation_external_handoff_ready"
        decision = (
            "hold_residual_adjacent_activation_external_acceptance_until_handoff_ready"
        )
        # Prefer residual handoff's cascaded next action. Without this, acceptance
        # defaulted to repair_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff
        # while reverse-flow focused validation was still unrecorded, which
        # poisoned operator-visible supervisor_next via pipeline render priority.
        handoff_next = str(handoff.get("supervisor_next_action") or "").strip()
        if handoff_next:
            supervisor_next_action = handoff_next
        elif handoff_status == "blocked_until_residual_adjacent_focused_validation_recorded":
            supervisor_next_action = (
                "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
            )
        elif handoff_status == "blocked_until_residual_adjacent_focused_validation_repaired":
            supervisor_next_action = (
                "repair_failed_residual_adjacent_focused_local_validation_commands"
            )
        elif handoff_status == "blocked":
            # Isolation or gate failure on residual handoff itself.
            supervisor_next_action = (
                "repair_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
            )
        else:
            supervisor_next_action = (
                "wait_for_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff"
            )
    elif handoff_ready:
        status = "accepted"
        decision = (
            "accept_activation_external_package_after_residual_adjacent_focused_validation_pass"
        )
        if remaining_residual_ids:
            supervisor_next_action = (
                "keep_activation_external_and_note_remaining_residual_adjacent_rows"
            )
        else:
            supervisor_next_action = (
                "keep_activation_external_after_residual_adjacent_focused_local_validation"
            )
    else:
        status = "blocked_until_residual_adjacent_activation_external_handoff_ready"
        decision = (
            "hold_residual_adjacent_activation_external_acceptance_until_body_free_pass_handoff"
        )
        supervisor_next_action = (
            "run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external"
        )

    export_hashes = command_hashes if status == "accepted" else []
    export_results = result_rows if status == "accepted" else []
    export_unlocked = unlocked_lanes if status == "accepted" else []
    export_selected_lane = selected_lane if status == "accepted" else "none"
    # Residual acceptance may advertise selection only when residual-active:
    # accepted, or blocked waiting on a residual-active handoff (not reverse-flow-
    # waiting handoff statuses such as blocked_until_focused_validation_ready).
    handoff_is_residual_active = handoff_status in {
        "ready",
        "blocked",
        "blocked_until_residual_adjacent_focused_validation_recorded",
        "blocked_until_residual_adjacent_focused_validation_repaired",
        "blocked_until_residual_adjacent_focused_validation_pass",
    }
    export_selected_residual = selected_residual_id if status == "accepted" else ""
    if (
        not export_selected_residual
        and status == "blocked_until_residual_adjacent_activation_external_handoff_ready"
        and handoff_is_residual_active
    ):
        export_selected_residual = selected_residual_id
    export_adjacent_ids = adjacent_ids if status == "accepted" else []
    export_remaining_ids = remaining_residual_ids if status == "accepted" else []
    export_retained_ids = retained_ids if status in {
        "accepted",
        "blocked_until_residual_adjacent_activation_external_handoff_ready",
        "not_applicable",
    } else []

    return {
        "schema_version": 1,
        "controller_surface": (
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance"
        ),
        "proposal_track": "prop-residual-adjacent-fortress-harness-eval",
        "legacy_proposal_tracks": [
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
        ],
        "companion_tracks": [
            "prop-residual-adjacent-fortress-harness-eval",
            "prop-residual-adjacent-hy3-harness-eval",
            "prop-harness-residual-hy3-fortress",
            "prop-harness-fortress-hy3-adjacent-eval",
            "prop-harness-fortress-adjacent-eval",
            "prop-harness-hy3-adjacent-eval",
            "prop-skill-reverse-flow-continue-local-validation",
            "prop-skill-rnskill-docs-companion",
            "prop-skill-pipeline-config-gates",
        ],
        "proposal_id": export_selected_residual,
        "selected_proposal_id": export_selected_residual,
        "selected_residual_proposal_id": export_selected_residual,
        "selected_residual_route_class": str(
            handoff.get("selected_residual_route_class")
            or residual_focused.get("selected_residual_route_class")
            or unlocked_apply.get("selected_residual_route_class")
            or residual_focused.get("route_class")
            or "agent_harness_eval_required"
        )
        if export_selected_residual
        else "none",
        "residual_selection_held_until_residual_active": not bool(export_selected_residual)
        and bool(selected_residual_id),
        "status": status,
        "decision": decision,
        "capability_action": (
            "accept_residual_adjacent_activation_external_package_after_focused_validation"
        ),
        "capability_pipeline": [
            "skill_route_discovery_capability_pipeline",
            "skill_route_discovery_local_comparison",
            "skill_route_discovery_reverse_flow_test_validation_lane",
            "skill_route_discovery_rnskill_docs_validation_lane",
            "skill_route_discovery_config_gate_boundary",
            "skill_route_discovery_local_apply",
            "skill_route_discovery_local_apply_completion",
            "skill_route_discovery_unlocked_local_test_lane_apply",
            "skill_route_discovery_focused_local_test_validation",
            "record_skill_route_discovery_focused_local_test_validation_results",
            "close_skill_route_discovery_focused_local_test_validation_with_outcome",
            "skill_route_discovery_focused_validation_activation_external_handoff",
            "skill_route_discovery_focused_validation_activation_external_acceptance",
            "skill_route_discovery_focused_validation_residual_adjacent_queue",
            "skill_route_discovery_residual_adjacent_harness_eval_local_apply",
            "skill_route_discovery_residual_adjacent_harness_eval_local_comparison",
            "skill_route_discovery_residual_adjacent_unlocked_local_lane_apply",
            "skill_route_discovery_residual_adjacent_focused_local_validation",
            "record_skill_route_discovery_residual_adjacent_focused_local_validation_results",
            "close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff",
            "skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance",
        ],
        "residual_adjacent_focused_validation_activation_external_handoff_status": (
            handoff_status or "none"
        ),
        "residual_adjacent_focused_validation_activation_external_handoff_decision": str(
            handoff.get("decision") or ""
        ),
        "residual_adjacent_focused_local_validation_status": focused_status or "none",
        "residual_adjacent_focused_local_validation_decision": str(
            residual_focused.get("decision") or handoff.get(
                "residual_adjacent_focused_local_validation_decision"
            )
            or ""
        ),
        "residual_adjacent_focused_local_validation_recorded": recorded,
        "residual_focused_validation_results_cover_expected": (
            results_cover_expected if recorded else False
        ),
        "residual_focused_validation_all_expected_passed": (
            all_expected_passed if recorded else False
        ),
        "residual_adjacent_unlocked_local_lane_apply_status": str(
            unlocked_apply.get("status")
            or handoff.get("residual_adjacent_unlocked_local_lane_apply_status")
            or "none"
        ),
        "route_class": "agent_harness_eval_required",
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": [
            lane
            for lane in list(
                handoff.get("allowed_local_lanes_after_local_comparison")
                or residual_focused.get("allowed_local_lanes_after_local_comparison")
                or unlocked_apply.get("allowed_local_lanes_after_local_comparison")
                or AGENT_HARNESS_EVAL_POST_COMPARE_LANES
            )
            if lane in AGENT_HARNESS_EVAL_POST_COMPARE_LANES
        ]
        or list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES),
        "direct_allowed_lanes_before_eval": [],
        "selected_local_lane": export_selected_lane,
        "preferred_local_lane": "test",
        "unlocked_local_lanes": export_unlocked,
        "skill_route_unlocked_local_lanes": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed_for_residual": True,
        "local_validation_required": True,
        "local_comparison_required": True,
        "local_comparison_passed": bool(
            residual_focused.get(
                "local_comparison_passed",
                handoff.get(
                    "local_comparison_passed",
                    unlocked_apply.get("local_comparison_passed"),
                ),
            )
        )
        if status == "accepted"
        else False,
        "focused_validation": {
            "status": focused_status or "none",
            "lane": export_selected_lane if status == "accepted" else "none",
            "required": True,
            "command_hashes": export_hashes,
            "command_results": export_results,
            "expected_command_count": len(export_hashes),
            "recorded_result_count": len(export_results),
            "results_cover_expected": results_cover_expected if recorded else False,
            "all_expected_passed": all_expected_passed if recorded else False,
            "recorded": recorded,
            "body_free": body_free_results,
            # Commands intentionally omitted: residual acceptance is hash-only.
            "commands_exported": False,
        },
        "activation_external_only": True,
        "supervisor_activation_allowed": False,
        "retained_boundary_proposal_ids": export_retained_ids,
        "adjacent_general_agent_proposal_ids": export_adjacent_ids,
        "remaining_residual_adjacent_proposal_ids": export_remaining_ids,
        "remaining_residual_adjacent_available": bool(export_remaining_ids),
        "residual_adjacent_harness_eval_available": bool(export_adjacent_ids),
        "residual_adjacent_handoff_surface": (
            "agent_harness_eval_cluster_local_apply" if export_adjacent_ids else "none"
        ),
        "general_agent_inherits_skill_unlock": False,
        "privacy_rows_selectable_for_local_apply": False,
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "theme_id": str(
            theme.get("theme_id")
            or handoff.get("theme_id")
            or residual_focused.get("theme_id")
            or unlocked_apply.get("theme_id")
            or "skill-route-discovery"
        ),
        "theme_pass": {
            "planned_passes": int(
                theme.get("planned_passes")
                or (handoff.get("theme_pass") or {}).get("planned_passes")
                or (residual_focused.get("theme_pass") or {}).get("planned_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("planned_passes")
                or 0
            ),
            "target_passes": int(
                theme.get("target_passes")
                or (handoff.get("theme_pass") or {}).get("target_passes")
                or (residual_focused.get("theme_pass") or {}).get("target_passes")
                or (unlocked_apply.get("theme_pass") or {}).get("target_passes")
                or DEFAULT_THEME_WINDOW_TARGET_PASSES
            ),
            "status": str(
                theme.get("status")
                or (handoff.get("theme_pass") or {}).get("status")
                or (residual_focused.get("theme_pass") or {}).get("status")
                or (unlocked_apply.get("theme_pass") or {}).get("status")
                or ""
            ),
        },
        "source_digest": source_digest
        or str(
            handoff.get("source_digest")
            or residual_focused.get("source_digest")
            or unlocked_apply.get("source_digest")
            or ""
        ),
        "supervisor_next_action": supervisor_next_action,
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_focused_local_validation",
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_residual_adjacent_focused_validation_activation_external",
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
        "raw_command_stdout_exported": False,
        "raw_command_text_exported": False,
    }


def build_skill_route_discovery_adjacent_harness_eval_handoff(
    *,
    selected_step: dict[str, Any],
    adjacent_general_agent_rows: list[dict[str, Any]] | None = None,
    retained_boundaries: list[dict[str, Any]] | None = None,
    local_comparison: dict[str, Any] | None = None,
    local_apply: dict[str, Any] | None = None,
    local_apply_completion: dict[str, Any] | None = None,
    theme_window: dict[str, Any] | None = None,
) -> dict[str, Any]:
    """Operator-visible handoff from skill-route residual rows to agent harness-eval.

    When the skill-route pipeline selects a fortress-style
    ``agent_harness_eval_required`` row (no reverse-flow skill candidate remains),
    do not fail skill-route local comparison or demand reverse-flow repair. Instead
    emit a body-free handoff that queues
    ``agent_harness_eval_cluster_local_apply`` for documentation/test/code_patch
    after harness-eval criteria pass. Privacy/offensive rows stay review-only;
    runtime action, provider launch, remote apply, and skill unlocks stay denied.
    """

    theme = theme_window if isinstance(theme_window, dict) else {}
    selected = selected_step if isinstance(selected_step, dict) else {}
    adjacent = list(adjacent_general_agent_rows or [])
    retained = list(retained_boundaries or [])
    comparison = local_comparison if isinstance(local_comparison, dict) else {}
    apply = local_apply if isinstance(local_apply, dict) else {}
    completion = local_apply_completion if isinstance(local_apply_completion, dict) else {}

    selected_route_class = str(selected.get("route_class") or "")
    selected_proposal_id = str(selected.get("proposal_id") or "")
    selected_is_adjacent = selected_route_class == "agent_harness_eval_required"
    general_agent_isolated = all(
        row.get("skill_route_discovery_inherited") is False
        and list(row.get("direct_allowed_lanes_before_eval") or []) == []
        for row in adjacent
    ) if adjacent else True
    privacy_isolated = all(
        str(row.get("route_class") or "")
        in {"privacy_boundary_review_only", "offensive_boundary_review_only"}
        for row in retained
    ) if retained else True
    skill_unlocks_closed = list(selected.get("unlocked_local_lanes") or []) == [] and list(
        apply.get("unlocked_local_lanes") or []
    ) == []

    if not selected_proposal_id:
        status = "not_applicable"
        decision = "no_selected_step_for_adjacent_harness_eval_handoff"
        supervisor_next_action = "wait_for_skill_route_or_adjacent_harness_selection"
    elif not selected_is_adjacent:
        status = "not_applicable"
        decision = "selected_step_is_not_adjacent_agent_harness_eval"
        supervisor_next_action = "continue_skill_route_discovery_pipeline"
    elif not general_agent_isolated or not privacy_isolated:
        status = "blocked"
        decision = "repair_adjacent_isolation_before_harness_eval_handoff"
        supervisor_next_action = "repair_skill_route_config_gate_isolation"
    elif not skill_unlocks_closed:
        status = "blocked"
        decision = "close_skill_route_unlocks_before_adjacent_harness_handoff"
        supervisor_next_action = "repair_skill_route_discovery_local_apply_before_handoff"
    else:
        status = "ready"
        decision = (
            "queue_selected_general_agent_row_for_agent_harness_eval_local_comparison"
        )
        supervisor_next_action = (
            "run_agent_harness_eval_local_comparison_for_selected_general_agent_row"
        )

    retained_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in retained
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )
    adjacent_ids = sorted(
        {
            str(row.get("proposal_id") or "")
            for row in adjacent
            if isinstance(row, dict) and str(row.get("proposal_id") or "").strip()
        }
    )

    return {
        "schema_version": 1,
        "controller_surface": "skill_route_discovery_adjacent_harness_eval_handoff",
        "proposal_track": selected_proposal_id or "prop-harness-fortress-local-eval",
        "status": status,
        "decision": decision,
        "selected_proposal_id": selected_proposal_id,
        "route_class": selected_route_class or "none",
        "capability_action": str(
            selected.get("capability_action")
            or "queue_general_agent_project_for_harness_eval"
        ),
        "evaluation_lane": "agent_harness_eval_required",
        "handoff_controller_surface": "agent_harness_eval_cluster_local_apply",
        "handoff_completion_surface": "agent_harness_eval_cluster_local_apply_completion",
        "allowed_local_lanes_after_local_comparison": list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES),
        "direct_allowed_lanes_before_eval": [],
        "skill_route_discovery_inherited": False,
        "skill_route_unlocks_closed": skill_unlocks_closed,
        "local_comparison_required": True,
        "local_comparison_status": str(
            selected.get("local_comparison_status")
            or comparison.get("status")
            or "not_applicable"
        ),
        "skill_route_local_comparison_status": str(comparison.get("status") or "none"),
        "skill_route_local_apply_status": str(apply.get("status") or "none"),
        "skill_route_local_apply_completion_status": str(completion.get("status") or "none"),
        "general_agent_isolation_passed": general_agent_isolated,
        "privacy_isolation_passed": privacy_isolated,
        "adjacent_general_agent_proposal_ids": adjacent_ids,
        "retained_boundary_proposal_ids": retained_ids,
        "runtime_action": "none",
        "external_skill_execution_allowed": False,
        "provider_launch_allowed": False,
        "remote_apply_allowed": False,
        "push_or_promotion_allowed": False,
        "kernel_restart_allowed": False,
        "supervisor_activation_allowed": False,
        "supervisor_next_action": supervisor_next_action,
        "theme_id": str(theme.get("theme_id") or "skill-route-discovery"),
        "theme_pass": {
            "planned_passes": int(theme.get("planned_passes") or 0),
            "target_passes": int(theme.get("target_passes") or DEFAULT_THEME_WINDOW_TARGET_PASSES),
            "status": str(theme.get("status") or ""),
        },
        "required_validation": [
            "pytest tests/test_github_growth.py -q -k skill_route_discovery_capability_pipeline",
            ADJACENT_HARNESS_EVAL_VALIDATION_COMMAND,
        ],
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def classify_skill_route_discovery_capability_route(proposal: dict[str, Any]) -> dict[str, Any]:
    """Classify one proposal into the skill-route capability pipeline without raw URL export."""

    proposal_id = str(proposal.get("proposal_id") or "").strip()
    kind = str(proposal.get("kind") or "no_action").strip() or "no_action"
    implementation_scope = str(proposal.get("implementation_scope") or "").strip()
    validation_gate = str(proposal.get("validation_gate") or "").strip()
    risk_flags = sorted({str(flag) for flag in proposal.get("risk_flags", []) if str(flag).strip()})
    evidence_url_hashes = sorted(
        {
            hashlib.sha256(str(url).strip().encode("utf-8")).hexdigest()[:16]
            for url in proposal.get("evidence_urls", [])
            if str(url).strip()
        }
    )
    summary_text = " ".join(
        str(proposal.get(key) or "")
        for key in (
            "summary",
            "kind",
            "validation_task",
            "rationale",
            "recommended_action",
            "proposal_id",
        )
    ).lower()
    evidence_blob = " ".join(str(url).lower() for url in proposal.get("evidence_urls", []) if str(url).strip())
    blob = f"{proposal_id.lower()} {summary_text} {evidence_blob}"

    preflight = proposal_validation_preflight(proposal)
    autonomous_text = autonomous_local_apply_text(proposal)
    autonomous_allowed = autonomous_text == "True" and preflight.get("status") == "ready"

    route_profiles = skill_route_discovery_route_profiles_for_text(blob)
    skill_route_discovery_first = "codex_workflow_gate" in route_profiles
    preferred_lane = skill_route_discovery_preferred_lane(route_profiles, kind)
    allowed_lanes = list(SKILL_ROUTE_DISCOVERY_ALLOWED_LANES)

    if "offensive-behavior" in risk_flags or validation_gate == "offensive-behavior-human-review":
        route_class = "offensive_boundary_review_only"
        capability_action = "retain_offensive_behavior_review_boundary"
        preferred_lane = "none"
        allowed_lanes = []
        route_profiles = []
        skill_route_discovery_first = False
        selection_reason = "offensive_behavior_remains_review_only"
        status = "blocked_by_safety_boundary"
        autonomous_allowed = False
    elif (
        "privacy-leakage" in risk_flags
        or validation_gate == "privacy-leakage-human-review"
        or any(marker in blob for marker in _SKILL_ROUTE_AGENT_CHIEF_MARKERS)
    ):
        route_class = "privacy_boundary_review_only"
        capability_action = "retain_privacy_leakage_review_boundary"
        preferred_lane = "none"
        allowed_lanes = []
        route_profiles = []
        skill_route_discovery_first = False
        selection_reason = "privacy_leakage_or_agent_chief_remains_review_only"
        status = "blocked_by_safety_boundary"
        autonomous_allowed = False
    elif (
        any(marker in blob for marker in _SKILL_ROUTE_FORTRESS_MARKERS)
        or any(marker in blob for marker in _SKILL_ROUTE_HY3_MARKERS)
        or any(marker in blob for marker in _SKILL_ROUTE_GENERAL_AGENT_HARNESS_MARKERS)
        or (
            "general_agent" in blob
            and not any(marker in blob for marker in _SKILL_ROUTE_REVERSE_FLOW_MARKERS)
            and not any(marker in blob for marker in _SKILL_ROUTE_RNSKILL_MARKERS)
            and not any(marker in blob for marker in _SKILL_ROUTE_GENERIC_SKILL_MARKERS)
        )
    ):
        route_class = "agent_harness_eval_required"
        capability_action = "queue_general_agent_project_for_harness_eval"
        preferred_lane = "none"
        allowed_lanes = list(AGENT_HARNESS_EVAL_POST_COMPARE_LANES)
        route_profiles = []
        skill_route_discovery_first = False
        selection_reason = "general_agent_project_requires_agent_harness_eval"
        status = "adjacent_harness_eval"
        autonomous_allowed = False
    elif route_profiles or any(marker in blob for marker in _SKILL_ROUTE_GENERIC_SKILL_MARKERS):
        if not route_profiles:
            route_profiles = ["generic_skill_workflow"]
            preferred_lane = skill_route_discovery_preferred_lane(route_profiles, kind)
        route_class = "skill_route_discovery"
        capability_action = "apply_one_local_skill_route_validation_candidate"
        selection_reason = (
            "codex_workflow_gate_skill_route_discovery_first"
            if skill_route_discovery_first
            else "generic_skill_workflow_skill_route_discovery"
        )
        if implementation_scope == "local_validation_candidate" and preflight.get("status") == "ready":
            status = "ready"
        elif implementation_scope == "local_validation_candidate":
            status = "validation_gap"
            autonomous_allowed = False
            selection_reason = "skill_route_candidate_has_preflight_gaps"
        else:
            status = "ready" if not risk_flags else "risk_review"
            if risk_flags:
                autonomous_allowed = False
                selection_reason = "skill_route_candidate_has_non_hard_risk_flags"
    elif implementation_scope == "reviewable_proposal_only" or kind in {"follow_up_issue", "no_action"}:
        route_class = "follow_up_only"
        capability_action = "preserve_follow_up_without_skill_route_apply"
        preferred_lane = "none"
        allowed_lanes = []
        route_profiles = []
        skill_route_discovery_first = False
        selection_reason = "proposal_is_follow_up_or_reviewable_only"
        status = "follow_up_only"
        autonomous_allowed = False
    else:
        route_class = "follow_up_only"
        capability_action = "preserve_follow_up_without_skill_route_apply"
        preferred_lane = _local_lane_from_proposal_kind(kind)
        allowed_lanes = []
        route_profiles = []
        skill_route_discovery_first = False
        selection_reason = "no_mapped_skill_route_capability"
        status = "follow_up_only"
        autonomous_allowed = False

    return {
        "proposal_id": proposal_id,
        "route_class": route_class,
        "capability_action": capability_action,
        "route_profiles": list(route_profiles),
        "preferred_local_lane": preferred_lane,
        "allowed_local_lanes": list(allowed_lanes),
        "skill_route_discovery_first": skill_route_discovery_first,
        "kind": kind,
        "implementation_scope": implementation_scope,
        "validation_gate": validation_gate,
        "risk_flags": risk_flags,
        "evidence_url_hashes": evidence_url_hashes,
        "local_comparison_required": route_class == "skill_route_discovery",
        "autonomous_local_apply_allowed": autonomous_allowed,
        "autonomous_local_apply_text": autonomous_text,
        "validation_preflight_status": str(preflight.get("status") or ""),
        "validation_gaps": list(preflight.get("validation_gaps") or []),
        "selection_reason": selection_reason,
        "status": status,
        "selected": False,
        "runtime_action": "none",
        "external_skill_activation_allowed": False,
    }


def skill_route_discovery_route_profiles_for_text(blob: str) -> list[str]:
    """Infer route profiles from proposal text and evidence refs without exporting URLs."""

    profiles: list[str] = []
    if any(marker in blob for marker in _SKILL_ROUTE_REVERSE_FLOW_MARKERS):
        profiles.append("codex_workflow_gate")
    if any(marker in blob for marker in _SKILL_ROUTE_RNSKILL_MARKERS):
        profiles.append("generic_skill_workflow")
    if "codex_workflow_gate" in profiles and "generic_skill_workflow" not in profiles:
        # reverse-flow also carries generic skill-workflow framing in public evidence
        if "reverse-flow" in blob or "reverse_flow" in blob or "reverse flow" in blob:
            profiles.append("generic_skill_workflow")
    if not profiles and any(marker in blob for marker in _SKILL_ROUTE_GENERIC_SKILL_MARKERS):
        profiles.append("generic_skill_workflow")
    # stable unique order
    ordered = []
    for profile in ("codex_workflow_gate", "generic_skill_workflow"):
        if profile in profiles and profile not in ordered:
            ordered.append(profile)
    for profile in profiles:
        if profile not in ordered:
            ordered.append(profile)
    return ordered


def skill_route_discovery_preferred_lane(route_profiles: list[str], kind: str = "") -> str:
    """Prefer test for codex workflow gates and documentation for generic skill collections."""

    _ = kind  # profile preference is authoritative for skill-route local lanes
    if "codex_workflow_gate" in route_profiles:
        return "test"
    return "documentation"


def select_skill_route_discovery_capability_step(
    candidate_rows: list[dict[str, Any]],
) -> dict[str, Any] | None:
    """Pick one next skill-route local apply candidate from classified rows."""

    if not candidate_rows:
        return None

    priority = {
        "skill_route_discovery": 0,
        "agent_harness_eval_required": 1,
        "follow_up_only": 2,
        "privacy_boundary_review_only": 3,
        "offensive_boundary_review_only": 4,
    }

    def _specificity_rank(row: dict[str, Any]) -> int:
        proposal_id = str(row.get("proposal_id") or "").casefold()
        # Prefer concrete reverse-flow test validation candidates, then reverse-flow
        # trend tokens, then rnskill, then umbrella skill-route pipeline props.
        if (
            "skill-pipeline-reverse-flow" in proposal_id
            or "skill_pipeline_reverse_flow" in proposal_id
            or "skill-reverse-flow-test" in proposal_id
            or "skill_reverse_flow_test" in proposal_id
            or "reverse-flow-test" in proposal_id
            or "reverse_flow_test" in proposal_id
        ):
            return 0
        if "reverse-flow-skill" in proposal_id or "reverse_flow_skill" in proposal_id:
            return 1
        if "reverse-flow" in proposal_id or "reverse_flow" in proposal_id:
            return 1
        if "rnskill" in proposal_id:
            return 2
        if "skill-route-discovery" in proposal_id or "skill_route_discovery" in proposal_id:
            return 3
        if "skill-pipeline" in proposal_id or "skill_pipeline" in proposal_id:
            return 3
        return 4

    def _row_rank(row: dict[str, Any]) -> tuple[Any, ...]:
        profiles = list(row.get("route_profiles") or [])
        # Prefer reverse-flow / codex_workflow_gate over generic rnskill collections.
        codex_rank = 0 if "codex_workflow_gate" in profiles else 1
        status_rank = 0 if row.get("status") == "ready" else 1
        return (
            priority.get(str(row.get("route_class") or ""), 99),
            codex_rank,
            status_rank,
            _specificity_rank(row),
            str(row.get("proposal_id") or ""),
        )

    ranked = sorted(candidate_rows, key=_row_rank)
    selected = ranked[0]
    # Only select skill-route rows for local apply; otherwise surface the top
    # non-apply outcome (adjacent harness eval or retained boundary).
    skill_ready = [
        row
        for row in ranked
        if row.get("route_class") == "skill_route_discovery" and row.get("status") in {"ready", "validation_gap", "risk_review"}
    ]
    if skill_ready:
        return skill_ready[0]
    return selected


def push_message_text(signal: GrowthSignal) -> str:
    return " ".join(f"{signal.title} {signal.relevance_reason}".lower().split())


def stable_push_message_hash(text: str) -> str:
    return hashlib.sha256(" ".join(text.split()).encode("utf-8")).hexdigest()[:16]


def render_markdown_digest(digest: dict[str, Any]) -> str:
    lines = [
        "# GitHub Growth Digest",
        "",
        f"Digest: `{digest['digest_id']}`",
        f"Generated: {digest['generated_at']}",
        "",
    ]
    source = digest.get("source")
    if source:
        lines.extend(
            [
                "## Trend Source",
                "",
                f"- Kind: `{source.get('kind', 'unknown')}`",
                f"- Query: `{source.get('query', '')}`",
                f"- Sort: `{source.get('sort', '')} {source.get('order', '')}`",
                f"- Total matches: {source.get('total_count', 0)}",
                f"- Incomplete results: {source.get('incomplete_results', False)}",
                "",
            ]
        )
        event_fetch_errors = source.get("event_fetch_errors") or []
        if event_fetch_errors:
            lines.extend(["### Event Fetch Warnings", ""])
            for error in event_fetch_errors:
                lines.append(f"- `{error.get('repo', 'unknown')}`: {error.get('error', '')}")
            lines.append("")
        repositories = source.get("repositories") or []
        if repositories:
            lines.extend(["### Trending Repositories", ""])
            for repository in repositories:
                delta = repository.get("star_delta_since_last_run")
                delta_text = "unknown" if delta is None else f"{delta:+d}"
                first_seen = "new" if repository.get("first_seen") else "seen"
                lines.append(
                    "- "
                    f"[{repository['full_name']}]({repository['html_url']}) "
                    f"- {repository.get('stargazers_count', 0)} stars ({delta_text}), "
                    f"{repository.get('language') or 'unknown language'}, {first_seen}"
                )
            lines.append("")
    lines.extend(["## Sources", ""])
    for repo in digest["repositories"]:
        lines.append(f"- `{repo}`")
    lines.extend(["", "## Items", ""])
    if not digest["items"]:
        lines.append("No new growth signals matched this pass.")
    for item in digest["items"]:
        risk = ", ".join(item.get("risk_flags") or ["none"])
        lines.extend(
            [
                f"- [{item['summary']}]({item['source_url']})",
                f"  - Event: `{item['event_kind']}`",
                f"  - Relevance: {item['relevance_reason']}",
                f"  - Risk flags: {risk}",
                f"  - Confidence: {item['confidence']:.2f}",
            ]
        )
    lines.extend(["", "## Proposals", ""])
    if not digest["proposals"]:
        lines.append("No proposals generated.")
    for proposal in digest["proposals"]:
        evidence = ", ".join(proposal.get("evidence_urls") or ["no external URL"])
        lines.extend(
            [
                f"- `{proposal['kind']}`: {proposal['summary']}",
                f"  - Evidence: {evidence}",
                f"  - Autonomous local apply: {autonomous_local_apply_text(proposal)}",
            ]
        )
        implementation_scope = str(proposal.get("implementation_scope") or "").strip()
        if implementation_scope:
            lines.append(f"  - Implementation scope: `{implementation_scope}`")
        validation_gate = str(proposal.get("validation_gate") or "").strip()
        if validation_gate:
            lines.append(f"  - Validation gate: `{validation_gate}`")
        validation_task = str(proposal.get("validation_task") or "").strip()
        if validation_task:
            lines.append(f"  - Validation task: {validation_task}")
    capability_step = digest.get("upstream_evidence_capability_step")
    if isinstance(capability_step, dict):
        lines.extend(["", "## Upstream Evidence Capability Step", ""])
        selected = capability_step.get("selected_step") if isinstance(capability_step.get("selected_step"), dict) else {}
        lines.extend(
            [
                f"- Status: `{capability_step.get('status', '')}`",
                f"- Theme: `{capability_step.get('theme_id', '')}`",
                f"- Selected proposal: `{selected.get('proposal_id') or 'none'}`",
                f"- Route class: `{selected.get('route_class') or 'none'}`",
                f"- Capability action: `{selected.get('capability_action') or 'none'}`",
                f"- Local lane: `{selected.get('local_lane') or 'none'}`",
                f"- Requires local compare before draft: `{bool(selected.get('requires_local_compare_before_draft'))}`",
                f"- Autonomous local apply: `{bool(selected.get('autonomous_local_apply'))}`",
                f"- Runtime action: `{capability_step.get('runtime_action', 'none')}`",
                f"- Raw evidence URLs exported: `{bool(capability_step.get('raw_evidence_urls_exported'))}`",
                f"- Privacy export allowed: `{bool(capability_step.get('privacy_export_allowed'))}`",
            ]
        )
        retained = capability_step.get("retained_boundaries") or []
        if retained:
            lines.append("- Retained review-only boundaries:")
            for boundary in retained:
                if not isinstance(boundary, dict):
                    continue
                lines.append(
                    "  - "
                    f"`{boundary.get('proposal_id') or 'unknown'}` "
                    f"(`{boundary.get('route_class') or 'unknown'}` / "
                    f"`{boundary.get('validation_gate') or 'none'}`)"
                )
        lines.append(f"- Promotion rule: {capability_step.get('promotion_rule', '')}")
    skill_pipeline = digest.get("skill_route_discovery_capability_pipeline")
    if isinstance(skill_pipeline, dict) and skill_pipeline:
        lines.extend(["", "## Skill Route Discovery Capability Pipeline", ""])
        selected = (
            skill_pipeline.get("selected_step")
            if isinstance(skill_pipeline.get("selected_step"), dict)
            else {}
        )
        lines.extend(
            [
                f"- Status: `{skill_pipeline.get('status', '')}`",
                f"- Theme: `{skill_pipeline.get('theme_id', '')}`",
                f"- Pipeline stages: `{', '.join(skill_pipeline.get('pipeline_stages') or [])}`",
                f"- Selected proposal: `{selected.get('proposal_id') or 'none'}`",
                f"- Route class: `{selected.get('route_class') or 'none'}`",
                f"- Capability action: `{selected.get('capability_action') or 'none'}`",
                f"- Route profiles: `{', '.join(selected.get('route_profiles') or []) or 'none'}`",
                f"- Selected local lane: `{selected.get('selected_local_lane') or 'none'}`",
                f"- Unlocked local lanes: `{', '.join(selected.get('unlocked_local_lanes') or []) or 'none'}`",
                f"- Local comparison required: `{bool(selected.get('local_comparison_required'))}`",
                f"- Local comparison status: `{selected.get('local_comparison_status') or 'none'}`",
                f"- Local comparison decision: `"
                f"{(skill_pipeline.get('local_comparison') or {}).get('decision') or 'none'}`",
                f"- Reverse-flow test validation lane: `"
                f"{(skill_pipeline.get('reverse_flow_test_validation_lane') or {}).get('status') or 'none'}`",
                f"- Rnskill docs validation lane: `"
                f"{(skill_pipeline.get('rnskill_docs_validation_lane') or {}).get('status') or 'none'}`",
                f"- Config gate boundary: `"
                f"{(skill_pipeline.get('config_gate_boundary') or {}).get('status') or 'none'}`",
                f"- Local apply handoff: `"
                f"{(skill_pipeline.get('local_apply') or {}).get('status') or 'none'}`",
                f"- Runtime action: `{skill_pipeline.get('runtime_action', 'none')}`",
                f"- Raw evidence URLs exported: `{bool(skill_pipeline.get('raw_evidence_urls_exported'))}`",
            ]
        )
        retained = skill_pipeline.get("retained_boundaries") or []
        if retained:
            lines.append("- Retained review-only boundaries:")
            for boundary in retained:
                if not isinstance(boundary, dict):
                    continue
                lines.append(
                    "  - "
                    f"`{boundary.get('proposal_id') or 'unknown'}` "
                    f"(`{boundary.get('route_class') or 'unknown'}`)"
                )
        adjacent = skill_pipeline.get("adjacent_general_agent_rows") or []
        if adjacent:
            lines.append("- Adjacent general-agent harness-eval rows:")
            for row in adjacent:
                if not isinstance(row, dict):
                    continue
                lines.append(
                    "  - "
                    f"`{row.get('proposal_id') or 'unknown'}` "
                    f"(`{row.get('evaluation_lane') or 'agent_harness_eval_required'}`)"
                )
        lines.append(f"- Promotion rule: {skill_pipeline.get('promotion_rule', '')}")
    return "\n".join(lines) + "\n"


def build_self_evolution_plan(
    digest: dict[str, Any],
    *,
    repo_path: Path,
    branch_prefix: str = "codex/blackhole-evolve",
    max_proposals: int = 3,
    force: bool = False,
    extra_instructions: str = "",
    self_model_path: Path | None = None,
) -> SelfEvolutionPlan | None:
    """Turn a digest into a concrete Codex CLI task for local self-improvement."""

    proposals = list(digest.get("proposals", []))[:max_proposals]
    if not proposals and not force:
        return None
    if not proposals:
        proposals = [
            {
                "proposal_id": "fallback-local-observability",
                "kind": "test",
                "summary": "Improve the blackhole-agent growth controller's observability or tests.",
                "evidence_urls": [],
                "requires_approval": False,
            }
        ]

    capability_theme_window = dict(
        digest.get("capability_theme_window")
        or build_capability_theme_window(
            proposals,
            generated_at=str(digest.get("generated_at") or ""),
        )
    )
    capability_step = digest.get("upstream_evidence_capability_step")
    if not isinstance(capability_step, dict) or not capability_step:
        capability_step = build_upstream_evidence_capability_step(
            proposals,
            theme_window=capability_theme_window,
            items=list(digest.get("items") or []),
        )
    skill_pipeline = digest.get("skill_route_discovery_capability_pipeline")
    if not isinstance(skill_pipeline, dict) or not skill_pipeline:
        skill_pipeline = build_skill_route_discovery_capability_pipeline(
            proposals,
            theme_window=capability_theme_window,
            items=list(digest.get("items") or []),
        )
    generated_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    self_model_before = read_self_model_snapshot(repo_path, self_model_path)
    branch_name = build_evolution_branch_name(branch_prefix, generated_at, proposals[0]["summary"])
    task = render_self_evolution_task(
        proposals,
        repo_path=repo_path,
        branch_name=branch_name,
        self_model_snapshot=self_model_before,
        digest_id=str(digest.get("digest_id", "")),
        digest_generated_at=str(digest.get("generated_at", "")),
        capability_theme_window=capability_theme_window,
        upstream_evidence_capability_step=capability_step,
        skill_route_discovery_capability_pipeline=skill_pipeline,
        extra_instructions=extra_instructions,
    )
    return SelfEvolutionPlan(
        generated_at=generated_at,
        repo_path=str(repo_path),
        branch_name=branch_name,
        self_model_path=self_model_before.path,
        self_model_before=self_model_before,
        task=task,
        proposals=proposals,
        source_digest_id=str(digest.get("digest_id", "")),
        source_digest_generated_at=str(digest.get("generated_at", "")),
        capability_theme_window=capability_theme_window,
        upstream_evidence_capability_step=dict(capability_step),
        skill_route_discovery_capability_pipeline=dict(skill_pipeline),
    )


def build_evolution_branch_name(branch_prefix: str, generated_at: str, title: str) -> str:
    stamp = generated_at.replace("-", "").replace(":", "").replace("+00:00", "Z").replace("Z", "")
    slug = slugify_branch_part(title)[:48] or "blackhole-growth"
    return f"{branch_prefix.strip('/')}/{stamp}-{slug}"


def slugify_branch_part(value: str) -> str:
    chars: list[str] = []
    previous_dash = False
    for char in value.lower():
        if char.isalnum():
            chars.append(char)
            previous_dash = False
        elif not previous_dash:
            chars.append("-")
            previous_dash = True
    return "".join(chars).strip("-")


def render_capability_theme_window_lines(theme_window: dict[str, Any]) -> list[str]:
    """Render the active theme window as bounded task instructions."""

    if not theme_window:
        return [
            "- No prior theme window was available; choose one coherent capability slice and make it replayable.",
        ]
    proposal_ids = ", ".join(bounded_unique_strings(theme_window.get("proposal_ids", []))) or "none yet"
    evidence_urls = ", ".join(bounded_unique_strings(theme_window.get("evidence_urls", []), limit=4)) or "none yet"
    return [
        f"- Theme: {theme_window.get('title', '')} (`{theme_window.get('theme_id', '')}`)",
        f"- Capability slice: {theme_window.get('capability_slice', '')}",
        (
            f"- Planned pass: {theme_window.get('planned_passes', 0)} of "
            f"{theme_window.get('target_passes', DEFAULT_THEME_WINDOW_TARGET_PASSES)} "
            f"({theme_window.get('status', 'active')})"
        ),
        f"- Anchoring proposal IDs: {proposal_ids}",
        f"- Evidence URLs carried by the window: {evidence_urls}",
        "- Continuity rule: advance this slice across passes; defer unrelated micro-patches unless they unblock the slice.",
        "- Completion rule: prefer an operator-visible behavior, integration path, or recovery workflow over another standalone fixture.",
    ]


def render_upstream_evidence_capability_step_lines(capability_step: dict[str, Any]) -> list[str]:
    """Render the selected upstream-evidence capability step for kernel tasks."""

    if not capability_step:
        return []
    selected = capability_step.get("selected_step") if isinstance(capability_step.get("selected_step"), dict) else {}
    lines = [
        "Upstream evidence capability step:",
        f"- Status: `{capability_step.get('status', '')}`",
        f"- Selected proposal: `{selected.get('proposal_id') or 'none'}`",
        f"- Route class: `{selected.get('route_class') or 'none'}`",
        f"- Capability action: `{selected.get('capability_action') or 'none'}`",
        f"- Local lane: `{selected.get('local_lane') or 'none'}`",
        f"- Requires local compare before draft: `{bool(selected.get('requires_local_compare_before_draft'))}`",
        f"- Autonomous local apply for selected step: `{bool(selected.get('autonomous_local_apply'))}`",
        f"- Runtime action: `{capability_step.get('runtime_action', 'none')}`",
        "- Prefer this selected local capability step over isolated notes when the theme window is active.",
        "- Keep privacy-leakage and offensive-behavior rows review-only; do not export raw evidence URLs or sensitive bodies.",
    ]
    retained = capability_step.get("retained_boundaries") or []
    if retained:
        retained_ids = ", ".join(
            str(row.get("proposal_id") or "unknown")
            for row in retained
            if isinstance(row, dict)
        )
        lines.append(f"- Retained review-only boundaries: {retained_ids or 'none'}")
    return lines


def _resolve_reverse_flow_evidence_binding(
    pipeline: dict[str, Any],
    focused_local_test_validation: dict[str, Any],
) -> dict[str, Any]:
    """Body-free reverse-flow-skill evidence binding for operator continue state.

    Binds the selected reverse-flow proposal to reverse-flow-skill markers
    (``lingbol088-spec/reverse-flow-skill`` / ``codex_workflow_gate``) without
    exporting raw evidence URLs or upstream bodies. Residual export remains
    gated separately until reverse-flow focused validation is recorded/closed.
    """

    selected = (
        pipeline.get("selected_step")
        if isinstance(pipeline.get("selected_step"), dict)
        else {}
    )
    proposal_id = str(
        focused_local_test_validation.get("selected_proposal_id")
        or focused_local_test_validation.get("proposal_id")
        or selected.get("proposal_id")
        or ""
    ).strip()
    profiles = [
        str(profile)
        for profile in list(
            focused_local_test_validation.get("route_profiles")
            or selected.get("route_profiles")
            or []
        )
        if str(profile).strip()
    ]
    skill_first = bool(
        focused_local_test_validation.get("skill_route_discovery_first")
        if "skill_route_discovery_first" in focused_local_test_validation
        else selected.get("skill_route_discovery_first")
    )
    route_class = str(
        focused_local_test_validation.get("route_class")
        or selected.get("route_class")
        or ""
    )
    blob = " ".join(
        [
            proposal_id,
            route_class,
            *profiles,
            str(selected.get("summary") or ""),
            str(focused_local_test_validation.get("proposal_track") or ""),
        ]
    ).lower()
    marker_hit = any(marker in blob for marker in _SKILL_ROUTE_REVERSE_FLOW_MARKERS)
    profile_bound = "codex_workflow_gate" in profiles and skill_first
    bound = bool(marker_hit or profile_bound)
    # Prefer the public reverse-flow-skill evidence marker when any reverse-flow
    # naming is present; profile-only binds stay generic reverse-flow.
    if (
        "lingbol088" in blob
        or "reverse-flow-skill" in blob
        or "reverse_flow_skill" in blob
        or "reverse-flow" in blob
        or "reverse_flow" in blob
        or "reverse flow" in blob
    ):
        source_marker = "lingbol088-spec/reverse-flow-skill"
    elif bound:
        source_marker = "reverse-flow"
    else:
        source_marker = ""
    return {
        "bound": bound,
        "selected_proposal_id": proposal_id if bound else "",
        "source_marker": source_marker if bound else "",
        "route_profiles": profiles if bound else [],
        "skill_route_discovery_first": skill_first if bound else False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }


def resolve_skill_route_discovery_pipeline_operator_state(
    pipeline: dict[str, Any] | None,
) -> dict[str, Any]:
    """Compute durable operator-visible continue state for a skill-route pipeline.

    Supervisors and record/close helpers attach these fields onto the pipeline
    packet so reverse-flow focused validation continue does not require re-rendering
    markdown to learn supervisor_next or residual fortress hold/export state.
    Residual fortress IDs stay held while reverse-flow focused validation is
    ready/unrecorded or failed. Partial body-free command-hash coverage is also
    exported so supervisors can finish reverse-flow record/close before residual
    export without re-rendering markdown.
    """

    empty_binding = {
        "bound": False,
        "selected_proposal_id": "",
        "source_marker": "",
        "route_profiles": [],
        "skill_route_discovery_first": False,
        "body_free": True,
        "raw_evidence_urls_exported": False,
        "raw_upstream_bodies_exported": False,
    }
    if not isinstance(pipeline, dict) or not pipeline:
        return {
            "supervisor_next_action": "none",
            "residual_adjacent_held_until_reverse_flow_focused_validation_recorded": False,
            "residual_adjacent_selection_held_until_residual_active": False,
            "residual_adjacent_export_held_on_reverse_flow_surfaces": False,
            "residual_export_allowed": False,
            "residual_adjacent_selected_proposal_id": "",
            "residual_work_is_residual_active": False,
            "reverse_flow_focused_validation_ready_unrecorded": False,
            "reverse_flow_focused_validation_record_helpers": [],
            "reverse_flow_focused_validation_results_cover_expected": False,
            "reverse_flow_focused_validation_recorded_result_count": 0,
            "reverse_flow_focused_validation_expected_command_count": 0,
            "reverse_flow_focused_validation_partial_results_recorded": False,
            "reverse_flow_focused_validation_recorded_command_hashes": [],
            "reverse_flow_focused_validation_recorded_command_hash_count": 0,
            "reverse_flow_focused_validation_missing_command_hashes": [],
            "reverse_flow_focused_validation_missing_command_hash_count": 0,
            "reverse_flow_focused_validation_pending_commands": [],
            "reverse_flow_focused_validation_pending_command_count": 0,
            "reverse_flow_focused_validation_pending_work_units": [],
            "reverse_flow_focused_validation_pending_work_unit_count": 0,
            "reverse_flow_focused_validation_continue_plan_mode": "none",
            "reverse_flow_focused_validation_continue_run_executable": False,
            "reverse_flow_focused_validation_continue_runnable_work_unit_count": 0,
            "reverse_flow_focused_validation_continue_run_recommended": False,
            "reverse_flow_focused_validation_continue_supervisor_wake": {},
            "reverse_flow_focused_validation_continue_dispatch": {},
            "reverse_flow_focused_validation_continue_dispatch_action": "none",
            "reverse_flow_focused_validation_continue_dispatch_execute_recommended": False,
            "reverse_flow_focused_validation_continue_dispatch_helper": (
                "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
            ),
            "reverse_flow_focused_validation_continue_dispatch_inventory_helper": (
                "package_reverse_flow_focused_validation_continue_dispatch_inventory"
            ),
            "reverse_flow_focused_validation_continue_dispatch_follow_through": {},
            "reverse_flow_focused_validation_continue_dispatch_follow_through_action": (
                "none"
            ),
            "reverse_flow_focused_validation_continue_dispatch_call_with_execute": False,
            "reverse_flow_focused_validation_continue_dispatch_follow_through_helper": (
                "follow_reverse_flow_focused_validation_continue_dispatch"
            ),
            "reverse_flow_focused_validation_continue_dispatch_follow_through_resolve_helper": (
                "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
            ),
            "reverse_flow_focused_validation_continue_operator_card": {},
            "reverse_flow_focused_validation_continue_operator_card_helper": (
                "package_reverse_flow_focused_validation_continue_operator_card"
            ),
            "reverse_flow_focused_validation_continue_progress_label": "0/0",
            "reverse_flow_focused_validation_continue_action_line": "",
            "reverse_flow_focused_validation_continue_progress_transition_helper": (
                "package_reverse_flow_focused_validation_continue_progress_transition"
            ),
            "reverse_flow_focused_validation_continue_exec_receipt_helper": (
                "package_reverse_flow_focused_validation_continue_exec_receipt"
            ),
            "reverse_flow_continue_decision": "none",
            "reverse_flow_bound": False,
            "reverse_flow_bound_proposal_id": "",
            "reverse_flow_bound_source_marker": "",
            "reverse_flow_evidence_binding": dict(empty_binding),
        }

    def _packet(key: str) -> dict[str, Any]:
        value = pipeline.get(key)
        return value if isinstance(value, dict) else {}

    local_apply = _packet("local_apply")
    local_apply_completion = _packet("local_apply_completion")
    unlocked_test_lane_apply = _packet("unlocked_local_test_lane_apply")
    focused_local_test_validation = _packet("focused_local_test_validation")
    activation_external_handoff = _packet("focused_validation_activation_external_handoff")
    activation_external_acceptance = _packet(
        "focused_validation_activation_external_acceptance"
    )
    residual_adjacent_queue = _packet("focused_validation_residual_adjacent_queue")
    residual_adjacent_local_apply = _packet("residual_adjacent_harness_eval_local_apply")
    residual_adjacent_local_comparison = _packet(
        "residual_adjacent_harness_eval_local_comparison"
    )
    residual_adjacent_unlocked_lane_apply = _packet(
        "residual_adjacent_unlocked_local_lane_apply"
    )
    residual_adjacent_focused_local_validation = _packet(
        "residual_adjacent_focused_local_validation"
    )
    residual_adjacent_focused_validation_activation_external_handoff = _packet(
        "residual_adjacent_focused_validation_activation_external_handoff"
    )
    residual_adjacent_focused_validation_activation_external_acceptance = _packet(
        "residual_adjacent_focused_validation_activation_external_acceptance"
    )
    adjacent_handoff = _packet("adjacent_agent_harness_eval_handoff")

    focused_status = str(focused_local_test_validation.get("status") or "")
    activation_external_status = str(activation_external_handoff.get("status") or "")
    activation_external_acceptance_status = str(
        activation_external_acceptance.get("status") or ""
    )
    residual_adjacent_queue_status = str(residual_adjacent_queue.get("status") or "")
    residual_adjacent_local_apply_status = str(
        residual_adjacent_local_apply.get("status") or ""
    )
    residual_adjacent_local_comparison_status = str(
        residual_adjacent_local_comparison.get("status") or ""
    )
    residual_adjacent_unlocked_lane_apply_status = str(
        residual_adjacent_unlocked_lane_apply.get("status") or ""
    )
    residual_adjacent_focused_status = str(
        residual_adjacent_focused_local_validation.get("status") or ""
    )
    residual_adjacent_activation_external_status = str(
        residual_adjacent_focused_validation_activation_external_handoff.get("status")
        or ""
    )
    residual_adjacent_activation_external_acceptance_status = str(
        residual_adjacent_focused_validation_activation_external_acceptance.get("status")
        or ""
    )
    unlocked_status = str(unlocked_test_lane_apply.get("status") or "")

    # Residual stages only own supervisor_next when residual work is residual-active.
    # Statuses that merely wait on reverse-flow focused validation / activation-external
    # acceptance must not override the reverse-flow primary stage.
    residual_handoff_is_residual_active = residual_adjacent_activation_external_status in {
        "ready",
        "blocked_until_residual_adjacent_focused_validation_recorded",
        "blocked_until_residual_adjacent_focused_validation_repaired",
        "blocked_until_residual_adjacent_focused_validation_pass",
        "blocked",
    }
    residual_focused_is_residual_active = residual_adjacent_focused_status in {
        "ready",
        "passed",
        "failed",
        "blocked",
    }
    residual_unlocked_is_residual_active = residual_adjacent_unlocked_lane_apply_status in {
        "ready",
        "blocked",
    }
    residual_comparison_is_residual_active = residual_adjacent_local_comparison_status in {
        "ready",
        "blocked",
    }
    residual_apply_is_residual_active = residual_adjacent_local_apply_status in {
        "ready",
        "blocked",
    }
    residual_queue_is_residual_active = residual_adjacent_queue_status in {
        "ready",
        "blocked",
    }
    residual_acceptance_owns_supervisor_next = (
        residual_adjacent_activation_external_acceptance_status == "accepted"
        or (
            residual_adjacent_activation_external_acceptance_status
            == "blocked_until_residual_adjacent_activation_external_handoff_ready"
            and residual_handoff_is_residual_active
        )
    )
    residual_handoff_owns_supervisor_next = (
        residual_handoff_is_residual_active and not residual_acceptance_owns_supervisor_next
    )
    residual_focused_owns_supervisor_next = (
        residual_focused_is_residual_active and not residual_handoff_is_residual_active
    )
    residual_unlocked_owns_supervisor_next = (
        residual_unlocked_is_residual_active and not residual_focused_is_residual_active
    )
    residual_comparison_owns_supervisor_next = (
        residual_comparison_is_residual_active and not residual_unlocked_is_residual_active
    )
    residual_apply_owns_supervisor_next = (
        residual_apply_is_residual_active and not residual_comparison_is_residual_active
    )
    residual_queue_owns_supervisor_next = (
        residual_queue_is_residual_active and not residual_apply_is_residual_active
    )
    residual_work_is_residual_active = (
        residual_acceptance_owns_supervisor_next
        or residual_handoff_owns_supervisor_next
        or residual_focused_owns_supervisor_next
        or residual_unlocked_owns_supervisor_next
        or residual_comparison_owns_supervisor_next
        or residual_apply_owns_supervisor_next
        or residual_queue_owns_supervisor_next
    )
    supervisor_next = (
        adjacent_handoff.get("supervisor_next_action")
        if str(adjacent_handoff.get("status") or "") == "ready"
        else None
    ) or (
        residual_adjacent_focused_validation_activation_external_acceptance.get(
            "supervisor_next_action"
        )
        if residual_acceptance_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_focused_validation_activation_external_handoff.get(
            "supervisor_next_action"
        )
        if residual_handoff_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_focused_local_validation.get("supervisor_next_action")
        if residual_focused_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_unlocked_lane_apply.get("supervisor_next_action")
        if residual_unlocked_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_local_comparison.get("supervisor_next_action")
        if residual_comparison_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_local_apply.get("supervisor_next_action")
        if residual_apply_owns_supervisor_next
        else None
    ) or (
        residual_adjacent_queue.get("supervisor_next_action")
        if residual_queue_owns_supervisor_next
        else None
    ) or (
        activation_external_acceptance.get("supervisor_next_action")
        if activation_external_acceptance_status
        in {
            "accepted",
            "blocked_until_activation_external_handoff_ready",
        }
        else None
    ) or (
        activation_external_handoff.get("supervisor_next_action")
        if activation_external_status
        in {
            "ready",
            "blocked_until_focused_validation_recorded",
            "blocked_until_focused_validation_repaired",
            "blocked_until_focused_validation_ready",
            "blocked_until_focused_validation_pass",
        }
        else None
    ) or (
        focused_local_test_validation.get("supervisor_next_action")
        if focused_status in {"ready", "passed", "failed"}
        else None
    ) or (
        unlocked_test_lane_apply.get("supervisor_next_action")
        if unlocked_status == "ready"
        else None
    ) or local_apply_completion.get("supervisor_next_action") or local_apply.get(
        "supervisor_next_action"
    ) or "none"
    residual_selected_proposal_id = (
        residual_adjacent_focused_validation_activation_external_acceptance.get(
            "selected_residual_proposal_id"
        )
        or residual_adjacent_focused_validation_activation_external_handoff.get(
            "selected_residual_proposal_id"
        )
        or residual_adjacent_focused_local_validation.get("selected_residual_proposal_id")
        or residual_adjacent_unlocked_lane_apply.get("selected_residual_proposal_id")
        or residual_adjacent_local_comparison.get("selected_residual_proposal_id")
        or residual_adjacent_local_apply.get("selected_residual_proposal_id")
        or residual_adjacent_queue.get("selected_residual_proposal_id")
        or ""
    )
    if not residual_work_is_residual_active:
        residual_selected_proposal_id = ""
    reverse_flow_focused_hold_active = bool(
        focused_local_test_validation.get("residual_adjacent_hold_active")
    ) or (
        focused_status == "ready"
        and not bool(
            focused_local_test_validation.get("focused_validation_recorded")
            or pipeline.get("focused_local_test_validation_recorded")
        )
    ) or focused_status == "failed"
    residual_selection_held = (not residual_work_is_residual_active) and any(
        bool(packet.get("residual_selection_held_until_residual_active"))
        for packet in (
            residual_adjacent_local_apply,
            residual_adjacent_local_comparison,
            residual_adjacent_unlocked_lane_apply,
            residual_adjacent_focused_local_validation,
            residual_adjacent_focused_validation_activation_external_handoff,
            residual_adjacent_focused_validation_activation_external_acceptance,
        )
        if isinstance(packet, dict)
    )
    residual_export_held = bool(
        focused_local_test_validation.get("residual_adjacent_ids_held_until_recorded")
        or activation_external_handoff.get("residual_adjacent_export_held_until_ready")
        or activation_external_acceptance.get("residual_adjacent_export_held_until_ready")
        or residual_adjacent_queue.get("residual_adjacent_export_held_until_ready")
        or reverse_flow_focused_hold_active
    )
    reverse_flow_ready_unrecorded = focused_status == "ready" and not bool(
        focused_local_test_validation.get("focused_validation_recorded")
        or pipeline.get("focused_local_test_validation_recorded")
    )
    focused_validation = (
        focused_local_test_validation.get("focused_validation")
        if isinstance(focused_local_test_validation.get("focused_validation"), dict)
        else {}
    )
    results_cover_expected = bool(focused_validation.get("results_cover_expected"))
    recorded_result_count = int(focused_validation.get("recorded_result_count") or 0)
    expected_command_count = int(
        focused_validation.get("expected_command_count")
        or len(list(focused_validation.get("command_hashes") or []))
        or 0
    )
    missing_command_hashes = [
        str(item).strip()
        for item in list(
            focused_validation.get("missing_command_hashes")
            or missing_skill_route_discovery_focused_validation_command_hashes(
                expected_command_hashes=list(
                    focused_validation.get("command_hashes") or []
                ),
                command_results=list(focused_validation.get("command_results") or []),
            )
        )
        if str(item).strip()
    ]
    recorded_command_hashes = [
        str(item).strip()
        for item in list(
            focused_validation.get("recorded_command_hashes")
            or recorded_skill_route_discovery_focused_validation_command_hashes(
                expected_command_hashes=list(
                    focused_validation.get("command_hashes") or []
                ),
                command_results=list(focused_validation.get("command_results") or []),
            )
        )
        if str(item).strip()
    ]
    pending_commands = [
        str(item)
        for item in list(focused_validation.get("pending_commands") or [])
        if str(item).strip()
    ]
    # Only surface missing/pending inventories while reverse-flow still needs
    # record/close or repair. Recorded hashes stay visible on ready/pass/fail.
    if focused_status not in {"ready", "failed"}:
        missing_command_hashes = []
        pending_commands = []
    if focused_status not in {"ready", "passed", "failed"}:
        recorded_command_hashes = []
    missing_command_hash_count = int(
        focused_validation.get("missing_command_hash_count")
        if focused_status in {"ready", "failed"}
        and focused_validation.get("missing_command_hash_count") is not None
        else len(missing_command_hashes)
    )
    recorded_command_hash_count = int(
        focused_validation.get("recorded_command_hash_count")
        if focused_status in {"ready", "passed", "failed"}
        and focused_validation.get("recorded_command_hash_count") is not None
        else len(recorded_command_hashes)
    )
    pending_command_count = int(
        focused_validation.get("pending_command_count")
        if focused_status == "ready"
        and focused_validation.get("pending_command_count") is not None
        else len(pending_commands)
    )
    partial_results_recorded = bool(
        reverse_flow_ready_unrecorded
        and (
            focused_validation.get("partial_results_recorded")
            or (recorded_result_count > 0 and not results_cover_expected)
        )
    )
    reverse_flow_binding = _resolve_reverse_flow_evidence_binding(
        pipeline,
        focused_local_test_validation,
    )
    residual_export_allowed = (not residual_export_held) and residual_work_is_residual_active
    record_helpers = [
        "record_skill_route_discovery_focused_local_test_validation_results",
        "close_skill_route_discovery_focused_local_test_validation_with_outcome",
        "package_reverse_flow_focused_validation_continue_operator_card",
        "follow_reverse_flow_focused_validation_continue_dispatch",
        "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through",
        "dispatch_reverse_flow_focused_validation_continue_supervisor_wake",
        "package_reverse_flow_focused_validation_continue_dispatch_inventory",
        "run_reverse_flow_focused_validation_continue_pending_work_units",
        "execute_reverse_flow_focused_validation_continue_run_plan",
        "build_reverse_flow_focused_validation_continue_run_plan",
        "resolve_reverse_flow_focused_validation_continue_run_supervisor_wake",
        "reverse_flow_focused_validation_continue_local_command_allowed",
        "record_reverse_flow_focused_validation_continue_outcomes",
        "materialize_reverse_flow_focused_validation_continue_record_rows",
        "merge_skill_route_discovery_focused_validation_command_results",
        "resolve_reverse_flow_focused_validation_continue_supervisor_next",
        "build_reverse_flow_focused_validation_continue_plan",
        "pending_skill_route_discovery_focused_validation_work_units",
    ]
    continue_plan = (
        focused_local_test_validation.get("continue_plan")
        if isinstance(focused_local_test_validation.get("continue_plan"), dict)
        else focused_validation.get("continue_plan")
        if isinstance(focused_validation.get("continue_plan"), dict)
        else None
    )
    if not isinstance(continue_plan, dict) and focused_status in {
        "ready",
        "passed",
        "failed",
    }:
        continue_plan = build_reverse_flow_focused_validation_continue_plan(
            focused_local_test_validation=focused_local_test_validation,
        )
    if not isinstance(continue_plan, dict):
        continue_plan = {}
    continue_plan_mode = str(continue_plan.get("mode") or "none")
    pending_work_units = [
        unit
        for unit in list(focused_validation.get("pending_work_units") or [])
        if isinstance(unit, dict)
        and (
            str(unit.get("command_hash") or "").strip()
            or str(unit.get("command") or "").strip()
        )
    ]
    # Align top-level supervisor_next with reverse-flow continue when partial
    # coverage already exists. Downstream residual packets may still say
    # run_focused... as a fallback; operator_state must stay reverse-flow-first.
    continue_run_executable = False
    continue_runnable_work_unit_count = 0
    continue_run_recommended = False
    continue_supervisor_wake: dict[str, Any] = {}
    continue_dispatch: dict[str, Any] = {}
    continue_dispatch_action = "none"
    continue_dispatch_execute_recommended = False
    continue_dispatch_follow_through: dict[str, Any] = {}
    continue_dispatch_follow_through_action = "none"
    continue_dispatch_call_with_execute = False
    if reverse_flow_ready_unrecorded:
        continue_supervisor_next = (
            resolve_reverse_flow_focused_validation_continue_supervisor_next(
                focused_local_test_validation=focused_local_test_validation,
            )
        )
        # Always prefer reverse-flow continue resolver while ready/unrecorded so
        # residual repair noise cannot outrank run_pending or record_remaining.
        supervisor_next = continue_supervisor_next
        reverse_flow_continue_decision = (
            "record_remaining_reverse_flow_focused_validation_command_hashes_before_residual_export"
            if partial_results_recorded
            else "record_or_close_reverse_flow_focused_validation_before_residual_export"
        )
        helpers = list(record_helpers)
        # Prefer continue_plan pending inventory when present so zero-row and
        # partial wakes share one durable work unit on operator_state.
        if continue_plan.get("pending_commands") is not None:
            pending_commands = [
                str(item)
                for item in list(continue_plan.get("pending_commands") or [])
                if str(item).strip()
            ]
            pending_command_count = int(
                continue_plan.get("pending_command_count")
                if continue_plan.get("pending_command_count") is not None
                else len(pending_commands)
            )
        if continue_plan.get("pending_work_units") is not None:
            pending_work_units = [
                unit
                for unit in list(continue_plan.get("pending_work_units") or [])
                if isinstance(unit, dict)
                and (
                    str(unit.get("command_hash") or "").strip()
                    or str(unit.get("command") or "").strip()
                )
            ]
        if continue_plan.get("mode"):
            continue_plan_mode = str(continue_plan.get("mode") or continue_plan_mode)
        # Annotate local-allowed continue-run executability without executing.
        continue_run_plan = build_reverse_flow_focused_validation_continue_run_plan(
            continue_plan=continue_plan if continue_plan else None,
            focused_local_test_validation=focused_local_test_validation,
        )
        continue_runnable_work_unit_count = int(
            continue_run_plan.get("runnable_work_unit_count") or 0
        )
        continue_run_executable = bool(continue_run_plan.get("executable"))
        # Durable inventory wake so supervisors can dispatch without re-resolving.
        continue_supervisor_wake = (
            resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
                pipeline=pipeline,
                continue_plan=continue_plan if continue_plan else None,
                run_plan=continue_run_plan,
            )
        )
        continue_run_recommended = bool(
            continue_supervisor_wake.get("continue_run_recommended")
        )
        # Prefer wake next action when present (already reverse-flow-first).
        if continue_supervisor_wake.get("supervisor_next_action"):
            supervisor_next = str(continue_supervisor_wake.get("supervisor_next_action"))
        # Durable inventory dispatch packet: action / execute_recommended without
        # re-deriving from nested wake fields or calling execute.
        continue_dispatch = (
            package_reverse_flow_focused_validation_continue_dispatch_inventory(
                pipeline=pipeline,
                inventory_wake=continue_supervisor_wake,
                execute_requested=False,
                record_requested=True,
            )
        )
        # Avoid embedding the full pipeline snapshot on durable operator_state.
        continue_dispatch = {
            key: value
            for key, value in continue_dispatch.items()
            if key != "pipeline"
        }
        continue_dispatch_action = str(continue_dispatch.get("action") or "none")
        continue_dispatch_execute_recommended = bool(
            continue_dispatch.get("execute_recommended")
        )
        continue_dispatch_follow_through = (
            resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
                inventory_dispatch=continue_dispatch,
                execute_requested=None,
            )
        )
        continue_dispatch_follow_through_action = str(
            continue_dispatch_follow_through.get("follow_through_action") or "none"
        )
        continue_dispatch_call_with_execute = bool(
            continue_dispatch_follow_through.get("call_dispatch_with_execute")
        )
    elif focused_status == "failed":
        reverse_flow_continue_decision = (
            "repair_failed_focused_local_test_validation_commands"
        )
        helpers = list(record_helpers)
        if continue_plan.get("mode"):
            continue_plan_mode = str(continue_plan.get("mode") or "repair")
        continue_supervisor_wake = (
            resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
                pipeline=pipeline,
                continue_plan=continue_plan if continue_plan else None,
            )
        )
        continue_run_recommended = bool(
            continue_supervisor_wake.get("continue_run_recommended")
        )
        continue_run_executable = bool(
            continue_supervisor_wake.get("continue_run_executable")
        )
        continue_runnable_work_unit_count = int(
            continue_supervisor_wake.get("runnable_work_unit_count") or 0
        )
        continue_dispatch = (
            package_reverse_flow_focused_validation_continue_dispatch_inventory(
                pipeline=pipeline,
                inventory_wake=continue_supervisor_wake,
                execute_requested=False,
                record_requested=True,
            )
        )
        continue_dispatch = {
            key: value
            for key, value in continue_dispatch.items()
            if key != "pipeline"
        }
        continue_dispatch_action = str(continue_dispatch.get("action") or "repair")
        continue_dispatch_execute_recommended = bool(
            continue_dispatch.get("execute_recommended")
        )
        continue_dispatch_follow_through = (
            resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
                inventory_dispatch=continue_dispatch,
                execute_requested=None,
            )
        )
        continue_dispatch_follow_through_action = str(
            continue_dispatch_follow_through.get("follow_through_action") or "repair"
        )
        continue_dispatch_call_with_execute = bool(
            continue_dispatch_follow_through.get("call_dispatch_with_execute")
        )
    elif focused_status == "passed":
        reverse_flow_continue_decision = str(supervisor_next or "none")
        helpers = []
        pending_commands = []
        pending_command_count = 0
        pending_work_units = []
        if continue_plan.get("mode"):
            continue_plan_mode = str(continue_plan.get("mode") or "keep_activation_external")
        continue_supervisor_wake = (
            resolve_reverse_flow_focused_validation_continue_run_supervisor_wake(
                pipeline=pipeline,
                continue_plan=continue_plan if continue_plan else None,
                recorded=True,
            )
        )
        continue_run_recommended = bool(
            continue_supervisor_wake.get("continue_run_recommended")
        )
        continue_dispatch = (
            package_reverse_flow_focused_validation_continue_dispatch_inventory(
                pipeline=pipeline,
                inventory_wake=continue_supervisor_wake,
                execute_requested=False,
                record_requested=True,
            )
        )
        continue_dispatch = {
            key: value
            for key, value in continue_dispatch.items()
            if key != "pipeline"
        }
        continue_dispatch_action = str(
            continue_dispatch.get("action") or "keep_activation_external"
        )
        continue_dispatch_execute_recommended = bool(
            continue_dispatch.get("execute_recommended")
        )
        continue_dispatch_follow_through = (
            resolve_reverse_flow_focused_validation_continue_dispatch_follow_through(
                inventory_dispatch=continue_dispatch,
                execute_requested=None,
            )
        )
        continue_dispatch_follow_through_action = str(
            continue_dispatch_follow_through.get("follow_through_action")
            or "keep_activation_external"
        )
        continue_dispatch_call_with_execute = bool(
            continue_dispatch_follow_through.get("call_dispatch_with_execute")
        )
    else:
        reverse_flow_continue_decision = str(supervisor_next or "none")
        helpers = []
        pending_commands = []
        pending_command_count = 0
        pending_work_units = []
        continue_plan_mode = "none"

    pending_work_unit_count = len(pending_work_units)

    state = {
        "supervisor_next_action": str(supervisor_next or "none"),
        "residual_adjacent_held_until_reverse_flow_focused_validation_recorded": (
            reverse_flow_focused_hold_active
        ),
        "residual_adjacent_selection_held_until_residual_active": (
            residual_selection_held
            or reverse_flow_focused_hold_active
            or not residual_work_is_residual_active
        ),
        "residual_adjacent_export_held_on_reverse_flow_surfaces": residual_export_held,
        "residual_export_allowed": residual_export_allowed,
        "residual_adjacent_selected_proposal_id": str(residual_selected_proposal_id or ""),
        "residual_work_is_residual_active": residual_work_is_residual_active,
        "reverse_flow_focused_validation_ready_unrecorded": reverse_flow_ready_unrecorded,
        "reverse_flow_focused_validation_record_helpers": helpers,
        "reverse_flow_focused_validation_results_cover_expected": results_cover_expected,
        "reverse_flow_focused_validation_recorded_result_count": recorded_result_count,
        "reverse_flow_focused_validation_expected_command_count": expected_command_count,
        "reverse_flow_focused_validation_partial_results_recorded": partial_results_recorded,
        "reverse_flow_focused_validation_recorded_command_hashes": recorded_command_hashes,
        "reverse_flow_focused_validation_recorded_command_hash_count": (
            recorded_command_hash_count
        ),
        "reverse_flow_focused_validation_missing_command_hashes": missing_command_hashes,
        "reverse_flow_focused_validation_missing_command_hash_count": (
            missing_command_hash_count
        ),
        "reverse_flow_focused_validation_pending_commands": pending_commands,
        "reverse_flow_focused_validation_pending_command_count": pending_command_count,
        "reverse_flow_focused_validation_pending_work_units": pending_work_units,
        "reverse_flow_focused_validation_pending_work_unit_count": pending_work_unit_count,
        "reverse_flow_focused_validation_continue_plan_mode": continue_plan_mode,
        "reverse_flow_focused_validation_continue_run_executable": continue_run_executable,
        "reverse_flow_focused_validation_continue_runnable_work_unit_count": (
            continue_runnable_work_unit_count
        ),
        "reverse_flow_focused_validation_continue_run_recommended": (
            continue_run_recommended
        ),
        "reverse_flow_focused_validation_continue_supervisor_wake": (
            dict(continue_supervisor_wake) if continue_supervisor_wake else {}
        ),
        "reverse_flow_focused_validation_continue_dispatch": (
            dict(continue_dispatch) if continue_dispatch else {}
        ),
        "reverse_flow_focused_validation_continue_dispatch_action": (
            continue_dispatch_action
        ),
        "reverse_flow_focused_validation_continue_dispatch_execute_recommended": (
            continue_dispatch_execute_recommended
        ),
        "reverse_flow_focused_validation_continue_dispatch_helper": (
            "dispatch_reverse_flow_focused_validation_continue_supervisor_wake"
        ),
        "reverse_flow_focused_validation_continue_dispatch_inventory_helper": (
            "package_reverse_flow_focused_validation_continue_dispatch_inventory"
        ),
        "reverse_flow_focused_validation_continue_dispatch_follow_through": (
            dict(continue_dispatch_follow_through)
            if continue_dispatch_follow_through
            else {}
        ),
        "reverse_flow_focused_validation_continue_dispatch_follow_through_action": (
            continue_dispatch_follow_through_action
        ),
        "reverse_flow_focused_validation_continue_dispatch_call_with_execute": (
            continue_dispatch_call_with_execute
        ),
        "reverse_flow_focused_validation_continue_dispatch_follow_through_helper": (
            "follow_reverse_flow_focused_validation_continue_dispatch"
        ),
        "reverse_flow_focused_validation_continue_dispatch_follow_through_resolve_helper": (
            "resolve_reverse_flow_focused_validation_continue_dispatch_follow_through"
        ),
        "reverse_flow_continue_decision": reverse_flow_continue_decision,
        "reverse_flow_bound": bool(reverse_flow_binding.get("bound")),
        "reverse_flow_bound_proposal_id": str(
            reverse_flow_binding.get("selected_proposal_id") or ""
        ),
        "reverse_flow_bound_source_marker": str(
            reverse_flow_binding.get("source_marker") or ""
        ),
        "reverse_flow_evidence_binding": dict(reverse_flow_binding),
    }
    operator_card = package_reverse_flow_focused_validation_continue_operator_card(
        state=state,
        follow_through=(
            continue_dispatch_follow_through
            if continue_dispatch_follow_through
            else None
        ),
    )
    state["reverse_flow_focused_validation_continue_operator_card"] = operator_card
    state["reverse_flow_focused_validation_continue_operator_card_helper"] = (
        "package_reverse_flow_focused_validation_continue_operator_card"
    )
    state["reverse_flow_focused_validation_continue_progress_label"] = str(
        operator_card.get("progress_label") or "0/0"
    )
    state["reverse_flow_focused_validation_continue_action_line"] = str(
        operator_card.get("action_line") or ""
    )
    state["reverse_flow_focused_validation_continue_progress_transition_helper"] = (
        "package_reverse_flow_focused_validation_continue_progress_transition"
    )
    state["reverse_flow_focused_validation_continue_exec_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_exec_receipt"
    )
    state["reverse_flow_focused_validation_continue_finish_receipt_helper"] = (
        "package_reverse_flow_focused_validation_continue_finish_receipt"
    )
    state["reverse_flow_focused_validation_continue_residual_open_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_open"
    )
    state["reverse_flow_focused_validation_continue_residual_entry_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_entry"
    )
    state["reverse_flow_focused_validation_continue_residual_follow_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_follow"
    )
    state["reverse_flow_focused_validation_continue_residual_comparison_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_comparison"
    )
    state["reverse_flow_focused_validation_continue_residual_unlocked_apply_helper"] = (
        "package_reverse_flow_focused_validation_continue_residual_unlocked_apply"
    )
    state[
        "reverse_flow_focused_validation_continue_residual_focused_validation_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_focused_validation"
    state[
        "reverse_flow_focused_validation_continue_residual_handoff_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_handoff"
    state[
        "reverse_flow_focused_validation_continue_residual_acceptance_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_acceptance"
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_cascade"
    state[
        "reverse_flow_focused_validation_continue_cascade_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade"
    state[
        "reverse_flow_focused_validation_continue_cascade_transition_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_transition"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake_route"
    # Body-free finish receipt so supervisors can log continue_finished /
    # residual_queue_ready without nested post-card / handoff re-assembly.
    finish_receipt = package_reverse_flow_focused_validation_continue_finish_receipt(
        pipeline=pipeline,
        post_operator_card=operator_card,
        exec_receipt=None,
        supervisor_wake=(
            continue_supervisor_wake if continue_supervisor_wake else None
        ),
        residual_hold_active=bool(
            reverse_flow_focused_hold_active
            if reverse_flow_ready_unrecorded or focused_status == "failed"
            else (
                continue_supervisor_wake.get("residual_hold_active")
                if continue_supervisor_wake
                else residual_export_held
            )
        ),
        continue_plan_mode=continue_plan_mode,
        supervisor_next_action=str(supervisor_next or "none"),
        executed=False,
        recorded=bool(focused_status == "passed"),
    )
    state["reverse_flow_focused_validation_continue_finish_receipt"] = finish_receipt
    state["reverse_flow_focused_validation_continue_finish_line"] = str(
        finish_receipt.get("finish_line") or ""
    )
    state["reverse_flow_focused_validation_continue_finished"] = bool(
        finish_receipt.get("continue_finished")
    )
    state["reverse_flow_focused_validation_continue_residual_queue_ready"] = bool(
        finish_receipt.get("residual_queue_ready")
    )
    # Body-free residual open bridges finish residual_queue_ready into residual
    # pipeline entry without residual_export on continue surfaces.
    residual_open = package_reverse_flow_focused_validation_continue_residual_open(
        pipeline=pipeline,
        finish_receipt=finish_receipt,
    )
    state["reverse_flow_focused_validation_continue_residual_open"] = residual_open
    state["reverse_flow_focused_validation_continue_residual_open_line"] = str(
        residual_open.get("residual_open_line") or ""
    )
    state["reverse_flow_focused_validation_continue_residual_open_ready"] = bool(
        residual_open.get("residual_open")
    )
    state["reverse_flow_focused_validation_continue_residual_adjacent_count"] = int(
        residual_open.get("residual_adjacent_count") or 0
    )
    # Body-free residual entry collapses residual open + residual apply selection
    # so selected fortress/Hy3 IDs are legible without nested re-assembly.
    residual_entry = package_reverse_flow_focused_validation_continue_residual_entry(
        pipeline=pipeline,
        residual_open=residual_open,
    )
    state["reverse_flow_focused_validation_continue_residual_entry"] = residual_entry
    state["reverse_flow_focused_validation_continue_residual_entry_line"] = str(
        residual_entry.get("residual_entry_line") or ""
    )
    state["reverse_flow_focused_validation_continue_residual_entry_ready"] = bool(
        residual_entry.get("residual_entry")
    )
    state["reverse_flow_focused_validation_continue_selected_residual_proposal_id"] = str(
        residual_entry.get("selected_residual_proposal_id") or ""
    )
    # Body-free residual follow collapses residual entry into residual comparison
    # policy so supervisors do not re-derive residual stage follow-through.
    residual_follow = package_reverse_flow_focused_validation_continue_residual_follow(
        pipeline=pipeline,
        residual_entry=residual_entry,
        residual_open=residual_open,
    )
    state["reverse_flow_focused_validation_continue_residual_follow"] = residual_follow
    state["reverse_flow_focused_validation_continue_residual_follow_line"] = str(
        residual_follow.get("residual_follow_line") or ""
    )
    state["reverse_flow_focused_validation_continue_residual_follow_ready"] = bool(
        residual_follow.get("residual_follow")
    )
    state["reverse_flow_focused_validation_continue_residual_follow_action"] = str(
        residual_follow.get("residual_follow_action") or "wait_for_reverse_flow"
    )
    state["reverse_flow_focused_validation_continue_call_residual_comparison"] = bool(
        residual_follow.get("call_residual_comparison")
    )
    # Body-free residual comparison collapses residual follow into residual
    # comparison readiness / unlocked-lane policy without residual_export.
    residual_comparison = (
        package_reverse_flow_focused_validation_continue_residual_comparison(
            pipeline=pipeline,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_comparison"] = (
        residual_comparison
    )
    state["reverse_flow_focused_validation_continue_residual_comparison_line"] = str(
        residual_comparison.get("residual_comparison_line") or ""
    )
    state["reverse_flow_focused_validation_continue_residual_comparison_ready"] = bool(
        residual_comparison.get("residual_comparison")
    )
    state["reverse_flow_focused_validation_continue_residual_comparison_action"] = str(
        residual_comparison.get("residual_comparison_action") or "wait_for_reverse_flow"
    )
    state["reverse_flow_focused_validation_continue_call_residual_unlocked_apply"] = (
        bool(residual_comparison.get("call_residual_unlocked_apply"))
    )
    # Body-free residual unlocked apply collapses residual comparison into
    # preferred test-first unlocked lane + focused-validation policy without
    # residual_export.
    residual_unlocked_apply = (
        package_reverse_flow_focused_validation_continue_residual_unlocked_apply(
            pipeline=pipeline,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_unlocked_apply"] = (
        residual_unlocked_apply
    )
    state["reverse_flow_focused_validation_continue_residual_unlocked_apply_line"] = (
        str(residual_unlocked_apply.get("residual_unlocked_apply_line") or "")
    )
    state["reverse_flow_focused_validation_continue_residual_unlocked_apply_ready"] = (
        bool(residual_unlocked_apply.get("residual_unlocked_apply"))
    )
    state[
        "reverse_flow_focused_validation_continue_residual_unlocked_apply_action"
    ] = str(
        residual_unlocked_apply.get("residual_unlocked_apply_action")
        or "wait_for_reverse_flow"
    )
    state[
        "reverse_flow_focused_validation_continue_call_residual_focused_validation"
    ] = bool(residual_unlocked_apply.get("call_residual_focused_validation"))
    # Body-free residual focused validation collapses residual unlocked apply into
    # command-hash progress + activation-external handoff policy without residual_export.
    residual_focused_validation = (
        package_reverse_flow_focused_validation_continue_residual_focused_validation(
            pipeline=pipeline,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_focused_validation"] = (
        residual_focused_validation
    )
    state[
        "reverse_flow_focused_validation_continue_residual_focused_validation_line"
    ] = str(
        residual_focused_validation.get("residual_focused_validation_line") or ""
    )
    state[
        "reverse_flow_focused_validation_continue_residual_focused_validation_ready"
    ] = bool(residual_focused_validation.get("residual_focused_validation"))
    state[
        "reverse_flow_focused_validation_continue_residual_focused_validation_action"
    ] = str(
        residual_focused_validation.get("residual_focused_validation_action")
        or "wait_for_reverse_flow"
    )
    state["reverse_flow_focused_validation_continue_call_residual_handoff"] = bool(
        residual_focused_validation.get("call_residual_handoff")
    )
    # Body-free residual handoff collapses residual focused validation into
    # activation-external handoff readiness + acceptance policy without residual_export.
    residual_handoff = (
        package_reverse_flow_focused_validation_continue_residual_handoff(
            pipeline=pipeline,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_handoff"] = residual_handoff
    state[
        "reverse_flow_focused_validation_continue_residual_handoff_line"
    ] = str(residual_handoff.get("residual_handoff_line") or "")
    state[
        "reverse_flow_focused_validation_continue_residual_handoff_ready"
    ] = bool(residual_handoff.get("residual_handoff"))
    state[
        "reverse_flow_focused_validation_continue_residual_handoff_action"
    ] = str(
        residual_handoff.get("residual_handoff_action") or "wait_for_reverse_flow"
    )
    state["reverse_flow_focused_validation_continue_call_residual_acceptance"] = bool(
        residual_handoff.get("call_residual_acceptance")
    )
    # Body-free residual acceptance collapses residual handoff into residual
    # activation-external acceptance status without residual_export.
    residual_acceptance = (
        package_reverse_flow_focused_validation_continue_residual_acceptance(
            pipeline=pipeline,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_acceptance"] = (
        residual_acceptance
    )
    state[
        "reverse_flow_focused_validation_continue_residual_acceptance_line"
    ] = str(residual_acceptance.get("residual_acceptance_line") or "")
    state[
        "reverse_flow_focused_validation_continue_residual_acceptance_ready"
    ] = bool(residual_acceptance.get("residual_acceptance"))
    state[
        "reverse_flow_focused_validation_continue_residual_acceptance_action"
    ] = str(
        residual_acceptance.get("residual_acceptance_action")
        or "wait_for_reverse_flow"
    )
    state[
        "reverse_flow_focused_validation_continue_residual_acceptance_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_acceptance"
    # Body-free residual cascade collapses residual acceptance into stage
    # progress / blocked_at / keep_activation_external policy without residual_export.
    residual_cascade = (
        package_reverse_flow_focused_validation_continue_residual_cascade(
            pipeline=pipeline,
            residual_acceptance=residual_acceptance,
            residual_handoff=residual_handoff,
            residual_focused_validation=residual_focused_validation,
            residual_unlocked_apply=residual_unlocked_apply,
            residual_comparison=residual_comparison,
            residual_follow=residual_follow,
            residual_entry=residual_entry,
            residual_open=residual_open,
        )
    )
    state["reverse_flow_focused_validation_continue_residual_cascade"] = residual_cascade
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_line"
    ] = str(residual_cascade.get("residual_cascade_line") or "")
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_ready"
    ] = bool(residual_cascade.get("residual_cascade"))
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_action"
    ] = str(
        residual_cascade.get("residual_cascade_action") or "wait_for_reverse_flow"
    )
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_progress_label"
    ] = str(residual_cascade.get("residual_cascade_progress_label") or "0/8")
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_blocked_at"
    ] = str(residual_cascade.get("residual_cascade_blocked_at") or "open")
    state[
        "reverse_flow_focused_validation_continue_residual_cascade_helper"
    ] = "package_reverse_flow_focused_validation_continue_residual_cascade"
    # Body-free continue cascade unifies reverse-flow progress with residual
    # cascade progress / blocked_at without residual_export.
    continue_cascade = package_reverse_flow_focused_validation_continue_cascade(
        pipeline=pipeline,
        operator_card=operator_card,
        residual_cascade=residual_cascade,
        follow_through=(
            continue_dispatch_follow_through
            if continue_dispatch_follow_through
            else None
        ),
        finish_receipt=finish_receipt if finish_receipt else None,
    )
    state["reverse_flow_focused_validation_continue_cascade"] = continue_cascade
    state[
        "reverse_flow_focused_validation_continue_cascade_line"
    ] = str(continue_cascade.get("continue_cascade_line") or "")
    state[
        "reverse_flow_focused_validation_continue_cascade_ready"
    ] = bool(continue_cascade.get("continue_cascade"))
    state[
        "reverse_flow_focused_validation_continue_cascade_action"
    ] = str(
        continue_cascade.get("continue_cascade_action") or "wait_for_reverse_flow"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_reverse_progress_label"
    ] = str(continue_cascade.get("reverse_progress_label") or "0/0")
    state[
        "reverse_flow_focused_validation_continue_cascade_residual_progress_label"
    ] = str(continue_cascade.get("residual_progress_label") or "0/8")
    state[
        "reverse_flow_focused_validation_continue_cascade_residual_blocked_at"
    ] = str(continue_cascade.get("residual_cascade_blocked_at") or "open")
    state[
        "reverse_flow_focused_validation_continue_cascade_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade"
    # Identity continue_cascade_transition on snapshot wakes documents the
    # body-free pre/post format without requiring a follow/dispatch wake.
    continue_cascade_transition = (
        package_reverse_flow_focused_validation_continue_cascade_transition(
            pre_cascade=continue_cascade,
            post_cascade=continue_cascade,
            executed=False,
            recorded=False,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_transition"
    ] = continue_cascade_transition
    state[
        "reverse_flow_focused_validation_continue_cascade_transition_line"
    ] = str(
        continue_cascade_transition.get("continue_cascade_transition_line") or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_transition_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_transition"
    state[
        "reverse_flow_focused_validation_continue_cascade_advanced"
    ] = bool(continue_cascade_transition.get("cascade_advanced"))
    state[
        "reverse_flow_focused_validation_continue_cascade_reverse_progress_transition"
    ] = str(
        continue_cascade_transition.get("reverse_progress_transition") or "0/0→0/0"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_residual_progress_transition"
    ] = str(
        continue_cascade_transition.get("residual_progress_transition") or "0/8→0/8"
    )
    # Identity continue_cascade_wake on snapshot wakes documents the body-free
    # wake_outcome format without requiring a follow/dispatch execute wake.
    continue_cascade_wake = package_reverse_flow_focused_validation_continue_cascade_wake(
        cascade_transition=continue_cascade_transition,
        exec_receipt=None,
        finish_receipt=finish_receipt if finish_receipt else None,
        residual_open=residual_open if residual_open else None,
        follow_through=(
            continue_dispatch_follow_through
            if continue_dispatch_follow_through
            else None
        ),
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake"
    ] = continue_cascade_wake
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_line"
    ] = str(continue_cascade_wake.get("continue_cascade_wake_line") or "")
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_outcome"
    ] = str(continue_cascade_wake.get("wake_outcome") or "identity")
    # Identity continue_cascade_wake_route maps wake_outcome → route_action so
    # supervisors pin one next-helper policy without nested re-assembly.
    continue_cascade_wake_route = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route(
            cascade_wake=continue_cascade_wake,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route"
    ] = continue_cascade_wake_route
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_line"
    ] = str(
        continue_cascade_wake_route.get("continue_cascade_wake_route_line") or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake_route"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_action"
    ] = str(continue_cascade_wake_route.get("route_action") or "inventory_only")
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_call_execute"
    ] = bool(continue_cascade_wake_route.get("call_follow_with_execute"))
    # Identity continue_cascade_wake_route_apply keeps pre==post so the apply
    # receipt format is legible on snapshot wakes before execute.
    continue_cascade_wake_route_apply = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply(
            pre_route=continue_cascade_wake_route,
            post_route=continue_cascade_wake_route,
            executed=False,
            recorded=False,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    ] = continue_cascade_wake_route_apply
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_line"
    ] = str(
        continue_cascade_wake_route_apply.get(
            "continue_cascade_wake_route_apply_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_action_transition"
    ] = str(
        continue_cascade_wake_route_apply.get("route_action_transition")
        or "inventory_only→inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_advanced"
    ] = bool(continue_cascade_wake_route_apply.get("route_advanced"))
    # Identity apply_follow maps applied_route_action → preferred helper so
    # supervisors pin one follow policy without re-mapping nested apply fields.
    continue_cascade_wake_route_apply_follow = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow(
            route_apply=continue_cascade_wake_route_apply,
            post_route=continue_cascade_wake_route,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
    ] = continue_cascade_wake_route_apply_follow
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_line"
    ] = str(
        continue_cascade_wake_route_apply_follow.get(
            "continue_cascade_wake_route_apply_follow_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_action"
    ] = str(
        continue_cascade_wake_route_apply_follow.get("follow_action")
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_preferred_helper"
    ] = str(
        continue_cascade_wake_route_apply_follow.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_call_execute"
    ] = bool(
        continue_cascade_wake_route_apply_follow.get("call_follow_with_execute")
    )
    # Identity apply_follow_pin maps follow policy → call recipe so supervisors
    # pin one helper invocation without re-deriving execute vs package mode.
    continue_cascade_wake_route_apply_follow_pin = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin(
            apply_follow=continue_cascade_wake_route_apply_follow,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
    ] = continue_cascade_wake_route_apply_follow_pin
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_line"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin.get(
            "continue_cascade_wake_route_apply_follow_pin_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_helper"
    ] = "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin"
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_action"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin.get("pin_action")
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_mode"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin.get("pin_mode")
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_preferred_helper"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_execute"
    ] = bool(
        continue_cascade_wake_route_apply_follow_pin.get("call_pin_with_execute")
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_ready"
    ] = bool(continue_cascade_wake_route_apply_follow_pin.get("pin_ready"))
    # Identity pin_call keeps pre_pin == post_pin so supervisors can read a
    # body-free call receipt before execute without inventing a false transition.
    continue_cascade_wake_route_apply_follow_pin_call = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call(
            pre_pin=continue_cascade_wake_route_apply_follow_pin,
            post_pin=continue_cascade_wake_route_apply_follow_pin,
            executed=False,
            recorded=False,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
    ] = continue_cascade_wake_route_apply_follow_pin_call
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_line"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_action_transition"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call.get("pin_action_transition")
        or "inventory_only→inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_mode_transition"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call.get("pin_mode_transition")
        or "inventory_only→inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_advanced"
    ] = bool(continue_cascade_wake_route_apply_follow_pin_call.get("pin_advanced"))
    # Identity pin_call_next seals the identity pin_call into a next-invoke
    # recipe so supervisors can read invoke policy before execute.
    continue_cascade_wake_route_apply_follow_pin_call_next = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next(
            pin_call=continue_cascade_wake_route_apply_follow_pin_call,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
    ] = continue_cascade_wake_route_apply_follow_pin_call_next
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_line"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_action"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next.get("next_action")
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_invoke"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next.get("next_invoke")
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_preferred_helper"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next.get("preferred_helper")
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_execute"
    ] = bool(
        continue_cascade_wake_route_apply_follow_pin_call_next.get(
            "call_next_with_execute"
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_ready"
    ] = bool(continue_cascade_wake_route_apply_follow_pin_call_next.get("next_ready"))
    # Identity next_call keeps pre_next == post_next so supervisors can read a
    # body-free next-call receipt before execute without inventing a false
    # next advanced transition.
    continue_cascade_wake_route_apply_follow_pin_call_next_call = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call(
            pre_next=continue_cascade_wake_route_apply_follow_pin_call_next,
            post_next=continue_cascade_wake_route_apply_follow_pin_call_next,
            executed=False,
            recorded=False,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
    ] = continue_cascade_wake_route_apply_follow_pin_call_next_call
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_action_transition"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call.get(
            "next_action_transition"
        )
        or "inventory_only→inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_invoke_transition"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call.get(
            "next_invoke_transition"
        )
        or "inventory_only→inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_advanced"
    ] = bool(
        continue_cascade_wake_route_apply_follow_pin_call_next_call.get("next_advanced")
    )
    # Identity next_call_follow maps identity next_call into a durable follow
    # policy so supervisors can read follow action/mode before execute without
    # inventing a false advanced transition.
    continue_cascade_wake_route_apply_follow_pin_call_next_call_follow = (
        package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow(
            next_call=continue_cascade_wake_route_apply_follow_pin_call_next_call,
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
    ] = continue_cascade_wake_route_apply_follow_pin_call_next_call_follow
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line"
        )
        or ""
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper"
    ] = (
        "package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_action"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "follow_action"
        )
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_mode"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "follow_mode"
        )
        or "inventory_only"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_preferred_helper"
    ] = str(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "preferred_helper"
        )
        or "package_reverse_flow_focused_validation_continue_dispatch_inventory"
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_call_execute"
    ] = bool(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "call_follow_with_execute"
        )
    )
    state[
        "reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_ready"
    ] = bool(
        continue_cascade_wake_route_apply_follow_pin_call_next_call_follow.get(
            "follow_ready"
        )
    )
    return state


def attach_skill_route_discovery_pipeline_operator_state(
    pipeline: dict[str, Any],
) -> dict[str, Any]:
    """Attach durable operator-state fields onto a skill-route pipeline packet.

    Build and record/close helpers call this so supervisors can continue reverse-flow
    focused validation from packet fields without re-rendering markdown.
    """

    if not isinstance(pipeline, dict):
        raise TypeError("pipeline must be a dict")
    state = resolve_skill_route_discovery_pipeline_operator_state(pipeline)
    updated = dict(pipeline)
    updated.update(state)
    updated["operator_state"] = dict(state)
    return updated


def render_skill_route_discovery_capability_pipeline_lines(
    pipeline: dict[str, Any],
) -> list[str]:
    """Render the skill-route capability pipeline for kernel tasks."""

    if not pipeline:
        return []
    selected = pipeline.get("selected_step") if isinstance(pipeline.get("selected_step"), dict) else {}
    local_comparison = (
        pipeline.get("local_comparison")
        if isinstance(pipeline.get("local_comparison"), dict)
        else {}
    )
    reverse_flow_lane = (
        pipeline.get("reverse_flow_test_validation_lane")
        if isinstance(pipeline.get("reverse_flow_test_validation_lane"), dict)
        else {}
    )
    rnskill_docs_lane = (
        pipeline.get("rnskill_docs_validation_lane")
        if isinstance(pipeline.get("rnskill_docs_validation_lane"), dict)
        else {}
    )
    config_gates = (
        pipeline.get("config_gate_boundary")
        if isinstance(pipeline.get("config_gate_boundary"), dict)
        else {}
    )
    local_apply = (
        pipeline.get("local_apply") if isinstance(pipeline.get("local_apply"), dict) else {}
    )
    local_apply_completion = (
        pipeline.get("local_apply_completion")
        if isinstance(pipeline.get("local_apply_completion"), dict)
        else {}
    )
    unlocked_test_lane_apply = (
        pipeline.get("unlocked_local_test_lane_apply")
        if isinstance(pipeline.get("unlocked_local_test_lane_apply"), dict)
        else {}
    )
    focused_local_test_validation = (
        pipeline.get("focused_local_test_validation")
        if isinstance(pipeline.get("focused_local_test_validation"), dict)
        else {}
    )
    activation_external_handoff = (
        pipeline.get("focused_validation_activation_external_handoff")
        if isinstance(pipeline.get("focused_validation_activation_external_handoff"), dict)
        else {}
    )
    activation_external_acceptance = (
        pipeline.get("focused_validation_activation_external_acceptance")
        if isinstance(pipeline.get("focused_validation_activation_external_acceptance"), dict)
        else {}
    )
    residual_adjacent_queue = (
        pipeline.get("focused_validation_residual_adjacent_queue")
        if isinstance(pipeline.get("focused_validation_residual_adjacent_queue"), dict)
        else {}
    )
    residual_adjacent_local_apply = (
        pipeline.get("residual_adjacent_harness_eval_local_apply")
        if isinstance(pipeline.get("residual_adjacent_harness_eval_local_apply"), dict)
        else {}
    )
    residual_adjacent_local_comparison = (
        pipeline.get("residual_adjacent_harness_eval_local_comparison")
        if isinstance(
            pipeline.get("residual_adjacent_harness_eval_local_comparison"), dict
        )
        else {}
    )
    residual_adjacent_unlocked_lane_apply = (
        pipeline.get("residual_adjacent_unlocked_local_lane_apply")
        if isinstance(
            pipeline.get("residual_adjacent_unlocked_local_lane_apply"), dict
        )
        else {}
    )
    residual_adjacent_focused_local_validation = (
        pipeline.get("residual_adjacent_focused_local_validation")
        if isinstance(
            pipeline.get("residual_adjacent_focused_local_validation"), dict
        )
        else {}
    )
    residual_adjacent_focused_validation_activation_external_handoff = (
        pipeline.get("residual_adjacent_focused_validation_activation_external_handoff")
        if isinstance(
            pipeline.get(
                "residual_adjacent_focused_validation_activation_external_handoff"
            ),
            dict,
        )
        else {}
    )
    residual_adjacent_focused_validation_activation_external_acceptance = (
        pipeline.get(
            "residual_adjacent_focused_validation_activation_external_acceptance"
        )
        if isinstance(
            pipeline.get(
                "residual_adjacent_focused_validation_activation_external_acceptance"
            ),
            dict,
        )
        else {}
    )
    adjacent_handoff = (
        pipeline.get("adjacent_agent_harness_eval_handoff")
        if isinstance(pipeline.get("adjacent_agent_harness_eval_handoff"), dict)
        else {}
    )
    stages = ", ".join(str(stage) for stage in pipeline.get("pipeline_stages") or [])
    profiles = ", ".join(str(profile) for profile in selected.get("route_profiles") or [])
    operator_state = resolve_skill_route_discovery_pipeline_operator_state(pipeline)
    supervisor_next = operator_state["supervisor_next_action"]
    residual_selected_proposal_for_render = operator_state[
        "residual_adjacent_selected_proposal_id"
    ]
    reverse_flow_focused_hold_active = operator_state[
        "residual_adjacent_held_until_reverse_flow_focused_validation_recorded"
    ]
    residual_selection_held = operator_state[
        "residual_adjacent_selection_held_until_residual_active"
    ]
    residual_export_held = operator_state[
        "residual_adjacent_export_held_on_reverse_flow_surfaces"
    ]
    lines = [
        "Skill route discovery capability pipeline:",
        f"- Status: `{pipeline.get('status', '')}`",
        f"- Pipeline stages: `{stages or 'none'}`",
        f"- Selected proposal: `{selected.get('proposal_id') or 'none'}`",
        f"- Route class: `{selected.get('route_class') or 'none'}`",
        f"- Capability action: `{selected.get('capability_action') or 'none'}`",
        f"- Route profiles: `{profiles or 'none'}`",
        f"- Selected local lane: `{selected.get('selected_local_lane') or 'none'}`",
        f"- Unlocked local lanes: `{', '.join(selected.get('unlocked_local_lanes') or []) or 'none'}`",
        f"- Local comparison required: `{bool(selected.get('local_comparison_required'))}`",
        f"- Local comparison status: `{selected.get('local_comparison_status') or 'none'}`",
        f"- Local comparison decision: `{local_comparison.get('decision') or 'none'}`",
        f"- Reverse-flow test validation lane: `{reverse_flow_lane.get('status') or 'none'}`",
        f"- Rnskill docs validation lane: `{rnskill_docs_lane.get('status') or 'none'}`",
        f"- Config gate boundary: `{config_gates.get('status') or 'none'}`",
        f"- Local apply handoff: `{local_apply.get('status') or 'none'}`",
        f"- Local apply decision: `{local_apply.get('decision') or 'none'}`",
        f"- Local apply completion: `{local_apply_completion.get('status') or 'none'}`",
        f"- Local apply completion decision: `{local_apply_completion.get('decision') or 'none'}`",
        f"- Unlocked local test lane apply: `{unlocked_test_lane_apply.get('status') or 'none'}`",
        f"- Unlocked local test lane apply decision: `{unlocked_test_lane_apply.get('decision') or 'none'}`",
        f"- Focused local test validation: `{focused_local_test_validation.get('status') or 'none'}`",
        f"- Focused local test validation decision: `{focused_local_test_validation.get('decision') or 'none'}`",
        f"- Focused local test validation recorded: `"
        f"{bool(focused_local_test_validation.get('focused_validation_recorded') or pipeline.get('focused_local_test_validation_recorded'))}`",
        f"- Focused validation results cover expected: `"
        f"{bool((focused_local_test_validation.get('focused_validation') or {}).get('results_cover_expected'))}`",
        f"- Focused validation activation-external handoff: `"
        f"{activation_external_handoff.get('status') or 'none'}`",
        f"- Focused validation activation-external decision: `"
        f"{activation_external_handoff.get('decision') or 'none'}`",
        f"- Focused validation activation-external acceptance: `"
        f"{activation_external_acceptance.get('status') or 'none'}`",
        f"- Focused validation activation-external acceptance decision: `"
        f"{activation_external_acceptance.get('decision') or 'none'}`",
        f"- Focused validation residual adjacent queue: `"
        f"{residual_adjacent_queue.get('status') or 'none'}`",
        f"- Focused validation residual adjacent queue decision: `"
        f"{residual_adjacent_queue.get('decision') or 'none'}`",
        f"- Residual adjacent harness-eval local apply: `"
        f"{residual_adjacent_local_apply.get('status') or 'none'}`",
        f"- Residual adjacent harness-eval local apply decision: `"
        f"{residual_adjacent_local_apply.get('decision') or 'none'}`",
        f"- Residual adjacent harness-eval local comparison: `"
        f"{residual_adjacent_local_comparison.get('status') or 'none'}`",
        f"- Residual adjacent harness-eval local comparison decision: `"
        f"{residual_adjacent_local_comparison.get('decision') or 'none'}`",
        f"- Residual adjacent harness unlocked lanes: `"
        f"{', '.join(residual_adjacent_local_comparison.get('unlocked_local_lanes') or []) or 'none'}`",
        f"- Residual adjacent unlocked local lane apply: `"
        f"{residual_adjacent_unlocked_lane_apply.get('status') or 'none'}`",
        f"- Residual adjacent unlocked local lane apply decision: `"
        f"{residual_adjacent_unlocked_lane_apply.get('decision') or 'none'}`",
        f"- Residual adjacent unlocked selected lane: `"
        f"{residual_adjacent_unlocked_lane_apply.get('selected_local_lane') or 'none'}`",
        f"- Residual adjacent focused local validation: `"
        f"{residual_adjacent_focused_local_validation.get('status') or 'none'}`",
        f"- Residual adjacent focused local validation decision: `"
        f"{residual_adjacent_focused_local_validation.get('decision') or 'none'}`",
        f"- Residual adjacent focused local validation recorded: `"
        f"{bool(residual_adjacent_focused_local_validation.get('focused_validation_recorded') or pipeline.get('residual_adjacent_focused_local_validation_recorded'))}`",
        f"- Residual adjacent focused results cover expected: `"
        f"{bool((residual_adjacent_focused_local_validation.get('focused_validation') or {}).get('results_cover_expected'))}`",
        f"- Residual adjacent focused validation activation-external handoff: `"
        f"{residual_adjacent_focused_validation_activation_external_handoff.get('status') or 'none'}`",
        f"- Residual adjacent focused validation activation-external decision: `"
        f"{residual_adjacent_focused_validation_activation_external_handoff.get('decision') or 'none'}`",
        f"- Residual adjacent focused validation activation-external acceptance: `"
        f"{residual_adjacent_focused_validation_activation_external_acceptance.get('status') or 'none'}`",
        f"- Residual adjacent focused validation activation-external acceptance decision: `"
        f"{residual_adjacent_focused_validation_activation_external_acceptance.get('decision') or 'none'}`",
        f"- Residual adjacent remaining residual rows: `"
        f"{', '.join(residual_adjacent_focused_validation_activation_external_acceptance.get('remaining_residual_adjacent_proposal_ids') or residual_adjacent_focused_validation_activation_external_handoff.get('remaining_residual_adjacent_proposal_ids') or []) or 'none'}`",
        f"- Residual adjacent selected proposal: `"
        f"{residual_selected_proposal_for_render or 'none'}`",
        f"- Residual adjacent held until reverse-flow focused validation recorded: `"
        f"{reverse_flow_focused_hold_active}`",
        f"- Residual adjacent selection held until residual-active: `"
        f"{residual_selection_held}`",
        f"- Residual adjacent export held on reverse-flow surfaces: `"
        f"{residual_export_held}`",
        f"- Residual export allowed: `"
        f"{bool(operator_state.get('residual_export_allowed'))}`",
        f"- Reverse-flow bound: `{bool(operator_state.get('reverse_flow_bound'))}`",
        f"- Reverse-flow bound proposal: `"
        f"{operator_state.get('reverse_flow_bound_proposal_id') or 'none'}`",
        f"- Reverse-flow bound source marker: `"
        f"{operator_state.get('reverse_flow_bound_source_marker') or 'none'}`",
        f"- Reverse-flow focused validation partial results recorded: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_partial_results_recorded'))}`",
        f"- Reverse-flow focused validation recorded result count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_recorded_result_count') or 0)}`"
        f" / `{int(operator_state.get('reverse_flow_focused_validation_expected_command_count') or 0)}`",
        f"- Reverse-flow focused validation recorded command hash count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_recorded_command_hash_count') or 0)}`",
        f"- Reverse-flow focused validation missing command hash count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_missing_command_hash_count') or 0)}`",
        f"- Reverse-flow focused validation pending command count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_pending_command_count') or 0)}`",
        f"- Reverse-flow focused validation pending work unit count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_pending_work_unit_count') or 0)}`",
        f"- Reverse-flow focused validation continue plan mode: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_plan_mode') or 'none'}`",
        f"- Reverse-flow focused validation continue-run executable: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_run_executable'))}`",
        f"- Reverse-flow focused validation continue-run recommended: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_run_recommended'))}`",
        f"- Reverse-flow focused validation continue-run runnable work unit count: `"
        f"{int(operator_state.get('reverse_flow_focused_validation_continue_runnable_work_unit_count') or 0)}`",
        f"- Reverse-flow focused validation continue dispatch action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_dispatch_action') or 'none'}`",
        f"- Reverse-flow focused validation continue dispatch execute recommended: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_dispatch_execute_recommended'))}`",
        f"- Reverse-flow focused validation continue dispatch follow-through action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_dispatch_follow_through_action') or 'none'}`",
        f"- Reverse-flow focused validation continue dispatch call with execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_dispatch_call_with_execute'))}`",
        f"- Reverse-flow focused validation continue dispatch helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_dispatch_helper') or 'dispatch_reverse_flow_focused_validation_continue_supervisor_wake'}`",
        f"- Reverse-flow focused validation continue dispatch inventory helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_dispatch_inventory_helper') or 'package_reverse_flow_focused_validation_continue_dispatch_inventory'}`",
        f"- Reverse-flow focused validation continue dispatch follow-through helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_dispatch_follow_through_helper') or 'follow_reverse_flow_focused_validation_continue_dispatch'}`",
        f"- Reverse-flow focused validation continue progress label: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_progress_label') or '0/0'}`",
        f"- Reverse-flow focused validation continue action line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_action_line') or 'none'}`",
        f"- Reverse-flow focused validation continue operator card helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_operator_card_helper') or 'package_reverse_flow_focused_validation_continue_operator_card'}`",
        f"- Reverse-flow focused validation continue progress transition helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_progress_transition_helper') or 'package_reverse_flow_focused_validation_continue_progress_transition'}`",
        f"- Reverse-flow focused validation continue exec receipt helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_exec_receipt_helper') or 'package_reverse_flow_focused_validation_continue_exec_receipt'}`",
        f"- Reverse-flow focused validation continue finish receipt helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_finish_receipt_helper') or 'package_reverse_flow_focused_validation_continue_finish_receipt'}`",
        f"- Reverse-flow focused validation continue residual open helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_open_helper') or 'package_reverse_flow_focused_validation_continue_residual_open'}`",
        f"- Reverse-flow focused validation continue residual open line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_open_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual entry helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_entry_helper') or 'package_reverse_flow_focused_validation_continue_residual_entry'}`",
        f"- Reverse-flow focused validation continue residual entry line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_entry_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual entry selected: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_selected_residual_proposal_id') or 'none'}`",
        f"- Reverse-flow focused validation continue residual follow helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_follow_helper') or 'package_reverse_flow_focused_validation_continue_residual_follow'}`",
        f"- Reverse-flow focused validation continue residual follow line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_follow_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual follow action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_follow_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual follow call comparison: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_call_residual_comparison'))}`",
        f"- Reverse-flow focused validation continue residual comparison helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_comparison_helper') or 'package_reverse_flow_focused_validation_continue_residual_comparison'}`",
        f"- Reverse-flow focused validation continue residual comparison line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_comparison_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual comparison action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_comparison_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual comparison call unlocked apply: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_call_residual_unlocked_apply'))}`",
        f"- Reverse-flow focused validation continue residual unlocked apply helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_unlocked_apply_helper') or 'package_reverse_flow_focused_validation_continue_residual_unlocked_apply'}`",
        f"- Reverse-flow focused validation continue residual unlocked apply line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_unlocked_apply_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual unlocked apply action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_unlocked_apply_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual unlocked apply call focused validation: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_call_residual_focused_validation'))}`",
        f"- Reverse-flow focused validation continue residual focused validation helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_focused_validation_helper') or 'package_reverse_flow_focused_validation_continue_residual_focused_validation'}`",
        f"- Reverse-flow focused validation continue residual focused validation line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_focused_validation_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual focused validation action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_focused_validation_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual focused validation call handoff: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_call_residual_handoff'))}`",
        f"- Reverse-flow focused validation continue residual handoff helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_handoff_helper') or 'package_reverse_flow_focused_validation_continue_residual_handoff'}`",
        f"- Reverse-flow focused validation continue residual handoff line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_handoff_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual handoff action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_handoff_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual handoff call acceptance: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_call_residual_acceptance'))}`",
        f"- Reverse-flow focused validation continue residual acceptance helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_acceptance_helper') or 'package_reverse_flow_focused_validation_continue_residual_acceptance'}`",
        f"- Reverse-flow focused validation continue residual acceptance line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_acceptance_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual acceptance action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_acceptance_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual acceptance ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_residual_acceptance_ready'))}`",
        f"- Reverse-flow focused validation continue residual cascade helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_helper') or 'package_reverse_flow_focused_validation_continue_residual_cascade'}`",
        f"- Reverse-flow focused validation continue residual cascade line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_line') or 'none'}`",
        f"- Reverse-flow focused validation continue residual cascade action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue residual cascade ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_ready'))}`",
        f"- Reverse-flow focused validation continue residual cascade progress: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_progress_label') or '0/8'}`",
        f"- Reverse-flow focused validation continue residual cascade blocked at: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_residual_cascade_blocked_at') or 'open'}`",
        f"- Reverse-flow focused validation continue cascade helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_helper') or 'package_reverse_flow_focused_validation_continue_cascade'}`",
        f"- Reverse-flow focused validation continue cascade line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_action') or 'wait_for_reverse_flow'}`",
        f"- Reverse-flow focused validation continue cascade ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_ready'))}`",
        f"- Reverse-flow focused validation continue cascade reverse progress: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_reverse_progress_label') or '0/0'}`",
        f"- Reverse-flow focused validation continue cascade residual progress: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_residual_progress_label') or '0/8'}`",
        f"- Reverse-flow focused validation continue cascade residual blocked at: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_residual_blocked_at') or 'open'}`",
        f"- Reverse-flow focused validation continue cascade transition helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_transition_helper') or 'package_reverse_flow_focused_validation_continue_cascade_transition'}`",
        f"- Reverse-flow focused validation continue cascade transition line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_transition_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade advanced: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_advanced'))}`",
        f"- Reverse-flow focused validation continue cascade reverse progress transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_reverse_progress_transition') or '0/0→0/0'}`",
        f"- Reverse-flow focused validation continue cascade residual progress transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_residual_progress_transition') or '0/8→0/8'}`",
        f"- Reverse-flow focused validation continue cascade wake helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake'}`",
        f"- Reverse-flow focused validation continue cascade wake line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake outcome: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_outcome') or 'identity'}`",
        f"- Reverse-flow focused validation continue cascade wake route helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route'}`",
        f"- Reverse-flow focused validation continue cascade wake route line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_action') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route call execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_call_execute'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route action transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_action_transition') or 'inventory_only→inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route advanced: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_advanced'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_action') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow preferred helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_preferred_helper') or 'package_reverse_flow_focused_validation_continue_dispatch_inventory'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow call execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_call_execute'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_action') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin mode: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_mode') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin preferred helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_preferred_helper') or 'package_reverse_flow_focused_validation_continue_dispatch_inventory'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_execute'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_ready'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin action transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_action_transition') or 'inventory_only→inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin mode transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_mode_transition') or 'inventory_only→inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin advanced: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_advanced'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_action') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next invoke: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_invoke') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next preferred helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_preferred_helper') or 'package_reverse_flow_focused_validation_continue_dispatch_inventory'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_execute'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_ready'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next action transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_action_transition') or 'inventory_only→inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next invoke transition: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_invoke_transition') or 'inventory_only→inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next advanced: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_advanced'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper') or 'package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow line: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line') or 'none'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow action: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_action') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow mode: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_mode') or 'inventory_only'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow preferred helper: `"
        f"{operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_preferred_helper') or 'package_reverse_flow_focused_validation_continue_dispatch_inventory'}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow call execute: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_call_execute'))}`",
        f"- Reverse-flow focused validation continue cascade wake route apply follow pin call next call follow ready: `"
        f"{bool(operator_state.get('reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_ready'))}`",
        f"- Reverse-flow continue decision: `"
        f"{operator_state.get('reverse_flow_continue_decision') or 'none'}`",
        f"- Adjacent agent harness-eval handoff: `{adjacent_handoff.get('status') or 'none'}`",
        f"- Adjacent handoff decision: `{adjacent_handoff.get('decision') or 'none'}`",
        f"- Theme complete: `{bool(local_apply_completion.get('theme_complete'))}`",
        f"- Supervisor next action: `{supervisor_next}`",
        f"- Skill route discovery first: `{bool(selected.get('skill_route_discovery_first'))}`",
        f"- Autonomous local apply for selected step: `{bool(selected.get('autonomous_local_apply'))}`",
        f"- Runtime action: `{pipeline.get('runtime_action', 'none')}`",
        "- Prefer this skill-route pipeline over isolated notes when the skill-route-discovery theme is active.",
        "- Map only to documentation, config, test, or code_patch; keep external skill execution and provider launch denied.",
        "- Keep privacy-leakage and offensive-behavior rows review-only; do not export raw evidence URLs or sensitive bodies.",
        "- Pass 2 locks reverse-flow codex_workflow_gate into a bounded local test lane only after pipeline-stage comparison.",
        "- Pass 3 packages reverse-flow local apply with body-free rnskill docs companion and config-gate boundaries.",
        "- Pass 4 completes the reverse-flow local test validation lane via skill_route_discovery_local_apply_completion.",
        "- After completion, skill_route_discovery_unlocked_local_test_lane_apply packages focused local test validation while activation stays external.",
        "- After unlock, skill_route_discovery_focused_local_test_validation records body-free command-hash results and keeps activation external.",
        "- While reverse-flow focused validation is ready/unrecorded or failed, residual fortress/Hy3 stages stay held; supervisor_next stays on reverse-flow focused validation and residual selected proposal is not advertised on residual packets or render.",
        "- Residual fortress/Hy3 selected_residual_proposal_id is exported only when residual work is residual-active; reverse-flow-waiting residual stages leave selection empty.",
        "- Reverse-flow focused validation, activation-external handoff/acceptance, and residual queue also hold residual adjacent_general_agent_proposal_ids and residual_adjacent_harness_eval_available until reverse-flow record/close and residual-active readiness; do not pre-export fortress IDs while reverse-flow waits.",
        "- Pipeline operator_state durably exports supervisor_next_action, residual hold/export flags, reverse_flow_continue_decision, reverse-flow-skill evidence binding, residual_export_allowed, partial body-free command-hash coverage, recorded/missing command-hash inventories, pending command texts/counts, pending work unit pairs (command + hash), and continue_plan mode after build and after record/close so supervisors continue reverse-flow without re-rendering markdown.",
        "- build_reverse_flow_focused_validation_continue_plan unifies zero-row first wakes (mode=run_pending) and multi-wake partial continue (mode=record_remaining) around pending_work_units (command + command_hash pairs) plus pending_commands / missing_command_hashes; residual export stays denied until reverse-flow record/close and residual-active cascade.",
        "- record_reverse_flow_focused_validation_continue_outcomes materializes body-free rows from continue-plan pending work unit outcomes and merges them; materialize_reverse_flow_focused_validation_continue_record_rows accepts hash-map, parallel bool, or row-dict outcomes for pending units only.",
        "- run_reverse_flow_focused_validation_continue_pending_work_units builds a local continue-run plan from pending_work_units, executes only allowlisted local pytest inventory lines (no shell metacharacters), and optionally records body-free outcomes; build_reverse_flow_focused_validation_continue_run_plan and execute_reverse_flow_focused_validation_continue_run_plan stay inspectable and never export stdout or enable activation.",
        "- resolve_reverse_flow_focused_validation_continue_run_supervisor_wake packages reverse-flow-first supervisor_next after continue-run inventory/run (mode, residual hold, handoff/acceptance status) so residual fortress stages stay blocked until reverse-flow record/close and activation-external acceptance; run-and-record attaches supervisor_wake without enabling activation.",
        "- package_reverse_flow_focused_validation_continue_dispatch_inventory packages a body-free inventory dispatch packet (action, execute_recommended, residual hold) without running commands so supervisors can decide whether to execute without re-deriving nested wake fields; residual export stays denied.",
        "- resolve_reverse_flow_focused_validation_continue_dispatch_follow_through collapses inventory action + execute_recommended into follow_through_action (execute_now|wait_for_local_allowlist|keep_activation_external|repair|noop) and call_dispatch_with_execute so supervisors do not re-derive execute policy from nested wake fields; residual export stays denied.",
        "- package_reverse_flow_focused_validation_continue_operator_card collapses progress (recorded/expected label such as 0/3), follow_through_action, preferred helper, residual hold, supervisor_next, and a body-free action_line into one operator card so supervisors do not re-assemble nested operator_state fields; residual export stays denied.",
        "- package_reverse_flow_focused_validation_continue_progress_transition collapses pre/post operator cards into progress_transition_label (for example 0/3→3/3), progress_advanced, follow_through_transition, recorded_delta, and transition_line so supervisors do not compare nested progress_label fields after continue wakes; residual export stays denied.",
        "- package_reverse_flow_focused_validation_continue_exec_receipt collapses run_plan + run_result + recorded into exec_line (for example exec mode=run_pending ran=3 passed=3 failed=0 skipped=0 recorded=true) and exec_plan_line so supervisors do not re-derive nested unit_results after continue execute wakes; residual export stays denied and stdout is never exported.",
        "- package_reverse_flow_focused_validation_continue_finish_receipt collapses post-continue progress, focused status, handoff/acceptance, and residual hold into body-free finish_line plus continue_finished / residual_queue_ready; residual export stays denied on continue surfaces even when residual_queue_ready is true.",
        "- package_reverse_flow_focused_validation_continue_residual_open collapses finish residual_queue_ready plus focused_validation_residual_adjacent_queue into body-free residual_open_line (for example residual_open ready=true count=1 status=ready next=hand_off_residual_adjacent_rows_to_agent_harness_eval_cluster_local_apply residual_export=false helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_apply) so residual pipeline entry is legible without nested re-assembly; residual export stays denied on continue surfaces.",
        "- package_reverse_flow_focused_validation_continue_residual_entry collapses residual open plus residual adjacent harness-eval local apply selection into body-free residual_entry_line (for example residual_entry ready=true selected=prop-harness-fortress-local-eval status=ready count=1 next=run_agent_harness_eval_local_comparison_for_residual_adjacent_row residual_export=false helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_apply) so residual selection is legible without nested re-assembly; residual export stays denied on continue surfaces and selected residual IDs stay empty while residual open is blocked.",
        "- package_reverse_flow_focused_validation_continue_residual_follow collapses residual entry into body-free residual_follow_line (for example residual_follow ready=true selected=prop-harness-fortress-local-eval action=open_residual_harness_eval_local_comparison call_comparison=true residual_export=false next=run_agent_harness_eval_local_comparison_for_residual_adjacent_row helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison) so residual comparison follow-through is legible without nested re-assembly; residual export stays denied on continue surfaces and call_residual_comparison is informational policy only.",
        "- package_reverse_flow_focused_validation_continue_residual_comparison collapses residual follow into body-free residual_comparison_line (for example residual_comparison ready=true selected=prop-harness-fortress-local-eval status=ready comparison=passed_local_comparison unlocked=documentation,test,code_patch action=open_residual_unlocked_local_lane_apply call_unlocked_apply=true residual_export=false next=apply_unlocked_documentation_test_or_code_patch_with_focused_validation_and_keep_activation_external helper=build_skill_route_discovery_residual_adjacent_harness_eval_local_comparison) so residual comparison readiness and unlocked-lane policy are legible without nested re-assembly; residual export stays denied on continue surfaces and call_residual_unlocked_apply is informational policy only.",
        "- package_reverse_flow_focused_validation_continue_residual_unlocked_apply collapses residual comparison into body-free residual_unlocked_apply_line (for example residual_unlocked_apply ready=true selected=prop-harness-fortress-local-eval status=ready lane=test preferred=test unlocked=documentation,test,code_patch comparison_ready=true action=open_residual_focused_local_validation call_focused_validation=true residual_export=false next=run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external helper=build_skill_route_discovery_residual_adjacent_unlocked_local_lane_apply) so residual unlocked-apply readiness and preferred test-first focused-validation policy are legible without nested re-assembly; residual export stays denied on continue surfaces and call_residual_focused_validation is informational policy only.",
        "- package_reverse_flow_focused_validation_continue_residual_focused_validation collapses residual unlocked apply into body-free residual_focused_validation_line (for example residual_focused_validation ready=true selected=prop-harness-fortress-local-eval status=ready lane=test preferred=test progress=0/3 unlocked_apply_ready=true action=run_residual_focused_validation call_handoff=false residual_export=false next=run_focused_local_validation_for_residual_adjacent_unlocked_lane_and_keep_activation_external helper=build_skill_route_discovery_residual_adjacent_focused_local_validation) so residual focused-validation readiness, body-free command-hash progress, and activation-external handoff policy are legible without nested re-assembly; residual export stays denied on continue surfaces and call_residual_handoff is informational policy only (true only after residual focused validation records a pass).",
        "- package_reverse_flow_focused_validation_continue_residual_handoff collapses residual focused validation into body-free residual_handoff_line (for example residual_handoff ready=true selected=prop-harness-fortress-local-eval status=ready lane=test preferred=test remaining=0 focused_ready=true action=open_residual_activation_external_acceptance call_acceptance=true residual_export=false next=keep_activation_external_after_residual_adjacent_focused_local_validation helper=build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff) so residual activation-external handoff readiness, remaining residual IDs, and acceptance policy are legible without nested re-assembly; residual export stays denied on continue surfaces and call_residual_acceptance is informational policy only (true only after residual handoff is ready).",
        "- package_reverse_flow_focused_validation_continue_residual_acceptance collapses residual handoff into body-free residual_acceptance_line (for example residual_acceptance ready=true selected=prop-harness-fortress-local-eval status=accepted lane=test preferred=test remaining=0 handoff_ready=true action=keep_activation_external residual_export=false next=keep_activation_external_after_residual_adjacent_focused_local_validation helper=build_skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance) so residual activation-external acceptance readiness, remaining residual IDs, and keep_activation_external policy are legible without nested re-assembly; residual export stays denied on continue surfaces even when residual acceptance is accepted.",
        "- package_reverse_flow_focused_validation_continue_residual_cascade collapses residual acceptance into body-free residual_cascade_line (for example residual_cascade ready=true selected=prop-harness-fortress-local-eval status=complete progress=8/8 blocked_at=none stages=open,entry,follow,comparison,unlocked_apply,focused_validation,handoff,acceptance remaining=0 action=keep_activation_external residual_export=false next=keep_activation_external_after_residual_adjacent_focused_local_validation helper=package_reverse_flow_focused_validation_continue_residual_acceptance) so residual cascade stage progress, blocked stage, remaining residual IDs, and keep_activation_external policy are legible without nested re-assembly; residual export stays denied on continue surfaces even when residual cascade is complete.",
        "- package_reverse_flow_focused_validation_continue_cascade collapses reverse-flow continue progress plus residual cascade into body-free continue_cascade_line (for example continue_cascade ready=false reverse_progress=0/3 residual_progress=0/8 residual_blocked_at=open reverse_action=execute_now residual_action=wait_for_reverse_flow action=execute_now call_execute=true residual_export=false next=run_focused_local_test_validation_then_keep_activation_external helper=follow_reverse_flow_focused_validation_continue_dispatch) so reverse-flow progress, residual cascade progress/blocked_at, and reverse-flow-first next action are legible without nested re-assembly; residual export stays denied on continue surfaces even when continue cascade is complete.",
        "- package_reverse_flow_focused_validation_continue_cascade_transition collapses pre/post continue_cascade packages into body-free continue_cascade_transition_line (for example continue_cascade_transition reverse=0/3→3/3 residual=0/8→0/8 blocked_at=open→open action=execute_now→keep_activation_external reverse_advanced=true residual_advanced=false cascade_advanced=true ready=false→false executed=true recorded=true residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_cascade_transition) so reverse-flow progress, residual cascade progress/blocked_at, and cascade action transitions are legible after continue wakes without nested pre/post comparison; residual export stays denied on continue surfaces even when reverse progress completes.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake collapses continue_cascade_transition plus exec_receipt, finish_receipt, and residual_open into body-free continue_cascade_wake_line with classified wake_outcome (for example continue_cascade_wake outcome=reverse_complete reverse=0/3→3/3 residual=0/8→0/8 cascade_advanced=true executed=true recorded=true finished=true residual_open=false residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_cascade_wake) so supervisors pin one wake outcome enum instead of re-assembling cascade_transition + exec + finish + residual_open; residual export stays denied on continue surfaces even when residual_open becomes ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route collapses continue_cascade_wake into body-free continue_cascade_wake_route_line with classified route_action (for example continue_cascade_wake_route outcome=execute_recommended action=execute_now call_execute=true residual_route=false reverse=0/3→0/3 residual=0/8→0/8 residual_export=false next=run_focused_local_test_validation_then_keep_activation_external helper=follow_reverse_flow_focused_validation_continue_dispatch route_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route) so supervisors pin one route action enum and preferred helper instead of re-assembling wake_outcome + follow policy; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply collapses pre/post continue_cascade_wake_route into body-free continue_cascade_wake_route_apply_line with pre→post route action transition (for example continue_cascade_wake_route_apply pre_action=execute_now post_action=keep_activation_external action=execute_now→keep_activation_external route_advanced=true call_execute=true→false residual_route=false→false reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply) so supervisors pin one apply receipt instead of re-comparing nested route packets after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow collapses continue_cascade_wake_route_apply into body-free continue_cascade_wake_route_apply_follow_line with applied route → preferred helper (for example continue_cascade_wake_route_apply_follow applied=keep_activation_external advanced=true executed=true recorded=true call_execute=false residual_route=false reverse=0/3→3/3 residual=0/8→0/8 residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_finish_receipt apply_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply) so supervisors pin one follow receipt instead of re-mapping applied route actions after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin collapses continue_cascade_wake_route_apply_follow into body-free continue_cascade_wake_route_apply_follow_pin_line with classified pin mode (for example continue_cascade_wake_route_apply_follow_pin action=execute_now mode=execute_helper call_execute=true pin_ready=true residual_route=false reverse=0/3→0/3 residual=0/8→0/8 residual_export=false next=run_focused_local_test_validation_then_keep_activation_external helper=follow_reverse_flow_focused_validation_continue_dispatch follow_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow) so supervisors pin one call recipe instead of re-deriving execute vs package policy after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call collapses pre/post continue_cascade_wake_route_apply_follow_pin into body-free continue_cascade_wake_route_apply_follow_pin_call_line with pre→post pin action/mode transition (for example continue_cascade_wake_route_apply_follow_pin_call pre_action=execute_now post_action=keep_activation_external action=execute_now→keep_activation_external mode=execute_helper→package_helper call_execute=true→false pin_advanced=true pin_ready=true→true residual_route=false→false reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_finish_receipt pin_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin) so supervisors pin one call receipt instead of re-comparing nested pin packets after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next seals continue_cascade_wake_route_apply_follow_pin_call into body-free continue_cascade_wake_route_apply_follow_pin_call_next_line with next invoke policy (for example continue_cascade_wake_route_apply_follow_pin_call_next action=keep_activation_external mode=package_helper invoke=package_helper advanced=true call_execute=false next_ready=true residual_route=false reverse=0/3→3/3 residual=0/8→0/8 residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_finish_receipt pin_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call) so supervisors pin one next-invoke recipe instead of re-deriving execute vs package policy from nested pin_call transitions after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call collapses pre/post continue_cascade_wake_route_apply_follow_pin_call_next into body-free continue_cascade_wake_route_apply_follow_pin_call_next_call_line with pre→post next action/invoke transition (for example continue_cascade_wake_route_apply_follow_pin_call_next_call pre_action=execute_now post_action=keep_activation_external action=execute_now→keep_activation_external invoke=execute_helper→package_helper call_execute=true→false next_advanced=true next_ready=true→true residual_route=false→false reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_finish_receipt pin_call_next_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next) so supervisors pin one next-call receipt instead of re-comparing nested pin_call_next packets after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call_follow maps continue_cascade_wake_route_apply_follow_pin_call_next_call into body-free continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line with applied next → preferred helper (for example continue_cascade_wake_route_apply_follow_pin_call_next_call_follow applied=keep_activation_external invoke=package_helper mode=package_helper advanced=true call_execute=false follow_ready=true residual_route=false reverse=0/3→3/3 residual=0/8→0/8 executed=true recorded=true residual_export=false next=keep_activation_external_after_focused_local_test_validation helper=package_reverse_flow_focused_validation_continue_finish_receipt next_call_helper=package_reverse_flow_focused_validation_continue_cascade_wake_route_apply_follow_pin_call_next_call) so supervisors pin one next-call follow receipt instead of re-mapping applied next action/invoke after continue wakes; residual export stays denied on continue surfaces even when residual_route opens after residual_open_ready.",
        "- follow_reverse_flow_focused_validation_continue_dispatch is the preferred policy-aware operator entry: package inventory, resolve follow-through, call dispatch with execute only when call_dispatch_with_execute is true, and attach post_follow_through plus operator_card/post_operator_card, progress_transition, exec_receipt, finish_receipt, residual_open, residual_entry, residual_follow, residual_comparison, residual_unlocked_apply, residual_focused_validation, residual_handoff, residual_acceptance, residual_cascade, continue_cascade, continue_cascade_transition, continue_cascade_wake, continue_cascade_wake_route, continue_cascade_wake_route_apply, continue_cascade_wake_route_apply_follow, continue_cascade_wake_route_apply_follow_pin, continue_cascade_wake_route_apply_follow_pin_call, continue_cascade_wake_route_apply_follow_pin_call_next, continue_cascade_wake_route_apply_follow_pin_call_next_call, and continue_cascade_wake_route_apply_follow_pin_call_next_call_follow after run/record; residual fortress stages stay blocked until reverse-flow record/close and activation-external acceptance.",
        "- dispatch_reverse_flow_focused_validation_continue_supervisor_wake remains the low-level single operator entry: inventory packet first (via package_reverse_flow_focused_validation_continue_dispatch_inventory), optional allowlisted run/record when continue_run_executable, always reverse-flow-first supervisor_wake plus post_dispatch_inventory, follow_through, operator_card progress labels, progress_transition, exec_receipt, finish_receipt, residual_open, residual_entry, residual_follow, residual_comparison, residual_unlocked_apply, residual_focused_validation, residual_handoff, residual_acceptance, residual_cascade, continue_cascade, continue_cascade_transition, continue_cascade_wake, continue_cascade_wake_route, continue_cascade_wake_route_apply, continue_cascade_wake_route_apply_follow, continue_cascade_wake_route_apply_follow_pin, continue_cascade_wake_route_apply_follow_pin_call, continue_cascade_wake_route_apply_follow_pin_call_next, continue_cascade_wake_route_apply_follow_pin_call_next_call, and continue_cascade_wake_route_apply_follow_pin_call_next_call_follow; residual fortress stages stay blocked until reverse-flow record/close and activation-external acceptance. operator_state also exports continue_run_recommended, continue_supervisor_wake, continue_dispatch (inventory packet without pipeline snapshot), continue_dispatch_action, continue_dispatch_execute_recommended, continue_dispatch_follow_through, continue_dispatch_follow_through_action, continue_dispatch_call_with_execute, continue_dispatch_helper, continue_dispatch_inventory_helper, continue_dispatch_follow_through_helper, continue_operator_card, continue_operator_card_helper, continue_progress_label, continue_action_line, continue_progress_transition_helper, continue_exec_receipt_helper, continue_finish_receipt, continue_finish_receipt_helper, continue_finish_line, continue_finished, continue_residual_queue_ready, continue_residual_open, continue_residual_open_helper, continue_residual_open_line, continue_residual_open_ready, continue_residual_adjacent_count, continue_residual_entry, continue_residual_entry_helper, continue_residual_entry_line, continue_residual_entry_ready, continue_selected_residual_proposal_id, continue_residual_follow, continue_residual_follow_helper, continue_residual_follow_line, continue_residual_follow_ready, continue_residual_follow_action, continue_call_residual_comparison, continue_residual_comparison, continue_residual_comparison_helper, continue_residual_comparison_line, continue_residual_comparison_ready, continue_residual_comparison_action, continue_call_residual_unlocked_apply, continue_residual_unlocked_apply, continue_residual_unlocked_apply_helper, continue_residual_unlocked_apply_line, continue_residual_unlocked_apply_ready, continue_residual_unlocked_apply_action, continue_call_residual_focused_validation, continue_residual_focused_validation, continue_residual_focused_validation_helper, continue_residual_focused_validation_line, continue_residual_focused_validation_ready, continue_residual_focused_validation_action, continue_call_residual_handoff, continue_residual_handoff, continue_residual_handoff_helper, continue_residual_handoff_line, continue_residual_handoff_ready, continue_residual_handoff_action, continue_call_residual_acceptance, continue_residual_acceptance, continue_residual_acceptance_helper, continue_residual_acceptance_line, continue_residual_acceptance_ready, continue_residual_acceptance_action, nested continue_residual_cascade, continue_residual_cascade_helper, continue_residual_cascade_line, continue_residual_cascade_ready, continue_residual_cascade_action, continue_residual_cascade_progress_label, continue_residual_cascade_blocked_at, nested continue_cascade, continue_cascade_helper, continue_cascade_line, continue_cascade_ready, continue_cascade_action, continue_cascade_reverse_progress_label, continue_cascade_residual_progress_label, continue_cascade_residual_blocked_at, nested continue_cascade_transition, continue_cascade_transition_helper, continue_cascade_transition_line, continue_cascade_advanced, continue_cascade_reverse_progress_transition, continue_cascade_residual_progress_transition, nested continue_cascade_wake, continue_cascade_wake_helper, continue_cascade_wake_line, continue_cascade_wake_outcome, nested continue_cascade_wake_route, continue_cascade_wake_route_helper, continue_cascade_wake_route_line, continue_cascade_wake_route_action, continue_cascade_wake_route_call_execute, nested continue_cascade_wake_route_apply, continue_cascade_wake_route_apply_helper, continue_cascade_wake_route_apply_line, continue_cascade_wake_route_action_transition, continue_cascade_wake_route_advanced, nested continue_cascade_wake_route_apply_follow, continue_cascade_wake_route_apply_follow_helper, continue_cascade_wake_route_apply_follow_line, continue_cascade_wake_route_apply_follow_action, continue_cascade_wake_route_apply_follow_preferred_helper, continue_cascade_wake_route_apply_follow_call_execute, nested continue_cascade_wake_route_apply_follow_pin, continue_cascade_wake_route_apply_follow_pin_helper, continue_cascade_wake_route_apply_follow_pin_line, continue_cascade_wake_route_apply_follow_pin_action, continue_cascade_wake_route_apply_follow_pin_mode, continue_cascade_wake_route_apply_follow_pin_preferred_helper, continue_cascade_wake_route_apply_follow_pin_call_execute, continue_cascade_wake_route_apply_follow_pin_ready, nested continue_cascade_wake_route_apply_follow_pin_call, continue_cascade_wake_route_apply_follow_pin_call_helper, continue_cascade_wake_route_apply_follow_pin_call_line, continue_cascade_wake_route_apply_follow_pin_action_transition, continue_cascade_wake_route_apply_follow_pin_mode_transition, continue_cascade_wake_route_apply_follow_pin_advanced, nested continue_cascade_wake_route_apply_follow_pin_call_next, continue_cascade_wake_route_apply_follow_pin_call_next_helper, continue_cascade_wake_route_apply_follow_pin_call_next_line, continue_cascade_wake_route_apply_follow_pin_call_next_action, continue_cascade_wake_route_apply_follow_pin_call_next_invoke, continue_cascade_wake_route_apply_follow_pin_call_next_preferred_helper, continue_cascade_wake_route_apply_follow_pin_call_next_call_execute, continue_cascade_wake_route_apply_follow_pin_call_next_ready, nested continue_cascade_wake_route_apply_follow_pin_call_next_call, continue_cascade_wake_route_apply_follow_pin_call_next_call_helper, continue_cascade_wake_route_apply_follow_pin_call_next_call_line, continue_cascade_wake_route_apply_follow_pin_call_next_action_transition, continue_cascade_wake_route_apply_follow_pin_call_next_invoke_transition, continue_cascade_wake_route_apply_follow_pin_call_next_advanced, nested continue_cascade_wake_route_apply_follow_pin_call_next_call_follow, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_helper, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_line, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_action, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_mode, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_preferred_helper, continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_call_execute, and continue_cascade_wake_route_apply_follow_pin_call_next_call_follow_ready while reverse-flow is ready/unrecorded or after pass.",
        "- Partial body-free command-hash rows stay on ready focused validation and accumulate across record calls via merge_skill_route_discovery_focused_validation_command_results; while partial, supervisor_next promotes to record_remaining_reverse_flow_focused_validation_command_hashes_then_keep_activation_external (not a full re-run); residual export remains denied until results cover expected hashes and reverse-flow record/close advances residual-active work.",
        "- After ready, record_skill_route_discovery_focused_local_test_validation_results merges new body-free command-hash rows with any prior partial rows while activation stays external.",
        "- After ready, close_skill_route_discovery_focused_local_test_validation_with_outcome materializes body-free expected-hash outcomes and refreshes activation-external handoff/acceptance.",
        "- After recorded pass, skill_route_discovery_focused_validation_activation_external_handoff packages keep_activation_external while push/promotion/restart stay denied.",
        "- After ready handoff, skill_route_discovery_focused_validation_activation_external_acceptance accepts the package while activation stays external.",
        "- After acceptance, skill_route_discovery_focused_validation_residual_adjacent_queue packages residual fortress/Hy3 rows for agent_harness_eval_cluster_local_apply without skill unlock inheritance.",
        "- After residual queue ready, skill_route_discovery_residual_adjacent_harness_eval_local_apply selects one residual fortress/Hy3 row and hands it to agent_harness_eval_cluster_local_apply with local comparison required.",
        "- After residual local apply ready, skill_route_discovery_residual_adjacent_harness_eval_local_comparison unlocks documentation/test/code_patch after harness criteria pass while skill unlocks stay closed.",
        "- After residual harness comparison ready, skill_route_discovery_residual_adjacent_unlocked_local_lane_apply packages unlocked documentation/test/code_patch with preferred test-first focused validation while skill unlocks stay closed.",
        "- After residual unlocked apply ready, skill_route_discovery_residual_adjacent_focused_local_validation records body-free command-hash results for the residual selected lane while skill unlocks stay closed and activation stays external.",
        "- After residual focused validation ready, record_skill_route_discovery_residual_adjacent_focused_local_validation_results or close_skill_route_discovery_residual_adjacent_focused_local_validation_with_outcome close body-free outcomes while activation stays external.",
        "- After residual focused validation recorded pass, skill_route_discovery_residual_adjacent_focused_validation_activation_external_handoff packages keep_activation_external while remaining residual fortress/Hy3 rows stay noted without skill unlock inheritance.",
        "- After ready residual handoff, skill_route_discovery_residual_adjacent_focused_validation_activation_external_acceptance accepts the residual keep_activation_external package while remaining residual rows stay noted without skill unlock inheritance.",
        "- Residual fortress-style general_agent_project rows hand off to agent_harness_eval_cluster_local_apply instead of failing skill-route comparison.",
    ]
    retained = pipeline.get("retained_boundaries") or []
    if retained:
        retained_ids = ", ".join(
            str(row.get("proposal_id") or "unknown")
            for row in retained
            if isinstance(row, dict)
        )
        lines.append(f"- Retained review-only boundaries: {retained_ids or 'none'}")
    adjacent = pipeline.get("adjacent_general_agent_rows") or []
    if adjacent:
        adjacent_ids = ", ".join(
            str(row.get("proposal_id") or "unknown")
            for row in adjacent
            if isinstance(row, dict)
        )
        lines.append(f"- Adjacent general-agent harness-eval rows: {adjacent_ids or 'none'}")
    return lines


def render_self_evolution_task(
    proposals: list[dict[str, Any]],
    *,
    repo_path: Path,
    branch_name: str,
    self_model_snapshot: SelfModelSnapshot,
    digest_id: str,
    digest_generated_at: str,
    capability_theme_window: dict[str, Any] | None = None,
    upstream_evidence_capability_step: dict[str, Any] | None = None,
    skill_route_discovery_capability_pipeline: dict[str, Any] | None = None,
    extra_instructions: str = "",
) -> str:
    proposal_lines: list[str] = []
    for index, proposal in enumerate(proposals, start=1):
        evidence = ", ".join(proposal.get("evidence_urls") or ["local controller fallback"])
        proposal_lines.extend(
            [
                f"{index}. {proposal['summary']}",
                f"   Proposal ID: {proposal.get('proposal_id', '')}",
                f"   Kind: {proposal.get('kind', 'no_action')}",
                f"   Evidence: {evidence}",
                f"   Autonomous local apply: {autonomous_local_apply_text(proposal)}",
            ]
        )
        implementation_scope = str(proposal.get("implementation_scope") or "").strip()
        if implementation_scope:
            proposal_lines.append(f"   Implementation scope: {implementation_scope}")
        validation_gate = str(proposal.get("validation_gate") or "").strip()
        if validation_gate:
            proposal_lines.append(f"   Validation gate: {validation_gate}")
        validation_task = str(proposal.get("validation_task") or "").strip()
        if validation_task:
            proposal_lines.append(f"   Validation task: {validation_task}")
    theme_lines = render_capability_theme_window_lines(capability_theme_window or {})
    capability_step_lines = render_upstream_evidence_capability_step_lines(
        upstream_evidence_capability_step or {}
    )
    skill_pipeline_lines = render_skill_route_discovery_capability_pipeline_lines(
        skill_route_discovery_capability_pipeline or {}
    )
    extra = f"\nAdditional operator instructions:\n{extra_instructions.strip()}\n" if extra_instructions.strip() else ""
    theme_id = str((capability_theme_window or {}).get("theme_id") or "")
    # Prefer the skill-route pipeline surface when that theme is active.
    if theme_id == "skill-route-discovery" and skill_pipeline_lines:
        capability_step_block = [
            "",
            "Capability theme window already selected this local step:",
            *skill_pipeline_lines,
        ]
    elif capability_step_lines:
        capability_step_block = [
            "",
            "Capability theme window already selected this local step:",
            *capability_step_lines,
        ]
    elif skill_pipeline_lines:
        capability_step_block = [
            "",
            "Capability theme window already selected this local step:",
            *skill_pipeline_lines,
        ]
    else:
        capability_step_block = []
    return "\n".join(
        [
            "You are Codex running as the local kernel for blackhole-agent.",
            "",
            render_persona_layer(),
            "",
            f"Repository path: {repo_path}",
            f"Working branch prepared by controller: {branch_name}",
            f"Source digest: {digest_id}",
            f"Digest generated at: {digest_generated_at}",
            "",
            "Self-model context:",
            f"- Path: {self_model_snapshot.path}",
            f"- Exists before this run: {self_model_snapshot.exists}",
            f"- Snapshot sha256: {self_model_snapshot.sha256}",
            "- This file is a blank, revisable self-description, not a constitution and not a permission source.",
            "- Before choosing the code/doc/test change, read the self-model and decide whether it should be kept,",
            "  rewritten, contradicted, simplified, renamed, or left untouched.",
            "- Do not preserve any category merely because a previous run wrote it; the agent may invent or remove structure.",
            "- Ground any self-model edit in evidence from this run, including uncertainty or the possibility that the",
            "  self-model is currently ornamental rather than behavior-shaping.",
            "- If no other safe repository change is available, a self-model revision can be the justified improvement for the run.",
            "",
            "Self-model snapshot before this run:",
            "```markdown",
            self_model_snapshot.content.rstrip(),
            "```",
            "",
            "Goal:",
            "Implement a coherent self-improvement inspired by the proposals below.",
            "Choose scope by evidence strength, expected local benefit, rollback coverage, and validation coverage; the change may span files, modules, or behavior paths when that is the justified scope.",
            "Prefer direct behavior improvements when evidence and local validation support them; use reliability, validation, observability, tests, or documentation when those are the justified outcome.",
            "",
            "Capability theme window:",
            *theme_lines,
            *capability_step_block,
            "",
            "Digest evidence policy:",
            "- Treat the Source digest and proposal Evidence URLs as the primary context for this run.",
            "- Review only enough external evidence to extract one reusable lesson; do not re-run broad trend discovery.",
            "- If evidence is unavailable, stale, or too vague, record that uncertainty and improve local validation instead.",
            "",
            "Runtime policy budget:",
            "- Network: use only proposal Evidence URLs and narrowly required official documentation.",
            "- Filesystem: stay inside this repository and create a rollback point before edits.",
            "- Shell: run local validation commands sized to the changed behavior.",
            "- Capabilities: use configured local capabilities when they are needed for the proposal and validation can cover them.",
            "- Autonomy: make one coherent improvement or tightly connected change set; review only offensive behavior, abuse, unauthorized access, or privacy leakage.",
            "- Activation: record push, promotion, restart, runner, or remote-execution changes in artifacts and rely on configured supervisors for activation.",
            "",
            "Proposals:",
            *proposal_lines,
            "",
            "Operating constraints:",
            "- Stay inside this repository.",
            "- Apply local repository changes autonomously when they are logged, rollback-backed, validated, and outside the narrow safety boundary.",
            "- Use only capabilities and runtime configuration available to this run.",
            "- Record material filesystem and external actions in run artifacts.",
            "- Keep the diff legible enough to audit after the fact; do not shrink a justified behavior change merely to look conservative.",
            "- Treat the capability theme window as continuity pressure: deepen that slice unless the selected evidence proves it is exhausted or unsafe.",
            "- Prefer a behavior path, controller surface, or integration seam over another isolated fixture when the theme already has sufficient tests.",
            "- Add or update tests or docs whenever behavior changes.",
            "- If you modify the self-model, edit the file at the self-model path directly and explain why it changed.",
            "- Run validation that matches the changed behavior and include the result in the final answer.",
            "- If a proposal crosses the offensive-behavior or privacy-leakage boundary, keep it review-only and improve the boundary or tests instead.",
            "- If the proposals are too vague, prefer a reversible local experiment over another validation-report-only refinement.",
            "",
            "Completion criteria:",
            "- The repository has a concrete local diff on this branch, or a clear no-op explanation if no safe change exists.",
            "- The final answer summarizes changed files, whether the self-model changed, validation, and remaining review notes.",
            extra,
        ]
    )


def write_self_evolution_plan(output_dir: Path, plan: SelfEvolutionPlan) -> tuple[Path, Path]:
    output_dir.mkdir(parents=True, exist_ok=True)
    timestamp = datetime.now(timezone.utc).strftime("%Y%m%dT%H%M%SZ")
    json_path = output_dir / f"self-evolution-plan-{timestamp}.json"
    markdown_path = output_dir / f"self-evolution-plan-{timestamp}.md"
    json_text = json.dumps(asdict(plan), indent=2, sort_keys=True) + "\n"
    markdown_text = "\n".join(
        [
            "# Self-Evolution Plan",
            "",
            f"Generated: {plan.generated_at}",
            f"Branch: `{plan.branch_name}`",
            f"Repository: `{plan.repo_path}`",
            f"Source digest: `{plan.source_digest_id}`",
            f"Capability theme: `{plan.capability_theme_window.get('theme_id', '')}`",
            (
                f"Upstream evidence capability step: "
                f"`{(plan.upstream_evidence_capability_step.get('selected_step') or {}).get('capability_action', '')}`"
                if plan.upstream_evidence_capability_step
                else "Upstream evidence capability step: none"
            ),
            (
                f"Skill route discovery capability pipeline: "
                f"`{(plan.skill_route_discovery_capability_pipeline.get('selected_step') or {}).get('capability_action', '')}`"
                if plan.skill_route_discovery_capability_pipeline
                else "Skill route discovery capability pipeline: none"
            ),
            "",
            "## Task",
            "",
            plan.task,
        ]
    )
    json_path.write_text(json_text, encoding="utf-8")
    markdown_path.write_text(markdown_text + "\n", encoding="utf-8")
    (output_dir / "latest-self-evolution-plan.json").write_text(json_text, encoding="utf-8")
    (output_dir / "latest-self-evolution-plan.md").write_text(markdown_text + "\n", encoding="utf-8")
    write_self_model_snapshot(output_dir, plan.self_model_before, phase="before")
    return json_path, markdown_path


def create_rollback_point(
    *,
    repo_path: Path,
    status_porcelain: str,
    command_runner: Any = subprocess.run,
) -> RollbackPoint:
    """Create a durable local git ref that can restore the pre-evolution HEAD."""

    created_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    timestamp = datetime.now(timezone.utc).strftime("%Y%m%dT%H%M%SZ")
    head = run_controller_command(
        ["git", "rev-parse", "--verify", "HEAD"],
        cwd=repo_path,
        command_runner=command_runner,
        check=True,
    ).stdout.strip()
    branch = run_controller_command(
        ["git", "branch", "--show-current"],
        cwd=repo_path,
        command_runner=command_runner,
        check=True,
    ).stdout.strip()
    repo_namespace = rollback_ref_repo_namespace(repo_path)
    rollback_ref = f"refs/blackhole-agent/rollback/{repo_namespace}/{timestamp}-{head[:12]}"
    run_controller_command(
        ["git", "update-ref", rollback_ref, head],
        cwd=repo_path,
        command_runner=command_runner,
        check=True,
    )
    restore_commands = build_restore_commands(original_branch=branch, rollback_ref=rollback_ref)
    return RollbackPoint(
        created_at=created_at,
        repo_path=str(repo_path),
        original_branch=branch,
        original_head=head,
        rollback_ref=rollback_ref,
        status_porcelain=status_porcelain,
        restore_commands=restore_commands,
    )


def rollback_ref_repo_namespace(repo_path: Path) -> str:
    """Return a short stable namespace for rollback refs from this checkout."""

    return hashlib.sha256(str(repo_path.resolve()).encode("utf-8")).hexdigest()[:8]


def build_restore_commands(*, original_branch: str, rollback_ref: str) -> list[list[str]]:
    checkout_command = ["git", "switch", original_branch] if original_branch else ["git", "switch", "--detach", rollback_ref]
    return [
        checkout_command,
        ["git", "reset", "--hard", rollback_ref],
        ["git", "clean", "-fd"],
    ]


def write_rollback_point(output_dir: Path, rollback_point: RollbackPoint) -> tuple[Path, Path]:
    output_dir.mkdir(parents=True, exist_ok=True)
    timestamp = datetime.now(timezone.utc).strftime("%Y%m%dT%H%M%SZ")
    json_path = output_dir / f"rollback-point-{timestamp}.json"
    markdown_path = output_dir / f"rollback-point-{timestamp}.md"
    json_text = json.dumps(asdict(rollback_point), indent=2, sort_keys=True) + "\n"
    markdown_text = render_rollback_point_markdown(rollback_point)
    json_path.write_text(json_text, encoding="utf-8")
    markdown_path.write_text(markdown_text, encoding="utf-8")
    (output_dir / "latest-rollback-point.json").write_text(json_text, encoding="utf-8")
    (output_dir / "latest-rollback-point.md").write_text(markdown_text, encoding="utf-8")
    return json_path, markdown_path


def render_rollback_point_markdown(rollback_point: RollbackPoint) -> str:
    lines = [
        "# Rollback Point",
        "",
        f"Created: {rollback_point.created_at}",
        f"Repository: `{rollback_point.repo_path}`",
        f"Original branch: `{rollback_point.original_branch or '(detached HEAD)'}`",
        f"Original HEAD: `{rollback_point.original_head}`",
        f"Rollback ref: `{rollback_point.rollback_ref}`",
        "",
        "## Recovery Commands",
        "",
        "Run these from the repository root only after choosing to discard the failed self-evolution diff:",
        "",
    ]
    lines.extend(f"```bash\n{render_shell_command(command)}\n```" for command in rollback_point.restore_commands)
    lines.extend(
        [
            "",
            "## Notes",
            "",
            "- `git reset --hard` discards tracked working tree changes.",
            "- `git clean -fd` removes untracked files and directories.",
            "- Keep this artifact outside any cleanup path until the recovered agent has started successfully.",
            "",
        ]
    )
    if rollback_point.status_porcelain.strip():
        lines.extend(["## Pre-Run Dirty Status", "", "```text", rollback_point.status_porcelain.rstrip(), "```", ""])
    return "\n".join(lines)


def render_shell_command(command: list[str]) -> str:
    return " ".join(shlex.quote(part) for part in command)


def prepare_self_evolution_branch(
    plan: SelfEvolutionPlan,
    *,
    output_dir: Path | None = None,
    allow_dirty: bool = False,
    command_runner: Any = subprocess.run,
) -> RollbackPoint | None:
    repo_path = Path(plan.repo_path)
    status = run_controller_command(["git", "status", "--porcelain"], cwd=repo_path, command_runner=command_runner)
    if not allow_dirty:
        if status.stdout.strip():
            raise RuntimeError("Refusing self-evolution on a dirty worktree. Commit/stash changes or pass --allow-dirty.")
    rollback_point: RollbackPoint | None = None
    if output_dir is not None:
        rollback_point = create_rollback_point(
            repo_path=repo_path,
            status_porcelain=status.stdout,
            command_runner=command_runner,
        )
        write_rollback_point(output_dir, rollback_point)
    run_controller_command(["git", "switch", "-c", plan.branch_name], cwd=repo_path, command_runner=command_runner, check=True)
    return rollback_point


def run_self_evolution_codex(
    plan: SelfEvolutionPlan,
    *,
    output_dir: Path,
    model: str | None = None,
    profile: str | None = None,
    require_codex_route: bool = True,
    claude_sdk_permission_mode: str | None = None,
    allow_claude_sdk_auto_permission_mode: bool = True,
    sandbox: str = "workspace-write",
    approval_policy: str = "never",
    ignore_user_config: bool = True,
    bypass_approvals_and_sandbox: bool = False,
    timeout_seconds: int = 3600,
    command_runner: Any = subprocess.run,
) -> SelfEvolutionRunResult:
    started_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    monotonic_started = time.monotonic()
    config = CodexCliConfig(
        model=model,
        profile=profile,
        require_explicit_route=require_codex_route,
        sandbox=sandbox,
        approval_policy=approval_policy,
        ignore_user_config=ignore_user_config,
        bypass_approvals_and_sandbox=bypass_approvals_and_sandbox,
    )
    kernel = CodexCliKernel(config, command_runner=command_runner)
    codex_metadata = {
        "model": config.model,
        "profile": config.profile,
        "require_explicit_route": config.require_explicit_route,
        "claude_sdk_permission_mode": claude_sdk_permission_mode,
        "allow_claude_sdk_auto_permission_mode": allow_claude_sdk_auto_permission_mode,
        "sandbox": config.sandbox,
        "approval_policy": config.approval_policy,
        "ignore_user_config": config.ignore_user_config,
        "bypass_approvals_and_sandbox": config.bypass_approvals_and_sandbox,
    }
    result: CodexCliRunResult = kernel.run(
        plan.task,
        cwd=Path(plan.repo_path),
        output_dir=output_dir,
        timeout_seconds=timeout_seconds,
    )
    finished_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    elapsed_seconds = round(time.monotonic() - monotonic_started, 3)
    run_result = SelfEvolutionRunResult(
        command=result.command,
        returncode=result.returncode,
        task_path=result.task_path,
        last_message_path=result.last_message_path,
        result_path=result.result_path,
        branch_name=plan.branch_name,
        stdout_tail=result.stdout_tail,
        stderr_tail=result.stderr_tail,
        last_message=result.last_message,
    )
    (output_dir / "latest-self-evolution-run.json").write_text(
        json.dumps(
            {
                "command": run_result.command,
                "codex_cli": codex_metadata,
                "provider_preflight": result.provider_preflight,
                "returncode": run_result.returncode,
                "task_path": str(run_result.task_path),
                "last_message_path": str(run_result.last_message_path),
                "result_path": str(run_result.result_path),
                "branch_name": run_result.branch_name,
                "stdout_tail": run_result.stdout_tail,
                "stderr_tail": run_result.stderr_tail,
                "last_message": run_result.last_message,
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    current_head = run_controller_command(
        ["git", "rev-parse", "--verify", "HEAD"],
        cwd=Path(plan.repo_path),
        command_runner=command_runner,
    ).stdout.strip()
    proposal_controls = [
        proposal_manifest_control(proposal)
        for proposal in plan.proposals
        if str(proposal.get("proposal_id") or "").strip()
    ]
    manifest = {
        "schema_version": 1,
        "started_at": started_at,
        "finished_at": finished_at,
        "elapsed_seconds": elapsed_seconds,
        "source_digest_id": plan.source_digest_id,
        "source_digest_generated_at": plan.source_digest_generated_at,
        "capability_theme_window": dict(plan.capability_theme_window),
        "upstream_evidence_capability_step": dict(plan.upstream_evidence_capability_step),
        "skill_route_discovery_capability_pipeline": dict(
            plan.skill_route_discovery_capability_pipeline
        ),
        "repo_path": plan.repo_path,
        "branch_name": plan.branch_name,
        "target_head": current_head,
        "self_model_path": plan.self_model_path,
        "returncode": run_result.returncode,
        "codex_cli": codex_metadata,
        "provider_preflight": result.provider_preflight,
        "task_path": str(run_result.task_path),
        "last_message_path": str(run_result.last_message_path),
        "codex_result_path": str(run_result.result_path),
        "proposal_controls": proposal_controls,
        "replayable_validation_report": build_replayable_validation_report(plan, proposal_controls),
        "validation_gates": [
            str(proposal.get("validation_gate"))
            for proposal in plan.proposals
            if str(proposal.get("validation_gate") or "").strip()
        ],
        "proposal_ids": [
            str(proposal.get("proposal_id"))
            for proposal in plan.proposals
            if str(proposal.get("proposal_id") or "").strip()
        ],
        "evidence_urls": sorted(
            {
                str(url)
                for proposal in plan.proposals
                for url in proposal.get("evidence_urls", [])
                if str(url).strip()
            }
        ),
    }
    (output_dir / "latest-self-evolution-manifest.json").write_text(
        json.dumps(manifest, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    write_self_model_snapshot(
        output_dir,
        read_self_model_snapshot(Path(plan.repo_path), Path(plan.self_model_path)),
        phase="after",
    )
    return run_result


def run_self_evolution_grok(
    plan: SelfEvolutionPlan,
    *,
    output_dir: Path,
    model: str | None = None,
    require_explicit_route: bool = True,
    sandbox: str = "workspace",
    permission_mode: str = "bypassPermissions",
    timeout_seconds: int = 3600,
    command_runner: Any = subprocess.run,
) -> SelfEvolutionRunResult:
    """Run one self-evolution task through Grok CLI and preserve controller artifacts."""

    started_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    monotonic_started = time.monotonic()
    config = GrokCliConfig(
        model=model,
        require_explicit_route=require_explicit_route,
        sandbox=sandbox,
        permission_mode=permission_mode,
    )
    kernel = GrokCliKernel(config, command_runner=command_runner)
    grok_metadata = {
        "model": config.model,
        "require_explicit_route": config.require_explicit_route,
        "sandbox": config.sandbox,
        "permission_mode": config.permission_mode,
        "output_format": config.output_format,
        "no_memory": config.no_memory,
        "no_subagents": config.no_subagents,
        "disable_web_search": config.disable_web_search,
        "controller_owned_git_actions_denied": ["commit", "push"],
    }
    result: GrokCliRunResult = kernel.run(
        plan.task,
        cwd=Path(plan.repo_path),
        output_dir=output_dir,
        timeout_seconds=timeout_seconds,
    )
    finished_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")
    elapsed_seconds = round(time.monotonic() - monotonic_started, 3)
    run_result = SelfEvolutionRunResult(
        command=result.command,
        returncode=result.returncode,
        task_path=result.task_path,
        last_message_path=result.last_message_path,
        result_path=result.result_path,
        branch_name=plan.branch_name,
        stdout_tail=result.stdout_tail,
        stderr_tail=result.stderr_tail,
        last_message=result.last_message,
    )
    (output_dir / "latest-self-evolution-run.json").write_text(
        json.dumps(
            {
                "command": run_result.command,
                "kernel": "grok",
                "grok_cli": grok_metadata,
                "provider_preflight": result.provider_preflight,
                "returncode": run_result.returncode,
                "task_path": str(run_result.task_path),
                "last_message_path": str(run_result.last_message_path),
                "result_path": str(run_result.result_path),
                "branch_name": run_result.branch_name,
                "stdout_tail": run_result.stdout_tail,
                "stderr_tail": run_result.stderr_tail,
                "last_message": run_result.last_message,
            },
            indent=2,
            sort_keys=True,
        )
        + "\n",
        encoding="utf-8",
    )
    current_head = run_controller_command(
        ["git", "rev-parse", "--verify", "HEAD"],
        cwd=Path(plan.repo_path),
        command_runner=command_runner,
    ).stdout.strip()
    proposal_controls = [
        proposal_manifest_control(proposal)
        for proposal in plan.proposals
        if str(proposal.get("proposal_id") or "").strip()
    ]
    manifest = {
        "schema_version": 1,
        "started_at": started_at,
        "finished_at": finished_at,
        "elapsed_seconds": elapsed_seconds,
        "source_digest_id": plan.source_digest_id,
        "source_digest_generated_at": plan.source_digest_generated_at,
        "capability_theme_window": dict(plan.capability_theme_window),
        "upstream_evidence_capability_step": dict(plan.upstream_evidence_capability_step),
        "skill_route_discovery_capability_pipeline": dict(
            plan.skill_route_discovery_capability_pipeline
        ),
        "repo_path": plan.repo_path,
        "branch_name": plan.branch_name,
        "target_head": current_head,
        "self_model_path": plan.self_model_path,
        "returncode": run_result.returncode,
        "kernel": "grok",
        "grok_cli": grok_metadata,
        "provider_preflight": result.provider_preflight,
        "task_path": str(run_result.task_path),
        "last_message_path": str(run_result.last_message_path),
        "grok_result_path": str(run_result.result_path),
        "proposal_controls": proposal_controls,
        "replayable_validation_report": build_replayable_validation_report(plan, proposal_controls),
        "validation_gates": [
            str(proposal.get("validation_gate"))
            for proposal in plan.proposals
            if str(proposal.get("validation_gate") or "").strip()
        ],
        "proposal_ids": [
            str(proposal.get("proposal_id"))
            for proposal in plan.proposals
            if str(proposal.get("proposal_id") or "").strip()
        ],
        "evidence_urls": sorted(
            {
                str(url)
                for proposal in plan.proposals
                for url in proposal.get("evidence_urls", [])
                if str(url).strip()
            }
        ),
    }
    (output_dir / "latest-self-evolution-manifest.json").write_text(
        json.dumps(manifest, indent=2, sort_keys=True) + "\n",
        encoding="utf-8",
    )
    write_self_model_snapshot(
        output_dir,
        read_self_model_snapshot(Path(plan.repo_path), Path(plan.self_model_path)),
        phase="after",
    )
    return run_result


def run_controller_command(
    command: list[str],
    *,
    cwd: Path,
    command_runner: Any = subprocess.run,
    timeout: int = 30,
    check: bool = False,
) -> subprocess.CompletedProcess:
    completed = command_runner(command, cwd=cwd, capture_output=True, text=True, timeout=timeout)
    if check and completed.returncode != 0:
        rendered = " ".join(shlex.quote(part) for part in command)
        raise RuntimeError(f"Command failed ({completed.returncode}): {rendered}\n{completed.stderr}")
    return completed


def run_intake_once(
    *,
    repos: list[str] | None = None,
    output_dir: Path,
    state_path: Path | None = None,
    memory_path: Path | None = None,
    token: str | None = None,
    topics: list[str] | None = None,
    lookback_hours: int = 1,
    max_events_per_repo: int = 100,
    trend_config: GitHubTrendConfig | None = None,
    client: GitHubEventsClient | None = None,
    repo_path: Path = Path("."),
    self_model_path: Path | None = None,
    proposal_mode: str = DEFAULT_PROPOSAL_MODE,
    proposal_model: str | None = None,
    proposal_profile: str | None = None,
    kernel: str = "codex",
    proposal_timeout_seconds: int = 180,
    ignore_user_config: bool = True,
    command_runner: Any = subprocess.run,
) -> DigestWriteResult:
    repo_specs = repos or []
    if not repo_specs and trend_config is None:
        raise ValueError("At least one repository is required")
    normalized_repos = ["/".join(parse_repo_spec(repo)) for repo in repo_specs]
    state_file = state_path or output_dir / "state.json"
    memory_file = memory_path or output_dir / "memory.json"
    state = load_state(state_file)
    memory = load_memory(memory_file)
    github = client or GitHubEventsClient(token=token)
    generated_at = datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")

    trend_result: GitHubTrendSearchResult | None = None
    trend_events: list[GitHubEvent] = []
    trend_source: dict[str, Any] | None = None
    if trend_config is not None:
        trend_result = github.search_trending_repositories(trend_config)
        trend_repos = [repository.full_name for repository in trend_result.repositories]
        normalized_repos = list(dict.fromkeys([*normalized_repos, *trend_repos]))
        trend_events = build_trend_events(trend_result.repositories, state=state, generated_at=generated_at)
        trend_source = build_trend_source_metadata(trend_result, state=state)

    events: list[GitHubEvent] = []
    event_fetch_errors: list[dict[str, str]] = []
    for repo in normalized_repos:
        try:
            raw_events = github.list_repository_events(repo, per_page=max_events_per_repo)
        except RuntimeError as error:
            if trend_config is None:
                raise
            event_fetch_errors.append({"repo": repo, "error": str(error)})
            continue
        events.extend(
            select_new_events(repo, raw_events, state, lookback_hours=lookback_hours)
        )
    if trend_source is not None:
        trend_source["event_fetch_errors"] = event_fetch_errors
    events.sort(key=lambda event: parse_github_timestamp(event.created_at), reverse=True)
    signals = extract_growth_signals([*trend_events, *events], topics=topics or list(DEFAULT_TOPICS))
    update_state(state, events)
    if trend_result is not None:
        update_trend_state(state, trend_result.repositories)
    heuristic_proposals = build_proposals(signals, memory=memory)
    digest = build_digest(
        normalized_repos,
        signals,
        state=state,
        generated_at=generated_at,
        source=trend_source,
        memory=memory,
        proposals=heuristic_proposals,
    )
    if validate_proposal_mode(proposal_mode) != "heuristic":
        digest["proposals"] = synthesize_digest_proposals(
            digest,
            signals,
            heuristic_proposals,
            mode=proposal_mode,
            output_dir=output_dir,
            repo_path=repo_path,
            self_model_path=self_model_path,
            model=proposal_model,
            profile=proposal_profile,
            kernel=kernel,
            ignore_user_config=ignore_user_config,
            timeout_seconds=proposal_timeout_seconds,
            command_runner=command_runner,
        )
        attach_upstream_evidence_capability_step(digest)
        attach_skill_route_discovery_capability_pipeline(digest)
    update_memory_from_digest(memory, digest)

    timestamp = datetime.now(timezone.utc).strftime("%Y%m%dT%H%M%SZ")
    output_dir.mkdir(parents=True, exist_ok=True)
    json_path = output_dir / f"github-growth-digest-{timestamp}.json"
    markdown_path = output_dir / f"github-growth-digest-{timestamp}.md"
    json_text = json.dumps(digest, indent=2, sort_keys=True) + "\n"
    markdown_text = render_markdown_digest(digest)
    json_path.write_text(json_text, encoding="utf-8")
    markdown_path.write_text(markdown_text, encoding="utf-8")
    (output_dir / "latest.json").write_text(json_text, encoding="utf-8")
    (output_dir / "latest.md").write_text(markdown_text, encoding="utf-8")
    save_state(state_file, state)
    save_memory(memory_file, memory)
    return DigestWriteResult(
        digest=digest,
        json_path=json_path,
        markdown_path=markdown_path,
        state_path=state_file,
        memory_path=memory_file,
    )


def _event_text(repo: str, kind: str, payload: dict[str, Any]) -> tuple[str, str, str]:
    if kind == "PullRequestEvent":
        pr = payload.get("pull_request") or {}
        title = _compact(pr.get("title") or "untitled pull request")
        action = payload.get("action") or "updated"
        return f"{action} pull request: {title}", pr.get("html_url") or _repo_url(repo), _compact(pr.get("body") or "")
    if kind == "PullRequestReviewEvent":
        pr = payload.get("pull_request") or {}
        review = payload.get("review") or {}
        title = _compact(pr.get("title") or "untitled pull request")
        action = payload.get("action") or "reviewed"
        state = _compact(review.get("state") or "review")
        return (
            f"{action} pull request review ({state}): {title}",
            review.get("html_url") or pr.get("html_url") or _repo_url(repo),
            _compact(review.get("body") or ""),
        )
    if kind == "PullRequestReviewCommentEvent":
        pr = payload.get("pull_request") or {}
        comment = payload.get("comment") or {}
        title = _compact(pr.get("title") or "untitled pull request")
        action = payload.get("action") or "commented"
        return (
            f"{action} pull request review comment: {title}",
            comment.get("html_url") or pr.get("html_url") or _repo_url(repo),
            _compact(comment.get("body") or ""),
        )
    if kind in {"IssuesEvent", "IssueCommentEvent"}:
        issue = payload.get("issue") or {}
        title = _compact(issue.get("title") or "untitled issue")
        action = payload.get("action") or "updated"
        comment = payload.get("comment") or {}
        body = comment.get("body") or issue.get("body") or ""
        return f"{action} issue: {title}", issue.get("html_url") or _repo_url(repo), _compact(body)
    if kind == "ReleaseEvent":
        release = payload.get("release") or {}
        name = _compact(release.get("name") or release.get("tag_name") or "release")
        action = payload.get("action") or "published"
        return f"{action} release: {name}", release.get("html_url") or _repo_url(repo), _compact(release.get("body") or "")
    if kind == "PushEvent":
        commits = payload.get("commits") or []
        ref = str(payload.get("ref") or "").removeprefix("refs/heads/")
        messages = [_compact(commit.get("message") or "") for commit in commits if isinstance(commit, dict)]
        summary = "; ".join(message for message in messages if message)
        title = f"push to {ref or 'repository'}"
        if messages:
            title = f"{title}: {messages[0]}"
        url = _repo_url(repo)
        if len(commits) == 1 and isinstance(commits[0], dict) and commits[0].get("sha"):
            url = f"{_repo_url(repo)}/commit/{commits[0]['sha']}"
        return title, url, summary
    if kind == "CreateEvent":
        ref_type = payload.get("ref_type") or "ref"
        ref = payload.get("ref") or ""
        return f"created {ref_type}: {ref}", _repo_url(repo), ""
    if kind == "ForkEvent":
        forkee = payload.get("forkee") or {}
        return f"forked to {forkee.get('full_name', 'unknown fork')}", forkee.get("html_url") or _repo_url(repo), ""
    return kind, _repo_url(repo), _compact(json.dumps(payload, sort_keys=True)[:500])


def _compact(value: object) -> str:
    return " ".join(str(value).split())


def _repo_url(repo: str) -> str:
    return f"https://github.com/{repo}"


# fmt: off
@app.command(help=_HELP_TEXT)
def main(
    repos: str = typer.Option("", "--repos", "-r", help="Optional comma-separated repositories; omit to discover public GitHub trends."),
    trend_query: str = typer.Option("", "--trend-query", help="Additional GitHub repository search terms, such as language:Python or topic:agents."),
    trend_window_days: int = typer.Option(7, "--trend-window-days", min=1, help="Discover repositories created within this many days."),
    trend_min_stars: int = typer.Option(25, "--trend-min-stars", min=0, help="Minimum stars for discovered trend repositories."),
    trend_limit: int = typer.Option(10, "--trend-limit", min=1, max=100, help="Maximum trend repositories to track per pass."),
    trend_sort: str = typer.Option("stars", "--trend-sort", help="Trend search sort: stars, forks, or updated."),
    trend_order: str = typer.Option("desc", "--trend-order", help="Trend search order: asc or desc."),
    include_forks: bool = typer.Option(False, "--include-forks", help="Include forked repositories in trend discovery."),
    output_dir: Path = typer.Option(Path(".blackhole-agent/github-growth"), "--output-dir", "-o", help="Directory for digest and state files."),
    state_path: Path | None = typer.Option(None, "--state", help="State file path. Defaults to <output-dir>/state.json."),
    memory_path: Path | None = typer.Option(None, "--memory", help="Memory file path. Defaults to <output-dir>/memory.json."),
    token_env: str = typer.Option("GITHUB_TOKEN", "--token-env", help="Environment variable containing a GitHub token."),
    topics: str = typer.Option("", "--topics", help="Comma-separated relevance terms. Defaults to agent/workflow/test/security topics."),
    lookback_hours: int = typer.Option(24, "--lookback-hours", min=1, help="Initial event lookback window when no state exists."),
    max_events_per_repo: int = typer.Option(100, "--max-events-per-repo", min=1, max=100, help="GitHub event page size; all Link-paginated pages are fetched."),
    interval_seconds: int = typer.Option(0, "--interval-seconds", min=0, help="Run forever at this interval; 0 runs exactly once."),
    evolution_mode: str = typer.Option("digest", "--evolution-mode", help="One of: digest, plan, codex."),
    kernel: str = typer.Option("codex", "--kernel", help="CLI kernel for LLM interpretation and mutation: codex or grok."),
    repo_path: Path = typer.Option(Path("."), "--repo-path", help="blackhole-agent checkout to improve in plan/codex mode."),
    force_evolve: bool = typer.Option(False, "--force-evolve", help="Create a fallback self-evolution task even without matched signals."),
    branch_prefix: str = typer.Option("codex/blackhole-evolve", "--branch-prefix", help="Branch prefix used by mutation mode."),
    self_model_path: Path = typer.Option(DEFAULT_SELF_MODEL_PATH, "--self-model-path", help="Repository-relative self-model file for revisable self-recognition."),
    proposal_mode: str = typer.Option(DEFAULT_PROPOSAL_MODE, "--proposal-mode", help="One of: heuristic, llm, hybrid."),
    proposal_model: str | None = typer.Option(None, "--proposal-model", help="Model for read-only LLM proposal interpretation. Defaults to --model when omitted."),
    proposal_timeout_seconds: int = typer.Option(180, "--proposal-timeout-seconds", min=1, help="Timeout for read-only LLM proposal interpretation."),
    model: str | None = typer.Option(None, "-m", "--model", help="Model to pass to the selected CLI kernel."),
    profile: str | None = typer.Option(None, "--profile", help="Codex config profile to pass in codex mode."),
    require_codex_route: bool = typer.Option(True, "--require-codex-route/--allow-default-codex-route", help="Require codex mode to pass an explicit --model or --profile instead of relying on the CLI default route."),
    claude_sdk_permission_mode: str | None = typer.Option(None, "--claude-sdk-permission-mode", help="Claude SDK permission mode recorded for supervisor child compatibility."),
    allow_claude_sdk_auto_permission_mode: bool = typer.Option(True, "--allow-claude-sdk-auto-permission-mode/--disallow-claude-sdk-auto-permission-mode", help="Accept supervisor Claude SDK auto-permission metadata."),
    sandbox: str = typer.Option("workspace-write", "--sandbox", help="Sandbox profile for the selected CLI kernel."),
    approval_policy: str = typer.Option("never", "--approval-policy", help="Legacy compatibility option; current codex exec has no approval flag."),
    ignore_user_config: bool = typer.Option(True, "--ignore-user-config/--use-user-config", help="Ignore user Codex config in codex mode while keeping auth available."),
    bypass_approvals_and_sandbox: bool = typer.Option(False, "--bypass-approvals-and-sandbox", help="Enable autonomous full-access permission mode for the selected kernel."),
    allow_dirty: bool = typer.Option(False, "--allow-dirty", help="Allow mutation mode to start from a dirty worktree."),
    codex_timeout_seconds: int = typer.Option(3600, "--codex-timeout-seconds", min=1, help="Timeout for the selected CLI kernel run."),
    extra_instruction: str = typer.Option("", "--extra-instruction", help="Additional instruction appended to the self-evolution task."),
) -> None:
    # fmt: on
    repo_list = parse_comma_separated(repos)
    if evolution_mode not in {"digest", "plan", "codex"}:
        raise typer.BadParameter("--evolution-mode must be one of: digest, plan, codex")
    if kernel not in {"codex", "grok"}:
        raise typer.BadParameter("--kernel must be one of: codex, grok")
    if proposal_mode not in PROPOSAL_MODES:
        raise typer.BadParameter("--proposal-mode must be one of: heuristic, llm, hybrid")
    if trend_sort not in {"stars", "forks", "updated"}:
        raise typer.BadParameter("--trend-sort must be one of: stars, forks, updated")
    if trend_order not in {"asc", "desc"}:
        raise typer.BadParameter("--trend-order must be one of: asc, desc")
    if evolution_mode == "codex" and interval_seconds > 0:
        raise typer.BadParameter("--evolution-mode codex is one-shot; use a scheduler to launch separate runs")
    topic_list = parse_comma_separated(topics) or list(DEFAULT_TOPICS)
    token = os.getenv(token_env)
    trend_config = None
    if not repo_list:
        trend_config = GitHubTrendConfig(
            query=trend_query,
            window_days=trend_window_days,
            min_stars=trend_min_stars,
            limit=trend_limit,
            sort=trend_sort,
            order=trend_order,
            include_forks=include_forks,
        )

    while True:
        result = run_intake_once(
            repos=repo_list,
            output_dir=output_dir,
            state_path=state_path,
            memory_path=memory_path,
            token=token,
            topics=topic_list,
            lookback_hours=lookback_hours,
            max_events_per_repo=max_events_per_repo,
            trend_config=trend_config,
            repo_path=repo_path.resolve(),
            self_model_path=self_model_path,
            proposal_mode=proposal_mode,
            proposal_model=proposal_model or model,
            proposal_profile=profile,
            kernel=kernel,
            proposal_timeout_seconds=proposal_timeout_seconds,
            ignore_user_config=ignore_user_config,
        )
        source = result.digest.get("source") or {}
        if source:
            console.print(
                f"Trend search tracked {len(source.get('repositories') or [])} repo(s) "
                f"from query [bold]{source.get('query', '')}[/bold]"
            )
        console.print(f"Wrote {len(result.digest['items'])} item(s) to [bold green]{result.markdown_path}[/bold green]")
        console.print(f"Updated memory at [bold green]{result.memory_path}[/bold green]")
        if evolution_mode in {"plan", "codex"}:
            plan = build_self_evolution_plan(
                result.digest,
                repo_path=repo_path.resolve(),
                branch_prefix=branch_prefix,
                force=force_evolve,
                extra_instructions=extra_instruction,
                self_model_path=self_model_path,
            )
            if plan is None:
                console.print("No self-evolution plan created because no proposals matched this pass.")
            else:
                _, markdown_path = write_self_evolution_plan(output_dir, plan)
                console.print(f"Wrote self-evolution plan to [bold green]{markdown_path}[/bold green]")
                if evolution_mode == "codex":
                    rollback_point = prepare_self_evolution_branch(
                        plan,
                        output_dir=output_dir,
                        allow_dirty=allow_dirty,
                    )
                    if rollback_point is not None:
                        console.print(
                            "Wrote rollback point to "
                            f"[bold green]{output_dir / 'latest-rollback-point.md'}[/bold green]"
                        )
                    if kernel == "grok":
                        run_result = run_self_evolution_grok(
                            plan,
                            output_dir=output_dir,
                            model=model,
                            require_explicit_route=require_codex_route,
                            sandbox=sandbox,
                            permission_mode=(
                                "bypassPermissions" if bypass_approvals_and_sandbox else "dontAsk"
                            ),
                            timeout_seconds=codex_timeout_seconds,
                        )
                    else:
                        run_result = run_self_evolution_codex(
                            plan,
                            output_dir=output_dir,
                            model=model,
                            profile=profile,
                            require_codex_route=require_codex_route,
                            claude_sdk_permission_mode=claude_sdk_permission_mode,
                            allow_claude_sdk_auto_permission_mode=allow_claude_sdk_auto_permission_mode,
                            sandbox=sandbox,
                            approval_policy=approval_policy,
                            ignore_user_config=ignore_user_config,
                            bypass_approvals_and_sandbox=bypass_approvals_and_sandbox,
                            timeout_seconds=codex_timeout_seconds,
                        )
                    console.print(
                        f"{kernel.title()} kernel exited with {run_result.returncode}; last message: "
                        f"[bold green]{run_result.last_message_path}[/bold green]"
                    )
        if interval_seconds <= 0:
            break
        time.sleep(interval_seconds)


if __name__ == "__main__":
    app()
