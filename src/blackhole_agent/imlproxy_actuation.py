"""Next unsaturated diversity-catalog family after RFC 9161 PROXY/ND.

Operational Aspects of Proxy-ARP/ND in Ethernet VPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After proxynd seals a proxynddigest
this slot is that next family: RFC 9251 IGMP and MLD Proxies for Ethernet VPN IGMP/MLD
over an imlproxyid-gated imlproxydigest.
The protocol hole stays open so later genesis can opt the imlproxy provider in
and seal a imlproxydigest.
"""

from __future__ import annotations

IMLPROXY_ACTUATION_ID = "capability.imlproxy-actuation"
IMLPROXY_ACTUATION_DONE_WHEN = (
    f"capability_exists:{IMLPROXY_ACTUATION_ID};"
    f"capability_proved:{IMLPROXY_ACTUATION_ID};"
    "no_skill_route"
)
IMLPROXY_ACTUATION_GOAL = (
    "Repair rfc9251 imlproxy igmp/mld cycle cannot land over http imlproxy imlproxyid: hosted igmp and mld proxies for ethernet vpn endpoints remain unsupported so a IGMP then MLD imlproxyid handshake cannot land and a sealed imlproxydigest cannot be produced. A missing imlproxy imlproxyid stays forbidden; fail-closed routing never opts the imlproxy provider in. An independent later poll of the stored imlproxydigest keeps the hole falsifiable. IGMP and MLD Proxies for Ethernet VPN sessions stay fail-closed without a imlproxyid-gated imlproxydigest."
)
