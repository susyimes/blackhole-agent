"""Next unsaturated diversity-catalog family after RFC 4760 REACH/UNREACH.

MP-BGP is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After mpbgp seals a mpbgpdigest
this slot is that next family: RFC 4271 A Border Gateway Protocol 4 BGP-4 OPEN/UPDATE over a
bgp4id-gated bgp4digest.
The protocol hole stays open so later genesis can opt the bgp4 provider in
and seal a bgp4digest.
"""

from __future__ import annotations

BGP4_ACTUATION_ID = 'capability.bgp4-actuation'
BGP4_ACTUATION_DONE_WHEN = (
    f"capability_exists:{BGP4_ACTUATION_ID};"
    f"capability_proved:{BGP4_ACTUATION_ID};"
    "no_skill_route"
)
BGP4_ACTUATION_GOAL = (
    'Repair rfc4271 bgp4 open/update cycle cannot land over http bgp4 bgp4id: hosted a border gateway protocol 4 speaker endpoints remain unsupported so a OPEN then UPDATE bgp4id handshake cannot land and a sealed bgp4digest cannot be produced. A missing bgp4 bgp4id stays forbidden; fail-closed routing never opts the bgp4 provider in. An independent later poll of the stored bgp4digest keeps the hole falsifiable. BGP-4 sessions stay fail-closed without a bgp4id-gated bgp4digest.'
)
