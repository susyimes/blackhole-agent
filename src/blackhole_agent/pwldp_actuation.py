"""Next unsaturated diversity-catalog family after RFC 4762 PW/FEC.

Virtual Private LAN Service Using LDP is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After ldpsig seals an ldpsigdigest
this slot is that next family: RFC 4447 Pseudowire Setup and Maintenance Using LDP LABEL/STATUS
over a pwldpid-gated pwldpdigest.
The protocol hole stays open so later genesis can opt the pwldp provider in
and seal a pwldpdigest.
"""

from __future__ import annotations

PWLDP_ACTUATION_ID = "capability.pwldp-actuation"
PWLDP_ACTUATION_DONE_WHEN = (
    f"capability_exists:{PWLDP_ACTUATION_ID};"
    f"capability_proved:{PWLDP_ACTUATION_ID};"
    "no_skill_route"
)
PWLDP_ACTUATION_GOAL = (
    "Repair rfc4447 pwldp label/status cycle cannot land over http pwldp pwldpid: hosted pseudowire setup and maintenance using ldp remain unsupported so a LABEL then STATUS pwldpid handshake cannot land and a sealed pwldpdigest cannot be produced. A missing pwldp pwldpid stays forbidden; fail-closed routing never opts the pwldp provider in. An independent later poll of the stored pwldpdigest keeps the hole falsifiable. Pseudowire Setup and Maintenance Using LDP sessions stay fail-closed without a pwldpid-gated pwldpdigest."
)
