"""Next unsaturated diversity-catalog family after RFC 8212 DEF/PROP.

Default EBGP Route Propagation is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After ebgp seals a ebgpdigest
this slot is that next family: RFC 8214 Virtual Private Wire Service Support in Ethernet VPN AD/VPWS
over an evpnid-gated evpndigest.
The protocol hole stays open so later genesis can opt the evpn provider in
and seal a evpndigest.
"""

from __future__ import annotations

EVPN_ACTUATION_ID = "capability.evpn-actuation"
EVPN_ACTUATION_DONE_WHEN = (
    f"capability_exists:{EVPN_ACTUATION_ID};"
    f"capability_proved:{EVPN_ACTUATION_ID};"
    "no_skill_route"
)
EVPN_ACTUATION_GOAL = (
    "Repair rfc8214 evpn ad/vpws cycle cannot land over http evpn evpnid: hosted virtual private wire service support in ethernet vpn endpoints remain unsupported so a AD then VPWS evpnid handshake cannot land and a sealed evpndigest cannot be produced. A missing evpn evpnid stays forbidden; fail-closed routing never opts the evpn provider in. An independent later poll of the stored evpndigest keeps the hole falsifiable. Virtual Private Wire Service Support in Ethernet VPN sessions stay fail-closed without a evpnid-gated evpndigest."
)
