"""Next unsaturated diversity-catalog family after RFC 8365 NVE/VNI.

Network Virtualization Overlay is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After nvo seals a nvodigest
this slot is that next family: RFC 8584 Ethernet VPN Designated Forwarder Election DF/NDF
over a dfeid-gated dfedigest.
The protocol hole stays open so later genesis can opt the dfe provider in
and seal a dfedigest.
"""

from __future__ import annotations

DFE_ACTUATION_ID = "capability.dfe-actuation"
DFE_ACTUATION_DONE_WHEN = (
    f"capability_exists:{DFE_ACTUATION_ID};"
    f"capability_proved:{DFE_ACTUATION_ID};"
    "no_skill_route"
)
DFE_ACTUATION_GOAL = (
    "Repair rfc8584 dfe df/ndf cycle cannot land over http dfe dfeid: hosted ethernet vpn designated forwarder election endpoints remain unsupported so a DF then NDF dfeid handshake cannot land and a sealed dfedigest cannot be produced. A missing dfe dfeid stays forbidden; fail-closed routing never opts the dfe provider in. An independent later poll of the stored dfedigest keeps the hole falsifiable. Ethernet VPN Designated Forwarder Election sessions stay fail-closed without a dfeid-gated dfedigest."
)
