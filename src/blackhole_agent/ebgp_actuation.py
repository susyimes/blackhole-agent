"""Next unsaturated diversity-catalog family after RFC 8210 SERIAL/RESET.

RPKI to Router Protocol is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After rtr seals a rtrdigest
this slot is that next family: RFC 8212 Default EBGP Route Propagation DEF/PROP
over an ebgpid-gated ebgpdigest.
The protocol hole stays open so later genesis can opt the ebgp provider in
and seal a ebgpdigest.
"""

from __future__ import annotations

EBGP_ACTUATION_ID = "capability.ebgp-actuation"
EBGP_ACTUATION_DONE_WHEN = (
    f"capability_exists:{EBGP_ACTUATION_ID};"
    f"capability_proved:{EBGP_ACTUATION_ID};"
    "no_skill_route"
)
EBGP_ACTUATION_GOAL = (
    "Repair rfc8212 ebgp def/prop cycle cannot land over http ebgp ebgpid: hosted default ebgp route propagation endpoints remain unsupported so a DEF then PROP ebgpid handshake cannot land and a sealed ebgpdigest cannot be produced. A missing ebgp ebgpid stays forbidden; fail-closed routing never opts the ebgp provider in. An independent later poll of the stored ebgpdigest keeps the hole falsifiable. Default EBGP Route Propagation sessions stay fail-closed without a ebgpid-gated ebgpdigest."
)
