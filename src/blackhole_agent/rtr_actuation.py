"""Next unsaturated diversity-catalog family after RFC 8205 SIGN/PATH.

BGPsec Protocol is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After bgpsec seals a bgpsecdigest
this slot is that next family: RFC 8210 RPKI to Router Protocol SERIAL/RESET
over a rtrid-gated rtrdigest.
The protocol hole stays open so later genesis can opt the rtr provider in
and seal a rtrdigest.
"""

from __future__ import annotations

RTR_ACTUATION_ID = "capability.rtr-actuation"
RTR_ACTUATION_DONE_WHEN = (
    f"capability_exists:{RTR_ACTUATION_ID};"
    f"capability_proved:{RTR_ACTUATION_ID};"
    "no_skill_route"
)
RTR_ACTUATION_GOAL = (
    "Repair rfc8210 rtr serial/reset cycle cannot land over http rtr rtrid: hosted rpki to router protocol endpoints remain unsupported so a SERIAL then RESET rtrid handshake cannot land and a sealed rtrdigest cannot be produced. A missing rtr rtrid stays forbidden; fail-closed routing never opts the rtr provider in. An independent later poll of the stored rtrdigest keeps the hole falsifiable. RPKI to Router sessions stay fail-closed without a rtrid-gated rtrdigest."
)
