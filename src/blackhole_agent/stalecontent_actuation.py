"""Next unsaturated diversity-catalog family after RFC 5987 Character Set and Language Encoding.

Character Set and Language Encoding is proved. Closed-contract leftover harvest used to steal genesis
with ``Mission contract is closed; later genesis can take the next unsaturated
diversity-catalog family.`` After Character Set and Language Encoding seals a charsetdigest this
slot is that next family: RFC 5861 HTTP Cache-Control Extensions for Stale Content
STALE/IFERROR over a staleid-gated staledigest. The protocol hole
stays open so later genesis can opt the stalecontent provider in and seal a
staledigest.
"""

from __future__ import annotations

STALECONTENT_ACTUATION_ID = "capability.stalecontent-actuation"
STALECONTENT_ACTUATION_DONE_WHEN = (
    f"capability_exists:{STALECONTENT_ACTUATION_ID};"
    f"capability_proved:{STALECONTENT_ACTUATION_ID};"
    "no_skill_route"
)
STALECONTENT_ACTUATION_GOAL = (
    "Repair rfc5861 stalecontent stale/iferror cycle cannot land over http "
    "stalecontent staleid: hosted stalecontent endpoints remain unsupported so a STALE then "
    "IFERROR staleid handshake cannot land and a sealed staledigest "
    "cannot be produced. A missing stalecontent staleid stays forbidden; fail-closed "
    "routing never opts the stalecontent provider in. An independent later poll of the "
    "stored staledigest keeps the hole falsifiable."
)
