"""Next unsaturated diversity-catalog family after RFC 792 Internet Control Message Protocol.

ICMP is proved. Closed-contract leftover harvest used to steal genesis with
``Mission contract is closed; later genesis can take the next unsaturated
diversity-catalog family.`` After ICMP seals a icmpdigest this slot is that
next family: RFC 791 Internet Protocol DATAGRAM/FRAGMENT over an
ipid-gated ipdigest.
The protocol hole stays open so later genesis can opt the ip provider in
and seal a ipdigest.
"""

from __future__ import annotations

IP_ACTUATION_ID = "capability.ip-actuation"
IP_ACTUATION_DONE_WHEN = (
    f"capability_exists:{IP_ACTUATION_ID};"
    f"capability_proved:{IP_ACTUATION_ID};"
    "no_skill_route"
)
IP_ACTUATION_GOAL = (
    "Repair rfc791 ip datagram/fragment cycle cannot land over http "
    "ip ipid: hosted ip endpoints remain unsupported so a DATAGRAM then "
    "FRAGMENT ipid handshake cannot land and a sealed ipdigest "
    "cannot be produced. A missing ip ipid stays forbidden; fail-closed "
    "routing never opts the ip provider in. An independent later poll of the "
    "stored ipdigest keeps the hole falsifiable."
)
