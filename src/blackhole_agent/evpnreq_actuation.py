"""Next unsaturated diversity-catalog family after RFC 7432 MAC/IP.

BGP MPLS-Based Ethernet VPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After macip seals a macipdigest
this slot is that next family: RFC 7209 Requirements for Ethernet VPN REQ/AA
over an evpnreqid-gated evpnreqdigest.
The protocol hole stays open so later genesis can opt the evpnreq provider in
and seal an evpnreqdigest.
"""

from __future__ import annotations

EVPNREQ_ACTUATION_ID = "capability.evpnreq-actuation"
EVPNREQ_ACTUATION_DONE_WHEN = (
    f"capability_exists:{EVPNREQ_ACTUATION_ID};"
    f"capability_proved:{EVPNREQ_ACTUATION_ID};"
    "no_skill_route"
)
EVPNREQ_ACTUATION_GOAL = (
    "Repair rfc7209 evpnreq req/aa cycle cannot land over http evpnreq evpnreqid: hosted requirements for ethernet vpn remain unsupported so a REQ then AA evpnreqid handshake cannot land and a sealed evpnreqdigest cannot be produced. A missing evpnreq evpnreqid stays forbidden; fail-closed routing never opts the evpnreq provider in. An independent later poll of the stored evpnreqdigest keeps the hole falsifiable. Requirements for Ethernet VPN sessions stay fail-closed without an evpnreqid-gated evpnreqdigest."
)
