"""Next unsaturated diversity-catalog family after RFC 4447 LABEL/STATUS.

Pseudowire Setup and Maintenance Using LDP is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After pwldp seals a pwldpdigest
this slot is that next family: RFC 3985 Pseudo Wire Emulation Edge-to-Edge (PWE3) Architecture PSN/NSP
over a pwe3id-gated pwe3digest.
The protocol hole stays open so later genesis can opt the pwe3 provider in
and seal a pwe3digest.
"""

from __future__ import annotations

PWE3_ACTUATION_ID = "capability.pwe3-actuation"
PWE3_ACTUATION_DONE_WHEN = (
    f"capability_exists:{PWE3_ACTUATION_ID};"
    f"capability_proved:{PWE3_ACTUATION_ID};"
    "no_skill_route"
)
PWE3_ACTUATION_GOAL = (
    "Repair rfc3985 pwe3 psn/nsp cycle cannot land over http pwe3 pwe3id: hosted pseudo wire emulation edge-to-edge (pwe3) architecture remain unsupported so a PSN then NSP pwe3id handshake cannot land and a sealed pwe3digest cannot be produced. A missing pwe3 pwe3id stays forbidden; fail-closed routing never opts the pwe3 provider in. An independent later poll of the stored pwe3digest keeps the hole falsifiable. Pseudo Wire Emulation Edge-to-Edge (PWE3) Architecture sessions stay fail-closed without a pwe3id-gated pwe3digest."
)
