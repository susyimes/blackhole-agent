"""Next unsaturated diversity-catalog family after RFC 1945 HTTP/1.0.

Hypertext Transfer Protocol -- HTTP/1.0 is proved. Closed-contract leftover harvest
used to steal genesis with ``Mission contract is closed; later genesis can
take the next unsaturated diversity-catalog family.`` After HTTP/1.0
seals a http10digest this slot is that next family: RFC 1738
Uniform Resource Locators RESOLVE/LOCATE over a urlid-gated
urldigest.
The protocol hole stays open so later genesis can opt the url provider in
and seal a urldigest.
"""

from __future__ import annotations

URL_ACTUATION_ID = "capability.url-actuation"
URL_ACTUATION_DONE_WHEN = (
    f"capability_exists:{URL_ACTUATION_ID};"
    f"capability_proved:{URL_ACTUATION_ID};"
    "no_skill_route"
)
URL_ACTUATION_GOAL = (
    "Repair rfc1738 url resolve/locate cycle cannot land over http "
    "url urlid: hosted url endpoints remain unsupported so a RESOLVE then "
    "LOCATE urlid handshake cannot land and a sealed urldigest "
    "cannot be produced. A missing url urlid stays forbidden; fail-closed "
    "routing never opts the url provider in. An independent later poll of the "
    "stored urldigest keeps the hole falsifiable."
)
