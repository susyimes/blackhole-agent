"""Next unsaturated diversity-catalog family after RFC 1630 Universal Resource Identifiers.

Universal Resource Identifiers are proved. Closed-contract leftover harvest
used to steal genesis with ``Mission contract is closed; later genesis can
take the next unsaturated diversity-catalog family.`` After URI
seals a uridigest this slot is that next family: RFC 1521
MIME BODY/TRANSFER over a mimeid-gated
mimedigest.
The protocol hole stays open so later genesis can opt the mime provider in
and seal a mimedigest.
"""

from __future__ import annotations

MIME_ACTUATION_ID = "capability.mime-actuation"
MIME_ACTUATION_DONE_WHEN = (
    f"capability_exists:{MIME_ACTUATION_ID};"
    f"capability_proved:{MIME_ACTUATION_ID};"
    "no_skill_route"
)
MIME_ACTUATION_GOAL = (
    "Repair rfc1521 mime body/transfer cycle cannot land over http "
    "mime mimeid: hosted mime endpoints remain unsupported so a BODY then "
    "TRANSFER mimeid handshake cannot land and a sealed mimedigest "
    "cannot be produced. A missing mime mimeid stays forbidden; fail-closed "
    "routing never opts the mime provider in. An independent later poll of the "
    "stored mimedigest keeps the hole falsifiable."
)
