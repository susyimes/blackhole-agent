"""Next unsaturated diversity-catalog family after RFC 5571 L2TP/SPOKE.

L2TP is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After l2tp seals a l2tpdigest
this slot is that next family: RFC 5565 Softwire Mesh Framework MESH/PEER over a
meshid-gated meshdigest.
The protocol hole stays open so later genesis can opt the mesh provider in
and seal a meshdigest.
"""

from __future__ import annotations

MESH_ACTUATION_ID = "capability.mesh-actuation"
MESH_ACTUATION_DONE_WHEN = (
    f"capability_exists:{MESH_ACTUATION_ID};"
    f"capability_proved:{MESH_ACTUATION_ID};"
    "no_skill_route"
)
MESH_ACTUATION_GOAL = (
    "Repair rfc5565 mesh mesh/peer cycle cannot land over http mesh meshid: hosted "
    "softwire mesh framework endpoints remain unsupported so a MESH then PEER meshid handshake "
    "cannot land and a sealed meshdigest cannot be produced. A missing mesh meshid "
    "stays forbidden; fail-closed routing never opts the mesh provider in. An "
    "independent later poll of the stored meshdigest keeps the hole falsifiable. "
    "Softwire mesh sessions stay fail-closed without a meshid-gated meshdigest."
)
