"""Next unsaturated diversity-catalog family after RFC 4380 BUBBLE/QUAL.

TEREDO is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After teredo seals a teredodigest
this slot is that next family: RFC 5214 Intra-Site Automatic Tunnel Addressing
Protocol ISATAP/PRL over an isatapid-gated isatapdigest.
The protocol hole stays open so later genesis can opt the isatap provider in
and seal an isatapdigest.
"""

from __future__ import annotations

ISATAP_ACTUATION_ID = "capability.isatap-actuation"
ISATAP_ACTUATION_DONE_WHEN = (
    f"capability_exists:{ISATAP_ACTUATION_ID};"
    f"capability_proved:{ISATAP_ACTUATION_ID};"
    "no_skill_route"
)
ISATAP_ACTUATION_GOAL = (
    "Repair rfc5214 isatap isatap/prl cycle cannot land over http "
    "isatap isatapid: hosted intra-site automatic tunnel addressing protocol endpoints remain unsupported "
    "so an ISATAP then PRL isatapid handshake cannot land and a sealed "
    "isatapdigest cannot be produced. A missing isatap isatapid stays forbidden; "
    "fail-closed routing never opts the isatap provider in. An independent later "
    "poll of the stored isatapdigest keeps the hole falsifiable. Isatap "
    "potential router lists stay fail-closed without an isatapid-gated "
    "isatapdigest."
)
