"""Next unsaturated diversity-catalog family after RFC 9574 OIR/BUM.

Optimized Ingress Replication for EVPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After oir seals an oirdigest
this slot is that next family: RFC 9014 Interconnect Solution for Ethernet VPN Overlay Networks IESI/GW
over an iesiid-gated iesidigest.
The protocol hole stays open so later genesis can opt the iesi provider in
and seal an iesidigest.
"""

from __future__ import annotations

IESI_ACTUATION_ID = "capability.iesi-actuation"
IESI_ACTUATION_DONE_WHEN = (
    f"capability_exists:{IESI_ACTUATION_ID};"
    f"capability_proved:{IESI_ACTUATION_ID};"
    "no_skill_route"
)
IESI_ACTUATION_GOAL = (
    "Repair rfc9014 iesi iesi/gw cycle cannot land over http iesi iesiid: hosted interconnect solution for ethernet vpn overlay networks remain unsupported so an IESI then GW iesiid handshake cannot land and a sealed iesidigest cannot be produced. A missing iesi iesiid stays forbidden; fail-closed routing never opts the iesi provider in. An independent later poll of the stored iesidigest keeps the hole falsifiable. EVPN Overlay Interconnect sessions stay fail-closed without an iesiid-gated iesidigest."
)
