"""Next unsaturated diversity-catalog family after RFC 7209 REQ/AA.

Requirements for Ethernet VPN is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After evpnreq seals an evpnreqdigest
this slot is that next family: RFC 4761 Virtual Private LAN Service Using BGP VE/NLRI
over a vplsid-gated vplsdigest.
The protocol hole stays open so later genesis can opt the vpls provider in
and seal a vplsdigest.
"""

from __future__ import annotations

VPLS_ACTUATION_ID = "capability.vpls-actuation"
VPLS_ACTUATION_DONE_WHEN = (
    f"capability_exists:{VPLS_ACTUATION_ID};"
    f"capability_proved:{VPLS_ACTUATION_ID};"
    "no_skill_route"
)
VPLS_ACTUATION_GOAL = (
    "Repair rfc4761 vpls ve/nlri cycle cannot land over http vpls vplsid: hosted virtual private lan service using bgp remain unsupported so a VE then NLRI vplsid handshake cannot land and a sealed vplsdigest cannot be produced. A missing vpls vplsid stays forbidden; fail-closed routing never opts the vpls provider in. An independent later poll of the stored vplsdigest keeps the hole falsifiable. Virtual Private LAN Service Using BGP sessions stay fail-closed without a vplsid-gated vplsdigest."
)
