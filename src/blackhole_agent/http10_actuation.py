"""Next unsaturated diversity-catalog family after RFC 2069 Digest Access Authentication.

Digest Access Authentication is proved. Closed-contract leftover harvest
used to steal genesis with ``Mission contract is closed; later genesis can
take the next unsaturated diversity-catalog family.`` After Digest Access
Authentication seals a responsedigest this slot is that next family: RFC 1945
Hypertext Transfer Protocol -- HTTP/1.0 GET/POST over a http10id-gated
http10digest.
The protocol hole stays open so later genesis can opt the http10 provider in
and seal a http10digest.
"""

from __future__ import annotations

HTTP10_ACTUATION_ID = "capability.http10-actuation"
HTTP10_ACTUATION_DONE_WHEN = (
    f"capability_exists:{HTTP10_ACTUATION_ID};"
    f"capability_proved:{HTTP10_ACTUATION_ID};"
    "no_skill_route"
)
HTTP10_ACTUATION_GOAL = (
    "Repair rfc1945 http10 get/post cycle cannot land over http "
    "http10 http10id: hosted http10 endpoints remain unsupported so a GET then "
    "POST http10id handshake cannot land and a sealed http10digest "
    "cannot be produced. A missing http10 http10id stays forbidden; fail-closed "
    "routing never opts the http10 provider in. An independent later poll of the "
    "stored http10digest keeps the hole falsifiable."
)
