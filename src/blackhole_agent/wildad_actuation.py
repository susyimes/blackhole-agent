"""Next unsaturated diversity-catalog family after RFC 6514 PMSI/NLRI.

BGP Encodings and Procedures for Multicast in MPLS/BGP IP VPNs is proved.
Closed-contract leftover harvest used to steal genesis with ``Mission contract
is closed; later genesis can take the next unsaturated diversity-catalog
family.`` After pmsi seals a pmsidigest this slot is that next family: RFC 6625
Wildcards in Multicast VPN Auto-Discovery Routes WILD/AD over a wildid-gated
wilddigest. The protocol hole stays open so later genesis can opt the wildad
provider in and seal a wilddigest.
"""

from __future__ import annotations

WILDAD_ACTUATION_ID = "capability.wildad-actuation"
WILDAD_ACTUATION_DONE_WHEN = (
    f"capability_exists:{WILDAD_ACTUATION_ID};"
    f"capability_proved:{WILDAD_ACTUATION_ID};"
    "no_skill_route"
)
WILDAD_ACTUATION_GOAL = (
    "Repair rfc6625 wildad wild/ad cycle cannot land over http wildad wildid: "
    "hosted wildad remain unsupported so a WILD then AD wildid handshake cannot "
    "land and a sealed wilddigest cannot be produced. A missing wildad wildid stays "
    "forbidden; fail-closed routing never opts the wildad provider in. An independent "
    "later poll of the stored wilddigest keeps the hole falsifiable. WILDAD sessions "
    "stay fail-closed without a wildid-gated wilddigest."
)
WILDAD_LEFTOVER = (
    "Later genesis can take RFC 6625 Wildcards in Multicast VPN Auto-Discovery "
    "Routes WILD/AD over a wildid-gated wilddigest."
)
