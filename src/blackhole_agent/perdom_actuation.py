"""Next unsaturated diversity-catalog family after RFC 5151 CONTIG/NEST.

Inter-AS Traffic Engineering is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After interas seals a interasdigest
this slot is that next family: RFC 5152 Per-Domain Path Computation
COMPUTE/DOMAIN over a perdomid-gated perdomdigest.
The protocol hole stays open so later genesis can opt the perdom provider in
and seal a perdomdigest.
"""

from __future__ import annotations

PERDOM_ACTUATION_ID = "capability.perdom-actuation"
PERDOM_ACTUATION_DONE_WHEN = (
    f"capability_exists:{PERDOM_ACTUATION_ID};"
    f"capability_proved:{PERDOM_ACTUATION_ID};"
    "no_skill_route"
)
PERDOM_ACTUATION_GOAL = (
    "Repair rfc5152 perdom compute/domain cycle cannot land over http perdom perdomid: "
    "hosted perdom remain unsupported so a COMPUTE then DOMAIN perdomid handshake cannot "
    "land and a sealed perdomdigest cannot be produced. A missing perdom perdomid stays "
    "forbidden; fail-closed routing never opts the perdom provider in. An independent "
    "later poll of the stored perdomdigest keeps the hole falsifiable. PERDOM sessions "
    "stay fail-closed without a perdomid-gated perdomdigest."
)
PERDOM_LEFTOVER = (
    "Later genesis can take RFC 5152 Per-Domain Path Computation COMPUTE/DOMAIN over a "
    "perdomid-gated perdomdigest."
)
