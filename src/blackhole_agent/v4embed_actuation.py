"""Next unsaturated diversity-catalog family after RFC 7756 DTM/MODE.

SIIT-DTM is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After siitdtm seals a siitdtmdigest
this slot is that next family: RFC 6052 IPv6 Addressing of IPv4/IPv6
Translators WKP/NSP over a v4embedid-gated v4embeddigest.
The protocol hole stays open so later genesis can opt the v4embed provider in
and seal a v4embeddigest.
"""

from __future__ import annotations

V4EMBED_ACTUATION_ID = "capability.v4embed-actuation"
V4EMBED_ACTUATION_DONE_WHEN = (
    f"capability_exists:{V4EMBED_ACTUATION_ID};"
    f"capability_proved:{V4EMBED_ACTUATION_ID};"
    "no_skill_route"
)
V4EMBED_ACTUATION_GOAL = (
    "Repair rfc6052 v4embed wkp/nsp cycle cannot land over http "
    "v4embed v4embedid: hosted ipv4-embedded ipv6 addressing endpoints remain unsupported "
    "so a WKP then NSP v4embedid handshake cannot land and a sealed "
    "v4embeddigest cannot be produced. A missing v4embed v4embedid stays forbidden; "
    "fail-closed routing never opts the v4embed provider in. An independent later "
    "poll of the stored v4embeddigest keeps the hole falsifiable. Well-known and "
    "network-specific translator prefixes stay fail-closed without a v4embedid-gated "
    "v4embeddigest."
)
