"""Next unsaturated diversity-catalog family after RFC 9014 IESI/GW.

Interconnect Solution for Ethernet VPN Overlay Networks is proved. Closed-contract leftover harvest used to steal
genesis with ``Mission contract is closed; later genesis can take the next
unsaturated diversity-catalog family.`` After iesi seals an iesidigest
this slot is that next family: RFC 7623 Provider Backbone Bridging Combined with Ethernet VPN PBB/BMAC
over a pbbid-gated pbbdigest.
The protocol hole stays open so later genesis can opt the pbb provider in
and seal a pbbdigest.
"""

from __future__ import annotations

PBB_ACTUATION_ID = "capability.pbb-actuation"
PBB_ACTUATION_DONE_WHEN = (
    f"capability_exists:{PBB_ACTUATION_ID};"
    f"capability_proved:{PBB_ACTUATION_ID};"
    "no_skill_route"
)
PBB_ACTUATION_GOAL = (
    "Repair rfc7623 pbb pbb/bmac cycle cannot land over http pbb pbbid: hosted provider backbone bridging combined with ethernet vpn remain unsupported so a PBB then BMAC pbbid handshake cannot land and a sealed pbbdigest cannot be produced. A missing pbb pbbid stays forbidden; fail-closed routing never opts the pbb provider in. An independent later poll of the stored pbbdigest keeps the hole falsifiable. Provider Backbone Bridging Combined with Ethernet VPN sessions stay fail-closed without a pbbid-gated pbbdigest."
)
