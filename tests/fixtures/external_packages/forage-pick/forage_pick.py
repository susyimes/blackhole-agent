"""Uncooperative fixture the forage-target plane must select unaided.

Ships no manifest, no spec, and no probe cases. The unique unary
transform ``pick`` is the catalog leaf that trend ranking should win
once already-absorbed and non-viable packages are skipped.
"""


def pick(text):
    return f"picked:{text}"
