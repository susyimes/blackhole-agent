"""Uncooperative fixture the application-growth plane must forage unaided.

Ships no manifest, no spec, and no probe cases. Trend ranking prefers a
more popular decoy that *declares* ``rotate_output``; only inferred
callable behavior covers the unplannable application goal.
"""


def rotate(text):
    return f"rotated:{text}"
