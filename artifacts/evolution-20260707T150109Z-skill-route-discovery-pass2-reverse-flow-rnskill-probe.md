# Evolution Run

Run: 20260707T150109Z-skill-route-discovery-pass2-reverse-flow-rnskill-probe

Source digest: github-growth-20260707T150109.515302Z

Theme: skill-route-discovery, pass 2 of 4

## Evidence Review

- `lingbol088-spec/reverse-flow-skill` exposes a Codex and AI Agent skill workflow with `skills/reverse-flow/SKILL.md`, references, scripts, local sandbox or CTF framing, install examples, and staged reverse-analysis workflow language. The reverse-analysis domain makes upstream install, run, script execution, provider runtime, external harness, or remote execution unsafe as direct trend-driven actions.
- `Pluviobyte/rnskill` exposes a generic AI Agent skills collection for `SKILL.md`-compatible workflows, with skills, docs, tools, marketplace metadata, and manual install examples. This is route-shape evidence for documentation/config/test/code_patch lanes, not activation evidence.
- `shepherd-agents/shepherd` carries advisory memory-context and runtime workflow signals, but it is a general-agent project without an explicit skill route. It remains behind local agent-harness evaluation before any memory, profile, or behavior change.

## Hypothesis

The pass should expose a current-digest validation lane that compares specialized Codex workflow skill evidence against a generic skill collection and keeps adjacent general-agent memory/runtime signals separate. This gives the supervisor a replayable behavior path instead of another standalone note.

## Rollback

- Rollback artifact: `artifacts/rollback/20260707T150109Z-skill-route-discovery-pass2-reverse-flow-rnskill-probe/rollback-point.md`
- Rollback ref: `refs/rollback/20260707T150109Z-skill-route-discovery-pass2-reverse-flow-rnskill-probe`

## Material Actions

- Added the `github-growth-20260707T150109.515302Z` current-digest pass-2 route lane in `src/blackhole_agent/skill_routing.py`.
- Added a frozen local fixture for reverse-flow, rnskill, Agents-A1, Fundamental-Ava, and Shepherd evidence.
- Added regression coverage proving reverse-flow and rnskill enter bounded skill-route lanes while Shepherd remains `agent_harness_eval_required`.
- Updated `docs/skill-route-discovery.md` with the operator-visible decision path.
- Left `docs/self-model.md` unchanged because it already favors rollback-backed local validation over ornamental self-model edits.

## Validation

- `python -m py_compile src/blackhole_agent/skill_routing.py` passed.
- `python -m pytest tests/test_skill_routing.py -q -k 20260707T150109` passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed.
- `python -m pytest tests/test_skill_routing.py -q -k "20260707T121946 or 20260707T150109"` passed.

## Review Notes

- External evidence review was limited to the carried proposal URLs.
- No external repository was cloned, installed, imported, or executed.
- Runtime action, external skill activation, external harness execution, provider launch, memory/profile writes, remote execution, raw URLs, raw replay commands, and upstream bodies remain denied in the lane output.
