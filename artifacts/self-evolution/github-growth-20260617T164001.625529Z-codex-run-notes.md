# Codex Run Notes

Source digest: `github-growth-20260617T164001.625529Z`
Selected proposal: `p1-native-harness-policy-fail-closed`

## Evidence

- Reviewed https://github.com/omnigent-ai/omnigent/issues/536.
- Reusable lesson: native or connector policy hooks must not treat unavailable, slow, malformed, or ambiguous policy responses as permission to execute governed tools.

## Hypothesis

Blackhole-agent's local tool routing should fail closed for malformed connector policy verdicts and should represent ASK-style policy outcomes as `review_only`, not as executable tools.

## Rollback

- Created local rollback ref: `refs/rollback/20260617T164001-native-policy-fail-closed`
- Wrote rollback artifact: `artifacts/rollback/20260617T164001Z-native-policy-fail-closed.txt`
- Original HEAD: `f75f1ec735d079c17c48fd9387f33cc0b5902c8f`

## Changes

- Added `ToolCallPolicyResult.review_required` to represent ASK-style policy outcomes.
- Routed policy review-required results to `review_only` before a tool enters the executable registry.
- Tightened malformed policy verdict handling so non-boolean `allowed`, non-boolean `review_required`, dict-like raw responses, and other invalid return types deny instead of relying on truthiness.
- Added focused regression tests for timeout, connection failure, generic server error, malformed result, malformed `allowed`, and review-required policy paths.
- Documented the fail-closed connector policy contract in `docs/architecture.md`.

## Self-Model Decision

Read `docs/self-model.md` and left it unchanged. The current self-model already prefers rollback-backed, locally validated behavior changes; this run's evidence was better captured as executable router behavior and architecture contract.

## Validation

- `uv run pytest tests/test_tool_routing.py` passed: 23 tests.
- `uv run ruff check src/blackhole_agent/tool_routing.py tests/test_tool_routing.py docs/architecture.md` passed.
- `uv run pytest` passed: 189 tests.

## Review Notes

- The proposal evidence concerned Omnigent native hooks. This repository does not have those exact hook subprocess files, so the local adaptation targets the repository's connector-native tool policy boundary in `tool_routing.py`.
- Provider/config/token preflight remains review-only for privacy-sensitive behavior and was not implemented in this pass.
