# Codex Run Notes: github-growth-20260617T034315.664305Z

## Evidence Reviewed

- https://github.com/ApodexAI/AgentHarness: public deep-research benchmark harness with smoke/full benchmark runs, subprocess isolation, progress checks, and reproducible outputs.
- https://github.com/samarailly51-pixel/opencode-harness: coding-agent harness emphasizing model-agnostic task inputs, permissioned tools, JSONL traces, reports, eval suites, and failure diagnosis.

## Hypothesis

Small, local agent-task fixtures should be able to exercise blackhole-agent proposal interpretation through the existing harness eval route. This makes proposal interpretation drift replayable without model credentials, remote execution, or broad trend rediscovery.

## Change

- Added `proposal_interpretation` as a supported local harness eval behavior in `src/blackhole_agent/harness_eval.py`.
- The adapter runs existing proposal replay interpretation, emits strict JSON fields, and checks accepted `evidence_refs` against supplied digest `item_id` values and selected context IDs.
- Added two focused fixtures under `tests/fixtures/harness_adapter/`:
  - accepted item-ID refs
  - rejected URL refs
- Extended `tests/test_harness_eval.py` to verify JSON serialization, input omission, and evidence-ref scoping.
- Updated `docs/architecture.md` with the new fixture behavior.

## Self-Model Decision

`docs/self-model.md` was left unchanged. The current preference already favors rollback-backed, locally validated implementation over report-only work, and this run followed that preference directly.

## Rollback

- Rollback artifact: `artifacts/rollback/20260617T034315Z-local-harness-adapter-route.txt`
- Rollback ref: `refs/blackhole-rollback/github-growth-20260617T034315.664305Z`
- Original HEAD: `7f4b9c5547ec8efd1c8426a5f8c63230311e9f21`

Rollback execution is explicit and destructive; a human operator or supervisor policy must choose it.

## Validation

- `uv run pytest tests/test_harness_eval.py tests/test_proposal_eval.py`: 17 passed
- `uv run pytest`: 126 passed
- `uv run ruff check .`: all checks passed

## Review Notes

- Proposal p1 was implemented as a local validation candidate.
- Proposal p2 remained review-only because token/config preflight behavior can cross privacy-leakage boundaries if implemented carelessly.
- Proposal p3 remained review-only because offensive or abuse-enabling harness behavior must not be implemented locally.
- No restart, promotion, push, or remote execution was performed by this kernel.
