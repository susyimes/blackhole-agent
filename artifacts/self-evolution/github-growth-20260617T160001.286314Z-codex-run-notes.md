# Codex Run Notes

Source digest: github-growth-20260617T160001.286314Z
Run theme: inline agent tool policy identity hardening

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent/issues/525
  - Reusable lesson: policy gates that deny a sub-agent by declared name fail if the gate only sees a generic transport tool name.
- https://github.com/omnigent-ai/omnigent/issues/523
  - Reusable lesson recorded as lower-priority for this repository: shard-load-sensitive REPL/pexpect readiness needs representative validation, but this repository has no REPL pexpect lane to harden in this pass.

## Hypothesis

Local tool routing should distinguish the executable transport name from the policy identity that a deny rule must evaluate. A wrapped local tool such as `sys_session_send` can now declare `policy_name="worker"` so existing deny evaluators match the inline tool or sub-agent identity without changing the registry key.

## Rollback

- Original branch: `codex/blackhole-evolve/20260617T160125.243678-investigate-and-harden-repl-pexpect-e2e-startup-`
- Original HEAD: `054f700ae63db9218700cc1b2effa1614cd15007`
- Rollback ref: `refs/rollback/20260617T160208Z-inline-agent-tool-policy`
- Rollback artifact: `artifacts/rollback/20260617T160208Z-inline-agent-tool-policy.txt`

## Material Actions

- Created rollback ref with `git update-ref`.
- Added rollback artifact.
- Updated `src/blackhole_agent/tool_routing.py` to add `ToolDescriptor.policy_name`, policy identity helpers, inspectable decision metadata, and policy evaluation through the declared identity.
- Updated `tests/test_tool_routing.py` with wrapped-tool deny and registry behavior coverage.
- Left `docs/self-model.md` unchanged; it already frames validated local behavior changes as acceptable and did not need to shape this implementation.

## Validation

- `python -m pytest tests/test_tool_routing.py`
  - Result before final import cleanup: `19 passed in 0.06s`
- `python -m pytest`
  - Result before final import cleanup: `183 passed in 89.72s`
- `python -m pytest`
  - Final result: `183 passed in 50.01s`

## Review Notes

- Proposal 2 was implemented locally because it is outside the offensive-behavior/privacy boundary and has direct focused tests.
- Proposal 1 was not implemented because this repository does not expose an analogous REPL pexpect startup lane in the inspected file set.
- Proposal 3 remains review-only per operator instruction; no provider secrets or private configuration values were read, printed, or logged.
