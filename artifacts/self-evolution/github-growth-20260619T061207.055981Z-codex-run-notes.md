# Codex Run Notes

Source digest: `github-growth-20260619T061207.055981Z`

Capability window: `skill-route-discovery`

## Hypothesis

Public skill-route evidence is more useful when the controller can replay it
through a local harness lane before activation. The existing unit-level
classification was strong, but the operator-visible local eval surface did not
directly validate that frozen skill evidence becomes only bounded local lanes.

## Rollback

Rollback ref: `refs/rollback/20260619T061207Z-skill-route-discovery-pass1`

Rollback artifact:
`artifacts/rollback/20260619T061207Z-skill-route-discovery-pass1.txt`

Recovery commands are recorded in the rollback artifact. Rollback execution is
explicit and destructive, so this run did not execute reset or clean commands.

## Change

Added the `skill_route_discovery_lane` local harness behavior. It accepts
body-free evidence items, repository summaries, or pre-shaped candidates, builds
the disabled skill-route discovery registry, renders proposal lanes, and reports
whether lanes are bounded, runtime-safe, and locally validated before
activation.

The harness output hashes source and evidence URLs instead of exporting them and
does not execute external skill code. Candidates requesting install, enable,
run, execute, clone-and-run, or local deletion remain rejected before lane
activation.

## Self-Model

Read `docs/self-model.md` before choosing the change. Left it unchanged because
the current preference already supports rollback-backed, locally validated
behavior improvements and did not conflict with this run's evidence.

## Validation

- `pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`
- `pytest tests/test_skill_routing.py -q`
- `pytest tests/test_harness_eval.py -q`
- `pytest tests/test_docs_contracts.py -q`
- `pytest -q`

Final result: `293 passed`.

## Review Notes

External evidence was used only at the proposal URL level supplied by the
window. No upstream code, installers, prompts, or skill bodies were imported or
executed. The new harness lane is metadata-only and should be the next
controller-visible surface for later passes in this capability slice.
