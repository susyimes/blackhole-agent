# Self-Evolution Run Notes

Source digest: `github-growth-20260618T094044.063929Z`
Branch: `codex/blackhole-evolve/20260618T094229.915048-add-or-extend-local-provider-config-preflight-te`
Rollback artifact: `artifacts/rollback/20260618T094043Z-provider-google-mock-approval-preflight.txt`
Rollback ref: `refs/rollback/20260618T094043Z-provider-google-mock-approval-preflight`

## Evidence

- `https://github.com/omnigent-ai/omnigent/pull/641`
- `https://github.com/omnigent-ai/omnigent/pull/642`

The reviewed upstream approval-test PR moved a subset of REPL approval checks onto mock LLMs while keeping unsupported tool/subagent/output-phase cases behind explicit skips. Its review also called out a provider-routing regression risk: mock base URLs must not leak into real-key runs. The local reusable lesson is to make provider-route assumptions and mock approval gates explicit, metadata-only, and covered by narrow tests.

## Hypothesis

Codex provider preflight and mock LLM route evaluation should expose enough structured state to detect:

- ambient Google credential misconfiguration without recording keys or credential paths
- mock approval/review-required paths that are passive and non-executing rather than silently treated as ordinary success

This improves future trend-validation reliability without live provider calls, secret handling, offensive behavior, or remote execution.

## Changes

- Added `ambient_google` metadata to Codex provider/config preflight.
- Added `build_ambient_google_preflight` with env-name presence, route hints, missing-key diagnostics, and value-redaction booleans.
- Added explicit `approval_required` and `approval_path` metadata to embedded native tool policy summaries inside `mock_llm_workflow_route`.
- Added focused tests for Google key/application-credential warning behavior and blank-value handling.
- Added focused mock LLM approval-path coverage proving review-required tool calls stay passive and body-free.
- Updated `docs/architecture.md` to document metadata-only ambient provider discovery.

## Self-Model Decision

Left `docs/self-model.md` unchanged. It already treats provider/config preflight, mock validation, tool routing, and rollback-backed local evolution as in scope while keeping privacy leakage and offensive behavior outside autonomous application. This run did not produce evidence that the self-description needed a new structure or correction.

## Validation

- `uv run pytest tests/test_codex_cli.py tests/test_harness_eval.py -q` passed: 46 tests.
- `uv run ruff check src/blackhole_agent/kernels/codex_cli.py src/blackhole_agent/harness_eval.py tests/test_codex_cli.py tests/test_harness_eval.py docs/architecture.md` passed.

## Review Notes

- Proposal 2 stayed review-only because it concerns CI merge-gate integrity and untrusted PR-head execution policy. No attack, exploit, unauthorized-access, or remote-execution behavior was implemented.
- No live provider credentials were read or used.
- No external provider calls were made.
- Raw OpenAI URLs, Google keys, Google credential paths, prompt bodies, session IDs, and native tool arguments are not exported by the new diagnostics or tests.
