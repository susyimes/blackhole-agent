# Self-Evolution Run Notes

Source digest: `github-growth-20260615T061233.549174Z`

## Evidence Reviewed

- `https://github.com/visa/visa-vulnerability-agentic-harness`

Reusable pattern extracted: the harness documents scan outputs that include a `run_manifest.json` with tool/version, model roles, config hash, target git SHA, and timing. The local lesson is to make blackhole-agent Codex runs easier to replay and review by writing a compact manifest next to the existing run result.

## Change

- Added `latest-self-evolution-manifest.json` after successful Codex self-evolution runs.
- The manifest records source digest identity, branch, target HEAD, task and last-message paths, evidence URLs, proposal IDs, validation gates, return code, and run timing.
- Updated the growth-controller test to verify the manifest shape.
- Updated the README artifact table.

## Self-Model Decision

`docs/self-model.md` was left unchanged. In this run it was still a blank, policy-neutral self-description, and the evidence supported improving replayable run artifacts rather than revising identity text.

## Validation

- `uv run --extra dev python -m pytest tests/test_github_growth.py::test_prepare_branch_and_run_codex_invoke_expected_commands`
- `uv run --extra dev python -m pytest tests/test_github_growth.py`
- `uv run --extra dev ruff check src\blackhole_agent\github_growth.py tests\test_github_growth.py`

Note: `uv run pytest ...` used an environment path that did not load the same project dev dependency set; validation used `uv run --extra dev python -m pytest ...` to bind pytest to the project interpreter.

## Review Notes

- This is an observability-only change. It does not alter proposal ranking, mutation permissions, supervisor promotion, or restart behavior.
- The evidence review was limited to the proposal URL and README content; no broad trend discovery was rerun.
