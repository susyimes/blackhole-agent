# github-growth-20260618T064044.692705Z Codex Run Notes

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent
- https://github.com/omnigent-ai/omnigent/pull/623

The reviewed PR migrates sandbox e2e checks from live LLM calls to a mock LLM path. Its review notes also identify a false-green risk: mock sandbox tests must prove the mock response was consumed and the intended tool/sandbox boundary was exercised, not merely that an outside-workspace side effect is absent.

## Hypothesis

Strengthening `mock_llm_workflow_route` to report queued-response consumption and declared tool-call observation improves local validation reliability without requiring live provider credentials, external execution, or secret-bearing preflight checks.

## Rollback

- Artifact: `artifacts/rollback/20260618T064044Z-mock-llm-sandbox-e2e-validation.txt`
- Ref: `refs/rollback/20260618T064044Z-mock-llm-sandbox-e2e-validation`
- Original HEAD: `41b9538204bef69f2c0b52d499f11ec658bb2145`

Rollback remains explicit and destructive; an operator or supervisor must choose recovery before running the recorded reset/clean commands.

## Changes

- Added queue-consumption evidence to `mock_llm_workflow_route` output.
- Added a mock tool-call contract that checks declared file-tool and native-policy tool calls were observed in consumed mock turns.
- Updated local mock-LLM fixtures and unit tests to assert the stronger contract.
- Documented the mock-LLM validation contract in `docs/architecture.md`.

## Self-Model Decision

Left `docs/self-model.md` unchanged. The current self-model already prefers local validated behavior changes over report-only scaffolding and did not need a new preference for this run.

## Validation

- `pytest tests/test_harness_eval.py -q` -> 29 passed
- `pytest -q` -> 209 passed
- `ruff check .` -> all checks passed

## Review Notes

- Provider/config/token preflight remains review-only for this run because proposal p2 is explicitly privacy-leakage-human-review scoped.
- No token, credential, private chat, PII, or raw native tool arguments were exposed by the new structured output.
