# Codex Run Notes: skill-route discovery index

Source digest: `github-growth-20260619T085206.999156Z`
Capability window: `skill-route-discovery`
Pass: 4 of 4

## Rollback

- Rollback ref: `refs/rollback/20260619T085206Z-skill-route-discovery-index`
- Rollback artifact: `artifacts/rollback/20260619T085206Z-skill-route-discovery-index.txt`
- Recovery command: `git reset --hard refs/rollback/20260619T085206Z-skill-route-discovery-index`

Rollback is explicit and destructive; this run only records the recovery path.

## Evidence

Selected local proposal: `p4-skill-route-discovery-index`.

Evidence URLs carried by the run:

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`

The provider/preflight proposals in this run were kept review-only because their
validation gate is privacy-leakage human review.

## Hypothesis

The skill-route discovery lane is more useful at the end of this capability
window if it emits a bounded operator checklist, not only aggregate lane counts.
Each row should preserve the source/capability/lane/test/rollback review shape
requested by the proposal while keeping raw source and evidence URLs out of
harness output and keeping external skill activation disabled.

## Changes

- Added `discovery_checklist` to `skill_route_discovery_lane` output.
- Checklist rows include hashed source, capability, allowed local lane, required
  validation command, rollback note, runtime action, and external activation
  flag.
- Updated focused harness tests for clean, blocked, and weak-evidence cases.
- Updated the skill-route discovery documentation and doc contract test.

## Self-Model

Read `docs/self-model.md` and left it unchanged. The current preference already
supports direct rollback-backed local evolution while keeping privacy leakage
and offensive behavior review-only. This run made that preference more
operator-visible in the harness output rather than adding another
self-description paragraph.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`
  - 4 passed, 62 deselected
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
  - 2 passed, 6 deselected
- `python -m pytest tests/test_skill_routing.py -q -k skill_route_discovery`
  - 12 passed, 9 deselected
- `python -m pytest tests/test_harness_eval.py tests/test_docs_contracts.py tests/test_skill_routing.py -q`
  - 95 passed
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py tests/test_docs_contracts.py`
  - passed

## Review Notes

- No upstream skill code, install scripts, prompts, repositories, or skill
  bodies were imported, cloned, installed, enabled, or executed.
- No remote mutation, push, promotion, restart, cloud sandbox, live provider
  call, or external runner activation was attempted.
- Raw source URLs and evidence URLs remain absent from harness output; source
  references are represented by stable hashes in the new checklist.
- Privacy-leakage/provider-preflight proposals remain review-only for a human
  boundary check.
