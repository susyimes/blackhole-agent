# Codex Run Notes

Source digest: `github-growth-20260620T101207.620676Z`
Capability window: `skill-route-discovery`, pass 2 of 4
Branch: `codex/blackhole-evolve/20260620T101322.298239-add-or-run-a-bounded-skill-route-discovery-valid`
Rollback artifact: `artifacts/rollback/20260620T101207Z-skill-route-discovery-pass2.txt`
Rollback ref: `refs/rollback/20260620T101207Z-skill-route-discovery-pass2`

## Evidence Reviewed

- `https://github.com/dongshuyan/compass-skills`: repository page shows a `skills/` directory, `skills.sh.json`, four `SKILL.md` skills, local task memory, handoff prompts, and user profile behavior. Local lesson: state/profile/handoff routes need metadata and privacy boundaries before activation.
- `https://github.com/majidmanzarpour/threejs-game-skills`: repository page describes a Three.js game director, specialist skills, browser/canvas/screenshot QA, optional asset providers, and a credential probe. Local lesson: game/director skill routes need validation targets plus provider/asset boundaries before activation.
- `https://github.com/baskduf/FableCodex`: repository page describes evidence-based Codex workflow gates, local ledgers, verification habits, install commands, and local state. Local lesson: workflow routes need selected digest IDs, body-free summaries, and local gate/test targets before activation.

No upstream code, install command, helper script, scaffold, provider route, skill body, profile write, memory write, asset generation, browser run, or credential probe was adopted or executed.

## Hypothesis

The route profile review already lists expected metadata for FableCodex, COMPASS, and Three.js profiles, but operators also need to know whether the frozen fixture actually supplied that metadata. A body-free metadata coverage check should make profile readiness auditable before activation without exposing upstream bodies or granting runtime authority.

## Changes

- Added `skill_route_discovery_profile_metadata_coverage()` and body-free evidence-record extraction in `src/blackhole_agent/harness_eval.py`.
- Wired `metadata_coverage` and `metadata_complete` into `route_profile_review` rows.
- Added `metadata_missing` diagnostics when a profile lacks required selected IDs, summaries, local validation targets, state/privacy boundaries, or asset/provider boundary notes.
- Extended `tests/test_harness_eval.py` with positive FableCodex coverage assertions and a missing-metadata review case.
- Documented the new profile metadata coverage gate in `docs/skill-route-discovery.md`.

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged because this run did not find evidence that the self-model is behavior-shaping or contradictory. The selected change follows its current preference for rollback-backed, locally validated behavior improvements over another standalone report.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` passed: 9 passed, 86 deselected.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 passed, 6 deselected.
- `python -m pytest tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery` passed: 17 passed, 25 deselected.
- `python -m pytest tests/test_harness_eval.py tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 34 passed, 111 deselected.

## Rollback

Destructive recovery remains operator/supervisor-controlled:

```bash
git switch codex/blackhole-evolve/20260620T101322.298239-add-or-run-a-bounded-skill-route-discovery-valid
git reset --hard refs/rollback/20260620T101207Z-skill-route-discovery-pass2
git clean -fd
```

## Review Notes

- `route_profile_review.status == "review"` now means the profile exists but required profile metadata is incomplete.
- The metadata coverage panel exports counts and stable hashes only; raw source URLs, evidence bodies, target paths, and upstream skill bodies remain withheld.
- Runtime action, external skill activation, external harness execution, provider launch, remote execution, upstream clone/install/run, memory/profile writes, and raw upstream body export remain denied.
