# Skill Route Discovery Pass 1 Validation Contract

Source digest: `github-growth-20260621T095207.700715Z`
Rollback point: `artifacts/rollback/20260621T095206Z-skill-route-discovery-pass1.txt`

## Evidence

- `https://github.com/dongshuyan/compass-skills`: public skill ecosystem with task clarification, repo-local task memory, handoff prompts, and local collaboration profile state. Local lesson: state handoff needs a config/privacy boundary before any profile or memory write.
- `https://github.com/majidmanzarpour/threejs-game-skills`: public Three.js game skill bundle with director/specialist routing, bundled scaffold/helpers, browser checks, screenshots, canvas-pixel checks, and optional provider-backed assets. Local lesson: game/frontend skill evidence should validate through local frontend/test lanes before activation.
- `https://github.com/baskduf/FableCodex`: public Codex workflow plugin with evidence gates, ledgers, examples, tests/evals, and verification habits. Local lesson: mixed Codex/skill/workflow evidence should prove `skill_route_discovery_first` before secondary workflow or harness activation.

## Hypothesis

The core disabled-registry lane map already emits bounded lanes, but downstream
operators still had to infer profile-specific validation gates from later
harness surfaces. Adding a body-free `route_validation_contract` directly to
candidate inventory and proposal-lane rows makes pass-1 route evidence
activation-ready only when the right local gate is explicit.

## Change

- Added profile validation contracts in `src/blackhole_agent/skill_routing.py`.
- Emitted `route_validation_contract` on every skill-route candidate inventory row and proposal-lane row.
- Added focused tests for COMPASS state handoff, FableCodex workflow-gate, Three.js game frontend, and generic downgraded-lane contracts.
- Updated `docs/skill-route-discovery.md` and docs-contract coverage.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k skill_route_discovery`: passed, 16 passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`: passed, 2 passed.
- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`: passed, 9 passed.
- `python -m pytest tests/test_proposal_eval.py -q -k "skill_route_discovery or route_hint_lane_map"`: passed, 5 passed.
- `python -m pytest tests/test_github_growth.py -q -k "skill_route or route_activation_preflight or route_hint_lane_map"`: passed, 10 passed.

## Review Notes

- The change grants no runtime action and adds no allowed lanes.
- The contract is derived from local route profile names and already bounded lanes only.
- External skill activation, external harness activation, provider launch, remote execution, raw source URL export, and raw upstream body export remain denied.
- The self-model was read and left unchanged because it already supports direct rollback-backed local evolution; this run did not produce evidence that the self-description shapes runtime behavior.
