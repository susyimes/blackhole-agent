# Codex Run Notes

Source digest: `github-growth-20260620T111207.610236Z`
Branch: `codex/blackhole-evolve/20260620T111503.746582-add-or-extend-a-local-validation-lane-that-can-c`
Rollback artifact: `artifacts/self-evolution/github-growth-20260620T111207.610236Z-rollback.md`
Rollback ref: `refs/blackhole-rollback/github-growth-20260620T111207.610236Z`

## Evidence Reviewed

- `https://github.com/dongshuyan/compass-skills`: public repository presents a `SKILL.md` ecosystem with local memory, handoff, profile, and clarification workflows plus install commands.
- `https://github.com/majidmanzarpour/threejs-game-skills`: public repository presents game-oriented director/specialist skills, bundled scaffolds, QA checks, optional provider keys, and installer paths.
- `https://github.com/baskduf/FableCodex`: public repository presents a Codex workflow plugin/skill pattern with evidence gates, findings ledgers, verification habits, local examples, and install commands.

## Hypothesis

The existing `skill_route_discovery_lane` already has bounded lanes, profile review, activation manifests, and completion handoff. The next useful pass is an operator-visible evidence-to-lane matrix that shows, per external source family, which local lanes were derived and which upstream install/execute/provider signals stayed downgraded or denied before activation.

## Changes

- Added `evidence_lane_matrix` to `skill_route_discovery_lane` output.
- Added `skill_route_discovery_evidence_lane_matrix()` to render body-free source-by-source rows with candidate/source hashes, route profiles, bounded local lanes, selected digest item IDs, hashed evidence URLs, downgrade/rejection pressure, blocked action hashes, and runtime/external activation denials.
- Added focused harness assertions for the existing FableCodex fixture.
- Added a multi-profile candidate regression covering FableCodex, COMPASS Skills, and Three.js Game Skills with unsupported `runtime_execution`, `install`, and `execute` lane pressure downgraded into review instead of activation.
- Documented the `evidence_lane_matrix` contract in `docs/skill-route-discovery.md`.

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged: the current preference already supports rollback-backed local behavior improvements over validation-report-only work, and this run found no evidence that the self-model is blocking or mis-shaping this capability slice.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` passed: 9 passed, 89 deselected.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 passed, 6 deselected.
- `python -m pytest tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery` passed: 17 passed, 25 deselected.
- `python -m pytest tests/test_harness_eval.py -q -k "skill_route_discovery"` passed: 18 passed, 80 deselected.
- `python -m pytest tests/test_harness_eval.py tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 37 passed, 111 deselected.

## Review Notes

- The new matrix is metadata-only. It does not install, clone, execute, scaffold, launch providers, run external harnesses, export raw upstream bodies, or export raw source/evidence URLs.
- Summary classification continues to filter unsupported suggested lanes before proposal mapping. Explicit candidate-lane fixtures exercise downgrade pressure when upstream evidence names unsupported lane shapes.
- Activation remains supervisor-controlled and local-validation-gated.
