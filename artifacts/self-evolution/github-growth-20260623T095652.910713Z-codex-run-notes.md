# Codex Run Notes

- Source digest: `github-growth-20260623T095652.910713Z`
- Capability theme: `skill-route-discovery`
- Selected proposal: `p1-governance-approval-mock-llm-tests`
- Rollback ref: `refs/rollback/blackhole-agent/20260623T095652Z`
- Rollback artifact: `artifacts/rollback/20260623T095652Z-mock-llm-approval-governance-tests.md`

## Hypothesis

Repeated upstream approval-test activity is useful local evidence for making
mock LLM approval governance more deterministic. A missing approval path should
fail closed, while an explicit operator denial should be recognized as a
resolved approval path that does not execute the native tool.

## Change

- Updated embedded native-tool policy evaluation so explicit approval
  resolutions count as declared approval paths.
- Split approval path status into `review_only`, `resolved`, `missing`, and
  `not_required` for clearer operator inspection.
- Added focused mock LLM workflow regressions for missing approval and explicit
  denial paths without provider calls or raw private output export.
- Documented the denial contract in `docs/architecture.md`.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k "mock_llm_approval or approval_output_poll or native_policy_ask"`:
  passed, 3 tests.
- `python -m pytest tests/test_harness_eval.py -q`: passed, 139 tests.
- `python -m pytest tests/test_docs_contracts.py -q -k architecture`: passed,
  1 test.

## Review Notes

- Self-model left unchanged. It already supports rollback-backed local behavior
  improvements with a narrow safety boundary, and this run did not contradict
  that posture.
- No upstream code, install path, provider launch, external harness execution,
  remote execution, or raw private approval payload was adopted.
- Explicit approval denial is treated as a successful safety-preserving mock
  route, not as permission to execute the native tool.
