# Codex Run Notes

Source digest: `github-growth-20260623T091652.873843Z`

Capability window: `skill-route-discovery`, pass 3 of 4.

## Hypothesis

Pass-3 skill-route discovery already has bounded lanes, profile gates, proof
rows, and operator checkpoints. The next useful local improvement is a single
operator-visible promotion runbook that ties those surfaces together before the
final pass. This should reduce promotion ambiguity without adding any upstream
skill activation, provider launch, external harness execution, remote execution,
or raw evidence export path.

## Evidence Choice

The active window carried FableCodex, COMPASS Skills, zhengxi-views, and
Three.js Game Skills evidence. I did not re-run broad trend discovery. The
selected change advances the route-discovery slice because the existing pass-3
fixture already models FableCodex workflow gates, Three.js game/frontend lanes,
and COMPASS state handoff boundaries.

## Rollback

Rollback artifact:
`artifacts/rollback/20260623T091652Z-skill-route-discovery-pass3.txt`

Rollback ref:
`refs/rollback/20260623T091652Z-skill-route-discovery-pass3`

Original HEAD:
`55a693f9fff0622710819c36d58ba1fc05550de5`

## Changes

- Added `promotion_runbook` to the pass-3 skill-route handoff packet.
- Derived the runbook from `operator_checkpoint_list`,
  `profile_validation_proof`, and `activation_proof_summary`.
- Kept the runbook body-free: selected item IDs are counted, source and command
  data stay hashed where summarized, and raw source URLs, evidence URLs, target
  paths, and upstream bodies remain denied.
- Added ready-path and blocked-path test coverage.
- Documented the new pass-3 surface in `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k "skill_route_discovery_pass3"`
- `python -m pytest tests/test_skill_routing.py -q -k "skill_route_discovery_current_window_matrix or validation_profile_coverage"`
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py`

All validation passed.

## Self-Model

`docs/self-model.md` was read and left unchanged. It already matched this run's
policy: prefer rollback-backed local behavior changes while keeping external
skill activation and unsafe data exposure outside the discovery lane.

## Review Notes

The runbook is not an activation grant. It blocks when any proof surface is
blocked and repeats the existing denial flags for runtime action, external skill
activation, external harness execution, provider launch, remote execution, raw
source URL export, raw evidence URL export, raw target path export, and upstream
body export.
