# Self-Evolution Run Notes

Source digest: `github-growth-20260619T181207.018032Z`
Theme: `runner-harness-control`
Pass: `3 of 4`
Branch: `codex/blackhole-evolve/20260619T181333.809701-add-a-focused-local-regression-lane-for-sub-agen`
Rollback ref: `refs/blackhole-rollback/20260619T181207Z-runner-harness-control-pass3`
Rollback artifact: `artifacts/rollback/20260619T181207Z-runner-harness-control-pass3.txt`

## Evidence

- `https://github.com/omnigent-ai/omnigent`: public repository describes a multi-harness agent framework with runner, policy, sandbox, and session-management concerns.
- `https://github.com/omnigent-ai/omnigent/issues/848`: upstream issue reports child-session completions not reaching the orchestrator inbox, intermittent empty child turns, transcript-only recovery by history polling, missing parent wake, and degraded child lifecycle metadata/close handling.

No upstream code, scripts, prompts, repositories, private data, or external services were imported or executed.

## Hypothesis

The local `agent_workflow_route` lane already detects inbox delivery failures. For this runner-control pass, the higher-value improvement is making recovery and replay legible to operators: when completion delivery fails, the report should say whether transcript polling is a recovery-only path, whether child-session cleanup is required or blocked, and what action should be taken next without exposing transcript bodies or session IDs.

## Changes

- Extended `evaluate_agent_workflow_orchestrator_inbox` with body-free recovery classification.
- Surfaced inbox recovery state through `control_plane.inbox_delivery_contract`.
- Strengthened the orchestrator inbox regression assertions for missing delivery, duplicate delivery, empty turns, and degraded lifecycle.
- Updated `docs/architecture.md` to describe the recovery/replay fields.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference for rollback-backed, locally validated behavior changes already matched this run; changing it would have been ornamental.

## Validation

Passed:

- `python -m compileall -q src\blackhole_agent\harness_eval.py`
- `pytest tests/test_harness_eval.py -q -k agent_workflow_route_orchestrator_inbox` passed: 1 passed, 81 deselected.
- `pytest tests/test_harness_eval.py -q` passed: 82 passed.
- `pytest tests/test_docs_contracts.py -q` passed: 8 passed.

## Review Notes

- This is a local fixture/replay contract, not a claim of protocol compatibility with Omnigent.
- Transcript polling is explicitly marked as recovery-only, not normal supervision behavior.
- Raw message bodies, transcript bodies, session IDs, child turn IDs, and agent names remain omitted or hashed.
- Activation, restart, push, promotion, and remote execution remain supervisor-controlled.
