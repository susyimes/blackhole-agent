# Codex Run Notes

Source digest: github-growth-20260616T165522.796293Z

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent
- https://github.com/microsoft/fastcontext

Omnigent evidence supports explicit policy and tool governance around multi-agent harnesses. FastContext evidence supports compact, replayable context discipline rather than broad trend rediscovery.

## Hypothesis

Tool-routing decisions should be first-class, inspectable metadata before descriptors enter model-facing executable registries. This improves future runner/tool validation by distinguishing allowed local routes, review-only safety-boundary routes, and unsupported provider/tool paths.

## Rollback

- Artifact: `artifacts/rollback/20260616T165522Z-runner-tool-routing-validation.txt`
- Local ref: `refs/rollback/20260616T165522Z-runner-tool-routing-validation`

## Change

- Added `ToolRouteDecision` and deterministic route classification in `src/blackhole_agent/tool_routing.py`.
- Added `risk_flags` to `ToolDescriptor` so review-only safety metadata affects routing and compatibility cache keys.
- Filtered `executable_tool_registry` through routing decisions.
- Added focused routing tests for executable local memory, review-only high-risk descriptors, and unsupported provider/tool types.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. The existing preference already supports rollback-backed, locally validated evolution while keeping offensive behavior, abuse, unauthorized access, and privacy leakage review-only. This run produced a concrete behavior improvement, so a self-model edit would be ornamental.

## Validation

- `uv run pytest tests/test_tool_routing.py` passed: 11 tests.
- `uv run ruff check src/blackhole_agent/tool_routing.py tests/test_tool_routing.py` passed.

## Review Notes

- No offensive, abuse-enabling, unauthorized-access, or privacy-leaking behavior was implemented.
- Review-only routing is metadata-only; it withholds risky descriptors from executable registries.
- The external evidence was high-level repository evidence, not a specific upstream implementation detail, so the local change is intentionally framed as a reusable governance pattern.
