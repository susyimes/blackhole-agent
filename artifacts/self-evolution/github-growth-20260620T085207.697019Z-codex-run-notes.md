# Self-Evolution Run Notes

Source digest: `github-growth-20260620T085207.697019Z`
Capability window: `skill-route-discovery`, pass 2 of 4
Branch: `codex/blackhole-evolve/20260620T085315.421232-add-a-local-skill-route-discovery-validation-cas`

## Evidence Reviewed

- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/baskduf/FableCodex`
- `https://github.com/majidmanzarpour/threejs-game-skills`

The external evidence was treated as repository-level route evidence only. It
suggests useful skill/workflow profiles, but it also contains install,
profile/state, browser/game, and workflow-pack signals that must stay bounded to
local documentation, config, test, or code_patch lanes before activation.

## Hypothesis

The skill-route discovery lane already classifies profile families, but
operators still benefit from seeing how each profile maps to bounded local
artifact targets before supervisor handoff. A body-free profile lane contract
can make FableCodex, COMPASS, and Three.js route evidence actionable without
exporting raw target paths, upstream bodies, or enabling upstream skills.

## Change

- Added `local_lane_contracts` to `skill_route_discovery_route_profile_review`.
- Each profile contract now carries hashed local target paths, target counts,
  required local artifact proof fields, `runtime_action: none`, and external
  skill/code denial flags.
- Updated focused harness tests for FableCodex, Three.js, and COMPASS-style
  profile evidence.
- Updated `docs/skill-route-discovery.md` with the operator-facing behavior.

Self-model: left unchanged. The existing self-model preference for
rollback-backed local evolution matched this run and did not need a revision.

## Rollback

Rollback artifact: `artifacts/rollback/20260620T085206Z-skill-route-discovery-pass2.txt`
Rollback ref: `refs/rollback/20260620T085206Z-skill-route-discovery-pass2`

Recovery is explicit/destructive and should be run only by operator policy:

```bash
git switch codex/blackhole-evolve/20260620T085315.421232-add-a-local-skill-route-discovery-validation-cas
git reset --hard refs/rollback/20260620T085206Z-skill-route-discovery-pass2
```

## Validation

- `pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` -> 9 passed, 84 deselected
- `pytest tests/test_skill_routing.py -q` -> 22 passed
- `pytest tests/test_harness_eval.py -q` -> 93 passed
- `pytest tests/test_docs_contracts.py -q` -> 8 passed

## Review Notes

- No upstream code, installer, scaffold, browser checker, profile writer, or
  skill body was imported or executed.
- Profile review still denies runtime action, external skill activation,
  external skill code, raw source URL export, raw target path export, and raw
  upstream body export.
- The new contracts are metadata for local review and proof collection; they do
  not replace focused local validation or supervisor readiness checks.
