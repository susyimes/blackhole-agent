# Codex Run Notes

Source digest: `github-growth-20260620T183207.725541Z`
Capability window: `skill-route-discovery`, pass 3 of 4
Branch: `codex/blackhole-evolve/20260620T183311.212536-add-or-run-a-bounded-skill-route-discovery-valid`
Rollback artifact: `artifacts/rollback/20260620T183206Z-skill-route-discovery-pass3.txt`
Rollback ref: `refs/rollback/20260620T183206Z-skill-route-discovery-pass3`

## Evidence Boundary

Primary evidence came from the provided digest context and carried proposal URL:
`https://github.com/baskduf/FableCodex`. The external repository was treated as
body-free trend evidence for mixed Codex/skill/workflow routing. No upstream
code, package body, script, installer, scaffold, prompt, profile, memory store,
or runtime action was adopted.

## Hypothesis

Mixed Codex/skill/workflow evidence needs a stronger operator-visible replay
contract than a classifier label alone. The local route should continue to
choose `skill_route_discovery_first`, but the controller surface should expose
the secondary harness lane as blocked until local corroboration, name the
activation gate, recommend only already-bounded local lanes, and repeat the
denied action set.

## Change

- Added `activation_gate`, `secondary_lane_status`,
  `recommended_local_lane_order`, and `denied_actions` to
  `mixed_skill_workflow_probe` output and candidate rows.
- Kept allowed local lanes constrained to documentation, config, test, and
  code_patch. The recommended order is filtered from those lanes and does not
  add a new lane.
- Updated the focused FableCodex mixed-probe regression test.
- Updated `docs/skill-route-discovery.md` with the new gate contract.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference for
rollback-backed, locally validated evolution outside the narrow safety boundary
matches this pass; changing it would have been ornamental rather than
behavior-shaping.

## Validation

- `pytest tests/test_github_growth.py -q -k mixed_skill_workflow`: passed, 1 passed.
- `pytest tests/test_proposal_eval.py -q -k route_hint_lane_map`: passed, 1 passed.
- `pytest tests/test_github_growth.py tests/test_proposal_eval.py tests/test_skill_routing.py tests/test_harness_eval.py -q -k "skill_route_discovery or mixed_skill_workflow or route_hint_lane_map"`: passed, 50 passed.
- `pytest tests/test_docs_contracts.py -q`: passed, 8 passed.

## Review Notes

The mixed probe remains metadata-only. It denies install, enable, run, execute,
clone-and-run, profile-write, memory-write, provider launch, remote execution,
raw source URL export, and upstream body export. Secondary
`agent_harness_eval` remains available only after local corroboration or a
separate general-agent-project claim.
