# Self-Evolution Run Notes

Source digest: `github-growth-20260617T090315.716930Z`
Branch: `codex/blackhole-evolve/20260617T090429.642738-add-or-improve-local-validation-cases-for-agent-`
Rollback artifact: `artifacts/rollback/20260617T090315Z-agent-codex-trend-validation-fixture.txt`
Rollback ref: `refs/rollback/20260617T090315Z-agent-codex-trend-validation-fixture`

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`: reviewed narrowly as Codex workflow evidence. The reusable lesson is evidence-gated local workflow validation: inspect first, keep evidence, and verify before completion.
- `https://github.com/omnigent-ai/omnigent`: reviewed narrowly as agent harness and policy-routing evidence. The reusable lesson is to keep agent workflow routing and policy checks explicit and locally testable.

No broad trend rediscovery was run.

## Hypothesis

External agent/Codex workflow trends should become local validation candidates only when the interpreter cites supplied `item_id` values and controller-owned replay checks prove the route remains local, reviewable, and validation-backed. URL refs or non-item identifiers should be rejected by fixtures before they can influence autonomous proposal routing.

## Change

- Added a proposal replay fixture for FableCodex plus Omnigent evidence that accepts one local-validation candidate and rejects two malformed candidates:
  - one citing an external URL as `evidence_refs`
  - one citing a repository label that is not a supplied `item_id`
- Added a harness-adapter fixture for the same evidence shape so local eval reports cover the proposal interpretation route without exporting fixture inputs.
- Updated manifest and tests to include the new case and aggregate counts.

`docs/self-model.md` was left unchanged. Its current preference already matches this run's evidence: apply rollback-backed local validation improvements when safe, while keeping privacy/token/provider routes review-only unless separately validated without leaking secrets.

## Validation

- Initial targeted run found one stale manifest case-count assertion.
- After fixing the assertion:

```text
uv run pytest tests/test_proposal_eval.py tests/test_harness_eval.py -q
18 passed in 0.38s
```

## Review Notes

- Provider/config/token preflight behavior stayed review-only for this run because the supplied proposal marks it non-autonomous under a privacy-leakage human-review gate.
- This change does not add runtime execution, remote access, token handling, or new external URLs outside the supplied proposal evidence.
