# Codex Run Notes

Source digest: `github-growth-20260618T215207.204133Z`
Selected proposal: `p2-skill-route-discovery-docs`

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`

The reviewed repositories describe reusable skill/workflow ecosystems: Codex
workflow gates, local task/profile skills, and a Three.js domain director with
specialist skills and verification helpers. The reusable lesson is a bounded
route contract, not upstream skill activation.

## Hypothesis

`skill_route_discovery` is safer and more useful when current wake evidence is
documented as classification-only and pinned by a focused local docs check. The
controller should continue to map this route only into documentation, config,
test, or code_patch lanes, and proposal candidates should cite selected
`item_id` values rather than repository URLs.

## Changes

- Updated `docs/skill-route-discovery.md` with the current source digest,
  reviewed evidence interpretation, and an explicit replay-check contract for
  lane and `item_id` citation discipline.
- Updated `tests/test_docs_contracts.py` so the documentation contract now
  verifies the current digest, no-permission boundary, allowed-lane statement,
  and item-id-only proposal citation rule.

No self-model change was made. The existing self-model already matched this
run's safe autonomous lane: try locally validated improvements while keeping
offensive behavior, abuse, unauthorized access, and privacy leakage review-only.

## Rollback

- Artifact: `artifacts/rollback/20260618T215503Z-skill-route-item-id-citation.txt`
- Ref: `refs/rollback/20260618T215503Z-skill-route-item-id-citation`

## Validation

- `uv run pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
  - Passed: 1 passed, 5 deselected.
- `uv run pytest tests/test_proposal_eval.py -q -k skill_route_discovery`
  - Passed: 1 passed, 13 deselected.
- `uv run pytest tests/test_skill_routing.py -q -k skill_route_discovery`
  - Passed: 12 passed, 9 deselected.

## Review Notes

- Proposal `p1-agent-harness-eval-lane` stayed review-only because the digest
  gate is `offensive-behavior-human-review`.
- Proposal `p3-debug-log-redaction-preflight` stayed review-only because the
  digest gate is `privacy-leakage-human-review`.
- No upstream code, install scripts, prompts, skills, scaffolds, profile stores,
  browser checks, generated assets, credentials, or runtime execution behavior
  were adopted.
