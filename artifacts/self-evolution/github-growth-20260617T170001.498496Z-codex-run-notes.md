# Codex Run Notes

Source digest: github-growth-20260617T170001.498496Z

Selected proposal: p1-agent-harness-eval-local-smoke

Hypothesis:

Agent-harness trend evidence is most useful locally as deterministic replay validation for proposal interpretation. The Visa harness evidence emphasizes structured agentic evaluation, deterministic controls, and reviewable artifacts, but its vulnerability-scanning behavior is security-adjacent and was not copied.

Evidence reviewed:

- https://github.com/visa/visa-vulnerability-agentic-harness

Changes made:

- Extended proposal route-hint lane validation so safe `agent_harness_eval` evidence can only produce `documentation`, `test`, or `code_patch` candidates.
- Preserved high-risk evidence routing for the separate review-only safety boundary.
- Added a frozen-evidence smoke test covering strict schema version 1 review output, selected `item_id` evidence refs, derived evidence URLs, `max_proposals`, and bad-lane rejection for `agent_harness_eval`.

Self-model decision:

Left unchanged. The current self-model already supports rollback-backed local validation over report-only work and did not need new structure for this run.

Rollback:

- Artifact: artifacts/rollback/20260618T010138Z-agent-harness-proposal-smoke.txt
- Ref: refs/rollback/20260618T010138Z-agent-harness-proposal-smoke

Validation:

- `python -m pytest tests\test_proposal_eval.py` passed, 13 tests.
- `python -m pytest` passed, 190 tests.

Review notes:

- Provider-config route behavior was deliberately left unchanged after validation showed that enforcing all route hints would reject an existing covered provider preflight `code_patch` lane.
- No offensive behavior, abuse, unauthorized access, or privacy-leakage behavior was implemented.
