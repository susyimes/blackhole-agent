# Self-Evolution Run Notes

Source digest: `github-growth-20260627T130311.159432Z`
Branch: `codex/blackhole-evolve/20260627T130417.540245-add-or-extend-a-local-skill-route-discovery-vali`
Theme: `skill-route-discovery`
Capability slice: Convert skill and route evidence into bounded local lanes that can be validated before activation.

## Rollback

- Rollback ref: `refs/rollback/20260627T130311Z-skill-route-discovery-pass1`
- Rollback artifact: `artifacts/rollback/20260627T130311Z-skill-route-discovery-pass1.txt`
- Recovery commands are recorded in the rollback artifact and require explicit operator choice before destructive reset or clean commands.

## Evidence And Hypothesis

The active evidence window carried zhengxi-views, Three.js Game Skills, and COMPASS Skills as route-shape examples. The reusable lesson is that frozen digest items may already contain bounded route classification metadata, and the local registry should preserve those route profiles instead of relying only on keyword inference.

Hypothesis: allowing explicit `route_classification.route_profiles` on evidence items to enter the disabled skill-route registry will make current-window proposal lanes more replayable while preserving the existing safety boundary: only documentation, config, test, and code_patch lanes; no runtime action; no external skill activation; no raw evidence URL export.

## Changes

- Preserved explicit evidence-item `route_profiles` and accepted explicit route metadata as a classification signal in `build_skill_route_discovery_registry_from_evidence_items`.
- Added `skill_route_discovery_lane_20260627_pass1_window.json` as a frozen local harness fixture for the current proposal IDs.
- Added regression coverage for the selected pass-1 game frontend test lane, queued generic documentation lane, queued COMPASS state-handoff config lane, and no-permission boundaries.
- Documented the 2026-06-27T13:03Z pass-1 handling in `docs/skill-route-discovery.md`.
- Left `docs/self-model.md` unchanged because the existing self-model already says local evolution should be rollback-backed, validated, and explicit about uncertainty; this run did not add new self-model evidence.

## Validation

Commands were run with `PYTHONPATH=src` so Python imported this worktree:

- `pytest tests/test_harness_eval.py -q -k "skill_route_discovery_20260627_pass1_window or local_harness_eval_runs_pass_and_fail_fixtures"`: passed, 2 passed.
- `pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`: passed, 9 passed.
- `pytest tests/test_skill_routing.py -q -k skill_route_discovery`: passed, 35 passed.
- `pytest tests/test_docs_contracts.py -q -k skill_route`: passed, 2 passed.

## Review Notes

- The normal Python environment in this workspace imports `blackhole_agent` from `C:\Users\svmes\Documents\Playground\blackhole-agent` unless `PYTHONPATH=src` is set. Validation for this run explicitly set `PYTHONPATH=src`.
- The pass-1 validation queue remains blocked for anchoring IDs that are present as window context but not represented as replayable evidence rows. The selected and queued skill-route profiles are still replayable through the pass-1 replay lane plan, with runtime and external activation denied.
