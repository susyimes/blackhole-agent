# Codex Run Notes

Digest: github-growth-20260618T223207.205993Z

## Selected Proposal

Selected `p3-runtime-process-tree-robustness`.

Evidence reviewed:
- https://github.com/omnigent-ai/omnigent
- https://github.com/visa/visa-vulnerability-agentic-harness

Hypothesis:
Omnigent-style supervised agent runtimes make process cleanup and terminal state
legibility important. This repository already had runner liveness tests and
stderr task-group cleanup tests, but lacked a local regression lane for orphaned
CLI process trees. A small, process-like cleanup helper plus heartbeat
classification coverage improves recovery without enabling offensive workflows.

Safety decision:
- `p1-agent-harness-local-eval` remains review-only because the evidence is a
  vulnerability harness and the proposal explicitly gates offensive behavior.
- No exploit, attack, malware, phishing, exfiltration, or unauthorized-access
  behavior was implemented.

Self-model:
Left unchanged. The current self-model already says local evolution may include
runtime behavior and tests when rollback-backed and validated; this run did not
produce new evidence that the self-model is behavior-shaping rather than a
preference note.

## Changes

- Added `cleanup_orphaned_process_tree` and `ProcessTreeCleanupResult` to
  `src/blackhole_agent/kernels/codex_cli.py`.
- Treated `terminating`, `stopping`, `cancelling`, `canceling`, and
  `shutting_down` child sessions as active in supervisor runner liveness.
- Added deterministic fake-process cleanup tests in `tests/test_codex_cli.py`.
- Added a supervisor heartbeat regression for terminating child sessions in
  `tests/test_supervisor.py`.
- Created rollback artifact
  `artifacts/rollback/20260618T223207Z-local-runner-cleanup.txt` and local ref
  `refs/blackhole-rollback/20260618T223207Z-local-runner-cleanup`.

## Validation

- `uv run pytest tests/test_codex_cli.py tests/test_supervisor.py`:
  48 passed.
- `uv run ruff check .`:
  all checks passed.
- `uv run pytest`:
  252 passed.

## Review Notes

- The cleanup helper is not yet wired into a concrete live transport path. It is
  a locally validated defensive primitive ready for a future transport-specific
  integration.
- Tests simulate process objects and do not spawn, terminate, or kill real
  processes.
