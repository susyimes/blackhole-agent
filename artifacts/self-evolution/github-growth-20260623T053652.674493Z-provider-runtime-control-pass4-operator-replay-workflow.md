# Provider Runtime Control Pass 4 Operator Replay Workflow

Source digest: `github-growth-20260623T053652.674493Z`

Rollback point: `refs/rollback/20260623T053651Z-skill-route-final-pass`

## Evidence

- `https://github.com/baskduf/FableCodex` was reviewed as a workflow-gated Codex skill repository with local evidence, tests, examples, and verification habits.
- `https://github.com/omnigent-ai/omnigent/issues/902` and `https://github.com/omnigent-ai/omnigent/pull/990` were reviewed as provider/runtime configuration evidence: wire API routing and backward-compatible runner/server configuration need diagnostics and recovery before launch.

## Hypothesis

The pass-4 `provider-runtime-control` window already emitted provider-runtime final diagnostics, but operators still had to inspect nested packets to reconstruct the replay order. A flat body-free workflow embedded in final diagnostics should make the supervisor handoff more replayable without increasing runtime authority.

## Change

- Added `provider_runtime_operator_replay_workflow` inside final provider-runtime diagnostics.
- The workflow records sample gate, recovery summary, diagnostic panel, and completion handoff steps with readiness, status, decision, recovery-code hashes, and local replay commands.
- The workflow repeats denials for runtime action, upstream skill activation, external harness execution, provider launch, remote execution, and raw evidence/provider body export.
- Updated focused tests for both missing-sample recovery and ready pass-4 handoff.
- Documented the new operator-visible surface in `docs/skill-route-discovery.md`.

## Validation

- `pytest tests/test_harness_eval.py -q -k "provider_runtime_control_pass_requires_replay_sample or provider_runtime_control_pass4_surfaces_completion_handoff"`
- `pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`
- `pytest tests/test_harness_eval.py -q -k provider_runtime_control`
- `pytest tests/test_skill_routing.py -q`
- `pytest tests/test_docs_contracts.py -q`

All listed commands passed.

## Review Notes

The self-model was read and left unchanged. Its current preference already supports rollback-backed, locally validated behavior changes, and this run produced a direct harness improvement rather than new self-description evidence.
