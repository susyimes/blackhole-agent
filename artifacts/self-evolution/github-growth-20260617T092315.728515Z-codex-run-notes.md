# Codex Run Notes

Source digest: `github-growth-20260617T092315.728515Z`
Proposal selected: `p1_mock_llm_integration_test_route`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/pull/481`

Omnigent is a public meta-harness for multiple AI agents, and PR #481 is titled "feat: migrate integration tests to mock LLM server". The reusable local lesson is to keep provider-dependent workflow validation executable without live model calls.

## Hypothesis

The existing local harness fixture evaluator can gain a deterministic mock LLM workflow route. That should let future proposal and agent workflow tests exercise model-dependent paths while keeping provider credentials absent and raw prompt/response bodies out of exported reports.

## Changes

- Added `mock_llm_workflow_route` to `src/blackhole_agent/harness_eval.py`.
- Added a provider-disabled mock LLM fixture at `tests/fixtures/local_harness_eval/mock_llm_workflow_route_provider_disabled.json`.
- Added focused tests that prove the provider-disabled route passes with mock responses and fails rather than attempting an external provider route.
- Created rollback artifact `artifacts/rollback/20260617T172518Z-mock-llm-integration-route.txt`.
- Left `docs/self-model.md` unchanged because its current preference for locally validated, rollback-backed behavior changes matched this run.

## Validation

- `python -m pytest tests/test_harness_eval.py -q` -> 8 passed
- `python -m pytest -q` -> 159 passed
- `python -m ruff check src tests` -> All checks passed

## Review Notes

- Mock response content is hashed in output metadata; raw prompt and response bodies stay inside the local fixture input and are not exported by the harness report.
- No restart, push, promotion, or remote execution was performed by the kernel.
