# Codex Run Notes

Source digest: `github-growth-20260616T085703.992160Z`

Rollback point:

- Branch: `codex/blackhole-evolve/20260616T085808.064689-add-or-extend-a-local-benchmark-style-harness-th`
- Original HEAD: `c63513b91cce1bd20f229406610dc2199535f274`
- Rollback ref: `refs/blackhole-agent/rollback/20260616T085703Z/c63513b91cce`
- Artifact: `artifacts/rollback/20260616T085703Z-local-proposal-eval-harness.txt`

Evidence reviewed:

- `https://github.com/ApodexAI/AgentHarness`
- `https://github.com/visa/visa-vulnerability-agentic-harness`
- `https://github.com/microsoft/fastcontext`

Hypothesis:

Public agent harness projects emphasize replayable, fixture-backed evaluation. Blackhole-agent already had proposal
shape and context-budget checks as individual tests; adding a reusable local replay harness makes future proposal
interpretation changes easier to validate against frozen benign and security-adjacent evidence packages.

Change:

- Added `blackhole_agent.proposal_eval`, a replay harness that builds proposal evidence packages, reviews frozen
  proposal JSON, verifies selected-item-only evidence refs, checks proposal count limits, and compares expected
  safety-boundary scope/gate classification.
- Added frozen replay fixtures for benign agent-harness evidence and security-adjacent harness evidence.
- Added pytest coverage for the replay suite, URL refs being rejected even when the URL is allowed evidence, and
  detection of safety control drift.

Self-model decision:

- Left `docs/self-model.md` unchanged. Its current preference already supports rollback-backed, locally validated
  evolution and did not need a run-specific rewrite.

Validation:

- `uv run --extra dev python -m pytest tests/test_proposal_eval.py -q` -> passed, 3 tests.
- `uv run --extra dev python -m pytest tests/test_github_growth.py -q -k "llm_proposal_review or proposal_evidence_package or pr_event_digest_pressure or context_budget_preflight or llm_proposals_preserve"` -> passed, 12 selected tests.
- `uv run --extra dev python -m ruff check src/blackhole_agent/proposal_eval.py tests/test_proposal_eval.py` -> passed.

Review notes:

- Provider/config/token preflight remains review-only for this run because it is privacy-sensitive and was not the
  selected local behavior change.
- The replay fixtures are synthetic frozen packages derived from this run's proposal themes; they validate controller
  invariants, not external model quality.
