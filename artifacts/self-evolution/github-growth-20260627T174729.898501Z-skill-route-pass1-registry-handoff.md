# Codex Run Notes: skill-route-discovery pass1 registry handoff

Source digest: `github-growth-20260627T174729.898501Z`

Branch: `codex/blackhole-evolve/20260627T174818.942636-add-or-extend-local-tests-for-skill-route-discov`

Rollback artifact: `artifacts/rollback/20260627T174729Z-skill-route-discovery-pass1.md`

Rollback ref: `refs/blackhole-rollback/20260627T174729Z-skill-route-discovery-pass1`

## Evidence

- `https://github.com/dongshuyan/compass-skills`: skill ecosystem with local task memory, handoff prompts, collaboration profile, and privacy boundary language.
- `https://github.com/lyra81604/zhengxi-views`: source-cited domain research skill evidence.
- `https://github.com/majidmanzarpour/threejs-game-skills`: Three.js/browser-game skill workflow evidence.
- `https://github.com/QwenLM/Qwen-AgentWorld`: general-agent project evidence, routed to agent-harness evaluation rather than skill-route lanes.

## Hypothesis

Pass-1 skill-route windows are easier to operate if the local harness exposes one body-free registry handoff that joins selected skill-route profiles with adjacent general-agent evidence before activation. Qwen-AgentWorld-style general-agent evidence should be visible as `agent_harness_eval_required` and must not inherit `skill_route_discovery` lanes.

## Changes

- Added `pass1_route_registry_handoff` to `evaluate_skill_route_discovery_lane`.
- Reused the existing general-agent reference classifier so pass-1 handoff and validation queue recognize Qwen-AgentWorld-style references, not only Omnigent/agent-harness strings.
- Added a regression that injects Qwen-AgentWorld into the active 20260627 pass-1 skill-route fixture and asserts it stays in adjacent agent-harness eval with no runtime action.
- Documented the new operator-visible pass-1 registry handoff.

Self-model: unchanged. The existing preference for rollback-backed local behavior matched this run.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k pass1_registry_handoff`: passed, 1 test.
- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`: passed, 9 tests.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`: passed, 2 tests.
- `python -m pytest tests/test_skill_routing.py -q -k skill_route_discovery`: passed, 37 tests.

## Review Notes

- The new handoff exports selected item IDs and source hashes only; raw GitHub URLs and upstream bodies remain omitted from harness output.
- No external skill code, general-agent harness, provider runtime, remote execution, memory write, or profile write is activated.
- The adjacent general-agent row is a validation handoff only; it names `agent_harness_eval_lane` replay and keeps `skill_route_discovery_inherited: false`.
