# Codex Run Notes

Source digest: `github-growth-20260616T171522.631356Z`

Evidence reviewed:
- `https://github.com/omnigent-ai/omnigent`

Hypothesis:
- Omnigent's multi-agent harness posture depends on explicit provider/key/runtime setup before agents are launched. blackhole-agent should fail before scheduling child wake work when supervisor provider token configuration is malformed, or when an operator explicitly requires a token env var and it is absent.

Rollback:
- Local ref: `refs/blackhole-agent/rollback/20260616T171522Z/d9c982c2a56c`
- Artifact: `artifacts/rollback/20260616T171522Z-provider-config-token-preflight.txt`

Changes:
- Added metadata-only supervisor provider/token preflight.
- Added `--require-token-env/--allow-missing-token-env` for strict scheduled runners.
- Added tests for missing required token env, token value non-disclosure, and malformed env var failure before scheduling.
- Documented the new startup preflight behavior.

Self-model decision:
- Left `docs/self-model.md` unchanged. It already names provider/config preflight as an allowed local evolution route, and this run did not produce evidence that the self-description is blocking behavior.

Validation:
- `uv run pytest tests/test_supervisor.py` passed.
- Missing-token dry run produced: `provider/config/token preflight failed: required token environment variable is not set or empty: BLACKHOLE_MISSING_TOKEN_FOR_PREFLIGHT`.
- `uv run pytest` passed.
- `uv run ruff check .` passed.

Review notes:
- Missing `GITHUB_TOKEN` remains allowed by default so read-only public GitHub discovery can still run unauthenticated.
- Strict token enforcement is opt-in with `--require-token-env`.
- Diagnostics record env var names and boolean presence only; token values are not recorded.
