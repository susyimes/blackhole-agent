# Codex Run Notes

Digest: github-growth-20260616T070601.401617Z
Proposal: p2-context-budget-benchmark

## Evidence Reviewed

- https://github.com/microsoft/fastcontext

Reusable lesson: fast context systems keep broad exploration outside the main solver context and return compact, citation-like evidence. For blackhole-agent, the local analogue is deterministic evidence selection with replayable item references under context pressure.

## Hypothesis

Trend evidence packages should preserve unambiguous selected item references even when upstream digest items reuse the same `item_id`. Without this, proposal review can collapse duplicate evidence refs and point to the wrong source URL.

## Changes

- Added deterministic duplicate `item_id` disambiguation in proposal evidence packages.
- Added a regression test with synthetic duplicate trend items, confidence ordering, privacy risk flags, truncation limits, stable selected/truncated ids, diagnostics, and proposal review evidence URL derivation.

## Self Model

Read `docs/self-model.md` before choosing the change. It was left unchanged because the current preference for reversible local implementation over report-only work matched this run's evidence and patch.

## Rollback

- Artifact: `artifacts/rollback/20260616T070601.401617Z-context-budget-benchmark.txt`
- Ref: `refs/rollback/20260616T070601.401617Z`
- Original branch: `codex/blackhole-evolve/20260616T070704.739375-introduce-a-local-benchmark-or-test-around-evide`
- Original HEAD: `9ad8033957afc2d279b91f736ae5eefdbd5dde8c`

## Validation

- `uv run pytest tests/test_github_growth.py -q`
  - Result: failed before forcing candidate source import because the existing virtual environment used a cached `blackhole-agent` wheel.
- `$env:PYTHONPATH='src'; uv run pytest tests/test_github_growth.py -q`
  - Result: passed, 58 tests.
- `$env:PYTHONPATH='src'; uv run pytest -q`
  - Result: passed, 71 tests.

## Review Notes

- No offensive behavior, abuse, unauthorized access, or privacy leakage route was implemented.
- The duplicate-id behavior only changes evidence package ids when duplicate source ids would otherwise be ambiguous.
- The stale-wheel behavior is an environment validation note, not a repository code change in this run.
