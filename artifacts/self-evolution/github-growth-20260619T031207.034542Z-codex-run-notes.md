# Codex Run Notes

Source digest: `github-growth-20260619T031207.034542Z`

Selected proposal: `p1-e2e-runner-workspace-changed-files`

Evidence URL: `https://github.com/omnigent-ai/omnigent`

Rollback point: `artifacts/rollback/20260619T031207Z-workspace-changed-files-e2e-validation.txt`

## Hypothesis

Omnigent presents itself as a meta-harness for coordinating coding agents, policy, sandboxing, shared sessions, and file state. The local reusable lesson is that e2e runner changed-file evidence should not merely prove that some workspace panel entry is visible; it should reject stale entries and overbroad entries while preserving privacy by exporting hashes instead of raw paths or file bodies.

## Self-Model

Read `docs/self-model.md` before choosing the patch. It already expresses the current run preference: favor locally validated behavior improvements when rollback-backed and outside the safety boundary. No edit was needed; this run produced a direct harness validation improvement instead of a self-model-only change.

## Changes

- Tightened `workspace_changes_panel` evaluation so visible changed-file entries must map to performed edits and path evidence for matched edits must agree by hash.
- Added distinct failure modes for `stale_visible_change_entries` and `unexpected_visible_change_entries`.
- Added focused regression tests for stale path evidence and overbroad changed-file evidence without exporting private paths or contents.
- Documented the workspace changes-panel contract in `docs/architecture.md`.

## Validation

- Baseline before patch: `uv run pytest tests/test_harness_eval.py -q -k workspace_changes_panel` passed with 1 existing test, confirming the gap was uncovered rather than already failing.
- Focused after patch: `uv run pytest tests/test_harness_eval.py -q -k workspace_changes_panel` passed with 3 tests.
- Harness suite: `uv run pytest tests/test_harness_eval.py -q` passed with 48 tests.
- Full suite: `uv run pytest -q` passed with 277 tests.
- Lint: `uv run ruff check .` passed.

## Review Notes

No remote mutation, restart, push, or provider execution was performed. The only external evidence reviewed was the proposal URL. The change remains local to deterministic harness evaluation, tests, docs, rollback, and run artifacts.
