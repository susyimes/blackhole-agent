# Codex Run Notes

Source digest: `github-growth-20260618T102044.179836Z`

Branch: `codex/blackhole-evolve/20260618T102156.503367-add-or-extend-a-local-regression-test-lane-for-c`

Rollback artifact: `artifacts/rollback/20260618T102044Z-codex-streaming-regression-lane.txt`

Rollback ref: `refs/blackhole-rollback/20260618T102044Z-codex-streaming-regression-lane`

## Evidence

- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/issues/654`

Reviewed issue #654 only far enough to extract the reusable local lesson. The
issue reports that Codex streaming in an agent harness can hang before
completion, leave a final sentence buffered, and concatenate a later paragraph
directly onto an earlier paragraph.

## Hypothesis

Codex streaming changes should have a token-free local regression lane before
runtime integration. A deterministic chunk fixture can catch the reported
failure shape without provider credentials, network calls, or a live UI.

## Changes

- Added deterministic Codex stream chunk and assembly helpers in
  `src/blackhole_agent/kernels/codex_cli.py`.
- Added focused tests in `tests/test_codex_cli.py` for delayed chunks,
  paragraph boundaries split across chunks, completion-time final flushing,
  idle stalls before completion, and missing completion markers.
- Left `docs/self-model.md` unchanged because it already describes the
  rollback-backed local validation preference used in this run.

## Validation

- `uv run pytest tests/test_codex_cli.py -q` passed: 19 tests.
- `uv run ruff check src/blackhole_agent/kernels/codex_cli.py tests/test_codex_cli.py` passed.

## Review Notes

- This repository currently shells out to `codex exec` and does not expose a
  live Codex streaming UI path, so the change adds a reusable local regression
  lane rather than patching runtime provider streaming.
- No upstream code, prompts, or runtime behavior were imported.
