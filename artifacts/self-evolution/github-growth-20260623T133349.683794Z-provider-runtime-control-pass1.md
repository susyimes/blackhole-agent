# Provider Runtime Control Pass 1

## Evidence

- Source digest: `github-growth-20260623T133349.683794Z`
- Upstream anchors reviewed:
  - `https://github.com/omnigent-ai/omnigent/issues/1026`
  - `https://github.com/omnigent-ai/omnigent/issues/1023`
  - `https://github.com/omnigent-ai/omnigent/issues/1022`

## Hypothesis

Claude SDK startup failures that surface as Errno 8 should be classified before provider launch with body-free diagnostics, recovery hints, and a locally replayable validation lane. This deepens the provider-runtime-control slice without launching upstream harnesses or exporting executable paths, provider config bodies, stderr, proxy URLs, credentials, or secrets.

## Change

- Added `startup_preflight` classification to `provider_runtime_preflight`.
- Added recovery hints for `provider_startup_*` failure classes.
- Added a Claude SDK Errno 8 fixture and focused assertions.
- Documented the startup preflight contract in `docs/architecture.md`.

## Validation

- `pytest tests/test_harness_eval.py -q -k provider_runtime_preflight` passed: 26 passed, 116 deselected.
- `pytest tests/test_harness_eval.py -q` passed: 142 passed.
- `pytest tests/test_docs_contracts.py -q` passed: 9 passed.

## Review Notes

- Proposal `p1-agent-harness-turn-context-recovery` remains useful for a later pass, but the current provider/runtime theme had a direct preflight behavior path already present in the repository.
- Proposal `p3-proxy-env-allowlist-review` remains review-only because proxy URLs can carry credentials. This run did not log, print, upload, or export proxy values.
- Self-model was read and left unchanged because it already treats provider/runtime preflight improvements as in-bounds and does not appear to be behavior-shaping beyond that preference.
