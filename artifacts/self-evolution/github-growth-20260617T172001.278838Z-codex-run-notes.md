# Codex Run Notes

Source digest: `github-growth-20260617T172001.278838Z`

## Evidence

- Reviewed `https://github.com/omnigent-ai/omnigent/issues/542`.
- Reusable lesson: provider subprocess shutdown should tolerate SDK transport shape drift. Missing or already-cleared
  stderr task-group state must be treated as an idempotent cleanup outcome, not as a startup/shutdown-failing
  `AttributeError`.

## Hypothesis

Adding a locally testable shutdown helper for subprocess CLI transport stderr task-group cleanup improves the Codex
kernel's provider-runner resilience and gives future provider adapters a covered compatibility boundary.

## Rollback

- Rollback artifact: `artifacts/rollback/20260617T172001Z-provider-runner-shutdown-idempotence.txt`
- Rollback ref: `refs/blackhole-rollback/20260617T172001-provider-runner-shutdown-idempotence`
- Original HEAD: `4a60021981bf2a057eae864c42f656854eb0c5a8`

## Changes

- Added `shutdown_subprocess_cli_transport_stderr` to `src/blackhole_agent/kernels/codex_cli.py`.
- Added regression tests in `tests/test_codex_cli.py` for absent `_stderr_task_group` state and repeated shutdown after
  cancel-scope cleanup.
- Left `docs/self-model.md` unchanged. It already describes the run's preference for rollback-backed, locally validated
  behavior changes and did not need new structure for this evidence.

## Review Notes

- Proposal `p1-provider-config-preflight-mock-llm` remained review-only because its validation gate is
  `privacy-leakage-human-review`.
- Proposal `p3-agent-harness-eval-fixture` remained review-only because its validation gate is
  `offensive-behavior-human-review`.
- This run did not expose, log, print, upload, publish, or share tokens, credentials, private keys, private chats, PII,
  or personal data.
- This run did not implement offensive, exploit, malware, phishing, exfiltration, or unauthorized-access behavior.

## Validation

- `python -m pytest tests/test_codex_cli.py` passed: 11 tests.
- `python -m ruff check src\blackhole_agent\kernels\codex_cli.py tests\test_codex_cli.py` passed.
- `python -m pytest` passed: 192 tests.
