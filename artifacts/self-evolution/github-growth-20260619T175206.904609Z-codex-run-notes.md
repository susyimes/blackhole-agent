# Self-Evolution Run Notes

Source digest: `github-growth-20260619T175206.904609Z`
Theme: `runner-harness-control`
Branch: `codex/blackhole-evolve/20260619T175409.454118-add-or-strengthen-a-local-regression-test-for-su`
Rollback ref: `refs/blackhole-rollback/20260619T175206Z-runner-harness-control-inbox-delivery`
Rollback artifact: `artifacts/rollback/20260619T175206Z-runner-harness-control-inbox-delivery.txt`

## Evidence

- `https://github.com/omnigent-ai/omnigent`: public repository describes a multi-harness agent framework with runner, policy, sandbox, and session management concerns.
- `https://github.com/omnigent-ai/omnigent/issues/848`: upstream issue reports child-session completion not reaching the parent orchestrator inbox, intermittent empty turns, missing parent wake, transcript-only recovery, and degraded child lifecycle metadata.

No upstream code, scripts, prompts, repositories, or private data were imported or executed.

## Hypothesis

The local `agent_workflow_route` lane should make sub-agent completion delivery legible end to end. A body-free replay contract can catch missing or duplicate parent-inbox completion, empty child turns, transcript-only recovery, missing parent wake, and degraded child lifecycle metadata before those problems become live runner-control regressions.

## Changes

- Added `orchestrator_inbox` evaluation to `src/blackhole_agent/harness_eval.py`.
- Added control-plane summary fields under `control_plane.inbox_delivery_contract`.
- Added a replay fixture for exactly-one completion delivery in `tests/fixtures/local_harness_eval/agent_workflow_route_orchestrator_inbox_delivery.json`.
- Added focused tests for success, missing delivery, duplicate delivery, empty turns, and degraded lifecycle.
- Documented the inbox-delivery replay contract in `docs/architecture.md`.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference for rollback-backed, locally validated behavior changes already matched this run; changing it would have been ornamental.

## Validation

Passed:

- `python -m compileall -q src\blackhole_agent\harness_eval.py`
- `pytest tests/test_harness_eval.py -q -k agent_workflow_route_orchestrator_inbox` passed: 1 passed, 81 deselected.
- `pytest tests/test_harness_eval.py -q -k "agent_workflow_route_control_plane or agent_workflow_route_orchestrator_inbox"` passed: 2 passed, 80 deselected.
- `pytest tests/test_harness_eval.py -q` passed: 82 passed.

## Review Notes

- The new lane is fixture-driven local validation, not a claim of wire-protocol compatibility with Omnigent.
- Raw message bodies, transcript bodies, child session IDs, message IDs, turn IDs, and agent names are not exported.
- Activation, restart, push, and remote execution remain supervisor-controlled.
