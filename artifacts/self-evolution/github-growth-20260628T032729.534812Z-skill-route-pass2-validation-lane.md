# Skill Route Discovery Pass 2 Validation Lane

- Source digest: `github-growth-20260628T032729.534812Z`
- Branch: `codex/blackhole-evolve/20260628T032821.225416-add-or-run-a-local-skill-route-discovery-validat`
- Rollback ref: `refs/rollback/blackhole-agent/20260628T032728Z-skill-route-discovery-pass2`
- Rollback artifact: `artifacts/rollback/20260628T032728Z-skill-route-discovery-pass2.md`

## Hypothesis

The active pass-2 proposal set needs an operator-visible local validation lane,
not another standalone fixture. Generic skill evidence should remain bounded to
documentation, config, test, or code_patch. Three.js/game frontend skill
evidence should first map to documentation or validation lanes. General-agent
project evidence without skill workflow hints should stay adjacent under
`agent_harness_eval_required`.

## Change

Added `active_window_pass2_validation_lane` to the skill-route proposal lane
map. The lane binds:

- `p1-skill-route-discovery-general` to a local test lane.
- `p3-game-skill-workflow-docs` to a local documentation lane.
- `p2-agent-harness-eval` to adjacent `agent_harness_eval_required`.

The lane exports hashes and selected item IDs, denies runtime and external
activation, and keeps raw source URLs, evidence URLs, target paths, and upstream
bodies out of the handoff.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k active_window_pass2_validation_lane`
- `python -m pytest tests/test_skill_routing.py -q`

Both commands passed.

## Review Notes

No self-model edit was made. The existing self-model already favors locally
validated behavior changes over validation-report-only work, so changing it in
this run would be ornamental.
