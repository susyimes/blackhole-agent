# Codex Run Notes

Digest: github-growth-20260617T132001.426764Z
Branch: codex/blackhole-evolve/20260617T132149.709695-add-or-extend-local-mock-llm-smoke-coverage-for-
Rollback ref: refs/rollback/20260617T132001Z-mock-llm-session-file-smoke
Rollback artifact: artifacts/rollback/20260617T132001Z-mock-llm-session-file-smoke.txt

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent/pull/510
- https://github.com/omnigent-ai/omnigent/issues/503

PR #510 showed a live e2e smoke migration to mock LLM mode for session and file-tool paths, with real-only paths skipped when mock fixtures cannot represent them. Issue #503 was treated as privacy-sensitive account/profile context evidence only; no provider token or account behavior was implemented in this run.

## Hypothesis

The local harness already validates provider-disabled mock LLM routing, but it did not model the two paths named in the digest proposal: session isolation and file-tool operations. Adding deterministic, body-free checks for those paths improves future reviewable mock-LLM proposals without requiring live credentials or exposing file contents.

## Changes

- Extended `mock_llm_workflow_route` evaluation with optional `session` and `file_tools` sections.
- Hashes session IDs, file paths, and mock file contents instead of exporting raw values.
- Fails locally when a session reuses the previous session ID while isolation is required.
- Fails locally when a file-tool operation is not mocked.
- Added a local fixture covering a session plus file-tool mock route.
- Left `docs/self-model.md` unchanged because its current preference already matches this run: reversible local behavior improvement with a narrow privacy/offensive boundary.

## Validation

- `pytest tests/test_harness_eval.py -q` passed: 14 passed.
- `pytest tests/test_agent_harness_smoke.py tests/test_e2e_journey_regressions.py tests/test_harness_eval.py -q` passed: 17 passed.
- `pytest -q` first exceeded the 120s command timeout before returning results.
- `pytest -q` rerun with a longer timeout passed: 169 passed in 93.52s.

## Review Notes

- No live provider credentials were used.
- No token, credential, private key, private chat, PII, or personal data was printed, logged, uploaded, or exported.
- Provider/account preflight and fork PR automation proposals remain review-only under the digest boundary.
