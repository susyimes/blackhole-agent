# Codex Run Notes

Source digest: github-growth-20260618T165207.105877Z

Proposal selected: p1-provider-preflight-timeout-diagnostics

Evidence reviewed:
- https://github.com/omnigent-ai/omnigent/issues/668

Hypothesis:
External provider harnesses can fail after a long terminal startup timeout when a native Claude Code CLI works in the interactive shell but is not visible to the tmux-launched runner environment. A metadata-only provider runtime preflight can detect the high-risk macOS iTerm2/tmux/native-Claude shape before invoking the runner.

Self-model:
- Read `docs/self-model.md`.
- Left unchanged because its current preference already supports direct provider/config preflight improvements when rollback-backed and locally validated.

Rollback:
- Artifact: `artifacts/rollback/20260618T165207Z-provider-config-preflight-lane.txt`
- Ref: `refs/rollback/20260618T165207Z-codex_blackhole-evolve_20260618T165403.538673-add-or-refine-a-provider-config-preflight-lane-t`
- Recovery remains explicit and destructive; no rollback command was run.

Changes:
- Extended `provider_runtime_preflight` with `native_terminal_timeout_risk` detection for macOS + iTerm2 + tmux + native Claude CLI not resolved by the runner.
- Kept diagnostics metadata-only: raw CLI paths, env values, URLs, and secrets are not exported.
- Added a replay fixture and direct unit coverage for the timeout-risk block.
- Documented the preflight behavior in architecture notes.

Validation:
- `pytest tests/test_harness_eval.py -q` -> 33 passed
- `ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py` -> passed
- `pytest -q` -> 239 passed

Review notes:
- Proposal p1 had a privacy-leakage human-review gate upstream. The local change does not expose, log, print, upload, publish, or share tokens, credentials, secrets, private keys, private chats, PII, personal data, raw CLI paths, or env values.
- Proposals p2 and p3 were left unimplemented in this wake to keep one coherent change set focused on provider/config preflight.
