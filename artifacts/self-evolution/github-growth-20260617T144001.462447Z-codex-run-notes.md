# Codex Run Notes

Source digest: `github-growth-20260617T144001.462447Z`

Proposal selected: `p1-sandbox-provider-preflight`

Evidence reviewed:
- `https://github.com/omnigent-ai/omnigent/issues/517`

Hypothesis:
- A sandboxed SDK provider can fail before useful work when a runner strips a `NO_SANDBOX`-style override or when the provider runtime cannot be safely wrapped. A local metadata-only fixture should catch this class by proving whether the override reaches the harness and by converting launch risk into a structured degraded/blocked diagnostic.

Rollback:
- Artifact: `artifacts/rollback/20260617T144001Z-provider-runtime-preflight.txt`
- Ref: `refs/rollback/20260617T144001Z-provider-runtime-preflight`

Changes:
- Added `provider_runtime_preflight` to local harness eval behavior dispatch.
- Added structured diagnostics for sandbox-incompatible SDK provider runtime startup:
  - verifies parent override flag propagation through runner allowlist/passthrough metadata
  - degrades to unwrapped supervisor mode when an override or auto-degrade is available
  - blocks before launch with actionable diagnostics when the override is stripped
  - records no environment values and does not export CLI paths
- Added a frozen local fixture for a Claude SDK macOS sandbox override route.
- Added tests for the degraded path and the stripped-override blocked path.

Self-model:
- Left unchanged. The current self-model already says provider/config preflight checks are in scope and does not need another preference layer for this run.

Validation:
- `uv run pytest tests/test_harness_eval.py` passed: 18 tests.
- `uv run pytest` passed: 178 tests.

Review notes:
- The upstream evidence was specific and recent, but the local implementation is a deterministic platform-gated simulation rather than a live macOS/Claude startup probe.
- This change does not alter the Codex CLI launch path directly; it adds replayable validation machinery for the provider-runtime failure class.
