# Self-Evolution Run: skill-route pass-4 operator replay manifest

- Source digest: `github-growth-20260627T200729.528763Z`
- Capability window: `skill-route-discovery`, pass 4 of 4
- Rollback ref: `refs/rollback/20260627T200916Z-skill-route-discovery-pass4-completion`
- Rollback artifact: `artifacts/rollback/20260627T200916Z-skill-route-discovery-pass4-completion.md`

## Evidence Reviewed

- `dongshuyan/compass-skills`: state/profile/handoff skill ecosystem evidence.
- `lyra81604/zhengxi-views`: source-cited domain research skill evidence with advice-boundary pressure.
- `majidmanzarpour/threejs-game-skills`: game/frontend skill workflow evidence.
- `QwenLM/Qwen-AgentWorld`: adjacent general-agent project evidence, kept in `agent_harness_eval_required`.

## Hypothesis

The final skill-route pass already has classification and completion handoff coverage, but the operator replay path is easier to audit if the lane map exposes a compact manifest that links selected lanes to hashed local artifact target sets and recovery hints before supervisor activation review.

## Change

- Added `pass4_operator_replay_manifest` to `build_skill_route_discovery_proposal_lane_map`.
- Derived it from `pass4_completion_handoff` to avoid reclassifying evidence.
- Kept the manifest body-free: it records lane names, hashes, candidate names, route profiles, recovery hints, and adjacent general-agent boundaries, while denying raw URLs, raw paths, raw replay commands, upstream bodies, runtime action, provider launch, remote execution, and external skill or harness activation.
- Documented the operator contract in `docs/skill-route-discovery.md`.
- Added focused route and docs contract tests.

## Self-Model

`docs/self-model.md` was read and left unchanged. It already matches this run's evidence-backed preference for reversible, locally validated behavior changes over more standalone report scaffolding.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "pass4_local_lane_validation or pass4_completion_handoff"`: passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`: passed.
- `python -m pytest tests/test_skill_routing.py -q`: passed.
- `python -m pytest tests/test_docs_contracts.py -q`: passed.

## Review Notes

- No external skill installation, upstream code execution, provider launch, remote execution, memory/profile write, or restart was performed.
- Qwen-AgentWorld-style adjacent evidence remains queued behind local agent-harness evaluation and does not inherit skill-route lanes.
