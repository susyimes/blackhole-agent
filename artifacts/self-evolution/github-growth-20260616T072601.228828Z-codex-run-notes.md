# Codex Run Notes

Digest: `github-growth-20260616T072601.228828Z`
Branch: `codex/blackhole-evolve/20260616T072712.586521-add-or-strengthen-provider-auth-preflight-checks`
Rollback ref: `refs/rollback/20260616T072601-provider-auth-scope-preflight`
Rollback artifact: `artifacts/rollback/20260616T072601Z-provider-auth-scope-preflight.txt`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent/pull/279`
  - Reusable lesson: tool descriptors must preserve parameter JSON schemas, and compatibility caches must include name, description, and canonical parameters rather than tool names alone.
- `https://github.com/omnigent-ai/omnigent/pull/284`
  - Reusable lesson: routing/runtime policy decisions should be explicit in local metadata instead of inferred from under-specified mutable state.

## Chosen Improvement

Implemented proposal `p2-tool-schema-cache-integrity` as a local validation primitive:

- Added `ToolDescriptor` with `parameters`, `provider`, and `session_id` metadata.
- Added a canonical compatibility key that includes all fields affecting call compatibility.
- Added `ToolCompatibilityCache` keyed by the full descriptor.
- Added tests proving same-named tools do not collapse when schema, provider, or session context differs, and proving emitted call metadata retains the JSON schema.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference already supports reversible, locally validated behavior changes, and this run had a concrete code/test improvement available.

## Review Notes

- Proposal `p1-provider-auth-scope-preflight` remains review-only because it explicitly touches credential inheritance and privacy-leakage boundaries. No token, credential, secret, private key, private chat, PII, or personal data was printed or recorded.
- Initial `uv run pytest tests/test_tool_routing.py` failed to import the newly added module through the `pytest` executable. Source-tree validation passed with `PYTHONPATH=src` and the dev extra.

## Validation

- `python -m ruff check src\blackhole_agent\tool_routing.py tests\test_tool_routing.py` passed.
- `$env:PYTHONPATH='src'; uv run --extra dev python -m pytest tests/test_tool_routing.py` passed: 3 tests.
- `$env:PYTHONPATH='src'; uv run --extra dev python -m pytest tests/test_tool_routing.py tests/test_codex_cli.py` passed: 7 tests.
