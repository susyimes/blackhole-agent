# Codex Run Notes

Source digest: `github-growth-20260616T083705.136973Z`
Rollback artifact: `artifacts/rollback/20260616T083859Z-pr-event-digest-resilience.txt`
Rollback ref: `refs/blackhole/rollback/20260616T083859Z`

## Selected Route

Implemented `p2-pr-event-digest-resilience`.

Hypothesis: high-volume PR-event streams with generic or untitled items should produce bounded proposal evidence while preserving explicit uncertainty about unseen PR details. Deterministic review should reject duplicate proposal candidates and truncated-item citations instead of allowing noisy repeated routes.

## Material Actions

- Read `docs/self-model.md`; left it unchanged because it already matched this run's rollback-backed local evolution and narrow safety boundary.
- Updated `src/blackhole_agent/proposal_synthesis.py` to add metadata-only truncation uncertainty diagnostics, prompt guidance, and duplicate proposal rejection.
- Updated `tests/test_github_growth.py` with a synthetic PR-event pressure fixture and expected preflight metadata.
- Updated `docs/architecture.md` to document selected-item-only citation and PR truncation uncertainty behavior.
- Created rollback artifact and local rollback ref before source edits.

## Validation

- `uv run --extra dev pytest tests/test_github_growth.py -q` -> passed, 62 tests.
- `uv run --extra dev ruff check src\blackhole_agent\proposal_synthesis.py tests\test_github_growth.py` -> passed.
- `uv run --extra dev python -c "import blackhole_agent.proposal_synthesis; import blackhole_agent.github_growth"` -> passed.

## Review Notes

- Proposal `p1-security-gate-smoke-local-validation` was kept review-only because it was flagged for offensive-behavior human review.
- No external code was imported from the evidence repository.
- The new uncertainty diagnostics are metadata-only and do not copy evidence text or URLs into preflight output.
