# Codex Run Notes

- Source digest: `github-growth-20260620T041207.556739Z`
- Capability theme: `provider-runtime-control`
- Branch: `codex/blackhole-evolve/20260620T041305.093400-create-a-bounded-local-discovery-note-or-invento`
- Rollback artifact: `artifacts/rollback/20260620T041422Z-provider-runtime-control-pass4-local.txt`
- Rollback ref: `refs/rollback/20260620T041422Z-provider-runtime-control-pass4-local`

## Hypothesis

Provider/runtime preflight already emits body-free diagnostics and recovery hints, but operators still need a compact,
replayable recovery plan before supervisor promotion. Adding an `operator_recovery_plan` to single preflight and
aggregate recovery-summary outputs should make blocked and degraded states easier to act on without exporting provider
bodies, credentials, raw paths, raw URLs, or launching a provider runtime.

## Evidence Used

The run stayed within the carried capability window and local repository evidence. The external proposal URLs were
treated as route-shape evidence only; no upstream code was imported, cloned, installed, or executed.

## Changes

- Added `provider_runtime_operator_recovery_plan` in `src/blackhole_agent/harness_eval.py`.
- Wired the plan into `provider_runtime_preflight` and `provider_runtime_recovery_summary` outputs.
- Extended provider-runtime tests to assert blocked usage-limit recovery, aggregate recovery-plan replay, degraded-only
  replay handling, launch denial, remote-execution denial, privacy-review signaling, and body-free output flags.

## Validation

- `pytest tests/test_harness_eval.py -q -k provider_runtime`
- `ruff check src\blackhole_agent\harness_eval.py tests\test_harness_eval.py`
- `pytest tests/test_harness_eval.py -q`

## Review Notes

- Self-model was read and left unchanged. It already permits rollback-backed provider/config preflight improvements and
  remains descriptive rather than permission-granting.
- The new recovery plan is a controller/operator surface only. It does not request restart, promotion, provider launch,
  remote execution, credential failover, external skill activation, or upstream repository execution.
