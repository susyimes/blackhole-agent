# Codex Run Notes: agent harness provider registration lane

Source digest: `github-growth-20260619T091207.004916Z`
Capability window: `skill-route-discovery`
Pass: 1 of 4

## Rollback

- Rollback ref: `refs/rollback/20260619T091327Z-skill-route-discovery-pass1`
- Rollback artifact: `artifacts/rollback/20260619T091327Z-skill-route-discovery-pass1.md`
- Recovery command: `git reset --hard refs/rollback/20260619T091327Z-skill-route-discovery-pass1`

Rollback is explicit and destructive; this run only records the recovery path.

## Evidence

Selected local proposal: `p1-agent-harness-eval-qwencode`, with the skill-route
window kept active through a bounded local lane.

Evidence URLs reviewed:

- `https://github.com/omnigent-ai/omnigent/issues/806`
- `https://github.com/visa/visa-vulnerability-agentic-harness`
- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`

Omnigent issue #806 was sparse and marked needs-info, so it was not enough to
activate a runtime integration. The reusable lesson was narrower: model how a
Qwencode-style harness provider would be registered and make missing local
config fail before activation. Visa's harness evidence supported structured,
provider-neutral harness evaluation artifacts. FableCodex and COMPASS Skills
reinforced the same bounded-lane rule: external workflow or skill evidence
should become local docs, fixtures, config, or code patches, not direct
execution.

## Hypothesis

Future provider and skill-route discovery passes need an operator-visible
pre-activation lane for new harness providers. The lane should prove the
registration shape, report missing config clearly, avoid SDK launch/import, and
avoid exporting env values or env-key names.

## Changes

- Added `agent_harness_provider_registration` to the local harness eval
  behavior set.
- Added a metadata-only provider registration evaluator that reuses local
  provider harness discovery, blocks missing required config, and emits an
  activation gate.
- Added a Qwencode-style fixture that proves a proposed `qwencode-cli` harness
  is blocked as `required_provider_config_missing` when its command and local
  config are absent.
- Updated harness aggregate tests, focused regression tests, and architecture
  docs for the new lane.

## Self-Model

Read `docs/self-model.md` and left it unchanged. Its current preference already
matches this run: reversible local behavior changes are preferred over another
standalone report, while privacy leakage and offensive behavior remain outside
autonomous execution.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k agent_harness_provider_registration`
  - 1 passed, 66 deselected
- `python -m pytest tests/test_docs_contracts.py -q -k architecture`
  - 1 passed, 7 deselected
- `python -m pytest tests/test_harness_eval.py tests/test_docs_contracts.py tests/test_tool_routing.py -q`
  - 98 passed
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py tests/test_docs_contracts.py`
  - passed
- `python -m pytest -q`
  - 301 passed

## Review Notes

- No upstream code, install script, prompt, skill body, SDK, or provider runtime
  was imported, installed, enabled, or executed.
- No remote mutation, push, promotion, restart, cloud sandbox, live provider
  call, or external runner activation was attempted.
- The new lane intentionally keeps `provider_runtime_launch_allowed: false`;
  it only permits local provider registration after metadata preflight passes.
- Environment values and environment key names are omitted from harness output;
  missing env configuration is represented by stable hashes.
