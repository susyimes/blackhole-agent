# Codex Run Notes

Source digest: `github-growth-20260616T133522.555788Z`

## Evidence

- Reviewed `https://github.com/microsoft/fastcontext` and `https://github.com/microsoft/fastcontext/issues/11`.
- Extracted lesson: local agent/tool paths should be regression-tested with real absolute prefixes, because shortened `/<repo-name>/...` style paths can pass through model output confidently while failing local filesystem permission and tool checks.

## Hypothesis

Blackhole-agent should preserve full local checkout and candidate worktree paths across supervisor command construction, Codex CLI invocation, and durable run artifacts. Focused regression tests make future path display or serialization changes safer on nested local worktrees.

## Rollback

- Rollback artifact: `artifacts/rollback/20260616T133522Z-path-truncation-regression-tests.txt`
- Rollback ref: `refs/rollback/20260616T133522.555788Z/codex_blackhole-evolve_20260616T133627.358702-add-regression-coverage-for-local-path-handling-`

## Changes

- `tests/test_codex_cli.py`: added nested local workspace coverage that verifies `codex exec --cd`, `--output-last-message`, and latest-run JSON preserve full local paths instead of Docker-style shortened paths.
- `tests/test_supervisor.py`: added candidate worktree coverage that verifies `git worktree add` and child `--repo-path` receive the full nested worktree path.

## Validation

- Initial fixture used an overlong Windows path and failed while writing the fake last-message file; the fixture was shortened to keep the test focused on blackhole-agent path preservation rather than OS long-path policy.
- Passed:
  - `$env:PYTHONPATH=(Resolve-Path src).Path; uv run pytest tests/test_codex_cli.py tests/test_supervisor.py -q`
  - `$env:PYTHONPATH=(Resolve-Path src).Path; uv run ruff check tests/test_codex_cli.py tests/test_supervisor.py`

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged because this run matched the existing preference for rollback-backed, locally validated evolution and did not reveal evidence that the self-model is behavior-shaping or misleading.

## Review Notes

- No production path normalization was added because the local failure mode is best covered at the existing supervisor/kernel path boundaries in this repository.
- No offensive behavior, unauthorized access, or privacy-leakage route was implemented.
