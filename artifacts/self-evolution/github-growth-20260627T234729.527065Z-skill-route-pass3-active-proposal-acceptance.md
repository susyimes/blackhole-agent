# Skill Route Pass 3 Active Proposal Acceptance

Source digest: `github-growth-20260627T234729.527065Z`
Rollback artifact: `artifacts/rollback/20260628T000000Z-skill-route-pass3-local-lanes.md`
Rollback ref: `refs/blackhole-rollback/20260628T000000Z-skill-route-pass3-local-lanes`

## Evidence

Reviewed the carried proposal evidence URLs only:

- `https://github.com/lyra81604/zhengxi-views`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/dongshuyan/compass-skills`

Reusable lesson: public skill repositories can identify a bounded local lane,
but route acceptance must remain body-free, fixture-backed, locally validated,
and non-executable.

## Hypothesis

Pass 3 should expose an operator-visible acceptance lane for the active proposal
IDs before pass-4 continuation. That lane should bind:

- generic skill workflow evidence to a local `test` lane
- game/frontend skill workflow evidence to a local `documentation` lane
- skill ecosystem state handoff evidence to a local `config` lane

It must not install, execute, activate, launch providers, write memory/profile
state, export raw GitHub URLs, export raw commands, or read upstream bodies.

## Local Changes

- Added `pass3_active_proposal_acceptance_lane` to the skill-route lane map.
- Added a frozen fixture for the current active proposals.
- Added regression coverage for accepted lanes, selected evidence IDs, denial
  flags, and hash-only replay/source exposure.
- Documented the new pass-3 acceptance surface.

## Self-Model Decision

`docs/self-model.md` was left unchanged. The current self-model already says to
prefer rollback-backed, locally validated behavior changes when safe; this run
used that preference instead of revising the self-description.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k pass3_active_proposal_acceptance_lane`
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
- `python -m pytest tests/test_skill_routing.py -q`

All validations passed.

## Review Notes

- The new surface is an acceptance checkpoint for supervisor replay, not
  activation authority.
- Adjacent general-agent proposals remain outside this lane unless they carry
  skill workflow evidence in a future digest.
