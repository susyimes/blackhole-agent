# Skill Route Discovery Pass 1 Proposal Intake

Source digest: `github-growth-20260627T090310.946057Z`

Rollback artifact:
`artifacts/rollback/20260627T090309Z-skill-route-discovery-pass1.txt`

## Hypothesis

The active skill-route-discovery pass needs an operator-visible intake surface
that maps carried proposal evidence to bounded local lanes before activation.
The current route map already exposes candidate and profile surfaces, but the
controller cannot directly see whether the active proposal IDs are covered by
local validation lanes.

## Change

Added `proposal_intake_lane` to the skill-route discovery lane map. It groups
recognized route profiles into current-window proposal IDs:

- `p1-skill-route-discovery-generic`
- `p2-game-frontend-skill-profile`
- `p3-skill-ecosystem-handoff`

Each row records candidate names, source hashes, route profiles, allowed local
lanes, selected local lanes, validation gates, validation targets, and replay
commands. The surface keeps runtime action, upstream skill activation, external
harness execution, provider launch, remote execution, raw source URL export,
raw target path export, and upstream body export denied.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "proposal_intake or source_cited_domain_research or current_window_matrix"`
- `python -m pytest tests/test_skill_routing.py -q`

Both passed.

## Review Notes

The self-model was read and left unchanged. It already describes the relevant
autonomy and safety boundary, and this run needed a concrete route-map behavior
surface rather than another self-description revision.

No external code, install scripts, upstream skill bodies, provider calls, or
runtime execution paths were adopted.
