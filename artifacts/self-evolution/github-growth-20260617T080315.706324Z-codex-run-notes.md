# Codex Run Notes

- Source digest: `github-growth-20260617T080315.706324Z`
- Proposal selected: `p4-ci-validation-gates`
- Rollback artifact: `artifacts/rollback/20260617T080551Z-ci-validation-metadata.txt`
- Rollback ref: `refs/blackhole-rollback/20260617T080551Z`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/pull/471`
- `https://github.com/omnigent-ai/omnigent/pull/472`
- `https://github.com/omnigent-ai/omnigent/pull/473`
- `https://github.com/omnigent-ai/omnigent/issues/160`

## Hypothesis

Omnigent's public CI metadata activity supports a local controller improvement: self-evolution manifests should expose
reviewer routing, non-blocking red coverage-drop signals, and a guard that prevents bypass-style labels from granting
autonomous local apply.

## Changes

- Added controller-owned review metadata to proposal manifest controls.
- Added aggregate reviewer routes, coverage-drop signals, and bypass-label guard fields to replayable validation reports.
- Blocked autonomous local apply when candidate metadata contains bypass-style labels such as `force-merge`, `ci-bypass`,
  or `skip-ci`.
- Added focused tests for coverage routing, bypass-label blocking, and updated manifest/report contracts.

## Self-Model Decision

`docs/self-model.md` was left unchanged. The current self-model already prefers rollback-backed locally validated
implementation over report-only work, and this run changed controller behavior rather than identity text.

## Review Notes

- The repository has no `.github` directory in this worktree, so the CI lesson was applied to local validation and
  self-evolution manifest metadata instead of adding an artificial workflow.
- The OIDC issue evidence was treated as review-only because it describes an unauthorized-access boundary; no exploit,
  attack path, or identity-bypass implementation was added.

## Validation

- `uv run pytest tests/test_github_growth.py -q` passed: 70 tests.
- `uv run ruff check .` passed.
- `uv run pytest -q` passed: 152 tests.
