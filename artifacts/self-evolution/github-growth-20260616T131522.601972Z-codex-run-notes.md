# Codex Run Notes

Source digest: `github-growth-20260616T131522.601972Z`

## Evidence

- Reviewed `https://github.com/omnigent-ai/omnigent` as the bounded external evidence URL.
- Extracted lesson: active agent-harness projects can provide useful aggregate movement signals, but generic or untitled PR lifecycle evidence should increase uncertainty and should not permit over-specific implementation claims.

## Hypothesis

Growth routes should not be eligible for local implementation unless they carry item-id evidence references, a concrete validation task, an evidence-backed rationale, and explicit uncertainty. This keeps autonomous changes replayable when public trend evidence is aggregate or missing PR detail.

## Rollback

- Rollback artifact: `artifacts/rollback/20260616T131522Z-validation-route-evidence.txt`
- Rollback ref: `refs/rollback/20260616T131522.601972Z/codex_blackhole-evolve_20260616T131642.772571-add-or-strengthen-local-tests-that-detect-whethe`

## Changes

- `src/blackhole_agent/proposal_synthesis.py`: reject proposal candidates with blank `rationale` or blank `uncertainty`.
- `tests/fixtures/proposal_replay/omnigent_route_contract.json`: added frozen Omnigent-shaped replay fixture with generic PR lifecycle signals, one valid route, and one underspecified route.
- `tests/fixtures/proposal_replay/manifest.json`: registered the Omnigent route-contract fixture.
- `tests/test_proposal_eval.py`: updated replay-suite expectations for the added fixture.

## Validation

- Initial unpinned command `uv run pytest tests/test_proposal_eval.py -q` imported `blackhole_agent` from `C:\Users\svmes\Documents\Playground\blackhole-agent\src` because the ambient environment pointed at another checkout; this was treated as an environment issue, not a code failure.
- Passed with this worktree pinned:
  - `$env:PYTHONPATH=(Resolve-Path src).Path; uv run pytest tests/test_proposal_eval.py -q`
  - `$env:PYTHONPATH=(Resolve-Path src).Path; uv run pytest tests/test_github_growth.py tests/test_proposal_eval.py -q`

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged because it already describes the applicable preference: direct local evolution is acceptable when rollback-backed, locally validated, and explicit about uncertainty.

## Review Notes

- Proposal p4 remains review-only by source policy; no provider secret/token readiness behavior was implemented in this run.
- No offensive behavior, unauthorized access, or privacy-leakage route was implemented.
