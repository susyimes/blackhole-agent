# Codex Run Notes

Source digest: github-growth-20260618T211207.175634Z
Prepared branch: codex/blackhole-evolve/20260618T211304.132185-add-a-local-provider-config-preflight-validation

## Hypothesis

Omnigent interrupt/session movement suggests a reusable local lesson: session interruptions should be validated with
explicit rebuild and idle-message replay checks, not only with generic session isolation. A deterministic local harness
lane can catch lost or duplicated pending idle messages without live providers and without exporting raw session,
agent, conversation, or message identifiers.

## Rollback

Rollback ref: refs/rollback/20260619T051206Z-interrupt-session-replay
Rollback artifact: artifacts/rollback/20260619T051206Z-interrupt-session-replay.txt

## Changes

- Added interrupt rebuild/replay evaluation to `mock_llm_workflow_route`.
- Added privacy-preserving output fields that hash session, agent, conversation, and idle message identifiers.
- Added failure mode `interrupt_replay_failed` when rebuild metadata is incomplete or replayed idle message IDs do not
  match the pending IDs as a multiset.
- Added a frozen local harness fixture for a passing interrupt rebuild and idle replay scenario.
- Added direct regression tests for the passing path and for replay drift with one lost and one duplicated idle message.

## Validation

- `python -m pytest tests/test_harness_eval.py` passed: 38 tests.
- `python -m pytest` passed: 248 tests.

## Review Notes

- Proposal `p1-provider-config-preflight-whitespace` remained review-only because it is tied to environment-derived API
  keys and the supplied validation gate is `privacy-leakage-human-review`.
- Proposal `10852202885-1` remained review-only because it explicitly concerns the privacy-leakage boundary.
- No self-model change was made. The existing self-model already says local behavior changes should be preferred when
  rollback-backed and validated, while keeping privacy-leakage routes review-only.
