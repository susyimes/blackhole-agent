# Codex Run Notes

Source digest: `github-growth-20260620T095207.635285Z`
Capability window: `skill-route-discovery`, pass 1 of 4
Branch: `codex/blackhole-evolve/20260620T095309.462351-document-the-intended-interpretation-of-skill-wo`
Rollback artifact: `artifacts/rollback/20260620T095345Z-skill-route-discovery-pass1.txt`
Rollback ref: `refs/rollback/20260620T095345Z-skill-route-discovery-pass1`

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/omnigent-ai/omnigent`

The skill repositories were treated as public route/profile evidence only.
Omnigent was treated as general agent-framework evidence where generic or
untitled pull request movement can provide weak supporting context, not a
standalone growth route.

No upstream code, install command, helper script, scaffold, provider route, or
skill body was adopted or executed.

## Hypothesis

The skill-route discovery lane already blocks weak generic PR/push clusters
before activation, but the operator-facing activity panel should expose the
same low-detail PR policy directly. This makes generic untitled PR activity
auditable as supporting context while preventing it from appearing equivalent
to push validation pressure or specific route evidence.

## Changes

- Passed `evidence_strength` into `skill_route_discovery_activity_signal_panel`.
- Added activity-panel fields for low-detail generic movement:
  `weak_generic_movement_count`, `local_corroborating_signal_count`,
  `generic_movement_policy`, row-level
  `weak_generic_supporting_context_only`, and local-corroboration state.
- Added diagnostics when generic upstream movement still needs local
  corroboration.
- Extended existing generic PR tests to assert the activity panel stays
  supporting-context-only until local corroboration exists.
- Documented the operator-facing low-detail PR policy in
  `docs/skill-route-discovery.md`.

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged
because the current self-description already matches this pass: local evolution
is allowed when rollback-backed and validated, while permissions and safety
boundaries remain external constraints. This run produced no evidence that the
self-model was constraining or contradicting the selected behavior.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`
  passed: 9 passed, 85 deselected.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
  passed: 2 passed, 6 deselected.
- `python -m pytest tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery`
  passed: 17 passed, 25 deselected.
- `python -m pytest tests/test_harness_eval.py tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_docs_contracts.py -q -k skill_route_discovery`
  passed: 33 passed, 111 deselected.

## Rollback

Destructive recovery remains operator/supervisor-controlled:

```bash
git switch codex/blackhole-evolve/20260620T095309.462351-document-the-intended-interpretation-of-skill-wo
git reset --hard refs/rollback/20260620T095345Z-skill-route-discovery-pass1
```

## Review Notes

- Generic or untitled PR movement is not activation-ready on its own.
- Route hints, CI words, and bounded lane names inside low-detail upstream PR
  evidence remain insufficient without a separate local corroboration record.
- Runtime action, external skill activation, external harness execution,
  provider launch, remote execution, upstream clone/install/run, and raw
  upstream body export remain denied.
