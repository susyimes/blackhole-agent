# Codex Run Notes

Source digest: `github-growth-20260616T163522.799130Z`

## Chosen Proposal

Selected `p2-provider-harness-routing`.

Hypothesis: Omnigent's public meta-harness pattern is useful locally as deterministic provider capability routing,
as long as the implementation only discovers local capability state and records skip reasons. This avoids adopting
review-only sandbox or offensive-adjacent execution behavior from `p1`.

Evidence reviewed:

- `https://github.com/omnigent-ai/omnigent`
- Relevant lesson: a common layer over Codex, Cursor, custom agents, and other providers benefits from explicit
  prerequisite checks and consistent fallback behavior.

## Changes

- Added provider harness candidate, discovery status, and selection dataclasses.
- Added injectable discovery for installed modules, commands, env names, runner enablement, and platform support.
- Added a default fallback order for Codex CLI, Copilot-like SDK, Cursor-like SDK, and single-file function agents.
- Added tests for deterministic fallback, missing optional extras, disabled runners, missing dependencies, missing env,
  unsupported platforms, and secret-free diagnostic serialization.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already matches this run: prefer direct, rollback-backed,
locally validated behavior changes while keeping offensive behavior, abuse, unauthorized access, and privacy leakage
outside autonomous adoption.

## Rollback

Rollback ref: `refs/rollback/blackhole-agent/20260616T163522Z`

Rollback artifact: `artifacts/rollback/20260616T163522Z-provider-harness-routing.txt`

## Validation

- `python -m pytest tests/test_tool_routing.py` passed: 8 tests.
- `python -m pytest` passed: 115 tests.
- `python -m ruff check src tests` passed.

## Review Notes

- This run does not implement remote sandbox execution, provider authentication flows, or new external runner launches.
- Provider SDK names in the default routing table are conservative local probes and can be refined when the repository
  gains concrete Copilot or Cursor integration packages.
