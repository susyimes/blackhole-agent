# Codex Run Notes

Source digest: `github-growth-20260617T180001.539628Z`

Branch: `codex/blackhole-evolve/20260617T180109.294803-add-or-tighten-local-tests-for-provider-config-p`

Rollback artifact: `artifacts/rollback/20260617T180001Z-provider-config-url-safety-playwright-tests.txt`

Rollback ref: `refs/blackhole-rollback/20260617T180001Z-provider-config-url-safety-playwright-tests`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent/pull/544`

The PR describes a test hardening change where URL-only safety tests remain collectible while Playwright-dependent configure checks are skipped when Playwright is absent. The reusable local lesson is that optional browser tooling should degrade or skip only the browser-dependent branch, while URL safety remains independently executable.

## Hypothesis

Provider runtime preflight fixtures should represent optional browser tooling as a scoped degraded state. Missing Playwright must not mask unsafe, local, or malformed base URLs, and preflight artifacts should avoid recording raw URL values.

## Changes

- Extended `provider_runtime_preflight` with metadata-only `browser_tooling` and `url_safety` output.
- Added URL safety diagnostics for missing required URLs, non-absolute non-http(s) URLs, localhost, loopback, private, link-local, and unspecified network targets.
- Added tests proving missing Playwright does not hide an unsafe local URL and that a safe URL degrades cleanly when browser configure checks are unavailable.
- Documented the provider runtime preflight contract in `docs/architecture.md`.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already covers rollback-backed provider/config preflight improvements and does not need another layer for this evidence.

## Validation

- `pytest tests/test_harness_eval.py -q`: 20 passed
- `pytest tests/test_codex_cli.py tests/test_tool_routing.py -q`: 34 passed
- `ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py`: passed
- `pytest -q`: 194 passed

## Review Notes

- Proposal 1 was applied as a local validation candidate.
- Proposal 2 was not selected because proposal 1 had stronger direct evidence for the prepared branch topic.
- Proposal 3 remains review-only under the offensive-behavior human-review boundary; no exploit, attack, exfiltration, unauthorized-access, live provider, or remote execution behavior was implemented.
- Raw base URLs and optional dependency values are not exported in the new preflight output.
