# Skill Route Registry Validation Lane

Source digest: `github-growth-20260624T111355.955192Z`
Run timestamp: `20260624T191751Z`
Branch: `codex/blackhole-evolve/20260624T111622.108333-add-a-local-validation-lane-for-agent-registry-c`
Rollback ref: `refs/rollback/blackhole-evolve-20260624T191751Z`
Rollback artifact: `artifacts/rollback/20260624T191751Z-skill-route-registry-validation-lane.txt`

## Evidence

- `p2-agent-registry-validation`: upstream agent project movement suggests registry changes need a local lane that validates loading, schema expectations, and route availability before activation.
- Capability window: `skill-route-discovery`, pass 3 of 4.
- Evidence URLs carried by the window:
  - `https://github.com/omnigent-ai/omnigent`
  - `https://github.com/dongshuyan/compass-skills`
  - `https://github.com/lyra81604/zhengxi-views`

No broad trend discovery was rerun. This pass used the digest-provided URLs as context and improved local validation machinery.

## Hypothesis

Skill route discovery already has candidate registry and lane-map tests, but operators lacked a compact route-availability report attached to the lane map. A validation lane that checks registry shape, expected route metadata, disabled-by-default state, and per-candidate availability should make future registry edits fail earlier and more legibly.

## Change

- Added `build_skill_route_discovery_registry_validation_lane` in `src/blackhole_agent/skill_routing.py`.
- Surfaced the report as `registry_validation_lane` from `build_skill_route_discovery_proposal_lane_map`.
- Added tests that:
  - prove valid registry records expose available routes and expected route metadata;
  - prove malformed records produce path-specific diagnostics;
  - prove the proposal lane map carries the validation lane.

The lane remains local-only: `runtime_action` is `none`, external skill activation is false, external harness execution is false, provider runtime launch is false, and remote execution is false.

## Self-Model

`docs/self-model.md` was read and left unchanged. Its current preference already matches this run: use rollback-backed local behavior changes instead of producing another validation-report-only artifact.

## Validation

- `pytest tests/test_skill_routing.py -q`
  - `36 passed`
- `pytest tests/test_harness_eval.py -q -k skill_route_discovery`
  - `44 passed, 109 deselected`
- `pytest tests/test_github_growth.py -q -k "skill_route or route_classifier or route_activation_preflight"`
  - `11 passed, 83 deselected`

## Review Notes

- No external code was cloned, installed, run, or imported.
- No restart, push, promotion, or remote execution was performed by this kernel.
- The change is intentionally a controller-visible behavior path, not only a standalone fixture.
