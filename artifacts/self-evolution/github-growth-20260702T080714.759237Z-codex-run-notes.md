# Codex Run Notes

Source digest: `github-growth-20260702T080714.759237Z`

Rollback point: `artifacts/rollback/20260702T080713Z/rollback.md`

## Evidence Reviewed

- `https://github.com/lyra81604/zhengxi-views`: public Agent Skill repository with `SKILL.md`, `skill.yml`, references, scripts, evals, source-cited workflow claims, and non-investment-advice boundaries.
- `https://github.com/QwenLM/Qwen-AgentWorld`: general agent project and benchmark evidence, not a skill-route repository in this run.
- `https://github.com/TianhangZhuzth/Fundamental-Ava`: general autonomous agent project evidence, not a skill-route repository in this run.

## Hypothesis

Pass 3 of the active `skill-route-discovery` slice is more useful to the
operator when adjacent general-agent projects are visible as per-project
`agent_harness_eval_required` gates before any documentation, test, or
code_patch lane can be selected. This keeps zhengxi-views and BioNeMo-style
skill evidence bounded to local lanes while preventing Qwen-AgentWorld and
Fundamental-Ava from inheriting `skill_route_discovery`.

## Changes

- Added `adjacent_agent_harness_eval_matrix`,
  `adjacent_general_agent_names`, and
  `agent_harness_eval_required_item_ids` to
  `current_digest_pass3_activation_review_lane`.
- Bound source digest `github-growth-20260702T080714.759237Z` to the active
  pass-3 proposal IDs and route specs.
- Added frozen fixture
  `tests/fixtures/skill_route_discovery/current_digest_20260702T080714_pass3_route_discovery_validation.json`.
- Added regression coverage for zhengxi-views, BioNeMo, Qwen-AgentWorld, and
  Fundamental-Ava pass-3 routing.
- Documented the body-free adjacent harness matrix in
  `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "20260702T080714 or 20260702T064714_pass3"` passed: 2 tests.
- `python -m pytest tests/test_skill_routing.py -q -k "current_digest_pass3_activation_review_lane or 20260702T080714 or 20260702T064714 or 20260702T040714 or 20260702T052715"` passed: 4 tests.
- `python -m pytest tests/test_skill_routing.py -q -k "20260702T080714 or 20260702T064714_pass3 or 20260702T040714_pass3 or 20260702T052715"` passed: 4 tests.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 tests.

## Review Notes

- Self-model was read and left unchanged. This run produced a direct bounded
  behavior improvement, so the self-model did not need to become the artifact of
  record.
- The new matrix is body-free and keeps runtime action, external skill or agent
  activation, external harness execution, provider launch, remote execution,
  profile writes, memory writes, raw URL export, replay-command export,
  target-path export, and upstream-body export denied.
