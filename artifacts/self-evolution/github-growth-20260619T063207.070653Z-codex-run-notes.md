# Codex Run Notes

Source digest: github-growth-20260619T063207.070653Z
Branch: codex/blackhole-evolve/20260619T063304.498768-create-or-strengthen-a-local-mock-llm-e2e-valida
Rollback artifact: artifacts/rollback/20260619T063206Z-mock-llm-chat-contract-lane.txt
Rollback ref: refs/rollback/blackhole-evolve-20260619T063206Z

## Evidence

- Primary proposal: p1_mock_llm_e2e_preflight.
- Upstream evidence reviewed: https://github.com/omnigent-ai/omnigent/pull/787 and https://github.com/omnigent-ai/omnigent.
- Reusable lesson: mock LLM e2e lanes should replace live provider credentials where possible, but must still prove the HTTP/mock-server contract that production code depends on. The upstream PR specifically needed non-streaming JSON support for a mock LLM server and review feedback warned about losing e2e guarantees when moving to mock-only coverage.

## Hypothesis

The local `mock_llm_workflow_route` evaluator should classify OpenAI-compatible `/v1/chat/completions` mock-server behavior directly, including non-streaming JSON versus streaming SSE response format checks, and should keep provider/config/token preflight failures distinct from mock-server contract drift.

## Changes

- Added a chat/completions mock-server contract evaluator to `src/blackhole_agent/harness_eval.py`.
- Added fixture coverage in `tests/fixtures/local_harness_eval/mock_llm_chat_completions_contract.json`.
- Added tests for the passing contract, response-format mismatch, provider-token preflight failure, body redaction, and aggregate local harness counts.
- Documented the new mock LLM contract in `docs/architecture.md`.

## Self-Model Decision

Left `docs/self-model.md` unchanged. The current self-model already says to prefer rollback-backed, locally validated behavior changes over validation-report-only work, which matched this wake. No new evidence showed that the file needed restructuring or contradiction.

## Validation

- `pytest tests/test_harness_eval.py -q -k "mock_llm_workflow_route or local_harness_eval"`: 26 passed, 36 deselected.
- `pytest tests/test_harness_eval.py -q`: 62 passed.
- `pytest -q`: 296 passed.

## Review Notes

- No offensive-behavior, abuse, unauthorized-access, or privacy-leakage route was introduced.
- Mock contract output intentionally omits raw chat messages, responses, base URLs, and token values.
- Activation/restart/push were not performed by this kernel; supervisor handoff remains external.
