# Codex Run Notes

Source digest: `github-growth-20260615T093747.722357Z`

Proposal used: `p3-local-validation-metadata-test`

Hypothesis: LLM-assisted proposal synthesis should reject candidate-supplied
evidence URLs, not merely ignore them, so every accepted proposal can be traced
back to frozen digest item IDs.

Change made:
- Added an explicit `evidence_urls` validation error in proposal candidate review.
- Added a regression test proving a candidate with a valid frozen `evidence_refs`
  value is still rejected when it supplies a new URL directly.

Self-model decision: left `docs/self-model.md` unchanged. It is still an
ornamental, blank self-description for this run; the safer improvement was a
local validation guardrail in proposal synthesis.

Rollback point:
- Artifact: `artifacts/rollback/20260615T093747.722357Z-codex_blackhole-evolve_20260615T093837.384374-open-a-review-issue-to-compare-agent-harness-pat.txt`
- Local ref: `refs/blackhole-agent/rollback/20260615T093747Z/20260615T093747.722357Z`

Validation:
- `uv run pytest tests/test_github_growth.py -k "llm_proposal_review_rejects_candidate_supplied_evidence_urls or llm_proposal_review_rejects_unknown_evidence_ref"` failed because the command used an outer virtualenv package rather than this worktree source.
- `$env:PYTHONPATH = (Resolve-Path 'src').Path; uv run pytest tests/test_github_growth.py::test_llm_proposal_review_rejects_candidate_supplied_evidence_urls tests/test_github_growth.py::test_llm_proposal_review_rejects_unknown_evidence_ref` passed.
- `$env:PYTHONPATH = (Resolve-Path 'src').Path; uv run pytest tests/test_github_growth.py` passed with 35 tests.

Review notes:
- No external evidence was fetched. The digest proposals were specific enough to
  justify a local validation improvement without re-running trend discovery.
- The outer `VIRTUAL_ENV` warning is worth separate cleanup, but it is outside
  this run's one conceptual improvement.
