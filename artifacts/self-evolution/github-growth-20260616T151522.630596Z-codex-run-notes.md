# Codex Run Notes

Source digest: `github-growth-20260616T151522.630596Z`

Proposal selected: `p1_memory_tool_local_design`

Evidence reviewed:
- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/issues/369`

Hypothesis:
First-party memory is useful for agent runtimes when it is local, namespace-isolated, non-secret, and covered by direct read/write/delete tests. The local route should reject likely credentials or personal data before persistence and keep storage inspectable as JSON.

Rollback:
- Artifact: `artifacts/rollback/20260616T151522Z-memory-tool-local-design.txt`
- Ref: `refs/rollback/blackhole-agent/20260616T151522Z-memory-tool-local-design`
- Original HEAD: `2e03a9ec2523c0abb1b00682981b4660795b9b25`

Changes:
- Added `blackhole_agent.local_memory.LocalMemoryStore` for namespace-scoped JSON memory.
- Added privacy guards that reject common token, private key, email, SSN, and secret assignment patterns before writes persist.
- Added `local_memory_tool_descriptor()` to the local tool routing metadata.
- Documented the local memory tool separately from trend-selection memory.
- Added focused regression tests for write/read/list/delete, privacy rejection, namespace isolation, and descriptor routing.

Self-model:
Read `docs/self-model.md` and left it unchanged. The current preference already explicitly allows locally validated memory improvements when rollback-backed and privacy-aware; a self-model edit would be ornamental for this run.

Validation:
- `uv run --extra dev pytest tests/test_local_memory.py tests/test_tool_routing.py tests/test_supervisor.py` passed: 21 tests.
- `uv run --extra dev ruff check .` passed.
- `uv run --extra dev pytest` passed: 107 tests.

Review notes:
- `uv run pytest` in this shell resolved `pytest.exe` from an unrelated parent virtualenv and failed to see the newly added module. Validation used `uv run --extra dev ...` so pytest and ruff were installed into this checkout's `.venv`.
- The memory tool is a local prototype and descriptor route; no remote storage, push, restart, promotion, or supervisor activation was performed by this kernel.
