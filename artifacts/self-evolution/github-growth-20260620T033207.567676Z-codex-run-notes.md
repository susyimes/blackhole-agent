# Codex Run Notes

Source digest: `github-growth-20260620T033207.567676Z`
Theme: `provider-runtime-control`
Branch: `codex/blackhole-evolve/20260620T033320.509201-add-or-extend-local-fixtures-tests-that-verify-r`

## Hypothesis

The prior provider/runtime-control pass made provider-runtime replay visible in
skill-route activation and handoff rows. The next useful operator-facing
improvement is to make the control object state the next safe action and a
body-free summary of recovery hint pressure, so blocked skill-route evidence can
be replayed locally without inspecting provider bodies, URLs, or raw diagnostics.

## Evidence Used

- Source digest proposals `p1-skill-route-discovery-fixture`,
  `p2-skill-route-discovery-doc`, and
  `p3-agent-project-comparison-harness`.
- Carried public evidence URLs:
  `https://github.com/baskduf/FableCodex`,
  `https://github.com/dongshuyan/compass-skills`,
  `https://github.com/majidmanzarpour/threejs-game-skills`, and
  `https://github.com/Peanut919/xuefeng-agent`.
- Local context reviewed: `docs/self-model.md`, `docs/skill-route-discovery.md`,
  `src/blackhole_agent/harness_eval.py`, `tests/test_harness_eval.py`, and
  `tests/test_docs_contracts.py`.

## Changes

- Extended `skill_route_discovery_provider_runtime_control()` with:
  `next_action`, `recovery_hint_count`, and hashed recovery hint codes.
- Added focused assertions for ready and blocked skill-route provider-runtime
  handoffs.
- Updated the skill-route discovery documentation and docs contract test so the
  next-action and hashed-code fields remain part of the documented contract.

## Safety And Privacy Notes

- No upstream repository code was cloned, installed, imported, or executed.
- No provider runtime, remote runner, installer, or external harness was
  launched.
- Recovery hint codes are still body-free; raw provider inputs, diagnostics,
  URLs, target paths, and evidence bodies remain omitted or hashed.

## Self Model

`docs/self-model.md` was read and left unchanged. Its current preference already
matches this run: provider/config preflight checks, controller metadata, tests,
and documentation are valid local evolution targets when rollback-backed and
validated.

## Rollback

Rollback ref: `refs/rollback/20260620T033207Z-provider-runtime-control-pass2`
Rollback artifact:
`artifacts/rollback/20260620T033207Z-provider-runtime-control-pass2.txt`

## Validation

Passed:

- `pytest tests/test_harness_eval.py -q -k "skill_route_discovery_lane or provider_runtime_preflight or provider_runtime_recovery_summary"`
  - `21 passed, 71 deselected`
- `pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
  - `2 passed, 6 deselected`
- `pytest tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_harness_eval.py -q`
  - `133 passed`
- `pytest tests/test_docs_contracts.py -q`
  - `8 passed`
