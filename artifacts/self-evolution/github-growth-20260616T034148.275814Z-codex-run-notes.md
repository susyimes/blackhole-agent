# Self-Evolution Run Notes

Source digest: `github-growth-20260616T034148.275814Z`

Proposal selected: `p1-validation-report-contract`

Hypothesis: harness-style evidence is useful locally only when validation reports can be replayed from their own provenance metadata. A completed report should not rely on top-level evidence URLs alone.

Evidence reviewed:
- https://github.com/omnigent-ai/omnigent/issues/241
- https://github.com/samarailly51-pixel/opencode-harness
- https://github.com/visa/visa-vulnerability-agentic-harness

Reusable lesson:
- The Omnigent issue shows a harness path where advertised model availability can exceed verified endpoint compatibility.
- OpenCode Harness emphasizes standardized traces and evaluation reports for coding-agent runs.
- Visa Vulnerability Agentic Harness emphasizes structured triage artifacts and human review for security findings.
- Local adoption should therefore strengthen validation report replayability, not enable new harnesses, security scanning, remote execution, credentials, restarts, pushes, or promotion.

Change made:
- `validation_report_completion_status` now requires `provenance.evidence_urls` to match top-level `evidence_urls`.
- Tests now cover mismatched provenance evidence URLs as incomplete adoption evidence.

Rollback:
- Artifact: `artifacts/rollback/20260616T034147Z-validation-report-contract.txt`
- Ref: `refs/blackhole-agent/rollback/20260616T034147Z-validation-report-contract`

Skipped capabilities:
- New agent harnesses
- Remote execution
- Security scanning
- Credentials or tokens
- Restart paths
- Push or promotion

Validation:
- `uv run --with pytest python -m pytest tests/test_github_growth.py -q` -> passed, 45 tests.
- `uv run ruff check src/blackhole_agent/github_growth.py tests/test_github_growth.py` -> passed.
- `uv run python -c "import blackhole_agent.github_growth"` -> passed.

Review notes:
- The bare `uv run pytest ...` command resolved a stale external `pytest` console script in this environment. Validation used `uv run --with pytest python -m pytest ...` so pytest and the current checkout were loaded by the same interpreter.
- Self-model was left unchanged. Its current preference already points at validation report contracts, and the behavior-shaping change belongs in controller code and tests.
