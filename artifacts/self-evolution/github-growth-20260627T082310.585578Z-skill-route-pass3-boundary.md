# Skill Route Pass 3 Boundary Handoff

Source digest: `github-growth-20260627T082310.585578Z`

## Evidence

- `https://github.com/lyra81604/zhengxi-views` exposes a public agent skill shape with `SKILL.md`, `skill.yml`, references, scripts, and evals. The local lesson is to keep skill evidence in bounded documentation, config, test, or code_patch lanes with local validation required.
- `https://github.com/QwenLM/Qwen-AgentWorld` is general-agent evidence, not a skill-workflow route. The local lesson is to surface it as adjacent `agent_harness_eval_required` evidence without inheriting `skill_route_discovery` lanes.
- `https://github.com/majidmanzarpour/threejs-game-skills` remains the game frontend skill profile anchor for pass-3 selected local test-lane continuity.

## Hypothesis

Pass 3 should provide an operator-visible handoff that separates active skill-route lanes from adjacent general-agent evidence before the final pass. This reduces accidental skill-lane inheritance while keeping Qwen/AgentWorld-style evidence visible for later harness evaluation.

## Changes

- Added `skill_route_discovery_pass3_adjacent_general_agent_project_eval` to the pass-3 handoff packet.
- Extended the pass-3 local harness fixture with Qwen-AgentWorld as adjacent evidence and assertions that it is gated to `agent_harness_eval_required`.
- Added a focused `zhengxi-views` summary classification regression proving `SKILL.md`/`skill.yml` metadata maps only to bounded local lanes.
- Created rollback artifact `artifacts/self-evolution/github-growth-20260627T082310.585578Z-rollback.md` and rollback ref `refs/rollback/blackhole-agent/20260627T082309Z-skill-route-pass3-boundary`.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k "skill_route_discovery_pass3_selects_bounded_lane_per_profile"` passed.
- `python -m pytest tests/test_skill_routing.py -q -k "zhengxi_skill_metadata or current_window_skill_workflow"` passed.
- `python -m pytest tests/test_harness_eval.py -q -k "local_harness_eval_runs_pass_and_fail_fixtures_without_exporting_inputs"` passed.
- `python -m pytest tests/test_skill_routing.py -q -k skill_route_discovery` passed: 28 tests.
- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` passed: 9 tests.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 tests.

## Review Notes

- No self-model edit was made. The existing self-model already states the relevant behavior preference: local evolution is allowed when rollback-backed, validated, and explicit about uncertainty.
- The new pass-3 panel exports hashes for adjacent general-agent refs and does not export raw source URLs, upstream bodies, provider inputs, or executable commands beyond replay command labels.
- External skill activation, external agent activation, external harness execution, provider launch, and remote execution remain denied.
