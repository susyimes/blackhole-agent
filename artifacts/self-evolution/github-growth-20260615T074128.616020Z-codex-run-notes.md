# Codex Self-Evolution Run Notes

Source digest: `github-growth-20260615T074128.616020Z`

Rollback ref: `refs/blackhole-agent/rollback/20260615T074128.616020Z`

Evidence reviewed:
- https://github.com/cellebrite-labs/ghidra-rpc
- https://github.com/visa/visa-vulnerability-agentic-harness
- https://github.com/omnigent-ai/omnigent

Reusable pattern:
- `ghidra-rpc` documents deterministic runtime paths using an 8-character hash from the absolute project path, so concurrent or repeated daemon sessions do not collide.
- The safe local borrowing is artifact namespace hygiene for rollback refs, not adopting reverse-engineering daemon behavior.

Hypothesis:
- Adding a stable checkout-path namespace to rollback refs reduces collision risk across sibling worktrees at the same HEAD while preserving the existing explicit recovery commands.

Change made:
- Rollback refs now include `refs/blackhole-agent/rollback/<repo-hash>/...`, where `<repo-hash>` is the first 8 hex characters of the checkout path SHA-256.
- The focused rollback test now verifies the namespace is derived from the repository path.

Validation:
- `PYTHONPATH=src uv run pytest tests/test_github_growth.py::test_prepare_self_evolution_branch_writes_rollback_point -vv`
- Result: passed.

Review notes:
- `docs/self-model.md` was read and left unchanged. The current self-model still appears ornamental for this run, and the safer improvement was a concrete rollback reliability patch with a focused test.
- The local shell had a parent `VIRTUAL_ENV` pointing at `C:\Users\svmes\Documents\Playground\blackhole-agent\.venv`; validation pinned `PYTHONPATH=src` to this worktree.
