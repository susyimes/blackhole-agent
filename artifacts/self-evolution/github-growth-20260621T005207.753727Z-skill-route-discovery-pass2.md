# Skill Route Discovery Pass 2

Source digest: `github-growth-20260621T005207.753727Z`

Rollback point:

- `artifacts/rollback/20260621T005207Z-skill-route-discovery-pass2.txt`
- `refs/rollback/20260621T005207Z-skill-route-discovery-pass2`

Hypothesis: pass-2 skill-route discovery needs an operator-visible replay packet
for the selected next validation lane, not another standalone fixture. Public
skill/workflow evidence from FableCodex, COMPASS Skills, and Three.js Game
Skills supports only bounded local lanes: documentation, config, test, and
code_patch. Omnigent-style low-detail PR evidence remains supporting context
only and did not independently justify implementation.

Change:

- Added `next_pass_replay_packet` under `next_pass_handoff`.
- The packet carries selected `item_id` references, hashed candidate sources,
  queued bounded local lanes, replay commands, completion blockers, and denial
  flags.
- Documented the packet in `docs/skill-route-discovery.md`.
- Extended focused pass-2 harness assertions and existing exact handoff tests.

Self-model decision: left unchanged. The current self-model already favors
rollback-backed local behavior improvements while keeping external skill
activation and privacy leakage outside the allowed boundary.

Validation:

- `pytest tests/test_harness_eval.py -q -k "skill_route_discovery_pass2_fixture or capability_window_reports_in_progress or capability_window_completion"`: passed
- `pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`: passed
- `pytest tests/test_docs_contracts.py -q`: passed
- `pytest tests/test_harness_eval.py -q`: passed

Review notes:

- The packet is body-free and exports no raw source URLs, raw evidence URLs,
  raw target paths, upstream bodies, secrets, credentials, or private data.
- The packet does not permit runtime action, provider launch, remote execution,
  external harness execution, external skill code, or external skill activation.
