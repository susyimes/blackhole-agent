# Skill Route Pass 4 Lane Policy Inventory

Source digest: `github-growth-20260627T084310.628741Z`

Branch: `codex/blackhole-evolve/20260627T084448.644202-add-or-extend-local-tests-that-verify-repositori`

Rollback artifact: `artifacts/rollback/20260627T084310Z-skill-route-discovery-pass4.md`

Rollback ref: `refs/rollback/20260627T084310Z-skill-route-discovery-pass4`

## Evidence

- `https://github.com/MartinYeung5/threejs-game-skills`
- `https://github.com/TheLostRiver/threejs-game-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/mrkr/threejs-game-skills`

These public repositories continue the Three.js game skill route shape:
director or specialist skill bundles, scaffold/helper material, QA or browser
validation language, and optional provider/asset-generation pressure. The local
lesson remains bounded lane discovery, not upstream skill activation.

## Hypothesis

The pass-4 route already exposes final handoff, lane queue, consistency guard,
and secondary harness bridge surfaces. The useful completion improvement is a
single operator-visible policy inventory that ties final profile rows back to
the allowed lane set and proposal kinds without adding a new lane or action
path.

## Changes

- Added `final_lane_policy_inventory` to `skill_route_discovery` completion reports.
- The inventory is derived from the final route handoff manifest, route validation lane queue, current-window evidence gate, and secondary harness bridge.
- It reports allowed local lanes, proposal kinds, selected local lanes, evidence hash counts, replay-command hashes, profile rows, and blocked secondary harness status.
- Extended the pass-4 closure fixture and focused Python assertions to cover the inventory.
- Documented the panel in `docs/skill-route-discovery.md` and added a docs contract assertion.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` passed: 9 passed, 146 deselected.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 passed, 9 deselected.
- `python -m pytest tests/test_harness_eval.py -q -k local_harness_eval_runs_pass_and_fail_fixtures_without_exporting_inputs` passed: 1 passed, 154 deselected.
- `python -m pytest tests/test_harness_eval.py -q -k "completion_guard or final_route_handoff_manifest or route_validation_lane_queue or secondary_harness_bridge"` passed: 1 passed, 154 deselected.
- `python -m pytest tests/test_harness_eval.py tests/test_docs_contracts.py tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery` passed: 79 passed, 147 deselected.

## Self-Model

`docs/self-model.md` was read and left unchanged. This run had a concrete safe
behavior-path improvement, and the current self-model already supports
rollback-backed, locally validated evolution while keeping uncertainty explicit.

## Review Notes

- The new inventory is body-free and hashes replay commands instead of exporting command bodies.
- It does not add lanes, accept raw repository URLs, export upstream bodies, install upstream skills, execute external harnesses, launch providers, perform remote execution, or grant runtime action.
- `agent_harness_eval_after_local_corroboration` remains blocked as a later local evaluation path, not an activation token.
