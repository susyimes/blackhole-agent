# Codex Run Notes

Source digest: `github-growth-20260619T235207.483878Z`
Branch: `codex/blackhole-evolve/20260619T235307.656454-add-a-local-validation-test-for-skill-route-disc`
Rollback ref: `refs/blackhole/rollback/20260620T235206Z-codex_blackhole-evolve_20260619T235307.656454-add-a-local-validation-test-for-skill-route-disc`
Rollback artifact: `artifacts/rollback/20260620T235206Z-provider-runtime-control-pass4.txt`

## Evidence

- `https://github.com/omnigent-ai/omnigent/issues/866` reports a Homebrew Apple Silicon install where the `jiter` native extension fails linkage repair and keeps an unresolved `@rpath` install name. Reusable lesson: provider startup diagnostics should catch fragile native linkage metadata before runtime launch.
- `https://github.com/dongshuyan/compass-skills` and `https://github.com/majidmanzarpour/threejs-game-skills` remain evidence for keeping public skill repositories in bounded, classification-only lanes until local validation exists. Existing skill-route tests already cover that boundary, so this pass used the provider-runtime control window for an operator-visible preflight behavior.

## Hypothesis

If provider runtime preflight can classify Apple Silicon Homebrew linkage failures as body-free metadata, the supervisor can block fragile provider launch, show a recovery hint, and replay validation locally without exporting paths, install names, tokens, or provider output bodies.

## Changes

- Added `install_linkage` metadata evaluation to `provider_runtime_preflight`.
- Added `provider_install_linkage_unresolved` as a blocking failure mode with a recovery hint.
- Added a local fixture for an Omnigent-style Apple Silicon Homebrew `jiter` linkage failure.
- Extended the provider-runtime recovery summary fixture and tests so the new blocker appears in aggregate operator readiness.

## Validation

- `pytest tests/test_harness_eval.py -q -k provider_runtime_preflight` -> `12 passed, 76 deselected`
- `pytest tests/test_harness_eval.py -q -k provider_runtime_recovery_summary` -> `1 passed, 87 deselected`
- `pytest tests/test_harness_eval.py -q` -> `88 passed`
- `pytest -q` -> `331 passed`

## Review Notes

- No offensive behavior, abuse, unauthorized access, or privacy-leakage behavior was implemented.
- Proposal `p2-agent-harness-eval-smoke` remains review-only for offensive-behavior boundary reasons.
- Proposal `p3-install-preflight-apple-silicon` was implemented as a local metadata preflight only; it does not run `brew`, `otool`, relink tools, providers, or external commands.
- `docs/self-model.md` was read and left unchanged because it already supports provider/runtime preflight evolution and did not need a behavior-shaping revision for this run.
