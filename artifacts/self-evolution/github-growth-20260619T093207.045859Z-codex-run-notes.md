# Codex Run Notes

Source digest: `github-growth-20260619T093207.045859Z`

Selected proposal: `p1-headless-tool-roundtrip-harness`

Hypothesis:
Headless model streams can surface `function_call` or `tool_call` events that are observed by a harness but not
proved to enter the executable tool-routing path. A dry-run local dispatch report gives the controller a bounded
lane for validating those events before any activation or callable execution.

Rollback:
- Branch: `codex/blackhole-evolve/20260619T093323.384963-add-or-strengthen-a-local-harness-test-that-veri`
- Original HEAD: `755677a5d961bbd01d08625662c7edca4be5eb63`
- Local rollback ref: `refs/rollback/20260619T093206Z-skill-route-discovery-pass2`
- Artifact: `artifacts/rollback/20260619T093206Z-headless-function-call-roundtrip.txt`

Material actions:
- Added a metadata-only headless function-call dispatch report in `tool_routing.py`.
- Added a `headless_tool_roundtrip` local harness behavior that routes function-call events through descriptor policy
  decisions without executing tools.
- Added a local fixture covering a headless text event plus a `function_call` event and verifying the call is dispatched.
- Updated architecture notes and harness aggregate expectations.

Self-model decision:
Left `docs/self-model.md` unchanged. Its current preference already supports a rollback-backed, locally validated
behavior improvement over a documentation-only note, and this run did not produce evidence that the self-description
is misleading.

Validation:
- `python -m ruff format src\blackhole_agent\tool_routing.py src\blackhole_agent\harness_eval.py tests\test_tool_routing.py tests\test_harness_eval.py`
- `python -m pytest tests\test_tool_routing.py tests\test_harness_eval.py -q` -> 92 passed
- `python -m ruff check src\blackhole_agent\tool_routing.py src\blackhole_agent\harness_eval.py tests\test_tool_routing.py tests\test_harness_eval.py` -> passed

Review notes:
- The lane is intentionally dry-run only. It proves dispatch routing and privacy handling but does not execute a
  callable or validate a live provider stream.
- External proposal URLs were not re-crawled; the carried digest and proposal evidence were sufficient for a bounded
  local harness improvement.
