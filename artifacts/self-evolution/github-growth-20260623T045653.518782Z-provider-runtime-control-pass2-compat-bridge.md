# Self-Evolution Run Notes

Source digest: `github-growth-20260623T045653.518782Z`
Branch: `codex/blackhole-evolve/20260623T045753.682159-prototype-a-local-provider-config-preflight-chec`
Rollback artifact: `artifacts/rollback/20260623T125823Z-provider-runtime-control-pass2-compat-bridge.md`
Rollback ref: local recovery artifact only for this kernel pass

## Evidence

- `https://github.com/omnigent-ai/omnigent/pull/990`: reviewed as the primary evidence. The PR describes a backward-compat harness where old runner/host are pinned together while server/client/tests stay current. The reusable local lesson is to require explicit component pinning, env neutralization, neutral cwd, and replay proof before launch.
- `https://github.com/omnigent-ai/omnigent`: reviewed only as context that the upstream project is an agent framework/harness.
- `https://github.com/baskduf/FableCodex`: opened as carried window context, but no behavior was copied from it in this pass.

No upstream code, raw provider config, token, credential, private chat, install command, provider launch, remote execution, promotion, push, or restart was executed.

## Hypothesis

Provider/runtime compatibility migrations should produce body-free, locally replayable diagnostics before a runner starts. In a Config-2-style bridge, old runner/host to current server launch should remain blocked until the runner/host pin, version backstop, environment neutralization, neutral cwd, and local resolution proof are all visible as metadata.

## Change

- Extended `provider_runtime_preflight` with `runner_compat` checks for old runner/host to current server compatibility bridges.
- Added stable blocked failure classes for missing bridge pins, runner/host colocation mismatch, missing env/cwd neutralization, and missing local replay proof.
- Added recovery hints and supervisor replay integration without exporting interpreter paths, version strings, cwd, environment values, or raw config bodies.
- Added a local harness fixture and inline regression coverage for blocked and ready compatibility-bridge cases.
- Documented the compatibility bridge contract in `docs/architecture.md`.

## Self-Model

Read `docs/self-model.md` before choosing the patch. It already prefers rollback-backed, locally validated provider/config preflight improvements and keeps permissions external, so it was left unchanged.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k provider_runtime_preflight` passed: 22 passed, 110 deselected.
- `python -m pytest tests/test_harness_eval.py -q -k "local_harness_eval_runs_pass_and_fail_fixtures or provider_runtime_recovery_summary"` passed: 2 passed, 130 deselected.
- `python -m pytest tests/test_docs_contracts.py -q` passed: 9 passed.
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py` passed.

## Review Notes

- Proposal `p2-provider-config-backcompat-preflight` had `Autonomous local apply: False` and a privacy-leakage review gate, so this pass did not apply upstream runner migration code or expose any provider/runtime body. It implemented a local diagnostic guard and tests only.
- Provider runtime launch, remote execution, promotion, push, and restart remain denied by the preflight/supervisor replay surfaces.
