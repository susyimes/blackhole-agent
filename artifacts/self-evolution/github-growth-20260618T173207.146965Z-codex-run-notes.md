# Codex Run Notes

Source digest: `github-growth-20260618T173207.146965Z`
Prepared branch: `codex/blackhole-evolve/20260618T173248.607427-add-or-adapt-a-local-agent-harness-evaluation-la`
Rollback ref: `refs/rollback/20260618T173248Z-agent-harness-eval-local-validation`
Rollback artifact: `artifacts/rollback/20260618T173248Z-agent-harness-eval-local-validation.txt`

## Evidence Reviewed

- `https://github.com/visa/visa-vulnerability-agentic-harness`

Reusable lesson: public agent harness projects are useful as frozen local evaluation patterns when their outputs are structured, replayable, and bounded. The Visa repository is security-adjacent; this run used it only as boundary evidence and did not implement vulnerability scanning, exploit validation, or unauthorized-access behavior.

## Hypothesis

Exposing route-hint lane policy in the local `proposal_interpretation` harness adapter and adding a frozen `agent_harness_eval` fixture will make proposal interpretation drift easier to catch. The fixture should verify strict JSON, item-id-only evidence refs, and the rule that `agent_harness_eval` maps only to `documentation`, `test`, or `code_patch`.

## Changes

- Added `route_hint_policy` to proposal interpretation adapter output.
- Added a frozen harness adapter fixture for `agent_harness_eval` that accepts a benign `test` proposal, rejects a `config` lane candidate, and rejects URL evidence refs.
- Updated harness tests to include the new fixture and direct adapter JSON contract.
- Left `docs/self-model.md` unchanged because it already states the preference used here: rollback-backed local validation over report-only work, with security-adjacent behavior kept at the safety boundary.

## Validation

- `uv run pytest tests/test_harness_eval.py tests/test_proposal_eval.py` passed: 46 tests.
- `uv run pytest` passed: 240 tests.

## Review Notes

- The aggregate local harness report still exports assertion results, not full fixture output, preserving the existing privacy boundary.
- The Visa evidence was reviewed at repository README level only; uncertainty is encoded in the fixture text and no upstream runtime behavior was copied.
