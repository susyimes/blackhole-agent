# Codex Run Notes

Source digest: `github-growth-20260616T105704.576924Z`

## Proposal Selected

Selected `p2-fastcontext-context-budget-memory-tests`.

The FastContext evidence describes a repository-exploration pattern that keeps broad exploration outside the main agent trajectory and returns compact file-line evidence to reduce context burden. The local lesson for this repository is to make constrained proposal evidence packages replayable and testable, including selected IDs, truncated IDs, truncation diagnostics, and self-model preservation under budget pressure.

Proposals `p1-omnigent-provider-tool-routing-preflight` and `p3-vulnerability-harness-safe-validation-mode` were left review-only because their validation gates are privacy-leakage and offensive-behavior human review.

## Hypothesis

If the replay harness can assert context-budget preflight metadata, including self-model truncation and missing-detail uncertainty, frozen proposal cases will catch regressions where generated proposals cite truncated evidence or lose bounded memory context under constrained packages.

## Changes

- Extended `src/blackhole_agent/proposal_eval.py` so replay cases can provide a bounded `self_model_snapshot`, set `max_self_model_chars`, expose `context_budget_preflight`, and assert expected preflight subsets.
- Added `tests/fixtures/proposal_replay/fastcontext_budget_memory_pressure.json` covering selected items, truncated items, field truncation, `missing_detail_risk: true`, self-model truncation, and rejection of a truncated `evidence_refs` value.
- Updated the benign replay fixture to assert `missing_detail_risk: false` and non-truncated self-model state.
- Updated replay manifest and proposal replay tests for the expanded fixture suite.

## Self-Model Decision

Read `docs/self-model.md` and left it unchanged. Its current preference already supports rollback-backed, locally validated test and replay-harness improvements while keeping privacy leakage and offensive behavior review-only.

## Rollback

Created rollback ref `refs/blackhole-rollback/github-growth-20260616T105704.576924Z` at `e8df0eb31000cf406adf3d8aecf1d9537f78be31`.

Rollback artifact: `artifacts/self-evolution/github-growth-20260616T105704.576924Z-rollback.md`.

## Material Actions

- Reviewed allowed external evidence URL: `https://github.com/microsoft/fastcontext`.
- Created local rollback ref and rollback artifact before source edits.
- Modified local source, fixtures, and tests only inside this repository.
- No tokens, credentials, secrets, private keys, private chats, PII, or personal data were printed, logged, uploaded, or shared.
- No offensive, exploit, malware, phishing, exfiltration, or unauthorized-access behavior was implemented.

## Validation

Command:

```powershell
uv run --extra dev python -m pytest tests/test_proposal_eval.py tests/test_github_growth.py -q -k "proposal_replay or context_budget or proposal_evidence_package or pr_event_digest_pressure"
```

Result: passed, `14 passed, 56 deselected`.

Note: `uv` warned that the outer `VIRTUAL_ENV` did not match this worktree's `.venv` and ignored it.

## Review Notes

- The provider/tool-routing preflight proposal remains review-only for this run because the supplied proposal explicitly marks it as non-autonomous with a privacy-leakage human-review gate.
- The vulnerability harness proposal remains review-only for this run because the supplied proposal explicitly marks it as non-autonomous with an offensive-behavior human-review gate.
- The new FastContext replay fixture is synthetic. It validates local replay invariants and selected-item-only citation behavior, not external model quality.
