# Codex Run Notes

Source digest: `github-growth-20260616T060601.112044Z`
Run branch: `codex/blackhole-evolve/20260616T060652.143885-add-or-extend-a-local-harness-style-validation-p`
Rollback artifact: `artifacts/rollback/20260616T060738Z-context-budget-diagnostics.txt`
Rollback ref: `refs/blackhole-rollback/20260616T060738Z/codex_blackhole-evolve_20260616T060652.143885-add-or-extend-a-local-harness-style-validation-p`

## Evidence And Hypothesis

Selected proposal: `p2_fast_context_budget_diagnostics`.

Reusable lesson: proposal interpretation should expose why evidence entered or missed the bounded context before the interpretation step runs. The local hypothesis is that item-level, redacted context-budget diagnostics will make proposal runs easier to replay and debug without copying evidence text, URLs, or secret-shaped content into preflight artifacts.

External evidence review was limited to the proposal URLs. FastContext describes delegated repository exploration that returns compact focused evidence and reduces the main agent's context burden, which supports adding local diagnostics for context selection. The local implementation was validated against frozen synthetic evidence packages.

## Changes

- Added redacted `item_selection_diagnostics` metadata to proposal evidence packages.
- Extended context-budget preflight output with selected item IDs, truncated item IDs, excluded item count, selection strategy, and per-item decisions.
- Preserved the existing risk-first, confidence-second ranking behavior while keeping invalid digest items visible as excluded preflight metadata.
- Added tests for selected items, max-item truncation, text truncation metadata, risk-flag ordering, confidence ordering, empty input, excluded non-object input, and URL redaction.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference for reversible local implementation already matched this run, and no evidence from the change showed that the file was actively behavior-shaping beyond the runtime task prompt.

## Review Notes

- `p1_harness_validation_profile` stayed review-only because its validation gate is `offensive-behavior-human-review`.
- `p3_provider_config_preflight` stayed review-only because its validation gate is `privacy-leakage-human-review`.
- The new diagnostics intentionally report IDs and metadata only; they do not include source URLs, summaries, relevance text, or self-model content.

## Validation

- `PYTHONPATH=src uv run pytest tests/test_github_growth.py -q -k "context_budget or proposal_evidence_package or llm_proposals_preserve"`: passed, 6 tests.
- `PYTHONPATH=src uv run ruff check .`: passed.
- `PYTHONPATH=src uv run pytest -q`: passed, 66 tests.

The first focused test attempt without `PYTHONPATH=src` imported a stale installed package and failed before exercising the edited source. The supervisor-style validation environment with `PYTHONPATH=src` was used for the accepted checks.
