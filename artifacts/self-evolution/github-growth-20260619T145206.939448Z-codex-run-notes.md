# Codex Run Notes

Source digest: github-growth-20260619T145206.939448Z

## Evidence

- Reviewed https://github.com/omnigent-ai/omnigent/pull/834.
- Reusable lesson: keep harness tests aligned to operator-visible behavior, and mark flaky teardown/status observations as non-load-bearing rather than letting stale markers decide pass/fail.
- Privacy boundary: no tokens, credentials, private chats, PII, or artifact bodies were exposed. New output hashes observation IDs and replay/report artifact paths.

## Hypothesis

The local `agent_workflow_route` lane should expose one runner workflow end to end: intake, mid-flight state, recovery, replay, and report artifacts. It should also distinguish durable observations from flaky teardown probes so future runner-control changes can be replayed without overfitting to brittle status text.

## Change

- Added a `runner_harness_control_plane` summary to `agent_workflow_route`.
- Added observation classification for load-bearing vs non-load-bearing probes.
- Added a replay fixture that records a reliable mid-flight observation and an unreliable non-load-bearing teardown observation.
- Documented the control-plane summary contract in `docs/architecture.md`.
- Created rollback artifact `artifacts/rollback/20260619T145206Z-runner-harness-control.txt` and rollback ref `refs/rollback/20260619T145206Z-runner-harness-control`.

## Validation

- `uv run pytest tests/test_harness_eval.py -q -k agent_workflow_route` passed: 4 passed, 75 deselected.
- `uv run pytest tests/test_harness_eval.py -q` passed: 79 passed.
- `uv run ruff check src\\blackhole_agent\\harness_eval.py tests\\test_harness_eval.py` passed.
- `git diff --check` passed with only Windows line-ending warnings.

## Review Notes

- Self-model left unchanged. It already states the local-evolution preference and narrow privacy boundary used by this run.
- No remote writes, restarts, provider launches, or external harness execution were performed.
- Proposal 10865479581-1 remains review-only for privacy-leakage behavior; this run only improved local body-free validation machinery.
