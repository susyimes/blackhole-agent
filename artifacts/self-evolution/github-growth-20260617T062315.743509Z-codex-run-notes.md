# Codex Run Notes

Source digest: `github-growth-20260617T062315.743509Z`

Branch: `codex/blackhole-evolve/20260617T062456.214431-add-or-improve-a-locally-gated-issue-triage-work`

Rollback point:

- Ref: `refs/rollback/20260617T062315Z-issue-triage-workflow-route`
- Artifact: `artifacts/rollback/20260617T062315Z-issue-triage-workflow-route.txt`
- Original HEAD: `128df83e150baf20fbcce8342a48d6b01ea243d6`

Evidence reviewed:

- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/pull/448`

Hypothesis:

The Omnigent issue-triage workflow suggests a useful local pattern for this repository: classify issue-like input into structured labels, priority, and next actions, but keep remote mutation behind an explicit controller approval gate. The evidence also showed review concerns around undefined workflow state and prompt/tool boundary risk, so this change keeps the classifier deterministic, local, allowlisted, and replayable.

Self-model decision:

Left `docs/self-model.md` unchanged. Its current preference for rollback-backed, locally validated behavior changes matched this run, and no evidence showed that the self-model was blocking or misdirecting implementation.

Changed behavior:

- Extended local issue triage records with `recommendation` fields for allowlisted labels, priority, next actions, and duplicate references.
- Added `mutation_plan` output that defaults to `dry_run` with no commands unless `allow_remote_mutation=True`.
- When remote mutation planning is approved, the module emits replayable `gh issue` command arrays only; it does not execute them.
- Added focused tests for dry-run behavior, approved mutation planning, duplicate comment planning, and persisted recommendation shape.

Validation:

- `uv run pytest tests/test_issue_triage.py -q` -> passed, 6 tests.
- `uv run pytest -q` -> passed, 144 tests.
- `uv run ruff check .` -> passed.

Review notes:

- Proposal 002 remains review-only because fork PR automation/security routing can cross into abuse or unauthorized-access review territory if implemented loosely.
- No remote GitHub state was mutated by this run.
