# Self-Evolution Run Notes

Source digest: `github-growth-20260619T151207.105362Z`
Capability theme: `runner-harness-control`
Branch: `codex/blackhole-evolve/20260619T151300.832093-create-a-local-validation-lane-for-agent-harness`

## Rollback

- Local rollback ref: `refs/rollback/20260619T151300Z-runner-harness-control-pass2`
- Rollback artifact: `artifacts/rollback/20260619T151300Z-runner-harness-control-pass2.txt`
- Original HEAD: `c4737ce0b8194496d5eda18c888a80edb111f86f`

## Hypothesis

Runner and harness control improves more from an operator-visible recovery and replay contract than from another standalone fixture. The existing `agent_workflow_route` control plane already records intake, mid-flight state, rollback, replay, and report readiness. Pass 2 should make recovery actionable by requiring an explicit handoff when a fixture declares it, while continuing to avoid raw command, path, rollback ref, or artifact export.

## Change

- Added `recovery_handoff` evaluation to `agent_workflow_route`.
- Added a `control_plane.recovery` surface with readiness, blockers, command counts, command hashes, replay readiness, validation command counts, and privacy booleans.
- Required recovery handoff failures now produce `recovery_handoff_incomplete` and keep the route recoverable when rollback exists.
- Updated the control-plane replay fixture and tests to cover a complete handoff and an incomplete handoff.
- Updated architecture documentation for the stronger control-plane contract.

## Self-Model Decision

`docs/self-model.md` was left unchanged. Its current preference for rollback-backed, locally validated behavior changes already matches this run; no new self-description was needed.

## Validation

- `uv run pytest tests/test_harness_eval.py -q -k "agent_workflow_route or local_harness_eval_runs"`: passed, 5 passed.
- `uv run pytest -q`: passed, 322 passed.
- `uv run ruff check .`: passed.

## Review Notes

- The Visa agent-harness proposal remains review-only for offensive-behavior boundaries; this run implemented only local, dry-run recovery/replay validation.
- Raw recovery commands, replay commands, rollback refs, and artifact paths are hashed or omitted from structured reports.
- No network calls, provider launches, restarts, pushes, or remote execution were performed.
