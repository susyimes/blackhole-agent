# Self-Evolution Run Notes

Source digest: `github-growth-20260623T105653.035465Z`
Branch: `codex/blackhole-evolve/20260623T105758.169972-add-a-local-regression-test-for-resolver-gate-ha`
Rollback ref: `refs/rollback/20260623T105653Z-nested-tool-parent-resolver-gate`
Rollback artifact: `artifacts/rollback/20260623T105653Z-nested-tool-parent-resolver-gate.txt`

## Selected Proposal

Selected `p1-nested-tool-parent-resolver-gate`, with a small behavior-observability extension from `p2-nested-tool-parent-resolver-patch`.

Evidence URL: `https://github.com/omnigent-ai/omnigent/issues/1014`

Evidence reviewed during the run: the issue describes a root-only `__web_researcher` gate where callers pass the bundle root even though the `web_fetch` owner can be a direct or transitive nested sub-agent. The recorded reproduction shape is root without `web_fetch`, nested sub-agent with `web_fetch`, and a resolver miss that falls back to the coordinator clone path.

Reusable lesson: resolver recovery gates for generated or implicit tools should prove that eligible tool parents can be found below a bundle root, not only at the root or at one shallow child. The local replay surface should show bounded depth metadata without exporting private agent names.

## Hypothesis

The existing `__web_researcher` mock workflow gate already handled a shallow nested `web_fetch` parent. Extending the regression to a two-level nested parent and emitting body-free depth metadata makes the resolver gate harder to regress and more operator-visible during skill-route completion replay.

## Changes

- Extended `evaluate_web_researcher_resolution_gate` with `parent_path_depth`, `matching_parent_depth`, and `nested_web_fetch_depths`.
- Added `bundle_spec_depth_by_path` to report the matched parent depth without exporting raw names.
- Deepened the positive nested `web_fetch` fixture to require traversal through an intermediate parent before accepting reconstruction.
- Kept the missing-parent denial case and asserted that no nested `web_fetch` owner depth is reported.

## Self-Model

Read `docs/self-model.md` and left it unchanged. Its current preference for rollback-backed, locally validated behavior changes matched this run; the useful update belonged in executable harness behavior and regression coverage rather than the self-description.

## Material Actions

- Created rollback ref and rollback artifact before editing.
- Modified local source and tests only.
- Did not fetch upstream code, install external skills, launch providers, run remote harnesses, restart the kernel, or expose private fixture names.

## Validation

- `uv run pytest tests/test_harness_eval.py -q -k "web_researcher or resolver_miss"` passed: 4 passed, 137 deselected.
- `uv run ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py` passed.

## Review Notes

- The upstream issue was treated as route-shape evidence only. No upstream code or issue body was adopted.
- The depth metadata is intentionally numeric and body-free; hashed path elements already cover path identity without exporting raw names.
