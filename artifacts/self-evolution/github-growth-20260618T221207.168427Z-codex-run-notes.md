# Codex Run Notes

Source digest: `github-growth-20260618T221207.168427Z`
Branch: `codex/blackhole-evolve/20260618T221307.423898-document-a-local-skill-route-discovery-inventory`

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`

The reviewed evidence remained repository-level and README-level. It supports
bounded local skill/workflow route controls, not installation, execution,
profile activation, asset generation, or upstream skill adoption.

## Hypothesis

Skill/workflow repository evidence should be constrained before any local
implementation lane is accepted. The existing controller policy already mapped
`skill_route_discovery` to documentation, config, test, and code_patch; the
improvement makes that policy independently preflighted before proposal
candidate normalization.

## Rollback

- Rollback ref:
  `refs/blackhole-agent/rollback/20260618T221207Z-skill-route-preflight`
- Rollback artifact:
  `artifacts/rollback/20260618T221207Z-skill-route-preflight.txt`

## Changes

- Added `build_route_hint_policy_preflight` to validate selected route hints
  and the controller-owned lane table before proposal interpretation accepts
  candidates.
- Wired proposal response review to fail closed when the route-hint preflight
  detects `skill_route_discovery` lane drift.
- Added replay tests for the valid policy and an unsafe `runtime_execution`
  lane.
- Documented the route-hint policy preflight in `docs/skill-route-discovery.md`.
- Left `docs/self-model.md` unchanged because its current preference already
  supports rollback-backed, locally validated behavior changes outside the
  narrow safety boundary.

## Validation

- `python -m pytest tests/test_proposal_eval.py -q -k "skill_route_discovery or proposal_replay_suite_accepts"`
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`
- `python -m pytest tests/test_proposal_eval.py -q`
- `python -m ruff check src/blackhole_agent/proposal_synthesis.py tests/test_proposal_eval.py`

All validation passed.

## Review Notes

No offensive behavior, abuse, unauthorized access, or privacy-leakage route was
added. The change remains metadata-only and does not introduce new approved
URLs, secret handling, external execution, push, promotion, or restart behavior.
