# Codex Run Notes

Source digest: `github-growth-20260623T093652.859112Z`

Capability window: `skill-route-discovery`, pass 4 of 4.

## Hypothesis

The active proposal stream carried a concrete Omnigent resolver-miss lesson:
`__web_researcher` reconstruction should not decide from the bundle root alone
when the effective `web_fetch` owner can be nested. The local reusable
improvement is a bounded harness lane that proves nested ownership before a
resolver miss is treated as safely reconstructed, while still blocking parent
clone fallback when no `web_fetch` owner is found.

## Evidence Choice

Primary evidence reviewed:

- `https://github.com/omnigent-ai/omnigent/issues/1014`
- `https://github.com/omnigent-ai/omnigent/pull/817`

The reviewed issue describes a root-without-`web_fetch` plus nested-with-
`web_fetch` topology where a root-only gate misses the real owner. I did not
rerun broad trend discovery or inspect unrelated proposal URLs.

## Rollback

Rollback artifact:
`artifacts/rollback/20260623T093652Z-web-researcher-nested-web-fetch-resolution.md`

Rollback ref:
`refs/rollback/20260623T093652Z-web-researcher-nested-web-fetch-resolution`

Original HEAD:
`8bf367e257576fde4986e2f431e4a33383ff54d0`

## Changes

- Extended the mock named sub-agent resolver-miss diagnostics with a
  `web_researcher_gate` block.
- Added bundle traversal helpers that can resolve a supplied nested parent path
  and detect `web_fetch` on nested parents without exporting raw agent names.
- Treat `__web_researcher` resolver misses as reconstructed only when the root
  or resolved nested parent owns `web_fetch`.
- Kept unresolved nested misses blocked as parent-clone fallback.
- Added accepting and blocking regression tests for nested `web_fetch` parent
  lookup.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k "web_researcher or resolver_miss or parent_clone"`
- `python -m pytest tests/test_harness_eval.py -q -k "mock_llm_workflow_route"`
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py`

All validation passed.

## Self-Model

`docs/self-model.md` was read and left unchanged. It already matched this run:
prefer a rollback-backed local behavior/test improvement over another
validation-report-only artifact, while keeping raw names and private content out
of exported diagnostics.

## Review Notes

This is a harness/modeling improvement, not live external runner activation. The
new diagnostics intentionally expose hashes and booleans only; raw parent,
child, session, prompt, and response bodies remain denied.
