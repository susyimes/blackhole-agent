# Codex Run Notes

Source digest: `github-growth-20260618T084044.013080Z`
Branch: `codex/blackhole-evolve/20260618T084134.589530-add-a-local-skill-route-discovery-mapping-layer-`
Starting HEAD: `00e014eb37af99af0f81e4d54dde2a15f7de9362`
Rollback ref: `refs/rollback/20260618T084043Z-skill-route-discovery-mapping-layer`
Rollback artifact: `artifacts/rollback/20260618T084043Z-skill-route-discovery-mapping-layer.txt`

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`

The reviewed public repositories expose Codex or agent skill packages with workflow gates, local state, skill ecosystems,
director routing, installers, scripts, scaffolds, and QA materials. The reusable local lesson is route-shape discovery,
not direct installation or execution.

## Hypothesis

External skill/project evidence should have a deterministic proposal-lane mapping layer after disabled registry
classification. Recognized skill evidence may become local documentation, config, test, or code_patch proposals, while
unsupported lane hints are downgraded and actionful or unsafe candidates are rejected before autonomous application.

## Changes

- Added `build_skill_route_discovery_proposal_lane_map` in `src/blackhole_agent/skill_routing.py`.
- Added regression tests for bounded proposal lanes, unsupported-lane downgrade, and actionful/unsafe rejection in
  `tests/test_skill_routing.py`.
- Documented the proposal-lane map in `docs/skill-route-discovery.md`.

## Self-Model

Read `docs/self-model.md` before choosing the change. Left unchanged: its current preference already supports
rollback-backed, locally validated behavior changes and the narrow safety boundary used by this run.

## Validation

- `python -m pytest tests\test_skill_routing.py` passed: 20 tests.
- `python -m pytest` passed: 220 tests.
- `python -m ruff check src\blackhole_agent\skill_routing.py tests\test_skill_routing.py` passed.

## Review Notes

- The mapping layer is intentionally metadata-only and returns `runtime_action: none`.
- Candidates with install, enable, run, execute, clone-and-run, delete, non-public URLs, decorated URLs, or other
  non-lane validation errors produce no proposal lanes.
- Unsupported lane hints can still preserve allowed local lanes, but the downgrade is explicit and auditable.
