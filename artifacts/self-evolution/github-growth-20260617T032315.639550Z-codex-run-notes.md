# Codex Run Notes

Source digest: `github-growth-20260617T032315.639550Z`

Proposal selected: `p1-agent-harness-local-eval-adapter`

Evidence reviewed:
- `https://github.com/ApodexAI/AgentHarness`: public benchmark harness with smoke/full runs, subprocess isolation, progress checks, and aggregate accuracy.
- `https://github.com/samarailly51-pixel/opencode-harness`: offline mock eval, reproducible eval suites, structured reports, traces, dashboards, and failure taxonomy.

Hypothesis:

A small local fixture adapter inside the existing privacy-preserving harness module will make blackhole-agent behavior checks more replayable and controller-readable without requiring network access, provider credentials, or remote execution.

Rollback:

- Created local rollback ref: `refs/rollback/20260617T032315Z-local-eval-adapter`
- Wrote rollback artifact: `artifacts/rollback/20260617T032315Z-local-eval-adapter.txt`

Changes:

- Added deterministic local harness eval dataclasses and runner functions in `src/blackhole_agent/harness_eval.py`.
- Added one passing and one intentionally failing local eval fixture under `tests/fixtures/local_harness_eval/`.
- Extended harness tests to confirm pass/fail counts, assertion failure details, stable input hashes, and no raw fixture body export.
- Documented the local eval adapter in `docs/architecture.md`.
- Left `docs/self-model.md` unchanged because its current preference for rollback-backed, locally validated behavior changes matched this run.

Validation:

- `uv run pytest tests/test_harness_eval.py` passed: 3 tests.
- `uv run ruff check .` passed.
- `uv run pytest` passed: 124 tests.

Review notes:

- The new adapter currently supports the `harness_run_summary` behavior only. That is deliberate: it gives the controller a stable first fixture path while leaving future behavior adapters explicit and testable.
- Provider/config/token preflight changes remained review-only for this run because proposal `p2-provider-config-token-preflight` crossed the privacy-leakage review gate.
