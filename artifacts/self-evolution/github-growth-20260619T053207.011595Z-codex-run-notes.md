# Codex Run Notes

Source digest: `github-growth-20260619T053207.011595Z`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent/pull/590`
  - Reusable lesson: policy verdicts that carry redacted/sensitive data need replayable tests that prevent later state from silently changing the user-visible boundary.
  - Local boundary: privacy-leakage behavior remains review-only; no sensitive values were logged or introduced.
- `https://github.com/omnigent-ai/omnigent/pull/779`
  - Reusable lesson: auth/routing propagation fixes need typed, test-backed paths and should not silently drop adjacent settings.
  - Local boundary: offensive or unauthorized-access adjacent behavior remains review-only.
- `https://github.com/omnigent-ai/omnigent`
  - Reusable lesson: push-derived lessons are usable only when commit-message clusters include clear test or CI evidence.

## Hypothesis

Proposal replay should not merely require any `*-human-review` gate for hard safety flags. It should preserve the exact controller-owned review gate for each boundary so privacy-leakage routes cannot drift into the offensive gate, and offensive-behavior routes cannot drift into the privacy gate.

## Changes

- Tightened `collect_safety_boundary_failures` to require exact hard-boundary review gates.
- Added a current-wake Omnigent replay fixture that keeps PR #590 and PR #779 review-only while accepting the mock e2e push lesson as a local test-pattern candidate.
- Updated the proposal replay manifest and benchmark expectations.
- Added a regression test for wrong-boundary-gate drift.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already says privacy leakage and offensive behavior stay review-only, while rollback-backed local validation may proceed for other changes. This run made that preference more executable in tests rather than adding another self-description paragraph.

## Rollback

Rollback artifact: `artifacts/rollback/20260619T053206Z-omnigent-policy-boundary.txt`

Rollback ref: `refs/rollback/20260619T053206Z-omnigent-policy-boundary`

## Validation

- `uv run pytest tests/test_proposal_eval.py` passed: 18 tests.
- `uv run pytest tests/test_github_growth.py tests/test_proposal_eval.py` passed: 100 tests.

## Review Notes

- Privacy-leakage and offensive-behavior proposals from this wake were kept review-only.
- No auth propagation, exploit, exfiltration, unauthorized-access, token logging, credential printing, or sensitive-data artifacting behavior was implemented.
- External evidence review was limited to the proposal URLs.
