# Skill Route Discovery Pass 4

Source digest: `github-growth-20260624T101356.474819Z`

Rollback point:

- Artifact: `artifacts/rollback-20260624T101354Z-skill-route-discovery-pass4.md`
- Ref: `refs/blackhole-rollback/20260624T101354Z-skill-route-discovery-pass4`
- Original HEAD: `86bf6417be02a34b513bbdc37dfb804a650fd895`

## Evidence Reviewed

- `https://github.com/majidmanzarpour/threejs-game-skills`: public skill package with `skills/`, helper scripts, install paths, game/front-end QA, scaffold, and provider-key boundaries.
- `https://github.com/dongshuyan/compass-skills`: public skill ecosystem evidence for profile/state handoff and route metadata.
- `https://github.com/omnigent-ai/omnigent`: public general agent framework and meta-harness evidence for policy, sandbox, multi-agent, and provider coordination signals.

Reusable lesson: skill-like public repositories can provide route and validation signals, but completion should remain a rollback-backed local workflow. Game/front-end skills and profile handoff skills should surface selected local lanes and replay commands without installing upstream code, executing helper scripts, exporting raw bodies, launching providers, or restarting the kernel.

## Hypothesis

The theme already had classification and lane tests. The next useful behavior is an operator-visible completion workflow that ties bounded skill-route lanes to rollback evidence, replay commands, privacy review, and external-supervisor handoff. This makes pass completion inspectable without broadening runtime authority.

## Changes

- Added `completion_workflow` to `build_skill_route_discovery_proposal_lane_map`.
- Added `_skill_route_discovery_completion_workflow`, which reports candidate readiness, selected lanes, validation targets, replay commands, required evidence, privacy-review status, rollback requirements, and supervisor-only handoff metadata.
- Extended the current-window skill-route test to verify the completion workflow remains bounded to documentation/config/test/code_patch validation and denies external skill activation, harness execution, provider launch, remote execution, push/promotion, and restart.
- Documented the completion-handoff rule in `docs/upstream-evidence-interpretation.md`.
- Left `docs/self-model.md` unchanged. It already matches this run's policy: reversible local evolution is allowed, and unsafe/offensive/privacy-leakage routes stay bounded or review-only.

## Validation

- `uv run --extra dev ruff check src/blackhole_agent/skill_routing.py tests/test_skill_routing.py` passed.
- `PYTHONPATH=src uv run --extra dev python -m pytest tests/test_skill_routing.py -q -k "skill_route_discovery_current_window_matrix_keeps_profile_lanes_bounded or game_frontend or mixed_codex_agent_workflow"` passed: 3 tests.
- `PYTHONPATH=src uv run --extra dev python -m pytest tests/test_skill_routing.py -q` passed: 33 tests.
- `PYTHONPATH=src uv run --extra dev python -m pytest tests/test_harness_eval.py -q -k "agent_harness_eval_lane_maps_general_agent_project_claims_before_activation or skill_route_discovery_lane_fixture_bounds_evidence_before_activation"` passed: 2 tests.

Initial `uv run python -m pytest ...` without dev extras failed because this worktree venv did not yet have `pytest`; rerunning with `--extra dev` passed.

## Review Notes

- No upstream skill code was installed, loaded, executed, or activated.
- No provider runtime, external harness, remote execution, push, promotion, or restart was performed.
- The new completion workflow intentionally exports only body-free metadata, hashes, counts, and local replay commands.
