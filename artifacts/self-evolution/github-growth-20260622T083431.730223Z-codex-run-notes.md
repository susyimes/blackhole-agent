# Self-Evolution Run Notes

- Source digest: `github-growth-20260622T083431.730223Z`
- Branch: `codex/blackhole-evolve/20260622T083540.499016-add-or-strengthen-local-validation-for-provider-`
- Theme: `skill-route-discovery`
- Capability slice: convert skill and route evidence into bounded local lanes that can be validated before activation.
- Pass: 3 of 4
- Rollback ref: `refs/rollback/20260622T083430Z-skill-route-discovery-pass3`
- Rollback artifact: `artifacts/rollback/20260622T083430Z-skill-route-discovery-pass3.txt`

## Evidence

- `https://github.com/dongshuyan/compass-skills`: public skill ecosystem with local skills, state, handoff, and profile workflows. Reusable lesson: skill ecosystem evidence should become bounded local state/config validation before any profile or memory behavior.
- `https://github.com/baskduf/FableCodex`: Codex/workflow evidence. Reusable lesson: mixed Codex workflow evidence stays in `skill_route_discovery_first` before any broader harness or workflow lane.
- `https://github.com/lyra81604/zhengxi-views`: domain skill evidence. Reusable lesson: domain-specific skill packages can inform local route profiles without granting runtime action.
- `https://github.com/omnigent-ai/omnigent/issues/940`: provider/runtime credential-family evidence. Reusable lesson: adjacent provider runtime work remains a preflight concern and does not expand skill-route activation.

## Hypothesis

The existing route-discovery lane already chooses `next_validation_step`, but the selected handoff should also tell the supervisor which local promotion proof must exist before activation. A body-free `promotion_proof` packet on activation rows and the selected next step makes the pass-3 handoff more operator-visible without exporting raw target paths or granting runtime authority.

## Changes

- Added `SKILL_ROUTE_DISCOVERY_LOCAL_ARTIFACT_TARGETS` and `promotion_proof` output in `src/blackhole_agent/skill_routing.py`.
- `local_activation_targets.rows[*]` and top-level `next_validation_step` now include required proof categories and hashed target paths for the selected bounded lane.
- `next_validation_step` now explicitly denies raw target path export.
- Extended `tests/test_skill_routing.py` and `tests/test_docs_contracts.py`.
- Documented the selected-step proof contract in `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "current_window_matrix or proposal_lane_map_downgrades or proposal_lane_map_rejects"`: passed, 3 passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery_doc_records_bounded_matrix`: passed, 1 passed.
- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane`: passed, 9 passed.
- `python -m pytest tests/test_skill_routing.py tests/test_docs_contracts.py -q -k skill_route_discovery`: passed, 21 passed.
- `python -m pytest tests/test_skill_routing.py tests/test_harness_eval.py tests/test_docs_contracts.py -q`: passed, 158 passed.
- `python -m compileall -q src\blackhole_agent\skill_routing.py`: passed.

## Review Notes

- No external skill code was installed, cloned, run, enabled, or activated.
- No provider runtime was launched.
- Raw upstream bodies, raw evidence URLs, and raw target paths remain denied in the changed handoff.
- The self-model was read and left unchanged; its current preference for rollback-backed, locally validated behavior matches this run and did not need a new behavioral claim.
