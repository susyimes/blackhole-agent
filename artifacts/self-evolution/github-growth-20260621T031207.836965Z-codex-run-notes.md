# Self-Evolution Run Notes

Source digest: `github-growth-20260621T031207.836965Z`
Branch: `codex/blackhole-evolve/20260621T031309.918487-run-bounded-skill-route-discovery-against-compas`
Capability window: `skill-route-discovery`, pass 1 of 4
Selected proposal: `p1-skill-route-discovery-compass`

## Rollback

- Rollback ref: `refs/rollback/20260621T031207Z-skill-route-discovery-compass-pass1`
- Rollback artifact: `artifacts/rollback/20260621T031207Z-skill-route-discovery-compass-pass1.md`

## Evidence Reviewed

- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/baskduf/FableCodex`

Bounded review only. No upstream code, scripts, skill bodies, or installers were imported, executed, cloned, or enabled.

COMPASS exposes SKILL.md packages for task clarification, repo-local task memory, handoff, and user profile state, with explicit local-storage and secret-exclusion boundaries. Three.js Game Skills keeps domain-specific assets and QA helpers separate from generic route lessons. FableCodex remains mixed Codex/workflow/skill evidence that should route through local skill discovery first.

## Hypothesis

COMPASS-style profile and task-memory evidence should be visible earlier in the local proposal lane map as a state/profile boundary contract, not only during deeper harness review. This makes the operator-visible route rows state that retention/privacy review is required and that profile, memory, global config, and private-context writes remain denied until local validation explicitly covers them.

## Changes

- Added `state_profile_boundary` metadata to `skill_ecosystem_state_handoff` candidate inventory rows and derived proposal lanes in `src/blackhole_agent/skill_routing.py`.
- Added focused assertions in `tests/test_skill_routing.py` proving the boundary appears for COMPASS-style routes and not for FableCodex or Three.js routes.
- Documented the lane-map boundary in `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k skill_route_discovery` passed: 15 passed, 9 deselected.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` passed: 2 passed, 7 deselected.
- `python -m ruff check src/blackhole_agent/skill_routing.py tests/test_skill_routing.py` passed.
- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` passed: 9 passed, 107 deselected.

## Review Notes

- The self-model was read and left unchanged. It already prefers rollback-backed local behavior changes over report-only scaffolding, which matches this run.
- The new boundary is metadata-only. It does not add runtime permissions, profile writes, memory writes, global config writes, provider launch, remote execution, upstream install, or external skill activation.
- Raw upstream bodies and raw source URLs remain outside harness output. The new route-map field contains only stable booleans and a local review-surface name.
