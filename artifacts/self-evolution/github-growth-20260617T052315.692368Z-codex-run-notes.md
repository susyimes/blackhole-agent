# Codex Run Notes

- Digest: `github-growth-20260617T052315.692368Z`
- Proposal selected: `p1-provider-routing-preflight`
- Evidence reviewed: `https://github.com/omnigent-ai/omnigent/issues/179`
- Hypothesis: an agent mutation route that does not pin its provider/model can silently execute on an unintended provider; blackhole-agent should fail visibly before Codex mutation when the route is implicit.
- Self-model decision: left `docs/self-model.md` unchanged because its current preference already permits rollback-backed provider/config preflight behavior changes and did not need new structure for this run.

## Changes

- Added a metadata-only Codex provider/config preflight in `blackhole_agent.kernels.codex_cli`.
- Controller-launched Codex mutation now requires an explicit `--model` or `--profile` by default and records the preflight in run artifacts and manifests.
- Supervisor validation now checks the Codex route before scheduling child work.
- Added `--allow-default-codex-route` as an explicit compatibility path for operators who intentionally want the CLI default route.
- Updated focused tests and documentation for the new default.

## Material Actions

- Created rollback ref `refs/rollback/github-growth-20260617T052315.692368Z`.
- Wrote rollback artifact `artifacts/self-evolution/github-growth-20260617T052315.692368Z-rollback.md`.
- Read the current self-model and left it unchanged.
- Reviewed the proposal evidence URL narrowly; no broad trend discovery was run.

## Validation

- `uv run pytest tests/test_codex_cli.py tests/test_supervisor.py tests/test_github_growth.py -q` -> passed, `98 passed`.
- `uv run ruff check .` -> passed.
- `uv run pytest -q` -> passed, `137 passed`.

## Review Notes

- The external evidence is an Omnigent issue about an unpinned GPT head silently falling through to Databricks. The local change is an inferred adaptation for this repository's Codex route rather than a claim that blackhole-agent had the same Databricks path.
- Profile names are intentionally not recorded in preflight artifacts; model names are recorded because they are route selectors and not secrets.
