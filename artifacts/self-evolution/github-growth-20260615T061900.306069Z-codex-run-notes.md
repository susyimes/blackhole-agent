# Self-Evolution Run Notes

- Source digest: `github-growth-20260615T061900.306069Z`
- Selected proposal: `trend:visa/visa-vulnerability-agentic-harness-2`
- Evidence URL: https://github.com/visa/visa-vulnerability-agentic-harness
- Rollback point: `refs/rollback/blackhole-agent/20260615T061900Z`

## Evidence Review

The Visa harness README presents run manifests as first-class output for scan
runs, including tool/config provenance, target git SHA, and timing. The reusable
local lesson is observability, not vulnerability-scanning behavior: self-evolution
artifacts should make the runtime settings used by the local Codex kernel easy
to audit without reconstructing them from the shell command.

## Hypothesis

If `latest-self-evolution-run.json` and `latest-self-evolution-manifest.json`
record the Codex CLI model, profile, sandbox, approval policy, and bypass flags
as structured metadata, future scheduled runs are easier to replay and review.

## Local Change

- Added a structured `codex_cli` block to self-evolution run metadata.
- Added the same `codex_cli` block to the self-evolution manifest.
- Extended the focused run test to assert both artifacts agree.

## Validation

Passed:

```powershell
$env:PYTHONPATH = (Resolve-Path src).Path; uv run --with pytest python -m pytest tests/test_github_growth.py::test_prepare_branch_and_run_codex_invoke_expected_commands -q
```

Note: bare `uv run pytest ...` continued to execute a package/test entry path
that produced stale artifact shape in this worktree. The source-tree validation
above imports the edited checkout directly and passed.

## Review Notes

- This is an observability-only change.
- No proposal ranking, runtime permission, restart behavior, remote execution,
  vulnerability-scanning behavior, or supervisor promotion behavior was changed.
- The self-model was read and left unchanged because it remains an intentionally
  blank, non-authorizing description and this run produced no behavioral
  self-description update.
