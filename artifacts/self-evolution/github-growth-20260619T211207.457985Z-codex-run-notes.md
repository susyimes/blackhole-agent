# Codex Run Notes

- Source digest: `github-growth-20260619T211207.457985Z`
- Capability theme: `provider-runtime-control`
- Branch: `codex/blackhole-evolve/20260619T211310.010951-prototype-a-bounded-skill-route-discovery-path-t`
- Rollback artifact: `artifacts/rollback/20260620T041422Z-provider-runtime-control-pass4.txt`
- Rollback ref: `refs/rollback/20260620T041422Z-provider-runtime-control-pass4`

## Hypothesis

Provider usage-limit preflights already block exhausted provider windows and keep
credential-pool failover review-only. The final pass can improve operator
recovery by emitting a body-free failover review plan with safe next-action
codes and local replay commands, without selecting, ranking, printing, or
switching pooled credentials.

## Changes

- Added `failover_review_plan` to provider usage-limit preflight output.
- Propagated the same plan into `provider_usage_limit_exhausted` recovery hints.
- Documented the plan in the provider-runtime recovery summary architecture note.
- Extended provider-runtime harness tests to assert review-only status, replay
  commands, launch denial, and credential/body redaction.

## Material Actions

- Created rollback ref and artifact before source edits.
- Edited local source, tests, documentation, and this run note only.
- No external repositories were cloned, installed, executed, or queried.
- No provider runtime was launched.

## Validation

- `pytest tests/test_harness_eval.py -q -k provider_runtime` passed.
- `pytest tests/test_docs_contracts.py -q` passed.
- `pytest tests/test_harness_eval.py -q` passed.

## Self-Model

`docs/self-model.md` was read and left unchanged. It already matched this run's
bounded local-evolution posture and did not need a narrower correction.

## Review Notes

- Credential-pool failover remains review-only because credential labels and
  token material are privacy-sensitive.
- The new plan is an operator-visible recovery handoff, not an activation path:
  `provider_runtime_launch_allowed` remains false and `failover_executed`
  remains false.
