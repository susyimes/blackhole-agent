# Skill Route Discovery Pass 2 Growth Summary

Source digest: `github-growth-20260627T180729.492580Z`

Rollback:

- Artifact: `artifacts/rollback/20260627T180728Z-skill-route-discovery-pass2-growth-summary.md`
- Ref: `refs/rollback/20260627T180728Z-skill-route-discovery-pass2-growth-summary`
- Original HEAD: `cd699d318b8f3887db847a0238f6bee124d7e046`

## Evidence

The active pass-2 window carries public skill-route evidence for:

- `dongshuyan/compass-skills`
- `lyra81604/zhengxi-views`
- `majidmanzarpour/threejs-game-skills`

The reusable lesson is that route discovery needs an operator-visible summary
after fixture validation and profile handoff, before any activation path. The
summary should be replayable and compact without exporting raw repository URLs,
raw commands, local target paths, or upstream bodies.

## Hypothesis

Adding a `growth_route_summary_artifact` to the lane map improves pass-2
observability by joining the existing fixture validation lane, profile handoff,
and validation handoff into one body-free review surface. It should not create
new lanes or grant runtime authority.

## Change

- Added `growth_route_summary_artifact` to
  `build_skill_route_discovery_proposal_lane_map`.
- The artifact reports proposal IDs, route profiles, selected bounded lanes,
  selected evidence item IDs, validation targets, downgraded unsupported lane
  pressure, source surface statuses, and replay-command hashes.
- The artifact denies runtime action, upstream skill or agent activation,
  external harness execution, provider launch, remote execution, profile
  writes, memory writes, raw URL export, raw path export, upstream body export,
  and raw replay-command export.
- Documented the new artifact in `docs/skill-route-discovery.md`.
- Left `docs/self-model.md` unchanged because it already matched this run's
  autonomy and rollback posture; the missing piece was an operator-visible
  behavior surface, not self-description.

## Validation

Passed:

```powershell
python -m pytest tests/test_skill_routing.py -q -k pass2_route_classification_fixture
python -m pytest tests/test_skill_routing.py -q
```

## Review Notes

- This is a local summary artifact only. It does not inspect upstream bodies,
  install upstream repositories, execute external harnesses, launch providers,
  or activate skills.
- Unsupported lane pressure remains downgraded and visible as review context:
  `install`, `provider_runtime`, and `runtime_execution`.
