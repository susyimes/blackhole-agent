# Codex Run Notes

Source digest: `github-growth-20260617T140001.546295Z`
Proposal focus: `proposal-003-skill-routing-compass`
Evidence URL: https://github.com/dongshuyan/compass-skills
Rollback artifact: `artifacts/rollback/20260617T140001Z-skill-routing-compass.txt`
Rollback ref: `refs/rollback/20260617T140001Z-skill-routing-compass`

## Hypothesis

COMPASS presents a local `SKILL.md` ecosystem with explicit skill invocation, workflow memory, clarification gates, and local-only state. The reusable local lesson is that skill routing should keep ranked evidence available while making unclear top-level skill selection inspectable instead of silently resolving it by deterministic sort order.

## Change

- Added `SkillRouteSelection` and `select_skill_for_task` as a controller-facing selection API.
- Preserved existing `rank_skills_for_task` behavior for audit and compatibility.
- Added an explicit `ambiguous_match` route when multiple top-ranked skills tie on route class and score.
- Added tests for deterministic selection, no-match fallback, and ambiguous explicit skill requests.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already says locally validated tool routing improvements are in scope, and this run did not produce evidence that the file is shaping behavior incorrectly.

## Validation

- `uv run pytest tests/test_skill_routing.py` first failed during collection because the new selection API did not exist.
- `uv run pytest tests/test_skill_routing.py` passed: 9 tests.
- `uv run ruff check src/blackhole_agent/skill_routing.py tests/test_skill_routing.py` passed.
- `uv run pytest` passed: 174 tests.

## Review Notes

- `proposal-001-omni-agent-runtime-preflight` remained review-only because its validation gate is privacy-leakage human review.
- `proposal-002-vulnerability-harness-safety-gates` remained review-only because its validation gate is offensive-behavior human review.
- This patch does not implement attack, exploit, malware, phishing, exfiltration, unauthorized-access behavior, or token/credential/PII handling.
