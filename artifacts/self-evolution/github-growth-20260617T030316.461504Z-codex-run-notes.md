# Self-Evolution Run Notes

Source digest: `github-growth-20260617T030316.461504Z`

Chosen proposal: `p3-generic-pr-metadata-normalization`

Evidence reviewed:
- `https://github.com/omnigent-ai/omnigent`
- Evidence supports treating high-volume agent workflow activity as supervised, policy-aware signal input. The reusable local lesson is to keep validation and evidence selection inspectable when upstream metadata is noisy.

Hypothesis:
- Repeated generic or untitled pull request events, especially opened/labeled activity, should not consume context slots or inflate proposal confidence as if each item contained independent implementation detail.

Change summary:
- Added deterministic low-detail PR cluster detection in proposal evidence selection.
- Downweighted repeated generic PR clusters and later duplicates within a cluster.
- Added metadata-only context diagnostics for generic PR cluster counts.
- Added regression coverage for repeated generic opened/labeled PR fixtures and updated context-budget preflight expectations.

Self-model decision:
- Left `docs/self-model.md` unchanged. Its current preference for rollback-backed local behavior changes is consistent with this run, and no new evidence showed that the file is behavior-shaping enough to justify a self-model edit.

Rollback:
- Artifact: `artifacts/rollback/20260617T030315Z-generic-pr-metadata-normalization.txt`
- Ref: `refs/blackhole/rollback/20260617T030315Z-generic-pr-metadata-normalization`

Validation:
- `uv run pytest tests/test_github_growth.py -q` -> passed, 68 tests.
- `uv run pytest tests/test_proposal_eval.py -q` -> passed, 12 tests.

Review notes:
- The change is local validation machinery only. It does not implement review-workflow routing or any offensive, abuse, unauthorized-access, or privacy-leakage behavior.
- Context diagnostics avoid copying upstream PR titles, URLs, or repository names into preflight cluster metadata.
