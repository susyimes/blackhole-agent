# Self-Evolution Run Notes: skill-route-discovery pass 1 validation lane

- Source digest: `github-growth-20260628T134729.588648Z`
- Branch: `codex/blackhole-evolve/20260628T134823.825876-add-or-extend-a-local-skill-route-discovery-vali`
- Rollback ref: `refs/blackhole-rollback/20260628T134728Z-skill-route-pass1`
- Rollback artifact: `artifacts/rollback/20260628T134728Z-skill-route-discovery-pass1.md`
- Self-model: read and left unchanged.

## Evidence

The current capability window carries zhengxi-views, Three.js Game Skills,
COMPASS Skills, and Qwen-AgentWorld. The reusable lesson is that public skill
or route repositories should become bounded local validation lanes first:
generic or source-cited skill workflows should be tested locally,
game/frontend skill workflows should be documented and fixture-validated before
runtime use, and state/profile handoff ecosystems should remain metadata-only
until a concrete local state boundary exists.

Qwen-AgentWorld-style evidence is adjacent general-agent evaluation evidence,
not a skill-route discovery candidate. It remains in `agent_harness_eval_required`.

## Hypothesis

If the controller exposes a current-digest pass-1 validation lane keyed to the
active proposal IDs, the supervisor can replay the skill-route slice without
guessing which local lane each profile should enter and without granting
runtime, install, provider, profile, memory, or external harness authority.

## Change

- Added `current_digest_pass1_validation_lane` to the skill-route proposal lane
  map.
- Mapped:
  - `p1-skill-route-discovery-generic` to the local `test` lane.
  - `p2-game-frontend-skill-profile` to the local `documentation` lane.
  - `p3-skill-ecosystem-state-handoff` to the local `config` lane.
- Kept adjacent Qwen/Looper-style general-agent proposals in eval-only policy.
- Documented the new surface in `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k current_digest_pass1_validation_lane`: passed, 1 test.

## Review Notes

- The lane is classification and validation metadata only. It does not export
  raw GitHub URLs or replay-command bodies.
- Remaining uncertainty is upstream implementation parity; this run did not
  import or execute upstream skill bodies.
