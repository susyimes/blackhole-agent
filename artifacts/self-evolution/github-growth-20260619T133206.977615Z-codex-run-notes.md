# Codex Run Notes

Source digest: `github-growth-20260619T133206.977615Z`
Run focus: provider and runtime preflight control, pass 1 of 4
Rollback artifact: `artifacts/rollback/20260619T133344Z-provider-runtime-token-preflight.txt`
Rollback ref: `refs/rollback/20260619T133344Z-provider-runtime-token-preflight`

## Evidence

- `https://github.com/omnigent-ai/omnigent` describes a multi-harness agent framework with provider selection, policy, sandboxing, and model/API-key flexibility.
- `https://github.com/omnigent-ai/omnigent/pull/183` was treated as harness-extension evidence, but privacy-leakage harness behavior remained review-only.
- `https://github.com/visa/visa-vulnerability-agentic-harness` was treated as local-evaluation inspiration only; no offensive or privacy-leakage behavior was executed.

## Hypothesis

Provider startup should reject dummy credential configuration before runner launch, but artifacts must remain body-free. A local preflight that distinguishes `absent`, `placeholder`, and `provided` improves recovery diagnostics without requiring real secrets in tests or CI.

## Change

- Added reusable token-quality classification for body-free provider diagnostics.
- Made Codex ambient OpenAI and Google preflight fail closed on placeholder API keys while still recording only env names, booleans, route hints, and quality classes.
- Made supervisor required token preflight reject placeholder token values before scheduling.
- Passed the supervisor-supplied environment into Codex provider preflight so replayed startup checks do not accidentally read unrelated process environment.
- Documented the new provider credential usability contract.

## Validation

- `uv run pytest tests/test_codex_cli.py tests/test_supervisor.py -q -k "provider_preflight or provider_config_preflight or runtime_startup_preflight or ambient_openai or ambient_google or codex_kernel_fails_before_exec"`: passed, 17 tests.
- `uv run pytest -q`: passed, 318 tests.
- `uv run ruff check .`: passed.

## Review Notes

- Self-model was read and left unchanged. It already supports rollback-backed, locally validated provider/config preflight improvements and did not contradict this run.
- Placeholder detection is intentionally conservative and class-based. It blocks common dummy values such as `dummy`, `placeholder`, `replace-me`, and `sk-dummy` without trying to validate provider-specific token formats.
- The privacy-leakage and vulnerability-harness proposal remains review-only; this run only improved local preflight diagnostics and validation.
