# Skill Route Discovery Pass 3 Activation Candidate Lane

Source digest: `github-growth-20260628T034729.532203Z`

Rollback point: `artifacts/rollback/20260628T034821Z-skill-route-discovery-pass3.txt`

## Hypothesis

The active pass-3 skill-route proposals should be visible as a bounded
operator replay surface before activation. The useful local improvement is not
to import external skills, run upstream harnesses, or write handoff state, but
to map the current proposal IDs into documentation, config, test, and
code_patch lanes with local validation and explicit denials.

## Evidence Interpreted

- `dongshuyan/compass-skills`: interpreted as
  `skill_ecosystem_state_handoff`; selected local lane `config`.
- `lyra81604/zhengxi-views`: interpreted as
  `source_cited_domain_research`; included in the aggregate test lane.
- `majidmanzarpour/threejs-game-skills`: interpreted as
  `game_frontend_workflow`; selected local lane `documentation`.
- `QwenLM/Qwen-AgentWorld`: adjacent general-agent evidence only; kept in
  `agent_harness_eval_required`.

## Material Actions

- Added `active_pass3_activation_candidate_lane` to the skill-route proposal
  lane map.
- Added a frozen current-wake fixture covering the three skill-route evidence
  items plus the adjacent Qwen-AgentWorld item.
- Added regression coverage asserting the new lane is ready, body-free,
  bounded to local lanes, and non-executable.
- Updated `docs/skill-route-discovery.md` with the new pass-3 operator surface.
- Left `docs/self-model.md` unchanged because it already supports this run's
  validated local behavior-path preference and does not need new authority.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "active_pass3_activation_candidate_lane or current_run_pass3_validation_lane or active_window_pass2_validation_lane"`: passed, 3 passed.
- `python -m pytest tests/test_skill_routing.py -q`: passed, 64 passed.
- `python -m pytest tests/test_docs_contracts.py -q`: passed, 11 passed.
- `python -m pytest -q`: passed, 465 passed.

## Review Notes

The new lane hashes source URLs and replay commands and exports no raw upstream
source URLs, evidence URLs, target paths, command bodies, or upstream bodies.
It denies runtime action, external skill or agent activation, external harness
execution, provider launch, profile writes, memory writes, and remote
execution. Unsupported suggested lanes are already sanitized before the
activation-candidate surface is built.
