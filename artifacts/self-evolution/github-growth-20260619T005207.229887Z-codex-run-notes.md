# Codex Run Notes

Source digest: github-growth-20260619T005207.229887Z
Run branch: codex/blackhole-evolve/20260619T005313.258838-add-or-refine-a-local-release-runbook-validation
Rollback artifact: artifacts/rollback/20260619T005207Z-provider-permission-default-preflight.txt
Rollback ref: refs/rollback/20260619T005207Z-provider-permission-default-preflight

## Selected Proposal

Selected `p3-provider-permission-default-preflight`.

Hypothesis: provider permission defaults should be explicit, locally replayable metadata before a scheduled wake starts. The upstream evidence showed a Claude SDK harness default changing to `auto`, with a review caveat that the safety claim is inherited from SDK behavior rather than locally enforceable. A metadata-only preflight gives this repository a durable local contract without launching the SDK or exporting raw environment values.

Evidence reviewed:

- https://github.com/omnigent-ai/omnigent/pull/705
- https://github.com/omnigent-ai/omnigent/pull/737

## Changes

- Added supervisor provider/config preflight metadata for Claude SDK permission mode.
- Defaulted the local expected mode to `auto`.
- Added config and CLI controls to set a mode and to disallow `auto` when runtime policy requires a different default.
- Recorded that `auto` is provider-enforced, not locally enforced.
- Added whitelist-based diagnostics so invalid permission-mode text is not echoed into artifacts.
- Documented the startup preflight behavior.

Self-model: read and left unchanged. Its current preference already includes provider/config preflight work and did not need a new claim for this run.

## Review-Only Items

- `p1-release-runbook-preflight` stayed review-only because its validation gate was privacy-leakage human review.
- `p2-governance-ask-label-write-guard` stayed review-only because its validation gate was offensive-behavior human review.

No tokens, credentials, private keys, private chats, PII, or personal data were exposed, logged, uploaded, published, or shared.

## Validation

- `uv run pytest tests/test_supervisor.py -q -k "provider_config_preflight or claude_sdk_permission_preflight or build_wake_command"`: passed, 10 passed and 26 deselected.
- `uv run pytest -q`: passed, 266 passed.
- `uv run ruff check .`: passed.

Notes: `uv` reported the ambient `VIRTUAL_ENV` path did not match the project `.venv` and ignored it. This did not affect test or lint success.
