# Codex Run Notes

Digest: `github-growth-20260616T044601.288948Z`
Proposal adopted: `p1-terminal-driven-e2e-harness`
Rollback ref: `refs/blackhole-agent/rollback/20260616T044601Z/dc9f2da437b3`

## Evidence

- `https://github.com/omnigent-ai/omnigent` describes a meta-harness for agents and emphasizes shared terminal/session workflows, multiple harnesses, policies, and sandboxing.

## Hypothesis

If the controller has a terminal-driven test that starts from CLI invocation and reaches proposal output, rollback preparation, Codex kernel command construction, and durable run artifacts, future runner/controller changes will be less likely to silently break the autonomous wake path.

## Change

- Added a CLI-level codex-mode journey test in `tests/test_github_growth.py`.
- The test uses a fake GitHub client and fake command runner, avoiding network access, real git mutation, and real Codex execution while preserving the controller path and artifact assertions.
- Left `docs/self-model.md` unchanged because its current preference for rollback-backed reversible local implementation directly matched this run.

## Validation

- `uv run pytest tests/test_github_growth.py::test_cli_codex_mode_runs_terminal_driven_controller_journey -q` passed.
- `uv run pytest tests/test_github_growth.py -q` passed.
- `uv run ruff check tests/test_github_growth.py` passed.

## Review Notes

- No runtime behavior changed.
- No new external capability, remote execution, push, promotion, restart, offensive behavior, or privacy-sensitive data handling was added.
- The test intentionally keeps the real Codex process mocked; it verifies controller decisions and command/artifact contracts rather than model behavior.

