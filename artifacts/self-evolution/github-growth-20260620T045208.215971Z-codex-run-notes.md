# Codex Run Notes

Source digest: `github-growth-20260620T045208.215971Z`
Branch: `codex/blackhole-evolve/20260620T045313.253065-add-or-extend-local-tests-for-skill-workflow-rou`
Rollback artifact: `artifacts/rollback/20260620T045206Z-provider-runtime-control-pass2.txt`
Rollback ref: `refs/blackhole-rollback/20260620T045206Z-provider-runtime-control-pass2`

## Hypothesis

The provider/runtime control slice is better advanced by making sampled provider-runtime replay failures visible as their own operator recovery path, rather than adding another duplicate skill-route fixture. Public skill evidence can still map only to local documentation, config, test, or code_patch lanes, but provider-runtime replay failure should not be collapsed into a generic activation blocker.

## Evidence

The active window carried skill/workflow evidence from:

- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/baskduf/FableCodex`
- `https://github.com/omnigent-ai/omnigent`

Existing local tests already covered bounded skill-route lanes and selected-item evidence citations. The uncovered operator-facing distinction was the provider/runtime replay recovery reason and next action when `provider_runtime_preflight_samples` block activation.

## Changes

- `src/blackhole_agent/harness_eval.py`: `skill_route_discovery_provider_runtime_control` now marks sampled provider-runtime replay blockers with `reason: provider_runtime_replay_not_ready`, `provider_runtime_replay_blocked: true`, and a provider-replay-specific next action.
- `tests/test_harness_eval.py`: added regression assertions that activation rows, local lane intake, and operator handoff preserve the provider-runtime replay blocker without exporting provider bodies, env keys, or source URLs.
- `docs/skill-route-discovery.md`: documented the provider-runtime replay blocker contract.
- `docs/self-model.md`: left unchanged; the existing self-model already states the relevant preference for locally validated behavior changes and narrow safety boundaries.

## Validation

- `pytest tests/test_harness_eval.py -q -k "skill_route_discovery_lane or provider_runtime_preflight or provider_runtime_recovery_summary"` passed: 22 passed, 71 deselected.
- `pytest tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_harness_eval.py -q -k "skill_route_discovery or proposal_interpretation or route_hint_policy or provider_runtime"` passed: 44 passed, 90 deselected.

## Review Notes

The change is local and body-free. It does not launch providers, execute remote runners, install external skill code, expose raw preflight inputs, or export raw diagnostics. Remaining uncertainty is that this pass did not re-query upstream repositories; it used the digest window and existing frozen local evidence, as requested.
