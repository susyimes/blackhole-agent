# Codex Run Notes

Source digest: `github-growth-20260616T054601.077662Z`
Run branch: `codex/blackhole-evolve/20260616T054711.788139-add-or-strengthen-provider-config-token-prefligh`
Rollback ref: `refs/blackhole-rollback/20260616T054600Z`
Rollback artifact: `artifacts/rollback/20260616T054600Z-provider-config-preflight.txt`

## Self-Model Reading

`docs/self-model.md` was read before choosing a change. It already allows reversible local context-budget experiments while keeping offensive behavior and privacy leakage review-only, so it was left unchanged.

## Hypothesis

The fastcontext-inspired proposal showed that large evidence packages should prioritize high-value evidence before truncation. The existing package truncated by input order, which could drop later high-confidence or risk-boundary items before proposal synthesis.

## Change

`build_proposal_evidence_package` now ranks digest items before applying `max_items`:

- risk-flagged items first
- then higher confidence
- then original input order as the deterministic tie-breaker

The context budget metadata records the selection strategy plus selected and truncated item IDs so truncation decisions are replayable without adding external fetches.

## Validation

- `uv run --extra dev python -m ruff check src tests`
- `uv run --extra dev python -m pytest tests/test_github_growth.py -k "context_budget or proposal_evidence_package_prioritizes_items_before_truncation or llm_proposals_preserve_non_safety_routes"`
- `uv run --extra dev python -m pytest`

All validation passed: 65 tests.

## Review Notes

Proposal `p1-provider-config-preflight` remains review-only because token/provider diagnostics can cross the privacy-leakage boundary. No tokens, credentials, secrets, private keys, private chats, PII, or personal data were printed, logged, uploaded, or shared.

Proposal `p2-flaky-test-marker-audit` was skipped because the available test suite did not expose flaky markers or render-style suppressions needing removal.

No external evidence fetch was performed during this kernel pass; the run used the provided proposal evidence summary.
