# Self-Evolution Run Notes

Source digest: `github-growth-20260623T043652.610653Z`
Branch: `codex/blackhole-evolve/20260623T043827.246447-add-or-extend-local-validation-covering-provider`
Rollback artifact: `artifacts/rollback/20260623T043651Z-provider-runtime-control-pass1-wire-api-chat.md`
Rollback ref: `refs/rollback/20260623T043651Z-provider-runtime-control-pass1-wire-api-chat`

## Evidence

- `https://github.com/omnigent-ai/omnigent/issues/902`: reviewed as the primary evidence. The issue reports that pi-native preserved `wire_api: chat` in configured provider data but generated a responses route for the runner, producing 404s against chat-completions-only gateways.
- `https://github.com/omnigent-ai/omnigent`: reviewed only as contextual evidence that Omnigent is a public agent framework/harness project.

No upstream code, provider config body, token, model body, private chat, install command, provider launch, remote execution, promotion, push, or restart was executed.

## Hypothesis

Provider/runtime preflight should resolve chat wire API configuration from both top-level and nested OpenAI-compatible provider config shapes before a harness can launch. If the configured chat route is absent or the runner-generated route resolves to responses, the preflight should block with body-free diagnostics and recovery hints.

## Change

- Added nested wire API resolution in `src/blackhole_agent/harness_eval.py` for `openai.wire_api`, `provider_config.openai.wire_api`, and `config.openai.wire_api`, while preserving the existing top-level runtime/provider precedence.
- Normalized OpenAI chat-completions aliases to the local `chat` route.
- Added focused regression tests in `tests/test_harness_eval.py` for old and new nested config shapes, missing required wire API config, and a chat-to-responses runner mismatch.
- Updated `docs/architecture.md` to document nested OpenAI-compatible config support and the body-free export boundary.

## Self-Model

Read `docs/self-model.md` before choosing the patch. It already supports rollback-backed, locally validated provider/config preflight improvements while keeping permissions external to the runtime policy, so it was left unchanged.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k provider_runtime_preflight` passed: 21 passed, 110 deselected.
- `python -m pytest tests/test_docs_contracts.py -q` passed: 9 passed.
- `python -m ruff check src/blackhole_agent/harness_eval.py tests/test_harness_eval.py` passed.

## Review Notes

- Diagnostics and recovery hints remain metadata-only: raw provider URLs, model names, raw wire API labels, token values, private chats, and provider config bodies are not exported.
- Proposal `p2-runner-version-compat-config-bridge` remains review-only because its validation gate was privacy-leakage human review.
- No activation, commit, push, promotion, or restart was performed inside this kernel run.
