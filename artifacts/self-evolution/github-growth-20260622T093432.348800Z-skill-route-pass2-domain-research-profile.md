# Skill Route Discovery Pass 2 Domain Research Profile

Source digest: `github-growth-20260622T093432.348800Z`

Branch: `codex/blackhole-evolve/20260622T093547.552325-add-or-extend-local-validation-coverage-for-skil`

Rollback ref: `refs/rollback/blackhole-agent/20260622T093430Z-skill-route-discovery-pass2`

Rollback artifact: `artifacts/rollback/20260622T093430Z-skill-route-discovery-pass2.txt`

## Evidence

- `https://github.com/baskduf/FableCodex`: existing mixed Codex/skill/workflow evidence remains covered by `codex_workflow_gate`.
- `https://github.com/dongshuyan/compass-skills`: existing ecosystem/state handoff evidence remains covered by `skill_ecosystem_state_handoff`.
- `https://github.com/lyra81604/zhengxi-views`: current-window evidence presents a source-cited domain research agent skill with investment-research framing.
- `https://github.com/omnigent-ai/omnigent`: remains outside this local skill-route profile change because the pass-2 slice is bounded to skill route discovery lanes.

No upstream code, dataset, install command, provider runtime, scaffold, skill body, or advice-generation path was imported or executed.

## Hypothesis

The active pass-2 skill-route window should distinguish source-cited domain research skills from generic skill workflows. A zhengxi-views-shaped repository should still map only to documentation, config, test, or code_patch lanes, but its handoff should explicitly require citation and advice-boundary validation before any activation.

## Change

- Added `source_cited_domain_research` route-profile recognition to the skill-route classifier.
- Added a profile validation contract with the gate `source_citation_and_advice_boundary_before_domain_skill_activation`.
- Added `domain_research_boundary` metadata denying upstream dataset import, provider-backed advice generation, financial/medical advice, and private-context export.
- Added focused regression coverage using a `zhengxi-views` shaped body-free summary.
- Updated `docs/skill-route-discovery.md` with the new profile and activation boundary.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference for rollback-backed, locally validated behavior changes matches this pass, and the new evidence did not show that the self-model needed a structural change.

## Validation

- `python -m pytest tests/test_skill_routing.py -q -k "source_cited_domain_research or skill_route_discovery_current_window_matrix or mixed_codex"`: passed, 4 tests.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery`: passed, 2 tests.
- `python -m pytest tests/test_skill_routing.py -q`: passed, 29 tests.
- `python -m ruff check src/blackhole_agent/skill_routing.py tests/test_skill_routing.py tests/test_docs_contracts.py`: passed.
- `python -m pytest tests/test_harness_eval.py -q -k "skill_route_discovery_lane or pass2_handoff"`: passed, 9 tests.

## Review Notes

- The new profile grants no runtime action and no external skill activation.
- The profile is evidence routing only; source-cited upstream claims do not become local knowledge or advice.
- Secondary harness, provider launch, remote execution, dataset import, profile writes, memory writes, and private context export remain denied.
