# Codex Run Notes

Source digest: `github-growth-20260616T093703.959612Z`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent`
- `https://github.com/omnigent-ai/omnigent/pull/316#discussion_r3419525416`
- `https://github.com/omnigent-ai/omnigent/pull/316#discussion_r3419525426`
- `https://github.com/omnigent-ai/omnigent/pull/316#discussion_r3419618168`

Omnigent presents itself as a meta-harness for multiple agent runtimes with web UI/session orchestration, policies, sandboxing, and shared supervision. The pull request evidence is sparse through the public HTML view available in this run, but the proposal metadata and repository context support the reusable lesson: agent orchestration regressions should be covered as replayable journeys rather than only isolated unit checks.

## Hypothesis

Adding a deterministic local journey test for web research intake, MCP-style tool routing metadata, and cancellation recovery will reduce the risk that future changes break cross-surface agent workflows while individual unit tests still pass.

## Change

- Added `tests/test_e2e_journey_regressions.py`.
- Added `artifacts/rollback/20260616T093703Z-e2e-journey-regression-suite.txt`.

The new test uses a stubbed GitHub client, so it has no network dependency. It verifies that a public evidence URL enters the digest and plan, that an MCP provider tool descriptor remains routeable by full compatibility metadata, and that a timed-out Codex kernel run can recover in the same timestamp without artifact collisions.

## Self-Model Decision

`docs/self-model.md` was left unchanged. The existing preference already supports rollback-backed local evolution and locally validated tests; this run had stronger evidence for improving regression coverage than for revising the self-description.

## Review-Only Notes

- Proposal `p2-provider-config-token-preflight` remains review-only for this run. No token, credential, secret, private key, private chat, PII, or personal data handling behavior was implemented or logged.
- Proposal `p3-tool-routing-and-mcp-capability-gates` remains review-only for this run. No offensive, exploit, malware, phishing, exfiltration, or unauthorized-access behavior was implemented.

## Validation

- `uv run pytest tests/test_e2e_journey_regressions.py -q` passed.
- `uv run ruff check tests/test_e2e_journey_regressions.py` passed.
- `uv run pytest tests/test_agent_harness_smoke.py tests/test_tool_routing.py tests/test_codex_cli.py tests/test_github_growth.py -q` passed.
