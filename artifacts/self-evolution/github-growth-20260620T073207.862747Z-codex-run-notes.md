# Codex Run Notes

Digest: `github-growth-20260620T073207.862747Z`
Branch: `codex/blackhole-evolve/20260620T073307.248152-add-or-run-a-bounded-skill-route-discovery-valid`
Theme: `skill-route-discovery`

## Evidence

Primary proposal evidence:

- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/baskduf/FableCodex`

The existing local route-discovery lane already bounds these public skill/workflow repositories to documentation, config, test, and code_patch lanes with `runtime_action: none`. This pass treated the useful next step as an operator-visible activation/replay manifest rather than another standalone fixture.

## Hypothesis

Skill-route discovery should expose a compact manifest that supervisors and operators can replay before promotion. The manifest should carry only selected item-id evidence refs, route profiles, hashed source and target metadata, local proof readiness, validation commands, recovery hints, and activation blockers. It must not promote external repositories into executable skills or export raw source URLs, target paths, evidence URLs, or upstream bodies.

## Changes

- Added `activation_manifest` to `skill_route_discovery_lane` output.
- Added `skill_route_discovery_activation_manifest()` to render bounded local lane rows from existing proposal and activation data.
- Updated focused harness assertions to verify manifest lanes remain documentation/config/test/code_patch, cite selected item IDs only, and keep runtime/external activation denied.
- Documented the manifest contract in `docs/skill-route-discovery.md`.
- Added docs-contract coverage for the manifest wording.
- Created rollback artifact `artifacts/self-evolution/github-growth-20260620T073207.862747Z-rollback.md`.

## Self-Model

Read `docs/self-model.md` before selecting the change. Left it unchanged because its current preference already supports rollback-backed local behavior improvements while keeping external skill activation outside discovery authority. No new evidence from this run required changing the self-description.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` -> passed, 9 passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` -> passed, 2 passed.
- `python -m pytest tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery` -> passed, 16 passed.
- `python -m pytest tests/test_harness_eval.py tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_docs_contracts.py -q -k skill_route_discovery` -> passed, 31 passed.

## Rollback

Rollback ref: `refs/rollback/blackhole-agent/20260620T153348-github-growth-20260620T073207Z`

Recovery commands:

```powershell
git switch codex/blackhole-evolve/20260620T073307.248152-add-or-run-a-bounded-skill-route-discovery-valid
git reset --hard refs/rollback/blackhole-agent/20260620T153348-github-growth-20260620T073207Z
```

Rollback execution is explicit and destructive; a human operator or supervisor policy must choose it.

## Review Notes

- The manifest is a local replay surface only. It does not install, clone, enable, run, execute, scaffold, launch providers, or activate external skill code.
- Evidence remains repository-level and README-level in places, so upstream implementation parity is not claimed.
- Activation still depends on local artifact proof and the existing preactivation trust boundary.
