# Codex Run Notes

Source digest: github-growth-20260618T031013.177089Z
Run started from branch: codex/blackhole-evolve/20260618T031119.096126-add-or-extend-local-mock-llm-e2e-coverage-for-to
Rollback artifact: artifacts/rollback/20260618T031012Z-mock-llm-agent-e2e-validation.txt
Rollback ref: refs/blackhole-agent/rollback/20260618T031012Z/37220dacc8ed

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent
- https://github.com/omnigent-ai/omnigent/pull/534
- https://github.com/omnigent-ai/omnigent/pull/539

Reusable lesson: mock-LLM e2e lanes are most useful when they cover realistic tool-call and policy behavior without external providers, and named sub-agent tests need explicit queue/persistence checks so shared-model mock queues do not silently desync.

## Hypothesis

Extending the existing local `mock_llm_workflow_route` fixture evaluator with body-free named sub-agent persistence, queue-desync detection, and embedded native policy summaries improves future trend validation without adding provider credentials or exporting private prompts, session IDs, names, or tool arguments.

## Changes

- Extended `mock_llm_workflow_route` with `sub_agents` metadata:
  - hashes named sub-agent names and turn session IDs,
  - checks that expected agent turns were observed,
  - detects queue fallback/mismatch as sub-agent mock route failure,
  - checks stable session identity when persistence is required.
- Added embedded `native_tool_policy` evaluation to the mock route, reusing the existing native policy model and surfacing only permission/status metadata.
- Added a durable local fixture covering mock LLM named sub-agent persistence plus fail-closed native tool policy.
- Added focused tests for the passing combined route, queue fallback/desync failure, and named session drift failure.

Self-model decision: left unchanged. The current self-model already favors rollback-backed, locally validated behavior improvements and did not need more structure for this run.

## Validation

- `python -m pytest tests/test_harness_eval.py -q` -> 27 passed
- `python -m pytest -q` -> 201 passed
- `python -m ruff check src\blackhole_agent\harness_eval.py tests\test_harness_eval.py` -> passed

## Review Notes

- No external provider calls were made.
- The new route does not execute native tools; denied/review/no-opinion native policy states remain passive.
- Fixture and result assertions verify that private sub-agent names, session IDs, policy session IDs, and tool arguments are not exported.
- The local evaluator remains metadata-only; it does not implement a live mock LLM HTTP server.
