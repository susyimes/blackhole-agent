# Self-Evolution Run Notes

Source digest: `github-growth-20260619T203207.276361Z`
Capability window: `provider-runtime-control`, pass 2 of 4

## Hypothesis

Provider/runtime failures are easier to recover from when each preflight emits
body-free recovery hints and replay commands directly, instead of requiring the
operator to run an aggregate recovery summary first.

## Evidence Used

- The run window carried provider/runtime control pressure and evidence URLs for
  public agent and harness projects.
- Existing local fixtures already modeled provider runtime preflight and
  aggregate recovery summaries without exporting secrets, prompts, URLs, or
  raw commands.
- Self-model review: left `docs/self-model.md` unchanged because its current
  preference for rollback-backed local behavior remains consistent with this
  run and did not need new authority.

## Rollback

Rollback ref: `refs/rollback/20260619T203207-provider-runtime-control-pass2`
Rollback artifact: `artifacts/rollback/20260619T203207Z-provider-runtime-control-pass2.txt`

## Changes

- Added per-preflight `recovery_hints` and `supervisor_replay` output to
  `provider_runtime_preflight`.
- Reused the existing provider runtime recovery hint builder so individual and
  aggregate recovery surfaces stay aligned.
- Extended tests for blocked model-command preflight and degraded mock-auth
  preflight.
- Documented the provider runtime recovery handoff boundary.

## Validation

- `pytest tests/test_harness_eval.py -q -k "provider_runtime_preflight or provider_runtime_recovery_summary"`
  - Result: 11 passed, 73 deselected.
- `pytest tests/test_harness_eval.py -q`
  - Result: 84 passed.
- `pytest tests/test_docs_contracts.py -q`
  - Result: 8 passed.

## Review Notes

- No provider was launched.
- No external harness execution, remote execution, restart, promotion, push, or
  credential access was attempted.
- Recovery hints remain metadata-only and keep
  `provider_runtime_launch_allowed: false`.
