# Codex Run Notes

Source digest: `github-growth-20260617T154001.235308Z`
Branch: `codex/blackhole-evolve/20260617T154107.164399-create-a-bounded-local-validation-lane-for-agent`
Rollback artifact: `artifacts/rollback/20260617T154000Z-skill-route-lane.txt`
Rollback ref: `refs/rollback/20260617T154000Z-skill-route-lane`

## Evidence Used

- `https://github.com/baskduf/FableCodex`
- `https://github.com/cellebrite-labs/ghidra-rpc`
- `https://github.com/dongshuyan/compass-skills`

The run did not re-run broad trend discovery or fetch additional external pages. The supplied digest plus the existing local route-discovery fixture were sufficient to identify the local gap.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already supports rollback-backed local routing improvements outside the narrow safety boundary, and this run produced executable controller/test evidence rather than a new durable self-description.

## Hypothesis

The evidence package already advertised `skill_route_discovery` as limited to documentation, config, test, and code_patch lanes, but candidate review relied on prompt/policy metadata rather than deterministic enforcement. A local proposal interpreter should reject skill-route candidates that cite frozen skill evidence while choosing an unbounded proposal kind.

## Changes

- Added a shared `ROUTE_HINT_VALIDATION_LANES` policy map in `blackhole_agent.proposal_synthesis`.
- Added deterministic candidate review for `skill_route_discovery` evidence refs so proposals must use one of `documentation`, `config`, `test`, or `code_patch`.
- Extended the skill workflow proposal replay fixture with a malformed `follow_up_issue` candidate and expected rejection.
- Added a focused unit test for route-hint lane rejection.
- Updated proposal benchmark aggregate expectations for the added rejected fixture candidate.

## Validation

Command:

```powershell
uv run pytest tests/test_proposal_eval.py tests/test_github_growth.py -q
```

Result: passed, 87 tests.

## Review Notes

- Proposals `p1-agent-harness-eval-lane` and `p3-mock-llm-e2e-preflight` remained review-only under their supplied offensive-behavior and privacy-leakage gates.
- This change does not add attack, exploit, malware, phishing, exfiltration, unauthorized-access, token, credential, private-key, private-chat, PII, remote execution, or external provider behavior.
- The guard currently enforces the lane constraint required by this run for `skill_route_discovery`. Provider/config and harness lane enforcement can be tightened separately if future fixtures show a compatible migration path.
