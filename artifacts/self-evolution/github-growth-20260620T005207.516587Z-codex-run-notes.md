# Codex Run Notes: github-growth-20260620T005207.516587Z

## Decision

Selected proposal `p1-skill-route-discovery-index`.

Hypothesis: current `skill_route_discovery` coverage should replay the four
carried skill evidence items together, proving that public skill repositories
map only into documentation, config, test, or code_patch lanes and that
accepted proposal citations use selected `item_id` values instead of repository
URLs or candidate-supplied evidence URLs.

Self-model decision: left `docs/self-model.md` unchanged. The current text
already favors rollback-backed local evolution outside offensive behavior,
abuse, unauthorized access, and privacy leakage, which matches this run's
bounded test-and-doc change.

## Evidence Reviewed

- `https://github.com/baskduf/FableCodex`
- `https://github.com/dongshuyan/compass-skills`
- `https://github.com/majidmanzarpour/threejs-game-skills`
- `https://github.com/xiaoguomeiyitian/threejs-game-skills`

Only repository-level public evidence was used. No upstream code, installers,
skill bodies, scaffolds, browser checks, asset generators, or runtime actions
were imported or executed.

## Rollback

Rollback ref: `refs/blackhole-rollback/20260620T005207Z-skill-route-discovery-pass3`

Rollback artifact:
`artifacts/rollback/20260620T005207Z-skill-route-discovery-pass3.txt`

Recovery is explicit and destructive:

```bash
git switch codex/blackhole-evolve/20260620T005306.377379-add-or-extend-local-tests-that-verify-skill-rout
git reset --hard refs/blackhole-rollback/20260620T005207Z-skill-route-discovery-pass3
```

## Changes

- Added `tests/fixtures/proposal_replay/skill_route_discovery_four_item_lanes.json`.
- Registered the new fixture and evidence URLs in `tests/fixtures/proposal_replay/manifest.json`.
- Extended `tests/test_proposal_eval.py` to assert:
  - the four selected skill item IDs are preserved;
  - accepted proposal kinds are exactly documentation, config, test, and code_patch;
  - accepted refs are selected `item_id` values, not truncated IDs;
  - accepted URLs are derived from selected refs, not supplied by candidates;
  - escaped `follow_up_issue` and candidate-supplied URL expansion are rejected.
- Updated `docs/skill-route-discovery.md` with the current digest and fork-lineage evidence URL.

## Validation

```bash
pytest tests/test_proposal_eval.py -q
```

Result: `19 passed in 0.39s`.

```bash
pytest -q
```

Result: `333 passed in 2.77s`.

## Review Notes

The change remains metadata-only. It does not activate, install, clone, run, or
import external skill repositories. Proposal `p3-agent-harness-trend-fixture`
remains review-only because it is outside this pass's selected skill-route
scope and carries offensive-behavior review constraints.
