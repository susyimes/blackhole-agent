# Self-Evolution Run Notes

- Source digest: `github-growth-20260618T023013.103109Z`
- Prepared branch: `codex/blackhole-evolve/20260618T023145.993477-add-or-extend-local-harness-tests-that-assert-to`
- Rollback ref: `refs/rollback/20260618T023012Z-local-harness-tool-policy`
- Rollback artifact: `artifacts/rollback/20260618T023012Z-local-harness-tool-policy.txt`

## Evidence

Primary evidence used: `https://github.com/omnigent-ai/omnigent/pull/579`.

Reusable lesson: when a governed native tool-call policy hook cannot obtain a usable policy verdict from the policy server, the tool-call gate must fail closed. Advisory or post-tool phases and ungoverned sessions should remain fail-open/no-opinion.

The proposal was marked review-only because it touches offensive-behavior boundary controls. This run did not implement native hook execution behavior. It added local, body-free regression coverage for the harness contract.

## Change

- Added `native_tool_call_policy` as a local harness behavior.
- Added fixture coverage for a governed `PreToolUse` hook failure denying the tool call.
- Added inline regression tests for governed fail-closed behavior plus advisory and ungoverned fail-open behavior.
- Left `docs/self-model.md` unchanged: its current preference for reversible local validation outside offensive/privacy boundaries already matched this run.

## Validation

- `python -m pytest tests/test_harness_eval.py` passed: 22 tests.
- `python -m pytest` passed: 196 tests.
- `python -m ruff check src tests` passed.

## Review Notes

- No offensive, exploit, unauthorized-access, malware, phishing, exfiltration, or privacy-leakage behavior was implemented.
- Fixture inputs include sentinel private values only to assert that raw session IDs and tool arguments are not exported.
