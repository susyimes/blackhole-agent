# Codex Run Notes

Source digest: `github-growth-20260617T104315.629229Z`

Prepared branch:
`codex/blackhole-evolve/20260617T104422.433524-add-or-strengthen-local-tests-for-mock-llm-keyed`

Rollback artifact:
`artifacts/rollback/20260617T104315Z-mock-llm-keyed-queues.txt`

## Evidence Reviewed

- https://github.com/omnigent-ai/omnigent
- https://github.com/omnigent-ai/omnigent/pull/486

The Omnigent PR described keyed mock LLM queues selected by request model with a default fallback. Its review also
called out fallback, reset, and concurrency risks. I treated that as evidence for strengthening this repository's local
mock LLM harness evaluator around deterministic per-key response consumption.

## Chosen Improvement

Implemented keyed mock LLM response queues for `mock_llm_workflow_route` while preserving the existing
`mock_llm.responses` list as the default queue. Workflow steps can now select queues with `model` or `response_key`.
When no matching key has responses, the evaluator consumes the `default` queue if available; otherwise the workflow is
reported as exhausted before any provider call path is enabled.

## Self-Model Decision

Left `docs/self-model.md` unchanged. Its current preference already matches this run: prefer locally validated behavior
changes over validation-report-only scaffolding when rollback and narrow tests cover the change.

## Validation

- `python -m pytest tests/test_harness_eval.py` passed: 11 tests.
- `python -m ruff check src\blackhole_agent\harness_eval.py tests\test_harness_eval.py` passed.
- `git diff --check` passed with only existing line-ending normalization warnings from Git.

## Review Notes

This is a local deterministic evaluator, not an async mock server. It does not attempt to solve server-side locking or
in-flight reset races from the upstream review; those would require a real server implementation path if added later.
