# Codex Run Notes

- Source digest: `github-growth-20260619T153207.041693Z`
- Capability window: `runner-harness-control`, pass 3 of 4
- Branch: `codex/blackhole-evolve/20260619T153301.360144-add-or-update-a-local-validation-lane-that-evalu`
- Rollback ref: `refs/blackhole-agent/rollback/github-growth-20260619T153207.041693Z`
- Rollback artifact: `artifacts/rollback/20260619T153207Z-runner-harness-control-pass3.txt`

## Evidence And Hypothesis

Reviewed the carried evidence URLs only:

- `https://github.com/omnigent-ai/omnigent` describes a meta-harness for multiple agent harnesses with shared policies, sandboxing, and model selection.
- `https://github.com/visa/visa-vulnerability-agentic-harness` remains security-adjacent, so offensive or abuse-enabling harness behavior stays review-only.
- `https://github.com/dongshuyan/compass-skills` supports the existing bounded skill-route discovery direction but did not displace this pass's runner-control focus.

Hypothesis: the local harness control plane should block provider launches when model command metadata is missing or malformed, and should report a body-free recovery hint. This advances intake/preflight, recovery, replay fixture, and report artifacts without running external harnesses.

## Change

- Added `provider_model_command` validation to `provider_runtime_preflight`.
- Missing required model commands now fail with `provider_model_command_missing`.
- Malformed configured commands now fail with `provider_model_command_malformed`.
- Valid configured commands are summarized by argument count and hashes only.
- `provider_runtime_recovery_summary` now emits a provider-model-command recovery hint.
- Added a replay fixture for Omnigent-style missing model command configuration.

## Safety And Review Notes

- No external harness execution was added.
- Raw model command values are not exported; configured command parts are hashed.
- The Visa harness proposal remains review-only for offensive-behavior boundary reasons.
- The self-model was read and left unchanged because it already matched the run's autonomy and validation policy, and this run produced a concrete behavior improvement.

## Validation

- `uv run pytest tests/test_harness_eval.py -q -k provider_runtime` -> passed (`11 passed, 69 deselected`)
- `uv run pytest tests/test_harness_eval.py -q -k local_harness_eval_runs_pass_and_fail_fixtures` -> passed (`1 passed, 79 deselected`)
- `uv run pytest tests/test_harness_eval.py -q` -> passed (`80 passed`)
- `uv run pytest -q` -> passed (`323 passed`)

