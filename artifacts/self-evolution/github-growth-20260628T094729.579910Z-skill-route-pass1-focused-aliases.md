# Skill Route Discovery Pass 1 Focused Proposal Aliases

- Source digest: `github-growth-20260628T094729.579910Z`
- Capability theme: `skill-route-discovery`
- Branch: `codex/blackhole-evolve/20260628T094830.403310-add-or-run-a-local-skill-route-discovery-validat`
- Rollback artifact: `artifacts/rollback/20260628T094728Z-skill-route-discovery-pass1.md`
- Rollback ref: `refs/rollback/20260628T094728Z-skill-route-discovery-pass1`

## Evidence

The carried repositories are public skill workflow signals:

- `lyra81604/zhengxi-views` exposes a public agent skill with `SKILL.md`, traceable source-citation framing, validation scripts, and explicit advisory boundaries.
- `majidmanzarpour/threejs-game-skills` exposes a Three.js/browser-game skill workflow with QA, scaffold, UI, gameplay, and optional asset workflow pressure.
- `dongshuyan/compass-skills` exposes a skill ecosystem/state-handoff shape with profiles, task memory, handoff, and skill metadata pressure.

These are route-discovery evidence only. They do not justify installing, enabling, cloning and running, launching providers, or writing profile or memory state.

## Hypothesis

The current pass-1 proposal names should be visible to the local replay lane even when older stable case IDs remain in use. Adding proposal aliases lets the controller match this wake's p1-p3 proposals while preserving prior fixture compatibility.

## Change

`current_pass_validation_cases` now emits `proposal_aliases` per row and a top-level `proposal_alias_ids` list. A new fixture replays the exact current digest profile set and confirms unsupported `install`, `provider_runtime`, and `runtime_execution` pressure is not present in emitted validation cases.

## Self-Model Decision

Read `docs/self-model.md` before selecting the change. It was left unchanged. Its current preference for rollback-backed, locally validated behavior changes matches this run, and no evidence showed the file is misleading or behavior-shaping enough to require revision.

## Validation

Local validation passed:

```powershell
python -m pytest tests/test_skill_routing.py -q -k current_pass1_aliases
python -m pytest tests/test_skill_routing.py -q -k current_pass_validation_cases
python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery
python -m pytest tests/test_skill_routing.py -q
```

## Review Notes

The alias field is replay metadata only. Runtime action, upstream skill activation, external harness execution, provider launch, profile writes, memory writes, remote execution, raw source URL export, raw target path export, and upstream body export remain denied by the lane contract.
