# Self-Evolution Run Notes

Source digest: `github-growth-20260619T003207.232172Z`

Rollback ref: `refs/blackhole-agent/rollback/20260619T003207Z/da648515002e`

## Evidence Reviewed

- `https://github.com/omnigent-ai/omnigent` presents Omnigent as a multi-agent meta-harness with policies, sandboxing, approval gates, and cloud sandbox options.
- `https://github.com/omnigent-ai/omnigent/pull/500#pullrequestreview-4529338631` exposed a low-detail PR/review page in this run; the visible page did not provide an implementation finding suitable for direct local adoption.
- `https://github.com/omnigent-ai/omnigent/pull/508#pullrequestreview-4529334260` exposed a concrete PR body: fresh E2E policy ALLOW responses, a regression test, and focused validation commands.

## Hypothesis

Low-detail PR/review/push clusters are useful activity evidence, but future proposals should not treat them as implementation evidence unless cached digest metadata confirms at least branch, merge timing, or touched subsystem. A deterministic local triage summary can keep that uncertainty visible and make proposal preflight reject under-confirmed upstream movement.

## Change

- Added `upstream_movement_triage` digest metadata that groups PR, review, and push items by branch, merge timing, and inferred subsystem when those details are available.
- Added per-proposal `upstream_movement_evidence` for PR/review/push-derived heuristic proposals.
- Added a proposal preflight validation gap, `missing_upstream_movement_confirmation`, when local-validation candidates depend on low-detail upstream movement.
- Added regression tests for digest grouping and proposal preflight behavior.

## Self-Model Decision

`docs/self-model.md` was read and left unchanged. Its current preference already says broad local changes are allowed when rollback-backed and validated, and this run did not produce new evidence that the self-description is behavior-shaping beyond the external task policy and tests.

## Validation

- `uv run python -c "import blackhole_agent.github_growth"` passed.
- `uv run pytest tests/test_github_growth.py -q -k "upstream_movement or push_commit_patterns"` passed: 4 passed, 77 deselected.
- `uv run pytest tests/test_github_growth.py -q` passed: 81 passed.

## Review Notes

- No remote mutation, restart, push, promotion, credential access, or runtime capability expansion was performed.
- The subsystem classifier is intentionally conservative and string-based. It is suitable for local preflight metadata, not authoritative upstream interpretation.
- Low-detail review anchors remain follow-up evidence until an inspected PR body, diff, commit, test result, or local failing test confirms a specific behavior.
