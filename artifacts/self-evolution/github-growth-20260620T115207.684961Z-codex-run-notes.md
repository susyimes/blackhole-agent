# Codex Run Notes

Source digest: `github-growth-20260620T115207.684961Z`
Capability window: `skill-route-discovery`, pass 3 of 4
Branch: `codex/blackhole-evolve/20260620T115325.868993-add-or-extend-local-skill-route-discovery-fixtur`

## Rollback

- Artifact: `artifacts/rollback/20260620T115207Z-skill-route-discovery-pass3.txt`
- Ref: `refs/rollback/20260620T115207Z-skill-route-discovery-pass3`
- Original HEAD: `7b3c71b494f859550a6eeac542bc16c06c1e14f9`

Explicit operator-approved recovery:

```bash
git switch codex/blackhole-evolve/20260620T115325.868993-add-or-extend-local-skill-route-discovery-fixtur
git reset --hard refs/rollback/20260620T115207Z-skill-route-discovery-pass3
```

## Evidence Reviewed

- `https://github.com/dongshuyan/compass-skills`: treated as installable local-skill and state/workflow evidence, not an installation signal.
- `https://github.com/majidmanzarpour/threejs-game-skills`: treated as domain director, asset, and QA helper routing evidence, not an execution or scaffold signal.
- `https://github.com/baskduf/FableCodex`: treated as Codex workflow-package evidence for bounded local lanes.

## Hypothesis

The skill-route discovery lane already exposes bounded lanes, artifact proofs,
and a final handoff. The next useful operator-visible behavior is an ordered
activation sequence that tells supervisors which local preactivation checks must
be replayed before any bounded documentation, config, test, or code_patch lane
is promoted.

## Changes

- Added `activation_sequence` to `skill_route_discovery_activation_manifest`.
- Added `skill_route_discovery_activation_sequence()` as a body-free ordered
  replay checklist for source lineage, bounded lanes, artifact proof, local
  validation, provider-runtime preflight replay, and supervisor handoff.
- Added `activation_sequence_status` to `completion_handoff`.
- Updated focused harness assertions and documentation contract coverage.
- Documented the sequence in `docs/skill-route-discovery.md`.

## Validation

- `python -m pytest tests/test_harness_eval.py -q -k skill_route_discovery_lane` -> passed, 9 passed.
- `python -m pytest tests/test_docs_contracts.py -q -k skill_route_discovery` -> passed, 2 passed.
- `python -m pytest tests/test_skill_routing.py tests/test_proposal_eval.py -q -k skill_route_discovery` -> passed, 17 passed.
- `python -m pytest tests/test_harness_eval.py tests/test_skill_routing.py tests/test_proposal_eval.py tests/test_docs_contracts.py -q -k skill_route_discovery` -> passed, 37 passed.

## Review Notes

- Self-model read and left unchanged. It already says local evolution should be
  rollback-backed, locally validated, and explicit about uncertainty; this run
  needed a controller-visible behavior improvement, not another preference note.
- The new sequence is replay metadata only. It does not install, clone, execute,
  scaffold, generate assets, launch providers, run external harnesses, promote
  remote execution, export raw evidence/source URLs, export raw target paths, or
  expose upstream skill bodies.

