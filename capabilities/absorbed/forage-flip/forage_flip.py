"""Uncooperative fixture the forage-growth plane must match unaided.

Ships no manifest, no spec, and no probe cases. Trend ranking prefers a
more popular decoy; only inferred callable behavior covers ``flip_output``.
"""


def flip(text):
    return f"flipped:{text}"
